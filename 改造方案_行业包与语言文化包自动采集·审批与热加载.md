# 技术方案 · 行业包与语言文化包自动采集 · 审批与热加载 + 硬闸打回重译

> 版本 v1.0 ｜ 2026-09-01 ｜ 决策人：项目所有者 ｜ 状态：🕐 设计定稿，待实施
>
> 需求拍板：
> ① 数据源 = **混合模式**（官方开放术语 API + 受限网页抓取 + LLM 生成），按 tier 标注可信度，超管审批把关
> ② 硬闸二次过滤失败 → **自动带 KB 提示词打回重译，上限 8 次**（`gate_retry_max=8`）
> ③ 行业包决策周期天数（汽车30天/房产60-90天/企业服务30-180天/教育15-30天/跨境高客单3-15天/婚庆30-60天）：**仅作背景信息，不参与系统逻辑**
> ④ 抓取节奏 = **首日铺底 + 每日增量**
> ⑤ 语言覆盖 = **全平台支持语言**（`kb.AllLangs` 16 列语言）
> ⑥ 进程形态 = **后端内嵌每日任务**（沿用 watchdog ticker 模式，无新增部署组件）
> ⑦ 调度 = **纯低占用驱动 + 断点续传**：监测到低占用开始，占用提升即暂停，中间可续传

---

## 一、目标语义（定稿）

自动从网上采集行业包与语言文化包数据，形成**增量待审池**，超管审批通过后**热加载**进正式知识库生效；同时在翻译流程中强化「硬闸二次过滤」——检出未用知识库或不符合语言文化/行业习惯的译文时，**附上对应 KB 提示词自动打回重译（≤8 次）**。

数据使用链路（复用现有 KB 包体系，不重写检索链）：

```
用户翻译请求
  → 组装 KB 可见范围 PackScope（按组织祖先链 + 租户行业 + 平台共享层）
  → 命中查询顺序（现有 Rank 语义，无需改动）：
     部门包(链内,距离0..N) → 跨部门(100,仅例句) → 企业包(200)
     → 行业包(300) → 语言文化包(400)
  → 命中注入 prompt（术语/句对例句 + 语言文化规范块）
  → AI 初翻/审校 → Gate 8 项硬校验 → 语言文化闸门
  → 硬闸二次过滤：未用 KB / 违反规范 → 带提示重译（≤8 次）→ 仍失败置 rejected 附原因
```

---

## 二、现状盘点（约 70% 已存在，本方案只新建缺失三块）

| 诉求 | 现状 | 位置 |
|---|---|---|
| 部门包→跨部门包→企业包→行业包→语言文化包 | ✅ 已实现（Rank：链内0..N/跨部门100/企业200/行业300/文化400）+ 组织隔离 | `kb/scope.go`、`store/kbpackages.go` |
| 语言文化包避雷（forbidden/替换/风格规范） | ✅ `kb_safety_phrases`（style/forbidden/replace），注入 prompt + 译文反查 | `engine.go:1780`、`culture/culture.go` |
| 8 项硬闸（漏译/乱码/非目标语/数字/长度/回环/残留/词面） | ✅ | `gate/gate.go` |
| KB 提示注入 prompt（术语/句对/规范块） | ✅ `buildExamplesPrompt` + `cultureRules` | `engine.go:891`、`engine.go:1780` |
| 超管审核后生效 | ✅ 安全句 pending→approved；TM 待审池 | `admin_kb.go:623`、`tmreview.go` |
| 包启停热加载（重写 tm_segments + 缓存失效） | ✅ | `admin_kb.go:504` |
| **数据采集引擎（扒数据）** | ❌ 全仓无网络抓取实现 | **本方案新建** |
| **每日增量包 → 批量审批 → 热加载闭环** | ❌ 仅有人工/LLM 投喂入口 | **本方案新建** |
| **硬闸二次过滤 + 打回重译（未用KB/违反规范）** | ❌ 现失败直接置 rejected，无自动重译 | **本方案新建** |

---

## 三、数据模型变更（幂等迁移）

### 3.1 新增表

```sql
-- 数据源管理（后台可启停/限频/看状态）
CREATE TABLE IF NOT EXISTS kb_pack_sources (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  kind TEXT NOT NULL,              -- official_api | limited_web | llm_gen
  name TEXT NOT NULL,              -- 源名称（如 维基词典API / 某汽车行业术语站）
  base_url TEXT DEFAULT '',        -- 入口 URL（llm_gen 可为空）
  lang TEXT DEFAULT '',            -- 目标语言（空=不限，按行业/全量）
  industry TEXT DEFAULT '',        -- 行业 code（空=不限/语言文化包）
  pack_type TEXT DEFAULT 'locale', -- industry | locale
  enabled INTEGER DEFAULT 1,       -- 1=启用参与采集 0=停用
  freq_hours INTEGER DEFAULT 24,   -- 采集频次（小时）
  tier INTEGER DEFAULT 3,          -- 数据可信度：1官方 / 2受限抓取 / 3LLM
  last_run_at TEXT DEFAULT '',     -- 最近一次采集时间
  last_status TEXT DEFAULT '',     -- 最近状态：ok / error:<msg>
  created_at TEXT DEFAULT ''
);

-- 增量待审池（条目：行业/语言文化的术语、TM、AI碎片）
CREATE TABLE IF NOT EXISTS kb_staged_entries (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  target_pack_id INTEGER NOT NULL,  -- 审批通过后落入的正式包 ID
  pack_type TEXT NOT NULL,          -- industry | locale
  source_id INTEGER DEFAULT 0,      -- 来自哪个数据源
  tier INTEGER DEFAULT 3,           -- 1官方 / 2受限抓取 / 3LLM
  layer INTEGER DEFAULT 1,          -- 1术语/2TM/3安全句/4碎片（复用 kb_entries 层契约）
  src_lang TEXT NOT NULL,
  src_text TEXT NOT NULL,
  tgt_lang TEXT NOT NULL,
  tgt_text TEXT NOT NULL,
  source_url TEXT DEFAULT '',       -- 出处（抓取/API 溯源；LLM 为提示描述）
  src_hash TEXT NOT NULL,           -- md5(src_lang|src_text|tgt_lang|tgt_text) 去重键
  status TEXT DEFAULT 'pending',    -- pending / approved / rejected
  created_at TEXT DEFAULT '',
  applied_at TEXT DEFAULT ''
);
CREATE INDEX IF NOT EXISTS idx_staged_status ON kb_staged_entries(status, pack_type, id DESC);

-- 增量待审池（语言文化安全句）
CREATE TABLE IF NOT EXISTS kb_staged_phrases (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  package_id INTEGER NOT NULL,      -- 审批后落入的语言文化包 ID
  lang TEXT NOT NULL,
  phrase TEXT NOT NULL,
  kind TEXT DEFAULT 'style',        -- style / forbidden / replace
  replacement TEXT DEFAULT '',
  tier INTEGER DEFAULT 3,
  src_hash TEXT NOT NULL,           -- md5(lang|kind|phrase|replacement) 去重键
  status TEXT DEFAULT 'pending',
  created_at TEXT DEFAULT '',
  applied_at TEXT DEFAULT ''
);
CREATE INDEX IF NOT EXISTS idx_staged_phr_status ON kb_staged_phrases(status, lang, id DESC);
```

### 3.2 既有表小改

- `kb_safety_phrases.source` 取值扩展：`manual / llm / scrape_api / scrape_web`（现仅 manual/llm）。
- `kb_entries.module` 复用现有模块标识（如 `scrape_api:<source_id>`），无需加列。

### 3.3 checkpoint（断点续传）

- 复用 `system_config` 键值对（无需新表）：
  - `kb_scrape_checkpoint_<date>_<source_id>` = 游标（如抓取页 offset / 已处理条目数）
  - `kb_scrape_daily_marker` = `YYYY-MM-DD`（当日全部源完成标记）
  - `kb_scrape_daily_source_done_<date>_<source_id>` = `1`

---

## 四、数据采集引擎（新建 `backend-go/internal/crawler/`）

### 4.1 分层数据源（tier 可信度）

| tier | 类型 | 说明 | 合规/风险 |
|---|---|---|---|
| 1 | `official_api` | 有开放许可的多语术语源（维基词典 API、政府/国际组织多语术语库、ISO 语言规范等） | 风险最低，质量最高 |
| 2 | `limited_web` | 行业垂直站/电商/政府网站的术语页受限抓取：遵守 `robots.txt` + 限频 + 超时 + UA 标识；只取术语表类结构化内容 | 需谨慎，内容入待审池由超管把关 |
| 3 | `llm_gen` | 按 行业×语言 批量生成规范/术语/避雷词，与 tier-1 交叉校验去噪后才入池 | 生成快，审核把关强 |

### 4.2 包内模块

```
backend-go/internal/crawler/
  crawler.go     // 包入口：RunOnce(ctx, opts) / RunSource(ctx, src)
  types.go       // NormalizedEntry / NormalizedPhrase / SourceCtl 等统一产出结构
  source.go      // 数据源表读写 + 启停/限频/状态维护
  fetch.go       // HTTP 抓取基座：robots.txt 尊重、限频、重试、UA、超时
  tier1_api.go   // 官方 API 适配器（按源注册）
  tier2_web.go   // 受限网页抓取适配器（select 术语表节点 → 结构化）
  tier3_llm.go   // LLM 批量生成器（含交叉校验去噪）
  dedup.go       // hash 去重（写入待审池 ON CONFLICT 跳过）
  checkpoint.go  // 断点续传读写（system_config 封装）
  load.go        // 负载判定（调用方注入阈值，见 §五）
```

### 4.3 产出归一化

```go
type NormalizedEntry struct {
  Layer      int    // 1术语/2TM/3安全句/4碎片
  SrcLang    string
  SrcText    string
  TgtLang    string
  TgtText    string
  Tier       int    // 1/2/3
  SourceURL  string
  PackType   string // industry / locale
}

type NormalizedPhrase struct {
  Lang        string
  Phrase      string
  Kind        string // style / forbidden / replace
  Replacement string
  Tier        int
}
```

写入待审池：`INSERT ... ON CONFLICT(src_hash) DO NOTHING`，已存在即跳过（跨日幂等）。

---

## 五、低占用调度 + 断点续传（内嵌后端）

### 5.1 状态机

```
idle(等待低占用) ──低占用且还有未完成源──→ running(采集)
running ──负载上升──→ paused(暂停，存checkpoint) ──低占用──→ resume(续传)
running ──当日全部源完成──→ 标记 kb_scrape_daily_marker=date → idle(次日)
```

### 5.2 负载判定（复用现有采样手段）

每 5 分钟探测（`watchdog.go` / `memmonitor.go` 已有 ticker 模式），全部满足即「低占用」：

| 指标 | 判定 | 配置键 | 默认 |
|---|---|---|---|
| 进行中翻译任务 | 在线队列/工单执行中计数 = 0 | — | — |
| LLM 错误率 | 近 5 分钟 `Engine.ErrorRate()` < 阈值 | `scrape_idle_err_rate` | 0.05 |
| 内存水位 | RSS < 阈值 | `scrape_idle_mem_mb` | 600MB |

### 5.3 采集块（chunk）粒度

- 每块 = 一个数据源的 ≤100 条产出（`crawler.ChunkSize` 可配）。
- 块间与条目间检测负载：占用越界 → 暂停当前 goroutine（pause channel / ctx cancel）→ 保存 checkpoint → 回 idle。
- 每块完成后**原子更新 checkpoint**（写 `system_config`），保证崩溃/重启可续。

### 5.4 断点续传

- 进程重启后：读 `kb_scrape_checkpoint_<date>_<source_id>` 与 `kb_scrape_daily_marker`。
- 未完成源从游标续传；已完成源跳过（配合 `src_hash` 去重天然幂等）。
- 当日全部启用源完成 → 写 `kb_scrape_daily_marker=YYYY-MM-DD`，次日自动开始新一轮。

### 5.5 首日铺底

- checkpoint 为空 → 从全量开始（同一机制，无需特判）。
- 或超管在后台「数据源」面板手动点「立即采集一轮」（忽略负载自适应跑）。

### 5.6 入口接线

- `api/watchdog.go` 新增 goroutine：`s.startPackScraper()`（24h 长驻，内部 5min 探测）。
- 失败写 `kb_pack_sources.last_status` + 审计；通知超管（复用 `notifications.go`）。

---

## 六、审批 + 热加载

### 6.1 后端接口（`api/admin_kb.go` 扩展，超管 L4）

```
GET    /api/admin/kb-scrape/sources           数据源列表
POST   /api/admin/kb-scrape/sources           新增/更新数据源
POST   /api/admin/kb-scrape/sources/status    启停数据源
POST   /api/admin/kb-scrape/sources/run       手动立即采集一轮
GET    /api/admin/kb-scrape/staged            待审池列表（按包/类型/状态/语言/源筛选，分页）
POST   /api/admin/kb-scrape/approve           批量通过/驳回（body: ids[]、action=approve|reject）
GET    /api/admin/kb-scrape/checkpoints       断点/进度查看（运营视图）
```

### 6.2 审批通过 → 原子应用 + 热加载

| 内容 | 应用方式 | 热加载 |
|---|---|---|
| 行业/语言文化**条目**（L1术语/L2TM/L4碎片） | `SaveEntry(target_pack_id, ...)` → 重写 `tm_segments`（按包类型优先级+宿主租户） | `invKB()` 失效 CJK 缓存；若为向量包触发 `rebuildIndexAsync()` |
| 语言文化**安全句** | `SaveSafetyPhraseEx(...)` 落正式库（tier 写入 source） | `cultureRules` 60s TTL 缓存自动刷新，无需额外操作 |
| 审批后 | 更新待审池 `status=approved, applied_at`；写审计；发通知 | — |

- 逐条事务应用，失败回滚该批；`src_hash` 保证同一待审项不会重复落正式库。
- 驳回：仅置 `status=rejected`，不落正式库。

### 6.3 前端（`frontend-react`）

- `AdminDashboard.tsx` 菜单新增 `dataSources`（`admin.menuDataSources`，minLevel=4 超管）。
- 新面板 `components/admin/DataSourcesP.tsx`：
  - 数据源 CRUD + 启停 + 「立即采集一轮」；
  - 「待审增量」Tab：tier 徽标（1官方/2受限/3LLM）、来源 URL、源语言/目标语言、批量通过/驳回按钮、进度/checkpoint 视图。
- `panels_d.tsx` 的 `KbP` 内可加「行业/语言包待审」入口或直接并入新面板。
- `i18n/dicts.zh.ts` / `dicts.en.ts` 增补文案。

---

## 七、硬闸二次过滤 + 打回重译（≤8 次）

### 7.1 现状问题

`gate` / `culture_gate` 失败 → `flow.go:181` 直接置工单 `rejected`，无自动重译、不附 KB 提示。

### 7.2 改造（`workflow.go` / `engine.go`）

1. `runGate` / `runCultureGate` 失败时**不再直接返回错误**，转入重译循环：
   - 组装修正提示：
     - Gate 8 项中失败项明细（如「数字保持失败」「残留源语言」）
     - 命中的避雷词 / 替换规范（`cultureRules` 返回的规则）
     - 该语言可用的 KB 示例与语言文化规范块（复用 `buildExamplesPrompt` + `cultureRules`）
2. 复用 `TranslateWithFeedback`（engine.go:774）带修正提示重译该语言 → 重跑 gate/culture 校验。
   - 通过 → 用新译文覆盖 `p.Translations[lc]` 并继续流程；
   - 不通过 → 循环直至 `retry_count >= gate_retry_max`（默认 8）。
3. `ticketPayload` 新增字段：
   ```go
   RetryCount map[string]int    `json:"retry_count,omitempty"` // 语言 → 已重试次数
   GateHints  map[string]string `json:"gate_hints,omitempty"`  // 语言 → 最近一次失败原因（附审批）
   ```
4. 仍失败（达上限）：置 `rejected`，`RejectReason` 附全部失败原因 + 已注入的 KB 提示（供审批人工判断）。
5. 护栏：每语言独立计数；`system_config.gate_retry_max`（默认 8）可调；「无上限」语义 = 不按次数拒绝，仅防死循环烧算力。
6. fast 模式：仍跳过 gate/culture（现状保持，重译循环只作用于 pro 流程）。

---

## 八、配置项汇总（`system_config`）

| 键 | 默认 | 说明 |
|---|---|---|
| `gate_retry_max` | 8 | 硬闸打回重译每语言上限 |
| `scrape_chunk_size` | 100 | 采集块大小（条） |
| `scrape_idle_mem_mb` | 600 | 低占用判定 RSS 水位（MB） |
| `scrape_idle_err_rate` | 0.05 | 低占用判定 LLM 错误率阈值 |
| `scrape_poll_sec` | 300 | 负载探测周期（秒） |
| `scrape_seed_once` | 1 | 首次部署是否自动铺底全量 |
| `kb_scrape_daily_marker` | — | 当日完成标记（内部） |
| `kb_scrape_checkpoint_*` | — | 断点游标（内部） |

---

## 九、实施步骤

| 阶段 | 内容 | 验收 |
|---|---|---|
| M0 | 数据模型 + 幂等迁移（3 新表 + source 扩展 + checkpoint） | `go build` 过；迁移幂等重跑 |
| M1 | crawler 包：types/fetch/dedup/tier1/tier2/tier3/checkpoint | 单测：去重、归一化、断点读写 |
| M2 | 低占用调度：watchdog 接线 + 负载判定 + 首日铺底 + 通知 | 空转不采集；伪造负载可暂停/续传 |
| M3 | 审批/热加载 API + 前端 DataSourcesP 面板 | 待审→通过→命中生效；驳回不动正式库 |
| M4 | 硬闸打回重译（workflow/engine + payload + 护栏） | 构造违规译文 → 重译≤8次 → 通过或附原因 rejected |
| M5 | 端到端验证 + `go test ./internal/api/ -run "KB|Scrape|Gate"` + `npm run typecheck` | 全绿 |
| M6 | 文档：部署指南 / ROX 手册补采集器与护栏配置 | 文档同步 |

---

## 十、验证清单

1. 首日铺底：空库跑一轮 → 待审池有行业+语言文化数据，来源 tier/URL 正确，无重复（hash）。
2. 断点续传：采集中断（高负载暂停/杀进程）→ 重启续传不重做已完成源。
3. 审批热加载：通过语言文化 forbid 词 → 触发翻译命中该词即打回；通过行业术语 → 精确/语义命中生效；驳回项不落正式库。
4. 硬闸重译：构造「漏数字 / 残留中文 / 命中避雷词」译文 → 自动带提示重译 ≤8 次 → 通过或 rejected 附原因与已用 KB 提示。
5. 调度：低占用才跑、占用上升即暂停、每日一轮标记、次日新一轮。
6. 权限：待审/审批仅超管（L4）；数据源管理仅超管；非超管不可见。

---

## 十一、遗留 / 待深化

- **tier-2 抓取站点清单**：具体源 URL 需在实施期由超管按行业/语言逐步补充（先以 tier-1 + tier-3 打底）。
- **LLM 生成预算**：每行业×语言×日条数上限建议再加 `system_config.scrape_llm_daily_cap`（默认如 2000 条）防成本失控。
- **向量包索引**：行业包如启用语义检索，审批热加载后触发异步重建（已有 `rebuildIndexAsync`），大批量时注意时序。
