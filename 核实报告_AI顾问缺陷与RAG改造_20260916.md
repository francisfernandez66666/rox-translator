# 核实报告 · AI 顾问缺陷逐条验证与 RAG 改造方案（2026-09-16）

> 验证方式：全部结论基于**真实代码阅读 + 本机实测复现**，非文档转抄。
> 测试环境：macOS arm64 / PostgreSQL 15.18 + pgvector / Go / Node 20。
> 本轮实际执行：`go test ./...` 22 包全绿（含 UAT 闸门内 `-race` 全量）、UAT 主矩阵 API 67/67、交易专项 308/308、双实例 e2e 8/8、vitest 79/79、ai-assist go test 4 包全绿、ai-assist 手工端到端 22 场景、前后端 API 契约 176↔251 路径逐条比对。
> 结论中标注【属实】/【属实但修正】/【不成立】，附代码证据（文件:行号）。

---

## 一、执行摘要

上一轮评审给出的 P0×1 / P1×2 / P2×3 逐条核实：**全部属实**（其中 P2「欢迎词刷屏」需修正表述）。
新发现一个用户已感知的核心问题：**AI 顾问从不调用 LLM，永远机械回复**。根因三层，已字节级复现，并给出 RAG 改造分期方案（§四）。

---

## 二、P0/P1/P2 逐条真实性核实

### P0-1 发布闸门被 autosales 新功能打红 ——【属实，双红已复跑确认】

- 证据 A（前端菜单确为 9 项）：`frontend-react/src/components/admin/AdminDashboard.tsx:48-62`，`ITEMS` 数组 9 个元素，第 61 行 `{ key: 'assist', label: 'admin.menuAssist', minLevel: 3 }`。
- 证据 B（e2e 硬断言 8 项）：`frontend-react/e2e/admin_tabs_lang.spec.ts:34` `expect(n, '一级菜单数=${n}，应收敛为 8').toBe(8)`。
- 证据 C（实测双红）：UAT 全量日志（tmp.yk07DIwEd3）`pixel_uat.log` 首轮失败 `Error: 一级菜单数=9，应收敛为 8`，`pixel_uat_retry.log` 复跑仍红 —— 非环境 flaky，真回归。
- 判定：autosales 批次加菜单时未同步 e2e 断言。README「Playwright 33 全绿」在当前分支已不成立。
- 修复：spec 断言 8→9（或改为「≥既有 8 项核心菜单且 key 集合包含 assist」），并补挂件 e2e（见 §三-6）。

### P1-2 挂件在本地开发 / macOS 桌面形态必然离线 ——【属实】

- 证据 A：`start.sh`、`build.sh` 全文 0 处 `assist`（实测 `grep -c "assist"` = 0），两脚本均不启动 ai-assist（:8790）。
- 证据 B：Go 主后端源码（backend-go，335 个 .go 文件）0 处 `/assist-api` 路由或反代逻辑（唯一 1 处 "assist" 为 `internal/api/public.go:261` 英文协议文案 "human-assisted"，无关）。
- 证据 C：前端请求基址 `frontend-react/src/api/assist.ts:8` `const ASSIST_API = import.meta.env.VITE_ASSIST_API || '/assist-api'` —— 同源路径，无反代即 404。
- 证据 D：`AiAssist.tsx:81-84` 请求失败即置 `offline`，显示「助手暂时联系不上（服务未启动）」。
- 判定：生产形态（Caddy `deploy/caddy/translator.conf:55-56` + `deploy/systemd/ai-assist.service` + 部署指南 §十三）接线完整可用；**缺口仅在 `start.sh -f` 开发形态与 `.app` 桌面形态**——两者挂件必离线，而桌面版是 README 宣传卖点。
- 修复：start.sh/build.sh 增加可选拉起 ai-assist（检测二进制存在则 `ASSIST_MOCK=1` 起服务）；或桌面版探测 `/assist-api/health` 失败时直接隐藏 FAB 按钮。

### P1-3 ai-assist 安全三连 ——【属实，三条独立核实】

1. **会话 ID 可预测**：`ai-assist/internal/api/server.go:443-453` `randHex` 用 LCG 线性同余生成器，种子 `time.Now().UnixNano() ^ pid<<20`，非 `crypto/rand`（全仓 `grep crypto/rand` = 0）。会话 ID 形如 `s<毫秒时间戳><8hex>`，可枚举猜测；C 端免鉴权（README 自述「会话 ID 自证」），`/api/assist/history?session=` 可读取他人对话。**隐私泄露面**。
2. **chat 无速率限制**：`ai-assist/internal/api/server.go:35-61` `Handler()` 全部路由无任何限流中间件；`grep -rn "ratelimit|limiter" ai-assist/` = 0。C 端免登录直通 LLM，一旦配置 `ASSIST_LLM_API_KEY`，公网可被刷爆上游 token 账单。**资金面**。
3. **管理 token 支持 query 传参**：`server.go:97-100` `tok = r.Header.Get("X-Assist-Admin")` 为空时回落 `r.URL.Query().Get("admin_token")`。token 进访问日志、浏览器历史、Referer。**凭证泄露面**。
- 修复：① session ID 改 `crypto/rand` 16 字节 hex；② C 端按 IP+session 令牌桶限流（主仓已有 `internal/infra/ratelimit` 可移植）；③ 删除 query 传参通道，管理台 HTML 改 header 传 token。

### P2-4 CORS 默认 `*` 回显任意 Origin ——【属实，已实测】

- 实测：`curl -X OPTIONS -H 'Origin: https://evil.example.com'` → `204` 且回显 `Access-Control-Allow-Origin: https://evil.example.com`（`server.go:68-91` corsMW，`ASSIST_CORS` 默认 `*` 时反射任意来源）。
- 影响：任意第三方页面可跨域调用 C 端接口（配合 P1-3-1 可脚本化枚举会话）。管理端因需要自定义头 `X-Assist-Admin`，预检会被拦，风险主要在 C 端数据读取。
- 修复：生产默认改为域名白名单（`ASSIST_CORS=https://主站域名`），`*` 仅限本地演示。

### P2-5 挂件 UI 细节 ——【属实但修正一处表述】

1. **暗色模式反色**【属实】：`AiAssist.tsx:139/150/156` 内联样式硬编码 `background:#fff`（面板/AI 气泡/chips 条）。后台暗色 2026-09-15 已全量适配（commit 5493e9f），挂件是漏网点。修复：改 `--adm-*` / `--td-*` 语义令牌。
2. **history 分支丢失 chips**【属实，本轮新发现】：`AiAssist.tsx:59-71` 命中历史回显分支时直接 `return`，**不调 `assistGreet`、不 `setChips`** —— 老用户刷新后快捷提问按钮永远消失（chips 只在全新会话分支 74-78 行设置）。且该分支也未拉 `/api/assist/features` 做快捷入口行。
3. **欢迎词落库**【属实但需修正表述】：`server.go:151` `handleGreeting` 每次调用无条件 `AddMessage(sid,"assistant",text)`。前端因 `initedRef`（`AiAssist.tsx:53-56`）+ 历史回显优先（59-71 行），**同一浏览器常态只落 1 条**；重复场景为：清缓存/隐身模式反复刷新、`send` 内 sid 为空时的二次 greet（97-104 行）。修正后的说法：**欢迎词会随「无历史会话的每次进入」重复落库**，管理台会话统计（msg_count）被欢迎词虚增。修复：greeting 改为 `INSERT OR IGNORE`（按 session 首条去重）或前端仅在新 session 时调用。

### P2-6 e2e 无 AI 挂件覆盖 ——【属实】

- `frontend-react/e2e/` 10 个 spec（a11y_errors/admin_tabs_lang/dark_admin_upload/mobile_uat/multilang_zip/pixel_quality/pixel_uat/smoke/tenant_label/usdt_payment）`grep -l "na-fab|assist"` = 0。
- 现有覆盖止步于单测：`api/assist.test.ts`（接口层 8 用例）+ `AiAssist.dom.test.tsx`（DOM 层 5 用例）。**挂件在真实浏览器中的展开/发送/深链跳转/离线降级零 e2e**。
- 修复：新增 `e2e/assist_widget.spec.ts`：FAB 可见性（登录/注册页隐藏）、mock `/assist-api` 后完整对话流、route 动作按钮 SPA 跳转、服务 404 时离线提示。

---

## 三、缺失内容逐条核实

| # | 缺失项 | 核实结论 | 证据 |
|---|---|---|---|
| 1 | e2e 菜单断言漂移 | 【属实】§二 P0-1 | spec:34 vs AdminDashboard:61 |
| 2 | start.sh/build.sh 不起 ai-assist | 【属实】§二 P1-2 | grep = 0 |
| 3 | ai-assist 会话数据无备份/无保留期清理 | 【属实】部署指南 §十三 733 行自述「回滚数据：直接删 assist.db* 重启（历史会话丢失）」；代码无 TTL/清理任务（store.go 全文无 DELETE messages 定时逻辑） | store.go |
| 4 | 挂件对话无隐私声明/PII 提示 | 【属实】AiAssist.tsx 无任何隐私文案；访客输入直接落库 | AiAssist.tsx 全文 |
| 5 | ai-assist 无多副本方案 | 【属实】SQLite 单写者（store.go:33 `SetMaxOpenConns(1)` 自述），无 PG 后端选项 | store.go |
| 8 | **后台无 AI 助手 LLM 配置界面（且无法热更）** | 【属实，2026-09-16 追加】详见 §4.2b | 见下 |
| 6 | 前后端契约断链 | 【不成立，反向确认无断链】前端 176 路径 vs 后端 251 路由逐条比对全命中；`/api/me2` 仅为 `core.test.ts:91` 单测假路径（测 401 去重跳转） | 本轮实测 |
| 7 | 密码存储不安全 | 【不成立】`auth.go:119-126` bcrypt + 历史 SHA-256 登录时透明升级 + token_version 撤销机制，设计正确 | auth.go |

---

## 四、★ 新发现：AI 顾问从不调用 LLM，机械回复（用户实测报告）

### 4.1 现象

用户输入「怎么充钱」，得到固定兜底话术：

> 这个问题我记下了，稍后帮你确认。你可以先说说你想翻译什么内容、翻成什么语言，我帮你看看哪个功能最合适～

### 4.2 根因链（三层，全部代码实证 + 本机复现）

**层 1 · LLM 从未配置 → 代码路径上根本没有调用 LLM**
- `ai-assist/cmd/server/main.go:74`：`ASSIST_MOCK=1` 或未配 `ASSIST_LLM_API_KEY/BASE_URL/MODEL` 时 `buildLLM` 返回 `llm.New(nil, ...)`（providers 空）。
- `ai-assist/internal/llm/llm.go:67`：`Enabled() = len(providers) > 0` → **false**。
- `ai-assist/internal/engine/engine.go:361`：`if e.llm.Enabled()` 不成立 → 整个 LLM 分支跳过，直接 `fallbackReply(hits)`。
- 启动日志实证：`[llm] ASSIST_MOCK=1，不调用真实模型（规则兜底）`。
- 结论：**当前部署形态下 AI 顾问是纯规则引擎，「AI 回答」从未发生过**。管理台配置了 persona/temperature 也没有用——temperature 只在 LLM 分支被读取（engine.go:362-368）。

**层 2 · 检索是「关键词子串包含」，无语义能力 → 同义说法全部脱靶**
- `engine.go:51-61` `hitScore`：`strings.Contains(input, keyword)` —— 逐个检查关键词表是否为用户输入的**字面子串**。
- 知识库 `recharge` 条目（「怎么充值」）keywords = `充值,付款,支付,对公,usdt,支付方式,怎么买` —— **不含「充钱」**。
- 「怎么充钱」对 KB 27 条 / scripts 3 条 keyword / flows 3 条 trigger **全部 0 命中**。
- 对照实测（同一服务、同一库）：`怎么充值`→flow 命中；`我想付款`→flow 命中；`怎么充钱`→三层全脱靶。

**层 3 · 兜底话术掩盖了「没听懂」**
- `engine.go:416-421` `fallbackReply`：hits=0 时返回固定话术（即用户看到的那句）。
- 用户体验上表现为「机械回复」；且该兜底文案承诺「稍后帮你确认」，实际没有任何人工跟进机制（无工单联动、无留言收集）。

### 4.2b 追加根因 · 管理后台无任何接入 LLM 的配置入口（用户实测报告）

**「AI 顾问不调 LLM」的运维面根因：LLM 六项配置无处可设，只能登服务器改环境变量。**

1. **配置来源 env-only**：`ai-assist/internal/config/config.go:61-66`，`ASSIST_LLM_BASE_URL / API_KEY / MODEL / MODEL_BACKUP / API_KEY_BACKUP / TIMEOUT` 全部 `getenv` 读取，进程启动时一次性固化。
2. **管理台零 LLM 字段**：`ai-assist/web/admin.html` 配置区仅 `welcome / persona / temperature / max_tokens / quick_chips` 5 项（实测 grep 计数）。且 `temperature`、`max_tokens` 只被 LLM 分支消费（`engine.go:362-368`）——**未接 LLM 时这两项是死配置，管理台却摆在最显眼处**。
3. **即使直写 configs 表也不生效（无热加载）**：`handleConfig`（`server.go:282-287`）对任意 key 无白名单 upsert，但 LLM client 在启动时构建（`main.go:52` `buildLLM(cfg)`），`engine.Respond` 运行期从不回读 LLM 配置。即：管理 API POST `key=llm_api_key` 静默无效，形成「配了以为能用」的陷阱。
4. **主后台 React 同样无表单**：`frontend-react/src` 全量 grep `ASSIST_LLM / assist llm` = 0。`AssistP.tsx` 只是 iframe 托管 ai-assist 自带管理台，其能力上限同第 2 条。
5. **当前唯一改法**：登服务器 `vi /etc/ai-assist/secrets.env` + `systemctl restart ai-assist`（部署指南 §十三 718 行）。SaaS 运营方若多租户白标部署，每租户一个 key 只能靠复制实例。

**修复方案（并入 R0/R1）**：
- `configs` 表新增白名单键：`llm_base_url / llm_api_key / llm_model / llm_model_backup`（`handleConfig` 同时加 key 白名单，堵住任意 upsert）。
- LLM client 改为**惰性重建**：`engine.llmReply` 每次调用先比对 `configs` 中 LLM 配置指纹，变更则重建 client（主仓影子余额 TTL 重播种/策略热加载均有先例形态）。
- 管理台 UI 照抄主站成熟模式：**Key 掩码回显**（主站 `admin_models.go:76/96-99` `maskKey`，UAT A5-mask 已有回归断言）+ **「测试连通」按钮**（发一条 1-token 请求回显模型名与延迟）+ **生效状态徽标**（已接入/规则模式）。
- env 优先级：显式 env > configs 表（生产 secrets.env 继续作为兜底与 CI 注入通道），管理台 UI 上注明当前生效来源。



```
POST /api/assist/chat {"message":"怎么充钱"}  → source=fallback, reply=这个问题我记下了…（用户所见完全一致）
POST /api/assist/chat {"message":"怎么充值"}  → source=flow（recharge-guide 第一步）
POST /api/assist/chat {"message":"我想付款"}  → source=flow
启动日志：[llm] ASSIST_MOCK=1，不调用真实模型（规则兜底）
```

### 4.3 复现记录（2026-09-16 本机，seed 原始数据）

```
POST /api/assist/chat {"message":"怎么充钱"}  → source=fallback, reply=这个问题我记下了…（用户所见完全一致）
POST /api/assist/chat {"message":"怎么充值"}  → source=flow（recharge-guide 第一步）
POST /api/assist/chat {"message":"我想付款"}  → source=flow
启动日志：[llm] ASSIST_MOCK=1，不调用真实模型（规则兜底）
```

### 4.4 RAG 改造方案（分期落地）

**目标**：客户任意口语化提问 → 语义检索知识库 → LLM 组织回答 → 命中功能入口时带深链按钮。检索不准时回退关键词层，LLM 不可用时回退规则层（现有降级链保持）。

**R0 · 止血（当天可上，无需 LLM）**
1. `hitScore` 增加双向包含 + 同义词归一表：`充钱≈充值≈付费≈交钱`，同义词表进 `configs` 由管理台在线维护（新增 admin config key `synonyms`）。
2. 兜底话术改造：hits=0 时不再空承诺，输出「没找到答案 + 引导提问 chips + 人工联系入口」，并把该问题落 `messages` 标记 `unanswered=1`，管理台「会话记录」页新增未答问题清单（运营每周人工补知识库——数据飞轮的最小闭环）。
3. 启动横幅与管理台显著提示「LLM 未接入，当前为规则模式」（现状只有一行 log，运维极易漏配）。
4. **管理台 LLM 配置 UI + 热加载**（配合 §4.2b：configs 白名单键 + client 惰性重建 + Key 掩码回显 + 测试连通按钮）——这是让「AI 顾问」真正成为 AI 的开关，与 R0.1 同批优先落地。

**R1 · 语义检索（真正 RAG，推荐 3~5 天）**
1. 向量化：OpenAI 兼容 `/embeddings`（复用 `ASSIST_LLM_BASE_URL`，主站已有同款 embedding 配置形态）；kb_entries 增列 `embedding BLOB`（JSON float32），管理台保存条目时异步生成，无向量时条目仅参与关键词层。
2. 检索：`RetrieveKB` 输入先向量化 → 余弦相似度 topK(5) → 与关键词打分**混合排序**（`score = 0.6*cos + 0.4*kw_norm`，双通道任一命中即入围）。
3. Prompt：现有 `buildSystemPrompt`（engine.go:381-398）已按「知识注入 + 铁律 + 【go:keys】动作标记」设计，语义命中条目直接替换关键词命中条目即可，动作提取 `postProcess` 无需改动。
4. 缓存：输入向量按文本 hash 缓存（SQLite `vec_cache` 表），控制 embedding 成本。

**R2 · 质量与成本护栏（上线前必做）**
1. chat 限流（P1-3-2 修复同批），LLM 成本才有上界。
2. evals 最小集：20 条口语化测试问句（含「怎么充钱」）断言 `source=llm` 且回复包含预期关键词，进 CI（主仓已有 `internal/evals` 形态可参考）。
3. 会话/未答问题导出 CSV（运营补料闭环）。

**验收标准（字节级）**：
- `怎么充钱` / `咋充钱` / `钱怎么交` → source=llm（或 R0 阶段 source=rule/fallback 但回复为 recharge 正确内容），不再是空承诺兜底话术；
- 20 条 evals 问句命中率 ≥ 90%；
- LLM 断供时全链路自动回退关键词层，挂件无 5xx。

### 4.5 ★ R0 批次实施记录（2026-09-16 当日完成，全部实测通过）

**R0.4 管理台 LLM 配置（热加载）**
- `server.go`：`configKeyWhitelist`（UI 5 项 + LLM 4 项 + synonyms）——非白名单 key PUT 一律 400（堵任意 upsert 陷阱）；`llm_api_key` 读回掩码（`sk-***56` 形态），掩码值回写自动 skip 不覆盖明文。
- `engine.go`：`ensureLLM()` 惰性重建——configs LLM 四项指纹比对，变更重建 client；**env 显式接入时优先**（`LLMMode()` 返回 env/db/rule 三态）。
- `admin.html`：「🤖 LLM 接入」卡（base_url/api_key(密码框)/model/model_backup + 🔌测试连通按钮 + 生效徽标「已接入(环境变量/后台配置)/⚠️规则模式」）；保存后下一条对话生效，无需重启。
- 新端点：`POST /api/assist/admin/llm/test`（回显模型名/耗时/错误）。

**R0.1 同义词归一**
- `engine.hitScore`：同义词表（`configs.synonyms`，每行「标准词=同义词1|同义词2」，管理台在线编辑，指纹失效重载）。
- seed 预置三组：`充值=充钱|交钱|付钱|怎么充`、`翻译=翻一下|翻个|转成`、`价格=多少钱|费用|贵不`。**老部署 configs 表非空不会自动灌**，需在管理台「配置」页粘贴一次。
- seed 数据修正：`what-is` 条目移除超泛关键词「是什么」（原任何「XX是什么」都误命中该条目）。

**R0.2 兜底改造 + 未答登记**
- 零命中回复改为「这个问题我还没学到，先记下来，学完就能答你啦 + 三个快捷入口」，不再空承诺。
- 未答问题去重登记（上限 200 FIFO）进 `configs.unanswered_questions`，管理台「会话记录」页新增「❓待补料问题」清单与计数——运营补知识库的数据飞轮最小闭环。

**R0.3 生效状态**：`GET /api/assist/admin/sessions` 新增 `llm_mode`（env/db/rule）与 `unanswered` 字段；管理台配置页顶部常显徽标。

**测试与验证**：新增单测 9 个（同义词命中/端到端/热加载/env 优先/未答登记/白名单/掩码/连通端点/sessions 载荷），`go test ./...` 4 包全绿 + `go vet` 通过。端到端终验：`怎么充钱`→充值引导+入口按钮✓、真未知问题→引导+未答登记✓、原关键词话术无回归✓、`能言是什么`精确命中✓、热加载 llm_mode=db✓、掩码回写 skip✓、连通测试报错可读✓。

**遗留（后续批次）**：
- R1 语义检索（embeddings + 余弦混合排序）未启动——R0 的关键词/同义词层已是其子集，R1 上线后无破坏性迁移；
- 生产部署需重启 ai-assist 生效（admin.html 与二进制更新）；Caddy/systemd 配置无变化；
- `ASSIST_CORS` 生产白名单、chat 限流、session ID 换 crypto/rand（P1-3）建议与 R1 同批。

### 4.6 ★ 生产 CSP 拒嵌修复（2026-09-16 用户实测报告 → 当日修复，已提交 5b1772b）

**现象**：超管打开「AI 助手」配置面板，浏览器报两类拦截：
1. `Framing 'https://langcross.lexicorn.cn/' violates frame-ancestors 'none'` —— 管理台 iframe 被自己站点拒绝嵌入；
2. `static.cloudflareinsights.com/beacon.min.js violates script-src` —— Cloudflare Web Analytics 注入的 beacon 不在白名单。

**根因**（服务器实测确认）：
- ai-assist 直连 `:8790` 无 CSP 头；经 Caddy 反代后整站严格 CSP（`frame-ancestors 'none'`，2026-09-16 D5 增补）连带套在 `/assist-api/*` 响应上；
- `X-Frame-Options: DENY` 同样连带（旧内核下与 CSP 同效）；
- 站点开启了 Cloudflare 代理注入 Web Analytics，但 `script-src` 白名单未收录 beacon 域。

**修复**（`/etc/caddy/translator.conf` + 仓库模板 `deploy/caddy/translator.conf` 同步）：
- `@strict_csp not path /assist-api/*` 互斥分流：严格版（+cloudflareinsights）仅作用于非 assist 路径；`/assist-api/*` 用管理台专用宽松版（`frame-ancestors 'self'`）；
- X-Frame-Options 同口径分流：严格路径 DENY / assist 路径 SAMEORIGIN。

**踩坑记录**（已写进配置注释）：
- Caddy `header{}` 块内不支持 matcher 语法——块内写 `@matcher`，`caddy validate` 静默通过但完全不生效（今日两次假修复后定位）；
- 块外同名 header set 与块内行不构成「后写覆盖」（Caddy 指令排序决定执行序），互斥分流是唯一可靠形态。

**验证**：公网四路响应头实测（主站严格 / /api 严格 / /assist-api 页面宽松 / /assist-api API 宽松）+ Playwright 同源 iframe 注入终验（`LOADED:AI 助手管理台` + `CSP_ERRORS=0`）。

---

## 五、本轮全量测试结果存档

| 套件 | 结果 |
|---|---|
| backend-go go test（22 包，含 -race） | 全绿 |
| ai-assist go test（4 包） | 全绿 |
| UAT 主矩阵 API A/B（PG15+pgvector 全新库） | 67/67 |
| UAT 交易专项 T 套件（USDT/RBAC/修复回归锁） | 308/308 |
| 双实例 e2e（JWT 互通/并发无超卖/台账勾稽/充值传播） | 8/8 |
| vitest 前端单测 | 79/79 |
| Playwright 全量 e2e | **1 红**（P0-1，复跑仍红） |
| ai-assist 手工端到端（C 端 13 + 流程 9 场景） | 22/22 通过（功能性），安全项 3 缺陷（P1-3） |
| 前后端契约比对 | 176↔251 无断链 |

*测试产物：/tmp/uat_full_run.log、/tmp/mi_e2e.log、/tmp/vitest_run.log、/var/folders/.../tmp.yk07DIwEd3/（UAT 工作目录）。*
