# 核实报告：架构评审发现逐条代码验证（2026-09-16）

> 方法：对《UAT 全量实测报告》中 8+2 条发现逐条回读真实代码（文件:行号），关键资金/权限类发现另在运行中的 UAT 环境（PG 方言 `translator_uat` 库 + :8899 服务 + mock LLM/chain）做 live 复现。不采信文档转述。
> 结论分级：**属实** / **属实但严重度修正** / **更正**。

## 结果总览

| # | 发现 | 判定 | 关键证据 |
|---|---|---|---|
| 1 | 微信/支付宝下单假二维码 + 静默回退 mock | **属实** | payment.go:163-165,227-231；pay.go:179-181 |
| 2 | 停机窗口计费泄漏 | **属实** | main.go:389-390；sink.go:145-152,167-214 |
| 3 | IsSuperAdmin 旧角色越权面 | **属实 + live 实锤**（跨租户退款已复现成功） | iam/auth.go:167-170；stream.go:519；admin_billing.go:334 |
| 4 | 退款/冲红前端零实现 | **属实** | 全 src 无 refund 封装、void 0 命中；后端实测健在 |
| 5 | 付费引导死胡同 + 轮询无终态 + subscribe 静默 | **属实，严重度下调** | App.tsx:196-201；PlansP.tsx:122,211,271 |
| 6 | 停止生成计费 / SSE 空闲 / sendFile 死代码 | **部分属实，"断连仍全额计费"降级为不成立** | translate.ts:28-56；stream.go:129,621；client.go:508/597/791 |
| 7 | TS/Java SDK 缺陷与漂移 | **属实（含 dist 带病发布）** | ts index.ts:199 + dist/index.js:169；java grep 0 命中 |
| 8 | 错误码双口径 / 裸 float / Playwright 不入 CI / e2e teardown | **属实，一处细化** | codes.go:32 vs :45+；PlansP.tsx:549；ci.yml |
| 运维A | 多实例并发跑迁移 | **属实**（-init-db 提供手动单飞雏形未强制） | main.go:65,205-213 |
| 运维B | orders 本地时区 vs 台账 UTC | **属实 + live 证据**（created_at "+08:00"） | billing.go:937 vs 同文件 15 处 UTC |

---

## 修复实施记录（2026-09-16 同日落地，改动后以官方 run_uat.sh 全新库回归）

| # | 修复 | 改动文件 | live 回归 |
|---|---|---|---|
| 3 | IsSuperAdmin 收紧为 `等级≥4 && tenant_id==0`；RequireRole(≥4) 同口径；users/update 与 bulk-import 拒绝把高角色落到具体租户 | iam/auth.go:167-201；api/auth.go:765；api/user_import.go:173 | 违规提升 → 400；强插违规行后退款/列租户 → 403；重启触发 store.go:570 存量归一（tid→0）已验证 |
| 2 | 优雅停机顺序调换：先 `s.Shutdown`（等在途排空）后 `Sink.Stop()`（最终 flush），消除 10s 计费泄漏窗口 | cmd/server/main.go:389-397 | 编译+race+服务启停正常 |
| 1 | 微信/支付宝 CreateOrder fail-closed（协议未接入即报错，不再吐假码）；handlePayCreate 渠道下单失败显式报错，删除静默回退 mock | payment.go:154-171,222-231；api/pay.go:169-190 | pay_mode=sdk 下 wechat 下单 → 显式"未接入真实协议"，不再静默出 mockpay 码；payment 单测契约同步更新并转绿 |
| 5 | 收银台轮询：in-flight 锁 + document.hidden 暂停 + paid/cancelled/refunded 均停并收起弹窗；subscribe 失败弹 toast；余额耗尽横幅/useChat 充值引导对租管+改跳后台计费 Hub | PlansP.tsx 多处；App.tsx:216;useChat.tsx:242;i18n billing.ts | tsc+vitest+E2E 全绿 |
| 4 | 前端封装 `/api/admin/orders/refund`、`/api/billing/invoices/void`；订单表补退款按钮（仅超管）、发票表补冲红按钮与作废态；金额列 toFixed 收口 | api/billing.ts；PlansP.tsx:317,549,563 | tsc+vite build+vitest 63/63+官方 E2E 33/33 通过 |
| 7 | TS SDK `waitTask` 优先级 bug 修正（`interval ?? (files?60:15)`）并重编 dist（v1.0.1）；文件方法 fs 依赖据实标注 Node-only；Java SDK 删除未实现的"文件批量/下载"宣称 | sdk/typescript/src/index.ts,dist;README;sdk/java TranslatorClient.java | dist 已同步修复；TS 包 tsc 通过 |

未纳入本轮（P2 滚动）：#6 SSE 空闲心跳、sendFile 接 UI；#8 大写错误码映射、Playwright 入 CI、e2e teardown 治理；运维A 迁移 Advisory Lock、运维B 时间戳统一 UTC。

## P2 修复实施记录（2026-09-16 第二批，全部落地）

| # | 修复 | 改动文件 | 验证 |
|---|---|---|---|
| 6a | SSE 双向防护：后端 `sseHeartbeat` 每 20s 写 `: ping` 注释帧（session 级 goroutine，与 D20 写锁序列化；stop 同步 join 保证 handler 返回后无写入）；前端 `readWithIdle` 60s 无字节 → cancel reader + `ApiError(stream_idle)`，UI 不再永卡 loading | api/stream.go（helper + 两 SSE handler 全写点加锁）；api/translate.ts consumeSSEStream | go vet/build/test ✅；E2E P2/文件链路 ✅ |
| 6b | 即时文件翻译接 UI：ChatWindow 📎 按钮 + 隐藏 input（accept=TRANSLATE_FILE_ACCEPT，复用 validateTranslateFile 40MB/白名单前端先行拦截），sendFile 不再是死代码；title 避开「发送」子串（曾撞 Playwright strict mode，已修） | ChatWindow.tsx；i18n chat.busy/chat.attachFile（zh+en） | tsc + vitest parity + E2E 33/33 ✅ |
| 8a | 错误码规范化：`normErrCode` 折叠大写/别名（INSUFFICIENT_BALANCE→insufficient_balance；QUOTA_EXCEEDED→daily_quota_exceeded），充值引导不再依赖单一口径 | useChat.tsx h6HandleErr | tsc ✅ |
| 8b | e2e 基建：playwright.config 加 actionTimeout/navigationTimeout/expect.timeout、trace 改 retain-on-failure；run_uat.sh E2E 步骤 `PWTEST_CHILD_PROCESS_TIMEOUT=60s`（teardown 挂起强杀提速 5×）+ 首轮非零自动 `--last-failed` 复跑甄别 flaky（复跑绿→WARN 放行留痕；复跑仍红→拦截） | playwright.config.ts；scripts/uat/run_uat.sh | 本轮实测：首跑 worker teardown 挂起误红场景由 60s 强杀+复跑机制兜底 |
| 8c | Playwright 进 CI：ci.yml 新增 `e2e` job（SQLite 方言跑全量 run_uat.sh：构建+mock LLM/chain+API+交易专项+E2E；race 由 backend job 覆盖故 UAT_SKIP_RACE=1）；本地 SQLite 矩阵先行验证通过 | .github/workflows/ci.yml | 本机 DB_DRIVER=sqlite 全绿（67/284/33 exit=0） |
| 运维A | 迁移单飞锁：main.go `acquireMigrateLock`（PG session 级 `pg_try_advisory_lock` 轮询 1s、最长 10 分钟排队；锁覆盖 kb/tenant/saas store 全部迁移与种子）；进程崩溃由 PG 自动释放；sqlite 跳过 | cmd/server/main.go | build + UAT 启动路径 ✅ |
| 运维B | 资金域时间戳统一 UTC：billing.go 全部 17 处写点/比较点转 `time.Now().UTC()`（orders/quota/发票/台账/PayDeadline/PackageExpires）；`alerts` 域保持原口径（dayAgo 比较点与 alerts 写点同为本地，同域自洽不动）。TEXT 列字典序比较自此与写点格式一致 | internal/store/billing.go | go test store/billing ✅ |

> 运维B 切换窗口说明：存量 pending 单（本地格式 `+08:00`）在新 UTC cut 下字典序偏大，会**延迟至多 8h 才被关单**（不会误杀新单——新单写 UTC 与 cut 同格式）；USDT 单走 `usdt_orders.expires_at`（本就 UTC）不受影响。发布后一个超时窗口周期即收敛，无需数据迁移。


---

## 1. 支付渠道假二维码 —— 属实

逐行读 `backend-go/internal/payment/payment.go` 全文：**整文件无一处 HTTP 调用**（grep `http.NewRequest|client.Do|Post(` 零命中）。

- 微信 `CreateOrder`（:154-166）：只要 AppID/MchID/APIv3Key 三个字符串非空即"配置成功"，返回 `weixin://wxpay/bizpayurl?pr=base64(订单号)`——`pr` 参数格式要求是微信侧生成的短链码，本地拼订单号扫出来必为废码。注释自认："此处骨架已就绪，商户号到位后补齐 HTTP 调用"。
- 支付宝 `CreateOrder`（:222-231）：返回 `"alipay://precreate?out=" + 订单号` 占位串，注释自认"当前未配商户号，返回占位内容以便测试"。
- 静默回退：`internal/api/pay.go:179-181`——渠道 `CreateOrder` 报错（未配置）时**无日志无提示**直接 `MockProvider.CreateOrder` 兜底，且 :188 `o.Channel = res.Channel` 把订单渠道改写为 mock。叠加 `handlePayNotify`（:369-372）"pay_mode≠mock 时 mock 回调一律 403"，结果：配了 `pay_mode=sdk` 但商户号缺失的环境，用户拿到 `mockpay://` 码、扫不动、回调进不来，**订单挂 pending 只能等超时取消或超管后台人工核销**。
- 补充事实：微信/支付宝的 `VerifyNotify` 回调验签是真实现（AES-256-GCM 解密 / RSA2 验签，:170-218），缺的只是下单侧。
- 当前可真实收款路径确认：`manual`（static_qr_image 静态码 + 人工核销，pay.go:151-166）与 `usdt`（链上对账/人工核销）；`mock` 仅限测试。

**建议**：回退处显式报错（区分"渠道未配置"5xx）而非降级 mock；或 pay_mode=sdk 且下单失败时直接拒单，别把 mockpay:// 呈现给用户。

## 2. 停机窗口计费泄漏 —— 属实

`backend-go/cmd/server/main.go:386-397`：

```go
billing.DefaultSink.Stop()            // ① 最终 flush 后 flusher 协程退出（run() 收到 stop 直接 return）
ctx, cancel := context.WithTimeout(context.Background(), 10*time.Second)
s.Shutdown(ctx)                       // ② 此后最长 10s 仍在服务在途请求
```

`internal/billing/sink.go:167` `Record()` **不检查 stop 状态**，①②之间完成的用量照常 append 进 `s.buf`，而 flusher 已死、进程随后退出——这批真实 LLM 成本永不落库不扣费（收入泄漏 + 影子余额与 DB 背离至进程终止）。修复即调换顺序（先 Shutdown 后 Stop）或 Stop 后 Record 同步直落。

## 3. IsSuperAdmin 结构性越权 —— 属实，且 live 复现了跨租户退款

代码链（逐处读原文确认）：
1. `internal/iam/auth.go:167-170`：`IsSuperAdmin(u) = RoleLevel(u.Role) >= 4`；:154-161 旧角色 `admin` 与 `super_admin` 同映射 4；`internal/iam/models.go:70` 注释自认「旧管理员角色 → 视为超级管理员」。**无 tenant_id==0 约束**。
2. 消费点：`api/stream.go:519` 产物下载归属校验以 `!auth.IsSuperAdmin(u)` 为跳过开关（跨租户下载他人产物）；`api/admin_billing.go:321` 退款走 `requireAdminUser`（`admin.go:24-33`，同样 level≥4），且 :334 **显式接受 body 里的 `tenant_id` 参数**——中间件（server.go:593-606）对 tid>0 用户强制本租户的收口被该参数绕开。
3. 违规行从哪来（比原发现更具体的触发面）：创建路径有收口（`api/auth.go:656-657`：role=admin/super_admin 强制 tid=0），**但 `users/update` 没有**（`api/auth.go:721+` 全文核读：仅校验"分配 ≥4 角色需 IsSuperAdmin"，无"高角色必须落 tid=0"），超管把某租户成员角色改成 `admin` 即产生 `role='admin' AND tenant_id>0` 行。
4. **Live 复现**（translator_uat 库，现场已还原、存量核查归零）：
   - 租户2 建单→mock 回调支付 → `orders#31 paid tid=2`；
   - 超管 `POST /api/admin/users/update {id:uatuser_b, role:"admin"}` → DB 行 `uatuser_b admin tid=3`；
   - uatuser_b 登录（自身 tenant_id=3）→ `POST /api/admin/orders/refund {"id":31,"tenant_id":2}` → **`{"success":true}`**，订单变 `refunded refund_money=8.97`——租户3 用户退掉了租户2 的款。
5. 存量：当前 UAT 库 `count(role in ('admin','super_admin') AND tenant_id>0)=0`；引导超管 `admin|admin|0` 合法（CreateUser(0,...)，iam/store.go:277）。生产库上线前必须同 SQL 审计一次。

**建议**：`IsSuperAdmin` 收紧为 `RoleLevel>=4 && u.TenantID==0`；users/update 对 ≥4 角色加 tid=0 收口；退款/产物下载逐个复核。

## 4. 退款/冲红前端零实现 —— 属实

- 前端 `src/` 全量 grep `refund`：仅订单状态标签（PlansP.tsx:42-67）、对账规则串（ReconcileP.tsx:13）、i18n 词条（dicts:audit.action.order_refund）——**无任何 API 封装/按钮**；`invoices/void|voidInvoice` 0 命中。
- 后端实测健在且守卫正确：`/api/admin/orders/refund`（server.go:406 → admin_billing.go:320，L4 专属、订阅退款联动撤销 PackageCode 镜像 :341-350）；`/api/billing/invoices/void` 路由存在。`src/api/billing.ts`/`admin.ts` 均无对应函数。
- 结论不变：SOP 文档承诺的退款操作目前只能 curl/DBA 执行。

## 5. 付费漏斗与收银台轮询 —— 属实，严重度下调为"体验缺陷+资金页健壮性"

- 耗尽横幅（App.tsx:196-201）→ `/packages`；`selfservice.tsx` 全文 grep `pay|充值|checkout|subscribe` **0 命中**（只读页确认）；useChat 余额不足弹窗同样跳 `/packages`（死胡同确认）。
- **修正原表述**："个人订阅用户在 UI 上无法付费"不准确——注册即建租户、注册者本人是 `tenant_admin`（L3，live 验证 `uatuser_a tenant_admin tid=2`），可进 `/admin → 计费 Hub（minLevel 3，AdminDashboard.tsx:58）→ PlansP 非超管分支` 完成下单支付；真正无入口的是企业内 L1/L2 成员（由租户付费属合理产品口径）。缺口收敛为：横幅引导错误 + 无直达收银台路由。
- 轮询三缺陷（PlansP.tsx）：:122 注释"每 6s"实为 `setInterval(checkStatus,3000)`；:271 `if (no.status==='paid') stopPolling()` —— **cancelled/refunded/USDT 过期单继续 3s 轮询且收款弹窗不收起**；checkStatus 无 in-flight 去重/AbortSignal。:211 `subscribe()` `if (!r.success) return` ——订阅被拒**静默无提示**。均确认属实。

## 6. 聊天"停止"与 SSE —— 部分属实，原"断连仍全额计费"降级为不成立

- **更正**：chat 流上下文确实挂 r.Context()——`stream.go:129` 经 `s.userOrgCtx(r)`（:620-621 `ctx := r.Context()`）构建；engine/llm 路径无 `context.Background()/WithoutCancel/Detach`（grep 0 命中）；LLM 三次调用全部 `http.NewRequestWithContext`（llm/client.go:508,597,791）。客户端断开 → ctx cancel → 在途 LLM 请求中止，未完成段不产生成本；已完成段按量计费与 UI 文案「已消耗不退还」（chat.ts:184）一致。**前端 stop 仅本地 abort 是真，但后端连带计费风险不成立**。
- 属实项：SSE 消费循环（translate.ts:28-56）`while(true) reader.read()` **无空闲超时/心跳**，中间层静默挂起连接时 UI 永卡 loading（只能手动点停止）。
- 属实项：`useChat.sendFile`（:179-206）经 store 导出（:306）但全 src 组件零调用（grep 仅 hooks 定义 + i18n 键），即时文件翻译在 React 工作台无 UI。

## 7. SDK —— 属实（TS 已带病发布至 dist）

- 优先级 bug 实锤：`sdk/typescript/src/index.ts:199` `const gap = interval ?? (st as any).type === "files" ? 60 : 15;`——JS 解析为 `(interval ?? cond) ? 60 : 15`，显式传入的 `interval` 恒被吞掉（文本任务传 5 也会睡 15/60s）；**`sdk/typescript/dist/index.js:169` 同错**，即已发布产物带病。
- `require("fs")` 于 :147/:229，与包声明的多端通用承诺冲突（浏览器打包必炸）——属实。
- Java：`TranslatorClient.java:21-23` 注释宣称支持"multipart=文件批量""download 产物下载"，类内 `createFileTask|downloadFile` grep 计数 **0**——属实（宣称与能力不符）。
- 四套 SDK（python/js/typescript/java）均为手写，`scripts/release-sdk.sh` 仅做版本号 bump，无 openapi-generator 痕迹；后端已有机器可读 spec（`internal/openapi/openapi_v1.go`）却未被消费——漂移即上述两 bug 的根因，属实。

## 8. 错误码口径 / 金额展示 / CI / e2e 基建 —— 属实，一处细化

- 双口径存在：`internal/errors/codes.go:32` 业务码大写系（`INSUFFICIENT_BALANCE`）vs :45+ OpenAPI 小写系（勿改值注释在案）。**细化**：聊天 SSE 错误事件下发的是 `billing.QuotaErrCode`（quota.go:226,241：`daily_quota_exceeded`/`insufficient_balance` **小写**），与 `useChat.tsx:239/243` 匹配一致——对话主链路余额不足引导实际是对的；风险面在非 SSE JSON 端点的大写 code 无人映射（`bizErrorCode()` 导出后全 src 0 调用，core.ts:170 确认），退化为通用红字。
- 裸 float 属实：PlansP.tsx:549 `{amount: row.amount_money}` 未 toFixed（REAL 列直插模板）；同行 :553「开发票」硬编码中文（EN 界面直出）也确认。
- Playwright 不入 CI 属实：`.github/workflows/ci.yml` 步骤仅 go vet / go test -race / npm ci / typecheck / vitest。
- e2e teardown 间歇挂起属实（本轮实测）：run_uat 首跑 33 用例全过但 4 worker×2 轮"did not exit within 300000ms after stop, force-killed"→ 闸门 exit=1；此后单独复跑（smoke 3、pixel_uat 8、其余 22、全量 33）四次全部 `exit=0` 干净收尾（全量 24.4s，multilang_zip 1 次 flaky 重试过）。属环境负载相关间歇缺陷，闸门需加 teardown 治理（如 `--only-changed` 拆分、worker 上限、afterEach 强清 UI 定时器）否则"发布闸门"会随机误红。

## 运维A. 迁移单飞 —— 属实

`cmd/server/main.go:65,205-213`：`-init-db` 提供"仅建 schema 后退出"的手动单飞模式（PG 切流时用），但常规启动路径仍内嵌全量迁移链——多实例滚动发布会并发执行 CREATE INDEX/去重 DELETE/回填，存在锁等待与回填竞态。缓解：发布规程强制 init-db 先行 + 建议升级为 Advisory Lock 自动单飞。

## 运维B. 时间戳混用 —— 属实（live 证据）

`internal/store/billing.go:937` orders.created_at 以 `time.Now().Format(time.RFC3339)` 写入（本机偏移，实测 UAT 落库值 `2026-09-16T06:33:49+08:00`），而同文件 15 处 `time.Now().UTC()`（台账/USDT 域）。pending 超时扫描、退款窗口等字符串比较路径在跨时区多实例/数据搬运后产生小时级偏差。建议 orders/tenants 写点统一 UTC。

---

## 修订后的修复优先级

| 序 | 项 | 量级 |
|---|---|---|
| P0 | #3 IsSuperAdmin 收紧 + users/update 收口 + 存量审计（已 live 证明可跨租户退款） | 半天 |
| P0 | #2 停机顺序调换（Shutdown→Stop） | 一行 |
| P0 | #1 渠道下单失败禁静默回退 mock，显式报错；补齐真实下单或产品上关闭 sdk 模式 | 1-3 天 |
| P1 | #5 轮询终态集合+in-flight 锁；subscribe 失败 toast；横幅改引导或开用户态收银台路由 | 1 天 |
| P1 | #4 前端封装退款（超管侧）+ 发票冲红入口 | 1 天 |
| P1 | #7 TS SDK 两处修复重发 dist；Java 补实现或删宣称；SDK 改 spec 生成 | 1-2 天 |
| P2 | #6 SSE 空闲心跳；sendFile 接 UI；#8 大写码映射/toFixed/Playwright 入 CI/teardown 治理；运维A/B | 一周内滚动 |
