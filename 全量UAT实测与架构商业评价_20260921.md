# 全量 UAT 实测与系统评价报告

- **日期**：2026-09-21
- **仓库**：`/Users/zhangzifei/Desktop/翻译助手`，分支 `autosales`（HEAD `4781b92`，领先远端 2 提交未推送）
- **方法**：以**源码为准**（仓库根 `.md` 文档仅作对照，多处已证实过时，偏差见 §9）
- **约定**：本次为**只读调研**，未修改仓库任何文件
- **复核**：2026-09-21 第二轮**逐条源码复核**已执行完毕——§0/§4/§6 的量化指标已按复核实测重算，
  **9 条原结论被更正、6 条新增发现、其余 25 条确认成立**，完整「确认/更正/新增」勘误账见 **附录 A**
- **规模实测**：后端 401 个 `.go` / 89,965 行；前端 214 个 `.ts/.tsx` / 61,165 行

> **重要前提：工作区是脏的。** `git status --porcelain` 复核时共 **184 条**（139 个已修改 + 45 个未跟踪）。
> 本报告的测试结论全部针对**当前工作区状态**，不等价于「HEAD 通过」；且 HEAD 本身也未经过 CI（见 §3.1）。

---

## 0. 结论速览

| 维度 | 结论 |
|---|---|
| 自动化测试通过率 | **后端 shell 断言 566/570 绿；前端 E2E 48/50 绿；vitest 224/224 绿；SDK 21/21 绿** |
| 真实代码缺陷（红灯归因） | **1 条**：AI 顾问菜单前后端权限口径不一致（租户管理员可见但必然 403）。**另 +1**（本日追加，不在 UAT 断言内）：`.md` 工单标题中文未译 —— 见 §3.5 |
| 新报缺陷（用户提交） | **1 族 6 条**：`T20260921075004EF8`（`产品方案书_v5.1.md`→en）残留大量中文 —— **RC-1** 前缀正则不支持"无空格"写法（**实测本工单 0 命中**）、**RC-2** KB 命中缺同文守卫、**RC-3** 代码围栏内整块不翻译（用户裁定为缺陷）、**RC-4** 产物文件名不翻译、**RC-5**（2026-09-22 实测新增）**行内标记被剥除致丢标记丢内容**（`**` 381→0、反引号内 16 个标识符被删）、**RC-6** 目录锚点不同步；交付口径＝清洗脏数据 + 保原格式 + 目标语言 + 文件名翻译 |
| 测试自身缺陷 | **1 条**：A3 选择器歧义（新 spec，从未进 CI） |
| 环境假红 | **1 条 4 断**：分片上传因本机沙箱拦截 `Application Support` 目录而 EPERM |
| 资金/支付链路 | **扎实**。幂等 + 行锁 + 并发回归测试齐备，USDT 对账链路完整且默认关闭 |
| 架构分层 | **有明显欠账**。`internal/api` 上帝包（270 handler / 24,426 行 / 474 个 `*Server` 方法 / 268 路由 / 949 处直连 store，无接口抽象） |
| 租户隔离 | **无结构性收口**。依赖 270 个 handler 逐个自觉；未登录兜底租户 1 |
| 质量保障 | **零件有测、装配无测**。gate/qa/postprocess 叶子函数有 30+ 断言，编排器步骤零测试 |
| 「字节级」口径 | **部分成立**。DB 字节值与 JSON 精确正则断言为真；但**「像素级」无像素基线比对**（零 `toHaveScreenshot`） |
| 商业成熟度 | **可收款、可交付、可上线**；缺可见的 LLM 质量基准、容量/SLA 模型、合规证据与灰度机制 |

---

## 1. 实测执行清单（本机实跑，非引用文档）

环境：macOS / Go 1.26.5 / Node 22.22.2 / PostgreSQL 15.18（运行中）/ Redis（运行中）/ pgvector 可用。

| # | 项目 | 命令 | 结果 |
|---|---|---|---|
| 1 | 后端编译 | `go build ./...` | ✅ 0 错 |
| 2 | 后端静态检查 | `go vet ./...` | ✅ 0 错 |
| 3 | 后端格式门禁 | `gofmt -l internal cmd` | ❌ **9 个文件不合格** |
| 4 | 后端全量单测 | `DB_DRIVER=sqlite go test -count=1 ./...` | ✅ 全包 ok（含 assist 全部子包） |
| 5 | 后端竞态检测 | `go test -race ./internal/...`（UAT 内置） | ✅ 通过 |
| 6 | 前端类型检查 | `npx tsc --noEmit` | ✅ 0 错 |
| 7 | 前端单测 | `npm test`（vitest） | ✅ 35 文件 / **224 用例全绿** |
| 8 | UAT 主矩阵（PG 方言） | `bash scripts/uat/run_uat.sh` | ⚠️ 见 §2 |
| 9 | UAT 主矩阵（关沙箱复跑） | `UAT_SKIP_RACE=1 ... run_uat.sh` | ⚠️ 失败集合**完全一致** |
| 10 | AI 顾问专项 | `bash scripts/uat/assist_uat.sh` | ✅ **38/38 全绿** |
| 11 | 多实例红线 | `bash scripts/uat/multi_instance_e2e.sh` | ✅ **8/8 全绿** |
| 12 | SDK 行为测试 | `sdk/typescript npm test` / `sdk/python unittest` | ✅ 8/8、13/13 |

---

## 2. 测试结果总表

### 2.1 后端 shell 断言（字节级：DB 值 / 精确 JSON 正则）

| 套件 | 覆盖 | PASS | FAIL |
|---|---|---|---|
| `api_uat.sh`（A/B 主链路） | 公开接口 / 认证注册 / 防刷与蜜罐 / 模型路由 / 估价 / 翻译扣费 / 余额硬闸 / 充值支付幂等 / 发票 / 邀请裂变 / 知识库 / OpenAPI / 租户隔离 / 运营策略引擎 | **88** | **0** |
| `api_uat_txn.sh`（T1–T52 专项） | 订单全生命周期 / 发票一致性 / 裂变奖励幂等 / OpenAPI 异步 / 并发扣费压力 / 工单隐私 / 退款折算 / 订阅到期 / 分片上传 / SCIM 2.0 / TMX / SLO / 质检闭环 / 逐段对照 / 伪标签清洗 / 任务系统 / 自动续费 / 优惠券 / AI 助手代理 | **432** | **4** |
| `assist_uat.sh`（AI 顾问） | 开场/话术/流程推进/历史鉴权/同义词归一/兜底引导/管理面白名单/密钥掩码/热加载/DB Token 桥接 | **38** | **0** |
| `multi_instance_e2e.sh` | 双实例 JWT 互通 / 跨实例并发不透支 / 账本对账 / 充值传播 | **8** | **0** |
| **合计** | | **566** | **4** |

关键断言示例（确为字节级）：
- `A9-pay-idempotent(999900==999900)` — 金额字节相等
- `T16` 30 路并发翻译耗尽余额 → `no-overdraft` / `no-double-deduct`
- `M3-ledger-reconcile(ledger=975 consumed=975)` — 账本逐笔对账
- `T51-preview-math(89.7-8.97=80.73)` — 折扣算术精确校验
- `T52-audit-no-request-body` — 审计体不得含请求体（防密钥泄漏）

### 2.2 前端（Playwright 50 条 + vitest 224 条）

| 项 | 结果 |
|---|---|
| E2E 总计 | **48 passed / 2 failed**（49.1s，4 workers，`retries: 1` 复跑仍红） |
| 失败 | `assist_admin.spec.ts` 的 **A3**、**A5** |
| vitest | 224/224 全绿 |
| 覆盖域 | 冒烟 / 像素断言 / 移动端 / a11y(axe) / 十语种 / 暗色 / 工作台全链路 / 工单+编辑器 / 后台各面板 / USDT 收银台 / 租户标签 / 多语言 zip |

---

## 3. 三条红灯的逐条定性（附证据）

### 3.1 ⚠️ 根因背景：本分支从未经过 CI

`.github/workflows/ci.yml`：

```yaml
on:
  push:
    branches: [main, master]     # ← 只覆盖 main/master
  pull_request:
```

当前分支 `autosales` **不在触发器内**，且未提 PR。因此：
- `e2e` job（SQLite 全量 run_uat）从未运行
- `uat-pg` job（PG + pgvector 全量）从未运行
- `assist` job 从未运行

叠加 `assist_admin.spec.ts` 是**未跟踪文件**（`git ls-files` 判定 untracked），它从未进入任何闸门。**这解释了红灯为何能长期存在而无人察觉。**

### 3.2 环境假红：T28 分片上传（4 条断言）

**现象**
```
FAIL|T28-chunk-0|want["success":true]|got[{"message":"服务处理出现异常...","success":false}]
FAIL|T28-chunk-1|...
FAIL|T28-status-received|want["received":\[0,1\]]|got[{"received":null,...,"total":0}]
FAIL|T28-merge-ok|want[kbmerged_[0-9a-f]{12}\.csv]|got[{"message":"分片不完整（已收 0/0）","success":false}]
```

**服务端原文（`server.log`）**
```
.../能言/_uploads/_chunks/t28-1790000101-73005/tmp.part
 -> .../能言/_uploads/_chunks/t28-1790000101-73005/idx_00000.part: operation not permitted
```

**代码**（`internal/api/upload_chunk.go`）：先 `os.Create(dir/tmp.part)` 落盘，再**原子 rename** 为 `idx_NNNNN.part`（注释：「半写中断不会留下脏分片」，设计正确）。

**判定为环境假红的三条依据**
1. 错误是 `EPERM`，不是逻辑错误；
2. 同一逻辑的 Go 单测 `TestH5ChunkNamingAndCollection` 用 `t.TempDir()` 执行，**通过**（`internal/api` 包 ok）；
3. 沙箱拒绝清单明确列出该路径的 `file-write-unlink`（与项目已知环境特性「`~/Library/Application Support/**` 改名可能被拦截」完全吻合）。

**修复方向（供参考，本次未改）**：UAT 用 `UPLOAD_DIR` 指向 `$WORK` 临时目录即可消除假红——否则这条红灯会在每次本机跑闸门时复现，钝化对真回归的敏感度。

### 3.3 测试自身缺陷：A3 选择器歧义

```ts
// e2e/assist_admin.spec.ts:65
await page.getByText('知识库', { exact: true }).first().click();
await page.getByRole('button', { name: '新建' }).click();   // ← Timeout 12s
```

后台**侧边栏**也有「知识库」菜单项（A5 的页面快照可见：`button "知识库"` 与 `button "组织与成员"` 并列于侧栏）。`.first()` 命中的是侧栏菜单而非 `AssistP` 面板内的页签 → 点击后导航到 KB 管理面板、`AssistP` 卸载 → 「新建」按钮永不出现。

**结论**：测例 bug，非产品 bug。同一文件内的 A1/A4 用 `.first()` 均正常，因为其断言对象不重名。

### 3.4 ★ 真实产品缺陷：A5 —— AI 顾问菜单前后端权限口径不一致

**测试断言**（期望非超管看不到「AI 助手」入口）
```ts
const token = await login(page, 'uatuser_a', 'uatpass123');
const res = await request.post(`${BASE}/api/admin/assist/config`, {...});
expect(body.success, '非超管读写 assist 管理面必须被拒').toBeFalsy();   // ✅ 通过（后端确实 403）
await page.goto('/admin');
await expect(page.getByText('AI 助手', { exact: true })).toHaveCount(0); // ❌ 实得 1
```

**根因（两侧源码对照）**

| 侧 | 位置 | 门槛 |
|---|---|---|
| 前端菜单 | `components/admin/AdminDashboard.tsx:70` | `{ key:'assist', label:'admin.menuAssist', minLevel: 3 }` → **租户管理员可见** |
| 后端代理 | `internal/api/admin_assist_proxy.go:110 / :182` | `s.requireAdminUser(r)` → **仅 super_admin(4)** |

而注册默认角色即 `store.RoleTenantAdmin`（`internal/api/register.go:358`），所以普通注册用户（`uatuser_a`）**必然看到入口、点进去必然全线 403**。

**定性**：这是**真实可用性缺陷**（UI 提供了一条永远失败的通路），而非测试误报——测试期望（非超管不应看到）才是正确的一方。修法二选一：把 `minLevel` 提到 4，或把后端代理放宽到 `requireTenantAdmin` 并按租户隔离 assist 数据。**必须两侧同时定调，否则必然再犯。**

### 3.5 ★ 真实产品缺陷（NEW，2026-09-21 追加）：`.md` 工单标题中文未译

> 独立缺陷记录见 `缺陷记录_工单T20260921075004EF8_md标题中文未译_20260921.md`（编号 BUG-20260921-MD01）。

**工单**：`T20260921075004EF8` / `产品方案书_v5.1.md` / 英语 / 2026-09-21 07:50:04 / **状态=已完成**。
**现象**：译文文件里有大量中文；用户观察到的规律是「**`#` 后面的中文不翻译**」。

**先排除（重要负面结论）**：**Markdown 提取/回写不是根因**。实测把 `extractMarkdown → ExtractTexts → ApplyAlignedText` 串起来跑往返（假翻译器把每个键替换成 `EN<键>`），`# 标题`（井号**后有空格**）形态**全部正确**：`# 产品方案书 v5.1 → # EN<产品方案书 v5.1>`、`## 一、产品定位 → ## EN<一、产品定位>`、`### 1.1 核心能力 → ### EN<1.1 核心能力>`、`- 支持十二语种互译 → - EN<…>`、`1. 第一步 → 1. EN<…>`、`> 引用块 → > EN<…>` 均正常；正式回归 `internal/fileproc/d5_writeback_test.go` 也以 `# 标题` 为基准形态。

**交付口径（用户裁定，2026-09-21 23:50）**：译文产物 = **清洗脏数据** 后，**保留原文原格式**、内容为**对应目标语言**、
**文件名也翻译掉**。并明确裁定：**「代码围栏内不翻译」是缺陷，不是设计**（此条我先前判断错误，已更正）。

**六条真实根因**（RC-1 / RC-2 哪条是本次主因待日志确证；RC-3~RC-6 看产物即可确认。RC-5 / RC-6 为 2026-09-22 用原件实测后新增）：

| # | 根因 | 代码证据 |
|---|---|---|
| **RC-1** | **「无空格」写法让 `#` 进入送模型的键** → 模型把 `#标题` 当注释**原样回显** → 回显检测丢弃 → 硬闸 2 轮零进展熔断 → 写回"未命中保留原文" → **标题留中文**。且若翻译成功，`#` 会**被丢掉**（`pfx` 为空不粘回）——**两种结局都是错的** | `text_formats.go:188` 前缀正则要求标记后必须跟 `[ \t]+`；`:113` 匹配不上就整行入库；`:158` 回写取 `pfx` 同样匹配不上。**实测 20 种写法有 6 种把标记留在键里**（`#标题`/`##标题`/`###1.1 …`/`-列表项`/`1.编号项`/`>引用块`），全部是"无空格"形态 |
| **RC-2** | **KB 直配命中路径缺「同文/回显」守卫**（模型路径 4 处都有，KB 路径唯独没有）→ 若 KB/TM 存在「源文=译文」行（双语对照导入 / 术语自映射 / 历史污染），标题会被"命中"成中文；**且因键已写入 `langTranslations`，硬闸补漏会永久跳过该段**（`file.go:597`）→ **不重试、不计入未译数、不告警**，永久锁死 | `file.go:483` 无守卫 vs `:513/:555/:636/:653` 均有 `!= 源文`；`addTrans`（`:399-406`）裸写无校验。标题是**跨文档版本最易被 KB 精确命中**的段 |
| **RC-3** ★ | **代码围栏（```）内整块不翻译**（提取侧 + 回写侧**双重跳过**）→ 围栏内的 ASCII 图/示例/话术整块留中文。**且既有测试把该行为断言成了「正确」**，修好会让 CI 变红 | 提取 `text_formats.go:104-108`（`inCode` 时 `continue`，整段不入库）；回写 `:148-152`（`inCode` 永不替换）；注释 `:129` 自陈"围栏内…**永不替换**"。**测试固化**：`d5_writeback_test.go:44` `t.Fatalf("代码围栏内容被误翻译")` |
| **RC-4** ★ | **产物文件名完全不翻译**（零实现）→ 中文文件名照抄 | `file.go:708 baseName := strings.TrimSuffix(filepath.Base(filePath), …)`，其后 4 处产物命名（`:717 :728 :782 :1051`）全部直拼原名；`git grep -i "文件名\|filename\|translatedName"` 在非测试代码中**零命中** |
| **RC-5** ★★ | **行内标记被剥除 ⇒ 译文丢标记、丢内容**（2026-09-22 实测新发现）。`emphasisRe` 的替换串**只引用了 `$1`** → `**加粗**` 只丢标记；`*斜体*` / `__粗__` / `_斜_` / **反引号代码** / `~~删除~~` 因 `$2`–`$6` **未被引用而整段（含内容）被删掉**。**实测：`**` 381 → 0；反引号内 16 个技术标识符（`shrink_draft` / `full_len` / `^[^<].*` / `翻译系统成本速算表_最简版.xlsx` …）从产物中彻底消失**，且**无任何回填逻辑** | `text_formats.go:25` 正则有 6 个捕获组 vs `:116` / `:165` 的 `ReplaceAllString(t, "$1")`；`git grep emphasisRe` 仅命中这两处。**严重度高于 RC-1/RC-3**：不是"少翻一段"，而是**内容删除**，且用户看不出 |
| **RC-6** | **目录锚点不同步** → 15 行 `- [一、项目背景与目标](#一项目背景与目标)` 的链接文字译了、**锚点仍是中文** ⇒ 译文目录点不动。`ApplyAlignedText:175-177` 对整行唯一链接**有意保 URL**（对真实 URL 是正确的），但标题译完后锚点会变，二者失配 | `text_formats.go:175-177` `singleLinkRe` 只保 URL、不重算 slug。**性质是功能缺口，不是漏译** |

**★ 用原件实测的结果（2026-09-22，用户提供 `/Users/zhangzifei/Downloads/产品方案书_v5.1.md`，712 行 / 22,708 字）**：

| 指标 | 数值 |
|---|---|
| 源文含中文行 | **418**（围栏内 72 / 围栏外 346） |
| 产物仍含中文行 | **87** ⇒ **20.8%**（围栏内 72 = 真漏译；目录锚点 15 = RC-6） |
| 围栏块 | **10 块，0 块有语言标注**；3 块 ASCII 图（37 行中文）、3 块伪码、1 块 Prompt 模板、1 块**安全词正则** |
| `**` 标记 | **381 → 0** |
| 反引号内技术标识符 | **16 个被整段删除** |
| 围栏标记 / 表格 / 缩进 / `│ ┌ █` | 计数守恒 ✅ |

**★ RC-3 对「产品方案书」这类文档杀伤很大**：中文产品文档里的 ``` 块大多**不是代码**，而是
ASCII 流程图/架构图（`┌──┬──┐`）、示例对话、报错样例、配置样例——这些本就该翻，现在整块留中文。
本工单原件里 **3 块 ASCII 图 / 37 行中文**就是这么留下的（占围栏内 72 行的一半）。

**★ 但"围栏一律整块翻译"也不能无脑用**：该原件**第 9 块是安全词正则白名单**
（`高压|触电|禁止拆卸|…`，同附录 A.1）——**那些中文是匹配模式，翻了会让安全句硬闸静默失效**。
⇒ 这是围栏必须**分三类**（真源码 / 文本示意图 / 数据白名单）而不能只分两类的直接证据。
详见缺陷记录 §5.7.6。

**★ RC-3 的裁定口径（用户，2026-09-21 23:5x）**：不是"整块全译"也不是"整块不译"，而是
**「真代码不翻译、注释翻译成对应语言」**——围栏内按行分类：注释（`//`/`#`/`--`/`<!-- -->` 等，
按围栏语言标注分派）→ 翻译；代码语句/表达式/键名 → 逐字节保留；围栏标记与缩进骨架 → 永不改动；
**无语言标注 / `text` / `plain` 的围栏按"无代码概念"整块翻译**（产品文档最常见形态）；
`json` 因无注释语法 → 整块按代码保留。另需处理 7 类边界（指令白名单 `//go:build`/`# noqa`/shebang、
字符串内伪注释、行尾注释只翻注释段、跨行块注释保留 `/* … */` 标记、ASCII 图列宽对齐等），
完整表与实现要点见缺陷记录 §5.5。
**2026-09-22 修正**：因实测发现"**0/10 块有语言标注** + 1 块是安全词正则"，该口径需**升级为三类分派**
（缺陷记录 §5.7.6）；**遗留待确认**：ASCII 图内中文说明（翻会破坏列宽对齐）与"安全词正则类"的
保护方式（建议 ` ```regex ` / ` ```nolocalize ` 显式标记）。

**为什么状态是「已完成」而非失败**：漏译率硬闸只在 **>50%** 时判失败（`file.go:100-108 leakedLang`），
本工单实测漏译率 **20.8%** < 50% → **"大量中文" 与 "已完成" 可以同时成立**，用户拿到残缺成品却无
任何失败提示。**且没有任何闸门看"格式有没有丢"**——RC-5 那 381 处标记消失就是这样潜伏下来的。

**一步确证法**：把交付译文按**六类**数一遍——① 无空格的 `#标题` ⇒ RC-1（**本工单 0 命中**，§3.5 已复核）；
② 有空格的 `# 标题` 却没翻 ⇒ RC-2；③ ``` 块内整块中文 ⇒ RC-3；④ 产物文件名是中文原名 ⇒ RC-4；
⑤ **加粗/反引号标记整体消失 ⇒ RC-5**；⑥ 只剩目录锚点是中文 ⇒ RC-6。日志侧用
`[kb-match] … 命中=` 与 `[tm-hardgate] … 结束：仍有 N/M 段未译出` / `连续 2 轮零进展` 交叉判断；
KB 脏行用 `SELECT … FROM tm_segments WHERE en <> '' AND en = zh` 查。

**为什么这个缺陷能长期存在**：`internal/engine/file.go`（1,100+ 行的文件翻译主编排）与
`internal/fileproc/text_formats.go` 的这条链路**零回归测试**——与本报告 §4.1-5「编排装配层零测试」同源。
「三条写入路径只有两条有守卫」「围栏内不译被测试固化为正确」「行内标记被整段删除」这类问题，
**没有任何闸门能发现**。

---

## 4. 代码级架构评审

### 4.1 后端（Go 单体，89,965 行）

**优点（实测确认）**

| 项 | 证据 |
|---|---|
| 支付/计费幂等与并发安全 **做对了** | `store/billing.go:180` 条件更新 + `RowsAffected` 判定；`:1360` `MarkOrderPaid` 用 `status='pending'` 抢占；`store/quota_grants.go:203` `deductWithGrantsTx` 走 `FOR UPDATE` + `balance>=?` 守卫 + 3 次重读重试；SQLite 侧靠 `_txlock=immediate` |
| 并发有回归保护 | `quota_grants_concurrency_test.go`、`quota_grants_pg_concurrency_test.go`、`coupons_pg_concurrency_test.go`、`billing/quota_test.go:96` |
| USDT 对账链路完整 | 唯一键 `(chain,tx_hash,log_index)` 幂等 + 确认数闸 + 多命中转告警不盲入账 + 孤儿池 + 30 天开关默认关（`pay_usdt_watch.go`） |
| 密钥加密 | `internal/secret` AES-256-GCM + `enc:v1:` 版本前缀 + 平滑迁移历史明文 |
| 生产 fail-fast | `cmd/server/main.go:81/93/96/128/275` 五处启动强校验（缺密钥、SQLite+非回环、Redis 不可用、PG 初始化失败一律拒绝启动） |
| 双方言 | `internal/db/rewrite.go:27` 以 SQLite 为唯一真源做运行时 SQL 翻译 |

**问题（按严重度）**

1. **🔴 `internal/api` 上帝包**：76 个生产文件 / 24,426 行 / **270 个 handler 函数** / **474 个 `*Server` 方法**（含 5 个仅存于测试文件的共 479）/ 268 路由 / **949 处直接 `s.Store.*`**。`server.go:51-79` 全部字段是**具体指针类型**（`*store.Store` 等），无接口抽象 → 无法 mock、测试必须拉真实 store、单文件最大 1,133 行（`tickets.go`）。
2. **🔴 租户隔离无结构性收口**：`server.go:647` 未登录请求**强制注入租户 1（rox）**，`currentTenant`/`effTenant` 兜底 `return 1`（`server.go:667`；另有 `:863`）。即**新增 handler 若漏写 `authUser`，不会 401，而是静默以 rox 身份读写**。`server.go:832` 注释自陈这正是历史 P0-2 事故。实测 270 个 handler 中 32 个体内无鉴权调用，逐一核对**当前全部合法**（公开页/自鉴权入口/fail-closed），但这是「每次都要记得写」的脆弱模式。
3. **🔴 `internal/store` 439 个导出方法全部无 `context.Context`**（实测 439/439 = 0；该包生产文件 49 个 / 14,086 行）。HTTP 取消/超时/trace_id 无法到达任何 DB 调用；客户端断连后计费扣减与工单落库仍执行到底。
4. **🟠 分层倒置 3 处**：`internal/infra/redis/notify.go:12` import `internal/queue`（基础设施层依赖业务队列）；`store/kbpackages.go:22`、`store/persona_scope.go:12` import `internal/kb`（持久层依赖业务域）；`engine/engine.go:30` import `internal/billing`。
5. **🟠 质量保障「装配层零测试」**：`grep runGate|runQA|runKBMatch|runReview|runCultureGate|.Execute(ctx)` 在全部 `*_test.go` 中**零命中**。即「决定放行/重翻/转人工」的编排接线无回归保护（叶子函数有测，见 §4.4）。
6. **🟠 22/52 包零测试（42%）**，含安全敏感包：`internal/secret`（136 行 AES-GCM）、`internal/infra/concurrency`（230 行）、`internal/infra/ratelimit`（80 行）、`internal/infra/redis`（597 行）、`internal/tenant`（600 行）、`internal/evals`（198 行）。
7. **🟡 SCIM Token 明文入库**（`store/scim.go:29` `token TEXT`，查询 `WHERE token=?`），与仓库其它密钥统一走 `EncryptSecret` 的口径不一致。令牌 48-hex 高熵故非高危，但 DB 读权限即等价活凭证。
8. **🟡 零 `Benchmark`**（`grep '^func Benchmark'` = 0），而存在性能敏感路径（3 路并发翻译、扣费事务、pgvector 回退全表扫描）。
9. **🟡 `log.Printf` 存量未迁移，且棘轮闸门只覆盖 72%**：`internal/` 生产代码 176 处（棘轮基线恰为 176，实测**恰等于基线**故闸门通过且不打印「已下降」提示）；但 `cmd/` 另有 **69 处**（`cmd/server/main.go` 独占 40）**从未被扫描**——`logratchet_test.go` 的 `filepath.Walk` 硬编码只走 `internal/`。**真实生产存量 = 245 处，闸门仅守住 176/245 ≈ 72%**，新增 `cmd/` 日志不受任何约束（附录 A-#5）。
10. **🟡 后台协程 `os.Exit`**：`api/watchdog.go:139` 自愈重启不执行优雅停机 → `billing` 最终 flush 不会跑。

### 4.2 前端（React + TS + Vite，61,165 行）

**优点（实测确认）**
- 路由级 `React.lazy` 11 处；zustand v5 单一 store + 按字段拆订阅（已修「每 token 全页重渲染」）
- **定时器与事件监听清理 100% 配对**（5 处 `setInterval` 全有 `clearInterval`；20 处 `addEventListener` 全有 `removeEventListener`）
- i18n 有真闸门：`locales.core.test.ts` 校验「键集全等 + 占位符一致 + 非 CJK 语种零汉字残留 + 繁中零简体残留」；`parity.test.ts` 38 用例
- 有源码级可读性闸门 `styles/readability.test.ts`（字号覆写 + WCAG 对比度）
- 有跨仓审计文案棘轮 `i18n/auditActionCoverage.test.ts`（扫 Go 侧 `LogAudit` 字面量求差集，只减不增）
- a11y 实测仅 1 处真缺口（`TicketsPage` 自绘弹窗缺 `role="dialog"`/Esc/焦点管理）

**问题（按严重度）**

1. **🟠 blob URL 泄漏 3 文件 / 4 处**：`MessageBubble.tsx:108`（create 1 / revoke 0）、`ReferralP.tsx:53`（1/0）、`PlansP.tsx:399/416`（2/0）只 `createObjectURL` 无 `revokeObjectURL`；逐文件全量扫描已确认其余 8 处**全部配对**（含 `ChatWindow.tsx` 的「延迟 5s 释放」正确写法、`api/kb.ts`、`admin/panels_a.tsx` 3/3）。
2. **🟠 19 处绕过统一 client 的裸 `fetch`**（原文 13 处为漏计）：`branding.tsx:175`、`App.tsx:46`、`LangMultiSelect.tsx:105`、`panels_a.tsx:560`、`PlansP.tsx:399/416`、`MessageBubble.tsx:108`、`api/kb.ts:21/210`、`api/referral.ts:65`、`api/admin.ts:20`、`api/assist.ts:79/86/97/106`、`api/billing.ts:208`、`api/tickets.ts:139`，另有 `api/core.ts:107`（即统一 client 自身，正当）与 `api/translate.ts:118`（SSE 流式，必须裸用）。**剔除后实际绕过 17 处**；其中文件内**无 401 处理**的 7 处：`api/tickets.ts:139`、`api/referral.ts:65`、`api/admin.ts:20`、`api/assist.ts` ×4。错误形态三套并存（`ApiError` / 裸 `Error` / `{success:false}`）。
3. **🟠 403 完全无拦截**（`core.ts:112` 只判 401；全仓 `403` 仅出现在 1 个测试 mock 中）。
4. **🟠 靠中文串做业务判定**：`MessageBubble.tsx:203-205` 用 `mode.includes('精确命中')` 决定徽章——后端改文案即静默退化为默认态。
5. **🟠 `any` 泛滥 624 处**（复核实测：`any` 令牌 **231** 次 + `Any` 别名令牌 **393** 次；原文 618/225 为旧口径），`type Any = Record<string, any>` 在 **16 个文件里各自重复定义**（复核实测恰 16 处：`KbUploadDialog` / `admin/{ApiKeysP,AssistP,CouponsP,KbP,ModelsP,OrgP,PlansP,ReconcileP,ReferralP,TaskCenterP,TenantsP,TicketsP,WebhooksP,WorkflowP,panels_a}`）；`panels_a.tsx:32` 注释自陈「统一用 any 承接」；重灾区 `PlansP.tsx`（99 行含 Any）、`KbP.tsx`（57 行）；甚至把 `any` 用在 CSS 对象上（丢掉 `React.CSSProperties` 校验）。
6. **🟠 无统一 loading / 无统一重试**：20+ 份各自 `useState(loading)`；重试仅 3 处硬编码。
7. **🟡 样式体系四重冗余（实为「三活一死」）**：`--lc-*`（825 次引用 / 61 个变量）+ `--adm-*`（230 / 17）+ `--npz-*`（80 / 6）+ **`--td-*`（theme.css 定义 52 处，真实 `var()` 引用仅 1 处且**在注释里** —— 死层）**；同一视觉概念被 3~5 套变量表达；内联 `style={{` **903 处**。`theme.css:1-13` 自陈这是「换肤低成本」的刻意技术债。
8. **🟡 巨型组件**：`Landing.tsx` 1746 行、`admin/PlansP.tsx` 966 行（单组件 ~40 个 state，承载 7 个业务域）、`admin/panels_a.tsx` 850 行（一个文件导出 7 个面板）、`admin/KbP.tsx` 781 行。
9. **🟡 入口 chunk 2.13 MiB**（`index-*.js`）：根因是 `i18n/index.ts:54-66` **静态 import 全部 12 份词典**，无非 ASCII 字符占 17.2%。
10. **🟡 僵尸依赖 `tdesign-react`** 仍在 `dependencies`（`package.json:18`），`src` 中 3 处提及**全为注释**，**0 处真实 import**，产物中不含。
11. **🟡 测试盲区**：`App.tsx`(395)、`hooks/useChat.tsx`(356) SSE 编排、`stores/*`、13 个后台面板（含 781 行的 `KbP.tsx`）**零单测**。

### 4.3 AI 顾问链路（`internal/assist`）

**架构**：**独立二进制 + 独立 SQLite + 独立 LLM 客户端**（`cmd/assist-server` 不 import 任何主服务业务包）→ 与主服务**只能经 HTTP 通信**。主后台通过 `internal/api/admin_assist_proxy.go`（224 行）做**白名单同源反代**：

| 设计点 | 实现 | 评价 |
|---|---|---|
| 路由白名单 | `assistProxyRoutes` 显式 7 条映射，**不做通配前缀代理**（注释：「避免把主后台变成任意内网 HTTP 中继」） | ✅ 正确 |
| 管理 Token 处置 | 服务端注入 `X-Assist-Admin` 头；转发前 `q.Del("admin_token")`；响应体限读 4MB | ✅ Token 全程不出后端进程 |
| 上游基址 | env > DB > 默认；scheme 非 http/https 一律回落（防 `file://` 注入） | ✅ 正确 |
| fail-closed | 无 Token 时**拒绝转发**，绝不静默返回假数据 | ✅ 正确 |
| 审计 | 写操作记 `assist_admin_write`，**detail 不含请求体**（防 llm_api_key 明文落审计） | ✅ 正确 |

**AI 顾问 UAT 38/38 全绿**，覆盖：开场白/chips、话术直配、流程推进、历史鉴权（无 token 401 / 坏 token 401）、同义词归一命中（「充钱」→「充值」）、零命中兜底引导 + 未答问题登记、管理面白名单 400、密钥掩码不泄漏、掩码回写跳过、LLM 配置热加载生效、DB Token 桥接与环境变量优先级。

**能力缺口（真实）**：检索为**纯关键词子串计数 + 同义词表归一**（`engine.go:122 hitScore` / `:152 synonymHit`），**无 embedding、无向量召回**（复核实测：`internal/assist` 全包 `grep embedding|vector` **零命中**）。对比同仓翻译知识库已具备 pgvector（`kb/db.go:122` `vector(1024)` + HNSW 索引 + `:1146 VectorSearch`），**AI 顾问的召回能力明显落后于翻译侧** —— 用户换一种说法提问即可能零命中（虽有兜底引导与未答问题登记闭环缓解）。

其余机制设计良好：复合意图让位（`:499 compoundIntent`，命中话术但知识库有跨领域条目时降话术为素材交给 LLM 融合）、零命中记 `unanswered_questions`（去重上限 200，供运营补料）、LLM 热加载指纹比对。

### 4.4 翻译质量链路

**工单流水线 10 步**（`internal/orchestrator/workflow.go:55 registerSteps`）：

```
kb_match(四层查找+术语子串) → ai_initial(初翻+术语注入)
  → evals_initial(Judge 评估) → review(审校 Agent)
  → evals_review → gate(ConstraintGate 8 项硬校验)
  → culture_gate(语言文化包反查) → qa(确定性质检)
  → approval(人工审批) → feedback(自迭代写 KB)
```

**质量保障机制与断言覆盖（实测）**

| 机制 | 实现 | 单测 |
|---|---|---|
| 后处理清洗 | `engine/postprocess.go`（630 行）：伪标签剥离、注释截断、退化重复折叠、品牌替换、非目标语汉字剥离 | ✅ 12 个 Test |
| 确定性 QA | `qa/qa.go`（203 行）：空译/同文/数字集合/占位符/长度/标点 六规则 | ✅ 10 + 1 Test |
| 硬闸 8 项 | `gate/gate.go`：非空 / 无源语言残留 / 无乱码 / 数字保持 / 不压缩过半 / 无回环复读 / 无 `<sN>` 残留 / 书写系统合理性 | ✅ 7 Test |
| KB 术语强制 | `engine/gates.go:39 termReqs` → 命中术语未体现即判不通过（`极石→ROX` 案例） | ✅ 间接覆盖 |
| 重试队列 | `engine.go:826 translateLangsConcurrent`：3 轮、失败排尾、2s 退避、`sleepCtx` 可取消 | ✅ 5 Test |
| 评估器 | `evals/evals.go`：低于阈值打标「质检存疑」+ 告警（同单同语言幂等） | ❌ **零测试** |
| **编排装配** | `runGate/runQA/runKBMatch/runReview/runCultureGate/Executor.Execute` | ❌ **零测试** |

**结论**：翻译质量机制的**零件做得相当细**（比多数同规模项目细），但**装配层（谁决定重翻、谁决定放行、跳过条件）没有任何回归断言**（复核实测：`runGate|runQA|runKBMatch|runReview|runCultureGate|Executor.` 在全部 `*_test.go` 中命中数恰为 **0**）。另有两个关键保护**确实有测试**（原报告误记为零测试，已更正）：`engine/hedge.go`（对冲竞速路由）与 `engine/placeholder.go`（占位符保护 `maskPlaceholders/unmaskPlaceholders`），二者各有 **4 个 Test**（`hedge_test.go` / `placeholder_test.go`）。

质量链路的端到端断言确实存在且本次全绿：`T46`（质检字段透出）、`T47`（逐段对照真值表 `seg0-source-truth`）、`T48`（伪标签清洗 `no-pseudo-residue`）。

---

## 5. 「字节级 / 像素级」口径的实测校正

用户要求「字节级别全场景全流程分支」测试。实测口径如下——**部分成立，需要区分**：

| 层 | 真实断言强度 | 证据 |
|---|---|---|
| **后端↔后端** | ✅ **真字节级** | 直接查库比对（`A1b-lead-db` `SELECT COUNT(*)=1`）、金额字节相等（`A9-pay-idempotent(999900==999900)`）、精确正则（`kbmerged_[0-9a-f]{12}\.csv`）、账本逐笔对账（`ledger=975 consumed=975`） |
| **前端↔后端** | ✅ **真字节级（部分）** | Playwright 直连真实后端；**SDK 的 multipart 报文字节级结构测试**（`createFileTask`：boundary 闭环/文件名/二进制透传/表单字段）是真正的字节级契约断言 |
| **前端↔前端** | ⚠️ **DOM 级，非字节/像素级** | vitest 224 条为组件 DOM 断言 |
| **「像素级 UAT」** | ❌ **名不副实** | 全仓 `toHaveScreenshot` / `toMatchSnapshot` / `toMatchImageSnapshot` **零命中**。`pixel_uat.spec.ts` 实为「元素可见 + 文本包含 + 字号阈值 + `scrollWidth<=clientWidth` 溢出检查」+ `page.screenshot()` **留档**。**没有像素基线，因此无法检出视觉回归** |

**空白分支（实测未覆盖）**：SSE 流式的**中断/重连**分支、限流 429 的**客户端退避**行为、并发下单的**前端竞态**、多实例下的**前端会话迁移**。E2E 覆盖 `48 passed` 但全部为单实例、正常路径为主。

---

## 6. 系统还缺什么（缺口清单，按优先级）

### P0 — 阻塞可信发布

| # | 缺口 | 证据 / 影响 |
|---|---|---|
| 1 | **CI 未覆盖当前开发分支** | `ci.yml` 仅 `main/master` + PR；`autosales` 上 184 个未提交改动 + 45 个未跟踪文件（含新 spec）从未过闸门 → 红灯可长期潜伏（本报告 §3 即实例） |
| 2 | **代码风格门禁在整条链路上都不存在，而 9 个文件不合格** | 全仓检索 `gofmt`：`ci.yml` 零命中、`AGENTS.md` 零命中、`scripts/`+`deploy/` 零命中，仅 `PROGRESS.md` 有 3 处描述性提及——**即 gofmt 从未被任何闸门约束**（原文「AGENTS.md 声明 CI 跑 gofmt」系误记，已更正）。实测工作区 **9 个文件** `gofmt -l` 不合格，其中 **7 个在 HEAD 就已不合格**（`estimate.go`/`points_view_test.go`/`referral.go`/`crawler.go`/`iam/models.go`/`persona_dict.go`/`persona_scope.go`），另 `admin_assist_proxy.go` 为新增文件 |
| 3 | **assist 菜单前后端权限口径不一致** | `AdminDashboard.tsx:70` `minLevel:3` vs `admin_assist_proxy.go:110` `requireAdminUser`(4)。租户管理员可见但必然 403（本次唯一真实产品缺陷） |
| 4 | **UAT 闸门含环境假红** | T28 因本机沙箱拦截 `Application Support` 改名而恒红；应用 `UPLOAD_DIR` 指向临时目录，否则真回归会被假红淹没 |
| 5 | **日志棘轮只守住 72%** | `logratchet_test.go` 硬编码 `filepath.Walk(internal)`，`cmd/` 的 **69 处** `log.Printf`（`cmd/server/main.go` 40 处）不受约束；真实存量 245 vs 受守 176 |

> **P0 追加（2026-09-21 晚间，用户提交缺陷，不参与上方编号以免打乱既有引用）**
> **BUG-20260921-MD01 · `.md` 工单翻译质量族（6 条）**（工单 `T20260921075004EF8`）：
> ① 结构前缀正则不支持「无空格」写法（`#标题` 把 `#` 带进送模型的键，且翻译成功会丢 `#`）
> —— **实测本工单 0 命中，排最后**；
> ② KB 直配命中路径缺「同文」守卫，坏 KB 行会把中文当译文**永久锁死且不告警**；
> ③ **代码围栏（```）内整块不翻译**（提取+回写双重跳过，且被 `d5_writeback_test.go` 断言为正确）
> —— 原件 10 块 / 72 行中文受影响，其中 3 块 ASCII 图共 37 行是"该翻没翻"；
> ④ **产物文件名不翻译**（零实现）；
> ⑤ ★ **行内标记被剥除致丢标记丢内容**（2026-09-22 实测新增，**严重度最高**：
> `**` 381→0、反引号内 `shrink_draft` / `full_len` / `翻译系统成本速算表_最简版.xlsx` 等
> **16 个技术标识符被整段删除**，且**用户看不出**——没有任何闸门检查"格式有没有丢"）；
> ⑥ 目录锚点不同步（15 行 `#中文锚点`，译文目录点不动，低优先）。
> 交付口径（用户裁定）：**清洗脏数据 → 保留原文原格式 → 内容为目标语言 → 文件名也翻译**。
> **另注**：原件第 9 块是**安全词正则白名单**（附录 A.1），**不可翻译**——
> 这是围栏政策必须**分三类**（真源码 / 文本示意图 / 数据白名单）的直接证据。
> 建议与 A5 同批修；修复顺序 **RC-5（内容在丢，最急）→ RC-2（2 行）→ RC-3+RC-5 合并做（同一条写回链）
> → RC-4 → RC-6 → RC-1**。详见 **§3.5** 与
> `缺陷记录_工单T20260921075004EF8_md标题中文未译_20260921.md`（含 §5.7.6 原件围栏实测清单）。

### P1 — 架构健康度

| # | 缺口 | 影响 |
|---|---|---|
| 6 | `internal/api` 无接口抽象（949 处直连 store） | 无法 mock；改一处回归面巨大；单文件 1,133 行 |
| 7 | 租户隔离无结构性收口 + 未登录兜底租户 1 | 新 handler 漏写鉴权 → 静默以 rox 身份读写（历史 P0-2 事故同源） |
| 8 | store 层 439/439 导出方法无 ctx | 请求取消/超时/trace 断链；断连后仍扣费写库 |
| 9 | 编排层零集成测试 | 「重翻/放行/转人工」决策无回归保护 |
| 10 | 22/52 包零测试（含 `secret`/`ratelimit`/`concurrency`/`redis`/`tenant`/`evals`） | 安全与并发基座无测试网（复核实测恰 **22/52**） |
| 11 | 无像素基线比对 | 视觉回归不可检出，全靠人眼 |
| 12 | 无 Benchmark | 性能回归不可量化（3 路并发、扣费事务、pgvector 回退全表扫描均有风险） |
| 13 | store 巨型文件行数失控 | **HEAD 上 `billing.go` = 2,201 行（与文档基线一致、仍合规）**，但**工作区已被改到 2,206 行（+5）**——即约束在未提交改动中已被打破，而 HEAD 尚合规；`kbscrape.go` 1,105 行文档未记；「只减不增」无任何测试守护（复核更正：原文「已打破」应限定为工作区） |

### P2 — 前端债与体验

| # | 缺口 |
|---|---|
| 14 | blob URL 不 revoke：**3 文件 / 4 处**（`MessageBubble`、`ReferralP`、`PlansP` ×2），泄漏随会话线性增长 |
| 15 | **19 处**裸 fetch（剔除 client 自身与 SSE 后**实际绕过 17 处**），其中 **7 处**无 401 处理；403 无统一拦截 |
| 16 | 靠中文串做业务判定（后端改文案即静默失效） |
| 17 | `--td-*` 死变量层（theme.css 定义 52 处，真实 `var()` 引用仅 1 处且**在注释里**）+ `tdesign-react` 僵尸依赖（src 3 处提及全为注释，0 真实 import） |
| 18 | 入口 chunk 2.13 MiB（12 份词典静态全量 import，应改按语言懒加载） |
| 19 | **624** 处 `any` 令牌（`any` 231 + `Any` 393），16 份重复的 `Any` 别名定义 |
| 20 | `App.tsx` / `useChat` / `stores/*` / 13 个后台面板零单测 |

### P3 — 产品与商业缺口

| # | 缺口 | 说明 |
|---|---|---|
| 21 | **AI 顾问无向量召回** | 纯关键词匹配；同仓翻译 KB 已有 pgvector，能力不对称；用户换个说法即可能零命中 |
| 22 | **无 LLM 质量基准（golden set）** | 有 QA/闸门等**规则**保障，但缺「翻译质量随模型/提示词变更的**可量化基准**」——无法回答「这次换模型质量是升是降」 |
| 23 | **无容量/SLA 模型** | 仓库有 `deploy/loadtest`、`容量预告与超卖预案`，但代码层零 Benchmark 与并发上限的数值化验证 |
| 24 | 缺灰度/回滚机制 | 未见 feature flag / 版本旁路（`mode_override` 是工单级，非发布级） |
| 25 | 缺合规证据链 | 多租户 + 企业客户场景下，未见等保/SOC2/GDPR 类审计材料；SCIM Token 还明文入库 |
| 26 | 可观测性有基础但未闭环 | `/metrics` + slog + trace_id + SLO/burn rate 已具备；但 `log.Printf` 真实存量 **245 处**（internal 176 + cmd 69，后者不受棘轮约束），错误路径 trace 断链 |
| 27 | 真实 LLM 端到端未验证 | 本次 UAT 全程用 `mock_llm`；生产 LLM 的**质量、时延、成本、限流退避**均无自动化证据 |

---

## 7. 架构设计评价（分项打分）

| 维度 | 评分 | 依据 |
|---|---|---|
| **资金安全与幂等** | **9 / 10** | 条件更新 + `RowsAffected` 抢占 + 行锁 + 3 次重读重试 + 并发回归测试；USDT 对账有唯一键幂等/确认数闸/多命中告警/孤儿池，且默认关闭 |
| **安全基座** | **7 / 10** | JWT + TokenVersion 会话撤销 + AES-GCM + fail-closed 代理 + SSRF 收敛 + CORS 前提自洽；扣分于租户隔离无收口、SCIM 明文 Token、`secret` 零测试 |
| **测试工程** | **7.5 / 10** | 570 条 shell 断言 + 224 vitest + 50 E2E + 610 个 Go Test + 21 SDK 用例，断言质量高（查库/精确正则/对账）；扣分于编排层零测试、42% 包零测试、无 Benchmark、无像素基线、CI 未覆盖分支 |
| **分层与模块化** | **5.5 / 10** | `api` 上帝包 + 无接口抽象 + store 无 ctx + 3 处分层倒置 + `service` 层形同虚设（仅 1 文件 / 13 处调用） |
| **前端工程** | **7 / 10** | 骨架健康（单 store / 路由 lazy / 清理 100% 配对 / i18n 与可读性真闸门 / 12 语种 2710 键全量覆盖）；扣分于四重样式变量、624 处 `any`、巨型组件、19 处裸 fetch、无视觉回归 |
| **可运维性** | **7 / 10** | 生产 fail-fast、双实例、PG+pgvector+Redis、systemd/caddy/多可用区/备份演练/冒烟脚本齐备；扣分于日志口径未收口、无灰度、无 Benchmark |
| **文档一致性** | **4 / 10** | 文档量与质量罕见地高，但**多处与代码不符**（见 §9）——**文件行数（`billing.go`）、i18n 键数、store 文件数、gofmt 门禁表述**四处失实 |
| **综合** | **7.0 / 10** | 一句话：**「关键路径做得很扎实、工程质量高于平均水准，但架构分层与隔离收口明显欠账」** |

---

## 8. 商业性价值评估

### 8.1 交付形态（实测，远超「一个翻译 API」）

| 面 | 实测内容 |
|---|---|
| 语种 | 12 语种 UI（2710 键全量词典，含阿拉伯语 RTL）+ 翻译多语言（zh/en/ja/ko/ru/ar/th/kk/zh_hant 等） |
| 翻译入口 | 即时翻译（SSE 流式）/ 文件翻译（docx·pptx·pdf·csv·txt，含版式回填三级链）/ 工单双模式 / OpenAPI / 浏览器扩展 / VS Code 扩展 |
| SDK | **TypeScript / JavaScript / Python / Java 四语言**，含行为级测试 |
| AI 层 | AI 顾问（销售+客服，话术/流程/知识库/功能入口四类对象，独立服务+管理台）、AI 初翻+校对、Judge 评估、审校 Agent，均**融合进计费链路** |
| 知识库 | 企业包/行业包四层查找 + 术语库 + 品牌术语自动种入 + TMX 导出 + **pgvector 语义检索（HNSW）** |
| 商业化闭环 | 积分计费 + 套餐订阅 + 自动续费（T-3 建单）+ **优惠券（试算/核销/门槛/限用/抢完/删模板留流水）** + 三级裂变邀请 + 发票（含冲红/重开）+ **退款按未消耗折算** + USDT(TRC20/ERC20/BEP20) + 微信 + 支付宝 + 对账 + 告警 |
| 企业能力 | 多租户 + RBAC(4 级) + 部门墙 + **SCIM 2.0 全生命周期** + SSO + 审计 + Webhook + 用量 CSV + 预算墙 + 敏感词 + 品牌定制(域名) |
| 运营工具 | 对账台、SLO/burn rate、漏斗（含二级裂变）、未答问题清单、任务系统、告警中心、系统设置 |

### 8.2 可商业化的三点硬支撑

1. **能收钱，且收得住**：并发扣费不透支、支付幂等、退款折算、发票冲红、USDT 自动对账——本次 566 条后端断言中资金相关全绿（除环境假红）。
2. **能交付**：四语言 SDK + 浏览器/VS Code 扩展 + OpenAPI + Webhook + SCIM/**SSO**，具备进企业 IT 白名单的技术面。
3. **能规模化**：多实例共享库已验证（8/8 绿，含跨实例 JWT 与「不双扣」），PG + Redis + pgvector 生产栈就绪，部署件（systemd/caddy/多可用区/备份演练）齐备。

### 8.3 商业化成熟度评级：**「可试运营，未达企业级 SLA 交付」**

| 门槛 | 状态 |
|---|---|
| 收单/收款/开票/退款 | ✅ 具备且有断言 |
| 计费与用量可信 | ✅ 有对账与账本断言 |
| 多租户与权限 | ⚠️ 功能具备，但隔离无结构性收口（P1-#7） |
| 质量可承诺 | ⚠️ 有质量闸门，但**无质量基准数字**，无法对外承诺「翻译质量指标」 |
| 容量可承诺 | ❌ 无 Benchmark / 无容量模型数值 |
| 可灰度可回滚 | ❌ 未见发布级开关 |
| 合规证据 | ❌ 未见等保/SOC2 材料；SCIM Token 明文 |

### 8.4 商业化建议（按投入产出排序）

1. **补 AI 顾问向量召回**（复用现成 pgvector 基建）——AI 顾问是销售转化入口，召回率直接挂钩留资转化率，是当前 ROI 最高的一处能力补齐。
2. **建立翻译质量 golden set 与评分基线**——把「质量」从「有闸门」升级为「有数字」，这是 To B 报价与 SLA 谈判的前提。
3. **把 CI 触发器扩到所有分支（或强制 PR）**——当前闸门对本分支零防护，是最大的系统性风险。
4. **修 A5 权限口径 + 给 `--td-*`/`tdesign-react` 做清理**——低成本的可用性与体积收益。
5. **补 3~5 个 Benchmark + 一次真实 LLM 端到端冒烟**——把「能扛多少、质量多少」变成可复现数字。

---

## 9. 与既有文档的偏差（文档已过时点）

| 文档声明 | 实测 | 判定 |
|---|---|---|
| **（原报告误记）** `AGENTS.md` 声明 CI 跑 `test -z "$(gofmt -l internal cmd)"` | 全仓检索：`AGENTS.md` **零处**提及 gofmt（其闸门只有 `go build`/`go vet`/`go test -race`）；`ci.yml` **零处** gofmt（但有 `go vet`）；`scripts/`+`deploy/` 零处 | ⚠️ **原报告为误记并已更正**：不存在「文档声明的 gofmt 门禁」；真实缺口是**整条链路都没有 gofmt 门禁**，而 9 个文件不合格 |
| `AGENTS.md`：`billing.go`（2201 行）只减不增 | **HEAD = 2,201 行（合规）**；工作区 **2,206 行**（`git diff` = `+5`，未提交） | ⚠️ **原报告「已打破」需限定**：HEAD 仍合规，仅工作区越界；且行数约束**无任何测试守护** |
| `AGENTS.md`：`internal/store/` 为「74 文件 / 约 16.8k 行」 | 实测 **88 个 `.go`**（49 生产 + 39 测试）/ **20,300 行**（生产文件 14,086 行） | ❌ 文件数与行数均失实（三种口径都对不上 74/16.8k） |
| `AGENTS.md`：i18n **2532 键**全量覆盖 | 实测 **2710 键**（10 份 locale 均 2710，闸门断言通过） | ❌ 少记 178 键 |
| 文档：前端 vitest 180 / Playwright 35 | 实测 **224** / **50**（13 个 spec 文件） | ❌ 计数过时 |
| `run_uat.sh` 注释：「前端像素级 UAT」 | `toHaveScreenshot`/`toMatchSnapshot`/`toMatchImageSnapshot` **零命中** | ⚠️ 表述夸大，实为 DOM 断言 + 截图留档 |
| 「AI 顾问 RAG 改造」（`核实报告_AI顾问缺陷与RAG改造_20260916.md`，文件确在） | `internal/assist` 全包 grep `embedding`/`vector` **零命中**；检索仍为 `engine.go:122 hitScore`/`:152 synonymHit` 关键词计数 | ⚠️ 若指向量 RAG，则未落地；翻译侧 KB 才有 pgvector（`kb/db.go:122` `vector(1024)` + HNSW + `:1146 VectorSearch`） |

**未验证项（本报告诚实边界）**
- 真实 LLM（非 mock）下的翻译质量、时延、成本；UAT 全程 mock。
- SQLite 与 PostgreSQL 迁移是否真正等价（`db/rewrite.go` 仅 5 类机械改写，`PRAGMA` 显式不在范围，无对拍测试）。
- `internal/db/jsonops.go` 的 `panic` 是否可由请求参数触达（未追全部调用点）。
- `/openapi/*` 下是否真无 Cookie 鉴权端点（CORS 无条件反射的前提）。
- T28 在 **CI（ubuntu）** 上是否确实转绿——本机两次运行均被同一路径限制拦截，未能取得本机绿证。

---

## 10. 一句话总结

这是一个**工程质量显著高于同类项目平均水平、但架构分层与隔离收口明显欠账**的复合体：**资金与安全的关键路径（幂等扣费、行锁并发、支付状态机抢占、USDT 对账、密钥加密、生产 fail-fast）做得扎实且有并发回归测试**；但它同时是**「270 个 handler 无接口抽象的上帝包 + 439 个无 ctx 的 store 方法 + 无结构性收口的租户隔离 + 装配层零测试的质量链路 + 从未覆盖当前分支的 CI」**。本次全流程字节级 UAT 实测 **566/570 条后端断言、48/50 条前端 E2E 通过**，其中**仅 1 条是真实产品缺陷（AI 顾问菜单前后端权限口径不一致），1 条是测试选择器 bug，4 条是本机沙箱造成的环境假红**——**功能面是通的**；而它最该补的不是功能，而是**质量基准数字、容量数字、以及把闸门真正架到当前分支上**。

---

## 附录 A — 二次源码复核勘误表（2026-09-21）

> **复核目的**：回答「本报告每条缺陷/缺口是否真实」——把 §0/§4/§6 里每一个可量化的论断，
> 全部回源码重新取证一次。方法：`git grep` / `gofmt -l` / `go list` / 逐文件 `wc -l` + 实跑棘轮测试，
> 并**对每条更正同时给出「错在哪」与「正确值」**（凡「确认」均已在正文就地保留原值）。
> **复核未改动仓库任何文件**（只读）。

### A.1 结论：确认成立（原报告无需改动，逐条取证）

| # | 论断 | 复核证据（实跑） |
|---|---|---|
| C1 | `internal/api` 上帝包：76 生产文件 / 24,426 行 / 270 个 handler / 268 路由 | `ls internal/api/*.go \| grep -v _test = 76`；`wc -l = 24426`；`^func \(...\*Server\) handle = 270`；`mux.HandleFunc( = 268` |
| C2 | 单文件最大 1,133 行 | `wc -l` 排序首位 `internal/api/tickets.go = 1133` |
| C3 | `internal/store` **导出方法 439 个，全部无 `context.Context`** | `find internal/store -name '*.go' ! -name '*_test.go'` 后 `^func (...*Store) [A-Z]` = **439**；其中含 `context.Context` = **0** |
| C4 | 22/52 包零测试（42%） | `go list ./... \| wc -l` = **52**；逐包查 `*_test.go` 缺失 = **22**（含 `secret`/`infra/redis`/`infra/concurrency`/`infra/ratelimit`/`tenant`/`evals`/`auth`/`config`/`errors`/`notify`/`openapi` + 9 个 `cmd` + 3 个 `assist` 子包） |
| C5 | 零 `Benchmark` | `git grep '^func Benchmark' -- '*.go'` = **0** |
| C6 | 编排装配层零测试 | `git grep 'runGate\|runQA\|runKBMatch\|runReview\|runCultureGate\|Executor\.' -- '*_test.go'` = **0** |
| C7 | 租户兜底：未登录强制租户 1 | `server.go:647 ctx = tenant.WithTenant(ctx, 1)`；`:667` 与 `:863` 各一处 `return 1` |
| C8 | 分层倒置 3 处 | `infra/redis/notify.go:12` → `internal/queue`；`store/kbpackages.go` + `store/persona_scope.go` → `internal/kb`；`engine/engine.go:30` → `internal/billing` |
| C9 | SCIM Token 明文入库 | `store/scim.go:29 token TEXT NOT NULL DEFAULT ''`；`:68 ... FROM scim_config WHERE token=?`（明文比对，未走 `EncryptSecret`） |
| C10 | 后台自愈协程 `os.Exit` | `api/watchdog.go:139 os.Exit(1)`（无优雅停机 flush） |
| C11 | 前端：`--td-*` 死变量层 | `theme.css` 定义 **52** 处；全 src `var(--td-` 仅 **1** 处且**在注释内** → 真实引用 **0** |
| C12 | 前端：`tdesign-react` 僵尸依赖 | `package.json:18` 仍在 `dependencies`；src 内 3 处提及（`App.tsx:7/96`、`SystemSettingsP.tsx:12`）**全是注释** → 真实 import **0** |
| C13 | 前端：样式四重冗余计量 | `var()` 出现次数：`--lc-*` **825** / `--adm-*` **230** / `--npz-*` **80** / `--td-*` **1（注释）**；内联 `style={{` **903** |
| C14 | 前端：零像素基线 | `toHaveScreenshot\|toMatchSnapshot\|toMatchImageSnapshot` 全仓 = **0** |
| C15 | 入口 chunk 2.13 MiB | `dist/assets/index-*.js` = **2.13 MiB** |
| C16 | i18n 2710 键 | 10 份 `src/i18n/locales/*.ts` **逐份 2710** |
| C17 | 巨型组件 | `Landing.tsx` **1746** / `admin/PlansP.tsx` **966** / `admin/panels_a.tsx` **850** / `admin/KbP.tsx` **781** |
| C18 | blob URL 泄漏 | 逐文件扫描：LEAK 仅 **3 文件 4 处**（`MessageBubble` 1、`PlansP` 2、`ReferralP` 1）；其余 8 处（含 `ChatWindow`）**全部配对** |
| C19 | 403 无拦截 | `core.ts` 仅 `if (response.status === 401)`；全 src 的 `403` 只出现在 1 个测试 mock 中 |
| C20 | 靠中文串判定 | `MessageBubble.tsx:203-205` `mode.includes('精确命中'/'模糊'/'语义高相似')` |
| C21 | AI 顾问纯关键词检索、无向量 | `internal/assist` 全包 grep `embedding`/`vector` = **0**；`engine.go:122 hitScore` / `:152 synonymHit` |
| C22 | 翻译 KB 有 pgvector | `kb/db.go:122 vector(1024)` + `:125 ensureVectorIndex()`（HNSW）+ `:1146 VectorSearch` |
| C23 | 质量零件测试数 | `gate_test.go` **7** / `qa_test.go` **10**+`screener_test.go` **1** / `postprocess_test.go` **12** |
| C24 | gofmt 实测 9 文件不合格，其中 7 个 HEAD 已不合格 | `gofmt -l internal cmd` = 9；逐文件 `git show HEAD:` 比对 → **7 个 HEAD 即不合格**（`estimate.go`/`points_view_test.go`/`referral.go`/`crawler.go`/`iam/models.go`/`persona_dict.go`/`persona_scope.go`）、`service/ticket.go` 仅工作区、`admin_assist_proxy.go` 为新增文件 |
| C25 | `ci.yml` 未覆盖 `autosales` | `on.push.branches: [main, master]` + `pull_request`；当前分支无 PR → 所有 job 零运行 |

### A.2 ★ 更正（原报告此处有误，正文已就地改）

| # | 原报告 | 复核后正确值 | 错因 |
|---|---|---|---|
| **X1** | §9：「`AGENTS.md`：CI 跑 `test -z "$(gofmt -l internal cmd)"`」→ 判「文档失实」 | **该引文在仓库中不存在**。`AGENTS.md` 零处提 gofmt（其闸门=build/vet/test-race）；`ci.yml` 零处 gofmt（但**有** `go vet`）；`scripts/`+`deploy/` 零处。真实缺口：**gofmt 从未被任何闸门约束** | 凭空引用了一条不存在的文档语句，并据此错误地判「文档失实」 |
| **X2** | §4.1-1 / §0 / §6：「479 个 `*Server` 方法」「984 处 `s.Store.*`」 | 生产文件口径：**474** 个方法（479 含 5 个仅存于测试文件的）；**949** 处 `s.Store.` | 未排除 `_test.go` / 计数工具口径不一致 |
| **X3** | §4.1-2：「实测 **269** 个 handler 中 **31** 个体内无鉴权」 | handler = **270**；无鉴权体内调用 = **32**（`grep` 口径校正） | 差 1，且原文未说明计数口径 |
| **X4** | §4.4：「`engine/hedge.go` 与 `engine/placeholder.go` **无测试**」 | **两者都有测试，各 4 个 Test**（`hedge_test.go` / `placeholder_test.go`，均 2026-09-16 建） | **漏检**——只在 `engine` 包内 grep 编排函数名，未单独确认这两个文件的 `_test.go` 存在 |
| **X5** | §4.1-9：「`log.Printf` 存量 **176** 处未迁移」 | 棘轮口径 internal = **176**（实跑棘轮测试通过，且**恰等于基线**）；但 `cmd/` 另有 **69** 处**从未被扫描** → **真实 245** | 直接采信了棘轮基线数，未发现闸门只 `Walk(internal)`，`cmd/` 是盲区 |
| **X6** | §6 P1-12 / §9：「`billing.go` 已 2,206 行，约束**已被打破**」 | **HEAD = 2,201 行（与文档基线一致、仍合规）**；仅**工作区** 2,206（`+5` 未提交）。`kbpackages.go` HEAD 与工作区均 1,371（合规） | 采信了工作区行数，未与 HEAD 区分，「已打破」结论过强 |
| **X7** | §4.2-5：「`any` 泛滥 **618** 处（**225** `any` + 393 `Any`）」 | **624**（`any` 令牌 **231** + `Any` 令牌 **393**）；16 份 `Any` 别名定义确认无误 | 令牌计数口径不同（`\b` 边界 vs 子串） |
| **X8** | §4.2-2 / §6 P2-14：「**13** 处裸 fetch，**4** 处无 401」 | 裸 fetch **19** 处；剔除 client 自身（`core.ts:107`）与 SSE（`translate.ts:118`）后**实际绕过 17 处**；文件内无 401 处理的 **7** 处（`tickets`/`referral`/`admin` + `assist` ×4） | 漏检 `branding.tsx`、`App.tsx`、`LangMultiSelect`、`kb.ts` ×2、`billing.ts` 等调用点 |
| **X9** | §9 未把 `AGENTS.md` 的 store 文件数纳入偏差表 | 实测 **88 个 `.go`**（49 生产 + 39 测试）/ **20,300 行**（生产 14,086 行），与文档「74 文件 / 16.8k 行」对不上 | 原报告未核此项，本次复核补入 §9 |

### A.3 ★ 新增发现（原报告未提，本次复核补）

| # | 发现 | 证据 | 风险 |
|---|---|---|---|
| **N1** | **日志棘轮存在结构盲区**（与 X5 同源，独立成条） | `logratchet_test.go` 的 `filepath.Walk(filepath.Join(root, "internal"))` 硬编码只扫 `internal/`；`cmd/` 69 处 `log.Printf` 不受约束（`cmd/server/main.go` 40 处最多） | 中——新增 `cmd/` 日志**永远不会**触发棘轮红灯，「只减不增」名存实亡 |
| **N2** | **棘轮基线恰好等于实测值**，故闸门仅「不升高」而非「在下降」 | 实跑 `TestLogPrintfRatchet` PASS 且**未打印** `已降至` 提示行 → `count == 176` | 低——提示正常，但说明近期迁移停滞（`engine→fileproc→orchestrator` 优先级未见推进） |
| **N3** | **`ci.yml` 实际有 `go vet` + Python 自检闸门** | `ci.yml:32-33 go vet ./...`；`:38-56` 装 `pymupdf`/`python-docx` 后直跑 `pdf_overlay.py`/`docx_translate.py` 的 `selftest`，依赖缺失（退出码 2）即红灯 | 低——是**正面**发现：CI 静态检查比原报告描述的更完整，原报告只批「无静态检查」不准确 |
| **N4** | **`admin_assist_proxy.go` 是未跟踪新文件**（A5 缺陷所在文件的测试 `admin_assist_proxy_test.go` 同为未跟踪） | `git status --porcelain` 列 `?? backend-go/internal/api/admin_assist_proxy.go` + `?? ..._test.go` | 中——A5 的**修复与该测试都不在任何闸门内**，再次印证「红灯为何长期潜伏」 |
| **N5** | **`--npz-*` / `--adm-*` / `--lc-*` 三层变量共存且各自有真实引用**（非纯死层，仅 `--td-*` 死） | 引用计数 80 / 230 / 825，去重变量 6 / 17 / 61 个 | 低——确认「四重样式变量」是**三活一死**，清理时应只删 `--td-*`，勿误伤 `--npz-*`（仍在用） |
| **N6** | **未跟踪文件数在复核期间由 44 → 45 增长** | header 首轮记 44，复核时 `git status` = 45 未跟踪 / 139 修改 / 共 184 | 低——说明**调研期间仓库仍在变动**，所有计数均需带时间戳（本附录口径：2026-09-21 复核时刻） |

### A.4 复核后仍无法判定（诚实边界，与 §9 一致）

以下 5 项**没有**在本次复核中被证实或证伪，原报告已列为「未验证项」，此处保持：
真实 LLM（非 mock）质量/时延/成本；SQLite↔PG 迁移等价性（无对拍）；`jsonops.go` 的 `panic` 可达性；
`/openapi/*` 无 Cookie 鉴权端点；T28 在 ubuntu CI 上是否转绿（本机沙箱两次同因拦截）。

### A.5 复核方法与可复现命令

```bash
# 规模
cd backend-go && ls internal/api/*.go | grep -v _test.go | wc -l
xargs wc -l < <(ls internal/api/*.go | grep -v _test.go) | tail -1
grep -hE '^func \([a-zA-Z_]+ \*Server\)' internal/api/*.go | wc -l          # 474（含测试 479）
grep -rhoE 'mux\.HandleFunc\(' internal/api/*.go | wc -l                     # 268
grep -hE '^func \([a-zA-Z_]+ \*Store\) [A-Z]' internal/store/*.go | wc -l    # 439（导出）
grep -hE '^func \([a-zA-Z_]+ \*Store\) [A-Z]' internal/store/*.go | grep -c 'context.Context'  # 0
# 零测试包 / Benchmark
go list ./... | wc -l                                                        # 52
git grep '^func Benchmark' -- '*.go' | wc -l                                 # 0
# 日志棘轮（含盲区）
go test -count=1 -run TestLogPrintfRatchet -v ./internal/observability/
grep -rhn 'log\.Printf(' cmd/ --include='*.go' | grep -vE '^[0-9]+:[[:space:]]*//' | wc -l   # 69
# 格式门禁
gofmt -l internal cmd                                                        # 9 个文件
git grep -n gofmt -- 'scripts/*' 'deploy/*' '.github/*'                      # 空
```
