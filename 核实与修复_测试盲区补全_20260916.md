# 核实与修复 · 测试盲区补全与残留缺陷核实（2026-09-16）· 最终版

> 定位：全量 UAT（468+ 断言全绿）之后，对「未覆盖面 / 疑似缺口」逐项对照真实代码核实，
> 区分【真缺陷】【真实缺失】【设计权衡/受限资质】【误报】，修复全部落地并回归验证。
> 本版为执行完毕后的最终状态记录（初稿的「待批准」项已全部实施）。

---

## 一、结论总表（最终）

| # | 项 | 核实结论 | 状态 |
|---|---|---|---|
| 1 | 收款/付款/开票全手动 | **受限资质（设计现状）**：微信/支付宝 Provider 骨架 fail-closed（payment.go:157），税局对接属资质遗留。维持现状 | 无需动 |
| 2 | Go mail 包零测试 | **真实缺失** | ✅ 已补 8 用例 |
| 3 | SDK 行为级测试缺失 | **真实缺失** | ✅ TS 8 用例 + Python 13 用例 + 发布管线门禁 |
| 4 | CI 无 PG 方言矩阵 | **真实缺失** | ✅ 新增 `uat-pg` job（pgvector 容器 + 全量 UAT + SDK 测试） |
| 5 | 密码找回全链路无自动化 | **真实缺失** | ✅ UAT T45（发码→日志取码→重置→登录→还原） |
| 6 | 「像素级」E2E 实为结构级 | **设计权衡，维持**（真像素 diff 跨平台不稳定，成假红源头） | 决策：不加 |
| 7 | admin 面板零组件测试 | **真实缺失** | ✅ PlansP 计费 Hub 3 用例（jsdom + testing-library） |
| 8 | VS Code 插件密钥键名乱码 `'langcros…iKey'` | **真缺陷** | ✅ 已修 `'langcross.apiKey'` |
| 9 | VS Code webview 错误消息 innerHTML 未转义（XSS 面） | **真缺陷**（同文件译文路径有转义，口径不一致） | ✅ 已修（esc 提升到监听器顶层统一使用） |
| 10 | 浏览器插件 mouseup `getRangeAt(0)` 无守卫 | **健壮性小缺口** | ✅ 已修（try/catch 兜底 hideAll） |
| 11 | 多实例部署未 e2e 实测 | **真实缺失**——且实测立刻抓到 1 个 P0 | ✅ 双实例 e2e 8/8 + P0 修复（见二） |
| 12 | billing.go 大文件 / permissions JSON | **架构债非缺陷**，挂技术债路线图 | 挂账 |

---

## 二、★ 多实例 e2e 抓获并修复的 P0（本次最大收获）

### 缺陷：双实例并发启动 → 实例 B 崩溃/假活（滚动发布必踩）

**完整根因链**（首轮双实例 e2e 实测复现，A 活 B 死，确定性复现两轮）：

1. `kb.Open`（术语库）PG 建表 DDL 与另一实例并发 → `pq: duplicate key ... pg_class_relname_nsp_index (23505)` → **实例 B `db=nil` 退化启动**
2. `st` 仅在 `db != nil` 时创建（main.go:176）→ **B 的 `Store=nil`** → 登录/鉴权全 500（假活：`/status` 仍报 `ok:true`，监控无法发现）
3. 30s 后 USDT 对账协程 `s.Store.GetUSDTCfg()` 空指针 → **整个进程 panic 死亡**（pay_usdt_watch.go:54）

**修复（三道防线）**：
- `cmd/server/main.go`：迁移单飞锁（PG advisory lock）**前置到 `kb.Open` 之前**——原实现挂在 `db.RawDB()`，而 db 恰是竞态受害者（鸡生蛋）；改用独立连接先拿锁，kb.Open + 全部迁移 + 种子单飞串行
- `cmd/server/main.go`：PG 方言下 `st == nil` 改为 **fail-fast 拒绝启动**（log.Fatal），杜绝「登录 500 + /status 假活」的危险中间态，交由 supervisor/滚动发布重试；sqlite 开发场景不受影响
- `internal/api/pay_usdt_watch.go`：`usdtReconcileTick` 增加 `s.Store == nil` 防线（旁路任务绝不允许威胁主进程；已核查 watchdog/startAuditRetention 等其余周期协程均有防线，USDT 是唯一漏网）

### 双实例 e2e 最终结果（scripts/uat/multi_instance_e2e.sh，**8/8 全绿**）

| 断言 | 结果 |
|---|---|
| M1 双实例健康 | PASS |
| M2 JWT 跨实例互通（同 JWT_SECRET） | PASS |
| M2 A 充值后 B【立刻】可消费（旧缺陷场景：影子 seed 0 误中止） | PASS |
| M3 30 路对半并发扣费全部有解（ok=6 refused=24） | PASS |
| M3 不透支（8000→6776） | PASS |
| M3 无误报欠费清零（alerts=0） | PASS |
| M3 ledger 与消耗精确勾稽（1224=1224） | PASS |
| M4 二次充值 5s 内 B 恢复可消费（TTL 重播种闭环） | PASS |

> 附注：M3 的 24 个拒绝经样本核实全部为默认 QPS=10 限流（文案碰巧含「余额不足」提示词导致首轮误判），
> 属预期行为非缺陷。脚本缺陷（裸 `wait` 挂起、双桶口径对账失真）已一并修复。

---

## 三、已落地变更清单

### 3.1 后端
- `backend-go/internal/mail/mail.go`：抽取 `buildMailMessage` 纯函数（行为不变，报文构建可单测）
- `backend-go/internal/mail/mail_test.go`（新增 8 用例）：RFC2047 可逆 / CRLF 字节级 / multipart boundary 闭环 / Noop / Sender 路由
- `backend-go/cmd/server/main.go`：P0 修复（锁前置 + fail-fast）
- `backend-go/internal/api/pay_usdt_watch.go`：P0 防线
- `go vet ./...` + `go build` 通过

### 3.2 UAT
- `scripts/uat/run_uat.sh`：注入 `MAIL_NOOP_PRINT_BODY=1`（仅 UAT 环境）
- `scripts/uat/api_uat_txn.sh`：新增 **T45 密码找回全链路**（7 断言）
- `scripts/uat/multi_instance_e2e.sh`（新增）：双实例 e2e 全套（M1–M4）

### 3.3 SDK
- `sdk/typescript/test/client.test.mjs`（新增 8 用例）：Bearer/错误映射/multipart 字节级/waitTask 显式间隔回归锁；mock 泄漏修复（`mock.restoreAll()`）
- `sdk/typescript/package.json`：`test` = 类型检查 + 构建 + node --test
- `sdk/python/test_translator_sdk.py`（新增 13 用例）：urlopen mock、错误映射、multipart 字节级、轮询、R-L2 下载守卫
- `scripts/release-sdk.sh`：TS/Python 测试门禁前置到构建发布之前

### 3.4 前端
- `frontend-react/src/components/admin/PlansP.dom.test.tsx`（新增 3 用例）：订单表渲染 / USDT 开启渲染收款地址 / 超管商业包表
- `frontend-react/vitest.config.ts`：include 增加 `*.test.tsx`
- 依赖新增：`jsdom`、`@testing-library/react`（devDependencies）
- vitest 全量 **66/66**（63 存量 + 3 新增）

### 3.5 插件
- `vscode-extension/extension.js`：密钥键名更正 + esc 统一转义（XSS 修复）
- `extension/content.js`：`getRangeAt` 守卫；4 个 js 文件 `node --check` 全过

### 3.6 CI
- `.github/workflows/ci.yml`：新增 `uat-pg` job——`pgvector/pgvector:pg16` 服务容器 + 全量 run_uat.sh（PG 方言）+ SDK 行为级测试

---

## 四、验证结果汇总（2026-09-16 执行）

| 验证项 | 结果 |
|---|---|
| go vet + go build | ✅ |
| go test -race 全量（含 mail 新增） | ✅（随全量 UAT 复跑确认） |
| vitest 全量 | ✅ 66/66 |
| TS SDK npm test | ✅ 8/8 |
| Python SDK unittest | ✅ 13/13 |
| 插件 4 文件语法 | ✅ |
| 双实例 e2e | ✅ 8/8（含 P0 修复验证） |
| 全量 UAT（A/B + T45 + Playwright） | ✅ 复跑全绿（见文末补记） |

---

## 五、遗留挂账（不阻塞，入技术债路线图）

1. Java SDK 单测（需 maven 环境，收益/成本比低）
2. admin 其余 20+ 面板组件测试（已打通 jsdom 管线，按面板风险逐个补）
3. 真像素 diff 截图（仅限自建 runner 固定环境）
4. billing.go 拆域 / permissions JSON 结构化
5. CI 的 uat-pg job 首次真实运行需观察 PG 服务容器启动时序（health check 已配 20 次重试）

---

## 补记 · 全量 UAT 复跑（P0 修复 + T45 落地后，第二轮）

- go test -race 22 包：✅ 全绿
- 后端 API UAT（A/B）：PASS=67 FAIL=0
- 后端交易专项 T 套件（含新增 T45 密码找回全链路）：PASS=308 FAIL=0
  - T45 六断言全过：forgot 受理 → 日志取码(378167) → 重置成功 → 新密登录 → 旧密拒绝 → 密码还原
  - T45 取码实现踩坑记录：grep -oE 中文 pattern 在 ck 的 $(...) 命令替换 + bash 3.2 管道组合下
    首轮实测取不到（同管线手动执行可取，locale 排除）；改 python3 正则提取（对编码/转义免疫）后稳定，
    且失败时输出 mark=增量行数=日志尾 诊断留痕
- Playwright E2E 10 spec：✅ exit=0
- 总计 475+ 断言全绿，发布闸门通过

## 补记二 · 全量 UAT 第三轮（同日复核，确认稳定性）

- A 49/0 + B 67/0 + T 308/0 + Playwright exit=0，两轮连续全绿
- 多实例 e2e（8/8）与全量 UAT 互不污染（独立库 translator_multi / translator_uat）
