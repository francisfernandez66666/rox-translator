# 重构方案：知识库包体系 · 计费闭环 · 审计分级 · 并发加固

> 版本：v1.0（2026-08-21）
> 状态：已评审通过，按批次执行
> 关联决策人确认：向量模型采用 `BAAI/bge-m3`；pack_id 方案；租户+行业关联列

---

## 一、背景

系统已完成四级角色（用户<部门管理员<租户管理员<超管）、IAM 子系统拆分（`internal/iam/`）、
角色化工作台前端。本方案解决落地过程中暴露的核心逻辑缺陷：

1. 句数扣减不受「强制计费」开关控制（关着也在偷偷扣）
2. 知识库包停用对存量数据无效（module 标记缺失，摘除不到）
3. 行业包双轨制混乱（每租户空壳包 vs 租户1共享内容包并存）+ **组织包数据跨租户泄漏**
4. `kb_entries` 与引擎检索层 `tm_segments` 历史上断链（AddTerm/UpsertSegment 从未被调用）
5. 向量索引（npz）不感知包体系
6. 平台上下文（tid=0）在多个面板行为未定义
7. 删除部门静默废掉部门管理员
8. 成员账户面板缺少级联交互
9. iam.Store 写操作无互斥保护

## 二、知识库包体系（目标架构）

### 2.1 包类型与挂接关系

| 包类型 | pack_type | 挂接对象 | 内容归属 | 维护者 |
|---|---|---|---|---|
| 部门包 | department | 部门（org_id），作用于部门及子部门下全部用户 | 本租户 | 租管/部门管理员 |
| 组织包 | tenant | 单一租户 | 本租户 | 租管 |
| 行业包 | industry | 同行业多个租户共享 | **租户1（唯一宿主）** | 仅超管 |
| 语言文化包 | locale | 全系统 | **租户1（唯一宿主）** | 仅超管 |

### 2.2 应用优先级链

```
部门包(0) > 组织包(1) > 行业包(2) > 语言文化包(3)
```

同一源句被多包命中时取优先级最高者。

### 2.3 pack_id 归属方案（替代 module 标记）

- `tm_segments` 新增 `pack_id INTEGER NOT NULL DEFAULT 0`
- SaveEntry / 导入 / 启停写回 时同时落 `pack_id`
- 包停用 = `DELETE FROM tm_segments WHERE pack_id=?`（精确摘除）
- 包启用 = 从 kb_entries 按 pack 回写（带优先级+宿主）
- 存量回填：按 kb_entries 反查归属补 pack_id；无法归属的历史行保持 pack_id=0、priority=9（兜底层）

### 2.4 共享宿主隔离（泄漏修复，关键）

**问题**：现检索 `tenant_id IN (?,1)` 会把租户1自己的组织包数据泄漏给所有租户。

**修复**：检索层 JOIN kb_packages 判定共享合法性：

```sql
WHERE tm.tenant_id = :tid
   OR (tm.tenant_id = 1 AND pkg.pack_type IN ('industry','locale'))
```

且行业包额外要求：`pkg.code = 租户注册行业编码`；语言文化包全量放行。

### 2.5 行业包单轨制

- 注册流程不再创建空壳行业包（删除 `EnsureIndustryPackage` 调用点）
- `tenants` 表新增 `industry TEXT DEFAULT ''` 列，注册时写入所选行业编码
- 行业包内容只在租户1维护；租户侧仅靠 `tenants.industry` 建立载入关系
- 已存在的空壳行业包行由迁移脚本清理（仅删空包，有内容的保留并提示）

## 三、向量索引包体系

### 3.1 模型

- Embedding 模型：`BAAI/bge-m3`（SiliconFlow，多语言，覆盖现有 17 语种语料）
- Key：复用 system_config.model_routes 中已保存的全局密钥

### 3.2 元数据扩展

npz 索引每行元数据增加：`pack_id`、`pack_type`、`org_id`、`tenant_id`。
重建工具读取 kb_entries 分包向量化写入新索引（tm_embeddings.npz v2）。

### 3.3 增量入库

知识库条目新增/导入后异步调用 embeddings API 追加进索引（批量、限流、失败重试队列）。
停用包 = 索引中摘除对应 pack_id 的向量；启用 = 重新嵌入回写。

### 3.4 检索语义

`Search(vec, topK, langs)` 增加 filter：本租户 + 用户所在部门子树的部门包 + 本租户组织包 +
注册行业的行业包 + 全部语言文化包；命中结果按 2.2 优先级排序。

## 四、计费门控

- `meterSentences` / `meterFileSentences` 开头检查 `sentence_enforced=="1"`；
  未开启强制计费时不扣减、不校验（前台亦不显示余额，已上线）
- 开启时维持现状：余额用尽拒绝翻译并提示购买

## 五、审计日志分级

现状：`audit_logs` 记录关键操作（含 before/after）；`/api/system/audit` 已按 effTenant 过滤。

| 角色 | 可见范围 | 展示 |
|---|---|---|
| 超级管理员 | 全部租户全量记录 | 增加租户名列（JOIN tenants） |
| 租户管理员 | 本租户记录 | 租管工作台新增「审计日志」面板 |

实现：
- `ListAuditFilter(tid=0,...)` 语义改为「全量」，返回附 tenant_name
- L3 工作台注册 audit 面板复用同一接口

## 六、平台上下文（tid=0）面板语义

| 面板 | tid=0 行为 |
|---|---|
| 用量报表 | 跨租户聚合（全部租户用量汇总，可按租户分组展示） |
| 套餐详情 | 跨租户订单聚合 |
| 邀请码 / API Key / Webhook | 跨租户列表（标注归属租户） |
| 模型接入（BYOK） | 提示「请选择具体租户后配置」（设置类操作必须在具体租户内） |

## 七、组织删除保护

`DeleteOrg` 删除部门时：
- 其子树内 `role=dept_admin` 的用户自动降级为 `user`（记审计 org_dept_admin_demote）
- 普通用户回收至根组织（现状保留）

## 八、表单级联统一

成员账户面板（Users.vue）开户表单改为与组织架构一致：
先选归属部门 → 角色选项随层级收窄（平台根=超管；租户根=租管/部门管理/用户；部门=部门管理/用户）

## 九、并发加固

`iam.Store` 全部写方法（CreateUser/UpdateUser/DeleteUser/CreateOrg/DeleteOrg/MoveOrg/
RenameOrg/SetKBPackageEnabled 等）加 `sync.Mutex` 互斥，消除高并发 SQLITE_BUSY 风险。

## 十、待办登记（本轮不实施，完成后另行决策）

| 项 | 说明 |
|---|---|
| 语言文化包 gate 闸门 | 输出前强制套用语言文化规则（pack role=gate 目前未消费） |
| Evals 占位 Key 空转 | 全局 key 为随机占位符时 Evaluator 应 Enabled=false |

## 十一、批次与验收标准

| 批次 | 内容 | 验收 | 状态 |
|---|---|---|---|
| 一 | 计费门控 + pack_id + 存量回填 + 启停重写 | 关开关不扣数；停用组织包后翻译不再命中其条目，启用恢复 | ✅ 已完成（3342 条已归属组织包） |
| 二 | 泄漏修复 + 行业单轨 | 其他租户检索不到 ROX极石汽车组织包数据；注册选行业后能命中该行业包条目 | ✅ 已完成 |
| 三 | 向量库 bge-m3 重建 + 增量入库 + 检索过滤 | 语义检索按包体系过滤；上传新条目自动出现在向量命中 | ✅ 已完成（3348 行 bge-m3 重建） |
| 四 | 审计分级 | 超管见全量带租户名；租管只见本租户 | ✅ 已完成 |
| 五 | 平台聚合 + 删除降级 + 级联 | tid=0 各面板有合理聚合视图；删部门后管理员自动变普通用户；两处开户级联一致 | ✅ 已完成 |
| 六 | 并发锁 | 写操作串行化，消除 SQLITE_BUSY 风险 | ✅ 已完成（execW 互斥） |

> 执行记录：commit cad0e4d…2143f4f（2026-08-21），生产已部署并健康检查通过。
> 待办（另行决策）：语言文化包 gate 闸门逻辑、Evals 占位 Key 空转处理。

## 十二、涉及文件清单

- 后端：internal/api/billing_api.go · internal/store/kbpackages.go · internal/store/store.go ·
  internal/kb/db.go · internal/kb/npz.go · internal/engine/engine.go · internal/api/register.go ·
  internal/tenant/tenant.go · internal/iam/store.go · internal/api/admin_kb.go ·
  internal/store/audit.go · internal/api/server.go · cmd/rebuild-index(新增)
- 前端：src/components/admin/Users.vue · Kb.vue · Usage.vue · Billing.vue · Invites.vue ·
  ApiKeys.vue · Webhooks.vue · Audit(新增) · AdminDashboard.vue · src/i18n/
