# ============================================================================
# requirements.txt — Python 依赖包
# ============================================================================
# 安装方式：pip3 install -r requirements.txt

# ---- Web框架 ----
# FastAPI: 后端Web框架，接收前端请求、调度技能
fastapi>=0.110.0
# Uvicorn: ASGI服务器，运行FastAPI应用
uvicorn>=0.27.0
# python-multipart: 文件上传支持（FastAPI的UploadFile依赖）
python-multipart>=0.0.6

# ---- AI/模型 ----
# openai: 调用OpenAI兼容的在线大模型API（智谱GLM等）
openai>=1.12.0

# ---- 数据处理 ----
# numpy: 向量计算（知识库语义匹配用）
numpy>=1.24.0
# requests: HTTP请求（调在线API、Ollama本地模型）
requests>=2.31.0

# ---- 文件处理 ----
# python-docx: 读写Word文档
python-docx>=1.0.0
# python-pptx: 读写PowerPoint文档
python-pptx>=0.6.21
# openpyxl: 读写Excel文档
openpyxl>=3.1.0

# ---- 环境 ----
# python-dotenv: 从.env文件加载配置
python-dotenv>=1.0.0
