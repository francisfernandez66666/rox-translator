# 性能优化方案（不换库）—— 能言翻译 SaaS

> ⚠️ **历史更正（2026-09-04）**：本文写于 2026-08-28「不换库」决策期。此后路线演进：**生产已于 2026-08-30 决定并落地 PostgreSQL 16 + pgvector 切流**（详见《改造方案_解锁PG与Redis及路线图.md》《PROGRESS.md》六），§4「不迁移 PostgreSQL」结论已被推翻；文中 A3/§3 内存参数亦按生产实测更新——当前生产为 `GOMEMLIMIT=850MiB / MemoryMax=1150M / WORKER_CONCURRENCY=4`（2026-09-04 复核），并非 650Mi/worker=2。末尾结论请勿照抄。

> 目标：在不迁移数据库（维持 SQLite）的前提下，解决两个生产症状：
> 1. **大 PDF 翻译卡死 / 超时 / 失败**
> 2. **多人同时翻译时请求变慢 / 报“数据库忙”（SQLITE_BUSY）**
>
> 约束：服务器内存硬上限 **1G**，不可升级。
> 决策：先做「不换库优化」（已与用户确认）。换 PostgreSQL 的成本与收益评估见文末。

---

## 0. 重要结论先行：发现双重计费隐患

账单存在 **双重计量**，与本次优化强相关：

- 每次 LLM 调用：`eng.LLM.OnUsage` → `ChargeUsageRealtime`（`internal/api/billing_api.go:119`）→ `RecordUsage` 扣一次双桶（`internal/store/billing.go:185`）。
- 工单收尾：`runTicket → chargeTokens`（`internal/service/ticket.go:295`）→ `MeterDeferred` → `Meter`（`internal/billing/quota.go:165`）→ `RecordUsage` **又扣一次**。

两处都走 `Bill.Meter`，传的是同一批收集总量（`engine.UsageTokens`）。

- 现状 `billing_enforced=0` 仅 `LogUsage`：表现为 **用量看板数据翻倍**。
- 一旦打开强制计费（商业化开关）：**实际双倍扣费 / 资损**。

本次「不换库优化」必须一并修复（见 Phase B.1）。

---

## 1. 诊断结论

### 症状一：大 PDF 卡死 / 失败 —— 与数据库无关

PDF 走 `fileproc` 子进程管线（pdf2docx → DOCX → LibreOffice），PDF 二进制/图片**从不进 SQLite**，库里只存路径字符串。卡死根因是 **1G 机器内存顶不住**：

- Go 服务 `GOMEMLIMIT=850Mi`（部署文档记录）常驻，python pdf2docx + LibreOffice 峰值各数百 MB → 触发 OOM-kill 子进程 → 管道挂住 → 表现为“卡死”。
- `procgate.go` 已把文件转换收敛为**串行**（默认 `FILEPROC_MAX_CONCURRENT=1` + `nice -n 10`），但**无前置拦截**与**子进程内存护栏**。
- `translateUploadMax=50MB`（`internal/api/upload.go:43`）仅按字节，未按 PDF 这种高危格式做早识别。

### 症状二：多人并发变慢 / 报数据库忙 —— 写放大

SQLite 单写者串行，并发工单全部排队撞 `busy_timeout=5000ms`（`internal/kb/db.go:60-69`）。写放大来源：

1. **每段翻译一次写事务**：`RecordUsage` 的 `BEGIN IMMEDIATE` + 双桶 UPDATE + INSERT ledger。大 PDF 上千段 × 多语言 = 上千个串行写事务。
2. **进度逐段 UPSERT**：`progFn`（`ticket.go:426`）每段每语言写一次 `SetTicketState`（含 JSON payload），与计量写互相排队。
3. **TM 命中逐条落库**：`bumpTmHitsFromTranslations`（`ticket.go:635`）逐条 `INSERT ... ON CONFLICT`。
4. **每次翻译请求一个 LIKE 全表扫描**：`gateUsage → CheckDailyQuota → DailyUsage`（`billing.go:375`），`created_at` 用 `LIKE` 做日聚合。
5. 收尾 `chargeTokens` 再写一笔（双计量，见 §0）。

---

## 2. 方案总览（不换库 × 1G 内存红线）

### Phase A — PDF / 内存（约 0.5 天）

| # | 文件 | 改动 |
|---|------|------|
| A1 | `internal/api/upload.go` | PDF 大小 >15MB **或** 提取页数 >120 页前置拦截 + 友好提示；`translateUploadMax` 按格式分流（pdf 收紧，其他保持） |
| A2 | `internal/fileproc/procgate.go` + 子进程 wrapper | 子进程加 `RLIMIT_AS`（/ `ulimit -v`，约 300–400Mi）内存护栏；超限快速失败 + 清晰文案，杜绝 OOM 后挂死 |
| A3 | 部署配置 + `internal/service/ticket.go` | `GOMEMLIMIT` 850Mi → **650Mi**；文件转换完成收尾 `debug.FreeOSMemory()` |
| A4 | `internal/engine/embedcache.go` + CJK scope 缓存 | embed 缓存、CJK 缓存加**容量上限**，防 KB 大时无限膨胀 |

### Phase B — 并发写瓶颈（约 2.5–3 天）

| # | 文件 | 改动 |
|---|------|------|
| B1 | `internal/service/ticket.go` + `internal/api/billing_api.go` | **修双重计费**：实时计量已启用时，`chargeTokens` 只回填 `tokens_billed` 展示，**不再二次 RecordUsage**；实时路径补 `user_id` 归属（`billing_api.go:136` 经 ctx 透传 authUser） |
| B2 | 新增 `internal/billing/usagesink.go` | **计量批量落库（核心）**：新增进程级 `UsageSink`，`OnUsage` 仅写内存缓冲 `(tid, userID, provider, model, quantity, bizKind, bizMode)`；后台 flusher 每 **2s** 或缓冲 **200 条** 触发一次，**单事务**：先 `deductWithGrantsTx` 扣 SUM，再多行 INSERT ledger |
| B3 | `B2` 续 | 保留“余额不足立即中止”语义：内存影子余额 + flush 时 `ErrInsufficientBalance` 触发 `llm.AbortFromCtx` 中止整单（原链路不变） |
| B4 | `internal/service/ticket.go` `progFn` | 进度写节流：逐段回调改本地累加 + 每 **1s** 或每 **100 段** 落库一次；`finalizeFileTranslate` 收尾写一次完成态（前端进度延迟 ≤1s，可接受） |
| B5 | `internal/service/ticket.go:635` | TM 命中批量入库：译文对内存累积到阈值后，**同一事务多行** `INSERT ... ON CONFLICT` |
| B6 | `internal/store/billing.go:375` | `DailyUsage` 计数器化：新建 `usage_daily(tenant_id, day, total)` 表，计量 flush 时同步 upsert；`CheckDailyQuota` 改读计数器（缺行回退全表扫描兜底），干掉每次翻译请求的 `LIKE` 全扫 |
| B7 | `internal/store/store.go` + 归档 | 索引微调：`users(tenant_id, org_id)`（用量看板 join）；`audit_logs` / `alerts` 定期归档 |

### Phase C — 验证

| # | 文件 | 改动 | 状态 |
|---|------|------|------|
| C1 | `deploy/loadtest_openapi.sh` | 改造加 `file` 模式（并发文件工单压测），并输出「是否出现 SQLITE_BUSY」判定 | ✅ 已加 |
| C2 | 运行验证 | `PPROF_ADDR` 起瞬时 heap，确认 1G 内峰值；打开 `billing_enforced=1` 冒烟验证 **不再双扣** | ⏳ 需在生产/预发环境实跑（本机无运行实例） |

> 说明：所有代码改动已 `go build ./...` 通过、`go vet` 通过、`internal/api` 单测通过。
> `internal/store` 的 `TestReferralFlow` 在本仓改动前即失败（已 `git stash` 复现），属历史问题，与本次优化无关。
> 实时并发写与内存峰值的真机验证需在有数据库与 LLM Key 的环境执行 C1 脚本完成。

---

## 3. 落地参数（保守档，已确认）

| 参数 | 现值 | 优化值 |
|------|------|--------|
| `GOMEMLIMIT` | 850Mi | **650Mi** |
| `WORKER_CONCURRENCY`（manifest） | 4 | **2** |
| `LLM_FILE_CHAT_CONCURRENT`（`client.go:43`） | 6 | **4** |
| 文件翻译每单并发（sem，ticket.go:325） | 3 | **1** |
| 子进程 `RLIMIT_AS` | 无 | **~350Mi** |
| PDF 前置拦截 | 仅 50MB | **>15MB 或 >120 页** |

---

## 4. 明确不做 & 何时才值得换库

- **不迁移 PostgreSQL / MySQL**：SQLite 写路径经 Phase B 改造后，写事务从“每秒 N 个”降到“每秒 0–1 个”，单写者串行完全够用；在 1G 机器上跑 PG 反而更挤。且代码库约 89 处 SQLite 专有语法（INSERT OR IGNORE / ON CONFLICT DO UPDATE(excluded.) / json_set / pragma_table_info / sqlite_master / AUTOINCREMENT / `created_at LIKE`），并发正确性依赖 `BEGIN IMMEDIATE` 单写者 + 守卫式 UPDATE + RowsAffected 抢占，整体迁移需数周且高风险。
- **何时才值得拆库**：单库 `usage_ledger` 超数千万行且多租户并发破百时 —— 届时建议把 **usage/audit 这类 append-only 流水**拆到独立存储（或按月分表），而非整体迁 PG。

---

## 5. 风险与回滚

- B2 批量落库延迟 2s 内才扣减，余额窗口有极小敞口（仅失败 flush 时触发中止，正常不影响）。
- 所有改动均有 `./start.sh regen && ./start.sh test` 回归（go test + Web 构建）。
- 若压测出现回归，可经环境变量逐档回退（worker / 文件并发 / GOMEMLIMIT 均为可配）。
