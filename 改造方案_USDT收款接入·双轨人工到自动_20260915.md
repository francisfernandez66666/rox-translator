# 改造方案 · USDT 收款接入（双轨：人工核销 → 链上自动到账）（2026-09-15）

> 目标：为平台增加 USDT（Tether）收款方式，覆盖海外客户「愿付币、不愿付卡」的转化缺口。
> 设计原则：**复用现有订单/到账/幂等骨架，不另起资金链路**——`orders(channel)` →
> `MarkOrderPaid/MarkOrderPaidByOrderNo`（单事务幂等抢占）→ payments 流水 → 权益发放，
> USDT 只是新增一个「钱怎么确认真的到了」的判定器。
> 状态：设计稿（未实施）。前置阅读：《核实与修复方案_E2E双红与验证闸门_20260915.md》B1/A6——
> 现有 wechat/alipay 为接口预留位，USDT 不走该「假回调」模式（链上无原生回调，全部主动查证）。

---

## 一、模式总览（两阶段交付）

| 阶段 | 模式 | 到账判定 | 交付量 | 建议节奏 |
|---|---|---|---|---|
| **M1** | 人工核销轨 | 客户提交 txid/流水截图 → 财务在区块链浏览器人工核对 → 超管后台「确认收款」（现 `admin_order_pay` 链路） | ~0.5 天 | **本周商业化可带** |
| **M2** | 自动到账轨 | 服务端轮询链上入账（TronGrid / Alchemy+Etherscan API），金额+窗口匹配 pending 单，`MarkOrderPaidByOrderNo` 幂等入账 | ~2 天 | 试运营第 2 周 |
| M3（备选） | 第三方 PSP | Binance Pay / NOWPayments 托管收币与合规，回调+主动查单双验 | ~1 天 | 合规评审后决策 |

> M1 先行原因：与 `static_qr` 静态码人工确认流程同构（白皮书 §收款「当前主通道」），
> 不改资金核心代码即可上线收款能力；M2 只是把「人查浏览器」换成「机器查 RPC」。

---

## 二、渠道与数据模型设计

### 2.1 渠道注册（复用现有校验点）
- `api/pay.go:129` 渠道白名单追加 `usdt`：
  `if req.Channel != "mock" && ... && req.Channel != "usdt" { 拒绝 }`
- `payment.Provider` 新增 `UsdtProvider`（`internal/payment/usdt.go`）：
  - `CreateOrder`：**不外呼**，本地生成收款要素（地址+精确金额+有效期）返回 `PayResult{Channel:"usdt", QRContent:<地址二维码URL>}`；
  - `VerifyNotify`：USDT 无渠道回调，本方法**只用于 PSP（M3）**；M2 走独立 reconciler，不经 `/api/pay/notify/:channel`
    （该口保持 Admin-Token 三道闸语义，不为其扩大攻击面）。
- `pay_mode` 语义不变：USDT 与 `static_qr` 并存，由**前端渠道选择器**按
  `payment.mode + usdt_enabled` 决定露出哪些 tab（后端 settings 出参新增 `usdt_enabled`，杜绝「配置未开但前端可下单」）。

### 2.2 金额换算与尾数防混淆（本方案的核心细节）
- 订单人民币应收不变（`amount_money` 分口径，B1 单一事实源）；
- 新增配置 `usdt_rate_fen_per_usdt`（如 `720000` = ¥7.2/USDT，超管后台手改；下单时把**换算快照**写进订单，
  避免汇率调整波及存量 pending 单）；
- 应收 USDT = `amount_money / rate`，**再叠加 6 位小数随机尾数** `tail ∈ [0.000001, 0.009999]`：
  - 尾数在下单时于「同地址全部 pending usdt 单」范围内查重分配（唯一索引兜底），使每单实付金额全局唯一——
    **无 memo 的链上转账靠金额本身对单**（TRC20/ERC20 均无收款备注通道）；
  - 唯一性冲突（两人同时下单）：尾数池 9999 个 × 窗口 24h，pending 同址上限告警（>200 时拒绝新单并告警）；
- 精度：TRC20 USDT 固定 6 位小数；ERC20/BEP20 兼容 6/18 位双写法（解析时统一除幂归 6 位）。

### 2.3 迁移（幂等，挂 `Store.New` 迁移链，双方言 dblib）
```
orders.channel            -- 无枚举约束，TEXT 扩值即可（仅 pay.go 白名单改动）
payments  + tx_hash TEXT   + 唯一索引 (tx_hash)          -- 链上交易防重入账
新表 usdt_deposits (
  id, chain TEXT,          -- trc20|erc20|bep20
  tx_hash TEXT, log_index INTEGER,   -- (chain,tx_hash,log_index) UNIQUE 幂等键
  from_addr TEXT, to_addr TEXT, amount_micro BIGINT,     -- 1e-6 USDT 整型存储，禁浮点
  block_no BIGINT, confirmations INT, seen_at TEXT,
  matched_order_id BIGINT DEFAULT 0   -- 0=未匹配（孤儿入账，人工处置）
)
```

### 2.4 配置键（system_config，后台「计费与套餐 Hub → 支付」分区）
| 键 | 说明 |
|---|---|
| `usdt_enabled` | 总开关（默认 0；关闭时下单/前端同步隐身） |
| `usdt_chains` | 逗号分隔启用链：`trc20,erc20,bep20`（默认仅 trc20——手续费 ~$1、确认 ~1min） |
| `usdt_addr_trc20` / `usdt_addr_erc20` / `usdt_addr_bep20` | 收款地址（校验链前缀格式；写审计 `usdt_addr_update`） |
| `usdt_rate_fen_per_usdt` | 人民币分/USDT 手工汇率 |
| `usdt_tail_enabled` | 尾数防混淆开关（默认 1；关掉则退化为纯人工对账模式） |
| `usdt_confirmations_trc20` 等 | 各链确认阈值（TRON 建议 19≈1min，ETH 12，BSC 15） |
| env：`TRONGRID_API_KEY` / `ETH_RPC_URL` / `BSC_RPC_URL` | RPC 凭证（secrets.env，不入 system_config） |

---

## 三、M1 人工核销轨（本周）

1. **下单**：收银台选 USDT（含链）→ `handlePayCreate(channel=usdt)` → 订单 pending，
   出参新增 `usdt_pay`：`{chain, address, amount_usdt(含尾数，字符串), rate_snapshot, expires_at, qr(地址二维码)}`；
2. **客户声明**：`/api/pay/manual-confirm` 扩展可选字段 `tx_hash`（正则 `^[0-9a-fA-F]{64}$`/`^[0-9a-fA-F]{6,64}$`(TRON)），
   落订单备注并随现有「critical 告警 + 站内信 + 邮件」通知超管（`pay.go:handlePayManualConfirm` 同一链路）；
3. **财务核对**：后台「计费与套餐 → 待确认单」列表展示 tx_hash 与跳转区块浏览器外链
   （`tronscan.org/#/transaction/…` / `etherscan.io/tx/…`）；人工核对四项：金额=应收（含尾数）、
   确认数达标、to=本方地址、该 tx 未在其他单出现；
4. **确认到账**：走现 `admin_order_pay`（`MarkOrderPaid` 单事务幂等）——**零新增资金代码**；
   补一条动作：确认时强制填写 tx_hash 写入 `payments.tx_hash`（唯一索引防一笔 tx 确认两单）。

## 四、M2 自动到账轨

**Reconciler（`internal/payment/usdtwatch`，watchdog 挂周期任务，复用 distlock 防多实例重复扫描）**：
```
每 30s（每启用链）：
  1. 拉取本方地址 Transfer 事件（since last_scanned_block，游标存 system_config）
     TRON: TronGrid /v1/accounts/{addr}/transactions (contractId=TR7NHq…)
     ETH/BSC: logs_filter topic0=Transfer, topic1=to_addr
  2. 归一化 → INSERT usdt_deposits（(chain,tx_hash,log_index) 冲突即跳过 = 幂等）
  3. 对 confirmations ≥ 阈值的未匹配入账：
     按 amount_micro 精确匹配 pending usdt 单（同址窗口：created_at±[now-24h, now]，
     且订单快照汇率；命中即 MarkOrderPaidByOrderNo（复用现幂等口）+ 回写 matched_order_id + payments.tx_hash；
     多单一命中金额（理论不可能，尾数唯一索引兜底）→ 不匹配，critical 告警人工裁决；
  4. 未命中入账（打错金额/无单转账）→ 留 pending 孤儿态 ≥72h，超期 critical 告警：人工退款（原路退回，
     手续费客户承担口径写 SOP）或豁免入账；
  5. 入账金额 < 应收（差尾数）→ 永不自动匹配；差额 ≥ 0.1% 提示客户按原单重付或人工改单。
```
前端轮询 `/api/pay/status` 语义不变（paid 即权益已到账）；新增 `usdt_wait` 阶段展示（确认数 x/n）。

**安全要点（逐条对应实现）**
| 威胁 | 防线 |
|---|---|
| 客户伪造 txid | txid 仅展示用；到账判定**只认服务端链上查询结果** |
| 一笔 tx 入账两次 | `usdt_deposits` 唯一键 + `payments.tx_hash` 唯一索引 + `MarkOrderPaid` 的 `status='pending'` 抢占条件（三保险，全部现成语义） |
| 同金额不同单串单 | 尾数唯一分配 + 迁移层 `UNIQUE(channel,amount_micro) WHERE matched_order_id=0` |
| RPC 假节点/回滚攻击 | 确认数阈值（TRON 19）+ 仅走 HTTPS 白名单 RPC + 快重启时游标回退 30 区块重扫（幂等键吸收重复） |
| 收款地址被调包 | 地址写 system_config 带审计；下单快照地址进订单，前端展示以订单出参为准 |
| 脏币/制裁地址 | M2.5：入账 from_addr 对 OFAC SDN 名单本地清单比对，命中冻结匹配 + critical 告警 |
| 汇率快照被篡改套利 | rate_snapshot 写订单后不可变；改价只影响新单（对齐 B1 单一事实源原则） |

---

## 五、UAT / 测试矩阵（闸门口径）

| 层 | 用例 |
|---|---|
| 单测 `payment/usdt_test.go` | 金额换算（分→6 位 micro，舍入边界）、尾数唯一分配与冲突回退、TRC20/ERC20 双精度解析、确认数阈值、RPC 响应 fixture 解析（httptest mock） |
| 单测 `store/usdt_test.go` | usdt_deposits 幂等键、payments.tx_hash 唯一、孤儿入账状态流转 |
| mock 组件 `scripts/uat/mock_chain.py` | TRON 兼容端点（地址交易列表可控注入「入账事件」），接入 run_uat 环境（同 mock_llm 模式） |
| **T42（api_uat_txn.sh）** | ①usdt_enabled=0 下单被拒；②M1：建单→出参含尾数金额→manual-confirm(txid)→admin_order_pay→余额入账→重复确认幂等；③M2：注入不足确认数事件→不匹配；达阈值→自动 paid；④同 tx 二次注入→只入账一次；⑤错金额转账→孤儿告警；⑥pending 双单金额不冲突 |
| e2e（Playwright） | 收银台 usdt tab 显隐（开关）→地址/金额渲染→「我已付费」提交 txid→状态轮询文案；暗色模式回归一张 |
| 生产冒烟 | 主网/测试网小额实转一笔（≤$10，TRC20 shasta 测试网优先），走完 M2 全链路截图归档 |

## 六、合规红线（必读，决定是否对外宣传）

1. **中国大陆**：根据 2021-09-24 十部委《关于进一步防范和处置虚拟货币交易炒作风险的通知》，
   虚拟货币相关业务活动（含**作为支付方式定价收费**）属非法金融活动；境内主体、境内客户场景**不得开通**本渠道，
   也不得在面向境内的落地页/白皮书出现「USDT 收款」字样。
2. 可行边界：收款主体与展业均在**海外**（本项目生产 langcross.lexicorn.cn / 首尔演示线）时，作为海外客户
   payment option 提供；订单仍按 USD/CNY 双计价留痕。
3. 财务/税务：USDT 入账按「当日汇率快照折算的应收金额」记收入，币→法币出金路径与汇兑损益归财务制度，
   本方案只保证**系统内应收口径不变**（amount_money 仍人民币分）；发票口径维持现行 SOP（普票补开）。
4. 建议 M3 用 Binance Pay/PSP 收编：托管合规、开票与反洗钱由 PSP 承担，平台只对接「查单+到账」，
   自有地址收款仅保留给小额与特殊客户。
5. 《商业化白皮书》《收款 SOP》《风险台账》三处需同步增补 USDT 行（实施时随代码一起改，不先挂空承诺）。

## 七、实施排期与工作量

| 项 | 工作量 | 依赖 |
|---|---|---|
| M1（开关+渠道注册+下单出参+txid 声明+确认落 tx_hash） | 0.5 天 | 无——可随本周发布（限海外线） |
| M2（迁移+mock_chain+reconciler+T42+e2e） | 2 天 | TronGrid key；《核实与修复方案》P1-1/P1-2 闸门修复先行合入，避免红灯叠加 |
| M2.5（OFAC 比对、游标自愈演练） | 0.5 天 | M2 |
| M3（PSP 评估对接） | 待商务 | 合规评审结论 |

> 上线顺序建议：先完成《核实与修复方案》P1-1（E2E 进闸门）+ P1-2（双红修复），使绿灯恢复可信，
> 再合入 USDT M1——新资金渠道永远不允许叠加在失守的闸门上。


---

## 八、落地记录（2026-09-16 补记）

**已交付（M1+M2 后端全量 + 前端全量 + 测试全量）**：
- provider `internal/payment/usdt.go`（换算/尾数/校验/TRON+EVM fetcher，BaseURL 可注入）＋单测；
- 数据层 `internal/store/usdt.go`：`usdt_orders`/`usdt_deposits` 双表、尾数唯一分配（8 次重试）、`payments.tx_hash` 唯一索引、幂等入账/匹配/游标＋单测；
- API：`/api/pay/create channel=usdt`、`/api/pay/manual-confirm` 声明哈希（缺失/格式分文案）、`/api/pay/status` 出 `usdt_pay`、订阅口支持 `channel=usdt`、notify 对 usdt 显式关闭；后台 `orders/pay` 确认 usdt 必填哈希 + 唯一性预检 + `usdt_info` 线索（声明哈希/精确额/浏览器外链）；`/api/admin/packages/settings` usdt_* 全键校验（开启需汇率+≥1 合法地址）；
- M2 对账器 `pay_usdt_watch.go`：默认休眠（usdt_auto_settle=0），开启后按链扫描→确认阈值→精确金额唯一命中→MarkOrderPaid 幂等入账；错金额孤儿池 + >72h critical；
- 前端：超管「运营」USDT 配置分区（开关×3/链/地址/钱包链接核验/汇率/确认数）、收银台 USDT 收款台（精确金额+尾数说明/地址复制/二维码/有效期/声明哈希闸）、待确认单 USDT 列与 tx_hash 回填；
- 测试：`scripts/uat/mock_chain.py`（TronGrid 兼容 + /inject /advance）；run_uat.sh 接线（8902/USDT_TRON_BASE/USDT_SCAN_INTERVAL_SEC=5，seed usdt_enabled=0 由 T42 自开自关）；T42 全链 28 断言；`e2e/usdt_payment.spec.ts` U1/U2；双方言矩阵全绿（PG：T 284/284、Playwright 33/33）。

**遗留（上线前置，非代码缺口）**：
1. 真链冒烟：对小额真实 TRC20 转入走一遍 M2（确认 TronGrid 生产响应结构与 usdt 精度），通过后方可开 `usdt_auto_settle=1`；
2. 运营配置：真实收款地址/汇率/确认数入超管后台；EVM 链如启用需配 `USDT_ETH_RPC/USDT_BSC_RPC`；
3. 合规：仅海外主体（langcross.lexicorn.cn）开通；境内主体保持关闭。
