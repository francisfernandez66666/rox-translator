# 翻译助手 SaaS 化销售差距分析

> 日期：2026-08-17
> 目的：对照《车企智能翻译平台·产品方案书》与 PLAN.md（P0 MVP），找出"能卖钱"还缺的能力清单与计划中未指明/被遗漏的项。

---

## 一、PLAN.md 已覆盖（SaaS 基础闭环）

| 能力 | 状态 |
|------|------|
| 三级角色 user/approver/admin + JWT + RBAC | ✅ 计划内（阶段 4/7） |
| 工单驱动流程（Ticket） | ✅ 计划内（阶段 2/7） |
| KB 三级包分层 + 四层检索 | ✅ 计划内（阶段 5） |
| evals 质量评估 | ✅ 计划内（阶段 8） |
| 管理后台（模型/策略/流程/用户） | ✅ 计划内（阶段 7） |
| 审批台（批准/编辑/驳回自动重启） | ✅ 计划内（阶段 6/7） |

**但这些只是"功能"，不是"能销售的产品"。** 销售一个 SaaS 还需要以下硬能力，PLAN.md 基本都没有。

---

## 二、销售必须、但 PLAN.md 缺失的能力（按优先级）

### 🔴 P0（不解决无法收款/开票/上线）

1. **租户体系（Tenant/Multi-tenant）**
   - 现有 PLAN 只有"三级角色"，没有"多租户"概念。
   - 销售场景：卖给 A 车企、B 车企、C 供应商…… 数据必须隔离（企业包、用户、工单、账单全部按租户）。
   - 需新增：`tenants` 表 + 所有业务表加 `tenant_id` 外键 + 路由级租户隔离中间件（RBAC 之上再叠租户维度）。
   - 现 PLAN 的"企业包（租户专属）"隐含了租户，但**用户体系、工单、计费、审批流都没挂租户**，等于只有一半。

2. **计费与计量（Billing & Metering）**
   - 方案书核心卖点："充值 token，余额不过期，不用买席位"。
   - 必须能：每次翻译/文件消耗统计 token 数 → 按租户/按部门计量 → 扣余额。
   - 需新增：`balance_accounts`、`usage_ledger`（按租户×时间×任务类型计量）、`rate_card`（单价表：普通句/安全句/高膨胀语种 ×倍率）。
   - PLAN 完全没有计费。

3. **支付与发票（Payment & Invoice）**
   - 销售闭环必须有充值入口：支付宝/微信/银行转账 → 到账 → 加余额。
   - 需新增：`orders`、`payments`、`invoices`（可对接第三方支付 SDK）。
   - PLAN 完全没提。

### 🟠 P1（上线后很快撞墙）

4. **多 LLM 供应商 + 成本核算**
   - PLAN 的 llmproxy 支持"任意填写模型"，但**没有成本核算**：不同供应商单价不同、翻译质量不同。
   - 需新增：供应商维度单价、token 成本记录（用于毛利核算）、模型路由策略（便宜模型 + 知识库命中优先）。
   - 否则"越用越便宜"无法量化呈现。

5. **租户级策略/白名单/隔离**
   - 安全句、术语、文化包是**租户自己上传**的（方案书：数字化营销部上传 Excel）。
   - 需新增：租户自服务上传 KB、租户自定义 Gate 参数、租户自己的安全词白名单。
   - PLAN 的 KB 管理是 admin 集中式，**没有租户自服务**。

6. **审计与合规留痕（ISO 17100 对齐的销售话术）**
   - 方案书反复强调"审计日志 + 人工批准留痕 + ISO 17100 对齐"。
   - 现有 PLAN 有 `audit_logs`，但需要**导出/查询/报表**能力（给客户合规审计用）。
   - 需新增：审计报表导出、按租户审计隔离、操作人 + 时间 + 前后值完整轨迹。

7. **配额与限流（租户级，防止滥用）**
   - 现 PLAN 无租户配额。一个租户死循环/大文件轰炸会拖垮共享资源。
   - 需新增：租户级 QPS / 并发 / 每日 token 上限 / 余额不足即停。

### 🟡 P2（销售增长与留存）

8. **试用与邀请机制**
   - 销售漏斗需要：注册 → 试用额度 → 转正式。
   - 现 PLAN 是"admin 后台创建用户，不开放自助注册"——**销售场景必须自助注册/试用**，否则每个客户都要 admin 手动建号，无法规模化。
   - 需新增：租户注册流程、试用额度、邀请码/团队管理。

9. **数据看板与用量报表**
   - 客户要看到"我充的钱花哪了"：按部门/按任务类型/按语言维度。
   - 需新增：租户用量报表页、余额明细、token 消耗趋势。
   - 方案书附录里明确有"数据看板"。

10. **API 对外开放 + API Key 管理**
    - 方案书卖点"≤2 秒/句（API）"——客户要集成到自己的研发流程（TMS/CI）。
    - 需新增：开放 API + 租户级 API Key 签发/轮换/权限控制 + API 文档 + 调用量统计。
    - 现 PLAN 只有内部 API，无对外集成能力。

11. **告警与运维监控（自己用的）**
    - LLM 供应商挂了（如今天的 Hunyuan 事故）、余额不足、错误率飙升——要有告警。
    - 需新增：健康检查面板（已有 health）、错误率监控、模型可用性告警、余额阈值告警。

---

## 三、PLAN.md 里有但需要"指明/补强"的项

| 项 | 问题 | 建议 |
|----|------|------|
| 多租户 vs 单租户 | PLAN 未明确最终形态。销售 SaaS 必须多租户，且共享 PG 实例 + 行级隔离（低成本） | 明确"单实例多租户（shared schema + tenant_id）" |
| 安全句的**租户归属** | 安全句是硬锁，但不同车企安全句不同 | kb_safety_phrases 加 tenant_id + 行业默认包 |
| 模型 Key 的**归属** | 不同租户可能自带模型 Key（BYOK） | 供应商配置分"平台级 Key"和"租户级 Key" |
| 驳回/重翻的**成本归属** | 驳回重翻产生额外 token，计费上归谁？ | 明确"驳回重翻计入租户用量"（或前 N 次免费） |
| evals 的**抽样率** | 全量 evals 开销大，销售场景成本敏感 | 增加可配置抽样率（如 100%/50%/10%） |
| 语言文化习惯包来源 | 政治文化避雷词清单需行业/地区数据，SaaS 通用包要持续维护 | 明确"平台维护通用包 + 租户可加私有包" |
| 并发/性能指标 | 方案书承诺"16 语种并发、≤2 秒/句 API" | 需压测基准 + 明确性能保障条款 |
| 数据导出（客户数据主权） | SaaS 客户要求能导出自己的知识库/翻译记录 | 需加"租户数据导出"能力 |
| 删除合规（GDPR 类） | 客户退订后数据删除流程 | 需明确租户注销/数据清除流程 |

---

## 四、建议的实施补充（追加到 PLAN.md）

### 追加阶段 0.5：SaaS 基础层（在 PG 阶段 1-2 同步做）
- `tenants` 表 + 全部业务表挂 `tenant_id`
- RBAC 升级为 `tenant_id + role` 双维度中间件
- 租户隔离中间件（所有查询强制带 tenant_id，防越权）

### 追加阶段 4.5：计费与配额
- `balance_accounts` / `usage_ledger` / `rate_card`
- 计量点：单句翻译、文件批量、驳回重翻、evals judge 调用
- 租户配额（QPS/并发/每日上限/余额停服）

### 追加阶段 6.5：对外销售接口
- 租户注册/试用/邀请
- 开放 API + 租户 API Key 管理 + 调用计量
- 用量报表 / 余额明细 / 审计导出

### 追加阶段 9.5：支付与发票（可选 MVP 简化）
- MVP 可用"线下转账 + admin 手动充值"过渡
- 二期再接支付宝/微信支付

---

## 五、优先级建议（如果资源有限）

**第一优先**（能做第一单生意的最小闭环）：
1. 多租户 + 租户隔离 ✅
2. 计量 + 余额 + 手动充值（线下收款，admin 充值）✅
3. 租户配额限流 ✅
4. 自助注册 + 试用额度 ✅

**第二优先**（客户留存与口碑）：
5. 租户自服务 KB 上传
6. 用量报表
7. 审计导出
8. API Key + 开放 API

**第三优先**（规模化运营）：
9. 在线支付
10. 告警监控
11. BYOK / 供应商成本核算

---

## 六、一句话总结

PLAN.md 是一个优秀的**单租户 SaaS 功能清单**，但缺了 **"卖" 的三个支柱：多租户隔离、计费计量、支付/发票**。补上这三点 + 租户自服务与配额，就从一个"企业内自用工具"变成了"可上线销售的产品"。