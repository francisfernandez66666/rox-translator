# 商业化白皮书 · 翻译助手（Token 预充值体系）

> 版本 v1.0 定稿 2026-08-25 ｜ 本文为对外商业化**唯一口径文档**
> 来源：《archive/BIZ_TOKEN_RECHARGE_2026-08-25.md》（客服项目原稿）经本项目化改写；技术实现细节以本文为准

---

## 一、商业模式一句话

> **翻译引擎与行业术语知识库免费送，AI 消耗量（token）预充值收费。**

| 类比 | 对应 | 说明 |
|------|------|------|
| 打印机 | 二进制引擎 + 行业术语库 | **免费分发**：官网下载、渠道铺货、零摩擦获客 |
| 墨盒 | Token 充值套餐 | **唯一收入流**：译得多买得多 |

设计理由：
- 客户付费与价值严格对齐——翻译量大说明产品有用，续费心甘情愿
- 免费分发消除一切试用阻力，铺货速度只受推广限制
- 每一份免费部署持续产出高质量对齐语料，反哺行业术语库迭代（数据飞轮）

## 二、计费单位：Token（不转译）

直接以 **token** 计价，不做"算力点""次"等二次转译。企业买家已被主流大模型厂商教育过该单位；财务可与上游供应商账单逐位核对。

销售参考口径：`1 token ≈ 0.75 个汉字或 0.25 个英文单词；一篇 5000 字产品手册约消耗 8000~15000 token（含审校流水线）。`

## 三、账户额度模型：发放台账 + 永久余额

### 3.1 结构

每个租户账户由两部分构成：

| 部分 | 名称 | 有效期 | 来源 |
|------|------|--------|------|
| 额度台账 | 体验包 / 订阅额度 | **带到期日，过期作废** | 新客礼包、订阅套餐每月发放 |
| 永久余额 | 充值余额 | **永久有效** | increment 充值包、邀请裂变奖励（付费奖励配为永久时；可后台改为限时台账，见 §五） |

> 台账设计说明：体验类额度允许多行并存（各自独立到期日），用于支持邀请裂变的"时长叠加"；消耗时按到期日从近到远逐行核销。

### 3.2 新客礼包

新租户完成注册（邮箱验证通过）自动发放：

```
体验包：300,000 token / 有效期 14 天
```

### 3.3 消费扣减顺序（对客户解释口径）

> "优先使用未过期的体验/订阅额度，用完才动您充值的永久余额；全部耗尽后翻译请求将被拒绝并提醒充值。"

```
① 存在未过期额度行 → 按到期日从近到远逐行扣减
② 额度行耗尽       → 扣减永久余额（原子操作，绝不扣负）
③ 全部为零         → 翻译请求拒绝，返回 insufficient_balance 并推送充值引导
```

灰度保护：上线初期 `billing_enforced=false`，额度耗尽不停服仅留痕告警；运营稳定后切 `true` 强制生效。

## 四、套餐体系

### 4.1 三类商品

| 类型 | 示例 | 到账 | 适用 |
|------|------|------|------|
| free 试用 | 新客礼包 | 注册自动发放入体验额度台账 | 所有新租户 |
| paid 包月订阅 | 基础版 ¥X/月·含 N 万 token；专业版 ¥Y/月·更高档 | 购买确认即发放，**有效期 t+30 天滚动** | 有稳定翻译量的长期客户 |
| increment 充值包 | 小包100万 / 中包500万 / 大包2000万 / 企业包1亿 | 一次性充入**永久余额**，永不过期 | 用量波动大或需长期储备的客户 |

存量 4 档旧套餐（按次计费时代）折算为 token 档位重新上架，价格锚点不变；"次"字段只读保留不再售卖。

### 4.2 定价原则

- **售价 = 上游大模型采购成本 × 3**（毛利约 66%）
- 以主力交付模型统一定价基准；分档系数留作后续运营弹性
- **所有数值后台可视化可调**（见 §九 参数化清单）

### 4.3 支付通道（复用现有收银台）

| pay_mode | 流程 | 状态 |
|---|---|---|
| mock | 下单→模拟到账即时开通（生产接口 403 封死）| 测试专用 |
| static_qr | 下单→展示收款码→客户点「我已付费」→财务群催告→超管核实确认→权益到账 | 当前主通道 |
| sdk 微信/支付宝 | 商户号到位后接入，回调自动到账 | 接口位已预留 |

保障：订单确认**幂等**（重复确认不二次发放）；pending 超 **15 分钟**自动关闭。

## 五、邀请裂变（多邀多得，单邀单个）

### 5.1 绑定铁律

| 规则 | 实现 |
|------|------|
| 一个被邀用户只能被绑定一次 | 首次有效，系统层面不可覆盖 |
| 同一用户被重复邀请无效 | 不产生任何奖励 |
| 同一对（邀请人→被邀人）每种奖励只发一次 | 去重闸门 |
| 邀请人可以邀请多个不同的人 | 不同被邀人之间奖励**叠加** |

### 5.2 计奖矩阵

| 被邀者行为 | 邀请者所得 | 去重维度 |
|---|---|---|
| 完成邮箱验证注册 | **+30 万体验 token** 且 **有效期 +14 天**（与既有体验包时长叠加）| 每个被邀人一次 |
| **首笔 paid 付费套餐**订单确认到账 | **+50 万 token**（后台可调；默认入永久余额，亦可配置为限时台账）| 每个被邀人一次（仅首笔）|
| 受邀者续费 / 再购 / 仅买充值包 | ❌ 不触发 | — |

叠加示例：
> 邀请 U 邀请 A、B、C 三人——A/B/C 注册分别使 U 的体验包各叠一层（+30万、+14天×3）；A 与 B 各自购买付费套餐到账后，U 累计 +100 万（默认永久余额配置下；若付费奖励配为限时，则计入带到期台账而非永久余额）；C 只买了充值包，U 无付费奖励。

### 5.3 载体与入口

- 邀请者专属链接：`https://langcross.lexicorn.cn/register?ref=<个人码>`（个人码唯一）
- **二维码**：后端生成 PNG，前台一键下载/复制链接
- 邀请记录页：被邀人 / 注册状态 / 是否已付费 / 奖励发放明细

### 5.4 防刷（2026-08-26 修订：oneid 双唯一）

- **账户体系**：`users.id` 为不可变主键（oneid）；邮箱是「同一时刻全局唯一」的绑定外键——
  换绑需通过新旧邮箱双验证，且**新邮箱不得存在于邀请奖励流水中（撞库即拒绝换绑）**。
- **奖励双唯一**：每类型奖励对「被邀账号 id」与「被邀邮箱快照」双重唯一——
  同一对(邀请人,被邀人)仅一次（既有）；该邮箱历史任意账号领取过同类型奖励即永久拒绝。
  注销重注、换绑流转均无法二次领取。
- **自助注销**：普通用户可自助注销；当日仍可使用，次日起无法登录；
  名下 API Key 即时停用；后台数据保留，管理员重新启用即可撤回。
- 被邀账号必须完成**邮箱验证**才计入奖励；同一邀请人单日新增 ≥5 笔奖励触发人工复核告警（不拦截，事后核查）。

## 六、各部署形态计费口径

| 形态 | 计费 | 说明 |
|------|------|------|
| SaaS 多租户 | 双部分标准计费 | 默认形态 |
| 本地化部署 | 同池计费 | 部署在客户服务器，但 **AI 调用统一走我方网关**（在线服务）；账号鉴权无机器绑定——多开实例=多消耗=多付费，天然公平 |
| OpenAPI 接入 | 同池计费 | 业务系统通过 API Key 将翻译引擎嵌入自有流程；消耗与本租户站内用量合并一个池子 |

## 七、安全与隔离承诺

0. **统一网关出站**：所有 LLM 调用一律经平台网关，租户无需也无法配置第三方模型凭据——token 定价权归平台，客户侧零凭据暴露面。
1. **Key 三绑定**：API Key 与「签发用户+所属租户」双重绑定，每请求运行时校验；跨租户查询一律 404 不泄露存在性
2. **明文只显示一次**：Key 仅创建时完整展示，库内只存哈希；停用即时生效
3. **译文交付隔离**：各租户译文/术语库严格租户隔离；OpenAPI 接入方的任务数据独立可审计
4. **数据回流脱敏**：用于行业术语库迭代的语料经脱敏处理，且租户可选择关闭回流

## 八、额度耗尽与防滥用

| 场景 | 客户看到 | 系统行为 |
|------|---------|---------|
| 余量 <10% | 后台横幅 + 群通知（去重，每周期一次）| 正常服务 |
| 全部耗尽 | 请求被拒，返回充值引导 | enforced=true 时 fail-closed 拒绝发起模型调用 |
| 用量突增 | — | 网关限流 + 告警人工核查 |
| 上线初期 | 一切照常 | billing_enforced=false 只记不停 |

## 九、财务对账口径

- usage_ledger 请求级落账：provider/model/prompt_tokens/completion_tokens/cost_micro，账单可与上游逐条核对
- 内部成本系数（markup_multiplier）与对外定价倍数（3×）两个口径互不混淆
- 分级看板：租户管理员看本租户用量趋势；超管看全平台成本占比与毛利
- 发票：orders 已预留字段，电子发票签约第三方后启用

## 十、角色流程速查

**租户管理员（客户）：**
```
注册（得30万体验token/14天）→ 后台选行业术语库 → 上传自有资料建私有KB
→ 发起文本/文件翻译工单 或 OpenAPI 批量接入
→ 低额提醒 → 💰商业化选购套餐/充值包 → 支付到账继续使用
→ 🔗邀请好友得体验叠加与永久奖励
```

**平台超管（我们）：**
```
维护行业术语包版本 → 核实 static_qr 订单到账（幂等）
→ 商业化参数调整（体验包/邀请奖励/阈值）
→ 监控：全平台 token 成本占比 / 租户用量报表 / 待确认订单 / 待审 TM 候选
```

## 十一、FAQ（本项目化预案）

**Q1 为什么按 token 不按"字/次"？**
不同内容难度差异巨大——单句口号与整份合规手册消耗差数十倍。按 token 最公平透明，且与我方付给大模型厂商的方式一致，可逐条对账。

**Q2 换便宜模型能省钱吗？**
能。支持分阶段模型配置（轻量模型做草翻、强模型做专业校对），默认已做此类优化帮您省成本。

**Q3 充值的 token 会过期吗？**
充值包永不过期；体验/订阅赠送额度按各自有效期（如新客礼包 14 天、订阅 t+30）到期作废。

**Q4 本地部署还要联网吗？要额外付费吗？**
需要联网（在线翻译场景）。本地化省的是软件授权费；AI 消耗统一走我方网关按 token 计费，您无需自购任何大模型 API。

**Q5 我上传的行业资料会被泄露吗？**
私有术语库/KB 严格租户隔离；参与行业库共建的语料均脱敏且可关闭回流。

**Q6 额度用完翻译请求会失败吗？**
灰度期不影响；强制计费启用后会拒绝请求并引导充值——提前关注低额提醒即可避免。

---

## 附：后台可调参数清单（商业化参数区块）

| 键 | 默认 | 说明 |
|---|---|---|
| free_trial_tokens | 300000 | 新客体验 token 数 |
| free_trial_days | 14 | 体验有效天数 |
| invite_reward_tokens | 300000 | 每邀1人·邀请者体验增量 |
| invite_extend_days | 14 | 每邀1人·邀请者时长叠加天数（后台可调，如设 30=一个月）|
| inviter_paid_reward_tokens | 500000 | 受邀者首笔付费套餐→邀请者 token（默认永久余额，可后台改为限时台账）|
| inviter_paid_reward_days | 0 | 受邀者首笔付费套餐→邀请者奖励有效期(天)；0=永久(默认)，>0=限时台账（按最早到期优先扣减）|
| order_pending_timeout_min | 15 | 订单自动关闭分钟数 |
| tm_review_threshold | 100 | TM 自闭环候选触发次数（既有）|

*本文与实现代码同步演进；任何规则变更先修订本文再动码。*
