# 能言 SaaS · 全面缺陷修复与前端增强修改文档（2026-09-12）

> 依据：2026-09-12 双方言矩阵全量 UAT 实测（SQLite/PG 均 67+79 API 断言全绿、前端 Playwright 16/16、Go 单测全包通过）+ 全量代码审查。
> 本文档**取代**《待修复与改进清单_20260911.md》中"P0 已清零"的结论——UAT 全绿是功能分支覆盖的结论，代码审查发现的资金/安全缺陷大多在 UAT 盲区（并发、PG 定时路径、退款公式、越权授权），必须处理。
> 每个修复项给出：问题 → 证据（文件:行号）→ 方案 → 验收标准。
> 条目总览：S 批 8 项｜A 批 5 项｜B 批 13 项｜C 批 32 项｜D 批 22 项（含增强 4）｜E 批 18 项｜F 批 11 项｜G 批 11 项｜**H 批 12 项（原 F12 规划能力，已拍板执行）**｜遗留（资质）4 项。已拍板：C26=token 真账+句数反推展示；F12=除商店上架外全做。

---

## 〇、范围声明

**本次不做（企业资质阻塞，接口保持现状）**：
- 微信/支付宝真实商户下单 HTTP 接入（`payment.go` "★ 真实接入点" 骨架保留，配好商户号即可补）
- 发票自动开具/税务渠道签约（数据一致性缺陷不在资质范围，见 C16，要修）

**本次照常做（虽涉支付但属代码缺陷而非资质）**：
- A5 支付渠道回调验签逻辑修复（URL 解码/金额舍入/按渠道路由 provider）——保证资质到位后零改动可用

---

## 〇-2、S 批 · 存储统一 PostgreSQL（2026-09-12 决策：生产只用 PG，SQLite 退出生产）

> 背景：项目此前"SQLite 一等公民、PG 二等公民"，本轮审查的资金/方言缺陷（A1/A2/C17）全部源于此。决策改为**生产统一 PostgreSQL**，一次性消除整类方言风险；SQLite 仅保留为本地开发/单测的可选便捷态。
> 可复用资产：`cmd/migrate-sqlite-to-pg`、`scripts/cutover-to-pg.sh`、`改造方案_解锁PG与Redis及路线图.md`。

| # | 事项 | 证据/落点 | 方案 | 验收 |
|---|---|---|---|---|
| S1 | 生产配置强制 PG | `config`、`cmd/server/main.go`、`deploy/systemd/prod.conf` | `DB_DRIVER` 缺省 `postgres`；检测到 sqlite 且非本机回环地址 → 启动 FATAL 拒绝 | prod 配置演练 |
| S2 | 行锁语义按 PG 落地 | A1（`quota_grants.go`、`queue/direct.go:57-63`） | `FOR UPDATE SKIP LOCKED` 成为主路径（不再是"PG 才需要"的补丁）；队列领取同修 | A1/G1 并发测试在 PG 绿 |
| S3 | JSON 助手 PG 优先 | A2、`db/jsonops.go` | `JSONSetStr/True/False/JSONNumAdd` 等以 PG `jsonb_set` 实现为准，SQLite 仅为兼容实现 | G2 grep 护栏 + jsonops 双方言单测 |
| S4 | 语义检索统一 pgvector | `kb/db.go:1087`、`kb/npz.go` | 生产唯一路径 = pgvector + **HNSW 索引**（D10 并入本项）；npz 仅保留给 dev/单测并在启动日志标注 | 检索性能基准（10 万行 <50ms） |
| S5 | 方言杂项清扫 | C17（`?` 占位符裸 SQL）、`INSERT OR IGNORE`（rewrite.go 已翻译，保留）、时间基准 C22 | 全仓禁裸 `QueryRow`，统一走 `db` 层；迁移清洗 | CI grep 裸 SQL 扫描 |
| S6 | 备份/恢复/监控切 PG 单轨 | `store/backup.go`、`deploy/observability`、`上生产操作手册` | 生产预案只保留 pg_dump/WAL 路径；SQLite VACUUM 路径标注 dev-only | `restore_drill.sh` 按 PG 演练一次 |
| S7 | UAT 矩阵主次调整 | `scripts/uat/run_uat.sh` | **PG 为必跑主矩阵**（发布闸门）；SQLite 矩阵降级为本地快跑选项，CI 至少保一份 | run_uat 默认值改 `DB_DRIVER=postgres` |
| S8 | 演示/引导脚本 | `scripts/bootstrap-demo.sh`、`bootstrap-seoul.sh` | 引导建 PG 库 + `CREATE EXTENSION vector` 前置检查 | bootstrap 端到端演练 |

**收益**：A1/A2/D10/D11/C17 从"补丁"升级为"主路径设计"；消除双实现漂移（kb 双后端、backup 双路径）的长期维护成本。
**风险**：单机部署门槛提高（需要一个 PG 实例）；迁移期间 dev.db 存量数据用现成 `migrate-sqlite-to-pg` 一次性搬迁。

---

## 一、A 批 · 资金安全（P0，最优先）

> 注：按 S 批决策生产统一 PostgreSQL，A1/A2 从"PG 兼容补丁"升级为**主路径必修项**。


### A1 PG 并发扣费误判 → 清零租户余额（事故级）
- 证据：`store/quota_grants.go:156-192`（SELECT 台账无 `FOR UPDATE`，守卫 UPDATE `RowsAffected==0` 直接 `return ErrInsufficientBalance`）；`billing/sink.go:220-242`（`ErrInsufficientBalance` → `SettleExhausted` 双桶清零 + critical 告警）；`db/db.go:61-67`（`_txlock=immediate` 仅 SQLite 生效）。
- 方案：
  1. `deductWithGrantsTx` 的 SELECT 加方言化行锁：PG 用 `FOR UPDATE OF quota_grants SKIP LOCKED`（SKIP LOCKED 跳过被并发事务核销中的行），SQLite 保持现状。
  2. 守卫 UPDATE 冲突不再返回 `ErrInsufficientBalance`，改为**重读该行余额后重试（每行上限 2 次）**，真实不足才报。
  3. `sink.go` 欠费结算前先 `TenantRemainTotal` 复核：总额确 <0 才走 `SettleExhausted`，否则按"其余错误"回插缓冲重试。
- 验收：新增 `go test -race` 并发测试（≥50 goroutine 同租户扣超，断言余额不为负、无 ErrInsufficient 误报）；UAT 新增 T16 并发压测断言（见 G 批）；PG 矩阵跑通。

### A2 PG 方言漏网：套餐到期摘除/提醒标记在 PG 必坏
- 证据：`store/packages.go:547-551`（`ExpirePackage` 裸 `json_set(...'$.package_code',...)`）、`:565-567`（`SetNotifiedExpFlag` 同款）；`db/rewrite.go` 不翻译 `json_set`；`db/jsonops.go` 只有 `JSONSetFalse`。影响 `api/watchdog.go:251,265,270,308` 每日任务。
- 方案：`jsonops.go` 补 `JSONSetStr`/`JSONSetTrue` 助手（PG 用 `jsonb_set(..., 'null'::jsonb)` / `'true'::jsonb`），替换两处裸 SQL；扩展示例覆盖。
- 验收：jsonops 单测双方言；UAT 新增到期摘除端到端断言（时间旅行：直接把 `package_expires_at` 改成过去 → 手动触发到期 handler → 断言 PG 下 permissions 已清）。

### A3 退款链路残缺（资损 + 无法对账）
- 证据：`store/billing.go:1250-1300`（纯充值单 `remain:=tokens` 全退，消耗核算只在 `pkgID>0` 分支；注释与实现矛盾）；`:1276`（消耗用 `SUM(quantity)`，字符/token 单位混用）；`refund_money` 不在 `orderCols`（`:94`）故 API 读不到；退款不写 payments 冲正流水、不清 `sentence_balance` 镜像（`api/admin_billing.go:232-243`）、不回收邀请奖励（`store/referral.go` 与退款零联动）、与发票无联动；升级单 `oldRemain` 转入部分也被整单全退。
- 方案：
  1. 消耗核算统一 token 口径（`cost` 列，依赖 C1 先归一），纯充值单同样核算 paid_at 之后的消耗；
  2. `orderCols` 补 `refund_money`；退款写 `payments` 冲正流水（负向记录，单位按 C2 归一后的口径）；
  3. 退款事务内：回收该订单触发的 `referral_rewards paid_perm`（台账作废/余额扣回，缺口告警）；清句数镜像；
  4. `refunded` 订单禁止开票、已开票置"需冲红"状态（数据层，不接税务）；
  5. 升级单退款：`granted` 取入账全额（含 oldRemain）口径重算，防旧包 token 白拿。
- 验收：T 套件新增 T21（已消耗充值单退款金额断言）、T22（退款后邀请奖励回收断言）、T23（refunded 单开票被拒断言）。

### A4 editor 横向越权
- 证据：`api/editor.go:47-58`（读/存/导出仅校验 `u.TenantID != t.TenantID`，同租户任意普通用户可读写他人工单全部段），与 `api/tickets.go`「仅创建者」口径冲突。
- 方案：抽公共 `canAccessTicketUser(u, t)`（创建者 ∪ 同租户审批角色/租户管理员 ∪ 超管），editor 三处 handler 统一接入。
- 验收：UAT T17：同租户 B 用户访问 A 的工单段 → 403。

### A5 渠道验签逻辑修复（代码缺陷，与资质无关）
- 证据：`payment/payment.go:366-375`（`parseKV` 不 URL 解码，真实支付宝 form 回调必挂）、`:425`（`toFen` 浮点截断 0.29→28 分）、`:232-257`（不校验 app_id/seller_id）；`api/pay.go:49-68`（`payProvider()` 只按全局 pay_mode 建一个 provider，`/api/pay/notify/alipay` 用 wechat 验签；sdk 模式硬编码 wechat，`channel=alipay` 订单永远验不过）。
- 方案：`parseKV` 加 `url.QueryUnescape`；`toFen` 改 `math.Round`；验签校验 app_id/seller_id（配置项，缺省跳过）；`payProvider(channel)` 按**订单渠道**路由而非全局模式。
- 验收：payment 包单测覆盖解码/舍入/渠道路径；T13 回调安全闸断言不回归。

---

## 二、B 批 · 安全与认证（P0/P1）

| # | 问题 | 证据 | 方案 | 验收 |
|---|---|---|---|---|
| B1 | 启动日志泄漏 DSN 密码与初始超管明文密码；登录日志打邮箱 | `cmd/server/main.go:128,185`（DSN）、`:164`（明文密码）、`api/auth.go:343` | DSN 脱敏函数（掩码密码段）；初始密码写 `0600` 文件 + 日志只提示路径；邮箱打码 | T18：启动日志 grep 无明文密码/完整 DSN |
| B2 | 无会话撤销：改密/重置后旧 JWT 24h 有效 | `iam/auth.go:61-68` | users 加 `token_version` 列；签发带 `tv` claim；`authUser` 比对；改密/重置/禁用 +1 | 单测 + T6 扩展（改密后旧 token 请求 401） |
| B3 | SSO 回调 token 走 URL 重定向；state cookie 无 Secure；provision 角色 `"member"` 非法 | `api/sso.go`（callback、provisionSSOUser:158） | 改一次性 exchange code（短 TTL 单票）前端换 token；cookie `Secure+HttpOnly+SameSite=Lax`；角色改 `user` | UAT 无 SSO 环境，单测覆盖 code 兑换 |
| B4 | 邮件 Noop 模式把验证码写日志 | `mail/mail.go:76` | 验证码不落日志（写 Redis/DB 带 TTL）；启动时 Noop+未配 SMTP → WARN 横幅 | 代码审查 |
| B5 | 匿名 quota 落租户 1；billing/config 暴露计费开关 | `api/billing_api.go:263,315` | quota 要求登录；config 收敛为公开字段白名单 | T14 扩展断言 401 |
| B6 | 策略保存注释称"安全裁剪"实际整包直写；时间窗可覆盖 `payment.mode`/`billing.enforced` 成后门；window.ID 不校验 | `api/ops_api.go:207-209,233-252`；`ops/policy.go` | 保存走字段 allowlist；**窗口 overrides 禁止 payment.mode、billing.enforced**；ID 非空+唯一校验 | UAT T19：窗口带 payment.mode 保存被拒 |
| B7 | webhook secret 明文存储 | `store/webhooks.go` | 复用 `EncryptSecret`（AES-GCM） | 存量迁移 + 单测 |
| B8 | 登录用户可按路径下载 UploadDir 任意文件 | `api/stream.go` handleEditorExportDownload | 复用产物归属校验（ticket→tenant→user 链） | T20：跨用户下载 403 |
| B9 | watchdog 自检连续失败即 `os.Exit(1)`，无重启风暴熔断 | `api/watchdog.go:97-104` | systemd `StartLimitBurst` 文档化 + 进程内退出前冷却计数 | 部署文档同步 |
| B10 | legacy 固定盐口令路径 | `iam/auth.go:239`（`trans-salt:`） | 保留透明升级，升级后强制落 bcrypt；加计数日志观察存量 | 单测 |
| B11 | 运营邮箱与默认域名硬编码泄漏运营方身份 | `api/mail_tpl.go:69`（CC `575160894@qq.com`）；`tenant.go:305,332,529`、`office.go:20`、`user_import.go:212`、`auth.go`、`crawler/fetch.go`（`lexicorn.cn`） | 全部收敛到 `system_config`/环境变量（`OPS_NOTIFY_EMAIL`、`BRAND_DOMAIN_SUFFIX`），代码零明文默认 | 全仓 grep 无命中 |

---

## 三、C 批 · 账务口径与一致性（P1）

| # | 问题 | 证据 | 方案 |
|---|---|---|---|
| C1 | 两套 cost 公式并存：单条 `qty×price×mult`，批量 `qty` 原样 | `store/billing.go:215 vs 271,321` | 统一 `cost=qty×price×mult`（批量落库前折算）；一次性迁移回填历史批量行；`SUM(cost)` 从此可对账 |
| C2 | `payments.amount_money` 存分、`orders.amount_money` 存元 | `billing.go:1011-1033` | 列更名 `amount_fen`（或统一元），迁移脚本 + 所有读写点 |
| C3 | plan 台账到期硬编码 +30 天，无视 `pkg.DurationDays`；升级转入 oldRemain 被压进新窗口 | `billing.go:1079,1102` | 到期按 `DurationDays`（0=永久）；升级行保留原到期时间 |
| C4 | `unitPrice` 永远查 `lang='*'`，rate_card 语种维度未接入 | `billing.go:357-375` | 按目标语种取价，缺行回落 `*` |
| C5 | `incrementDailyUsage` 事务外吞错 → usage_daily 少计；日限额回退查询是死兜底 | `billing.go:506-524` | 并入扣费事务；回退分支改"当日无行才聚合 ledger" |
| C6 | 默认值散落 5+ 处（trial 30万/14天、markup 1.5、rate 500） | `register.go:520/526`、`admin_packages.go:183/189`、`plans_api.go:39/45`、`policy.go:213-217`、`packages.go:515/522`、`billing.go:888/1507`、`billing_api.go:104/175` | 收敛到 `ops/policy` 默认值单一来源（`DefaultEffective`），其余处引用 |
| C7 | 月度重置三步非原子可超上限；且把升级转入行 `left=total` 拉回造成价值通胀 | `ops_api.go:289-306`；`store/quota_grants.go:76-86` | 单事务 + 租户行锁计数；重置只作用于本周期 plan 行 |
| C8 | `Merge` 浅拷贝改写 base 的 ModeRules map | `ops/policy.go:241,269` | 深拷贝；单测锁定 |
| C9 | MarkOrderPaid default 分支按 `pkgSentences×rate` 入账，无 markup、无视订单 `amount_tokens` | `billing.go:1132` | 统一取订单 `amount_tokens`（下单为唯一事实源） |
| C10 | token 回填不含 markup，升级抵扣 ratio 失真 | `billing.go:884-910` | 回填口径与新单一致 |
| C11 | `DeductSentences` 无 `n<=0` 守卫（负数变加钱）；`int64(...)` 向下取整小额免费化 | `packages.go:345-357`；`billing.go:215,342` | 参数守卫 + `math.Round` |
| C12 | `nonSuperDisplayFactor=5` 真实/展示双口径 | `api/admin_billing.go:22,70-101` | 展示乘数移到前端品牌层或直接移除；若保留，响应字段名区分 + 水印提示 |
| C13 | watchdog 余额告警只看永久桶，与已整改的 `TenantRemainTotal` 口径分裂 | `api/watchdog.go:357-373` | 改用 `TenantRemainTotal` |
| C14 | 包创建不校验 `price_money>=0`；0 元与不传不可区分；DurationDays 改不回不限期 | `api/admin_packages.go:63-93,154,183` | 指针字段 + 负价拒绝 |
| C15 | `DeletePackage` 不检查 pending 订单/活跃订阅引用 | `store/packages.go:144-147` | 引用检查，被引用改停用 |
| C16 | 同单可重复开票；无作废/冲红状态 | `store/billing.go:1381-1397` | `(order_id,status<>'void')` 唯一约束 + void 流转（数据层，与 A3.4 联动） |
| C17 | PG 下 `?` 占位符裸 QueryRow 静默失败 | `api/api_openapi_tasks.go:547`；`plans_api.go:91-98` 裸 SQL | 全部走 `db.QueryRow(dialect)` 方言层 |
| C18 | `auto_charge`/plans mock 确认错误被吞，用户拿到"成功"实为 pending | `admin_billing.go:175-179`、`plans_api.go:182,256` | 错误传播 + 响应带真实状态 |
| C19 | manual 单 15 分钟超时取消后，已转账用户无补单通道 | `billing.go:1540-1556` | 用户"我已付费"失败时自动重建订单（复制金额/券证字段）或允许超管对待审计注入单直接放行 |
| C20 | 欠费批次不落任何账：已发生成本无痕 | `billing/sink.go:226-242` | 欠费结算批改走 `LogUsageBatch`（留痕不扣费），告警带批次明细 |
| C21 | 死代码/废弃导出 | `billing_api.go:601`（`var _ =`）、`billing.go:165`（`Deduct`） | 清理/转私有 |
| C22 | 时间基准混存（本地 RFC3339 vs UTC 字典序侥幸成立） | `quota_grants.go:154-158` 等 | 存储统一 UTC RFC3339；过期比较统一 `time.Now().UTC()`；迁移清洗脚本 |

---

## 四、D 批 · 引擎与文件正确性（P1/P2）

| # | 问题 | 证据 | 方案 |
|---|---|---|---|
| D1 | **docx 写回 XML 实体截断**：译文含 `& < > " '` 产出残缺实体（"R&D" 场景文档损坏） | `fileproc/docx.go:276-280` | 长度计算统一用转义后字节/游标；新增含特殊字符端到端回归测试 |
| D2 | **FindExact SELECT 38 列 vs scan 39 列恒失败**，静默丢 KB 精确命中 | `kb/db.go:570-574,672` | 补 `pack_id` 列 + 单测 |
| D3 | 停用包术语仍经 `kb_entries` 路径注入 prompt | `store/kbpackages.go:714,748,777`；`SetKBPackageEnabled:992-996` | 三查询补 `COALESCE(p.enabled,1)=1` |
| D4 | PDF 纯 Go 兜底 `WriteTranslatedPDF` 死代码：pdf2docx 不可用直接判失败 | `fileproc/pdfwrite.go:87`；`engine/file.go:644-656` | 接线为兜底链一级（版式降级为对照表可选） |
| D5 | txt/csv/md 写回失真：空行/重复行丢失、md 结构被剥毁 | `engine/file.go:874-884` | 按原行对齐输出（不先去重）；md 只替换文本节点保留标记 |
| D6 | tier2 采集把 `<th>` 表头当术语对，自动审批入正式库 | `crawler/tier2_web.go:113-131` | 跳过表头行 + 黑名单（"中文/English"类） |
| D7 | docx 脚注/尾注未提取；header/footer 同名循环笔误 | `fileproc/docx.go:106` 等 | 补提取；修部件遍历 |
| D8 | pptx 备注页/母版/图表文字未提取 | `fileproc/pptx.go:64,159` | 增 notesSlides（可配置开关） |
| D9 | 多处 `time.Sleep` 不响应取消（客户端断开后仍烧 token） | `engine.go:846,1295,1310,1641`；`file.go:667` | `select { case <-ctx.Done() case <-time.After() }` |
| D10 | pgvector 无索引，语义检索全表扫（**并入 S4**） | `kb/db.go:1087` | 生产唯一路径 pgvector + HNSW（cosine）索引迁移；npz 降级为 dev-only |
| D11 | `LIKE '%x%'` 子串查询不可走索引，KB 增大线性劣化（**并入 S4/S5**） | `kb/db.go FuzzyHits`；`kbpackages.go:748-774` | PG `pg_trgm` GIN 索引；结果短 TTL 缓存 |
| D12 | robots 不可达 fail-open（注释自认） | `crawler/fetch.go:94-106` | 加配置 `CRAWLER_ROBOTS_FAIL_CLOSED`，默认改 fail-closed |
| D13 | tier3 注释称用 stage 模型实际用全局 | `crawler/tier3_llm.go:151-158` | 接 `resolveStageModel("ai_initial")` |
| D14 | 采集器每批新建 fetchBase：限频/robots 缓存跨批失效、词表重复下载 | `tier1_api.go:69`；`tier2_web.go:34` | fetchBase 提升为采集任务级复用 |

---

## 五、E 批 · 前端功能缺陷（P1）

| # | 问题 | 证据 | 方案 | 验收 |
|---|---|---|---|---|
| E1 | **聊天消息不持久化**：发送/回填不走 `schedulePersist`，刷新即丢（违背设计注释） | `hooks/useChat.tsx:171-172,201` vs `:93-109` | `startSend`/`patchMsg` 完成后统一触发节流持久化 | vitest：发送后重挂载能恢复 |
| E2 | **聊天记录跨账号泄漏**：localStorage 全局键 `chat_msgs_v1` | `useChat.tsx:34` | 键加用户哈希 `chat_msgs_v1_{uid}`；登出清除 | vitest |
| E3 | `/?token=` 注入登录态：token 进地址栏/Referer/历史 | `App.tsx:21-33` | 改一次性 exchange code（配合 B3）；过渡期至少改 fragment + 禁 Referer 策略 | E2E 更新 |
| E4 | `...options` 覆盖合并 headers → 传 headers 的调用全丢 Content-Type | `api/core.ts:91-95` | `headers: {...合并, ...(options.headers||{})}` | vitest 断言 |
| E5 | SSE 通道无 401 处理：token 过期只显示"请求失败(401)"不清态 | `api/translate.ts:66,114` | 非 2xx 解析错误码，401 走 `handleUnauthorized` | vitest |
| E6 | 工单详情轮询闭包捕获旧 `detail=null`，轮询永不刷新 | `TicketsPage.tsx:102-112` | 轮询 id 用 `useRef` 存当前打开工单，interval 内只读 ref | 手工 + Playwright |
| E7 | `about:blank` + `document.write` 打开后端 HTML | `ApiKeysP.tsx:107-108` | 改受控 iframe `srcdoc`（sanitize）或后端签名 URL 新标签 | 代码审查 |
| E8 | `history.pushState` 与 react-router 脱节（面板切换/通知点击） | `stores/admin.tsx` gotoPanel、`Bell.tsx` | 统一 `useNavigate`/location search 驱动 PanelKey | E2E：前进后退行为一致 |
| E9 | `AuditP` 定义了无入口；`invites/usage/alerts/mailTpl/footer/dataSources` 等 PanelKey 在 renderPanel 缺分支返回 null | `panels_a.tsx:459`；`AdminDashboard renderPanel` | 补菜单入口（超管）+ 补全分支 | Playwright P5 扩展截图 |
| E10 | 错误处理系统性缺席：十余处 submit/操作 `await` 无 catch，失败静默 | `Login.tsx` doRegister/doSendCode/doReset、`modals.tsx`、`TicketsPage` run/cancel/delete、`OrgP.createUser`、`DataSourcesP` 各操作、`IndustriesP` 全部 | 统一 `runGuarded()` 包装：catch → `message.error(err)`；ESLint 规则兜底 | 全仓 grep `await ` 无裸调用 |
| E11 | 统一错误码体系零消费（余额不足无法差异化提示） | `core.ts` `bizErrorCode`/`ApiError.code` 无调用方 | 关键码接 UI：`insufficient_balance` → 充值引导弹窗；`billing_stopped` → 顶部横幅 | E2E 截图断言 |
| E12 | 硬编码 URL 绕过 API_BASE | `branding.tsx`（/api/tenant/branding）、`PlansP.tsx:221`、`ApiKeysP.tsx:133` | 统一走 `API_BASE` | 代码审查 |
| E13 | 密码输入框缺 `type="password"` | `panels_a.tsx:313`、`OrgP.tsx:457` | 修复 + label | 代码审查 |
| E14 | 死代码：`ticketList/billingBalance/billingUsage/billingConfig/apiKeyReveal/kbPackageUpdate/publicIndustries/getDownloadUrl/openAPIDocsUrl`、ChatWindow 死导入/死变量、core.ts:45 陈旧注释 | `api/*.ts`、`ChatWindow.tsx:14,229-233` | 删除或接入真实调用点；`tsconfig` 开 `noUnusedLocals/Parameters` | `tsc` 通过 |
| E15 | 品牌 Logo/背景 base64 无大小校验（存储膨胀） | `BrandP.tsx` 上传 | 前端限 200KB + 后端 Content-Length 校验（`api/tenant/branding` 侧） | 单测 |
| E16 | i18n 键跨域误用 | `PlansP.tsx:243,249`（借 apikeys/webhooks 键）、`TicketsPage` 列标题借 `users.colLastLogin` | 建本域键 | parity 测试 |
| E17 | Markdown 正则 lookbehind，Safari<16.4 抛 SyntaxError | `MessageBubble.tsx:287` 附近 | 换无 lookbehind 实现或特性检测降级为纯文本 | 手工 |
| E18 | 工单号 `T2026...` 输入编辑器产生 `NaN` 请求 | `EditorPage.tsx:143` | 数字 ID 校验 + 按 order_no 反查（或提示正确入口） | 手工 |

---

## 六、F 批 · 前端 UI 与能力增强

### F1 顶栏租户 Tag（解锁 3 个 skip 的正式需求）
- 证据：`e2e/tenant_label.spec.ts:29-30` `describe.skip`，注释"待实现 Header 租户 Tag"。
- 方案：Header 加租户名徽标（个人用户显示"个人版"，企业显示租户名+角色），i18n 双语。
- 验收：取消 skip，Playwright 3 用例转 PASS（矩阵共 19 passed）。

### F2 i18n 硬编码补全
- 范围：`EditorPage.tsx` 整页、`TicketsPage` STEP_NAMES/列标题、`ChatWindow` 语言下拉/余额条、`DataSourcesP`/`IndustriesP`/`BrandTermsP` 三面板、`App.tsx` 抽屉项、`useChat.tsx:171` "准备中"。
- 验收：`i18n/parity.test.ts` 扩展"组件内不得含中文字面量"扫描（排除注释）。

### F3 RTL 与多语言体验
- 品牌语言含 ar/fa 但全站无 RTL：`document.documentElement.dir` 随语言切换；CSS 改逻辑属性（`margin-inline` 等）优先覆盖工作台/账单页。

### F4 无障碍基线（对齐 WCAG AA 子集）
- 可点击 `<a onClick>` 改真实 `button`（BrandTermsP:186-187 等全仓巡检）；表格操作列 aria-label；`:focus-visible` 样式；`prefers-reduced-motion`；表单错误 `aria-invalid`。
- 验收：axe-core 扫描 E2E（`@axe-core/playwright`），关键页 0 severe。

### F5 暗黑模式
- TDesign 主题变量 + 自绘 CSS 变量双轨，localStorage 记忆，跟随系统。

### F6 移动端深化
- 现仅 900/640 两断点：后台表格窄屏卡片化/横向滚动指示；工单/编辑器窄屏布局；像素巡检用例扩展语言矩阵（ar/en）。

### F7 工作台能力增强
1. **消费估价接 UI**：后端 `/api/translation/estimate` 已有、前端零调用 → 发送前 token/费用预估提示，余额不足前置预警（配合 E11）；
2. 会话历史检索与导出（本地持久化后加关键词过滤）；
3. 译文双栏对照视图 + 一键复制原文/译文；
4. KB 命中徽标（精确/模糊/语义，后端 `kb-stats` 已有）；
5. 快捷键（Cmd+Enter 发送、Cmd+K 语言切换）。

### F8 自服务页增强
- 用量趋势图（`usage/me` 数据已备）；订单/台账/奖励明细分页与筛选；余额不足横幅 + 静态码/线下付款引导（配合 C19）；发票列表状态（待开/已开/需冲红，配合 C16/A3）。

### F9 后台面板增强
- 运营策略时间窗**表单化编辑器**（现在 JSON textarea，易配出后门——配合 B6 字段限制）；
- 告警中心筛选/静默；对账视图（orders ↔ payments ↔ usage_ledger 三表勾稽，数据层 C1/C2 完成后才有意义，放本批末位）。

### F10 SDK 与插件
- 扩展 popup 与 Python SDK 示例前缀 `tk_` → `rk_`（`extension/popup.js:20`、`sdk/python`、`sdk/js` 注释）；
- 浏览器扩展补：术语高亮回显、快捷键、错误码提示（与前端 E11 同口径）；
- OpenAPI Spec 自动生成（后端 `openapi/v1.json` 手写 → 路由注册期自动导出，凭据无关，做）。

### F11 前端测试基线
- vitest 从 1 文件扩至：`core.ts`（headers/401/超时/错误码）、`useChat`（持久化/隔离/模式记忆）、selfservice 估价换算、每个 E 批修复项至少 1 用例。

---

## 七、G 批 · 自动化 UAT 盲区补强

| # | 新增断言 | 位置 |
|---|---|---|
| G1 | T16 并发扣费压测：并行 30 请求打空余额 → 无双扣、无负余额、无误触发清零告警 | `scripts/uat/api_uat_txn.sh` |
| G2 | json_set 方言护栏：CI `grep -rn 'json_set(\|json_extract(' backend-go --include='*.go' | grep -v jsonops\|_test` 有输出即 fail | `build.sh` / CI |
| G3 | `go test -race ./...` 进 run_uat | `scripts/uat/run_uat.sh` |
| G4 | T17 editor 同租户越权 403；T18 启动日志脱敏 grep；T19 策略窗口禁改 payment.mode；T20 跨用户产物下载 403；T21-T23 退款口径（A3 配套） | txn 套件 |
| G5 | PG 矩阵补 watchdog 路径：注入触发"到期摘除/提醒标记"handler 断言 permissions 实际变更 | txn 套件 |
| G6 | Playwright：tenant_label unskip（F1）、axe 扫描（F4）、错误态截图矩阵（E11） | `e2e/` |
| G7 | 前端 vitest 组件测试基线（F11） | `vitest` |

---

## 七-2、增补清单（同日二轮盘点补漏，分别并入对应批次执行）

### 并入 C 批（账务/裂变）
| # | 问题 | 证据 | 方案 |
|---|---|---|---|
| C23 | 裂变奖励"占用与到账不原子"：流水 INSERT 成功后发放失败错误被吞，**占用闸反向把补发路径焊死**（钱没到、永久无法重试） | `store/referral.go:151-172,200-218`；`store/billing.go:1141-1143`（提交后调用且返回值丢弃） | 占用与到账同事务；或到账失败落"补偿队列"由 watchdog 重试 |
| C24 | `EnsureRefCode` check-then-UPDATE 竞态且 UPDATE 错误未检查，冲突时可能返回**未持久化的码**（邀请链接失效） | `referral.go:65-88` | 改单语句 UPSERT + 随机码重试环 |
| C25 | 邀请奖励"日上限判断→发放"非原子，并发注册可小幅击穿 `maxDaily` | `api/register.go:458-463`；`referral.go:313-319` | 计数器条件 UPDATE（带 `count<max` 守卫）后再发放 |
| C26 | **句数余额是纯镜像、真实链路永不扣减**（`DeductSentences` 生产零调用，已核实）——展示口径"剩余句数"会虚高 | `store/packages.go:338-357`；后端 42 处引用；前端 `selfservice.tsx`/`PlansP.tsx`/`ChatWindow.tsx` 等多处句数展示 | **【已拍板 2026-09-12：以 token 为唯一真账，句数=token 反推展示】** ① 后端统一出参 `tokens_available` + `sentences_estimate = total_available / rate`（rate 走 C6 收敛后的单一来源）；② 前端所有"剩余句数"改读反推值，展示文案"约可翻 N 句"；③ `DeductSentences` 删除，`sentence_balance` 保留为发放流水（只增），加注释"镜像字段，禁止读取判额/对账"；④ 全仓 grep `sentence_balance` 的 42 处逐点改造：展示→反推值，逻辑（判额/告警/重置）→一律改 token 口径 | 前端显示句数 ≈ 真实 token 余额换算；任何翻译消耗后句数展示正确下降；死函数清零 |
| C27 | `order_no` 唯一索引是 `(tenant_id, order_no)` 租户级，`FindOrderByOrderNo` 却按全局 LIMIT 1 取单——理论错账窗口 | `billing.go:1440 vs 1188-1196` | 全局唯一索引（`WHERE order_no<>''`）+ 迁移查重 |
| C28 | 计费设置保存：审计日志在校验**之前**落笔（400 失败也留了"已保存"审计）；`billing_enforced` 未校验值域 | `api/admin_packages.go:263-268,320-342` | 先校验后审计；值域白名单 |
| C29 | `api/pay.go:19` 头注释"1 元 = 1000 token"与实际换算率（1元=10token）矛盾 | pay.go:19 | 注释修正（文档-代码漂移示例，全仓注释巡检一轮） |
| C30 | 体验额度可发放给 `tenant_id=0`（该桶本就豁免扣费），无意义分支留雷 | `register.go:519-547`；`billing_api.go:45-46` | tid<=0 拒绝发放 |
| C31 | 运营策略热路径无缓存：`effectivePolicy` 每次 ≥3 条 SQL + 双 JSON 解析 + 窗口排序，每次 LLM 用量回调触发，抵消批量落库性能优化 | `api/ops_api.go:119-137` | 3-5s TTL 缓存 + 配置写失效钩子（PG advisory 通知可选） |
| C32 | "0=未设置"语义无法显式清零（`omitempty` + 零值判断）：限时奖励改不回永久、倍率关不成 0 | `ops/policy.go:53-67,295` | 策略字段全部指针化，区分"未设置/显式0" |

### 并入 D 批（引擎）
| # | 问题 | 证据 | 方案 |
|---|---|---|---|
| D15 | `isRateLimited/isNetworkError` 对错误文本做字符串匹配，模型供应商换文案即失灵 | `engine/engine.go:1291-1324` 附近 | 改结构化判定（HTTP status/类型断言/SDK 错误码） |
| D16 | `detectHanVariant` 繁简细分仅测试引用，未接生产采集路径 | `crawler/detect.go:61,86` | 接入 tier1/tier2 语言判定或删除 |
| D17 | `FILEPROC_MAX_CONCURRENT` 默认 1，所有文档转换全串行（吞吐瓶颈） | `fileproc/procgate.go` | S 系列稳定后按内存水位调参 + 热加载配置 |
| D18 | 批量翻译固定 2 并发，大文件多语言下 LLM 配额利用不足 | `engine.go:1611` | 与 llm 三池 QoS 联动为可配参数 |
| D19 | **增强**：占位符"掩码-回填"硬保护（现状仅 prompt 约束 + QA 检测告警，模型破坏占位符无法自动修复） | `engine.go:1003-1005`；`qa/qa.go:111-117` | 翻译前掩码替换（`⟦pN⟧`）、翻后回填 + 违规自动重翻 |
| D20 | **增强**：token 级流式输出（现状 SSE 仅阶段级进度） | `api/stream.go:68,163` | chat 路径透传上游 `stream:true`，前端逐字渲染 |
| D21 | **增强**：docx 多 run 段落的格式保留（现状译文并入首 run，加粗/颜色混排丢失） | `fileproc/docx.go` | 按 run 边界分配译文或保留首 run 格式标记 |
| D22 | **增强**：超长文本句级切分（对话模式无独立句切分器，超长句直接进 prompt） | `engine/text.go` | 句级 splitter（CJK 感知）+ 段落预算 |

### 并入 B 批（安全收尾）
- **B12** 队列领取后按 `leased_by + ORDER BY id DESC LIMIT 1` 反查任务，同 workerID 多任务时理论错拿（`queue/direct.go:73`）→ 随 S2 改 `RETURNING id` 精确化。
- **B13** `gateUsage` 对超管/`tid<=0` 全量豁免（`billing_api.go:45-46`）保留为运营设计，但加显著注释 + 豁免路径写审计（防止未来当漏洞"修复"或误扩大）。

### 并入 G 批（测试补强）
- **G8** 后端测试裸区补单测：`go test ./...` 显示 **iam（JWT/bcrypt/加密，最高危区）零测试文件**，另有 `service`、`mail`、`openapi`、`tenant`、`infra/distlock`、`infra/ratelimit` 无测试 → 按风险序补 iam > tenant > service。
- **G9** `deploy/loadtest/k6.js` 纳入发布流程：A1/S2 修完后跑 PG 并发压测（100 VU 同租户扣费），断言无双扣/负余额/误清零。
- **G10** 安全响应头：Caddy 仅有 `X-Frame-Options DENY`，**无 CSP、无 HSTS 检查**（`deploy/caddy/translator.conf:22`）→ 补 CSP（脚本/样式白名单收紧）、HSTS、`Referrer-Policy`（顺带缓解 E3 token-in-URL 的 Referer 面）。
- **G11** 合规项已核实无需新增：自助注销含宽限期机制已存在（`api/auth.go:105-111,448`）✓。

### F12 → H 批（已拍板 2026-09-12：除商店上架外全部执行，排第 8 批）
- 全部纳入本轮排期（见下表 H1-H12）；仅 **Chrome 商店上架**不做（自托管扩展 + 文档说明安装方式即可）。OpenAPI Spec 自动生成已在 F10、暗黑模式已在 F5，不重复列。

## 七-3、H 批 · 规划能力落地（原 F12，2026-09-12 拍板执行）

> 前置依赖：H 批全部排在 A-G 缺陷修复之后；个别项标注硬依赖。预估为单人工作量。

| # | 项 | 内容 | 依赖 | 预估 |
|---|---|---|---|---|
| H1 | 术语强制模式 100% | Gate 拦截升级为"命中术语表→译文强制替换+违规自动重翻"闭环（现状部分覆盖） | 随 D3/D19 | 1 周 |
| H2 | Constrained Decoding | 供应商支持则走 logit/logprobs 约束术语零违规；不支持的模型降级为 H1 事后闭环（双轨） | H1、网关能力探测 | 2 周 |
| H3 | KB 包级权限矩阵 | 五档可见范围之上补 读/写/管理 三级授权（表 + 面板 + API 校验） | — | 1 周 |
| H4 | TM 自动审核 | 工单反馈进 TM 前加质量预筛（QA 分+长度比+占位符一致性），通过率目标 >80%，低分入人审 | qa/ 已有基础 | 1.5 周 |
| H5 | 大文件导入断点续传 | KB/TM 批量导入 100MB+ 分片上传 + 失败续传（后端已有 checkpoint 模式可抄 crawler） | — | 1 周 |
| H6 | Hedged Requests 竞速路由 | 慢分位请求向次优供应商并行对冲（P99 目标降 30%）；仅 pro 模式与异步任务启用控成本 | 熔断/池已有 | 1 周 |
| H7 | 成本/延迟感知动态路由 | 按供应商实时 P50/P99/单位成本动态调路由权重（滑窗统计 + 后台可视化） | H6 同期做 | 1 周 |
| H8 | Zustand 状态统一 | 4 层 Context（auth/chat/branding/admin）迁 Zustand，路由级 store 划分 | 排在 E 批全部落地后 | 1 周 |
| H9 | 多级分销/裂变漏斗 | 邀请树（限 2 级防合规风险）、归因看板、自动结算；幂等复用 referral_rewards 占用优先模式 | **硬依赖 C23-C25 修复** | 1.5 周 |
| H10 | SCIM 2.0 组织同步 | AD/LDAP 用户/组增量同步（Users/Groups PATCH），租户管理员自助配置 | 组织模块已备 | 1.5 周 |
| H11 | SLO/SLI + Burn Rate 告警 | 可用性/翻译成功率/P99 三 SLO，多窗口 burn rate，告警噪音 <20% | observability 已有 prometheus 栈 | 1 周 |
| H12 | 编辑器插件（VS Code + Trados/memoQ） | VS Code 侧边栏翻译/术语检索（走 OpenAPI key）；Trados/memoQ 双向 TM 同步（社区常见对接需求） | OpenAPI 稳定（F10 后） | 1 周 + 2 周 |

**H 批小计 ≈ 15-16 人周**；建议独立成"第二阶段"按 H1→H4→H3→H5→H11→H6→H7→H9→H10→H8→H12 顺序滚动，双人约 2 个月。企业销售信号强的（H10 SCIM、H3 权限矩阵）可提前插队。

---

## 八、执行批次与顺序（建议）

| 批次 | 内容 | 预估 | 出口标准 |
|---|---|---|---|
| 1 | **S1-S3/S5/S7 存储统一（前置闸门）** + A1-A5 资金安全 + G2 方言护栏 | 3-4 天 | PG 为默认生产方言；`DB_DRIVER=postgres` 矩阵为发布闸门；新并发测试 + T16-T23 全绿 |
| 2 | B1-B13 安全认证（含 B11 硬编码收敛、B12 随 S2） | 2-3 天 | T18-T20 绿；日志/接口脱敏复查；全仓 grep 无运营邮箱/域名明文 |
| 3 | C1-C32 账务口径（含迁移脚本，C17/C22 并入 S5；C26 已拍板：token 真账+句数反推展示） | 4-5 天 | 对账勾稽测试绿；裂变补偿队列测试；时间基准迁移演练；句数展示=token/rate 换算断言 |
| 4 | **S4/S6/S8 pgvector+HNSW+备份+引导（D10/D11 并入）** + D1-D18 引擎文件（D19-D22 增强项可独立排后） | 4-5 天 | docx 特殊字符/FindExact/停用包回归；检索性能基准达标；`restore_drill.sh` PG 演练通过 |
| 5 | E1-E18 前端缺陷 | 3 天 | vitest 基线 + E2E 全绿、无静默失败 |
| 6 | F1-F11 UI/能力增强（F12 已拍板，全部转入第 8 批 H 表） | 1.5-2 周 | skip 用例转正、axe 无 severe、parity 扫描过 |
| 7 | G1-G11 穿插各批（每批修完即加断言，防回归；G8 补 iam 单测随 B 批、G9 k6 随 A/S 批、G10 CSP 随 B 批）；run_uat 默认切 PG 主矩阵 | — | run_uat 一键（PG）常绿，SQLite 保留为本地快跑选项 |
| 8 | **H1-H12 规划能力落地（原 F12，已拍板执行；Chrome 商店上架除外）** | 15-16 人周（第二阶段） | 见七-3 各项验收；H9 以 C23-C25 落地为前置 |

**第一阶段（缺陷+UI，批次 1-7）：约 4 周单人 / 2-3 周双人。** 第二阶段（H 批）：约 15-16 人周，双人滚动约 2 个月，H10/H3 如有企业客户在谈可提前插队。每批完成后以 `DB_DRIVER=postgres bash scripts/uat/run_uat.sh`（PG 主矩阵，发布闸门）全绿为准，`bash scripts/uat/run_uat.sh`（SQLite 本地快跑）作为兼容参考一并执行；文档同步更新本清单勾选状态。

---

## 九、遗留（企业资质解锁后立即启动）

1. 微信 Native / 支付宝 precreate 真实 HTTP 下单 + 商户证书（A5 已把验签/路由/精度修好，剩协议实现）
2. 发票自动开具/税务 UKEY 或第三方签约（C16/A3 已把数据模型、唯一性、冲红状态修好）
3. 订阅自动续费（依赖 1 的代扣签约）
4. 品牌自定义域名 CNAME + 备案（现硬依赖 `*.lexicorn.cn` 默认值，见 M5 项已列）

---

## 十、执行记录（滚动更新）

### 批次 1 ✅（S1/S2/S3/S5 + A1-A5 + G2）2026-09-12 完成
- 双矩阵验证通过（当时基线：A/B 67 + T 79，PASS=FAIL 0）。

### 批次 2 ✅（B1-B13 安全认证）2026-09-12 完成
| 项 | 落点 | 说明 |
|---|---|---|
| B1 | `config.MaskDSN/MaskEmail`；main.go 启动日志、初始密码写 0600 文件 | 日志零明文 |
| B2 | `users.token_version` 列 + JWT `tv` claim + `authUser` 逐请求比对；`ResetPassword` 递增 | 改密/重置即撤销全部旧会话 |
| B3 | `api/sso.go`：state cookie `Secure`（HTTPS 判定）；回调命中已有账号复核停用状态；provision 角色 `member`→`user`；重定向改 60s 一次性 `sso_code`（vcode 暂存）+ `POST /api/auth/sso/exchange` 换 JWT | 前端消费端在 E3 一并落地 |
| B4 | `mail.NoopSender` 不再打印正文/验证码（`MAIL_NOOP_PRINT_BODY=1` 才开） | |
| B5 | `/api/billing/config` 收敛 tenant_admin+；`/api/billing/quota` 要求登录且按 **本人租户**（旧实现匿名兜底租户 1） | |
| B6 | `ops.ValidateWindowOverrides/ValidatePolicyWindows`：窗口 id 非空+唯一、overrides 禁 `billing.enforced`/`payment.mode`/`auto_charge`、禁嵌套；策略/单窗两条保存链路接入 + 单测 | 堵住时间窗后门 |
| B7 | webhook secret 静态加密（复用 `EncryptSecret`，读链路透明解密，历史明文兼容、更新幂等）+ 单测 | |
| B9 | selfcheck 自动重启熔断：`/tmp/translator-selfcheck-restarts` 跨进程计数，1h 内 ≥3 次拒绝再退并升级人工告警；`prod.conf` 增 `[Unit] StartLimitIntervalSec=600/StartLimitBurst=5` 双保险 | |
| B10 | legacy 口令透明升级保留 + 计数日志；**联动修正**：升级内嵌 ResetPassword 会 bump token_version，登录链路同步 `u.TokenVersion++` 防「签发即失效」 | |
| B11 | 运营身份代码零明文：`api/branding.go`（`BRAND_DOMAIN_SUFFIX`/`BRAND_PRIMARY_PREFIX`/`OPS_NOTIFY_EMAIL` + system_config 优先级）；tenant.go×4、office.go、user_import.go、mail_tpl.go CC、crawler UA、openapi 文档示例（`{{OPENAPI_HOST}}` 占位，渲染出口替换）全部收敛；`prod.conf` 示例值。**遗留**：前端 BrandP/dicts 与 deploy/caddy 中的域名为部署配置/公开品牌展示，E/F 批前端侧一并评估 | |
| B13 | `gateUsage` 豁免：显著注释定性「运营设计勿当漏洞修」+ 超管豁免低频审计（每人每小时 1 条，`billing_exempt_superadmin`） | |
| B8/B12 | 批次 1 已随 A4/S2 完成 | |

**批次 2 附带修复（UAT 暴露）**
- `billing` 批量落账 `usage_ledger.created_at` 原写 **flush 时刻**，导致退款消耗核算（A3）把迟到 flush 的历史用量误计入本单支付后消耗（T1-balance-revert 376 漂移）。`UsageSink` 入队记 `When`，`UsageBatchRow.OccurredAt` 透传落账（保持全仓本地 RFC3339 字符串口径）。
- UAT T 套件更新：T6 新增 `T6-old-token-revoked`（改密后旧 token 必须 401，B2 验收）；两次改密后刷新 H1。断言基线 79→**80**。

**验证**：`go test ./internal/...` 全绿；`DB_DRIVER=postgres bash scripts/uat/run_uat.sh` → 67+80 全绿；SQLite 同矩阵全绿；前端像素 16 passed/3 skipped 不变。
### 批次 6.5 ✅（D19-D22 引擎增强，2026-09-12）

| # | 状态 | 落点 | 说明 |
|---|---|---|---|
| D19 | ✅ | 新增 `engine/placeholder.go`(+test 4例) + `qa.PlaceholderRe` 导出 + `singleLangRaw` 改名/包装 | 占位符掩码-回填硬保护：mask（`{var}/%s/<tag>` 与 QA 同源，重复占位符共享令牌）→ 翻译（ctx 注记「⟦Pn⟧ 逐字保留」）→ unmask（宽松括号变体容错）；令牌丢失**自动重翻一次**，仍丢失兜底追加句尾（QA placeholder 规则自此可过）；`engine` 测试全绿 |
| D20 | ✅ | `llm.StreamChat` + `engine/sentence` 双 sink 键 + `api/stream.go` + 前端 `translate.ts/useChat` | token 级流式：SSE 新增 `delta{lang,text}` 事件；上游 `stream:true + stream_options.include_usage`，**无 usage 回传视为不可计费端点→透明回退非流式**（mock LLM 走此路径零破坏）；SSE 写加 `sseMu`（progress/delta 并发管线串行化）；前端单目标语言时逐字渲染进气泡（done 全量覆盖） |
| D21 | ✅ | `fileproc/docx.go`(+docx_runs_test 3例) | 多 run 段落写回格式保留：枚举全部非 hyperlink w:t 节点→按原文 rune 权重比例分配译文→逐节点写回（各 run rPr 加粗/颜色原样保留）；转义按 D1「转义后长度」纪律逐节点执行；单 run 段落保持旧路径 |
| D22 | ✅ | 新增 `engine/sentence.go`(+test 4例) + singleLang 接线 | CJK 感知句级切分（。！？；!?;\n 界，Dr./U.S. 缩写保护，数字分号防误切）+ 600-rune 段落预算 + 超长单句硬切；>1200 rune 触发，切分拼接无损（测试断言）；顶层 attempt>0 不重复拆分防递归 |

### 批次 8 ✅（H1-H12 规划能力落地，2026-09-13）

| # | 项 | 落点 | 关键决策/说明 |
|---|---|---|---|
| H1 | 术语强制闭环 | `engine` Gate 拦截→命中术语强制替换→违规自动重翻（上批已交付） | ForceTerms 全链路测试绿 |
| H2 | Constrained Decoding 双轨 | `llm/client.go` `TermConstraints{Source,Target}` ctx 注入（CallChat/StreamChat 载荷 `x_term_constraints`）；`config.SupportsConstraints`（路由配置 `supports_constraints`）；`engine.singleLangRaw` 仅当术语在源文实际出现才注入 | main.go 取「全部在用路由均支持」AND 口径，不支持模型自动退回 H1 事后闭环；`h2_constraints_test` |
| H3 | KB 包级权限矩阵 | `store/kbgrants.go`（`kb_pack_grants` 表，read<write<manage）；`api/kb_grants.go` 鉴权层（角色≥2 维持部门全量；成员按授权，granted 成员跳过类型/部门圈定但不含删除）；`/api/admin/kb-packages/grants\|mine` + KbP「授权」弹窗 | admin_kb.go 读/写/管理三档守卫全覆盖；`kbgrants_test` |
| H4 | TM 自动审核 | `qa/screener.go` `ScreenPair`（100−40×错误−8×告警）；工单反馈 ≥`TMFeedbackMinQAScore`（默认 80）直接 SaveBack，低分入 `tm_review(source=feedback)` 人审 + 审计 | 前端工单页已有 feedback 来源标签，零 UI 改动；`screener_test`/`h4_feedback_test` |
| H5 | 断点续传分片上传 | `api/upload_chunk.go`（chunk/status/merge，≤8MB/片，24h TTL 清扫）；前端 >4MB 自动分片、断点跳过已收片、3 次重试、进度回显；recognize-kb 支持 `?merged=` | 合并产物过扩展名白名单；`h5_chunk_test` |
| H6 | Hedged Requests | `engine/hedge.go`：滑窗延迟统计，对冲延迟=max(HedgeDelayMs, 1.5×P50) 封顶 8s；主路快速失败立即回退降级链 | 仅非流式/未熔断/有备路；`HEDGE_ENABLED=1`（默认关）+`HEDGE_DELAY_MS=1500`；`hedge_test`×4 |
| H7 | 动态路由 | `pickPrimaryRouteDynamic`：得分=(权重+1)×健康度×P95 速度因子×成本因子（冷启动中性）；`ObserveRouteTokens` 按 model 归因 token；`/api/admin/ops/routes` 可视化 | `DYNAMIC_ROUTING=1`（默认关=静态权重）；`h7_routes_test` |
| H8 | Zustand 状态统一 | `stores/auth.tsx`/`stores/admin.tsx`/`branding.tsx`/`hooks/useChat.tsx` 迁 zustand（chat 为每账号动态 store）；Provider 退化为纯副作用壳，公共 hook 契约不变 | **回归修复 2 处**：① FrontShell 头部/尾部 lazy 组件（Bell/AccountMenu/SiteFooter/KbUploadDialog）无 Suspense 边界 → React #426 白屏，已包整树兜底边界；② StrictMode 双挂载下会话恢复把「实例被取代」误判为「校验失败」清空 token → 401 风暴，改为仅响应确实无效才清凭证 |
| H9 | 二级裂变漏斗 | `store/referral.go` `rewardSecondLevel`（付费永久包→上级得 `paid_perm_l2`=直推奖励×`referral_l2_pct`%（默认 30，system_config 可调），仅个人版、防环、幂等）；`InviteFunnel` L1/L2 邀请·付费·奖励漏斗；`/api/referral/funnel` + 邀请页看板 | 复用 referral_rewards 唯一索引幂等模式；`h9_referral_test` |
| H10 | SCIM 2.0 | `api/scim.go` 完整 Users/Groups（eq filter、PATCH active、members 全量替换、Group↔org、软删）+ Bearer `scim_config.token`（48 hex，与会话隔离）；`store/scim.go`；租户自助 `/api/tenant/scim`（开关/轮换/根部门映射）+ 账号页 SCIM 卡片 | Entra/Okta/OneLogin 标准端点 `/scim/v2/*`；`h10_scim_test` |
| H11 | SLO/Burn Rate | `api/slo.go` 三 SLO（可用性 99.9 / 翻译成功率 99 / P99≤8s）分钟环采样、多窗口 burn rate（1h×6h、5m×1h 双窗，样本不足不评）→ `CreateAlert` kind=`slo_*` 自动开合；`/api/admin/ops/slo` + Ops 页可视化；metrics 补 P99/成功率 | 告警噪音控制=双窗同判 + 去重；`h11_slo_test` |
| H12 | VS Code + Trados/memoQ | ① `vscode-extension/`（零构建 JS 插件：侧边栏工作台、选中翻译 Cmd/Ctrl+Alt+T、**术语检索 `GET /openapi/v1/terms`（命令+侧边栏面板，`store.SearchTerms` 精确优先/双向命中/包可见性口径同 H1 闸门）**、SecretStorage 存 Key，走 `/openapi/v1/translate`，规范已入 openapi.v1.json）；② `GET /api/translation/export-tmx[?lang=&module=]`（TMX 1.4，部门管理员，租户隔离）+ 既有 import-tmx 成双向闭环；README 附对接说明 | `buildTMX` 纯函数 + `h12_tmx_test`/kb `ExportRows` 测试 |

**新增配置/开关**（全部默认关闭或兼容旧行为）：`HEDGE_ENABLED`/`HEDGE_DELAY_MS`、`DYNAMIC_ROUTING`、`TMFeedbackMinQAScore`(80)、路由 `supports_constraints`、`system_config.referral_l2_pct`(30)、SCIM 每租户开关+令牌。

**测试基建修正（本批 UAT 门禁暴露）**：`config.Default()` 会写全局 `config.C` 且 env `DB_DRIVER=postgres` 下把「内存 SQLite 单测」误导成 PG 方言（`information_schema` 报错），h4/h5/hedge/h7/stagemodel 等测试固定 `DatabaseDriver="sqlite"`；`hedge_test` 共享计数器改原子、`observeRoute` 懒初始化挪进锁内（race 检测）；上述修正后 **`DB_DRIVER=postgres` 环境 `go test -race ./internal/...` 20 包全绿**。

**验证**：`go vet` + `go test -race ./internal/...` 全绿；`tsc` 0 错、vitest 51/51、vite build 成功；UAT 双矩阵 **PG 67+114 全绿 / SQLite 67+114 全绿**、e2e 25/25、竞态门禁通过。

### 批次 7 ✅（G 批 · UAT 盲区补强，2026-09-13）

| # | 状态 | 落点 | 说明 |
|---|---|---|---|
| G1 | ✅ | `api_uat_txn.sh` T16 | 并发扣费压测：全新个人租户钉余额 8000 → 30 路 openapi 并发打空。断言：30 请求全部有应答（成功/insufficient 二分）、终局余额 ≥0、**ledger Σcost 与 (前-后) total_available 差严格对账（无双扣/漏扣）**、清零 critical 告警不误触发（≤1）。实跑 ok=3~6 / refused 24~27，对账分毫不差 |
| G2 | ✅ | `build.sh [1.5/4]` | json_set/json_extract 方言护栏扫描（S 批随批落地，本批复核通过：业务层 0 命中，jsonops/rewrite/测试豁免） |
| G3 | ✅ | `run_uat.sh` §1.5 | `go test -race -count=1 ./internal/...` 进发布闸门（失败即中止；`UAT_SKIP_RACE=1` 本地快跑可跳）。竞态检测通过 |
| G4 | ✅ | `api_uat_txn.sh` T17-T23 | T17 同租户工单隐私（peer 经 admin users/create 注入→detail 403「无权查看他人工单」，owner 正对照）；T18 启动日志脱敏（无明文超管密码 + DSN 口令必须 `:****` 掩码，run_uat 导出 `UAT_SERVER_LOG`）；T19 时间窗覆盖白名单（payment.mode / billing.enforced 拒存、合法 markup 窗放行）；T20 编辑器产物 B8 闸（未登记文件 404、`..%2F` 穿越拒绝且不回显内容）；T21 已消耗订单退款按未消耗比例折算（实退 < 全额，A3 配套）；T22 退款联动撤销裂变付费奖励（邀请者余额回原值 + paid_perm 流水删除）；T23 已退款订单禁止开票（「已退款订单不可开具发票」） |
| G5 | ✅ | 新端点 `/api/admin/ops/watchdog/subscription-scan`（超管）+ T24 | 订阅到期摘除可验证入口（生产仍为启动扫+24h 周期，端点幂等仅手动触发）：注入付费订阅→`dbjsonset` 把 package_expires_at 钉到 2020→触发扫描→**断言 tenants.permissions 实际摘除 + audit_logs.package_expire + 非超管 403 + 重复扫描幂等**。附带 dblib 新增 `dbjsonstr/dbjsonset` 双方言助手 |
| G6 | ✅ | 新 e2e `a11y_errors.spec.ts`（6 用例，`@axe-core/playwright`） | axe：登录态 4 页 critical 违规=0（违规明细 JSON 随附件落盘）；扫描自适应静态营销页（/pricing 服务端直出无 #root）。错误态截图矩阵：/api 500、401（业务面注入）、403 admin、匿名受限页、超管后台——断言不白屏且无未捕获异常，截图存档 `artifacts/error_states/`；tenant_label unskip 随 F1 已转正 |
| G7 | ✅ | （随批次 6/F11） | vitest 基线 29→51 例（chatStorage/quotaCalc/theme/core/renderMarkdown） |

**G 批执行中额外发现并修复的产品缺陷**：
- **`unitPrice` 空语免费洞（A 级账务）**：C4 引入的四级回退在 `lang==""` 时因 `err` 保持 nil **跳过全部回退查询**，直接以 unit_price=0 落账——openapi 同步翻译（划译插件全部流量！）与 kb_embed 等所有无语种计量此前**静默零扣费**。修复为 `lang==""` 显式进入回退链；T16 对账断言自此真正消耗>0（本洞正是被 G1 压测抓出）
- **F10 OpenAPI 自动合并键序回归**：map 序列化把 `"openapi"` 版本键排到 `components/info` 之后，破坏首字节嗅探（A12 断言实测抓出）→ `marshalSpecOrdered` 按规范惯例重建顶层键序
- **F8 BillingCenter 移动端溢出**：`.ss-grid` 1fr 轨道隐含 min-content 被 nowrap 宽表撑破（`minmax(0,1fr)`+`min-width:0`）+ 页面根容器 `width:100%`；G6 移动巡检自此覆盖 /billing 五 Tab
- **axe critical 补名**：ChatWindow Textarea、会话搜索 Input、LangMultiSelect（`inputProps` 透传 aria-label/placeholder）、工单/反馈/编辑器各输入框全部获得可编程标签

**run_uat 主矩阵切换**：默认 `DB_DRIVER=postgres`（发布闸门），`DB_DRIVER=sqlite` 保留本地快跑；用法注释同步。

### 批次 6 ✅（F1-F11 UI/能力增强，2026-09-12）

| # | 状态 | 落点 | 说明 |
|---|---|---|---|
| F1 | ✅ | `App.tsx` FrontShell + `AdminDashboard.tsx` + `tenant_label.spec.ts` | 顶栏租户 Tag：企业=🏢租户名（超长省略，title 全文），个人=👤个人版（`app.personalPlan` 双语键）；后台非超管顶栏 `ad.tenantName` 范围 Tag；**e2e describe.skip 已解除**（3 用例转正） |
| F2 | ✅ | 8 文件 + 4 新面板词典 | 206→0 中文字面量清零：EditorPage/TicketsPage/ChatWindow/DataSourcesP/IndustriesP/BrandTermsP/App/useChat 全部键控（含 JSX 文本/语言名下拉）。新建 `panels/industries.ts`(29)+`brandterms.ts`(29)+`chatwin.ts`(41)+`datasources.ts`(94) 并注册 index.ts/parity；`app.*`/`tk.ed*`/`chat.*` 键入 base；DataSourcesP 64 条 TODO-EN 已人工校对清零。**parity.test.ts 新增「F2 组件中文字面量扫描」**（注释剔除+字面量/JSX 文本双模式）——vitest 29 全绿、tsc 零错、vite build 成功 |
| F3 | ✅ | `i18n/index.ts` + `theme.css`/`mobile.css` + `MessageBubble/ChatWindow` | RTL 接线：`document.documentElement.dir/lang` 随语言切换（`RTL_LANGS`=ar/fa/he/ur/ps/ku/dv，setLang+init 双挂）；工作台气泡 `dir="auto"`（按内容方向，混输安全）；theme.css 物理属性→逻辑属性（margin/padding/border-inline-*、inset-inline-end 浮窗），暗黑/RTL 两轨共用 |
| F4 | ✅ | `BrandTermsP/TicketsP` + `theme.css` | 可点击 `<a onClick>`→真 `<button>`（键盘可聚焦、type=button、aria-label）；`TicketsP` 反馈链接 aria-label；全局 `:focus-visible` 焦点环 + `prefers-reduced-motion` 动效降级。**axe E2E 扫描归 G6**（需 @axe-core/playwright） |
| F5 | ✅ | 新增 `lib/theme.ts` + `App.tsx` + theme.css dark 块 | 明暗三态 auto/light/dark：`localStorage('app_theme')` 记忆、matchMedia 实时跟随系统；双轨生效=html[data-theme]+color-scheme（自绘 CSS dark 覆写）+ TDesign `--td-*` 变量整体换暗；顶栏 🌗/☀️/🌙 循环按钮（title 双语 `app.theme.*`）；页面底色改 `var(--npz-page-bg)` |
| F6 | ✅ | `mobile.css` + `EditorPage` | ≤900px 后台表格横向滚动+右缘渐隐滚动指示；工单/编辑器工具条 `.editor-toolbar` flex 堆叠。ar/en 像素矩阵归 G6 扩 |
| F7 | ✅ | `ChatWindow/MessageBubble/api/translate.ts` | ①预估接线：`estimateTranslation()` 调 `/api/translation/estimate`，输入 600ms 防抖，余额条显「预估 X–Y token · 余额可能不足」琥珀告警；②会话搜索（🔍/⌘K 切换、`shownMessages` 过滤）+ 导出 Markdown（本地 Blob）；③气泡双语对照（details 折叠显示上一条用户原文）+ 一键复制译文（clipboard+1.5s 反馈）；④KB 命中标记前端既有（matched_zh/mode/source-badge 链路已通）；⑤Cmd+Enter 发送已由既有 Enter 覆盖。**顺带修复 F2 扫描器两盲区**：JSX 文本+表达式混排（余额条/待保存/待审 Tag 等 14 处补键控）+跨行文本（缩翻/发放开关等）；parity 门禁升级为全文扫描 |
| F8 | ✅ | 新增 `api/my_billing.go`(store)+`api/my_billing.go`(5端点)+`MyBilling.tsx`+`panels/mybilling.ts`(46键) | 账单中心落地：`/billing` 路由升级 BillingCenter——余额+不足横幅（复用）+**近30天用量趋势**（SVG 柱状零依赖，UTC 日轴补零）+**订单/台账/奖励/发票四表分页筛选**（订单状态过滤、台账 biz_kind SQL 真分页、奖励/发票内存分页）+发票状态直出（待开/已开/已冲红 C16）；后端 `MyDailyUsage/MyLedgerPage` +5 个 `/api/billing/my/*` 端点（authUser 门槛、tenant+user 双收敛、订单行不透出 prepay/二维码）；**5 端点 SQLite 冒烟全 200**；静态码/线下引导既有（exhaustedHint→/packages 订阅流 C19） |
| F9 | ✅ | `panels_e.tsx`(Ops 时间窗) + `panels_a.tsx`(AlertsP) + `store/alerts.go`+`store/reconcile.go` + `api/admin_evals.go`+`api/admin_reconcile.go` + `ReconcileP.tsx`+`panels/reconcile.ts`(25键) | ①时间窗覆盖因子**表单化编辑器**（15 个白名单字段：markup/mode_rules/invite/limits；表单↔原始 JSON 双向同步单一数据源，表单外键保留警示，B6 服务端校验兜底不变）；②告警中心**筛选+静默**：级别/类型客户端筛选、`alert_silences` 表(tenant+kind upsert)、静音门禁 CreateAlertEx、顺带关现存 open、`/api/system/alerts/{silence,unsilence}`（审计+超管可指定他租）、静音规则 Tag 条+解除、时长弹层 1h/12h/24h/7d；③**对账视图** `/api/admin/reconcile`（超管）：orders↔payments 五规则勾稽（missing_payment/status_mismatch/fen_mismatch(分口径 rate×tokens±1)/refund_no_flow(A3 历史注记)/orphan_payment）+规则筛选+汇总 Tag；前端菜单 reconcile(minLevel4) |
| F10 | ✅ | `extension/`(popup+content+background+manifest+css) + `sdk/`(py/js/ts/java) + `route_registry.go`+`openapi_v1.go` | ①Key 前缀示例 `tk_`→`rk_` 全清（5 文件 5 处，对齐 apikeys.go 真实生成前缀）；②扩展补三件：**快捷键**（manifest.commands Alt+Shift+T/⌥T + background 服务者转发 + content onMessage 直翻当前选区）、**术语/结果回显**（译文成功后 `mark.__trz_mark__` 高亮页面选区，收起即清除；气泡增显 模式/消耗/原文对照）、**错误码提示**（ERR_HINT 表与 E11 同口径：invalid_api_key/insufficient_balance/key_quota_exceeded/text_too_long 等 9 码，失败气泡「翻译失败（code）：指引」）；③**OpenAPI 规范路由注册期自动导出**：routeMux 包装 ServeMux 登记 231 处路由（零调用点改动），/openapi/v1.json 装配期把缺失的 /openapi/* 路由补为 x-auto 条目（?manual_only=1 退回纯手工）；**SQLite 冒烟实测 9 条 auto 路径并入（16 总路径）**，embed 校验测试仍过 |
| F11 | ✅ | 新增 `lib/chatStorage.ts`+`lib/quotaCalc.ts`+`lib/markdown.ts` + 4 测试文件 + `vitest.config.ts`/`vitest.setup.ts` | 测试基线 1→6 文件 29→51 用例：core.ts（E4 headers 合并/E5 401 清token+去重跳转/登录接口豁免/ApiError code+error_code 别名/超时中文错误，fetch 全桩）、chatStorage（E1 回填持久化剥离 progress+>2MB 裁 50/E2 账号隔离键+anon 不落盘/损坏容错/200 收敛）、quotaCalc（估价换算 C26 句数口径抽纯，ChatWindow 接线）、theme（F5 三态+auto 跟随+循环序）、renderMarkdown（E17 无环视/粗斜体不误吞——**renderMarkdown 自 MessageBubble 抽提 lib/markdown.ts**，node 环境免 tdesign 重链）；vitest 配置补 `@` 别名 + sessionStorage/window 全局桩 |

### 批次 5 ✅（E1-E18 前端批，2026-09-12）

| # | 落点 | 说明 |
|---|---|---|
| E1 | `hooks/useChat.tsx` | `startSend` 追加消息对、`patchMsg` 回填均接入 `schedulePersist`（进度剥离+400ms 防抖）——发送/回填即时落盘 |
| E2 | `useChat.tsx` | 存储键按账号隔离 `chat_msgs_v1:<uid>`；切换账号重载对应键；**旧全局键一次性清除不迁移**（归属不可证）；未登录（anon）不落聊天 |
| E3 | `App.tsx` | `/?token=` 注入登录态 → `/?sso_code=` POST `/api/auth/sso/exchange` 换 token（60s/单次消费，对齐批次2 B3 后端）；token 不再进地址栏/Referer/历史；历史 `token` 参数仅抹除不再登录 |
| E4 | `api/core.ts` | `fetch` 参数顺序修正：headers 合并改在 `...options` **之后**（旧写法凡传 headers 即丢 Content-Type/认证头）；timeoutMs/signal 从展开中剔除 |
| E5 | `core.ts` + `api/translate.ts` | `handleUnauthorized` 导出；chat/文件两条 SSE 通道 401 → 清登录态跳登录（抛 `ApiError`），不再只在气泡里显示"请求失败(401)" |
| E6 | `TicketsPage.tsx` | 详情轮询改读 `detailIdRef`（旧闭包捕获创建时刻 `detail=null`，永不刷新） |
| E7 | `ApiKeysP.tsx` | 文档预览 `document.write` → Blob URL（后端 HTML 不再继承当前页 origin），弹窗被拦截有提示（新增 i18n 键） |
| E8 | `stores/admin.tsx`/`AdminDashboard.tsx`/`selfservice.tsx` | 全部 `history.pushState(+合成 popstate)` 改 react-router `navigate`；面板跳转/返回工作台/自助页快捷入口与路由同步 |
| E9 | `AdminDashboard.tsx` | `invites/usage/alerts/audit/mailTpl/footer/dataSources` 7 个孤儿面板补 renderPanel 分支+侧栏菜单项（i18n 标签原已存在） |
| E10 | 新增 `lib/runGuarded.ts` | Login(doSendCode/doRegister/doForgot/doReset)、modals(反馈/换绑邮箱/注销)、TicketsPage(run/cancel/delete)、OrgP(createUser/bulkImport)、DataSourcesP(8 处)、IndustriesP(5 处) 统一 catch→toast，消除 unhandled rejection 静默 |
| E11 | `billing/quota.go`+`api/stream.go`+前端 | `quotaErr` 附稳定码（`insufficient_balance`/`daily_quota_exceeded`）+`QuotaErrCode`；SSE error 事件与 JSON 拒绝透传 `error_code`；聊天侧：余额不足→「去充值」确认框（navigate /packages）、日限→气泡提示明日恢复；FrontShell 新增余耗=0 顶部横幅（复用 ss.exhaustedHint） |
| E12 | `branding.tsx`/`LangMultiSelect.tsx`/`ApiKeysP.tsx` | 裸 `/api/...`、`/openapi/docs` 改 `API_BASE`/`openAPIDocsUrl()` |
| E13 | `OrgP.tsx`/`panels_a.tsx` | 管理员建用户/改密两处密码框补 `type="password" autocomplete="new-password"` |
| E14 | `api/{tickets,billing,apikeys,industry,kb,core}.ts` 等 | 删 8 个零调用导出（ticketList/billingBalance/billingUsage/billingConfig/apiKeyReveal/publicIndustries/kbPackageUpdate/getDownloadUrl；openAPIDocsUrl 已接线保留）；ChatWindow 死导入/死定义清理、ModelsP `applyRoutePreset` 接入「添加路由」按钮；core.ts 陈旧注释更正；**tsconfig 开启 `noUnusedLocals/noUnusedParameters`**（26 处告警全清） |
| E15 | `api/tenant.go`+`BrandP.tsx` | 后端 `validateBrandPayloads`：Logo≤~300KB/背景≤~800KB（base64 字符上限），branding-set 与 tenant-update 双入口拦截；前端选图预检（类型+体积） |
| E16 | i18n | 新增本域键 `packages.nameRequired/confirmDeletePkg`、`tk.colCreatedAt`；PlansP/TicketsPage 不再借 apikeys/webhooks/users 键 |
| E17 | `MessageBubble.tsx` | 斜体正则去 lookbehind：`(^|[^*])\*([^*\n]+)\*(?!\*)`（Safari<16.4 编译期 SyntaxError 消除，行为等价） |
| E18 | `EditorPage.tsx` | 工单号加载成功回填数字 ID；save 前 `Number.isInteger(id>0)` 校验（`?id=NaN` 请求绝迹） |

**验证**：`tsc --noEmit`（含新开启的 noUnused*）零错、`vite build` 成功、vitest 通过；`go build ./...`+单测绿；UAT PG 与 SQLite 均 `67+80` FAIL=0，像素 16 passed/3 skipped。

**遗留**：E2 旧全局聊天记录直接作废（设计取舍）；Chrome 商店上架仍除外（H 批）。

### 批次 4 ✅（D 引擎批 + S4/S6/S8，2026-09-12）

**正确性/数据**
| # | 落点 | 说明 |
|---|---|---|
| D1 | `translateDocxParagraph` 游标改按**转义后**长度计算 | 旧 `firstIdx+len(translated)` 把 `&amp;` 拦腰截断成残缺实体（"R&D" 文档损坏）。`TestD1EscapeCursorParity` 端到端回归（XML token 解析 + 文本 round-trip） |
| D2 | `kb.FindExact` SELECT 改 `rowCols` 常量 + **补齐 sharedFilterSQL 的 2 个租户参数**；`FuzzyHits` 同病同修 | 精确/模糊两条命中链此前**恒报错被当「无命中」吞掉**（列 42≠43、参数 4≠3）。`TestD2FindExactColumnParity`（含模糊链）锁定列序与参数对账 |
| D3 | `FindTermsBySubstring/FindEntriesBySource/Scoped` 加 `p.enabled=1`；kb 共享层 EXISTS 同步加条件 | 停用包术语不再注入 prompt。`TestD3DisabledPackTermsExcluded` |
| D5 | 新增 `fileproc.ApplyAlignedText`：txt/csv/md **按原文件行对齐写回**（空行/重复行/围栏/分隔线原样保留），md 提取键剥结构前缀、写回粘回，整行单链接输出 `[译文](url)`；旧 `writeTranslatedText`（从去重表重建全文）删除 | 提取/写回两侧键口径同步改。`TestD5AlignedWriteback` |
| D6 | tier2 表头行（含 `<th>`）整行跳过，`<td>` 单列取值 | 表头不再自动审批入正式库；`TestD6HeaderRowSkipped`（旧用例按缺陷行为断言 3 行，已更正） |
| D7 | docx 页眉页脚改 zip 条目全量遍历（旧列表写死 header1-3 且同名循环笔误，header4+ 全漏）；**补采+写回 footnotes.xml/endnotes.xml** | 脚注尾注进入翻译链 |
| D8 | pptx `notesSlides` 纳入提取/写回（`PPTX_TRANSLATE_NOTES=0` 关闭）；母版/版式**刻意不译**（样板文字重复出现+计费污染），注释钉死 | |

**引擎/吞吐**
| # | 落点 | 说明 |
|---|---|---|
| D4 | PDF 写回三级链：缓存 DOCX → pdf2docx → **`WriteTranslatedPDF` 版式重建兜底**（原死代码接通）；失败日志区分 | pdf2docx 不可用的部署不再「PDF 永远失败」 |
| D9 | 全部请求路径退避改 ctx 感知：`engine.sleepCtx`/`llm.sleepCtx`/`crawler.sleepCtx`；crawler `throttle(ctx)` 锁外等待；ticket/orchestrator 循环可中断 | 客户端断开不再白烧 token/占连接。**教训**：ticket worker 首版误用已 `cancel()` 的 ctx → worker 即退（T5 挂起），改 `stopCh` 并复跑锁定 |
| D12 | robots 网络错误/5xx → **fail-closed** 停抓（小时级缓存防风暴）；4xx（无 robots）放行 | 对齐主流爬虫口径 |
| D13 | tier3 `llmRoute` 真正读 `stage_models.ai_initial`（密文解密），注释与实现一致 | 运营阶段模型对采集生效 |
| D14 | `Crawler.sharedFetch()` 跨批次/跨源复用 fetchBase（限频/robots 缓存/连接池全程有效）；wiktionary 种子词表进程内缓存 | |
| D15 | 新增 `llm.StatusError`（429 等类型化）；`isRateLimited/isNetworkError/isRateLimit` 一律 `errors.As` 优先、子串匹配降级为兼容兜底 | 供应商换文案不失灵 |
| D16 | `DetectSourceLang` 汉字分支接入 `detectHanVariant`（生产采集路径首次生效） | 繁体不再误标 zh；`TestD16HanVariantWired` |
| D17 | `FILEPROC_MAX_CONCURRENT` 默认按物理内存自适应（<3G=1、3-6G=2、≥6G=4），env 显式覆盖不变 | 升配机不再全串行 |
| D18 | `LLM_LANG_CONCURRENCY`（默认 3=旧值）/`LLM_BATCH_CONCURRENCY`（默认 2→4）可配 | |

**S4/D10/D11/S6/S8**
- S4/D10：`kb.ensureVectorIndex`——HNSW(cosine) 优先、IVFFlat 兜底、双失败显式告警；成功日志标注「npz 仅 dev/单测」。**PG 实机演练**：10 万行 vector(1024) 建 HNSW（40s，提示调 `maintenance_work_mem`），`EXPLAIN` 命中 `Index Scan using idx_tm_embedding_hnsw`，查询 0.8-4.9ms ≪ 50ms 验收线。
- D11：kb `tm_segments.zh` + 主库 `kb_entries.source_text` pg_trgm GIN（`Store.KBSearchIndexMigrate` 挂入启动迁移，扩展缺失仅告警）；EXPLAIN 验证 `Bitmap Index Scan on idx_tm_zh_trgm`；`FindTermsBySubstring` 60s 结果缓存 + 术语/包全部写路径 `invalidateTermCache` 联动（D3 测试捕获并修复）。
- S6：SQLite VACUUM 路径标注 dev-only；新增 `scripts/restore_drill.sh`（pg_dump -Fc → scratch 库 pg_restore → 7 张关键表行数对账 → trap 清理），**本地 PG 演练通过**。
- S8：bootstrap-demo 建库步骤补 `vector`+`pg_trgm` 创建+存在性硬检查；bootstrap-seoul 补 pg_trgm。

**批次 4 UAT 修正**：`api_uat_txn.sh` T1 支付前 `sleep 4`——A/B 套件尾部 SSE 计量在响应完成时才入队（When 合法滞后 1-2s），恰落 T1 支付窗口时会被 A3 消耗核算误计（376 波动，T 套件偶发 79/1）；等待 sink 冲刷后连续两轮 PG + 一轮 SQLite 均 80/0。

**遗留（按计划）**：D19（占位符掩码-回填）/D20（token 级流式）/D21（docx 多 run 格式）/D22（句级切分）四项「增强可独立排后」未在本批；npz 双路径保留（dev/单测）。

**验证**：`go test ./internal/...` 全绿（新增 D1/D2/D3/D5/D6/D16/S4 相关 6 个回归文件）；PG `67+80` 全绿 ×2 连跑；SQLite `67+80` 全绿；像素 16 passed/3 skipped。

### 批次 3 ✅（C1-C32 账务口径，2026-09-12）

**3a 账务核心**
| # | 落点 | 说明 |
|---|---|---|
| C1/C11 | `Store.pricingCost(taskType,provider,lang,qty)` 统一 4 条写路径 `cost=ROUND(qty×单价×系数)`；`backfillUsageCostC1`（幂等旗标 `cost_formula_backfill_c1`）历史重算 | 舍入 ROUND 半进位 |
| C2 | `payments.amount_fen` 落分列 + 存量回填；两条 INSERT 改分口径 | 浮点元账消灭 |
| C3 | 订阅升级：旧包剩余按原 `expires_at` 结转 `source='order_carry'`，新包只进 `pkgTokens`；`planGrantExpiry` 0=永久哨兵 9999 | 不再清零旧周期 |
| C5 | `incrementDailyUsageTx` 进 3 条事务路径；`DailyUsage` 兜底改存在性口径 | 日统计与 ledger 勾稽 |
| C9/C10 | `MarkOrderPaid` 默认分支用 `pkgTokens`；`PackageOrderTokenBackfill` 乘 `MarkupMultiplier()` | 发放/补算同口径 |
| C16 | `idx_invoices_order_active`（`WHERE status<>'void'` 部分唯一）+ `CreateInvoice` 退款单/有效票前置 + `handleInvoiceVoid` | 一单多票需先作废 |
| C19 | `ReopenManualOrder`：已取消手工单确认到账 → 克隆新 pending 单（`manual_confirm=1`），告警带新单号 | 钱到票不复活 |
| C22 | 台账关键域写点统一 UTC（grants/ledger `created_at`、`orders.paid_at`、`usage_daily` 日键）；`cmd/dbtimecheck` 审计/`-fix` 工具，PG 实库演练 50 行转换 | 全仓 UTC 刻意不做（会移报表日界），见执行说明 |
| C27 | `idx_orders_no_global ON orders(order_no) WHERE order_no<>''` 部分唯一（重复即启动告警） | 撞号根治 |
| C29/C30 | 定价注释纠错（1 元=10 token 默认）；`grant_trial` `tid<=0` 显式拒绝（删除平台租户 1 兜底发放） | |

**3b 计量/策略/管理面**：C4 `target_lang` 全链路进 ledger（`LangFromContext/LangFromOptions` 注入，`unitPrice` 四级回退 zh→en→provider-default→0）；C6 默认值单一来源（`ops.DefaultEffective` 访问器 + `api.trialConfig()`，散点 300000/14/1.5/500 清零，`referral_daily` 等 5 文件改造）；C7 `ResetPackageMonthly` 单事务原子（结转台账不触碰，返回 rejected 计数）；C8 `Merge` 先克隆 `ModeRules`（租户/窗口补丁不再污染上层）；C12 展示倍率改 `system_config usage_display_factor`（缺省 1.0 关闭，出参透出）；C13 watchdog 告警用 `TenantRemainTotal`（含台账）；C14 负价拒存 + `duration_days=-1` 显式永久 + 更新接口指针化区分「未传/清零」；C15 `DeletePackage` 引用检查（pending 订单/生效 grants 拒删）；C18 自动扣款/确认到账三处透传 `MarkOrderPaid` 错误并回显真实订单态；C21 死代码清零 + `Deduct→deduct` 私有化；C28 套餐配置审计 `LogAuditDiff`（含 diff）；C31 热路径 `effectivePolicyCached`（3s TTL + Server 实例级 gen 失效，翻译计量/支付模式/任务闸门/usage 看板接入；管理视图/注册保持直读）。

**3c 裂变与欠费**：C23 `GrantTrialStack/RewardPaidPermanent` 「占用流水+台账/余额+日计数」单事务，失败整体回滚可重触发（旧「占错闸焊死补发」终结）；`MarkOrderPaid` 提交后邀请奖励错误不再丢弃（日志留痕）。C24 `EnsureRefCode` 条件 UPDATE+唯一索引仲裁 + 有界重试环 + 回读持久值（不再返回未落库码）。C25 日上限改 `referral_daily` 事务内条件守卫（`cnt<max`），触顶 `ErrReferralCap`→critical 告警（`TestC25DailyCapAtomic` 覆盖）。C20 欠费批次 `SettleExhausted` 后 `LogUsageBatch` **留痕不扣费**，告警附 provider/task 明细。

**3d C26（已拍板：token 唯一真账）**：`DeductSentences`/`ErrSentenceExhausted` 删除（生产零调用死码）；`sentence_balance` 定性「发放流水镜像（只增不减、退款回冲钳 0），禁止判额/对账」——`tenant.Permissions`/packages.go/migrate 全部注释钉死；出参改造：`/api/billing/usage/me` 返回 `tokens_available`+`sentences_estimate`（旧 `sentence_balance` 键=反推值兼容）；前端 `selfservice.tsx`/`PlansP.tsx` 剩余句数改读 `balance_sentences_approx` 反推值，文案「约可翻 N 句（按剩余 token 折算）」；`ChatWindow` 原已反推 ✓。

**批次 3 修复过程暴露并回归**：C25 守卫 SQL 在 PG 报 `column reference "cnt" is ambiguous` → 改 `referral_daily.cnt` 限定（与 usage_daily 同款）；C31 包级缓存导致多测试 server 串 key → 改 Server 实例字段；B6-invite-factor UAT 转绿。

**验证**：`go test ./internal/...` 全绿（含新增 `c8_merge_test`/`C25 原子性`/`C32 显式清零` 等）；PG `run_uat.sh` **67+80 全绿**；SQLite 矩阵 **67+80 全绿**；前端像素 16 passed/3 skipped；`tsc --noEmit` 无错。
