# 能言 LangCross 翻译助手 · 全量端到端评估报告

> 评估日期：2026-09-20　评估方式：**读代码为主 + 实跑闸门/UAT**（文档仅作线索，凡与代码冲突一律以代码为准）
> 代码盘面：后端 Go 65,126 行（17,823 行测试）／前端 TS/TSX 38,300 行（209 文件）／SDK 4 语种／48 张表／249 个 API 路由

---

## 〇、一句话结论

**工程完成度远高于同类早期 SaaS——UAT 断言密度（460+）和 CI 五段闸门是真实资产；但当前发布闸门是红的（前端 tsc + vitest 双红灯），且商业化最关键的「自动收款」链路只做了一半。**
架构属于「**胖控制器 + 强数据层**」的务实单体：能扛住多实例和并发扣费不透支，但领域层缺失、事务边界松散，属于「**能跑、能卖、但再长一倍就会痛**」的阶段。

---

## 一、实跑结果（不是看文档，是真跑）

### 1.1 后端闸门：全绿

| 项 | 结果 |
|---|---|
| `go build ./...` | ✅ 0 error |
| `go vet ./...` | ✅ 0 warning |
| `go test -race ./...` | ✅ **全包 ok**（api 60.6s、engine 15.4s、db 5.0s…），零 data race |

### 1.2 UAT 矩阵：`scripts/uat/run_uat.sh`（PG 方言）

| 套件 | 结果 |
|---|---|
| A 主链路（注册/计量/支付/KB/APIKey/租户隔离…） | ✅ **PASS=70 FAIL=0** |
| B 计费策略（快速模式免费/限额/平台订阅/邀请） | ✅ **PASS=18 FAIL=0** |
| T 交易与功能专项（T1–T48） | ⚠️ **PASS=322 FAIL=4** |
| 前端 E2E / 像素级 UAT | ✅ exit=0，FAIL=0 |

**4 个失败全部是 T28 分片上传**，且失败原因不是逻辑错误：

```
FAIL|T28-chunk-0| rename .../Library/Application Support/能言/_uploads/_chunks/t28-.../tmp.part
                  → idx_00000.part: operation not permitted
```

两个结论：
1. **环境因**（已复核）：沙箱禁止写 `~/Library/Application Support`，非代码回归。
2. **代码因（真实缺陷，且比初判严重得多）**：`internal/api/upload_chunk.go` 有 5 处把 `err.Error()` 回给客户端（:93/:103/:115/:218/:245），把**服务器绝对路径**原样吐出。

   ★ **复核后修正**：这 5 处只是冰山一角。全仓实测 **`"message": err.Error()` 共 306 处、跨 38 个文件**：

   | 文件 | 处数 |
   |---|---|
   | `api/admin_kb.go` | 43 |
   | `api/orgs.go` | 30 |
   | `api/admin_billing.go` | 19 |
   | `api/auth.go` | 18 |
   | `api/tenant.go` | 14 |
   | `api/admin_apikeys.go` | 13 |
   | `api/admin_evals.go` | 13 |
   | `api/admin_models.go` | 12 |

   多数回显的是 store 层的中文业务错误（如「组织不属于当前租户」），本身无害；但**同一条路径会混入 SQL 错误、文件路径、IO 错误**。这是**系统性的信息泄漏面**，不是 upload 的局部问题——只是 upload 恰好在沙箱下被触发才暴露。

### 1.3 其余 UAT

- `assist_uat.sh`：**PASS=38 FAIL=0**（AI 顾问：意图让位、掩码脱敏、热重载、管理端 CRUD、Token 桥接）
- `multi_instance_e2e.sh`：**PASS=8 FAIL=0** —— 双实例共享 PG：JWT 跨实例 ✅、并发 30 请求**不透支** ✅、流水勾稽 `ledger=975 consumed=975` ✅、充值跨实例传播 ✅

### 1.4 前端闸门：🔴 **双红（这是本次最严重发现）**

```
src/i18n/locales.core.test.ts(23,7): error TS6133: 'CORE' is declared but its value is never read.
Test Files  1 failed | 27 passed (28)
Tests      12 failed | 169 passed (181)
```

12 个失败全部集中在 `src/i18n/locales.core.test.ts`：

```
× ru/fr/ar/es/pt/de/th/ja/ko/zh_hant：全量 2532 键逐键覆盖
    → expected 453 to be 2532
× zh_hant：值内零简体残留 → 疑似简体残留 191 键（app.modelBackup、app.modelFallback…）
× 全量词典已翻键 → ru 取词 admin.alerts: expected 'Alerts' to be undefined
```

**根因（这是文档/实现/闸门三方漂移，不是随机翻红）：**

| 层 | 口径 | 证据 |
|---|---|---|
| `locales/ru.ts` 实现 | 只翻 **453** 键核心集 | 文件头注释「只覆盖 CORE_KEYS 核心集（…共 453 键）」 |
| `i18n/index.ts` 注释 | 「新语种只要求覆盖核心集」 | index.ts:9-11 |
| **AGENTS.md §5** | 「其余十语种只覆盖 CORE_KEYS」 | 工程约定文档 |
| **新测试闸门** | 要求 **2532** 键全量 | `locales.core.test.ts:3` 明写「历史 CORE_KEYS 核心集口径已被全量口径吸收」 |

即：**闸门已被升级到全量口径，十份词典没跟上**。`git status` 显示 `i18n/index.ts` 与 `locales.core.test.ts` **正处于未提交的工作区改动**——这次升级是半成品。

**连锁影响**：`.github/workflows/ci.yml` 的 `frontend` job 跑 `npm run typecheck` + `npx vitest run` → **CI 现在必红**。

另外一个真实质量问题：**繁体词典 191 个键仍是简体字形**（关/开/与/专/业/设/登…），繁体用户会看到半成品。

### 1.5 SDK 与管线自检

| 项 | 结果 |
|---|---|
| SDK Python `unittest` | ✅ 13/13 |
| SDK TS `node --test` on **Node 20** | ✅ 8/8（字节级 multipart 断言） |
| SDK TS on **Node 22.22** | 🔴 `Cannot find module .../test` —— `node --test test/` 的目录参数在 Node 22 失效 |

⚠️ **SDK 发布链路在 Node ≥22 上坏掉**：`package.json` 里 `prepublishOnly: npm test`，意味着**在 Node 22 机器上 `npm publish` 直接失败**。CI 固定 Node 20 所以今天看不出来，但这是个定时炸弹。

`fileproc` Python 自检：docx ✅；pdf ⚠️ 本机缺 `pymupdf` 被跳过（CI 显式装依赖并把「skip」判红，设计正确）。

---

## 二、架构评价

### 2.1 分层：胖控制器 + 贫血服务层（最大结构债）

```
internal/api      23,174 行 / 71 文件 / 261 个 handler   ← 业务逻辑的实际所在地
internal/store    12,750 行 / 45 文件 / 80 张表的 SQL
internal/engine    6,455 行                              ← 翻译领域逻辑（唯一像样的领域层）
internal/service     974 行 / 1 文件                     ← 只有 ticket worker，不是服务层
```

- `api` 层对 `store` 的直调点 **936 处**，59 个文件 import store。
- **好的一面（已复核）**：api 层**非测试源码不 import `database/sql`**（14 个 import 全部落在 `_test.go`）。
  ★ 精确化：仍有 **8 处** SQL 语句直接写在 api 层（`db.QueryRow(s.Store.DB(), ...)`，
  如 `api_openapi_tasks.go:596`、`metrics.go:332`、`plans_api.go:83`）。数量相对 store 的数千处可忽略，
  但严格说不是「100% 收在 store」，而是「**数据访问抽象未被打破，但有 8 处分层泄漏**」。
- **坏的一面**：`service` 名存实亡（只被 `api/server.go:45` 挂了一次）。要加一条「创建工单 + 校验配额 + 落审计」的规则，你只能写在 handler 里，然后祈祷别的文件也有人 copy 了一份。

**判定**：这是**有意识的取舍**（避免 >3000 行无收益重构，AGENTS.md 三.1 明确写了），作为单体是合理的。但 `api` 已 23k 行，**再增长 50% 就会出现「同一个业务规则三个 handler 三种实现」**。

### 2.2 事务边界：单笔原子，跨表裸奔

全项目 `BeginTx` 只 **1 处**（`queue/direct.go:64`）；`db.Begin()` 28 处**全在 store 层**。api/service 层**零事务**。

跨表写未包事务的典型（已核实）：

| 位置 | 写序列 |
|---|---|
| `api/tickets.go:199-213` | CreateTicket → UpdateTicket → LogAudit → UpdateTicket → EnqueueTicketRun |
| `service/ticket.go:372-396` | **chargeTokens(扣费)** → UpdateTicket(completed) → SetTicketExpiry → CreateNotification → DispatchWebhook |

`service/ticket.go:371` 的注释自己承认「扣费失败仅告警不回滚」。

**缓解**：单笔扣费本身是原子的（`store/billing.go:218/330/394/435`、`quota_grants.go:203` 用 `Begin()` + 守卫式 `UPDATE ... WHERE left>=?` + `RowsAffected`），所以**不会超卖**。多实例压测 M3 也证实 `no-overdraft(7025)` + 流水勾稽相等。
**真实风险**：扣费成功但工单状态写失败 → **钱扣了活没干**（用户会投诉）。

### 2.3 计费：防超卖做对了，对账和退款没做完

✅ **做对的**：
- 无透支开关（全仓 grep `AllowNegative|negative|透支` 零命中），靠守卫式 UPDATE + PG `FOR UPDATE`
- 幂等：支付回调三道闸（Token / 渠道+金额比对 / `WHERE status='pending'` 条件抢占）
- 对外**统一积分口径**，出口删掉 token 裸值（`api/points_view.go:36-39`）

🔴 **没做完的**：

1. ~~**退款口径错位**（`store/billing.go:1709` 用 `SUM(quantity)` 而非 `SUM(cost)`）~~
   → **★ 复核后撤回（见 §七勘误 E1）**。实测只有 `translate` 与 `kb_embed` 两类任务写 `usage_ledger`，
   `rate_card` 中二者均为 `price=1 / mult=1.0`，故 `cost == quantity` **恒等**；`evals`/`gate` 根本不入账。
   降级为**未来风险**：建议仍改用 `SUM(cost)` 以防后代入账新任务类型时踩坑。
2. **`rate_card` 配置与入账路径脱节**（`store/store.go:868-870`）：seed 了 `evals(1, 0.5)` 与 `gate(0, 0)`，
   但全仓**没有任何 `RecordUsage` 调用点使用这两个 taskType**——定价是死配置。
   另注 `unitPrice`（`billing.go:583`）只兜底 `mult == 0 → 1.0`，**不兜底 `price == 0`**，防护只做了一半。
3. **异步落库无持久化**：`billing/sink.go` 内存缓冲 2s 批量落库，进程 SIGKILL/OOM/panic → 缓冲内用量
   **既不落 ledger 也不扣费**（窗口 ≤2s 的收入泄漏）。
   ★ 复核补充：写入失败会**回插缓冲下一周期重试**（`sink.go:372` 注释明写「避免 fail-open 少计费」），
   只有缓冲超 **50000 条**才丢弃最旧部分——不是一失败就丢。
4. **无定时自动对账、无差异告警**（已复核）：`admin_reconcile.go:81` 只判 `paidRows == 0`，
   **不判 `paidRows > 1`**——同一订单两条 paid 流水（重复支付）会被漏检，因为每条单独看都等于应收额。
5. **无 idempotency_key 字段**：orders/payments 表没有幂等列，webhook 重投会重复触发业务。

### 2.4 并发与多实例：Redis 是「隐形强依赖」

```
distlock/lock.go:30      rdb != nil ? redisLock(SETNX+看门狗) : localLock(sync.Mutex)
concurrency/semaphore.go:31  rdb != nil ? redisSem : chanSem(带缓冲 channel)
```

`REDIS_ADDR` 不配就**静默降级为进程内锁**。**多实例部署不配 Redis = 分布式锁失效**（N 实例各持一把锁）。代码里埋了 `errCount` 降级计数，但**没有启动期强校验**。

> 这是「能跑但没人告诉你什么时候会炸」的典型。AGENTS.md 应该把 `REDIS_ADDR` 列为多实例部署的**强制项**，并在启动日志 WARN。

### 2.5 错误处理：体系建了，没推广

`internal/errors` 定义了完整的 `ErrorCode` + `APIError` + `HTTPStatus()` + trace_id 透传。实际使用率：

| 写法 | 次数 |
|---|---|
| `writeJSON` 裸 JSON | **1226** |
| `s.writeError`（走 apierrors） | **45**（仅 3 个文件） |
| **HTTP 200 包 `success:false`** | **230** |

**结构化错误覆盖率 < 4%**，错误码体系基本空转。230 处「200 包失败」会让 API 客户的 `resp.ok` 判断全部失效——这是**对外 API 的硬伤**。

### 2.6 租户隔离：靠 handler 守卫，store 层不一致

注入点：`withTenant` 中间件（`server.go:600-634`）+ `effTenant`（138 处调用），优先级 JWT > API Key 租户 > `X-Tenant-ID`(仅超管) > 默认租户 1，未登录忽略 `X-Tenant-ID`。设计正确。

**不一致的证据**：
- `store/orgs.go:61 RenameOrg(id,name)` → `iam/store.go:416` SQL 为 `UPDATE orgs SET name=? WHERE id=?`，**无 tenant 过滤**，靠 `api/orgs.go:300` 前置守卫
- 同包 `MoveOrg`（`iam/store.go:427`）**有** `org.TenantID != tid` 校验
- `store/tickets.go:115 GetTicketGlobal(id)` 无租户过滤，靠后置守卫

即「**handler 守得住就安全，忘了就跨租户**」。UAT 的 `A13-cross-tenant-order`、`T17-peer-deny`、`T30-member-write-denied` 覆盖了主要路径，但**没有机制保证新增接口也守得住**（缺 store 层强制 tid 参数）。

### 2.7 安全面：做得出乎意料地扎实

✅ 已核实到位的：
- JWT 未配置 → **32 字节随机密钥 + 告警**（不落默认弱密钥）；`ADMIN_INIT_PASSWORD` 未配 → 随机 12 位写 0600 **且不打印**
- 登录防爆破：5 次/5 分钟窗口 → 5 分钟冷却，且**持久化到 `rate_limits` 表**（不是纯内存），支持 `TRUST_PROXY_XFF`
- 口令：bcrypt + 历史 SHA-256 **透明升级**（`auth.go:123`）
- 敏感词过滤、路径穿越防护（`T20-traversal-deny`）、CSP（`T44-csp-header`）、日志 DSN/口令脱敏（`T18-*`）、无 token 裸调拦截（`T36-*-no-token-naked`）
- pprof 默认只监听 `127.0.0.1:18787`
- 全站仅 1 处 `dangerouslySetInnerHTML`，且上游已转义，无 `eval`/`new Function`

⚠️ 待修：
1. **`internal/assist/config/config.go:62`**：`AdminToken: getenv("ASSIST_ADMIN_TOKEN", "change-me-please")` —— **硬编码兜底令牌**。assist 管理台在没配环境变量时任何人可用此 token 进。应改「缺失即拒绝启动」。
2. **`REQUIRE_PROD_SECRETS` 只在注释里出现**（`iam/auth.go:31`），代码未实现 → 生产无密钥强校验。
3. `/metrics` 手工拼 Prometheus 文本，**无鉴权**（自研而非 client_golang）。
4. 上传接口泄漏绝对路径（见 1.2）。

### 2.8 可观测性：有仪表盘，缺追踪

✅ 优雅停机（`main.go:422-440`，先 Shutdown 再 Stop sink，注释说明修正了收入泄漏）、Prometheus/Grafana/Alertmanager/Loki 全套部署件、Alertmanager webhook 入站、自建 trace_id、audit_logs、容量压测基线（k6 + 台阶矩阵，P99 预算 15s）
❌ **无 OpenTelemetry / 分布式追踪**；健康检查只有 `/api/health`，**无 readiness/liveness 分离**（K8s 友好度不足）；审计写失败**只计数不阻塞**，`audit.go:47` 明写「失败不影响主流程」。

---

## 三、前后端契约交叉比对（249 路由 × 200 引用）

我写了脚本从后端 `HandleFunc` 注册表与前端 `/api/*` 字面量做双向 diff：

- **前端调用但后端无注册**：仅 2 条，都在 `core.test.ts`（`/api/me`、`/api/me2` 测试桩）→ **无死链**。
- **后端已注册但前端无引用**：51 条。扣除第三方回调（`pay/notify`）、基础设施（`caddy/ask`、`alerts/alertmanager`）、assist 独立服务、下载类（走 `window.location` 动态拼）后，**真正没接线的业务能力**：

| 后端能力 | 状态 | 商业含义 |
|---|---|---|
| `/api/translation/export-tmx`、`import-tmx`、`import-bitext`、`kb-stats` | **前端零引用** | 🔴 **TMX 导出（Trados/memoQ 桥）后端做完了，用户看不见**。这是本地化行业最硬的采购标准，白做了 |
| `/api/scim/v2/{Users,Groups,Schemas}` | 前端零入口 | 🔴 SCIM 用户目录同步是企业 IT 的采购红线，没有配置入口 |
| `/api/sso/{login,callback,providers}`、`/api/auth/sso/exchange` | 仅 App.tsx 一处裸 fetch | ⚠️ SSO 半接线 |
| `/api/admin/models/routes` + `/save` | 前端零引用 | 模型路由配置只能在后端改 |
| `/api/admin/flow/run`、`/api/apikeys/reveal`、`/api/admin/users/import-template` | 前端零引用 | 运营能力未 UI 化 |
| `/api/billing/{balance,usage,config}` | 仅注释/旧代码残留 | ⚠️ **僵尸路由**，与 `/api/billing/my/*` 并存，易误导外部集成方 |
| `/api/translate`、`/api/chat`（非 stream） | 前端只调 stream 版 | 正常（供 SDK/API 客户） |

### 前端自身的坏味道（已核实）

1. **同一接口两套封装**：`/api/plans` 在 `api/billing.ts:138` 和 `usePlans.ts:48` 各写一份，后者用 `any` 取值。
2. **「我的余额」三套并存**：`/api/me/package` / `/api/billing/my/overview` / 注释遗留的 `/api/billing/balance`。
3. ~~两条流式旁路裸 fetch 无超时、各自重复实现 401~~
   → **★ 复核后撤回（见 §七勘误 E3）**。实测 `translate.ts:127/205` **已统一复用** `handleUnauthorized`
   （`core.ts:43` 导出，注释标注「★ E5：SSE 通道 401 与其他请求同源处理」），是我读漏了。
   准确表述：两处均接受可选 `signal?: AbortSignal` 参数，**能力具备，但调用方不传时无超时保护**。
4. **401 跳首页与注释自相矛盾**（已复核）：`core.ts:43` 注释写「清除登录态并**跳转回登录页**」，
   实现是 `window.location.href = '/'`（首页）。跳首页会丢失当前路由、不做登录回跳——
   **注释记录的意图与代码不符**，属于「以为修好了其实没修对」。
5. **无重试**：全仓无 retry/backoff。
6. **多标签页登录态不同步**：token 在 `sessionStorage`（避开 localStorage 是优点），但 A 标签登出后 B 标签仍持有 token，无 `storage` 事件同步。
7. **内存泄漏**：`modals.tsx:117/198` 的倒计时 `setInterval` **无 useEffect 清理**。
8. **巨型组件**：`Landing.tsx` 1747 行、`PlansP.tsx` 892、`panels_a.tsx` 851、`AiRegisterFlow.tsx` 769、`KbP.tsx` 762。
9. **`any` 滥用 167 处**（非测试）。
10. **a11y 覆盖低**：174 个非测试文件仅 90 处 `aria-*`、26 处 `role=`；Button/Input/Select/DataTable **无 aria**；Dialog/Drawer **无 focus-trap**。

---

## 四、还缺什么（按优先级）

### P0 —— 挡在发布前面的

1. **把前端闸门修绿**：要么把十语种词典补到 2532 键、修 191 个繁体残留，要么把 `locales.core.test.ts` 退回核心集口径并同步改 AGENTS.md。**现在 CI 是红的。**
2. **删掉 `assist/config/config.go:62` 的硬编码兜底 token**，改为缺失即拒绝启动。
3. **错误信息脱敏（范围从 5 处扩到 306 处）**：`"message": err.Error()` 共 **306 处 / 38 文件**。
   建议统一走 `writeError` 或新增 `writeInternalErr(w, err)`：对外固定文案，原文进 slog + trace_id。
4. ~~`RefundOrder` 改 `SUM(cost)`~~ → **降级为 P2**（复核后确认当前口径恒等，见 §七 E1）。
5. **SDK 的 `node --test test/` 改为 `node --test "test/*.test.mjs"`**，否则 Node 22 上 `npm publish` 失败。
6. **401 跳转改为跳登录页并带回跳参数**（`core.ts:48`），对齐注释声明的意图。

### P1 —— 影响钱和客户信任的

6. **TMX 导出接进前端**（后端已就绪）——本地化行业采购硬指标。
7. **SCIM/SSO 配置界面**（后端已就绪）——企业采购红线。
8. **落地页价格从 i18n 词典改为取 `/api/plans`**：现在 `Landing.tsx:920` 明写「价位取词典不取 /api/plans」，改价要改 12 份词典 + 发版，且首页与 `/pricing` 价格源不同**可能不一致**。
9. **对账定时化 + 告警**，并补 `paidRows > 1`（重复支付）规则。
10. **sink 落库加持久化**（spool 到 DB/文件），或至少缩短 flush + 丢弃时告警。
11. **多实例部署把 `REDIS_ADDR` 列为强制项**并在启动期 WARN/拒绝。
12. **给「工单完成 + 扣费 + 流水」补事务**（`service/ticket.go:372-396`）。
13. **限流文案误导**：`api_openapi_tasks.go:131/666` 无条件拼接 `gateErr.Error() + "。如余额不足请充值或升级套餐"`，限流时也告诉客户「余额不足」——会把限流投诉变成充值投诉。

### P2 —— 工程质量债

14. 把 1226 处 `writeJSON` 的错误路径收敛到 `apierrors.WriteError`，消灭 230 处「200 包失败」。
15. 删除 `/api/billing/{balance,usage,config}` 僵尸路由，或标注 deprecated。
16. `/metrics` 加鉴权；`REQUIRE_PROD_SECRETS` 落实。
17. 补 readiness/liveness 分离；接入 OpenTelemetry。
18. 前端：401 登录回跳、流式请求加超时、清理 `setInterval`、`/api/plans` 双封装合一、`any` 收敛。
19. 补**翻译核心流程的 E2E**——现在 11 个 spec / 35 个 test 里**没有一条跑「上传文件 → 翻译 → 下载」**，最赚钱的链路没有浏览器级回归。
20. 继续下调 `log.Printf` 棘轮（当前卡在基线 **179**，自 2026-09-18 起零进展）。

---

## 五、架构设计评分

| 维度 | 评分 | 说明 |
|---|---|---|
| **功能完整度** | 9/10 | 文件翻译/对话翻译/KB/TM/质量评估/审批/OpenAPI/SDK/Webhook/USDT/多租户/组织预算，该有的几乎都有 |
| **正确性（钱相关）** | 7/10 | 防超卖做对了、多实例不透支做对了；退款口径错、异步落库无持久化、事务边界松散 |
| **并发与分布式** | 7/10 | 双实例压测实跑通过；但 Redis 缺失静默降级为内存锁 |
| **安全** | 8/10 | 防爆破/密钥/脱敏/穿越/CSP 都做了；一个硬编码 token + 路径泄漏拉分 |
| **可测试性** | 9/10 | 460+ UAT 断言、五段 CI、PG+SQLite 双方言、棘轮闸门——**这是本项目最强的一项** |
| **可维护性** | 5/10 | api 23k 行胖控制器、service 层名存实亡、错误处理体系 4% 采用率、文档与实现已漂移 |
| **可观测性** | 6/10 | 有全套部署件和优雅停机；无追踪、无 readiness/liveness、审计可静默丢弃 |
| **前端工程** | 6/10 | 架构简洁（零重型依赖，包体 693KB/gzip 238KB）；闸门红、a11y 弱、巨型组件、any 泛滥 |
| **加权总分** | **7.3/10** | 「工程纪律强于架构设计」——测试资产远超架构资产 |

**一句话**：这是一个**靠极强的测试纪律撑住的务实单体**。同样的功能，换一个没有这套 UAT 的团队来做，会差得多。但它的天花板也在这里——**架构债不会让系统崩溃，只会让下一个功能越来越贵**。

---

## 六、商业价值评估

### 6.1 已经具备的变现基础（真实资产）

| 能力 | 状态 | 商业含义 |
|---|---|---|
| 多租户 + 组织/部门双层预算墙 | ✅ | 能卖团队版，管理员可控预算 |
| 积分预充值计量 + 影子余额实时扣减 | ✅ | 现金流前置，毛利可预测 |
| 套餐包 + 永久余额 + 体验额度三层 | ✅ | 支持免费试用 → 转化漏斗 |
| 邀请返利 + UTM 归因 + 漏斗统计 | ✅ | 增长闭环已有（`funnel` 表 + `referral_rewards`） |
| OpenAPI + 4 语种 SDK + Webhook | ✅ | **可走 API 批发/集成商渠道**，这是 B2B 放大的关键 |
| 知识库/术语库/翻译记忆/品牌词 | ✅ | **迁移成本护城河**：客户用越久越难换 |
| 12 语种界面 + 浏览器语言自动检测 | ✅ | 出海可售 |
| USDT 链上支付 + 链上自动结算 | ✅ | 跨境/不便开国内支付的场景 |
| AI 销售顾问（assist，38 条 UAT） | ✅ | 官网 7×24 接待 + 选型引导，降低获客人力 |
| 文件版式保持（docx/pptx/xlsx/pdf/epub/srt/po…） | ✅ | 比通用大模型翻译**贵得有理由** |

### 6.2 卡在变现路上的三个洞

**洞一：收款自动化没做完 —— 最致命。**

```
payment.go:158  WechatProvider.CreateOrder → return error "微信 Native 下单尚未接入真实协议…请改用 static_qr/usdt"
payment.go:222  AlipayProvider.CreateOrder → 同理
```

真实可用的渠道只有：**静态收款码 + 人工确认（manual）**、**USDT 链上**、**mock（测试）**。
微信/支付宝的适配器**类写好了、下单直接报错**。

★ **复核后必须澄清的两点**（否则会把「已知的有意设计」当成 bug 去修）：

1. **这是 2026-09-16 的 fail-closed 整改，不是疏忽。** 代码注释明写：此前返回本地拼装的假
   `code_url` / `alipay://` 占位串，会让「配置齐全」的环境把**不可支付的废码**呈现给用户；
   现在改为显式报错、上层 `handlePayCreate` 拒绝出单。**方向是对的。**
2. **回调验签反而是真实现**：`WechatProvider.VerifyNotify` 做了 APIv3 AES-256-GCM 解密，
   `AlipayProvider.VerifyNotify` 做了 RSA2 验签——**比下单那一侧更完整**。

所以它不是「安全漏洞」，而是「**有意留白的未完成能力**」：商户资质到位后，补齐
`/v3/pay/transactions/native` 与 `alipay.trade.precreate` 两个 HTTP 调用 + 签名即可。商业结论不变。

后果链：
- 中国大陆 B2B 客户的主流付款方式（微信/支付宝扫码自动到账）**不可用**
- 只能「客户扫码 → 手动点「我已付费」→ 超管后台确认 → 到账」——**每笔都要人工对账**
- 转化漏斗在最后一步断掉（付款中断 → 放弃），且**漏单/错单风险由人承担**
- 客单价越低，人工确认成本占比越高，**小额自助转化基本不成立**

**这是当前 ROI 最高的一个改动**：微信 Native v3 + 支付宝当面付 precreate 都是成熟协议，两个 `CreateOrder` + 两个 `VerifyNotify` 的实现量不大，却能直接把「人工对账」变成「自动到账」。

**洞二：没有自动续费 —— NRR 上不去。**
全仓 grep `auto_renew` 零命中，只有到期 T-7/T-3/T-1 提醒通知（`watchdog.go:313`）。
SaaS 的估值乘数很大程度看 NRR。**现在每一笔续费都要客户主动再走一次「下单 → 扫码 → 人工确认」**，在一个已经不自动到账的链路里，续费率会非常难看。

**洞三：定价运营不闭环 —— 改价要发版。**
- 落地页三档价格写死在 i18n 词典（12 语种 × 3 档），`/pricing` 走 `/api/plans`，**两个页面价格源不同**
- 无优惠券/折扣码/促销（grep `coupon|discount|promo_code` 零命中）
- 无多币种（`packages` 表无 currency 列，`balance_accounts.currency` 写死 `'tokens'`）
- 无定时对账与差异告警

做促销、做汇率本地化、做渠道价，现在都得改代码 + 发版。

### 6.3 商业化优先级建议（按 ROI 排序）

| 序 | 动作 | 投入 | 收益 |
|---|---|---|---|
| 1 | **接通微信 Native + 支付宝当面付** | 中（协议成熟） | 直接解锁自助付费，砍掉 100% 人工对账，打通转化漏斗最后一步 |
| 2 | **把 TMX 导出/导入接进前端** | 小（后端就绪） | 拿下本地化行业采购硬指标，支撑提价 |
| 3 | **订阅自动续费（到期代扣/续费单自动创建）** | 中 | NRR 提升，估值乘数提升 |
| 4 | **落地页价格改走 `/api/plans` + 后台改价** | 小 | 运营可自主调价/做促销，不再为改价发版 |
| 5 | **SCIM/SSO 配置界面** | 中 | 过企业 IT 采购红线，进中大型客户 |
| 6 | **优惠券/折扣码 + 多币种展示** | 中 | 渠道分销、海外定价 |
| 7 | **修前端闸门 + 补翻译主流程 E2E** | 小 | 恢复发布能力，主链路不再裸奔 |

### 6.4 总体商业判断

**产品力是够的，工程纪律是强的，商业闭环差最后一口气。**

- 差异化成立：不做「又一个 GPT 翻译壳」，而是「**版式保持 + 术语库/记忆沉淀 + 组织预算管控 + API/SDK**」——这四点构成了真实的企业采购理由和迁移成本。
- 变现路径清晰：预充值积分 → 现金流前置；OpenAPI+SDK → 渠道放大。
- **但当前状态下，「自助付费」这一步是断的**。一个陌生客户今天从落地页进来，注册能拿到 1000 积分试用，翻译体验大概率能打动他，**然后在付款环节撞上「扫码 + 人工确认」**。这个断点的损失，远大于目前所有 P1/P2 技术债的总和。

**建议：把「接通微信/支付宝自动到账」当作下一阶段唯一的第一优先级**，它的商业收益 > 其余所有待办之和。

---

## 七、★ 复核勘误（2026-09-20 二轮：回到源码逐条验真）

初稿部分结论来自子代理汇报与单点抽样，本轮**逐条回源码核对**。下表如实标出哪些站得住、哪些要撤回。

### E1　【撤回】「退款口径错位 `SUM(quantity)` vs `SUM(cost)`」—— 现状不成立

- 初稿：`billing.go:1709` 用 `SUM(quantity)` 核算消耗，而扣费 `cost = qty × price × mult`，evals(×0.5)/gate(×0) 下会算错。
- 复核：查 `RecordUsage` 的**全部调用点**，实际只有两类任务入账——
  `billing_api.go:170` 的 `translate`、`engine.go:2305` 的 `kb_embed`。
  `rate_card` seed（`store.go:868-870`）中 `translate=(1, 1.0)`；`kb_embed` 无配置 → 走
  `unitPrice` 末段兜底 `return 1, 1.0`。**两者 `cost == quantity` 恒等。**
  `evals`/`gate` 在 rate_card 里有定价，但**全仓零入账调用点**，是死配置。
- 结论：**当前不会算错**。代码注释「quantity 与入账同为计费 token，单位一致」是**准确的**。
  降级为 P2「未来风险」——建议仍改用 `SUM(cost)`，防止后代入账新任务类型时踩坑。

### E2　【降级】「gate `price=0` 是收入洞」—— 当前无损失

- `evals`/`gate` 不入账，所以 `price=0` 未产生实际免扣。
- 但 `unitPrice`（`billing.go:583`）只有 `if mult == 0 { mult = 1.0 }`，**不兜底 `price == 0`**——
  注释写「防止免费乘数导致费用为 0」，防护只做了一半。属**潜在一致性缺陷**。

### E3　【撤回】「两条流式旁路无超时、各自重复实现 401」

- 复核 `translate.ts:127` / `:205`：均已 `import { handleUnauthorized } from './core'` 并调用，
  注释标注「★ E5：SSE 通道 401 与其他请求同源处理」。**这个坑已被修好**，是我读漏了。
- 两处均接受 `signal?: AbortSignal`（可选）。准确说法：**能力具备，调用方不传时无超时**。

### E4　【严重低估】错误回显：从 5 处 → **306 处 / 38 文件**

初稿只点了 `upload_chunk.go` 5 处。全仓实测 `"message": err.Error()` **306 处**，
Top：`admin_kb.go` 43、`orgs.go` 30、`admin_billing.go` 19、`auth.go` 18、`tenant.go` 14、
`admin_apikeys.go` 13、`admin_evals.go` 13、`admin_models.go` 12。
**这是系统性信息泄漏面，不是 upload 的局部问题**——这一条比初稿严重得多，应升 P0。

### E5　【性质修正】微信/支付宝「未接入」是**有意 fail-closed**，且验签已实现

见 §6.2。方向正确，是未完成能力而非缺陷；`VerifyNotify` 两侧反而做完整了。

### E6　【数字校正】

| 项 | 初稿 | 实测 | 判定 |
|---|---|---|---|
| `s.Store.` 直调 | 936 | **978** | 量级一致 |
| `writeJSON` 调用 | 1226 | **1268** | 一致 |
| 200 且 `success:false` | 230 | **239** | ✅ 成立（样本 `admin_apikeys.go`） |
| `s.writeError` / `apierrors` | 45 | **45 / 46** | ✅ 采用率 3.5%，「<4%」成立 |
| `log.Printf`（非注释行） | 179 | **179** | ✅ 与棘轮基线**精确吻合**，零进展属实 |
| 前端 `any` | 167 | **165** | ✅ 一致 |
| 前端 `aria-*` / `role=` | 90 / 26 | **111 / 18** | 量级一致，a11y 薄弱成立 |
| 十语种 locale 键数 | 453 | **453（10 份全部）** | ✅ 完全证实 |
| api 层裸 SQL | 「0 行」 | 不 import `database/sql`（14 处全在 `_test.go`），但有 **8 处** SQL 语句写在 api 层 | 需精确化 |

### E7　【措辞收紧】两处「听起来像漏洞、其实是纵深防御不足」

- `RenameOrg`/`DeleteOrg` 的 SQL 确实无 `tenant_id` 过滤（`iam/store.go:416/456`），`MoveOrg` 有——同包不一致属实。
  但 `api/orgs.go:358` 注释明写「过组织归属 + validateOrg **双闸口**」，且 UAT 有 `T17-peer-deny`、`T30-member-write-denied` 覆盖。
  → 是**依赖 handler 守卫、缺少 store 层强制约束**，不是当前可被利用的越权。
- `service/ticket.go:372-396` 无事务属实，但**入口有补偿**：`ticket.go:364` 先查 `GetTicketGlobal`，
  若状态为 `cancelled`/`queued` 则记 `ticket_dup_exit` 审计并放弃收尾——**专门防双扣费**。
  → 团队清楚这个风险并加了幂等护栏，只是没走事务。

### E8　【新增发现】`core.ts` 401 注释与实现自相矛盾

`core.ts:43` 注释：「清除登录态并**跳转回登录页**」；实现：`window.location.href = '/'`（**首页**）。
即「以为修好了、其实没修对」——这类比完全没修更难发现，因为注释会让人误判已解决。

---

## 附：本次评估的实测证据索引

| 结论 | 复现方式 |
|---|---|
| 后端全绿 | `cd backend-go && go build ./... && go vet ./... && go test -race ./...` |
| UAT 88/88 与 322/326 | `bash scripts/uat/run_uat.sh` → `/tmp/uat_main.log`（A-PASS=70 B-PASS=18 T-PASS=322 FAIL=4） |
| T28 真实成因 | `/tmp/uat_main.log:282-285` + `backend-go/internal/api/upload_chunk.go:93/103/115/218/245` |
| assist 38/38 | `bash scripts/uat/assist_uat.sh` |
| 多实例不透支 | `bash scripts/uat/multi_instance_e2e.sh` → `M3-no-overdraft(7025)`、`M3-ledger-reconcile(ledger=975 consumed=975)` |
| 前端双红 | `cd frontend-react && npx tsc --noEmit` / `npx vitest run src/i18n/locales.core.test.ts` |
| SDK Node22 坏 | `cd sdk/typescript && node --test test/`（Node 22 报 MODULE_NOT_FOUND；Node 20 报 8/8） |
| 路由双向 diff | 后端 `HandleFunc` 注册表 249 条 × 前端 `/api/*` 字面量 200 条 |
| 微信/支付宝未接入 | `backend-go/internal/payment/payment.go:158` / `:222` |
| 退款口径错位 | `backend-go/internal/store/billing.go:1709-1714` |
| 硬编码 token | `backend-go/internal/assist/config/config.go:62` |
| 限流文案误导 | `backend-go/internal/api/api_openapi_tasks.go:131` / `:666` |
