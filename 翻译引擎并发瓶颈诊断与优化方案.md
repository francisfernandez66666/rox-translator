# 技术方案 · 翻译引擎并发瓶颈诊断与吞吐优化

> 版本 v1.0 ｜ 2026-08-26 ｜ 现象：多文件并翻时长时间卡顿、整体吞吐远低于预期
> 诊断方式：全链路代码走查（engine/llm/service/queue/fileproc）× 生产服务器约束（2 核 / 1.6G 内存 / GOMEMLIMIT=850Mi）交叉推演
> 性质：性能根因分析与改造设计 ｜ 状态：⏳ 待实施
> 关联：《评审整改·余额贯通与商业化收口方案.md》；硬闸无上限重试为特性设计（该文档 §〇），本方案不动它。

---

## 一、现象复现推演（为什么是「卡很久」而不是「慢一点」）

以典型负载推演：**1 个 300 段 × 2 语言的 pro 模式文件工单**：

| 阶段 | LLM 调用量（全部争抢同一信号量） | 估算占用 |
|---|---|---|
| KB 匹配（langOnly=true） | 未命中精确/CJK 缓存的段**每段每语言 1 次 Embed**（语义段在 langOnly 早退之前执行）：最多 300×2=600 次 | ≈240 slot·s |
| 批量初翻 BatchTranslate | 每语言 ceil(300/15)=20 批 ×(主调用+429 重试/fallback) | ≈400 slot·s |
| 批量审校 ReviewTranslationBatch | 每语言 ~10 块 | ≈300 slot·s |
| **合计** | — | **≈940 slot·s ÷ 3 并发槽 ≈ 5~6 分钟（零争抢下限）** |

系统实际同时存在：4 worker × 工单内 3 文件并发 × 多语言 goroutine + 前台交互 `/api/chat`，而**全进程只有 3 个 LLM 并发槽**。排队深度线性堆积；任一工单翻译阶段超过 20 分钟后，「卡死巡检」把仍在运行的工单重复入队，工作量瞬间翻倍——正反馈雪崩。这就是「卡很久且越卡越久」的完整机制。

## 二、根因清单（按贡献度降序）

### R1 🔴 全进程唯一 LLM 信号量 = 3，且 Embed 与 Chat 错误耦合
- llm/client.go:27 `MaxLLMConcurrent = 3`，acquire/release 包住所有 doChat 与 embedChunk。
- 要害①：IO 密集调用，2 核 CPU 不是瓶颈，3 是硬编码且不可配置。
- 要害②：Chat 走 SiliconFlow、Embed 走智谱——两家账号限额互不相干，却被同一信号量绑死。KB 匹配阶段数百次小 Embed 占满 3 槽，真正的翻译 Chat 反而排队。

### R2 🔴 文件管线逐段逐语言单条 Embed：无批处理、无缓存
- translateOneInner 语义段对每个未命中段调 `e.LLM.Embed(ctx, zhText)` 单条请求；HandleFile 对每个 kbLang 各跑一遍 TranslateOne——**同一段文本被嵌入 K 次**。
- 300 段 × 3 语言 ≈ 900 次 HTTP 往返，本可压缩为 `ceil(去重段数/32)` ≈ 10 次 EmbedBatch。

### R3 🔴 卡死巡检会重复执行仍在运行的工单（正确性 bug，雪崩放大器）
证据链：
1. store/tickets.go:278 `RequeueStalledTickets`：`updated_at < 20min 前` 即置 queued 并**无条件释放 jobs 租约**；
2. 但其注释前提「updated_at 由各步骤持续刷新」对文件工单**不成立**：runFileTicket 只在 3~4 个里程碑写状态，BatchTranslate 的 onBatchDone 回调传的是 **nil**（engine/file.go:222、244）——长翻译阶段内 tickets.updated_at 完全不动；
3. 原 goroutine 的 ctx 无任何取消机制继续在跑；第二个 worker 领到被释放的 job 再跑一遍；
4. 收尾守卫只拦 `status=='cancelled'`（service/ticket.go:183），拦不住被巡检改写的 queued 态。

后果：双份 LLM 消耗、**chargeTokens 双份扣费**、双份站内信/webhook、BumpTmHit 计数翻倍——且发生在系统最拥塞的时刻。

### R4 🟠 子进程资源无闸：LibreOffice/pdf2docx 在 1.6G 机器上可多开
- runFileTicket 允许 3 文件并发（service/ticket.go:332）；fileproc 每次 extract/apply 各起一个 python 子进程（pdf2docx/PyMuPDF 峰值数百 MB）或 headless LibreOffice（250~600MB RSS）。
- 2 个 PDF 同时转换 + Go GOMEMLIMIT=850Mi → 内存超售 → swap 抖动 → 全机无差别变慢（quant-research 拖死全机是同款先例），观感即「引擎卡」。

### R5 🟠 BatchTranslate 解析失败的兜底是纯串行
- engine/engine.go:1351 起：`<sN>` 解析命中率 <90% 时未译出段**逐段串行** singleLang。批量输出格式稍有漂移，100 句文档退化为 100 次串行往返。

### R6 🟠 交互式翻译与批任务无 QoS 分道
- 前台一句划译级请求与后台 4 worker 批任务抢同 3 槽；一个大工单足以让交互请求排队数十秒起步。

### R7 🟡 次要项
- `SetEmbedOverride` 裸写 Client 字段无锁（数据竞争，-race 必报；当前全局阶段模型同值实害有限）；
- cultureRules 在 cultureMu 锁内做 DB Query，短暂放大尾延迟；
- 信号量排队完全无观测（等待时长/队列深度黑盒）。

## 三、改造设计

### R0 快赢（纯配置/运维，当天可做）
| 项 | 动作 |
|---|---|
| 开启自愈 | `watchdog_selfcheck_restart=1` |
| 内存护栏 | memmonitor 阈值复核；确认 quant-research 保持禁用 |
| 基线 | 记录改造前 `/metrics` 的 LLM 错误率与内存曲线作对照 |

### R1 信号量拆分 + 可配置（小改动大收益）
```go
// llm/client.go
chatSem chan struct{} // env LLM_CHAT_CONCURRENT，默认 3（保守不变）
embSem  chan struct{} // env LLM_EMBED_CONCURRENT，默认 6（智谱独立额度）
```
doChat 用 chatSem、embedChunk 用 embSem，两类供应商额度解耦；生产按 SiliconFlow 账号限流实测上调 chat 值（IO 密集，CPU 非约束）。

### R2 Embed 批处理 + 进程级缓存（文件管线最大单点收益）
1. **嵌入缓存**：engine 加 embedCache（键 sha1(zh)，上限 ~20000 条，超限整代清除），命中跳过 HTTP；
2. **管线级预取**：HandleFile 在 KB 匹配前对**去重后的全部源文**一次 EmbedBatch(32)，向量表随 ctx 注入（WithEmbedLookup）；translateOneInner 语义段先查表、miss 再回源。Embed 调用量从「段数×语言数」降为「去重段数/32」。

### R3 卡死巡检正确性修复（bug 修复，三件套缺一不可）
1. **心跳**：runTicket 处理期间 60s Ticker 调新方法 `Store.TouchTicket(id)`（单 UPDATE updated_at）——顺带治好前端进度条长时间静止；
2. **巡检加租约条件**：RequeueStalledTickets 仅当对应 jobs 行不在「running 且租约未超时」状态才允许重排/释放，不再凭 updated_at 单方面判死刑；
3. **完成守卫收紧**：runTicket 收尾发现 status 已非自己写入的 in_progress（如被重排为 queued）→ 放弃计费/完成态/通知并记审计「检测到重复执行，本副本退出」，杜绝双扣费。

### R4 文件子进程资源闸
- fileproc 包级信号量 `FILEPROC_MAX_CONCURRENT`（默认 **1**：1.6G 机器上 Office/PDF 转换必须串行；升配后再调大），罩住 runDocxScript / runDocxScriptStdin / writePDFViaPython 三处 exec；
- Linux 下子进程包 `nice -n 10`，防转换抢占 Go 主进程 CPU。

### R5 批量兜底并行化 + 二次收编
- 解析命中率 <90% 时先用减半 bs 重发一批；仍失败再走兜底；
- 兜底循环由串行改为有界并发（sem=chatSem 容量），复用统一限流。

### R6 交互 QoS 车道
- chat 通道保留 1 个专属槽（chatSem 拆 reserved+shared 两段：交互请求可抢占 reserved，批任务只能用 shared）；实现保持简单——`TryAcquireReserved` 仅 /api/chat* 路径调用。

### R7 次要项修复
- SetEmbedOverride 改为 mutex 保护或下沉为 CallChat 参数传递；
- cultureRules 先查缓存、锁外查询、回锁写缓存；
- 观测性：semaphore 等待 >1s 打 `[llm-queue] waited=Xs kind=chat|embed` 日志 + metrics 增加 inflight/queue_depth 两个计数。

## 四、验收清单

1. 压测基线对比：同一 300 段×2 语言样例包，改造前后端到端耗时下降 ≥50%（R1/R2 生效的直接证据）；
2. 构造 >20min 长工单：全程仅执行一次（jobs 不被提前释放、无双份通知/无双份 tokens_billed）；
3. 同时提交 3 个含 PDF 的工单：任一时刻最多 1 个转换子进程（ps 观测），机器无 swap 抖动；
4. 批量输出格式漂移注入测试（mock 返回乱序编号）：不再出现 N 倍串行时长；
5. 大批量工单运行期间发起前台 /api/chat：首 token 等待 <5s（reserved 槽生效）；
6. `go test -race ./internal/llm/... ./internal/engine/...` 通过（R7 竞态清零）。

## 五、实施顺序

| 序 | Commit | 内容 | 依赖 |
|---|---|---|---|
| 1 | `llm-sem-split-config` | R1 + R7 观测日志 | 无 ★最高优先 |
| 2 | `stall-sweep-correctness` | R3 三件套（心跳/租约条件/完成守卫） | 无 ★正确性 |
| 3 | `embed-batch-cache` | R2 | 建议 Commit 1 后 |
| 4 | `fileproc-resource-gate` | R4 | 无 |
| 5 | `batch-fallback-and-qos` | R5 + R6 | Commit 1 |
| 6 | 杂项 | R0 配置 + R7 其余 | 无 |

每步独立可回滚；改完 `go build/vet/test`（含 -race）全绿。

## 六、实施记录

| 项 | 状态 | 落点 |
|---|---|---|
| R1 信号量拆分+可配置 | ✅ | llm/client.go：chatSem(env LLM_CHAT_CONCURRENT, 默认2)+chatFast 保留槽+embSem(env LLM_EMBED_CONCURRENT, 默认6)；semSlot 凭据按原通道归还；排队>1s 打 [llm-queue] 观测日志；Inflight 计数 |
| R2 Embed 批处理+缓存+预取 | ✅ | engine/embedcache.go（进程缓存 sha1 键，上限 20000 条整代清空 + WithEmbedLookup ctx 向量表）+ HandleFile 去重后 EmbedBatch(32) 管线预取 + translateOneInner 三级查找（预取→缓存→回源）——Embed 调用量从「段数×语言数」降为「去重段数/32」 |
| R3 卡死巡检正确性 | ✅ | TouchTicket 心跳(runTicket 60s Ticker) + RequeueStalledTickets 加「jobs 无 running 行」前置条件（不再释放活跃租约）+ runTicket 收尾守卫拦截 cancelled/queued 双态防双扣费 |
| R4 子进程资源闸 | ✅ | fileproc/procgate.go：FILEPROC_MAX_CONCURRENT(默认1) 信号量罩住 runDocxScript/runDocxScriptStdin/writePDFViaPython；Linux nice -n 10；排队>2s 打 [fileproc-queue] 日志 |
| R5 批量兜底并行化 | ✅ | BatchTranslate 重构：低解析率块(bs=5)二次收编 → 仍未译出段有界并行(sem=3)兜底，消除纯串行退化 |
| R6 交互 QoS 车道 | ✅ | llm.WithInteractive ctx 标记；handleChatStream/handleChat/handleOpenAPITranslateSync 可抢占保留槽，批任务仅用后台槽 |
| R7 次要项 | ✅ | SetEmbedOverride/embedOverride 互斥锁化；cultureRules 维持锁内快查（后续可再优化）；[llm-queue]/Inflight 观测落地 |

---

*本文与《评审整改·余额贯通与商业化收口方案.md》并列构成收费前整改批次；性能数字为推演值，以 Commit 1 落地后的实测基线为准修订。*
