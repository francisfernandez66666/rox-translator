# 全链路 UAT 自动化测试报告（2026-09-05）

> 范围：后端↔后端（Go 单测 + 接口断言）、前端↔前端（像素级 Playwright）、前端↔后端（浏览器真实联调）、交易/计费全链路。
> 方式：`scripts/uat/run_uat.sh` 一键从零拉起 mock LLM + 全新库后端 + 全量断言 + 前端 E2E，可重复执行。

## 一、测试结果总览

| 层级 | 套件 | 结果 | 说明 |
|---|---|---|---|
| 后端单测/集成 | `go test ./...` | **16 包全 ok / 0 失败** | api/store/engine/kb/payment/orchestrator 等 |
| 后端接口断言 | `api_uat.sh`（A1–A16 = 49 断言 + B1–B18 = 18 断言，共 67） | **67/67 通过** | 覆盖认证/计费/翻译/支付/裂变/OpenAPI/权限/文件 + 运营策略计费因子（B 阶段） |
| 前端 E2E | smoke + pixel + quality + **mobile（2026-09-07 移动端自适应）** | **15/15 通过** | 像素渲染 + 运行时健康 + 全链路翻译回流 + 移动端抽屉/输入行/全站防溢出 |
| 前端构建 | `tsc --noEmit` + `vite build` | **通过**（修复 1 处回归后） | — |

关键链路均以真实后端 + mock LLM（OpenAI 兼容 chat/embeddings）贯通验证：注册→体验额度发放→聊天翻译→实时计量→余额扣减；充值下单→模拟支付→幂等→发票；邀请裂变；KB 术语命中；OpenAPI 密钥鉴权同步翻译；docx 文件翻译交付译文文件。

## 二、UAT 期间修复的缺陷

1. **`frontend-react/src/i18n/panels/usage.ts`（TS 回归）**：用量看板新增组织下钻时 `usage.colCost` 键重复，`tsc --noEmit` 直接报 TS1117。已删除重复键，typecheck 转绿。
2. **`ChatWindow.tsx` 缺失 `data-testid="translate-input"`**：仓库自带 smoke 断言了该节点但前端从未实现（注释自认"前端需补充"）。已补上。
3. **`e2e/smoke.spec.ts` 与产品实况脱节**：标题正则 `/翻译助手/` 不匹配实际标题「智能翻译平台」；未登录直接访问 `/` 断言工作台输入框必然失败；`/status` 硬编码 8787 旧端口。已修正为：登录态注入后断言工作台、标题正则放宽、`API_URL` 可配、未登录兜底断言登录页。

## 三、产品侧发现与修复状态

> ✅ **P1/P2/P3 三项缺陷已全部修复并在本报告 B 阶段回归通过**（提交 `df664fb`，已部署生产 2026-09-05）。

| 级别 | 原问题 | 修复状态 |
|---|---|---|
| **P1** | 企业成员邀请加入（主站）不可用：`register.go` 对"有效邀请码 + role_choice=member"路径同样执行 `req.Invite=""`，主站企业成员注册永远报「请提供租户编码」；仅品牌专属域名路径可用。 | ✅ 已修复：加入路径保留 `joinWithInvite` 标记与邀请码，走成员创建分支（角色=user、同租户重名 400、`MarkInviteCodeUsed`）。B 阶段新增 3 项断言全过 |
| **P2** | 平台级商业包订阅不可达：`/api/plans` 列出全部上架包（含 tenant_id=0 平台包），但 `GetPackageByCode` 严格按租户匹配 → 平台包必然返回「套餐不存在或已下架」。 | ✅ 已修复：`GetPackageByCode` 改为 `tenant_id IN (0, ?)` + 租户包优先回落平台包；新增 `TestGetPackageByCodeTenantPriority` 回归锁定。B 阶段平台包订阅断言通过 |
| **P3** | token 扣费批量落库（2s flush）：`/api/billing/balance` 与台账延迟 ≤2s 可见，对外部对账口径有 2s 窗口。 | ✅ 已修复：`billing/sink.go` 新增进程级 `Flush()`，响应前显式落库；覆盖对话/文件/余额/OpenAPI 同步 4 处调用点；`TestSinkFlushSynchronous` 锁定。B 阶段对账口径断言通过 |
| **P3（遗留建议）** | 余额硬闸默认关闭：`billing_enforced=0`（默认）时仅留痕不扣减。 | ⏳ 产品决策待办：建议生产开启并配置试用上限；运营策略引擎的计费因子可配置 `enabled=false` 直接中止翻译，两者可叠加 |

> 2026-09-05 运营策略（计费因子流程引擎）上线后补充的发现与回归（B 阶段 18 断言全绿）：
> 策略 GET/保存、fast 模式免费翻译（余额不变 + `biz_mode=fast` 台账留痕）、`limit_chars` 拦截 + pro 不受限、平台包订阅回落、套餐重置后二次触发限额、邀请奖励按策略因子 +900000 入账、P1 成员加入链路、活跃时间窗覆盖免费（上海时区 ±1 天窗口）——全部符合预期。

## 四、交易与计费专项结论

- 双桶台账（`quota_grants` 发放额度 + `balance_accounts` 永久余额）口径正确：体验发放进台账、充值进永久桶、扣费按台账→永久顺序。
- 支付链路幂等验证通过：同一订单二次 simulate 不重复入账（余额不变）。
- 发票基于已支付订单创建成功。
- 余额不足拦截在 `billing_enforced=1` 下生效（实测「组织 token 已耗尽」）。
- 恶意/异常分支：无 token 401、错误密码拒绝、跨租户订单/审计隔离、租户管理员仅见本租户用户、API Key 明文仅创建时返回、密钥脱敏回显——全部通过。

## 五、自动化测试程序

```
scripts/uat/
├── run_uat.sh        # 一键编排：构建→mock LLM→全新库后端→配置→API断言→前端E2E→汇总
├── api_uat.sh        # 后端 67 断言（A1-A16 计 49 + B1-B18 计 18），BASE_URL/UAT_DB/ADMIN_PASS 可配
└── mock_llm.py       # OpenAI 兼容 mock（chat 回译文 + 1024 维确定性向量）
frontend-react/e2e/
├── pixel_uat.spec.ts     # 7 项像素级：登录/工作台翻译回流/自服务/工单/后台面板/公开页/无溢出
├── pixel_quality.spec.ts # 2 项运行时健康：控制台错误/请求失败/页面异常
└── smoke.spec.ts         # 3 项冒烟（已修正可跑）
```

运行：`bash scripts/uat/run_uat.sh`（默认端口 8899/8901，`KEEP=1` 保留环境）。像素截图落 `frontend-react/artifacts/` 供人工复核。

## 七、移动端自适应批注（2026-09-07 追加）

- 主站/演示站移动端适配上线（提交 `e564079`）：后台侧边栏 ≤900px 转抽屉（汉堡唤起/遮罩点关）、聊天输入行换行、登录卡全宽、宽表格横向滚动、公开页（定价/协议）同步适配。
- 新增 `e2e/mobile_uat.spec.ts`（390×844 手机视口）3 项断言：后台抽屉行为 + 工作台输入栏 + 全站 8 页无横向溢出；全套件 E2E 由 12/12 升至 **15/15**，后端 API 67/67 不变。
- 详见 `PROGRESS.md` 〇-XXI 与《部署指南.md》头部注记。

## 六、风险与注意

- 自动化 UAT 使用 mock LLM（译文为占位文本），真实翻译质量/供应商容错需以真实 Key 在隔离环境复测。
- 环境侧：服务内置 watchdog 默认探活 8787，本地改端口须设 `SELFCHECK_URL`，否则进程会被"自动重启"自杀。
