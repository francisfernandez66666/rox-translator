# 能言 SaaS · 产品与技术文档

> 版本：v2.0.0 ｜ 生成日期：2026-09-08
> 依据：README、PROGRESS.md、部署指南.md、商业化白皮书、权限关系.md 及全仓代码（backend-go / frontend-react / sdk / extension / deploy / scripts）
> 生产地址：https://langcross.lexicorn.cn（主站） ｜ https://rox-test.lexicorn.cn（演示站）

---

# 第一部分 · 产品文档

## 一、产品定位与一句话商业模式

> **翻译引擎与行业术语知识库免费送，AI 消耗量（积分）预充值收费。**

**能言**是面向企业与团队的 AI 翻译协作平台，将**文件翻译、对话式翻译、知识库（KB）/翻译记忆（TM）**一体化，并采用 **积分预充值**的订阅商业化模型（对外只有积分一种口径，内部记账单位不外露）。

| 类比 | 对应 | 说明 |
|------|------|------|
| 打印机 | 翻译引擎 + 行业术语库 | 免费分发，零摩擦获客 |
| 墨盒 | 积分充值套餐 | 唯一收入流，译得多买得多 |

核心价值主张：
- **付费与价值严格对齐**——翻译量大即产品有用，续费自愿。
- **免费分发消除试用阻力**——官网/渠道铺货。
- **数据飞轮**——每一份免费部署持续产出高质量对齐语料，反哺行业术语库迭代。

---

## 二、目标用户与角色体系

### 2.1 角色层级（四级，L1~L4）

| 等级 | 角色 | 权限范围 |
|------|------|----------|
| L4 | 超级管理员（super_admin / admin） | 平台全量：租户管理、模型路由、运营策略、全局模型密钥、审计、系统告警 |
| L3 | 租户管理员 / 审核员（tenant_admin / approver） | 本租户：套餐、组织、外部调用、品牌、数据采集审批、邀请 |
| L2 | 部门管理员（dept_admin） | 本部门：知识库、工单、用量、概览 |
| L1 | 普通用户（user） | 工作台翻译、我的余额/套餐/账号 |

### 2.2 租户-组织-部门四级树

```
平台（tenant 0，能言根组织）
└─ 租户（tenant，如 ROX极石汽车）
   └─ 组织根（root）
      └─ 组织 / 部门（org / dept，可多级）
```

- 知识库可见性按**组织祖先链**动态判定（本部门→父→…→根，就近覆盖）。
- 部门级隔离：部门包仅本部门及链内可见，跨部门共享包可单独开关。
- 部门预算徽标、邀请码绑定组织。

---

## 三、核心功能模块

### 3.1 多格式文件翻译

- **入口口径（★ 2026-09-22 #36 起）**：文件翻译**只在「文档翻译」工单页**发起（`TicketsPage` → `/api/tickets/create-file`），走建单→审批→翻译→交付→保留期完整链路；即时翻译对话框不再接受附件。历史直连接口 `/api/translate` 仍在（强制登录，供集成方单文件直翻与 UAT 断言使用），但界面已无入口。
- **格式支持**：docx / pptx / xlsx / pdf / txt / csv / md → 输出译文文件；srt / vtt / json / yaml 等以**对照表（xlsx）**交付。
- **文件工单交付双模式（2026-09-13）**：建单时选择「交付方式」——
  - **还原文件**（默认）：原格式交付，另附译文纯文案 .md 兜底产物（详情页可单独下载）；版式回写失败自动降级为 .md 交付（工单成功 + 告警 + 站内信），不整单失败。
  - **纯文案**：anydoc 本地提取（doc/docm、ppt/pps/pot/pptm/ppsx/ppsm、xls/xlsm/xlsb、odt/ods/odp、rtf/epub 老格式与 ODF 系额外准入）→ 译文交付 `.md`；不承诺版式，扫描版 PDF 不支持（无 hosted OCR，数据不出境）。
- **多文件混合工单 + 多目标语言打包 zip 下载**。
- **PDF 保真翻译管线（两阶段）**：
  1. `extract`：pdf2docx 转 DOCX 并提取段落键（表格/嵌套/文本框/页眉脚）。
  2. `apply`：在缓存 DOCX 上 w:t 级替换（图片/排版零破坏）+ LibreOffice 转回 PDF。
  - 限制：PDF >40MB 或 >120 页友好拒绝并提示转 docx（防超大文件卡死）。OCR 已移除，图片内容按产品策略不翻译。

### 3.2 双模式翻译

| 模式 | 说明 |
|------|------|
| ⚡ 快速 | AI 初翻 + 校对，低成本高吞吐（可配置免费 `charge=false`） |
| 🎓 专业校对 | 知识库 + 评估（Evals/Judge）+ 硬闸（Gates）全流水线 |

### 3.3 即时（对话式）翻译

- **界面形态（★ 2026-09-22 #36）**：原文输入与译文结果合并在同一个整屏对话框内（占满除页眉外的可视区，结果气泡渲染在框内滚动区），不再是「输入框 + 独立气泡列表」两段式；页脚只在抽屉里保留。
- 聊天式交互、上下文审校、风格指令、SSE 流式输出（逐段上屏）。
- 语义检索 + 知识库术语命中高亮；**纯文本**翻译（附件入口已下线，见 §3.1）。
- **缩翻**（限长改写）现作用于文本翻译：勾选后随 `options.max_length` 下发，引擎文本路径消费。
- **计量即时性**（★ 2026-09-22 修复）：SSE 收尾在 done/error 终帧**之前**同步冲刷计量缓冲（`billing.Flush()`），保证前端收到完成信号后那一次余额/「今日已耗」刷新一定读到已落库的新值；回归断言 `A7s`（API 层）与 `TF2`（Playwright 层）双向锁定。

### 3.4 知识库与翻译记忆（KB/TM）

- **五档可见范围**（按优先级/相似度排序）：

```
链内部门包(本部门→父→…→根, 就近覆盖) > 企业包 > 历史无主行 > 行业包/文化包
回退层：跨部门共享包（需策略开关 + 包级 share_cross_dept）
```

- **KB 四层统一**：L1 术语 / L2 翻译记忆（TM）/ L3 安全句 / L4 碎片。
- 行业包 / 语言文化包（通用语言习惯包）自动采集（官方 API / 受限抓取 / LLM 生成三档数据源），自动审批热加载。
- **TM 自闭环**：`tm_review` 待审池唯一入库通道，超管人工审核通过才落正式 TM。
- **职业角色知识库包（persona，★ 2026-09-19）**：用户可设 job_role（15+ 预置职业角色词典），翻译时自动装配对应角色包，定向术语与语气风格；后台「角色包」面板统一管理。

### 3.5 组织与租户隔离

- 多租户、角色权限、部门预算、组织继承链与部门级隔离。
- 品牌定制按**子域名**解析租户品牌（名称 / Logo / 背景图）。

### 3.6 积分计费体系

- **对外只有积分一种口径**（★ 2026-09-19 全面落地）：界面、账单、API 出参一律积分展示，积分↔内部记账单位的换算与汇率全部收敛在服务端，任何接口不下发 token 裸值与汇率。
- **双桶台账**：额度台账（带到期日，体验/订阅）+ 永久余额（充值/increment），内部以记账单位落账。
- 扣减顺序：未过期额度行（按到期近到远）→ 永久余额 → 全部耗尽拒绝（`insufficient_balance`）。
- 预充值、订阅套餐、发票、待人工确认订单。

### 3.7 邀请裂变

- 个人邀请码 + 专属链接 + 二维码。
- 注册奖励 +1,000 体验积分 + 14 天叠加；首笔付费 → 邀请者 +1,667 积分。
- 仅**个人用户**（is_personal）可得邀请奖励；企业用户/平台超管前端隐藏入口（2026-09-07 起）。
- 防刷：oneid 邮箱双唯一 + 奖励双唯一 + 自助注销。

### 3.8 注册与邮件体系

- 自助注册分**个人 / 企业**两类；企业区分管理员（新建企业）/ 普通成员（凭企业邀请码加入）。
- 超管可配 6 类邮件模板 + 注册成功自动发送《产品手册》PDF 邮件。
- 中文邮件 RFC2047 主题 + base64 正文。

### 3.9 开放 API（OpenAPI）

- **异步任务模型**：创建任务 → 工单 ID → 轮询 status/download；多语言文件任务产物自动打包 zip（单文件×多语言在工单侧预打包，多文件任务下载端打包）。
- **同步短文翻译**：`POST /openapi/v1/translate`（≤5000 字符、≤5 语言），供划译插件与 Office taskpane。
- API Key（三绑定 + 哈希存储 + 仅展示一次）、余额查询、密钥轮换。
- 三语言 SDK：Python / TypeScript / Java（★ 2026-09-15 起走版本化发布：PyPI `langcross-translator`、npm `@langcross/translator-sdk`，Maven 源码分发；版本由 `scripts/release-sdk.sh` 三端同步校验，变更史 `sdk/CHANGELOG.md`）。

### 3.10 管理后台

- **三工作台**：超管 / 租管 / 部门管，租户切换器。
- 一级菜单（2026-09-15 Tab 精简后 8 项）：总览（含用量明细）、工单、个人中心、知识库（含数据源）、组织与成员、计费与套餐、外部调用（含 SDK）、系统与运维。
- 「计费与套餐」Hub：套餐与订单｜租户管理（L4）｜成本对账（L4）；「系统与运维」Hub：注册触达｜邮件模板｜流程引擎｜协议签署｜审计日志｜运营策略｜模型与供应商｜品牌与页脚。旧深链 key 自动跳转对应 Hub 子页。
- 审计日志列表按操作时间倒序（2026-09-15 修正，历史 id 排序曾受 PG 序列失步干扰，现由启动自愈 `syncSequencesPG` 兜底）。

### 3.11 运营策略引擎（计费因子流程引擎）

- 将体验/邀请/任务/注册/限额/翻译模式/支付/文件上限等计费因子收敛为**平台统一策略 + 活跃时间窗**。
- 超管后台「运营策略」面板可视化配置、保存即时生效。
- **权限**：仅超管（L4）可写（2026-09-07 收口），租户只读。

### 3.12 任务中心

任务中心有**两条并存**的发放路径，都受运营策略 `task.enabled` 总开关控制（关闭后手动领取与事件自动发放同时停发）：

1. **手动领取（历史能力）**：超管在「运营 → 任务」自定义每日（daily）/一次性（once）任务，用户在任务中心点击领取，奖励为**永久积分**。接口 `/api/me/tasks`、`/api/me/tasks/claim`；超管侧 `/api/admin/tasks`（列表/保存/删除）。
2. **事件自动发放（★ 2026-09-22 #33 新增）**：五类增长任务由服务端业务事件自动触发，用户无需点击，发放成功即在任务中心显示进度与「已到账」徽标：

   | 任务 | 触发点 | 奖励 | 有效期 | 频次上限 |
   |------|--------|------|--------|----------|
   | 每日登录 `login_daily` | 登录成功 | +100 临时积分 | 3 天 | 每日 1 次 |
   | 每周发起翻译 `translate_week` | 文本/文件翻译成功收尾 | +100 临时积分 | 7 天 | 每日 1 次、每周 5 次 |
   | 邀请好友注册 `invite_register` | 受邀方注册成功 | +500 临时积分 | 14 天 | 按受邀事件，可叠加 |
   | 邀请好友充值 `invite_paid` | 受邀方任意一笔支付到账 | +1000 **永久**积分 | 不过期 | 按事件，可叠加 |
   | 上传专属知识库 `kb_upload` | 知识库解析成功 | +600 **永久**积分 | 不过期 | 一次性 |

   - **临时/永久双桶**：临时积分写 `quota_grants`（`kind='task'`、`source='task:<task_key>'`），永久积分写 `balance_accounts.balance`。扣费走 `DeductWithGrants` 的「到期日升序优先」，任务临时积分天然先于充值永久余额消耗；月度套餐重置只碰 `kind='plan' AND source='order'`，不会误清任务台账。
   - **叠加与顺延**：带有效期的任务每次发放按 `StackExpiry` 把到期日顺延到「当前最晚未过期到期日 + 有效期」，多次登录/翻译不会互相覆盖。
   - **幂等与并发**：去重靠 `user_task_rewards` 唯一索引 `(task_id,user_id,dedup_key)`，频次靠 `user_task_period_cnt` 条件计数器（`cnt<cap` 守卫）；重复/触顶整体回滚，不留「占了名额没发钱」的半状态。发放结果 reason 取值：`disabled/duplicate/capped_day/capped_week/no_reward/bad_task`。
   - **超管「重置积分消耗量」**（`/api/admin/tasks/reset-consumption`，写审计）：把任务临时积分台账的 `left` 拉回 `total`（消耗完的一并恢复），**只恢复余额、不改到期日**；`subscribedOnly=true` 时仅作用于存在未过期订阅台账（`kind='plan'`）的租户。
   - 命名提醒：`/openapi/v1/tasks*` 是 OpenAPI **翻译作业**（建单/轮询/下载），与增长任务中心无关，勿混用。

### 3.13 其他

- **Office 划译插件**（extension）：Word 侧加载 taskpane，选区翻译插回文档。
- **对照编辑器**：双栏对照、术语高亮、逐段通过/驳回批注。
- **移动端自适应**（2026-09-07）：≤900px 后台侧栏转抽屉、聊天输入行换行、全站防横向溢出。
- **可读性基线**（★ 2026-09-21 用户反馈，2026-09-22 落地 #35）：登录后的前台/后台字号整体上调一档（基础 14→15、正文与表格 13→14、标签与说明 12→13、极小徽标 10/11→11/12），次级/弱说明文字色提亮到对页面底与卡面**两档都过 WCAG AA 4.5:1**（实测 text-2 8.78/7.96、text-3 7.79/7.06、text-4 6.80/6.16）。实现方式为 `styles/theme.css` 的**页面级覆写**（`:root:root` 抬令牌特异度、`html .lc-*` 抬类级），因为 `src/ui/langcross/**` 是交付包冻结区禁改；后续新增面板沿用同档字号与色阶，勿再回退旧值。

---

## 四、商业化套餐体系

| 类型 | 示例 | 到账 | 适用 |
|------|------|------|------|
| free 试用 | 新客礼包 1,000 积分 / 14 天 | 注册自动发放入体验额度台账 | 所有新租户 |
| paid 包月订阅 | 基础版 / 专业版（含每月积分额度） | 购买确认即发放，t+30 天滚动 | 稳定翻译量长期客户 |
| increment 充值包 | 3,000 / 15,000 / 60,000 / 300,000 积分 | 一次性充入**永久余额**，永不过期 | 用量波动大或需长期储备 |

- **定价原则**：售价 = 上游大模型采购成本 × 3（毛利约 66%），后台可视化可调。
- **支付通道**：mock（测试）/ static_qr（静态码 + 人工确认）/ 微信、支付宝（接口位预留）/ USDT（TRC20/ERC20/BEP20，海外线：尾数对单 + 人工核销，可选链上自动对账，默认关）。

---

## 五、产品演进路线（里程碑）

| 阶段 | 时间 | 核心交付 |
|------|------|----------|
| 〇-A | 2026-08-27 | React+TDesign 重写、租户级唯一、KB 嵌入计费 |
| 〇-B~〇-D | 2026-08-27~28 | 品牌子域、邀请裂变、实时计费、需求交付 |
| 〇-E~〇-F | 2026-08-28 | OpenAPI 全功能 UAT、中低优整改、文件翻译修复 |
| 〇-G~〇-I | 2026-08-30 | PG 切流、Redis、错误码、邮件异步、SDK 鉴权统一 |
| 〇-II~〇-IV | 2026-08-31 | 体验额度统一、演示独立化、订单通知链路 |
| 〇-V~〇-VII | 2026-09-01~02 | 行业/文化包自动采集 + 硬闸、前端审计、符号残留根治 |
| 〇-VIII~〇-XI | 2026-09-02~03 | 前后端契约审计、自动审批、任务中心、待审分页 |
| 〇-XII~〇-XIX | 2026-09-04~05 | KB 性能优化、共享包宿主迁移、品牌术语种入、用量看板修复、运营策略引擎 |
| 〇-XX~〇-XXI | 2026-09-05~07 | 运营策略收口、支付确认链路修复、移动端自适应 |
| 〇-XXII~〇-XXXIII | 2026-09-08~13 | 九宫格 Hub 重构、PDF 写回管线（pdfwrite/anydoc 桥接）、质检 evals 闭环、演示站独立部署 |
| 〇-XXXIV~〇-XLI | 2026-09-14~16 | 沙箱化 systemd 迁移（User=translator + secrets.env）、USDT 三链对账、AI 销售助手并入主 module、审计/密钥掩码收口 |
| 〇-XLI~〇-XLVI | 2026-09-17~20 | 工程闸门体系（AGENTS.md/gofmt/log 棘轮/路由鉴权/分层守卫）、积分口径全对外收敛、12 语种 i18n、i18n 取词踩空补齐 |
| 〇-XLVII | 2026-09-21 | 全站十语种 2532 键全量词典、任务系统前端与 i18n 底座 |
| 〇-XLVIII | 2026-09-22 | 任务系统事件自动发放（#33 五类任务 + 临时/永久双桶 + 超管重置）、后台 AI 助手重做（#34）、可读性上调（#35）、即时翻译对话框合并并下线文件翻译（#36）、评估报告缺陷批 #37–#43 与 md 交付物 RC-1~RC-6、SSE 计量即时落库修复 |

> 逐批明细（含验收口径与踩坑）以 [PROGRESS.md](PROGRESS.md) 为唯一权威时间线，本表只做里程碑索引。

---

# 第二部分 · 技术文档

## 一、技术栈总览

| 层 | 技术 |
|----|------|
| 后端 | Go（单二进制，module `translator`），REST API |
| 数据库 | PostgreSQL 16 + pgvector 0.6.0（生产，同机自建）；SQLite（本地/历史，双方言兼容） |
| 向量检索 | pgvector 原生 embedding 列（1024 维）；npz 文件兜底 |
| 前端 | React 18 + LangCross 自研组件库（src/ui/langcross；TDesign 存量过渡共存，Vite 5 构建），TypeScript |
| 队列 | 自研 `internal/queue`（jobs 表 direct 队列）；Redis 可选多实例唤醒 |
| 分布式 | `internal/infra`（redis / distlock / ratelimit / concurrency），进程内降级 |
| 消息/邮件 | 异步邮件队列（jobs 表），SMTP + RFC2047 编码 |
| 反向代理 | Caddy（自动 HTTPS / on-demand TLS / 安全头 / 支付回调注入） |
| 进程管理 | systemd（`translator.service`，1G 内存红线调优） |
| 测试 | Go test（`internal/...`）、Vitest、Playwright E2E |
| SDK | Python / TypeScript / Java（同一 OpenAPI 契约；npm/PyPI 版本化发布） |

## 二、项目目录结构

```
能言/
├── backend-go/          # Go 后端源码（单二进制）
│   ├── cmd/             # 服务与工具入口
│   │   ├── server/      # 主服务（main.go）
│   │   ├── migrate-sqlite-to-pg/   # SQLite→PG 一次性迁移
│   │   ├── backfill-embeddings/    # npz 向量回填 PG embedding 列
│   │   ├── rebuild-kb-index/       # 重新计算向量写回 PG
│   │   └── auto-approve/           # 历史待审批量自动审批工具
│   └── internal/        # 各业务子系统
│       ├── api/         # HTTP 接口层（路由/中间件/handler）
│       ├── store/       # 平台数据层（用户/工单/KB包/计费/审计等）
│       ├── engine/      # 翻译引擎（KB 匹配/LLM/后处理/硬闸）
│       ├── kb/          # 知识库数据库与向量检索
│       ├── tenant/      # 租户/组织/部门存储与权限
│       ├── billing/     # 计费服务（限流/配额/实时计量批量落库）
│       ├── ops/         # 运营策略引擎（计费因子流程引擎）
│       ├── crawler/     # 行业/文化包自动采集（三档数据源）
│       ├── culture/     # 语言文化规范（中性词豁免等）
│       ├── gate/        # 硬闸护栏（质量校验）
│       ├── evals/       # LLM-as-Judge 评估器
│       ├── llm/         # LLM 客户端（Chat/Embed/用量收集）
│       ├── service/     # 工单服务（入队/worker/通知/续传）
│       ├── queue/       # 异步队列（jobs 表）
│       ├── payment/     # 支付（mock/wechat/alipay）
│       ├── fileproc/    # 文档转换子进程（pdf2docx/LibreOffice/anydoc 纯文案提取）
│       ├── doc/         # docx 纯 Go 段落抽取/回写
│       ├── auth/        # 认证（JWT/OIDC/SSO）
│       ├── iam/         # 身份管理
│       ├── infra/       # Redis 分布式原语
│       ├── config/      # 运行时配置（模型/语言/目录）
│       ├── errors/      # 统一错误码体系
│       ├── observability/ # slog 日志
│       └── ...
├── frontend-react/      # React 前端（自研组件库 src/ui/langcross）
│   ├── src/
│   │   ├── api/         # 前端 API 客户端
│   │   ├── components/  # 页面与组件（Chat/Tickets/Editor/selfservice/admin）
│   │   ├── stores/      # 状态管理（auth/admin）
│   │   ├── hooks/       # 自定义 hooks（useChat）
│   │   ├── i18n/        # 中英词典（dicts.zh/en）
│   │   ├── lib/         # 工具库（industries 等）
│   │   ├── styles/      # 样式（theme.css / mobile.css）
│   │   └── types/       # TS 类型
│   ├── e2e/             # Playwright 端到端测试
│   └── vite.config.ts / vitest.config.ts
├── deploy/              # systemd / Caddy / 脚本 / 观测 / 压测 / 多AZ
├── scripts/             # 数据库初始化与切流
├── extension/           # 浏览器划译插件（MV3）
├── sdk/                 # OpenAPI SDK（python / typescript / java）
└── data/                # 运行时数据
```

## 三、后端架构

### 3.1 服务启动链路（cmd/server/main.go）

1. **内存优化**：未设置 `GOMEMLIMIT` 时默认 650Mi，部署经环境变量覆盖（生产 850MiB）。
2. **结构化日志**：slog JSON 日志器，全链路带 trace_id。
3. **配置加载**：`config.Default()` → config.json / 环境变量覆盖。
4. **Redis 初始化**：`REDIS_ADDR` 非空启用分布式能力，空则降级进程内。
5. **数据库打开**：`kb.Open`（SQLite/PostgreSQL 双方言）→ 租户迁移 → 平台 Store。
6. **平台初始化**：admin 账号（`EnsureAdmin`）、余额账户（`EnsureBalance(0/1)`）、默认 KB 包（`EnsureDefaultPackages`）。
7. **向量索引加载**：`kb.LoadNPZ`。
8. **翻译引擎创建**：`engine.NewEngine`，装配 LLM 客户端、评估器。
9. **模型路由水合**：从 `system_config.model_routes` 加载多供应商路由 + 后台 LLM 密钥解密水合。
10. **HTTP 服务**：`api.NewServer` → 计费服务、工单服务、路由注册、看门狗。
11. **实时计费钩子**：`eng.LLM.OnUsage = srv.ChargeUsageRealtime`（边工作边计费）。
12. **优雅停机**：SIGTERM/SIGINT → 批量落库 → 10s 内优雅关闭。

### 3.2 中间件栈（Handler）

```
withAPIVersion → withTraceID → withMetrics → withTenant → withCORS → withBodyLimit → withAccessLog → mux
```

- **withAPIVersion**：解析 API 版本（Accept / X-API-Version，默认 v1，v2 预留）。
- **withTenant**：租户解析优先级 = 登录 JWT > API Key 所属租户 > X-Tenant-ID（仅超管）> 默认租户 1。
- **安全约束**：普通用户租户取自 JWT（防越权）；未登录忽略 X-Tenant-ID（防伪造）；超管（tid=0）可切换生效租户。
- **withCORS**：白名单校验，`/openapi/*` 前缀无条件反射（Key 鉴权无 Cookie，CSRF 不成立）。
- **withBodyLimit**：非 multipart 请求体 ≤5MB。

### 3.3 数据模型（核心表）

| 表 | 职责 |
|----|------|
| `users` | 用户（tenant_id + username 唯一，role/status/deactivated_at） |
| `tenants` | 租户（code/name/brand_names/status/policy_config） |
| `orgs` | 组织树（parent_id、层级、部门预算） |
| `tickets` / `ticket_state` / `ticket_files` | 工单 + 步骤轨迹 + 多文件产物 |
| `kb_packages` | 知识库包（tenant/industry/locale 三类，parent_id） |
| `kb_entries` | KB 条目（四层 L1~L4，含 1024 维 vector） |
| `kb_safety_phrases` | 安全句锁死串库 |
| `tm_segments` | 翻译记忆段（pgvector embedding 列） |
| `balance_accounts` | 租户 token 永久余额账本 |
| `quota_grants` | 额度台账（带到期日，可叠加） |
| `usage_ledger` / `usage_daily` | 计量明细 + 日用量计数器 |
| `orders` / `payments` / `invoices` | 订单 / 支付流水 / 发票 |
| `api_keys` | 开放 API Key（哈希存储） |
| `packages` / `rate_card` | 套餐 / 单价表 |
| `system_config` | 系统配置（键值，含 ops_policy、model_routes 等） |
| `audit_logs` / `alerts` / `notifications` / `feedbacks` | 审计 / 告警 / 站内信 / 反馈 |
| `tm_review` / `tm_hit_count` | TM 自闭环待审池 |
| `referral_rewards` | 邀请奖励 |
| `kb_pack_sources` / `kb_staged_entries` / `kb_staged_phrases` | 采集数据源 / 待审池 |
| `user_tasks` / `user_task_claims` | 任务中心 |
| `jobs` | 异步任务队列（邮件/重试/死信） |
| `translation_edits` | 对照编辑器段落 |
| `invite_codes` / `webhooks` / `rate_limits` / `kb_upload_rewards` | 邀请码 / Webhook / 限流 / KB 上传奖励 |

- 建表采用 `CREATE TABLE IF NOT EXISTS` 幂等 + 老库补列迁移（`EnsureColumns`）+ 缺列索引补建。
- 双方言兼容：`db.CurrentDialect()` 分支 SQLite/PostgreSQL 语法差异。

### 3.4 翻译引擎管线（engine）

`TranslateOne` 复刻 Python lib.py 的 translate_one **四段匹配流程**：

```
① 精确命中（两段式：链内优先 → 跨部门共享包）
② CJK 标点无关精确（缓存键含组织链指纹 + 开关态）
③ 模糊子串（链内可整句采用；跨部门仅作例句参考）
④ 语义检索（pgvector 优先 / npz 回退；高相似+字符重叠才整句替换）
→ 未命中 → 模型兜底（并发逐语言）
→ L2 语言文化硬闸门（replace 自动替换 / forbidden 拦截）
```

关键机制：
- **模型路由**：超管多供应商路由（按权重选主，失败按权重降序降级）+ 分阶段模型（kb_match / ai_initial / evals / review / kb_embed / initial_evals / review_evals）。
- **主模型熔断**：Hunyuan 连续失败超阈值 → 熔断降级，冷却后自动恢复。
- **429/网络错误降级重试**、finish_reason=length 翻倍重试、截断自修复。
- **请求级用量计量**：`WithUsageRecorder` 注入 UsageCollector，全链路（初翻/校对/Judge/文化闸门/embedding）真实 token 归集。
- **基于 ctx 的进度回调**（替代单例字段，防并发串台）。
- **硬闸护栏**（Gate 8 项硬校验 / 语言文化闸门）：失败附 KB 参考自动重译，循环至通过或达 `gate_retry_max`（默认 8）上限。

### 3.5 计费架构（billing）

- **实时计量（边工作边计费）**：`llm.Client.OnUsage` 每次 LLM 调用即时上报用量。
- **批量落库**：`UsageSink` 内存累积 + 周期（2s/200 条）按租户单事务批量落库，消除并发 SQLITE_BUSY。
- **影子余额**：内存维护每租户影子余额，余额不足立即触发 abort（延迟 ≤ 刷新周期）。
- **同步冲刷**：`Flush()` 在对话/文件/余额查询前显式落库（P3 修复，消除 2s 对账窗口）。
- **双桶扣减**：额度台账（到期近到远）→ 永久余额，事务原子。
- **运营策略引擎**：`ops/policy.go` 解析顺序 = 代码默认 → 存量散键 → 平台 ops_policy → 活跃时间窗（最高 priority 最后应用）。

### 3.6 租户解析与隔离

- 生效租户 `effTenant`：超管（tid=0）由 X-Tenant-ID 指定，否则平台上下文；普通用户固定自身租户。
- 平台根账号（tid=0）无知识库归属，超管平台视角纯模型翻译，不泄露具体租户 KB 数据。

### 3.7 异步任务模型（工单 / 队列）

- 翻译统一「工单 ID + 轮询」异步模型（OpenAPI 同构）。
- `queue.NewDirect`（jobs 表）+ worker 池（`WORKER_CONCURRENCY` 默认 4）。
- 启动回收中断任务（`RecoverStale`）、断点续传（`BootResume`）、卡死巡检（>20min 无进展重排）。
- 队列租约心跳（`Heartbeat` 每 60s 续租，防双跑双扣费）。

---

## 四、前端架构

### 4.1 技术要点

- React 18 + LangCross 自研组件库（src/ui/langcross）+ TypeScript，Vite 5 构建，SPA 单页应用。
- React Router v7（声明式路由：`/admin` 后台、`/tickets` 工单、`/editor` 对照编辑、`/pricing` 公开定价页、`/` 工作台）。
- Provider 嵌套：`AuthProvider → ChatProvider → AdminProvider → BrandingProvider`。
- 状态管理：`stores/auth.ts`、`stores/admin.ts`、`hooks/useChat.ts`。
- i18n：`dicts.zh.ts` / `dicts.en.ts` 键对等（Vitest 35 用例校验中英键对等，含面板词条文件）。

### 4.2 后台一级菜单与面板归属（2026-09-15 精简：8 项一级）

| 一级菜单 | 组件 | 权限 | 子 tab / 收纳面板 |
|------|------|------|------|
| 总览 Overview | panels_a | L2 | 仪表盘 + 用量明细（UsageP，含 CSV 导出） |
| 工单 Tickets | panels_d | L2 | 工单审批 |
| 个人中心 Personal | PersonalCenterP | L2 | 邀请好友 + 任务中心 |
| 知识库 Kb | panels_d | L2 | 包/条目/安全句/数据采集 + 数据源（DataSourcesP） |
| 组织与成员 Org | OrgP | L3 | 组织/部门树｜邀请码｜成员账户 |
| 计费与套餐 Billing | BillingHubP | L3 | 套餐与订单（PlansP）｜租户管理（TenantsP，L4）｜成本对账（ReconcileP，L4） |
| 外部调用 External | ExternalCallsP | L3 | 开放 API｜回调通知｜官方 SDK（SdkP 三端安装指引） |
| 系统与运维 System | SystemSettingsP | L4 | 注册触达｜邮件模板｜流程引擎｜协议签署｜审计日志｜运营策略｜模型与供应商｜品牌与页脚 |

### 4.3 移动端自适应

- `styles/mobile.css`（叠加 theme.css）：断点 900px/640px。
- 后台侧边栏转抽屉（汉堡 + 遮罩）、聊天输入行换行、表格横向滚动、登录卡全宽。
- `App.tsx` 主内容区 `app-main` 加 `min-width:0`（根治宽表格横向溢出）。
- `e2e/mobile_uat.spec.ts`（390×844 手机视口）自动化核对。

---

## 五、安全设计

| 领域 | 措施 |
|------|------|
| 认证 | JWT（`JWT_SECRET` 随机强密钥）、Bearer Token |
| 密钥管理 | API Key 哈希存储、AES-GCM 加密（`enc:v1:`）、仅创建时明文展示一次 |
| 支付回调 | 所有渠道统一 `X-Admin-Token` 校验（Caddy 反代注入），明文回调拒绝 |
| 限流 | 登录/注册限流落库（`rate_limits` 表）、IP 频控、Caddy 枚举封禁 |
| CORS | 默认拒绝反射（deny-by-default），`/openapi/*` 无条件放行 |
| 进程沙箱 | `deploy/systemd/prod.conf`：User=translator + ProtectSystem + 密钥 EnvironmentFile(0600) |
| 租户隔离 | JWT 租户强制、跨租户查询 404 不泄露存在性、KB 组织链隔离 |
| 数据安全 | GDPR 擦除（12 表 + 工单磁盘产物清理）、API 版本化、错误码体系 |
| 输入校验 | KB 导入白名单、列名拼接防标识符注入、body 上限 |

---

## 六、部署架构

### 6.1 生产形态

- 单个 Go 二进制 + `frontend-react/dist` 静态资源（`-frontend` 参数托管 SPA）。
- Caddy 反向代理 → 127.0.0.1:8787，自动 HTTPS + on-demand TLS（`*.lexicorn.cn` 通配符 A 记录）。
- PostgreSQL 16 + pgvector（同机自建）。
- systemd `translator.service` + 六个 drop-in（concurrency/hardening/mail/mem/pdffont/secrets）。

### 6.2 部署流程

1. 交叉编译 Linux 二进制（`GOOS=linux GOARCH=amd64 CGO_ENABLED=0`）。
2. scp 二进制 + `frontend-react/dist` 打包上传。
3. 备份旧版 → 替换 bin/web → `systemctl restart translator`。
4. `deploy/deploy_check.sh` 一键验收（8~9 项）。

### 6.3 1G 内存红线调优

| 项 | 值 |
|----|-----|
| GOMEMLIMIT | 850MiB |
| MemoryMax | 1150M |
| worker | 4 |
| LLM 并发 | Chat 2 + 1 保留槽、Embed 6 |
| FILEPROC 并发 | 1（串行，`FILEPROC_RLIMIT_AS_MB` 硬上限） |
| PDF 拦截 | >40MB 或 >120 页前置拒绝 |

### 6.4 数据库切流（SQLite → PostgreSQL）

- `--init-db` 建 schema → `migrate-sqlite-to-pg` 迁数据 → `backfill-embeddings` 回填向量。
- `/status` 返回 `dialect:"postgres"` 验证。
- 回滚：删 `DB_DRIVER`/`DB_DSN` 两行重启。

---

## 七、开放 API 契约

```
POST /openapi/v1/tasks              创建（JSON=文本 / multipart=文件 ≤20个·40MB）
GET  /openapi/v1/tasks/status?id=   轮询（文本 15s / 文件 60s）
GET  /openapi/v1/tasks/download     文件产物下载（file_id 可选，缺省 zip）
POST /openapi/v1/translate          同步短文（≤5000 字符、≤5 语言）
GET  /openapi/v1/balance            积分余额与 ≈句数（出参零 token 裸值）
GET  /openapi/v1/kb/stats           KB 统计
GET  /openapi/v1/billing/usage      用量
POST /openapi/v1/apikey/rotate      密钥轮换
```

错误码（snake_case）：`invalid_api_key` / `key_quota_exceeded` / `insufficient_balance` / `rate_limited` / `daily_quota_exceeded` 等。

---

## 八、运营策略引擎（因子模型）

解析顺序：`代码默认 → 存量散键 → 平台 ops_policy → 活跃时间窗`。

| 因子域 | 关键项 |
|--------|--------|
| package | trial_tokens / trial_days / monthly_reset |
| invite | enabled / reward_tokens / paid_reward_tokens / max_daily |
| task | enabled（任务中心总开关） |
| registration | enabled / ip 频控 / email_verify |
| limits | max_qps / max_concurrent / 默认日上限 |
| mode | fast/pro：charge / markup / limit_chars / enabled |
| payment | mode / auto_charge |
| content | file_max_mb |

- 权限：`POST /api/admin/ops/policy/save` 仅超管（L4）可写；租户只读。
- 实现：`internal/ops/policy.go`（OperationsPolicy patch + EffectivePolicy + Merge/ActiveWindows）。
- 存储：`system_config.ops_policy`（JSON，含 `promo_windows`）；`ops_resets` 记录套餐重置计数。

---

## 九、测试体系

| 层 | 工具 | 范围 |
|----|------|------|
| 后端单测 | `go test ./...` | store/api/engine/crawler/culture/ops 全包 |
| 前端类型 | `tsc --noEmit` | TS 类型检查 |
| 前端单测 | Vitest | i18n 中英键对等（35 用例） |
| E2E | Playwright | 后台/工作台/移动端（15 用例） |
| UAT 脚本 | scripts/uat/api_uat.sh | 后端 API A+B 阶段 67/67 |
| 部署验收 | deploy/deploy_check.sh | 探活/metrics/回调/双桶 8~9 项 |

---

## 十、关键环境变量

| 变量 | 说明 |
|------|------|
| `JWT_SECRET` | JWT 签名密钥（生产必须随机） |
| `ADMIN_INIT_PASSWORD` | 初始化超管密码 |
| `ADMIN_TOKEN` | 管理接口凭证（支付回调校验） |
| `SILICONFLOW_API_KEY` / `ONLINE_API_KEY` | 翻译 / Embedding Key（后台可配置覆盖） |
| `MAIL_ENABLED` / `SMTP_*` | 真实邮件发信 |
| `DB_DRIVER` / `DB_DSN` | 数据库后端（sqlite/postgres） |
| `REDIS_ADDR` / `REDIS_PASSWORD` | Redis 分布式能力 |
| `GOMEMLIMIT` | Go 内存上限（生产 850MiB） |
| `WORKER_CONCURRENCY` | 工单 worker 并发（默认 4） |
| `TRUST_PROXY_XFF` | 反代取真实 IP（Caddy 部署必须开启） |
| `REQUIRE_PROD_SECRETS` | 生产密钥强校验 |

---

## 十一、当前生产状态

| 项 | 值 |
|----|-----|
| 生产域名 | https://langcross.lexicorn.cn（2026-08-24 起） |
| 演示域名 | https://rox-test.lexicorn.cn |
| 服务器 | 43.108.86.140（阿里云，Ubuntu 24.04） |
| 数据库 | PostgreSQL 16 + pgvector 0.6.0 |
| 服务 | translator.service（Go 单二进制，/status 返回 v3, ok:true） |
| 计费 | 积分口径对外（内部 token 实时归集记账）+ 运营策略引擎（超管可配） |
| 前端 | React + LangCross 自研组件库（Vue/TDesign 旧栈逐步退场中） |

---

## 十二、已知技术债与约束

- **单实例架构红线**：进程内存态组件（resetCodes / 验证码 / 限流器 / 采集调度 goroutine 等）多实例部署会失效，扩容前需改造为 Redis/DB 共享存储。
- **主站 systemd 已迁移 prod.conf 沙箱**（2026-09-14：User=translator、密钥收敛 `/etc/translator/secrets.env`）；演示站 translator-demo 仍为历史 root 部署，待停机窗口统一。
- **超大 PDF**（>40MB/>120 页）不做翻译，提示转 docx。
- **微信/支付宝官方自动回调**与 **Grafana/Loki 观测栈**为接口位已预留/代码已就绪，待接入环境即启用。当前口径：收款=静态收款码+人工核对单（SOP 化）；异地备份=本机 cron 每日拉取+校验（`deploy/fetch_backups_local.sh`）；监控告警=Prometheus+Alertmanager+node_exporter 三件套（S9，告警经 `/api/alerts/alertmanager` 收口入站+机器人双推）；人机验证=主站注册已启用 Cloudflare Turnstile。
- **业务语言**：主站/演示站已适配移动端；本地化部署的 AI 调用统一走平台网关。

---

*本文依据代码与既有文档综合整理，与实现代码同步演进。*