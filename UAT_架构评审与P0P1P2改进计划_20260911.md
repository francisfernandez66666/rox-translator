# 能言 SaaS · 全场景全流程 UAT 报告 + 架构评审 + P0/P1/P2 改进计划

> 生成时间：2026-09-11  
> 审计方式：全量阅读 backend-go / frontend-react / scripts 源码 + 实时运行 `run_uat.sh` + `go test ./...` + `tsc --noEmit` + `vitest run`  
> 范围：后端↔后端 / 前端↔前端 / 前端↔后端 + 功能与交易专项

---

## 一、UAT 测试结果（2026-09-11 实时运行）

| 测试层 | 结果 | 详情 |
|--------|------|------|
| Go 后端全包单测 `go test -count=1 ./...` | **18 包全 ok** | 62+ 测试文件，0 失败 |
| 后端 API 全链路 `api_uat.sh`（A1-A16 + B1-B8） | **67/67 通过** | 认证/计费/翻译/支付/裂变/OpenAPI/权限/文件/运营策略 |
| 前端类型检查 `tsc --noEmit` | **✓ 无错误** | |
| 前端单元测试 `vitest run` | **24/24 通过** | i18n zh/en 键对等性 |
| 像素级 E2E `playwright test e2e/` | **16 通过 / 3 失败** | 失败项：`tenant_label.spec.ts`（测试夹具缺口，非产品缺陷） |

### 失败项根因

3 个失败全在 `e2e/tenant_label.spec.ts`：测试需要 `corp1`/`person1` 用户，但 `api_uat.sh` 只创建了 `uatuser_a`/`uatuser_b`。**非产品缺陷，测试夹具缺口。**

---

## 二、架构评审（基于源码，非旧文档）

### 2.1 真实分层拓扑

```
cmd/server/main.go
  ├── config.Default() + config.LoadConfigFromJSON()
  ├── redis.Init()                    ← REDIS_ADDR 非空启用分布式，空则降级进程内
  ├── kb.Open(dbPath)                 ← 打开术语库（SQLite 或 PostgreSQL，由 DB_DRIVER 切换）
  ├── tenant.NewStore()               ← 租户存储（共享同一 DB 连接）
  ├── store.New(db.RawDB())           ← 平台存储（users/tickets/billing/audit/kb/packages）
  ├── engine.NewEngine(cfg, db, kbIndex, ts)  ← 翻译引擎
  ├── evals.New(cfg, llmClient, st)   ← 评估器
  ├── api.NewServer(cfg, eng, db, dist, st, ts) ← HTTP 服务
  │     ├── billing.NewService(st)    ← 计费（QPS/并发/余额/每日上限）
  │     ├── queue.NewDirect(st.DB())  ← 异步队列（jobs 表）
  │     ├── service.NewTicketService() ← 工单服务（worker 池）
  │     ├── TicketSvc.BootResume()    ← 断点续传
  │     ├── TicketSvc.StartStallSweep() ← 卡死巡检
  │     ├── TicketSvc.StartWorkers(n)  ← worker 池启动
  │     ├── routes()                  ← 150+ 路由注册
  │     └── startWatchdog()           ← 后台巡检
  └── billing.InitGlobalSink()       ← 实时计量批量落库
```

### 2.2 已确认的基础设施状态

| 组件 | 状态 | 源码证据 |
|------|------|----------|
| **PostgreSQL** | ✅ 已切换 | `DB_DRIVER=postgres` 支持，`config.go:348-353` 强校验 DSN 非空，`cutover-to-pg.sh` 一键切流 |
| **pgvector** | ✅ 已集成 | `kb/db.go:1045-1090` `formatVector`/`UpsertEmbedding`/`VectorSearch`，`rebuild-kb-index` 回填向量 |
| **Redis** | ✅ 可选启用 | `redis.Init(cfg.RedisAddr)`，启用后分布式锁/信号量/跨实例通知，空则降级进程内 |
| **结构化日志** | ✅ 已实施 | `main.go:56` `slog.SetDefault(observability.NewLogger())`，JSON 格式+trace_id |
| **pprof** | ✅ 已启用 | `main.go:329-347` 绑定 `127.0.0.1:18787`，仅本机 |

### 2.3 文件处理管线（真实情况）

从源码看，**Python 子进程依赖比想象中小**：

| 格式 | 提取 | 写回 | Python 依赖 |
|------|------|------|-------------|
| **DOCX** | Go 原生 `archive/zip` + XML 解析 (`fileproc/docx.go`) | Go 原生 XML 替换 (`w:t` 级) | **无** |
| **PDF** | `pdftotext` CLI (poppler) 或 Go `ledongthuc/pdf` 回退 | **无原格式回写**，降级为 xlsx 对照表 | 仅 CLI 工具 |
| **PPTX** | Go 原生 `archive/zip` | Go 原生 XML 替换 | **无** |
| **XLSX** | Go 原生 | Go 原生 | **无** |
| **SRT/VTT/JSON/YAML** | Go 原生 | 以 xlsx 对照表交付 | **无** |
| **TMX** | Go 原生解析 | 导入 KB | **无** |

**Python 子进程仅用于**：
1. `docx_translate.py` — 早期遗留（当前 docx 已用纯 Go 实现，可能仍有部分路径依赖）
2. `pdfwrite.py` — PDF 有原格式回写需求时（当前 PDF 降级为 xlsx）
3. LibreOffice — 仅在需要将 docx 转 PDF 时调用

### 2.4 翻译引擎核心（engine.go, 2169 行）

```
TranslateOne(ctx, zhText, targetLangs, stage)
  ├──[1] 精确命中: FindExactScoped → 两段式（链内优先 → 跨部门共享包回退）
  ├──[2] CJK 标点无关: getCJKCache(scope) → 内存缓存（分片上限 128）
  ├──[3] 模糊子串: FuzzyHitsScoped → LIKE + 长度差 ≤30
  ├──[4] 语义检索: LLM.Embed → pgvector VectorSearch（优先） → npz 回退
  ├──[5] 例句池: chainHits(采用) + crossHits(仅参考)
  └──[6] 模型兜底: translateLangsConcurrent
        ├── resolveModel → ModelRoutes 按权重选主 + 降级链
        ├── breaker.IsOpen() → 熔断跳过（5 次失败 → 30min 冷却）
        ├── LLM.CallChat → chatSem 信号量控制
        │   └── OnUsage → billing.UsageSink.Record()（实时计费）
        ├── 429/网络错误 → 沿路由链降级重试
        ├── finish_reason=length → 翻倍 max_tokens 递归（≤2 次）
        └── 轮次化重试: 失败语言排尾, 每语言最多 3 次, 轮间 2s 退避
```

### 2.5 计费双桶模型（billing/）

```
billing.Service
  ├── TryQPS(tid)        ← QPS 窗口（Redis 秒窗 / 本地滑动窗口）
  ├── TryAcquire(tid)    ← 并发名额（Redis SETNX / 本地 channel）
  ├── CheckBalance(tid)  ← 余额检查（双桶：未过期台账 + 永久余额）
  ├── CheckDailyQuota()  ← 每日上限
  └── Meter()            ← 计量（内存累积 → 批量 flush → 事务落库）

UsageSink（全局单例）
  ├── Record(usageRecord) ← 追加 buf + 内存影子余额扣减
  ├── flush()             ← 每 2s 或 200 条触发，按租户分组批量落库
  └── shadow[tid] < 0 → Abort()（余额不足即时中止翻译）
```

### 2.6 容错机制汇总

| 机制 | 实现 | 配置 |
|------|------|------|
| 熔断器 | `engine.Breaker`，连续 5 次失败 → Open → 30min 冷却 → Half-Open | 可配 |
| 模型降级 | ModelRoutes 按权重降序逐一尝试 | admin 热更新 |
| Redis 降级 | Redis 不可用 → 自动降级进程内实现 | 无感切换 |
| 重试队列 | 失败语言排尾，每语言最多 3 次，轮间 2s | 可配 |
| 截断自修复 | finish_reason=length → 翻倍 max_tokens 递归 ≤2 次 | 固定 |
| 崩溃恢复 | 启动 RecoverStale 回收 + StallSweep 20min 巡检 + BootResume 断点续传 | 固定 |
| 优雅停机 | SIGTERM → Sink.Stop() → HTTP.Shutdown(10s) | 固定 |
| OOM 保护 | GOMEMLIMIT=650Mi + Linux oom_score_adj=1000 + 可选 RLIMIT_AS | 环境变量 |

### 2.7 安全边界

| 维度 | 实现 |
|------|------|
| 认证 | JWT Bearer + API Key (AES-GCM 加密) + 首登强制改密 |
| 租户隔离 | 普通用户强制 JWT tenant_id，超管 X-Tenant-ID 切换，未登录默认租户 1 |
| API Key 权限 | translate / kb / billing / all 四级隔离 |
| CORS | deny-by-default + 白名单，/openapi/* 无条件反射（CSRF 不成立） |
| 密钥安全 | `enc:v1:` 前缀密文落库，启动水合解密，REQUIRE_PROD_SECRETS 强校验 |
| 暴力破解 | loginLimit 登录限流 + regGuard 注册频率护栏 |
| 请求体限制 | JSON 5MB，multipart 由 handler 自控 |
| pprof | 仅 127.0.0.1:18787，外网不可达 |

### 2.8 API 端点清单（150+）

| 域 | 路由组 | 端点数 | 示例 |
|----|--------|--------|------|
| 健康/公开 | `/status`, `/api/health`, `/pricing`, `/docs/*` | 10+ | 健康检查、定价页、条款/SLA/隐私 |
| 认证 | `/api/auth/*` | 10 | 登录/注册/改密/忘记密码/邮箱验证码 |
| 翻译 | `/api/chat`, `/api/translate`, `/api/download/*` | 10+ | 对话/文件/SSE 流式/估价/KB 识别 |
| 租户管理 | `/api/tenant/*` | 10+ | CRUD/状态/导出/擦除/品牌/邀请 |
| 工单 | `/api/tickets/*`, `/api/approve/*` | 10+ | CRUD/运行/下载/取消/审批 |
| 后台 KB | `/api/admin/kb-*`, `/api/admin/brand-terms` | 20+ | 包/条目/安全句/行业/品牌/采集/导入 |
| 后台模型 | `/api/admin/models/*`, `/api/admin/policy/*` | 8 | 模型路由/阶段模型/策略 |
| 计费 | `/api/billing/*`, `/api/pay/*`, `/api/package/*` | 20+ | 余额/用量/订单/发票/支付/套餐/订阅/升级 |
| OpenAPI | `/openapi/v1/*` | 8 | 异步任务/同步翻译/余额/KB/用量/Key轮换 |
| Webhook | `/api/webhooks/*` | 4 | CRUD/测试 |
| 任务中心 | `/api/admin/tasks/*`, `/api/me/tasks/*` | 5 | 管理/领取 |
| 系统 | `/api/system/*`, `/api/evals/*` | 5 | 健康/审计/告警/评估 |
| 组织 | `/api/org/*` | 8 | 部门 CRUD/用户/预算/限额 |
| 其他 | `/api/admin/users/*`, `/api/feedback/*`, `/api/notifications/*` | 15+ | 用户/反馈/通知 |

---

## 三、项目评价

| 维度 | 评分 | 说明 |
|------|------|------|
| **功能完备度** | ★★★★★ | 翻译/工单/知识库/计费/裂变/品牌/运营策略/行业包/OpenAPI/SDK/Office 插件全覆盖 |
| **工程质量** | ★★★★☆ | 交易链路零差错、安全基线到位、测试资产厚实（130+ 断言）；扣分项：前端代码组织 |
| **架构设计** | ★★★★☆ | 分层清晰、依赖单向、容错完备；已切 PG+pgvector+Redis 可选 |
| **商业化可行性** | ★★★★★ | 打印机+墨盒模型实证、邀请裂变全链路通过、毛利 66%、运营策略引擎即时生效 |
| **可维护性** | ★★★☆☆ | 前端大文件（panels_d 1315 行）、`any` 类型泛滥、无组件级单测、手搓路由 |
| **可扩展性** | ★★★★☆ | PG+Redis 已就绪，可多实例部署；剩余瓶颈：Python 文件处理、LLM 并发槽 |

---

## 四、P0 改进计划（必须做）

### P0-1 前端 Error Boundary + 安全加固

| 子任务 | 交付物 | 验收标准 | 工时 |
|--------|--------|----------|------|
| 全局 ErrorBoundary | `ErrorBoundary.tsx` 包裹 `App.tsx` 根 | 组件崩溃显示降级页+重试，不白屏 | 0.5 天 |
| Markdown XSS 修复 | `MessageBubble.tsx` 加固 escapeHtml 或换 react-markdown | 无 XSS 可利用路径 | 1 天 |
| Token 安全存储 | `api/core.ts` token 迁移到 httpOnly cookie 或 in-memory | XSS 无法窃取 token | 1 天 |
| React Router 迁移 | 替换 `usePath()` 手搓路由 | 懒加载、URL 参数、嵌套路由、前进后退恢复 | 3 天 |

### P0-2 真实翻译质量验证

| 子任务 | 交付物 | 验收标准 | 工时 |
|--------|--------|----------|------|
| 真实 API Key 端到端 | `scripts/uat/real_llm_uat.sh` | 真实 Key 跑全链路，人工评审译文 | 1 天 |
| 翻译质量评估集 | `evals/corpus.json` 50+ 条 | LLM-Judge ≥4/5、无术语违规 | 1 天 |
| 品牌术语保护验证 | 34 语言断言 | 极石→ROX 零音译 | 0.5 天 |

### P0-3 支付 SDK 授权准备（待授权，仅接口预留）

| 子任务 | 交付物 | 验收标准 | 工时 |
|--------|--------|----------|------|
| 微信支付接口预留 | `payment/wechat.go` 骨架+配置 | 授权后填商户号即启用 | 1 天 |
| 支付宝接口预留 | `payment/alipay.go` 骨架+配置 | 同上 | 1 天 |
| 支付渠道工厂 | `payment/factory.go` | `pay_mode` 配置切换 mock/wechat/alipay | 0.5 天 |
| 签名验证测试 | mock 模拟真实签名流程 | 签名→验签→回调→确认全链路 | 0.5 天 |

### P0-4 前端 Bundle 代码分割

| 子任务 | 交付物 | 验收标准 | 工时 |
|--------|--------|----------|------|
| React.lazy 路由级懒加载 | `App.tsx` 按路由拆 chunk | 首屏 JS < 300KB | 1.5 天 |
| Vite chunk 拆分 | `vite.config.ts` 手动 chunk | 消除 >500KB 告警 | 0.5 天 |

---

## 五、P1 改进计划（应该做）

### 5.1 Python 文件处理管线增强

| 子任务 | 交付物 | 验收标准 | 工时 |
|--------|--------|----------|------|
| docx_translate.py 评估 | 确认是否仍需 Python 路径 | 如纯 Go 已覆盖则移除 Python 依赖 | 0.5 天 |
| PDF 有格式回写 | 评估 LibreOffice 容器化可行性 | PDF→PDF 回写能力（非降级 xlsx） | 1 天 |
| 子进程闸并发提升 | `FILEPROC_MAX_CONCURRENT` 默认调为 2-4 | 大文件翻译排队时间降 50% | 0.5 天 |
| Python 进程健康检查 | 子进程僵死自动重启 | 不阻塞翻译队列 | 1 天 |

### 5.2 翻译引擎增强

| 编号 | 项目 | 预估 | 关键验收 |
|------|------|------|----------|
| P1-E1 | Hedged Requests 竞速路由 | 1 周 | P99 延迟降 30% |
| P1-E2 | 成本/延迟感知动态路由 | 1 周 | 成本最优路由生效率 >90% |
| P1-E3 | 文件翻译流式进度 | 1.5 周 | 前端实时渲染进度 |
| P1-E4 | 术语强制模式 | 1 周 | 术语不合规拦截率 100% |

### 5.3 知识库完善

| 编号 | 项目 | 预估 | 关键验收 |
|------|------|------|----------|
| P1-K1 | 包级权限矩阵 | 1 周 | 读/写/管理三级权限 |
| P1-K2 | TM 自动化审核 | 1.5 周 | 预筛通过率 >80% |
| P1-K3 | 大文件导入断点续传 | 1 周 | 100MB+ 可断点续传 |

### 5.4 计费深化（支付 SDK 授权后）

| 编号 | 项目 | 预估 | 关键验收 |
|------|------|------|----------|
| P1-B1 | 配额预留/预授权 | 1 周 | 并发不超卖 |
| P1-B2 | 订阅自动续费/升降级 | 1.5 周 | 续费成功率 >99% |
| P1-B3 | 发票自动开具/红冲 | 1 周 | 开票成功率 >98% |
| P1-B4 | 供应商对账/成本分析 | 1 周 | 月度对账自动化 |

### 5.5 组织与权限

| 编号 | 项目 | 预估 | 关键验收 |
|------|------|------|----------|
| P1-O1 | SCIM 2.0 组织同步 | 1.5 周 | AD/LDAP 实时增量同步 |
| P1-O2 | 品牌白标部署 | 1 周 | 自定义域名 CNAME 验证 |

### 5.6 OpenAPI 与集成

| 编号 | 项目 | 预估 | 关键验收 |
|------|------|------|----------|
| P1-A1 | Webhook 重试/死信/签名 SDK | 1 周 | 指数退避、死信可视 |
| P1-A2 | OpenAPI Spec 自动生成 + SDK 流水线 | 1 周 | Spec 与实现 100% 一致 |

### 5.7 运维与可观测

| 编号 | 项目 | 预估 | 关键验收 |
|------|------|------|----------|
| P1-R1 | 统一错误码体系 | 0.5 周 | 全接口标准错误码 |
| P1-R2 | TraceID 透传 | 1 周 | 故障定位 <10min |
| P1-R3 | SLO/SLI + Burn Rate 告警 | 1 周 | 告警噪音 <20% |
| P1-R4 | 自动化备份/恢复演练 | 1 周 | RPO <1h、RTO <4h |

---

## 六、P2 改进计划（可以做）

### 6.1 前端现代化

| 编号 | 项目 | 预估 | 备注 |
|------|------|------|------|
| P2-F1 | Zustand 状态统一 | 1 周 | 迁移 auth/chat/branding/admin |
| P2-F2 | 暗黑模式 + WCAG 2.1 AA | 1.5 周 | 对比度、键盘导航、ARIA |
| P2-F3 | Lingui 国际化工程化 | 1 周 | CI 阻断缺失键 |
| P2-F4 | Service Worker 离线 | 1.5 周 | Workbox/IndexedDB |
| P2-F5 | 组件级单元测试 | 2 周 | MessageBubble/LangMultiSelect/Login |
| P2-F6 | 后台面板拆分 | 1 周 | panels_b/c/d → 独立文件 |
| P2-F7 | SSE 解析去重 | 0.5 周 | 提取公共函数 |
| P2-F8 | i18n 硬编码补全 | 1 周 | selfservice/ChatWindow 走 i18n |

### 6.2 插件生态

| 编号 | 项目 | 预估 | 备注 |
|------|------|------|------|
| P2-P1 | VS Code/Cursor 插件 | 1 周 | 侧边栏翻译 |
| P2-P2 | Trados/memoQ/QT 插件 | 2 周 | 双向同步 TM |
| P2-P3 | Chrome/Edge 应用商店上架 | 0.5 周 | 合规隐私政策 |

### 6.3 智能化

| 编号 | 项目 | 预估 | 备注 |
|------|------|------|------|
| P2-I1 | Constrained Decoding 术语强制 | 2 周 | 术语零违规 |
| P2-I2 | 多级分销/裂变漏斗分析 | 1.5 周 | 分销树、归因 |
| P2-I3 | 容量规划/自动扩缩容 | 1.5 周 | 基于队列积压决策 |
| P2-I4 | 混沌工程/自愈 | 1 周 | 月度演练 |

---

## 七、架构演进路线图

```
Week 1-4:   ████ P0-1 ErrorBoundary+路由+安全加固 (关键路径)
            ████ P0-2 真实翻译质量验证 (并行)
            ████ P0-3 支付SDK接口预留 (并行)
            ████ P0-4 Bundle代码分割 (并行)
Week 5-8:   ████ P1 Python文件管线增强
            ████ P1 引擎增强 (竞速/成本/进度/术语强制)
            ████ P1 KB完善 (权限/TM审核/断点续传)
Week 9-12:  ████ P1 计费深化 (支付授权后)
            ████ P1 组织权限 (SCIM/白标)
            ████ P1 OpenAPI/SDK 流水线
            ████ P1 运维可观测 (错误码/日志/SLO/备份)
Week 13-16: ████ P2 前端现代化 + 插件生态 + 智能化
```

---

## 八、商业性价值评估

1. **收入链路无技术断点**：双桶台账 + 幂等支付 + 发票/退款闭环 + 余额硬闸，67 项 API 断言全部通过
2. **财务可审计**：`usage_ledger` 请求级落账（provider/model/tokens/cost），订单↔余额↔台账分毫不差
3. **增长引擎跑通**：邀请裂变全链路验证（注册→付费→奖励→去重），oneid 双唯一防刷
4. **数据飞轮运转**：行业/语言文化包自动采集→待审→热加载，免费部署→语料回流→术语库迭代
5. **定价弹性**：全部计费因子后台可调，运营策略引擎即时生效无需发版
6. **安全卖点**：统一网关出站 + 租户严格隔离 + 最小权限 Key + 脱敏回流
7. **差异化壁垒**：知识库四层继承链（部门→企业→行业→通用）+ 实时计费批量 Sink（余额耗尽即停）+ 9 步自动流水线

> **结论**：基于本次源码级全量审计（非旧文档），**PostgreSQL + pgvector 已切流完成**，Python 子进程依赖范围远小于预期（docx/pptx/xlsx 已用纯 Go 实现），架构实际成熟度高于此前评估。当前系统达到可继续商业化运营的质量基线，建议按 P0→P1→P2 分三期演进。
