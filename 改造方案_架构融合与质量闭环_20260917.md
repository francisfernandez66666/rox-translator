# 改造方案：架构融合与质量闭环（2026-09-17）

> 依据：2026-09-17 全量 UAT 实测（PG 方言）+ 全码阅读核实。本文档先落方案，未动代码。
> UAT 结果基线：go test -race 22 包全绿；API A/B 67/0；T 专项 308/0；Playwright 34/1 红；ai-assist 27/0；双实例 e2e 8/0；vitest 79/79。

---

## 一、缺陷与缺失内容核实结论（真实代码证据）

以下每条均经真实代码核实，标注证据位置；与此前口头结论的差异已修正。

### 1.1 唯一 UAT 红点：`_tmp_admin.spec.ts` 自身缺陷（非产品缺陷）——属实

| 证据 | 说明 |
|---|---|
| `frontend-react/e2e/_tmp_admin.spec.ts:4` | 写死 `page.goto('https://langcross.lexicorn.cn/assist-api/assist/admin')`，指向**生产站**而非 `BASE_URL`；同目录其余 34 条用例均打本地 UAT 服务 |
| `frontend-react/e2e/_tmp_admin.spec.ts:12` | 依赖 `process.env.ASSIST_TOK`，UAT 环境无此变量 → 「Token 已验证」场景在闸门中永远红 |
| 实测 | run_uat.log：首轮 `1 failed / 34 passed`，`--last-failed` 复跑仍红，闸门 exit=1 |

**危害**：发布闸门长期带一条「已知红」运行，钝化对真回归的敏感度（run_uat.sh 的 flaky 复跑机制对它无效，因为它不是 flaky 而是必红）。
**处置**：删除，或移至 `e2e/manual/` 并加 `test.skip(!process.env.ASSIST_TOK, ...)` 守卫；生产探针职责改由 `deploy/smoke_kb_scope.sh` 类冒烟脚本承担。**P0，5 分钟工作量。**

### 1.2 ai-assist 与主后端技术栈割裂——属实（但数据隔离本身是合理设计）

| 证据 | 说明 |
|---|---|
| `ai-assist/go.mod` vs `backend-go/go.mod` | 两个独立 Go module（`module ai-assist` / `module translator`），各自编译、各自 go.sum |
| `deploy/systemd/ai-assist.service` | 独立 systemd 单元、独立端口（8790）、独立 `ASSIST_ADMIN_TOKEN`（默认 `change-me-please`，`ai-assist/internal/config/config.go:57`） |
| `frontend-react/src/components/admin/AssistP.tsx:12,32` | 管理台 = iframe 托管 `/assist/admin` + **要求用户手工粘贴 Token**（提示语原文「首次使用需在右上角填入管理 Token」），与主后台 SSO 体验断层 |
| `.github/workflows/ci.yml` | **零处提及 assist**：ai-assist 的 27 个单测函数与 assist_uat.sh 均不进 CI，只靠手工跑 |
| 日志/观测 | ai-assist 用标准 `log`（engine.go:94 等），未接主服务 `internal/observability` 的 slog JSON + trace_id 口径 |

**需要澄清的一点**：ai-assist 用独立 SQLite 存会话/知识库，与主业务库分离，这个**隔离本身合理**（访客免登录服务不应碰业务库）；割裂的是「仓库/构建/观测/鉴权/CI」五件事，不是数据库。

### 1.3 store 单包过大——属实

| 证据 | 说明 |
|---|---|
| `backend-go/internal/store/` | **74 个文件、16713 行**（含测试） |
| 最大文件 | billing.go 2201 行、kbpackages.go 1371 行、kbscrape.go 1105 行、store.go 899 行 |
| 影响面 | `internal/api/` 90+ 文件全部直接引用 `store.Store` 的方法面；`store.Store` 为上帝对象，任何域改动都在同一包内叠加 |

### 1.4 LLM 供应商路由测试缺口——部分属实（修正此前结论）

**已有测试**（此前说「路由没有测试」不准确，特此修正）：
- `engine/h7_routes_test.go`：动态权重冷启动等价、错误率惩罚、延迟惩罚、token 记账，4 个用例
- `engine/hedge_test.go`：慢主路对冲、快主路跳过对冲、双败、流式不适用，4 个用例
- `llm/collector_test.go`：usage 并发收集 3 个用例；`llm/h2_constraints_test.go`：约束解码注入

**真实缺口**（grep `CallChatFallback` 在全部 `*_test.go` 零命中）：
1. `llm/client.go:479 CallChatFallback`——429 → 等待 → `HunyuanFallbackModel` 降级重试的核心链路**无任何单测**
2. `llm/client.go:449 CallChat` / `doChat`——超时、非 200、坏 JSON、finish_reason 分支无单测
3. `engine/engine.go:1131 resolveStageModel`——stage_models 密文解密（enc:v1:）、密钥继承全局默认、旧键回退，无单测
4. `engine/engine.go:1163 pickPrimaryRoute` 静态分支——全 0 权重取首个、单路由、DynamicRouting 关闭路径无直接单测（h7 只测动态打分函数）

### 1.5 evals 无不合格处置——属实

| 证据 | 说明 |
|---|---|
| `orchestrator/workflow.go:309` | `SaveRecord(..., "passed")` —— 状态**硬编码 passed**，与得分无关 |
| `orchestrator/workflow.go:360` | 校对评估同样恒为 `"passed"` |
| 全仓 grep | 无任何消费方读取 `EvalScores` 做阈值判断/打标/通知；评估分数落库后即「死数据」 |
| 前台 | `EvalScores` 未透出到工单详情（见 1.6），用户与运营都看不到 |

### 1.6 用户侧 QA 报告透出缺失——属实（修正表述）

- **已有**：`api/tickets.go:688-728` —— QA 报告写进**下载的 xlsx 对照表**「QA」列（error ✖ / warning ⚠），交付物里是有的。
- **缺失**：前端全仓 grep `qa_report|QAReport` **零命中**——工单详情页/列表页 UI 完全不展示质检结果；工单详情 API（非下载接口）也未返回 `qa_report` 字段。付费用户在界面里感知不到质检投入，仅下载后才能看到。

### 1.7 CI 缺口——属实

`.github/workflows/ci.yml` 已覆盖：go vet + go test -race、SQLite 全量 run_uat、PG 矩阵 uat-pg job（含 Playwright）。**未覆盖**：ai-assist 单测、assist_uat.sh、multi_instance_e2e.sh、sdk 三端测试、vitest（前端单测也只在本地跑）。

---

## 二、五项改造方案

### 改造 1：ai-assist 与主后端技术栈融合

**目标**：收敛为单仓库单 module、统一观测与 CI，保留数据隔离；管理台 Token 免手填。

**分两阶段（推荐 A 先行）**：

**A 阶段（低成本收敛，建议本次做）**
1. **并入主 module**：`ai-assist/*` 迁为 `backend-go/cmd/assist-server/` + `backend-go/internal/assist/{engine,llm,store,api}`；删除独立 go.mod（依赖本就只有 modernc.org/sqlite，与主库同版本 v1.56.0，零冲突）。`ai-assist/seed`、`web` 随迁，路径改 embed 或相对可执行目录。
2. **统一观测**：`log.Printf` → 主仓 `internal/observability` slog JSON 带 trace_id。
3. **管理台 Token 贯通**：assist-server 启动时从主库 `system_config`（新增 key `assist_admin_token`，密文 enc:v1: 存储，复用 `store.EncryptSecret`）读取，env `ASSIST_ADMIN_TOKEN` 优先级更高（保底）；前端 `AssistP.tsx` 改为经主后台 `/api/admin/assist/token` 下发（仅超管可见），iframe 免手填。
4. **部署收敛**：`deploy/systemd/ai-assist.service` 改指向同一二进制 `ExecStart=.../translator-server -mode assist`（或独立子命令 `translator-assist`），单构建产物两形态。
5. **CI 补齐**：ci.yml 增加 ai-assist `go test` job + `assist_uat.sh`（自起服务，无外部依赖，CI 可跑）。

**B 阶段（后续可选）**
6. 健康接入 S9：`/health` 进 Alertmanager 黑盒探测；assist SQLite 加定时备份（复用 fetch_backups_local.sh 模式）。

**改动清单**：ai-assist 4 个包整体搬迁（路径级改动，逻辑不动）、main 装配、AssistP.tsx、admin_token 配置链、systemd 单元、ci.yml。
**风险**：路径迁移波及 assist_uat.sh 与部署指南引用路径——脚本同步改。数据隔离保持：assist 仍用独立 SQLite 文件，不并入业务库。
**验收**：assist_uat.sh 27 断言全绿；CI 出现 assist job 并绿；AssistP 打开不再要求手填 Token。

### 改造 2：store 拆分（务实路线，避免大爆炸）

**目标**：控制复杂度增长，而非一次性物理拆包（74 文件被 api 层 90+ 文件引用，直接拆子包改动面 >3000 行且无行为收益）。

**三步走**：
1. **第一步·文件归组（本次）**：store 包内按域建子目录语义分组（Go 同包不同目录不可行 → 改为文件前缀归组现状已有，补一份 `store/README.md` 域索引：billing*/usdt*/kbpackages*/referral*/webhooks*/tickets* …），并把 `store.go`（899 行）里的通用工具（EncryptSecret/DecryptSecret/动态 Row 等）下沉到 `internal/db` 或 `internal/secret`，store 反向引用——切断 store 最容易被新代码继续塞杂物的入口。
2. **第二步·冻结规则**：新增约定——billing.go（2201 行）与 kbpackages.go（1371 行）**只减不增**，新方法按域新建文件；`AGENTS.md`/README 记录。
3. **第三步·域拆包（评估后执行）**：选引用面最小的两个域先行试点拆独立包：`webhooks`（547 行）与 `referral`（549 行）→ `internal/biz/webhooks`、`internal/biz/referral`，store 保留薄委托；跑通后再评估 billing 拆分（需同步 api/billing_api.go、admin_billing.go 引用面）。

**验收**：go test -race 全绿；UAT A/B + T 专项全绿（对账类断言最敏感，天然验证拆分无行为回归）。

### 改造 3：LLM 供应商路由补测试

**新增测试文件与用例清单**：
1. `internal/llm/client_test.go`（新）：
   - `TestCallChatFallback429`：httptest 首响应 429 → 断言等待后用 `HunyuanFallbackModel` 重试成功
   - `TestCallChatFallbackExhausted`：持续 429 → 返回错误、onRateLimited 回调触发次数
   - `TestCallChatBadJSON/Non200/Timeout`：坏 JSON、500、超时三分支错误路径
   - `TestStreamChatFirstChunk`：SSE 首块解析（现有覆盖盲区）
2. `internal/engine/stagemodel_test.go`（扩充既有文件）：
   - `TestResolveStageModelEncrypted`：stage_models 带 enc:v1: 密钥解密命中
   - `TestResolveStageModelInheritKey`：阶段模型空密钥 → 继承全局
   - `TestResolveStageModelFallback`：未配置/坏 JSON → ok=false 回退 resolveModel
3. `internal/engine/pickroute_test.go`（新）：
   - `TestPickPrimaryStaticAllZeroWeights`、`TestPickPrimarySingleRoute`、`TestPickPrimaryDynamicToggle`（DynamicRouting 开关两路径）

**验收**：`go test -race ./internal/llm/... ./internal/engine/...` 全绿；用例均不依赖外网（httptest mock）。

### 改造 4：evals 不合格处置（total<60 打标 + 运营提醒）

**设计**（落点 `orchestrator/workflow.go` runEvalsInitial / runEvalsReview）：
1. **阈值配置**：system_config 新增 `evals_fail_threshold`（默认 60，0=关闭处置仅记录）、`evals_alert_enabled`（默认 1）。
2. **打标**：评估 total < 阈值时：
   - `p.EvalScores[lc]` 照记；新增 `p.QualityFlaggedLangs []string`（payload 序列化持久化）
   - 工单表新增可空列 `quality_flagged`（0/1，迁移幂等，复用 Ensure 列模式）；工单列表/详情 API 透出该字段
   - SaveRecord 的 status 参数改为按阈值传 `"passed"` / `"failed"`（修复 1.5 的硬编码）
3. **运营提醒**：复用既有通知渠道四路（企业微信/钉钉/Slack/Teams，`internal/notify` 既有能力）+ S9 告警中心 `/api/alerts`，发送「工单 #ID 语言 lc 评估 total=X 低于阈值」限频告警（同工单同语言 1 次，payload 记 notified 标记防重）。
4. **不做自动打回重译**（与 gate 硬闸职责区分：gate 是确定性规则可自动重译；Judge 主观分重译易震荡烧钱）——只打标 + 提醒，人工决策。
5. **前端**：工单列表对 `quality_flagged=1` 显示「质检存疑」徽标；详情页展示各语言 EvalScores 五维分。
6. **测试**：单测（阈值边界 59/60/61、开关关闭、限频幂等）+ UAT T46 断言（mock LLM 让 Judge 返回低分 → 断言工单被打标 + 告警 payload）。

### 改造 5：用户侧 QA 报告透出

**设计**（现状：仅 xlsx 下载有 QA 列，界面零透出）：
1. **API**：工单详情接口（tickets.go 详情 handler）在 FinalResult 解析时一并返回 `qa_report`（qa.Report 已 JSON 序列化存 payload，零 schema 改动）与改造 4 的 `eval_scores`。
2. **前端 TicketsPage.tsx**：
   - 列表行：`qa_report.errors>0` → 红点「N 项错误」；仅 warnings → 黄点「N 项提示」；`quality_flagged` → 「质检存疑」徽标
   - 详情抽屉：「质检报告」区块——Pass/Errors/Warnings 汇总行 + Issues 明细表（语言/规则/级别/说明，复用 qa 后端字段 lang/rule/level/detail）+ 各语言评估分（如有）
3. **文案口径**：error 级文案强调「已自动重译后仍存在，建议人工复核」，避免用户误读为未处理（gate 已重译 ≤8 次的语境要讲清楚）。
4. **测试**：vitest 组件测试（徽标渲染三分支）+ e2e 断言（mock LLM 制造 warning 工单 → 详情页可见质检区块）。

---

## 三、执行顺序与闸门

| 序 | 项 | 量级 | 优先级 |
|---|---|---|---|
| 0 | 修复 `_tmp_admin.spec.ts`（1.1） | 5 分钟 | **P0 立即** |
| 1 | 改造 3（LLM 路由补测试） | 半天 | P1（为 1/4/5 铺底） |
| 2 | 改造 4（evals 处置）+ 改造 5（QA 透出）——两者共享工单标记/前端展示层，同批做 | 1-2 天 | P1 |
| 3 | 改造 1A（ai-assist 收敛） | 1 天 | P1 |
| 4 | 改造 2 第一步+第二步（store 归组/冻结/下沉） | 半天 | P2 |
| 5 | 改造 2 第三步（webhooks/referral 试点拆包）与 1B（S9/备份） | 评估后排期 | P2 |

**每项完成闸门**（沿用现有发布闸门口径）：
`go test -race ./...` 全绿 → `bash scripts/uat/run_uat.sh`（PG 主矩阵，修复 1.1 后应为 35/35 全绿零豁免）→ `assist_uat.sh` 27 断言 → `multi_instance_e2e.sh` 8 断言 → 新增单测/UAT 断言随项附上。

## 四、遗留观察项（不在本次范围，记录备查）

- ai-assist 管理页 `web/admin.html` 为单文件内联 JS（600+ 行），融合 A 阶段不动它，B 阶段可考虑 React 化并入主前端（目前 iframe 方案可用性尚可）
- `internal/openapi`、`internal/observability`、`internal/infra/ratelimit` 无测试文件（UAT 兜底覆盖），随改造 3 之后再补
- CI 的 PG 矩阵仍标注依赖 self-hosted runner（ci.yml:7 注释），已部分落地 uat-pg job，待确认 runner 常态化
