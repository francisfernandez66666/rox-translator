# UAT 全场景全流程分支像素级测试报告（2026-09-08）

> 测试方式：`scripts/uat/run_uat.sh` 一键从零拉起 mock LLM + 全新库后端 + 全量断言 + 前端 E2E，叠加 `scripts/uat/api_uat_txn.sh` 交易专项深度断言与 `deploy/deploy_check.sh` 部署验收，另以本地 PostgreSQL 16 + pgvector 0.8 补跑 PG 方言集成测试。
> 范围：后端↔后端 / 前端↔前端 / 前端↔后端 + 功能与交易专项，桌面 + 移动端像素级。

---

## 一、测试结果总览

| 层级 | 套件 | 结果 | 说明 |
|---|---|---|---|
| 后端↔后端·单元/集成 | `go test -count=1 ./...` | **18 包全 ok / 0 失败** | api/store/engine/kb/payment/orchestrator 等 62 个测试文件 |
| 后端↔后端·PG 方言 | `PG_TEST_DSN` 指向本地 PG16+pgvector | **db/store/kb 全过** | 真实生产方言迁移+向量检索（预建 KB 模块懒建表后） |
| 后端↔后端·API 全链路 | `scripts/uat/api_uat.sh`（A1-A16 计 49 + B1-B8 计 18） | **67/67** | 认证/计费/翻译/支付/裂变/OpenAPI/权限/文件 + 运营策略计费因子 + P1 成员加入/P2 平台包/P3 落库 Flush |
| 后端↔后端·交易专项 | `scripts/uat/api_uat_txn.sh`（T1-T14 计 66） | **66/66** | 新增脚本：线下订单/人工确认/发票一致性/付费奖励/异步任务/改密/硬闸/任务中心/KB/Webhook/告警/品牌种入/回调安全闸/权限边界 |
| 后端↔后端·部署验收 | `deploy/deploy_check.sh` | **8/8** | 探活/metrics 收敛/CORS/支付回调三闸/注册双桶/注销/划译 |
| 前端↔前端 | `tsc --noEmit` + `vitest run` + `vite build` | **✓ / 24/24 / ✓** | i18n 中英对等性等；构建仅 bundle 体积告警 |
| 前端↔前端·扩展 | `extension/` JS/JSON 语法校验 | **✓** | content.js/popup.js/manifest.json 合法 |
| 前端↔后端 | Playwright E2E（真实后端+mock LLM，桌面+移动视口） | **16/16** | smoke3 + pixel_uat8 + pixel_quality2 + mobile_uat3 |
| SDK | Python/JS 实链回环 + TS 源码 | **✓** | 文本/文件异步任务、权限矩阵、轮换、坏钥 |
| 服务健康 | UAT 全期 server.log | **0 ERROR / 0 panic** | 全链测试期间无崩溃 |
| 交易台账 | 订单↔余额↔台账精确对账 | **分毫不差** | 见 §四 |

**合计断言：后端 67 + 交易专项 66 + 部署 8 + 前端 E2E 16 + 前端单测 24 + Go 全包 = 全部通过。**

---

## 二、前端↔前端（像素级）

| 项 | 结果 |
|---|---|
| 类型检查 `tsc --noEmit` | ✓ 无错误（含本次新增/修改的 e2e） |
| 单测 `vitest run` | 24/24（i18n zh/en 键对等性 + 相关逻辑） |
| `vite build` | ✓ 3978 模块，产出 index.html + css(306KB) + js(1261KB，gzip 371KB)（⚠️ 未做代码分割，有 chunk>500KB 告警） |
| 像素断言（Playwright） | 关键页元素可见 + 全站无横向溢出（`scrollWidth<=clientWidth+2`，桌面+390×844 移动端）+ 无 console.error/pageerror/请求失败 |
| 截图落盘 | `frontend-react/artifacts/*.png`×17（1280×720 桌面 / 390×844 移动），供人工复核 |
| 路由分支 | `/` `/billing` `/invites` `/packages` `/my` `/tickets` `/editor` `/admin` `/pricing` `/docs/*` 全部断言通过；企业用户 `/invites` 正确隐藏邀请（产品意图锁定，新增断言） |

> 注：本报告执行终端无法直接读图，像素校验以自动化断言（元素/溢出/控制台/截图）为准，raw 图留 `artifacts/` 供人工目检。

---

## 三、前端↔后端（浏览器真实联调）

- 登录页渲染、工作台「输入→发送→译文回流」真实全链路（真实后端+mock LLM，60s 内完成）。
- 自服务四页（余额/邀请/套餐/账号）以企业+个人双账号校验；后台超管各面板（概览/模型/知识库/工单/系统/个人）；公开定价/协议。
- 移动端：后台侧栏 900px 断点转抽屉（汉堡唤起/滑入/遮罩点关/菜单点关）、聊天输入行换行、全站 8 页+公开页无横向溢出。
- 运行时健康：全部关键页 0 控制台错误、0 页面异常、0 请求失败。

---

## 四、功能与交易专项（重点）

### 4.1 交易链路（全部实测通过）

| 场景 | 断言 | 结果 |
|---|---|---|
| 线下订单：admin 代建单→pending→确认→paid→入账 | 余额精确 +50000，状态机 pending→paid | ✓ |
| 退款：paid→refunded→余额精确回滚复原 | 余额精确回 0；重复退款被拒 | ✓ |
| 静态码人工确认 | manual 下单→返回二维码→「我已付费」manual_confirm=1→超管待审列表→确认到账 | ✓ |
| 发票金额一致性（B1 定价回填） | 发票 amount_money == 订单 amount_money（12345.6==12345.6） | ✓ |
| 支付幂等（同单二次 simulate / confirm） | 余额不变；付费奖励不重复发放 | ✓ |
| 受邀人首笔付费→邀请者奖励 | 邀请者永久余额 +500000，`referral_rewards`(type=paid_perm) 落账，按被邀人去重 | ✓ |
| OpenAPI 异步任务（文本+文件） | create→poll→completed；文本任务 download 返回 no_result（正确契约） | ✓ |
| 支付回调三道闸 | 缺凭证 403 / 错误凭证 403 / 金额不符 400 | ✓ |
| 旧密码失效/新密码登录/改回恢复 | 全部通过；忘记密码防枚举；错误重置码拒绝 | ✓ |
| 任务中心 | 超管建任务(enable)→用户领取→重复领取拒（已领取） | ✓ |
| API Key 权限矩阵 | translate/billing/all 端点隔离正确；默认最小权限；坏钥拒绝；轮换成功 | ✓ |

### 4.2 台账对账（财务口径复核）

租户2 全期事务精确对账：
```
balance_accounts = 1,264,688 = 1,000,000(A9充值) + 8,888(T2) + 123,456(T3) + 8,888(T2二次) + 123,456(T3二次)
订阅 75,000,000 → quota_grants(kind=plan)，不入永久余额
退款订单 → 余额精确归零复原；pending 订单不入账
trail 300,000 → 已核销 3,210 与 usage_ledger 口径一致
```
→ 无重复入账、无丢失交易，幂等与原子性在真实请求下成立。

### 4.3 运营策略计费因子引擎（api_uat B 阶段 18 断言）

策略读写、fast 免费（余额不变+台账留痕）、limit_chars 闸门（fast 拒/pro 过）、平台包订阅回落、套餐月度重置+二次限额、邀请奖励按因子 +900000、P1 企业成员加入（同租户/role=user/邀请码置用）、时间窗覆盖免费——全部符合预期。

---

## 五、发现的测试资产问题（均已就地修复，非产品缺陷）

| # | 位置 | 问题 | 处理 |
|---|---|---|---|
| 1 | `frontend-react/e2e/pixel_uat.spec.ts` P3 | 用企业账号 `uatuser_a` 断言 `/invites` 邀请内容，与产品「企业用户彻底隐藏邀请页」（提交 e09636a 意图）冲突导致 1 项失败 | 改为个人账号测邀请页渲染 + **新增「企业隐藏邀请页」锁定断言**；全套 16/16 |
| 2 | `deploy/deploy_check.sh` [5/7] | 注册请求缺 `agreed:true`（产品要求先同意协议），注册被正确拒绝→误报「注册未返回 api_key」 | 补 `agreed:true`，8/8 |
| 3 | `backend-go/internal/kb/pg_vector_test.go` | PG 向量测试假设 `tm_segments` 已建，但该表由 KB 模块延迟创建（`ensureTables`），直跑报 `relation "tm_segments" does not exist`（测试夹具缺口，非运行时缺陷） | UAT 环境预建表后 5 项 vector/PG 用例全过；建议测试自建表 |
| 4 | 新增 `scripts/uat/api_uat_txn.sh` | 交易专项首版因 bash 嵌套引号拆分 + 契约字段名差异产生假性失败 | 已修正：curl 先存变量再断言、字段按实际契约（invoice.amount_money / task_id 顶层 / status 查询参数 id 等）；最终 66/66 |

---

## 六、项目评价

### 强项（工程成熟度高）

1. **交易与计费核心链路的严谨性**：双桶台账（发放额度+永久余额）、原子扣减绝不扣负、订单确认幂等、退款回滚+订阅身份撤销、发票金额单一事实源——在 130+ 真实 API 断言与精确对账下全部成立。
2. **自动化测试资产厚实**：62 个 Go 测试文件、API 断言套件（A/B/T 三阶段）、Playwright 像素级（桌面+移动）、i18n 对等性、SDK 权限矩阵，`run_uat.sh` 一键可重复执行。
3. **前后端契约对齐**：159 个前端 API 调用与后端路由经程序化核对一致；本次新增交易专项进一步验证全部路由。
4. **安全基线到位**：API Key 三绑定+最小权限、支付回调凭证/渠道/金额三闸、跨租户 404 不泄露、密钥脱敏、运营策略超管专属。
5. **运营策略引擎**：计费因子流程引擎+活跃时间窗平台级可视化配置，即时生效，是商业化灵活性的架构保障。
6. **工程规范**：全量中文注释、文档体系完备（PROGRESS/部署指南/商业化白皮书）、移动端自适应+全站防溢出。

### 弱项与风险（非阻塞）

| 项 | 说明 |
|---|---|
| 前端 bundle 无代码分割 | 1.26MB js（gzip 371KB），构建告警，见《性能优化方案-不换库.md》 |
| 真实翻译质量未验证 | mock LLM 译文为占位文本，需真实 Key 隔离复测（外部依赖） |
| 真实支付渠道未实链 | 微信/支付宝需商户号，接口位已预留，当前 static_qr 全链已通 |
| 测试夹具缺口 | pg_vector_test 需预建 `tm_segments`；`TestVectorSearchRealEmbeddingOnPG` 依赖外部 Embedding API Key |
| billing_enforced 默认关闭 | 生产灰度策略（只记不停）；运营稳定后建议切 true（产品决策待办） |

---

## 七、商业性价值评估

1. **商业模式可行性获技术实证**：打印机+墨盒模型（引擎/术语库免费+token 预充值唯一收入流）对应的一整套计费能力——双桶台账、幂等支付、发票/退款闭环、余额硬闸、邀请裂变——已全部实测通过，收入链路没有技术断点。
2. **财务可审计性**：`usage_ledger` 请求级落账（provider/model/tokens/cost），订单金额与发票金额单源一致，台账与余额对账分毫不差——企业客户与上游账单可逐位核对，是 B 端信任的关键证据。
3. **增长引擎可跑通**：邀请裂变「体验叠加+付费奖励」经真实注册→付费→奖励全链路验证，奖励去重/防刷（oneid 双唯一、邮箱撞库拒绝）生效，具备低成本获客的现实基础。
4. **数据飞轮在运转**：行业/语言文化包自动采集→待审→自动审批→热加载进知识库，UAT 验证通过；免费部署→语料回流→术语库迭代的闭环有架构支撑。
5. **防御性与合规性**：统一网关出站（客户零第三方凭据）、租户严格隔离、最小权限 Key、脱敏回流——是面向企业采购的安全卖点，亦是"按量计费不泄露密钥"商业承诺的实现基础。
6. **定价弹性**：上游成本×3（约 66% 毛利）且全部计费因子后台可调，运营策略引擎使活动/时间窗/免费策略可即时投放，商业化运营无需发版。

> 结论：**全场景全流程分支像素级 UAT 全部通过**（后端 API 67/67、交易专项 66/66、部署 8/8、前端 E2E 16/16、单测 24/24、Go 全包、台账零差错），项目达到可继续商业化运营的质量基线；本报告同期修复合规的 1 处过时 E2E 断言与 1 处过时部署检查脚本，并沉淀了可复用的交易专项 UAT 脚本。