# 改造方案：工单文件翻译双模式——还原文件模式 / 纯文案模式（anydoc 接入与纯文案兜底交付）

> 日期：2026-09-13　范围：backend-go（fileproc/engine/service/api）+ frontend-react + SDK 参数透传 + 部署依赖
> 状态：方案稿（评审通过后分期实施）
> 前端用户可见选择：**工单文件翻译 → 交付方式：〔还原文件模式〕/〔纯文案模式〕**

## 〇、背景与目标

### 现状痛点

1. **回写失败 = 整单失败**：2026-09-09 产品决策"有回写能力的格式写回失败不降级 xlsx，重试 3 次仍失败置工单失败"（`engine/file.go` L621-681）。翻译结果明明已经完成（这是花 token 换来的核心资产），却因版式还原这一"增值步骤"失败而颗粒无收——用户拿到的是"翻译失败"，实际是"还原失败"。
2. **格式还原不满意时无退路**：PDF 还原受 pdf2docx/LibreOffice 链限制（字体兜底替换、表格近似）、扫描版不译；用户若只需要译文文案，被迫下载 docx/pdf 再手动复制。
3. **老格式与 ODF 系全不支持**：doc/xls/ppt（OLE 老格式）、odt/ods/odp、rtf、epub、docm/xlsm/xlsb 均在白名单外（`api/tickets.go` L225-237）。
4. **PDF 提取链路重**：pdf2docx 转缓存 DOCX 是 Python 子进程，是整条管线最慢最脆的一环（600s 墙钟预算都花在这）。

### anydoc 是什么（firecrawl/anydoc，v0.2.4，MIT，Rust，21k★）

- 单向转换：**doc/docm、ppt 全家、xls/xlsx/xlsb、odt/ods/odp、rtf、epub、csv、pdf → 高质量 GitHub Flavored Markdown**。所有格式汇入同一文档模型（标题锚点、粗斜体、合并单元格表格、脚注尾注、公式→LaTeX、列表编号保序），由单一序列化器输出，14 格式质量与速度全面优于 libreoffice/pandoc/markitdown/docling/mammoth（中位 4.4ms/文档）。
- 按文件**内容魔数**探测格式（伪扩展名文件也能正确识别），错误分类清晰（`Encrypted`/`NeedsOcr`/`Malformed`/`ResourceLimit` 等）。
- 绑定：Rust/Node/Python/WASM，**无 Go 绑定**→ 走 Python binding（`pip install firecrawl-anydoc`，导入名 `anydoc`，转换释放 GIL），融入现有 venv + `subprocess.go`/`procgate.go` 治理体系。
- **两个关键边界（必须向用户传达）**：
  1. **不能反向**（MD→原格式）——反向靠 pandoc/LibreOffice 重建，版式与字体必丢，保真度严格劣于我们现有的"原生格式内原位 XML 回写"（D21 run 级 rPr 保留），因此**主管线不动**；
  2. **本地不做 OCR**——扫描版 PDF 抛 `NeedsOcr`；hosted OCR 走 Firecrawl 商业云（文档出境，私有化部署不可接受，**不启用**）。

### 目标

把文件工单升级为两种交付模式，对应前端一个二段选择：

| 模式 | 管线 | 交付物 | 定位 |
|---|---|---|---|
| **还原文件模式**（默认，= 现状增强） | 现有原生提取→翻译→原位回写 | 原格式译文文件 **+ 新增兜底**：`译文.md` 纯文案附加产物；回写失败不再整单失败，降级纯文案交付 | 版式/字体保真优先 |
| **纯文案模式**（新增，anydoc 驱动） | anydoc→结构化 MD→MD 管线提取→翻译→MD 对齐写回 | `译文.md`（可选双语对照 xlsx） | 只译内容，不还原格式；快、稳、格式覆盖最广 |

设计原则：
1. **翻译是资产，回写是增值**——翻译成功即工单成功；
2. 纯文案模式**独立分段**，不与还原模式共用提取键（pptx 按句末标点分组 vs MD 按行/单元格，两套粒度不可互套），重复翻译成本靠 KB/TM 命中兜底；
3. 还原模式既有行为对老工单、OpenAPI 任务完全向后兼容（缺省 = restore）。

## 一、总体架构

```
POST /api/tickets/create-file  (multipart，新增 delivery=restore|text 字段)
   │  白名单按 delivery 分档：restore=现有 12 种；text=现有 + doc/xls/ppt/odt/ods/odp/rtf/epub/docm/xlsm/xlsb
   │  （建议）anydoc 魔数探测校验扩展名与内容一致性，不符拒建单
   ▼
tickets 表新增列 delivery('restore' 默认 / 'text')
   ▼
worker runFileTicket (service/ticket.go L473)
   ├─ delivery=restore ──► Engine.HandleFile（现状主管线，不动）
   │       变化A：完成后额外产出 {base}_{lang}.md 纯文案附加产物（落 translated/ + 产物登记）
   │       变化B：写回重试 3 次仍失败 → 不再置失败：以纯文案 .md 为交付物，
   │              file_writeback 轨迹置 warning「还原失败，已降级纯文案交付」+ 站内通知
   └─ delivery=text ─────► Engine.HandleFileText（新增，复用第2步翻译段全部能力）
           ① fileproc.AnydocToMarkdown：python -m anydoc 转换脚本（新增 anydoc_md.py）
              错误映射：Encrypted→文件加密/带密码；NeedsOcr→扫描版无法提取文案；
                        Malformed/ResourceLimit→文件结构损坏
           ② ExtractTexts(.md) 结构感知提取（text_formats.go L96-123，现成）
           ③ 翻译：KB 直配 / BatchTranslate / pro 校对 / 硬闸补漏 / 品牌归一化 / 段级闸门（全复用）
           ④ ApplyAlignedText(".md")（现成）→ 交付 {base}_{lang}.md；勾选对照表另产 WriteComparisonXlsx
```

## 二、后端设计（backend-go）

### 2.1 数据模型（store）

| 变更 | 位置 | 说明 |
|---|---|---|
| `Ticket` 增字段 `Delivery string`（`json:"delivery"`，`'restore'`/`'text'`，空=restore） | `store/tickets.go` L17-38 结构体 + `GetTicket`/`GetTicketGlobal`/`GetTicketByNo` 三处 SELECT + `CreateTicket`/`UpdateTicket` | 列迁移进 `store.go` L600+ 既有 ALTER 序列：`tickets.delivery TEXT DEFAULT 'restore'` |
| `TicketFile` 同样携带 delivery（继承工单值，无需新列——建单时写死即可）| `store/ticketfiles.go` | 多文件工单模式统一 |
| 历史工单零迁移回填：读空值归一化为 restore | — | 向后兼容 |

### 2.2 API（api/tickets.go、api_openapi_tasks.go）

1. **create-file**（`handleTicketCreateFile` L200）：
   - 读 `r.FormValue("delivery")`，归一化（`normalizeTaskDelivery`：仅 accept `text`，其余含空=`restore`）；
   - 白名单分两档：
     - restore：`.docx .xlsx .pptx .pdf .txt .csv .srt .vtt .md .json .yaml .yml`（现状 L225-237 不变）；
     - text：上述 + `.doc .xls .ppt .odt .ods .odp .rtf .epub .docm .pptm .xlsm .xlsb`；
   - PDF 前置拦截 `checkPdfLimits` 仅对 restore 模式生效（text 模式无 pdf2docx/LibreOffice 链，页数上限可放宽到 400 页，仍受 40MB 总限）；
   - （P3 增强）保存文件后调用 anydoc `format_from_bytes` 做魔数校验，伪扩展名（.docx 实为 .doc）直接 400 提示改用纯文案模式。
2. **download**（`handleTicketDownload` L503）：
   - 新增 query 参数 `fmt=text`：对已完成工单**动态拼装**纯文案并流式下载为 `{ticket_no}_译文.md`（复用文本工单动态生成 xlsx 对照表的既有套路 L610-674，零落盘）。数据源：`final_result` 中"键→译文"映射 + `texts` 顺序（需要 2.4 把顺序键写入 payload）；
   - restore 模式多语言 zip 中追加各语言 `.md` 兜底产物（2.4 产物）。
3. **detail**：`ticket.files[]` 增加 `text_path` 字段指向兜底 .md，前端据此显示次级下载按钮。
4. **OpenAPI 任务**（`api_openapi_tasks.go` multipart L230）：透传 `delivery` 字段；白名单同步分档；状态响应回显 delivery。SDK `createFileTask(options)` 增 `delivery?: 'restore'|'text'`（sdk/typescript，仅参数透传+文档，P3）。

### 2.3 引擎：新增纯文案管线（engine）

新文件 `engine/file_text.go`：

- `HandleFileText(ctx, filePath, options, prog) *FileTranslateResult`——从 `HandleFile`（file.go L202-722）抽取"第2步翻译"段为共享内部函数（语言解析、fast/pro、embedding 预取、KB 直配、BatchTranslate、硬闸补漏、漏译率 50% 闸、品牌归一化、applySegmentGates 全部原样复用），入口差异只在第1步提取、出口差异只在第3步交付：
  - 第1步：`fileproc.AnydocToMarkdown(ctx, path)` → md 字符串 → 落临时 `{base}.anydoc.md` → `ExtractTexts` 走 .md 分支；
  - 第3步：`ApplyAlignedText(".md", tmpMd, outPath, tr)` → `{base}_{lang}.md`；不再写回任何 office 格式；
  - 进度阶梯：`第1步/2 理解文档(anydoc)` → `file_extract`；第2步同现状；第3步 `file_writeback` 语义改为"组装文案产物"（成功几乎必然，失败仅剩写盘 IO）。
- txt/csv/md/json/yaml/srt/vtt 输入在 text 模式下**跳过 anydoc**，直接走各自现有提取器（本就是文本，省一次子进程）；其中 srt/vtt/json/yaml 交付物从"唯一 xlsx 对照表"升级为"译文.md + 对照 xlsx 可选"。
- `FileTranslateResult` 增字段 `TextDeliverables []string` 与 `Degraded bool`（还原失败降级标记）。

新文件 `fileproc/anydoc.go` + `fileproc/anydoc_md.py`：

- Go 侧仿 `runDocxScript`（pdfwrite.go L157-199）：`runAnydocScript(ctx, args...)` → `runSubprocess`（subprocess.go L92，600s 上限对 ms 级任务绰绰有余，可收紧到 60s）→ 走 `procgate.go` 并发闸（`FILEPROC_MAX_CONCURRENT`）；
- Python 侧 `anydoc_md.py`：读文件/入参 → `anydoc.to_markdown(path)` → stdout 输出 MD；异常按类型打印 `ERR:<variant>` 单行供 Go 映射错误话术；
- `healthcheck.go` 启动探测增 `python -c "import anydoc"`，不可用时能力降级：create-file 拒收 text 模式新格式（老 12 种格式 text 模式仍可走 Go 原生提取器，不受影响）。

### 2.4 还原模式两处变化（层次1）

1. **附加纯文案产物**：`runFileTicket` 单文件与多文件两分支在 `HandleFile` 成功后，用 `res.Data.TotalTexts` 序键 + `res.Data.Translations`（lang→键→译文）拼装各语言 `{base}_{lang}.md`（顺序=提取顺序；表格单元格间以 ` | ` 连接、段落间空行；未译出段用原文，与回写侧行为一致）。新函数 `fileproc.WriteTranslationMd(outPath, texts, tr)`（放 text_formats.go）。产物 `RegisterArtifact` 登记、计入 detail.files；多语言时并入既有 zip。
   - 增强（P3）：docx 提取器（fileproc/docx.go）为段落键附带结构元数据（`pStyle` 标题层级、表格归属），拼装 MD 时还原 `#`/`##` 与表格分组——不依赖 anydoc 也让兜底文案"像模像样"。
2. **回写失败降级**（engine/file.go L633-681 重写）：重试 3 次仍失败时不再 `return Error`，改为：
   - 该语言交付物 = 2.4-1 的纯文案 .md；
   - `FileTranslateResult.Degraded=true`，reply 追加 "⚠️ {lang} 版式还原失败，已降级为纯文案交付"；
   - `runFileTicket` 落 `file_writeback=warning` 轨迹 + `CreateNotification`（复用漏翻可见性通道 file.go L690-699 的模式）；
   - 仅当**连 MD 都写不出**（磁盘 IO 级故障）才维持整单失败。

### 2.5 计费与配额

- 两模式 token 口径不变（按译文实际用量计费）；text 模式因跳过校对回写无节省可让——不动运营策略引擎因子；
- 余额预估 `estimateTicketTokens`：text 模式源字符估算改为"文件大小 ÷ 3"维持现状即可（MD 化后字符量同数量级）。

## 三、前端设计（frontend-react）

### 3.1 建单表单（TicketsPage.tsx，仅 `mode==='file'` 时显示）

在语言/质量模式行上方新增"交付方式"二段按钮（与文本/文件切换按钮同风格，`Button variant=base|outline`）：

- **📐 还原文件模式**（默认选中）：副文案 `maximally 保留原文件版式、字体与样式，交付原格式译文文件`；
- **📄 纯文案模式**：副文案 `只输出译文内容（Markdown 文档），速度更快、成功率高；额外支持 doc/xls/ppt/odt/rtf/epub 等格式；不还原原版式`。

联动逻辑：
- 切到纯文案模式：文件 accept 列表放宽（`TRANSLATE_FILE_ACCEPT` 增新格式，见 `src/api/translate.ts` validateTranslateFile L166-193 增加按模式分档参数）；已选中的不合规文件红标提示；
- 选择记忆存 `localStorage('ticket_delivery')`，缺省 restore；
- 提交：`ticketCreateFile(files, { ..., delivery })` → FormData 追加 `delivery` 字段（src/api/tickets.ts L129-141）。

### 3.2 工单列表与详情气泡

- 列表"文件工单"列加模式徽标：📐 / 📄（i18n key `tk.deliveryRestore` / `tk.deliveryText`，中英文文案入 `src/i18n/panels/tickets.ts`）；
- 详情下载区：
  - restore：主按钮=原格式产物；次按钮=`下载译文文案(.md)`（走 `download?id=&fmt=text` 或已生成的附加产物，优先后者）；
  - text：下载 = `{lang}.md`；勾选过对照表则另有 xlsx；
  - 降级工单（Degraded/warning 轨迹）：黄色提示条"版式还原失败，已交付纯文案"，附"重新发起还原模式工单"快捷入口（复制建单参数）。
- 进度阶梯：text 模式步骤文案适配（理解文档 20% → 初翻/校对 → 组装文案产物 80% → 完成）；`ticketProgressPct`（api/tickets.go L411）按 delivery 分支。

### 3.3 范围外

浏览器插件 / VSCode / Office 加载项本次不加双模式入口（均为同步短文，无工单形态）；OpenAPI 文档页补 delivery 参数说明。

## 四、部署依赖

- venv `pip install firecrawl-anydoc==0.2.4`（PyPI 轮子为预编译 Rust 扩展，无需 Rust 工具链）；`部署指南.md` 依赖清单与 `scripts`（如有装包脚本）同步；
- 无新增常驻服务、无新增网络出口（anydoc Rust 核心承诺零网络调用；`ocr=hosted` 一律不启用，代码层显式不传该参数）；
- 升级观察：anydoc 为快速迭代项目（2026-07 发布至今 16 版），requirements 锁版本，季度评估升级。

## 五、分期实施计划

### Phase 1：层次1 兜底（不依赖 anydoc，改动小，建议先行上线）

| # | 改动 | 文件 | 预估 |
|---|---|---|---|
| 1.1 | `WriteTranslationMd` + HandleFile 成功后附加各语言 .md 产物 + 归属登记 | engine/file.go、fileproc/text_formats.go、service/ticket.go | 0.5d |
| 1.2 | 回写失败→降级纯文案交付（warning 轨迹+通知，不整单失败） | engine/file.go L633-681、service/ticket.go | 0.5d |
| 1.3 | download `fmt=text` + detail.files `text_path` + 前端次级下载按钮/降级提示 | api/tickets.go、TicketsPage.tsx、api/tickets.ts、i18n | 1d |
| 1.4 | 单测+回归（重点：多语言 zip、老工单兼容、失败注入） | tests | 0.5d |

### Phase 2：纯文案模式（anydoc 接入）

| # | 改动 | 文件 | 预估 |
|---|---|---|---|
| 2.1 | `anydoc_md.py` + Go 封装 + healthcheck + 部署文档 | fileproc/ | 1d |
| 2.2 | tickets.delivery 列 + create-file 分档白名单 + OpenAPI 透传 | store/api | 1d |
| 2.3 | `HandleFileText`（抽取共享翻译段）+ runFileTicket 分支 | engine/、service/ | 1.5d |
| 2.4 | 前端交付方式选择器 + 分档 accept + 徽标 + i18n | TicketsPage.tsx 等 | 1d |
| 2.5 | PoC 语料：每种新格式×中英×fast/pro；扫描 PDF/加密文件错误话术验证 | UAT 用例 | 1d |

### Phase 3：增强（按需排期）

- docx 提取器结构元数据（pStyle→标题层级）回灌兜底 MD；MD 表格按单元格拆键；行内粗斜体标记透传与写回校验；soffice 归一化老格式→还原模式高保真路径（doc→docx→ApplyDocx）；anydoc 魔数校验拦截伪扩展名；SDK `delivery` 参数；文件工单接入在线审批（缺口9 单列方案）。

## 六、测试与验收（关键用例）

1. **向后兼容**：不传 delivery 的历史前端/开放 API 建单 → 行为与现在完全一致（restore、原白名单、原计费）；
2. **降级链路**：注入 LibreOffice 失败（停服务）→ docx/pdf 工单不再失败，completed + 纯文案产物 + warning 轨迹 + 通知；
3. **纯文案正确性**：anydoc 输出 MD 的表格行/围栏代码块经提取→翻译→`ApplyAlignedText` 后结构零破坏（代码块不进翻译键——现有 md 提取器 L96-123 已保证）；
4. **格式矩阵**：doc/xls/ppt/odt/rtf/epub 各 2 份真实文档 text 模式全流程；加密 docx/pdf → 明确"文件加密"失败话术；扫描 PDF → "扫描版无法提取文案，请转 Word 或用还原模式+人工"话术；
5. **性能**：text 模式 PDF 端到端较 restore 模式提速对比（预期跳过 pdf2docx+LibreOffice 后提取从分钟级→毫秒级）。

## 七、风险与决策记录

| 风险/决策 | 说明 | 对策 |
|---|---|---|
| LLM 破坏 MD 结构 | 表格竖线、列表编号、围栏标记被模型改写 | 沿用"剥结构→纯文本键→行对齐写回"协议；表格整体成行的键匹配失败时保留原文（不毁结构）；表格拆键列 P3 |
| anydoc 项目年轻（2026-07 起） | API/输出细节可能演进 | 锁版本 + 转换失败自动回退现有提取器（text 模式下老 12 格式本就不依赖 anydoc）|
| 分段不一致 | 纯文案与还原结果可能微异（键粒度不同导致上下文不同） | 明示为两种产品形态；TM/KB 保证句级一致性主体 |
| 扫描版 PDF | 两模式都无解（本地无 OCR，hosted 出境不启用） | 维持 image_heavy 前置提示；文案中说明 |
| 字体/版式预期管理 | "纯文案"名称本身完成预期（不承诺还原） | 前端副文案明确"不还原原版式"；还原模式话术不变 |

## 八、不做什么（边界）

- 不做 MD→docx/pdf 反向还原（pandoc/LibreOffice 重建必丢原版式与字体，保真度劣于现有原位回写，且引入"翻译完版式不对"的更高预期落空风险）；
- 不接入 Firecrawl hosted OCR（数据出境，与私有化承诺冲突）；
- 不替换任何现有格式的原位回写路径。
