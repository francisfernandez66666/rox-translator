# 核实与修复方案 · E2E 双红 / 验证闸门 / 发布缺口真实性核验（2026-09-15）

> 背景：全量 UAT 复跑（PG 主矩阵 + 重建 dist + 手工资金冒烟 + 独立浏览器会话定位）后，
> 对本轮发现的问题与既有文档声明做**逐条真实性核验**（以代码与实跑为准，文档仅参考），
> 并对上轮评审中两处**过头结论**做修正。每项附证据与可执行的修改方案。

---

## 一、实测基线（本轮全量重跑记录）

| 套件 | 结果 |
|---|---|
| `go test -race ./internal/...` | ✅ 22 包全绿，无数据竞争 |
| API 主链路 UAT（PG 方言） | ✅ 67/67 |
| 交易/功能专项 T 套件（PG 方言） | ✅ 255/255 |
| 前端 vitest | ✅ 63/63（含 i18n 中英 parity） |
| `tsc --noEmit` / `go vet ./...` | ✅ 零错误 |
| Playwright E2E（9 spec / 31 用例） | ❌ **29 过 / 2 败**（均在 `admin_tabs_lang.spec.ts`） |
| 手工资金闭环（订单→支付→到账→余额） | ✅ 通（100 积分→30000 token→¥8.97→mock 到账，价格与白皮书一致） |

---

## 二、真实性核验总表

### A. 属实项（本轮证实）

| # | 问题 | 证据 | 定级 |
|---|---|---|---|
| A1 | 一级菜单文案三处漂移：测试/README 说「组织与成员」，代码实际渲染「🏢 企业管理」 | `frontend-react/src/i18n/dicts.zh.ts:569`（menuOrg='🏢 企业管理'）vs `README.md` 管理后台条目 vs `e2e/admin_tabs_lang.spec.ts:29`；独立会话实测 MENU 输出=「企业管理」 | P2（文案）+ 流程（见 A4） |
| A2 | E2E「语言多选」用例与默认态冲突：聊天默认预选 `["en"]`，测试搜「英语」按 Enter 实际是 **toggle 取消选中**，断言 `selected==1` 必红 | `hooks/useChat.tsx:42`（loadLangs 兜底 `["en"]`）+ `components/LangMultiSelect.tsx:140-144`（onEnterKey→toggle）；实测：click ja 正常、默认 en 在场时 Enter「英语」→ selected=0/chips=0 | P2（测试缺陷；组件 Enter 语义与点击一致，非产品 bug） |
| A3 | `run_uat.sh` 发布闸门对前端 E2E **不设防**：Playwright 失败不影响退出码 | `scripts/uat/run_uat.sh:120-121`（结果仅 tee 进日志，exit 未采集）+ `:133`（仅校验 API_FAIL/TXN_FAIL） | **P1**（闸门失守） |
| A4 | `run_uat.sh:59` dist 存在即跳过构建 → E2E 可能对着旧包跑 | 本轮实测命中：dist 构建 20:29 < HEAD 源码提交 21:13（AdminDashboard.tsx 更新晚于 dist 44 分钟），首轮 E2E 结果部分失真 | **P1** |
| A5 | commit c5f3376 声明「Playwright 29 全绿」与事实不符：该提交**自带**的 3 条新用例中 2 条从未绿过（全量实为 31 用例 29/2）；09-14 的「27/27 全过」（`UAT_缺陷清单与处置记录_20260914.md:8`）是当时旧套件的真实记录 | 重建 dist 后复跑仍 2 failed；git show HEAD 新增 `admin_tabs_lang.spec.ts` | **P1**（验证纪律） |
| A6 | 微信/支付宝「在线支付」为预留位未实现：`CreateOrder` 不调任何渠道 HTTP API，返回拼串占位二维码 | `internal/payment/payment.go:155-166, 221-231`（「★ 真实接入点」注释）；全文件 `http.Post/http.NewRequest/client.Do` 计 0 次 | P1（功能未完成，但见 B1——文档已如实标注） |
| A7 | 无 CI：全部验证依赖本地手跑 | 仓库无 `.github/`，无任何 workflow | P1 |
| A8 | 邮件默认 NoopSender：不配 SMTP 时注册验证码只进日志，自助注册闭环断裂 | `internal/mail/mail.go:3-8` | P1（上线配置项，非代码缺陷） |

### B. 修正项（上轮评审结论过头，本轮以代码为准纠偏）

| # | 上轮结论 | 核实修正 | 证据 |
|---|---|---|---|
| B1 | 「在线支付是空壳——文档幻觉」 | **过头**。白皮书与配置清单一直如实标注：`static_qr 当前主通道`、`sdk 微信/支付宝=接口位已预留`、`pay_mode=mock 仍为默认`；前端收银台按 pay_mode 渲染，未向客户露假渠道。A6 仍是真实差距，但属「未完成的预留」而非「文档欺诈」 | `商业化白皮书-翻译助手.md:95-96`、`生产配置清单_20260904.md:48-52`、`PlansP.tsx:74` |
| B2 | 「JWT_SECRET/ADMIN_TOKEN 生产密钥面失控，强校验应默认开启」 | **过头**。systemd 模板已默认 `Environment=REQUIRE_PROD_SECRETS=1`，三密钥任一缺失即 FATAL 拒启；Caddy 模板为 `/api/pay/notify/*` 注入 `X-Admin-Token`，回调链路闭环。剩余风险仅在「不走模板的手动部署」，属运维 checklist 项 | `deploy/systemd/prod.conf:29-52`、`cmd/server/main.go:69-88`、`deploy/caddy/translator.conf:30-32` |
| B3 | 「协议文本缺法务审阅=缺码」 | 部分属实：注册必须勾选协议，且内置了中英文基线条款文本（非空壳）；「正式文本需法务复核」是**上线动作**而非代码缺失 | `api/public.go:257,374` |

---

## 三、修改方案（按优先级）

### P1-1 闸门补防（A3/A4）· `scripts/uat/run_uat.sh` · 约 15 分钟
```bash
# ① dist 构建：存在≠新鲜，改为「有源码比 dist 新就重建」（vite build 实测 ~10s）
if [ ! -f frontend-react/dist/index.html ] || [ -n "$(find frontend-react/src -newer frontend-react/dist/index.html -name '*.ts*' | head -1)" ]; then
  (cd frontend-react && npm run build) || { echo "前端构建失败"; exit 1; }
fi

# ② Playwright 结果进退出码（line 120 之后）
PW_EXIT=$?   # 需要 ( ... playwright ... ); PW_EXIT=$? 显式采集，tee 会吞管道码
[ "$PW_EXIT" -eq 0 ] || { echo "❌ 前端 E2E 失败，发布闸门拦截"; PW_FAIL=1; }
# ③ 汇总行追加 PW_FAIL 判定：
[ "${API_FAIL:-1}${TXN_FAIL:-1}${PW_FAIL:-0}" = "000" ] || exit 1
```
> 注：管道尾码需 `set -o pipefail`（脚本 18 行已有）+ `PIPESTATUS[0]` 采集，实现时用
> `(cd frontend-react && npx playwright test ...); PW_EXIT=${PIPESTATUS[0]}` 或落盘后再判。

### P1-2 修 E2E 双红（A1/A2）· 约 20 分钟
- **T1**：`dicts.zh.ts:569` `'admin.menuOrg': '🏢 企业管理'` → `'🏢 企业管理 · 组织与成员'`？——**不建议**。定唯一口径：产品意图（README 09-15 批次）为「组织与成员」，故
  - `dicts.zh.ts`：`'admin.menuOrg': '🏢 组织与成员'`；`dicts.en.ts:567`：`'Enterprise'` → `'Org & Members'`；
  - `admin.wsCompany/tagCompany`（工作台区块标签）保持「企业管理」不动，仅改菜单口径；
  - 同步核对 `i18n/parity.test.ts`（自动）与 `pixel_uat` 截图基线（如有按标签断言处）。
- **T3**：改测试不动产品（Enter=toggle 与面板点击语义一致）：
  - 用例改为搜「**日语**」（默认选中项是 en，避开 toggle-off 干扰）：`fill('英语')`→`fill('日语')`，
    并补一步 `await expect(page.getByTestId('lang-chips').locator('.tag-lang')).toHaveCount(2)`（默认 en + 新选 ja），
    或先点掉默认 chip 再断言 1。二选一，推荐前者（同时覆盖「已选≠1」的 chip 单展示语义）。

### P1-3 验证纪律（A5）
- 本文件落档后，commit message 中的「全绿」声明必须附**当轮套件清单**（用例数=通过+失败），禁写总量口径模糊的「29 全绿」；
- 闸门修复（P1-1）落地后，E2E 红将无法再被提交绕过。

### P1-4 最小 CI（A7）· 0.5 天
- 新增 `.github/workflows/ci.yml`：`go vet + go test -race ./internal/...`（SQLite 方言即可）→
  `npm run typecheck && npx vitest run`；PG 全矩阵（`run_uat.sh`）保留夜间/发布前手跑或 self-hosted runner，
  GitHub runner 无 pgvector 时以 `DB_DRIVER=sqlite` 进 PR 闸门 + 发布标签触发 PG 全量。

### P1-5 上线配置动作（A8 + B2 残余 + B3）
| 动作 | 验收标准 |
|---|---|
| `MAIL_ENABLED=1 + SMTP_*`（secrets.env） | 真实注册收到验证码邮件并完成验证（截图归档） |
| 按 `deploy/systemd/prod.conf` 部署（禁绕过模板起进程） | `/status` 出 `dialect=postgres`；启动日志无「随机密钥」告警 |
| 协议正式文本法务复核后替换后台内容 | 后台「协议签署」版本号更新 + 老用户重确认策略确认 |

### P2 遗留台账（不阻塞本周）
- 微信/支付宝真实渠道联调（商户号到位后，`payment.go` 两处「★ 真实接入点」补 HTTP 调用 + 回调验签实测）；
- 审计防篡改哈希链（《P0P2待办核实报告》P1-4 远期项，近期「写失败告警」已具备）；
- api 包巨型化拆分（server.go 242 路由 → 按域拆分注册），择机重构不夹带功能发布。

---

## 四、USDT 收款

见《改造方案_USDT收款接入·双轨人工到自动_20260915.md》（同日交付）。要点：M1 人工核销轨本周可交付（完全复用
static_qr 人工确认链路），M2 链上轮询自动到账；**合规红线**（境内不得开展虚拟货币支付业务）在方案 §六 单列，
上线范围限定海外站点/海外主体。


---

## 五、落地记录（2026-09-16 补记）

| 项 | 状态 | 备注 |
|---|---|---|
| P1-2a menuOrg 文案（zh/en） | ✅ | dicts.zh「🏢 组织与成员」/ dicts.en「Org & Members」，E2E T1 绿 |
| P1-2b T3 用例重写 | ✅ | 断言改「fill→selected=2/chips=2、trigger 无语言标签」，不再对默认 ["en"] 按 Enter |
| P1-1 run_uat.sh 闸门 | ✅ | dist 新鲜度重建 + PW_EXIT 进退出码 + 汇总行含 E2E 结果 |
| P1-4 CI | ✅ | .github/workflows/ci.yml（vet/test -race + typecheck/vitest） |
| 终验 | ✅ | PG 主矩阵：race 通过、API 67/67、T 284/284（含新增 T42）、Playwright 33/33，EXIT=0；SQLite 矩阵同绿 |
| 附带修复 | ✅ | 超管审计视图 JOIN 列歧义（store/audit.go `a.` 限定，T41 闸门抓到）；tdesign disabled Button 渲染为 div.t-is-disabled 的 E2E 定位口径 |
