# 容量预告与超卖预案（k6 实测定案，2026-09-14）

> 数据来源：主站 `POST /openapi/v1/translate`（同步短文 ≈50 汉字→英文，随机串绕 TM 缓存），k6 本地→`https://langcross.lexicorn.cn`，台阶各 75s（VU 档间冷却 6s）。
> 压测窗口：为放开租户闸临时设 `tenant_quota_1={qps:60,concurrent:20}`，**压测后已恢复默认 {qps:10,concurrent:3}**；压测 Key 已 disabled、临时发放的 120 万 token trial grant 已删除、服务器临时工具已清理。

## 一、实测台阶（单租户独占整机，无其他流量）

| 并发 VU | 请求数 | 成功率 | 限流数(rate_limited) | 系统错误 | P95 | P99 | 最大 |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 4 | 41 | 100% | 0 | 0 | 7.9s | 8.3s | 8.4s |
| 8 | 53 | 100% | 0 | 0 | 12.9s | 13.1s | 13.3s |
| 12 | 60 | 100% | 0 | 0 | 18.7s | 18.8s | 18.9s |
| 16 | 64 | 100% | 0 | 0 | 24.2s | 24.4s | 24.4s |
| 24（闸=20） | 205 | **31.2%** | 141 | 0 | 26.8s | 27.0s | 27.1s |

**形态解读**
1. **有效吞吐天花板 ≈0.85 req/s（≈50 req/min），LLM 上游约束**：goodput 在 VU≥8 后不再随并发增长（0.71→0.80→0.85），时延线性抬升=典型串行排队。单请求真实模型时延 ≈7–8s。
2. **过载是「温和拒绝」不是雪崩**：VU=24 超出租户并发闸（当时 20）后，多余请求立即被 `rate_limited` 挡回（HTTP 200 业务码），系统错误率恒 0，LLM 错误率 0，熔断未开。
3. **内存无虞**：30 分钟窗口宿主机可用内存最低 36%（红线 8%），监控（S9）覆盖此维度。

## 二、容量预告（对外/对内口径）

| 维度 | 数字 | 说明 |
|---|---|---|
| 同步短文翻译 | **同时在线交互 ≤4 人/租户**体验良好（P95≈8s）；≤8 可接受（P95≈13s） | 划词/对话框场景 |
| 平台整体同步吞吐 | ≈50 次/分钟（单实例） | LLM 供应商侧延迟主导，加机器不解决 |
| 长文/文件翻译 | **一律走异步任务队列**（OpenAPI tasks / 工单），不占同步预算 | 队列串行 worker，天然削峰 |
| 单租户默认闸 | QPS 10 / 并发 3（`tenant_quota_<tid>` 可调，重启回放） | 默认值即超卖保护：第 4 个并发起温和拒绝 |
| 客户承诺话术 | 「高峰期复杂文档可能需要排队，复杂大文件建议提交工单异步处理」 | 勿承诺秒级交付 |

**推导**：默认闸=3 并发/租户，P95≈7s（与实测 VU=4 档一致）；若 N 个付费租户同时重度使用，平台 goodput 50/min 会被摊薄——**试运营期按 ≤15 个「活跃付费租户」滚动观察**（详见监控项三）。

## 三、超卖预案（触发即执行）

| 信号 | 观察口 | 动作 |
|---|---|---|
| LLM 排队常态化（同步 P95 >15s 持续 10min） | 后台用量面板 / metrics `translator_translations_total` | ① 收紧重点租户 `tenant_quota_<tid>`（并发 3→2）；② 引导切异步工单 |
| `translator_llm_breaker_open=1`（熔断开） | S9 告警 LLMBreakerOpen → 告警中心+运营群 | 检查上游供应商状态页/换 key；持续>15min 公告暂停新建工单 |
| 新注册激增超出承接能力 | `/api/admin/funnel` + tenants 数 | 后台 `registration_enabled=0` 熔断新注册（**现成功能**，随时可切）；已注册不受影响 |
| 宿主机 avail<15%/8% | S9 告警 HostMemoryWarn/Critical | 关闭演示站释放 ~450M；排查文件转换子进程 |
| 磁盘 <12% | S9 告警 HostDiskLow | 清理 `/opt/translator/log` 与过期备份 |

**排队话术（现有实现，无需开发）**：闸满时同步接口返回业务码 `rate_limited`，文案「并发请求过多，请稍后再试。如余额不足请充值或升级套餐」——客户侧表现为快速失败而非卡死，符合保护性降级预期。

## 四、后续加容量路径（按性价比）
1. **LLM 供应商提额/多路由**（当前 chat 并发 3 + 保留槽 1，`DYNAMIC_ROUTING`/hedged 已有开关默认关）——瓶颈在上游延迟，优先谈 QPS/开通更快模型档。
2. 升级云主机内存（1.6G→4G）后可提 `LLM_CHAT_CONCURRENT` 与 worker——当前 36% avail 说明内存不是约束，**不必急于升配**。
3. 同步接口独立小并发池（交互保留槽已有雏形），必要时给 OpenAPI 客户单列预算。

---
*复测方法：`k6 run -e BASE=... -e TOKEN=... -e VUS=8 -e DURATION=75s deploy/loadtest/k6.js`（先临时放宽 `tenant_quota_<tid>`，测完恢复）。*
