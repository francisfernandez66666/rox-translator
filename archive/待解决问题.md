# 待解决问题清单

> 更新：2026-08-26 ｜ 历史已解决问题全部归档压缩（详见 git 历史），本表只保留「在办/观察中」事项。

---

## 一、🔴 在办 / 观察中

- [ ] **translator 锁饥饿脆弱性**（2026-08-23 复发一次，人工重启恢复）
  - 现象：整机 CPU/内存受压时出现「互斥锁上 25 goroutine 堆积、不自愈」
  - 缓解已上线：`watchdog_selfcheck_restart`（连续 3 次探活失败自动退出交 systemd 拉起）。~~默认 0~~ **已于 2026-09-04 改为默认开启**（显式 "0" 才关），该根治项已随版本生效
  - 2026-08-26 增补：卡死巡检已加「running 租约不重排」条件+工单心跳（防重复执行放大负载）；metricsText 读锁泄漏全局假死已修复（该问题曾被误判为锁饥饿复发，注意甄别）
  - 长期方向：Store 锁拆分 / DB 操作带超时
- [ ] **泄漏密钥吊销轮换确认**（历史清洗+强推已完成；后台换 key 后在本条打勾）

## 二、⚪ 长期缺口

见 `未完成项目.md`（订阅续期/发票落地/gitleaks 门禁/插件上架/SDK 发布等均在该表挂账）。
