# 技术方案 · 评审整改：余额贯通 / 商业化收口 / 安全加固二期

> 版本 v1.0 ｜ 2026-08-26 ｜ 来源：全仓端到端评审（文档 × 代码 × 部署链交叉核对）
> 性质：**开启强制计费（billing_enforced=1）前必须完成的整改批次** ｜ 状态：⏳ 待实施
> 原则：每项独立可验证、独立可回滚；改完跑 `go build/vet/test` + `vue-tsc --noEmit && vite build` 全绿。

---

## 〇、特性确认（非缺陷，明确出清单）

| 项 | 结论 |
|---|---|
| **文件管线硬闸重试无轮数上限**（engine/file.go 复核补漏循环） | ✅ **产品特性设计**（决策人 2026-08-26 确认）：「直到全部译出或任务超时/取消为止」即硬闸语义本体。既有 `ctx.Err()` 取消保护 + 500ms 轮间隔 + 25min job 超时三重护栏维持现状，**不做任何改动**。后续任何评审不得再将其列为缺陷 |

---

## 一、修复项总览

### A 组 · P0（计费口径裂缝，开闸前必修）

| # | 问题 | 现场位置 | 后果 |
|---|------|---------|------|
| A1 | 双桶台账只接入了「实扣」，**展示与预检都只看永久余额** | billing_api.go:132 balancePayload、billing/quota.go:196 CheckBalance、service/ticket.go:151 worker 预检、plans_api.go:60、api_openapi_tasks.go balanceOut | 新注册租户 30 万 token 在 quota_grants、永久余额=0：页眉徽标显「0 token ≈ 0 句」；**billing_enforced=1 后全部被 fail-closed 拒绝**——预检口径与 DeductWithGrants 实扣口径分裂。《TOKEN双桶方案》§七承诺的 `sub_grants_left/permanent_balance` 出参至今未实现 |
| A2 | 浏览器划词插件 CORS 断链风险 | extension/manifest.json `host_permissions: []`；server.go:184 CORS 白名单默认仅 localhost | MV3 content script 跨域 fetch 受页面 CORS 约束，第三方站点划词大概率失败（LLM网关方案验收第 3 条需在非本站页面复测） |

### B 组 · P1（财务口径失真）

| # | 问题 | 现场位置 | 后果 |
|---|------|---------|------|
| B1 | 在线充值订单 `amount_money` 恒 0 + 定价换算散落三处且注释与代码不符 | pay.go:100（money 传 0）、pay.go:123（注释 ×0.1 分 vs 代码 ×10 分）、billing.go:305 回调现场算 `tokens×10` | 发票开 0 元单（见 B2）、回调核对无单一事实源 |
| B2 | payments 流水对套餐订单记 0/0 | billing.go:557-560（`tokens×10`，套餐单 amount_tokens 恒 0） | P0-5「流水记实金额」只修了充值单一半，套餐单对账原料仍失真 |
| B3 | 退款不回收权益，且 increment/套餐单退款扣回量为 0 | billing.go:680 RefundOrder（读 amount_tokens，套餐/增量单恒 0） | 「退了钱额度照用」；增量包退款实际一分 token 都没扣回 |

### C 组 · P1（安全/一致性）

| # | 问题 | 现场位置 | 后果 |
|---|------|---------|------|
| C1 | `/api/download` 有 JWT+目录白名单但**无租户/用户归属校验** | stream.go:377-435（对比：handleTicketDownload/OpenAPI download 均有归属判定） | 白名单内任何登录用户可读任何租户的上传件与产物（横向越权；路径含纳秒戳难枚举但非零风险） |
| C2 | OCR 移除残留死代码（大小写漏检致「零残留」验收失真） | engine/file.go:111-124,334-342（pdfPageOcrMode + 调 ApplyTranslatedPdfPageOcr）、fileproc/pdfwrite.go:108-139（mode=="pageocr" 分支 + ApplyTranslatedPdfPageOcr） | 会向已删除 pageocr 子命令的 docx_translate.py 发调用（Python 侧异常返回即触发必败分支）；注释误导后人 |

### D 组 · P2（加固与卫生）

| # | 问题 | 现场位置 |
|---|------|---------|
| D1 | `/metrics` 公网暴露无鉴权 | deploy/caddy/translator.conf:31-33 直接反代 |
| D2 | JWT_SECRET/ADMIN_INIT_PASSWORD 缺失时静默兜底不拒启 | iam/auth.go:28、cmd/server/main.go:107-114 |
| D3 | model_routes 供应商 Key 明文存 system_config | admin_models.go 保存链（SQLite 文件即钥匙串） |
| D4 | 日限额口径错位：max_daily_chars（字符）对比 DailyUsage（token 成本） | billing_api.go:53-62、billing/quota.go:181 |
| D5 | 部署指南 §八-B2 首行「同步接口已移除」与 server.go:340 矛盾 | 部署指南.md |
| D6 | `alert()` 残留（违反自家弹窗规范） | frontend/src/App.vue onPwdDone |
| D7 | 白皮书 §七.4「数据回流脱敏 + 租户可关闭回流」无对应实现 | tmreview.go / store/tmreview.go |

### E 组 · 工程欠账（随批次顺带）

| # | 事项 |
|---|------|
| E1 | engine/api/service 层单测补充（清单见 §三.E1） |
| E2 | 「单实例内存态组件」红线写入部署指南（resetCodes/emailCode/loginLimit/regGuard/quotaByTenant/metrics 六处进程态） |
| E3 | RebuildKBIndex 全量 Embed 无计量挂钩 |

---

## 二、逐项设计

### A1 双桶余额贯通（最优先，单独一个 Commit）

**目标口径**：「可用余额 = Σ未过期台账 left + 永久 balance」，全系统（展示/预检/出参/≈句数）唯一。

1. **store 层新增聚合查询**（quota_grants.go 追加）：

```go
// TenantRemainTotal 双部分可用余额一次聚合：(未过期台账合计, 永久余额)。
func (s *Store) TenantRemainTotal(tid int64) (grants, permanent int64, err error)
```
   单条 SQL：`SELECT COALESCE(SUM(CASE WHEN g.expires_at>? THEN g.left ELSE 0 END),0), b.balance FROM balance_accounts b LEFT JOIN quota_grants g ON g.tenant_id=b.tenant_id WHERE b.tenant_id=? GROUP BY b.balance`（now 参数传入；空账户行为 EnsureBalance 先行保证）。

2. **预检口径切换**（三处，全部改为总余额判断）：
   - `billing/quota.go CheckBalance` 内部改用 TenantRemainTotal（签名不变，调用方零改动）：`grants+permanent <= 0 → quotaErr{"额度不足，请充值或升级套餐"}`
   - `service/ticket.go:150-157` worker 预检同步改判总额（拒绝文案不变，error_code 前缀 insufficient_balance 不变）。
   - `billing_api.go gateUsage` 无需动（它调 CheckBalance）。

3. **出参扩展**（向后兼容，新增字段）：
   - `billing_api.go balancePayload` 改签名为 `(grants, permanent, total, approx)`；
   - `handleMyPackage` / `handleOpenAPIBalance` / `handleOpenAPITaskStatus` 出参新增：
     `sub_grants_left`（台账剩余）、`permanent_balance`（永久余额）；`balance_tokens` 语义 = 总额（老前端只读此字段 → 自动显示正确总额）；`balance_sentences_approx` 按总额折算。
   - 管理端 `handleBalance`（admin_billing.go）同样补双桶字段。

4. **前端（可选增强，不阻塞）**：Billing.vue 余额卡加「台账 X / 永久 Y」明细行；App.vue 页眉徽标无需改动。

5. **回归测试**：store 层新增用例——构造「快过期行 + 大额行 + 永久=0」断言 TenantRemainTotal 与 CheckBalance 放行；构造全零断言拒绝。

**验收**：新注册租户（礼包入台账）页眉显示 ≈300000 token；手工置 `billing_enforced=1` 后该租户翻译正常、耗尽后返回 insufficient_balance。

### A2 插件 CORS 断链修复（双保险，两案并做）

1. **主案（插件侧）**：extension/manifest.json 增加
   ```json
   "host_permissions": ["http://*/*", "https://*/*"]
   ```
   MV3 下 content script 对已授权 host 的 fetch 以扩展身份发出，绕过页面 CORS。Chrome Web Store 上架时在权限说明中注明「仅用于连接用户自行配置的翻译服务地址」。
2. **副案（服务端兜底，防企业客户自装 unpacked 插件受限场景）**：`withCORS` 对 `/openapi/*` 前缀路径无条件反射 Origin（`Access-Control-Allow-Origin: <origin>` + Vary），理由：该前缀全部为 API Key 鉴权、无 Cookie 会话面，不存在 CSRF 利用价值；其余路径维持白名单不动。
3. popup.html 使用说明补一句「服务地址需以 http(s):// 开头」。

**验收**：任一第三方 https 页面（如 wikipedia）划词出泡成功；`curl -H "Origin: https://example.com" -i https://<host>/openapi/v1/balance` 响应含对应 ACAO 头。

### B1 定价换算收敛 + orders.amount_money 回填

1. **单一事实源**：store 新增 `PriceFenPerToken()`（读 system_config `price_fen_per_token`，默认 **10**＝现网实际执行口径，1 token = 0.1 分；EnsureBillingDefaults 落默认值，面板可改）。
2. **下单回填**：`handlePayCreate`（在线渠道）与 `handleAdminOrderCreate` 创建后统一
   `amount_money = tokens * PriceFenPerToken() / 100.0` 写回 orders（UpdateOrderPrepay 扩展或新 Update 方法一并更新）。
3. **回调核对改读落库值**：`handlePayNotify` 金额核对由现场 `o.AmountTokens*10` 改为 `int64(o.AmountMoney*100)`（B1.2 已回填；对存量 pending 单执行一次性迁移：
   `UPDATE orders SET amount_money = amount_tokens * (SELECT CAST(value AS REAL)/100 FROM system_config WHERE key='price_fen_per_token') WHERE status='pending' AND amount_money=0 AND package_id=0;`——挂 Store.New 幂等执行）。
4. pay.go:123 误导性注释随改动重写；线下单（channel=offline）维持人工录金额不受影响。

### B2 payments 流水记真实应收

`MarkOrderPaid` 流水 INSERT 改为：
```
amount_money 分 = CASE：package_id>0 → orders.amount_money×100（套餐真实售价）
                    否则     → amount_tokens × PriceFenPerToken()
```
（第一步条件更新后 SELECT 已在事务路径上，补 Scan amount_money 即可。）审计不变。

### B3 退款权益回收（依赖 B1/B2 落地后实施）

1. **应扣 token 反推**：`RefundOrder` 不再读 amount_tokens（套餐/增量单恒 0），改为：
   - `package_id>0` → `pkg.Sentences × TokenSentenceRate()`；
   - 否则 → `amount_tokens`。
2. **事务内追加权益回收**（BEGIN IMMEDIATE 已有）：
   - paid 单：`UPDATE quota_grants SET left=0 WHERE tenant_id=? AND source='order' AND ref_id=? AND kind='plan' AND left>0`（作废未过期订阅台账，回收量写入审计 diff）；随后清订阅镜像身份 `perms.PackageCode=''`（tenant.Update）。
   - increment 单：现有永久桶守卫扣减覆盖（金额换成反推值后自然生效）。
3. **商业规则声明（默认采纳，决策人可改）**：退款=全额退款 + 权益全额作废（含已消耗部分不追讨差额）。当前无自动退款通道（超管手动触发），资损风险可控。
4. RefundOrder 错误文案区分「余额不足以扣回」与「订单状态不允许」。

### C1 /api/download 产物归属校验（两阶段灰度）

1. **新表**（store.New 幂等建表）：
```sql
CREATE TABLE IF NOT EXISTS output_artifacts (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    path TEXT NOT NULL UNIQUE,          -- 产物绝对路径（唯一索引 idx_artifacts_path）
    tenant_id INTEGER NOT NULL DEFAULT 0,
    user_id INTEGER NOT NULL DEFAULT 0,
    ticket_id INTEGER NOT NULL DEFAULT 0,
    created_at TEXT
);
```
2. **登记点（写入即登记）**：
   - stream.go 四接口成功分支：遍历 `res.Files` 逐条 `RegisterArtifact(path, tid, uid, 0)`；
   - service/ticket.go：`SetTicketFileResult` / `SetTicketResultPath` 处登记（带 t.TenantID / creatorUID / t.ID）。
3. **下载判定**（handleDownload，resolveSafePath 之后）：
   - 按 path 查登记行；命中 → 普通用户要求 `tenant_id==effTenant && user_id==u.ID`；dept/tenant_admin 及以上放宽为同租户；超管全放行。未命中 → **Phase1 放行 + warning 审计日志（过渡期 = 一个产物保留周期 14 天）**；
   - Phase2（下一版本）：未命中一律 404。UploadDir 根下的「本用户当次会话上传件预览」场景由登记覆盖（上传成功即登记）。
4. OpenAPI 下载端点已有 APIUserID 强绑定，不动。

**验收**：跨租户两个账号互拉对方产物路径 → 403；本人/同租户管理员 → 200。

### C2 OCR 死代码清理（本次用大小写不敏感扫描验收）

| 位置 | 处置 |
|---|---|
| engine/file.go:113 `var pdfPageOcrMode bool` 及 :118 赋值 | 删除变量与赋值 |
| engine/file.go:335-342 `if pdfPageOcrMode { … }` 整支 | 删除（保留 pdfCacheDocx 主路径 + xlsx 对照降级） |
| fileproc/pdfwrite.go ExtractTextsPdfDocx `mode=="pageocr"` 判段（127-130） | 删除（Python 现恒返回 "docx"） |
| fileproc/pdfwrite.go `ApplyTranslatedPdfPageOcr`（133-139） | 函数整体删除 |
| 相关注释（file.go:110-111、pdfwrite.go:99,108-109「图片OCR」表述） | 同步改写为「图片区域按产品策略不翻译」 |

**验收**：`grep -rni "pageocr\|_ocr\|pytesseract" backend-go/` 零命中；PDF 图片型样例工单回归（提取→翻译→回写→xlsx 降级分支各一）。

### D 组逐项

| # | 设计要点 |
|---|---|
| D1 | 双层收敛：① caddy translator.conf 删公网 `handle /metrics`（Prometheus 改内网抓取/SSH 隧道，部署指南 §十验证命令同步改为服务器本机 curl）；② 代码兜底：`METRICS_TOKEN` 环境变量非空时 handleMetrics 校验 `Authorization: Bearer`，未设置保持现状（本地开发友好） |
| D2 | main.go 启动尾段新增：环境变量 `REQUIRE_PROD_SECRETS=1`（生产 systemd drop-in 显式开启）时，`JWT_SECRET`、`ADMIN_INIT_PASSWORD`、`ADMIN_TOKEN` 任一缺失即 `log.Fatal` 拒启并列出缺失项；未开启时维持现随机兜底行为（本地开发不受影响）。部署指南 §六 systemd 片段补 `Environment=REQUIRE_PROD_SECRETS=1` |
| D3 | model_routes 保存链路接入 store/crypto.go：`EncryptPlain(api_key)` 入库；启动水合（main.go:145-163）与 admin_models 读取处 `DecryptPlain` 还原；掩码回填逻辑保持（未修改掩码 → 回填旧值重新加密后存储）。**运维约束**：解密失败（空串）打 critical 告警并跳过该路由——JWT_SECRET 轮换必须同步重存模型路由（写入部署指南 §十二常见问题）。验收：`sqlite3 tm.sqlite3 "SELECT value FROM system_config WHERE key='model_routes'" \| grep -c 'sk-'` 为 0 |
| D4 | tenant.Perms 新增 `MaxDailyTokens int64`（json:max_daily_tokens；老 permissions JSON 缺省 0=不限，无需迁移）；gateUsage 优先读 MaxDailyTokens（>0 时比较 DailyUsage 的 token 成本口径），否则回退旧字符口径（过渡兼容）；注册默认 perms 同时写 `MaxDailyChars:20000` 与 `MaxDailyTokens:20000`；后台 Quota 面板加「每日 token 上限」输入框（quotaQPS 同款读写位） |
| D5 | 部署指南 §八-B2 首行改述：「文本/文件任务统一异步『工单 ID+轮询』；另有同步短文端点 `POST /openapi/v1/translate`（≤5000 字符、≤5 语言）专供划译插件与 Office taskpane」；PROGRESS.md 文档索引补本方案链接 |
| D6 | App.vue `onPwdDone` 的 `alert(t('pwd.done'))` 替换为应用内 toast（复用 menu-drop 式浮层或 fb-modal 轻提示，2s 自动消失）；`grep -rn "alert(" frontend/src` 清零（i18n 文案中的「提醒」字样除外） |
| D7 | ① tenants 表 migrateColumns 补 `feedback_opt_out INTEGER NOT NULL DEFAULT 0`；② Models.vue 租户策略卡加「参与行业记忆共建（数据回流）」开关（走 /api/admin/policy/save 扩展字段或独立轻接口，租户管理员可关、超管可代管）；③ 生效点：`BumpTmHit` 达阈值产候选前、`CreateTmReview(source='bitext'/'tmx')` 入池前——opt_out 租户**只计数不产候选不入池**（本租户私有 TM 与审批流 approved 直入不受影响，二者属租户私域非「回流」）；④ 脱敏：tmreview 存储入口统一过 `maskPII()`（手机号 `1[3-9]\d{9}`→`1xx****xxxx`、邮箱 `@` 前掩码），超管人工审核作为第二道闸（白皮书 §七.4 口径闭环） |

### E 组逐项

| # | 设计要点 |
|---|---|
| E1 | 优先用例清单（httptest mock OpenAI 兼容端点即可，无需真实 Key）：① engine/singleLang——主路由失败按权重降级序、熔断开启跳主模型直 fallback、finish_reason=length 翻倍重试 ≤2 次；② engine/InvalidateKBCaches 后 CJK 缓存重建可见新术语；③ api/handleDownload 归属四象限（本人/同租户他人/跨租户/超管）；④ api/handlePayNotify 三道闸矩阵（无头/错头/渠道不符/金额不符/mock 封禁/manual 单拒绝）；⑤ chargeTokens markup<1 兜底取 total |
| E2 | 部署指南新增 §八-D「单实例架构红线」：列出 resetCodes / emailVerifyCodes / loginLimiter / registerGuard / billing.quotaByTenant / metrics 六个进程内存态组件，声明「横向扩容前必须先行改造为 Redis/DB 存储」，并同步进 PROGRESS 技术备忘 |
| E3 | RebuildKBIndex 入口注入 `WithUsageRecorder` + 租户 ctx（发起管理员的 effTenant；平台上下文 tid=0 时 Meter 自动走 LogUsage 只留痕不扣费），Embedding 成本进入 usage_ledger 可对账 |

---

## 三、不做的事（明确边界）

1. **硬闸重试无轮数上限**：特性设计，保持现状（见 §〇）。
2. 微信/支付宝官方验签、电子发票自动开具、ICP 备案：仍在《未完成项目.md》资质签约类挂账，本方案不含。
3. JWT jti/吊销列表、密码弱哈希强制迁移：仍归《P0安全止血方案》§三声明的《企业级合规加固方案》（未立项）。
4. 前端引入 vue-router/pinia 持久化/组件测试等工程化重构：收益不阻塞收费，延后至有真实多页面需求再立项。
5. 多实例（水平扩容）改造：本方案仅立红线成文（E2），不做实现。
6. 存量已完成订单的 amount_money 回填：仅回填 pending 单（B1.3）；历史 paid 单对账以 payments 流水（B2 起为新口径）+ orders.created_at 期间的人工台账为准，不改历史。

## 四、验收清单（汇总）

1. 新注册租户：页眉徽标 ≈300000 token；`GET /openapi/v1/balance` 含 `sub_grants_left=300000 & permanent_balance=0 & balance_tokens=300000`。
2. `billing_enforced=1` 后：仅台账租户可翻译；台账+永久双耗尽返回 insufficient_balance（站内与 OpenAPI 同码）。
3. mock 全链路下单：orders.amount_money>0；payments 流水分值=订单应收；发票开出金额>0。
4. 套餐 paid 单退款后：quota_grants 对应 plan 行 left=0、PackageCode 清空、审计含回收量。
5. 跨租户下载对方产物路径 → Phase1 得 200 但审计出现 warning、Phase2 起 403/404；本人与同租户管理员正常。
6. 第三方 https 页面划词成功；`grep -rni "pageocr" backend-go/` 零命中。
7. 公网 `curl https://langcross.lexicorn.cn/metrics` → 404/401；`REQUIRE_PROD_SECRETS=1` 且缺 JWT_SECRET 启动即退出并打印缺失清单。
8. 库内 model_routes 无明文 sk-；`grep -rn "alert(" frontend/src` 零命中；opt_out 租户达标不产生 tm_review 候选。
9. `go build/vet/test ./...` 全绿（含 A1/E1 新增用例）；`vue-tsc --noEmit && vite build` 全绿；`./deploy/smoke_kb_scope.sh` 通过。

## 五、实施顺序（Commit 切分，每步独立可回滚）

| 序 | Commit | 内容 | 依赖 |
|---|--------|------|------|
| 1 | `billing-total-bucket` | A1 全部（store 聚合 + 三处预检 + 出参 + 测试） | 无 ★最高优先 |
| 2 | `finance-price-source-of-truth` | B1+B2（定价常量/回填/迁移/流水） | 无 |
| 3 | `download-artifact-ownership` | C1 Phase1（登记+判定+灰度放行） | 无 |
| 4 | `plugin-cors-and-ocr-cleanup` | A2 + C2 | 无 |
| 5 | `refund-revoke-entitlements` | B3 | Commit 2 |
| 6 | `hardening-p2-batch` | D1~D7 + E2 + 文档同步 | 无 |
| 7 | `tests-and-metrics-hook` | E1 + E3 | 建议 Commit 3 之后 |

## 六、实施记录

| 项 | 状态 | 落点 |
|---|---|---|
| A1 双桶余额贯通 | ✅ | store.TenantRemainTotal + CheckBalance/worker 预检改总额口径 + balancePayload 四值出参（sub_grants_left/permanent_balance 落地）+ TestTenantRemainTotal |
| B1+B2 定价收敛与流水实收 | ✅ | PriceFenPerToken(price_fen_per_token=10) 单一事实源 + UpdateOrderMoney 下单回填 + orderMoneyBackfill 存量 pending 迁移 + 回调核对读落库金额 + MarkOrderPaid 流水记真实应收（套餐单修复） |
| B3 退款权益回收 | ✅ | RefundOrder 重写：句数×换算率反推应扣 token；paid 作废 plan 台账行+alert 留痕；increment/充值单永久桶守卫扣回；API 层清订阅身份 PackageCode |
| C1 download 归属校验 Phase1 | ✅ | output_artifacts 表(UNIQUE path) + 4 处登记点（stream×2/ticket worker/OpenAPI&内部建单源文件）+ handleDownload 同租户+本人(或租管+)判定，未登记历史产物灰度放行留审计 |
| C2 OCR 死代码清理 | ✅ | engine/file.go pdfPageOcrMode 变量与写回分支删除；pdfwrite.go pageocr 分支与 ApplyTranslatedPdfPageOcr 删除；grep -rni pageocr 零命中 |
| D1 metrics 鉴权 | ✅ | METRICS_TOKEN 惰性读取，非空时 Bearer 校验（Caddy 公网反代已摘除）；**附带挖出并修复潜伏全局死锁**——metricsText 双 RLock 单 RUnlock，每次导出泄漏读锁致 countHTTP 写锁永久排队、整机假死（Prometheus 首次抓取即引爆），已合并为单次配对加解锁并经 deploy_check 连续请求验证 |
| D2 生产密钥强校验 | ✅ | REQUIRE_PROD_SECRETS=1 时三凭证缺一即 log.Fatal 拒启 |
| D3 model_routes/stage_models Key 加密 | ✅ | store.EncryptSecret/DecryptSecret(enc:v1: AES-GCM) + admin_models 4 条链路 + main.go 水合解密（失败路由告警跳过）+ engine.resolveStageModel 解密；库内不再存明文 sk- |
| D4 日限额 token 化 | ✅ | Perms.MaxDailyTokens + gateUsage token 口径优先（回退旧字符口径兼容存量）+ quota GET/SAVE 透传 + 注册默认 20000 |
| D6 alert 清除 | ✅ | App.vue onPwdDone 改应用内 toast（.app-toast）；Billing.vue 随套餐整合删除 |
| D7 数据回流开关+脱敏 | ✅ | PolicyConfig.DataFeedbackOptOut 三态 + policy GET/SAVE + Models.vue 策略卡开关 + feedbackOptOut() enforcement（opt_out 只计数不入池）+ maskPII 手机号/邮箱掩码入 tm_review |
| E3 RebuildKBIndex 计量 | ✅ | WithUsageRecorder 注入 + 收尾按 enforced RecordUsage/LogUsage 入 usage_ledger |
| 文档同步 | ✅ | 本表 + PROGRESS 索引 + 部署指南 §八-B2 改述/§八-B3 新环境变量/§八-D 单实例红线 + 白皮书 §5.4 oneid 双唯一与自助注销规则 |

---

*本文与《商业化白皮书》《TOKEN双桶改造实施方案》《P0安全止血与并发原子性修复方案》互为上下游；任何规则变更先修订本文再动码。*
