# 改造方案：运营策略引擎——计费/模式/套餐/时间窗/邀请/注册/限额/支付因子流程引擎式配置

> 日期：2026-09-05　范围：backend-go + frontend-react　状态：方案定稿（评审通过后实施）
> 演进：本文由《计费流程引擎因子配置》扩展——在「模式定价/套餐/邀请」基础上，
> 将运营时间窗（推广期）与注册/限额/支付等常见运营参数一并因子化，统一为**运营策略引擎**。

## 〇、背景与目标

现行运营参数散落在数十个 `system_config` 散键、`tenants` 权限列与业务代码常量中：

| 领域 | 现状参数（散键/常量） | 痛点 |
|---|---|---|
| 计费开关 | `billing_enforced`、`billing_markup_multiplier` | 无法按模式区分定价 |
| 模式定价 | 无（全模式统一扣费）；交互台账 `biz_mode` 硬编码 `"pro"` | 无法「推广期快速免费、专业才扣」 |
| 运营时间 | 无 | 促销/推广期只能人肉改配置再改回 |
| 套餐 | `free_trial_tokens/days`、无月度重置 | 无法 GPT 式重制套餐 |
| 邀请 | `invite_reward_*`、`referral_*` 散键 | 分散不可编排 |
| 注册 | `registration_enabled`、`register_ip_*`、`email_verify_enabled` | 散键 |
| 限额 | `quota_max_qps/concurrent`、租户 `max_daily_*` | 分散在内存与权限列 |
| 支付 | `pay_mode`、`auto_charge`、`static_qr_image` | 散键 |
| 内容 | `translateUploadMax`（常量）、缩翻 `max_length`（每请求参数） | 无法运营级开关 |

**目标**：把这些运营参数统一收编进**运营策略引擎（Operations Policy Engine）**，以「因子 + 时间窗」编排：
1. **模式定价因子**：fast/pro 可配扣费/免费/成本系数/输入上限；
2. **运营时间窗因子**：推广期内自动覆盖指定因子（如快速免费），到期自动失效；
3. **套餐因子**：体验额度、月度用量重置；
4. **邀请奖励因子**：奖励 token/天数/日上限/总开关；
5. **注册/限额/支付/内容因子**：常见运营开关集中可配；
6. 两级作用域：平台默认 + 租户覆盖，解析顺序平台→租户→时间窗。

## 一、因子模型（运营策略 OperationsPolicy）

沿用流程引擎「因子 + 开关」心智，整体 JSON 结构（`mode` 取值 `fast`/`pro`）：

```jsonc
{
  "version": 1,
  "tz": "Asia/Shanghai",                    // 全局时区（时间窗判定用；可被时间窗自身 tz 覆盖）

  "billing": {                              // 计费基础因子
    "enforced":            true,             // 强制计费（对应 billing_enforced）
    "markup_multiplier":   1.5,              // 全局成本系数（对应 billing_markup_multiplier）
    "mode_rules": {                         // ★ 模式定价因子
      "fast": { "enabled": true, "charge": false, "markup": 0, "limit_chars": 500 },
      "pro":  { "enabled": true, "charge": true,  "markup": 0, "limit_chars": 0 }
    }
  },

  "promo_windows": [                        // ★ 运营时间窗因子（推广期）
    {
      "id": "fast_free_2026q4",
      "name": "快速模式推广期",
      "start": "2026-10-01T00:00:00",       // RFC3339 或 YYYY-MM-DD（按 tz 解释）
      "end":   "2026-12-31T23:59:59",
      "tz":    "Asia/Shanghai",
      "priority": 10,                       // 重叠窗口大者优先；同优先级数组序后者优先
      "overrides": {                        // 窗口内覆盖的因子（结构与策略同构，走同一 Merge）
        "billing": { "mode_rules": { "fast": { "charge": false, "limit_chars": 1000 } } },
        "invite":  { "reward_tokens": 500000 }
      }
    }
  ],

  "package": {                              // 套餐因子
    "trial_tokens":         300000,          // 新租户体验 token（对应 free_trial_tokens）
    "trial_days":           14,              // 体验时长天（对应 free_trial_days）
    "monthly_reset_enabled": false,          // 是否开放「本月套餐用量重置」
    "monthly_reset_limit":  1                // 每租户每月重置次数上限（0=不限）
  },

  "invite": {                               // 邀请奖励因子
    "enabled":            true,             // 总开关（对应 referral_enabled）
    "reward_tokens":      300000,           // 邀请注册奖励 token（对应 invite_reward_tokens）
    "reward_days":        14,               // 奖励时长天（对应 invite_extend_days）
    "paid_reward_tokens": 0,                // 付费用户额外奖励（对应 paid_reward_tokens）
    "paid_reward_days":   0,                // 付费用户额外时长（对应 paid_reward_days）
    "max_daily_rewards":  50                // 单邀请人日发奖上限（对应 referral_max_daily_rewards）
  },

  "registration": {                         // 注册因子
    "enabled":              true,           // 注册开关（对应 registration_enabled）
    "ip_min_interval_sec":  60,             // 同 IP 最小间隔（对应 register_ip_min_interval_sec）
    "ip_daily_limit":       3,              // 同 IP 24h 注册上限（对应 register_ip_daily_limit）
    "email_verify_enabled": false           // 邮箱验证强制（对应 email_verify_enabled）
  },

  "limits": {                               // 限额因子（租户级可覆盖）
    "max_qps":               100,           // 全局 QPS 上限（对应 quota_max_qps）
    "max_concurrent":        50,            // 并发上限（对应 quota_max_concurrent）
    "default_max_daily_chars":   20000,     // 新租户默认日字符上限（注册发放进 permissions）
    "default_max_daily_tokens":  20000      // 新租户默认日 token 上限（注册发放）
  },

  "payment": {                              // 支付因子（平台级，租户不可覆盖）
    "mode":        "mock",                  // mock/wechat/alipay/static_qr（对应 pay_mode）
    "auto_charge": false                    // 下单即到账（对应 auto_charge；static_qr 走人工确认）
  },

  "content": {                              // 内容/翻译因子
    "file_max_mb":     40                   // 文件翻译/工单上传上限 MB（现状常量 translateUploadMax）
  }
}
```

### 因子字段 → 落点映射（含改造读点）

| 因子 | 落点（现状） | 生效路径（改造） |
|---|---|---|
| `billing.enforced` | `system_config.billing_enforced` | `Bill.Enabled()` 等散读点统一改读最终策略 |
| `billing.markup_multiplier` | `system_config.billing_markup_multiplier` | `markupMultiplier()` 改读最终策略 |
| `billing.mode_rules.*` | 无（新增） | 实时计量 `ChargeUsageRealtime`（按模式扣费/免费）、输入闸门 `limit_chars` |
| `promo_windows` | 无（新增） | 有效策略解析时叠加覆盖 |
| `package.trial_tokens/days` | `free_trial_tokens/days` | `register.go` 体验额度发放 |
| `package.monthly_reset_*` | 无（新增） | 套餐重置接口闸门 |
| `invite.*` | `referral_*`/`invite_reward_*` | `register.go` 邀请裂变发放 |
| `registration.*` | `registration_enabled`/`register_ip_*`/`email_verify_enabled` | `register.go`、`email_verify.go` 读点 |
| `limits.*` | `quota_max_qps/concurrent`（billing_api.go:334） | QPS/并发闸门；新租户权限初始化 |
| `payment.*` | `pay_mode`/`auto_charge`/`static_qr_image`（pay.go、admin_billing.go:171） | 支付创建/模拟读点 |
| `content.file_max_mb` | 常量 `translateUploadMax=40MB`（upload.go:48，stream.go/tickets.go 引用） | 文件翻译/工单上传上限 |

> 兼容策略：一期以「最终策略（EffectivePolicy）作为唯一权威」，各读点改为 `策略值非零/显式 → 用策略；否则回落既有散键/常量`，保证存量部署零感知。

## 二、作用域与解析顺序

### 2.1 存储

| 层级 | 存储 | 写入权限 | 说明 |
|---|---|---|---|
| 平台默认 | `system_config.ops_policy` | 超管 | 平台权威默认（含 `promo_windows`） |
| 租户覆盖 | `tenants.policy_config.ops_policy` | 租户管理员（本租户） | 字段级覆盖，未配置回落平台 |
| 重置计数 | `tenants.policy_config.ops_resets` | 只写（内部） | `{"month":"YYYY-MM","count":n}` |

### 2.2 解析顺序（effective policy for tenant）

```
最终策略 = 代码内置默认 DefaultEffective()
         → Merge(平台 ops_policy)                      // 平台默认
         → Merge(租户 ops_policy)                      // 租户覆盖（显式字段）
         → Merge(命中活跃窗口 overrides，priority 降序) // 运营时间窗
```

- 「命中」= `now(窗.tz) ∈ [start, end]`；
- 租户覆盖仅作用于租户可覆盖组：`billing.mode_rules / package / invite / limits`；平台专属组 `payment / registration / content / promo_windows 定义` 租户不可写；
- `tz` 缺省用平台 `tz`（默认 `Asia/Shanghai`）。

### 2.3 时间窗细节

- `start`/`end` 支持 `YYYY-MM-DD`（当日 00:00:00 起 / 23:59:59 止）、`YYYY-MM-DD HH:MM`、RFC3339；
- 优先级：`priority` 大者优先；重叠且同优先级按数组序后者优先；
- GET 响应返回每个窗口的 `active` 布尔与当前时刻，便于运营排查；
- 未来扩展：`audience`（租户/行业）定向推广（二期）。

## 三、因子生效链路

### 3.1 模式定价（mode_rules）
1. `handleChat`/`handleTranslateFile`/工单任务在进入引擎前把模式写入 ctx：`tenant.WithMode(ctx, mode)`（`mode` 取 `options["mode"]`，`""/pro` 视为专业模式）；
2. `ChargeUsageRealtime` 读 `tenant.ModeFromCtx(ctx)`，取最终策略模式因子：
   - `charge=false` → **免费**：`Store.LogUsage` 仅留痕计量（用量看板可见、`cost=0`），不扣双桶台账；
   - `charge=true` → 按 `markup`（>0 优先于全局系数）折算后走既有 `RecordUsage` 扣减；
   - 台账 `biz_mode` 由硬编码 `"pro"` 修正为真实模式（修复口径偏差）。
3. 未配置策略时 `charge` 默认 `true`（fail-closed，防漏计费）。

### 3.2 模式输入上限（limit_chars）
- **对话（/api/chat）**：`handleChat` 在 `gateUsage` 后校验 `len([]rune(message)) ≤ limit_chars`（0=不限），超限结构化拒绝、不计费不落账；
- **文件（/api/translate）**：段落提取完成后、LLM 前按「本模式源文本总字符」同规则校验，超限返回已提取段数/超限字符数；
- **工单**：orchestrator 入口按工单模式校验（复用同一闸门函数）。

### 3.3 套餐月度重置（GPT 式重制）
- `POST /api/admin/billing/package/reset`：闸门 = `monthly_reset_enabled` 开 且 当月次数 < `monthly_reset_limit`（`>0` 时生效）；
- 动作：`quota_grants` 中 `expires_at>now AND kind='plan' AND left<total` 的行执行 `left=total`（当前套餐期），审计 `billing_package_reset`；
- 计数：`tenants.policy_config.ops_resets`，跨月自动归零；
- 结果返回重置行数与剩余额度，供前端回显。

### 3.4 邀请奖励
`register.go` 裂变发放处读最终策略 `invite.*`（`enabled/reward_tokens/reward_days/max_daily_rewards`），策略显式值优先，旧散键兜底。

### 3.5 注册 / 限额 / 支付 / 内容
各读点改为从最终策略取值，未配置回落既有散键（见 §一映射表）。新租户权限初始化时，`default_max_daily_chars/tokens` 写入 `tenants.permissions`。

## 四、API 设计

### 4.1 路由总览

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| GET | `/api/admin/ops/policy` | 超管/租户管理员 | 返回平台默认、租户覆盖、合并后基础与最终策略、窗口 active 状态 |
| POST | `/api/admin/ops/policy/save` | 超管（scope=platform）/租户管理员（scope=tenant） | 保存策略 |
| POST | `/api/admin/ops/policy/window/save` | 超管 | 新增/更新单个推广时间窗 |
| POST | `/api/admin/billing/package/reset` | 租户管理员+ | 重置当前套餐月度用量 |

> 路由前缀参照既有 admin 面板模式；超管经后台作用域切换（`X-Tenant-ID`）管理指定租户策略。

### 4.2 各接口出入参

**GET `/api/admin/ops/policy`**
```
请求：GET /api/admin/ops/policy
响应 200：
{
  "success": true,
  "scope": "platform",                     // platform | tenant（当前查看层级）
  "now": "2026-09-05T12:00:00+08:00",
  "platform":  { ...OperationsPolicy 原始 JSON... },
  "tenant":    { ...OperationsPolicy 或 null... },   // 当前租户覆盖
  "base":      { ...EffectivePolicy... },             // 平台默认→租户覆盖合并结果（不含窗口）
  "effective": { ...EffectivePolicy... },             // base + 命中窗口覆盖后的最终值
  "windows": [
    { "id": "fast_free_2026q4", "active": true, "start": "...", "end": "...", "priority": 10 }
  ]
}
```

**POST `/api/admin/ops/policy/save`**
```
请求体：
{
  "scope": "platform",                    // platform（超管）| tenant（租户管理员，默认）
  "policy": { ...OperationsPolicy 可覆盖字段... }   // 全量提交（保存前做租户可覆盖组白名单裁剪）
}
响应 200：{ "success": true, "message": "运营策略已保存", "scope": "platform" }
响应 403：{ "success": false, "message": "无权限修改平台策略" }
响应 400：{ "success": false, "message": "策略校验失败：..." }
```

**POST `/api/admin/ops/policy/window/save`**
```
请求体：{ "window": { ...PromoWindow... } }   // id 已存在=更新，否则新增
响应 200：{ "success": true, "message": "推广时间窗已保存" }
校验：start<end、overrides 仅含可覆盖因子、tz 合法
```

**POST `/api/admin/billing/package/reset`**
```
请求体（可选）：{ "tenant_id": 123 }         // 超管代操作
响应 200：{ "success": true, "message": "本月套餐用量已重置", "reset_count": 1, "remaining": 100000 }
响应 403：{ "success": false, "message": "未开放套餐重置" | "本月重置次数已达上限" }
```

### 4.3 错误码

| HTTP | 业务码 | 场景 |
|---|---|---|
| 400 | `OPS_INVALID_POLICY` | 策略 JSON 非法 / 窗口 start≥end / 未知模式键 |
| 403 | `OPS_FORBIDDEN_PLATFORM` | 非超管写平台策略 / 租户管理员写平台专属组 |
| 403 | `OPS_RESET_DISABLED` | 套餐重置未开放 |
| 403 | `OPS_RESET_LIMIT` | 本月重置次数达上限 |
| 400 | `MODE_LIMIT_CHARS` | 输入超模式 `limit_chars` 上限 |
| 403 | `MODE_DISABLED` | 模式 `enabled=false` 被禁用 |

## 五、前端面板

新增后台「运营策略」面板（`key=ops_policy`，超管全功能；租户管理员只读本租户+可覆盖本租户可覆盖组）。

### 5.1 布局（自上而下，卡片化）

| 卡片 | 字段 | 控件 | 备注 |
|---|---|---|---|
| 模式定价 | fast/pro × {启用, 扣费, 成本系数, 输入上限} | 开关/数字输入 | 「扣费」关闭=推广期免费（图例提示） |
| 推广期 | 窗口列表（名称/起止/优先级/命中态）+ overrides JSON | 表格 + JSON 编辑器 + 启停 | 「新增窗口」弹窗；展示 active 徽标 |
| 套餐 | 体验 token、体验天数、月度重置开关、重置次数上限 | 数字输入/开关 | 「重置本月用量」按钮（二次确认，展示剩余） |
| 邀请奖励 | 总开关、奖励 token、奖励天数、付费奖励 token/天数、日上限 | 开关/数字输入 | |
| 注册 | 注册开关、同 IP 间隔、日上限、邮箱验证开关 | 开关/数字输入 | |
| 限额 | QPS、并发、新租户日字符/token 上限 | 数字输入 | |
| 支付 | 模式（mock/wechat/alipay/static_qr）、自动到账 | 下拉/开关 | 平台专属，租户只读 |
| 内容 | 文件上限 MB | 数字输入 | |

### 5.2 交互
- 「保存」：提交 `scope` 对应策略；「恢复平台默认」：清空本租户覆盖；
- 作用域切换：后台租户切换器生效，超管可切平台/任一租户；
- 面板顶部展示 `effective` 摘要与当前命中窗口提示（「推广期生效中：快速模式免费」）；
- i18n 增补 `ops.*` 中英键。

### 5.3 权限矩阵

| 角色 | 查看平台策略 | 改平台策略/窗口 | 查看租户策略 | 改本租户策略 | 套餐重置 |
|---|---|---|---|---|---|
| 超管 | ✅ | ✅ | ✅（可切任意租户） | ✅（代操作） | ✅ |
| 租户管理员 | ✅（只读） | ❌ | ✅ | ✅（仅可覆盖组） | ✅（本租户） |
| 普通用户 | ❌ | ❌ | ❌ | ❌ | ❌ |

## 六、兼容性与迁移

1. 策略未配置（空）→ 全部回落既有散键/常量行为，线上零感知；
2. 台账 `biz_mode` 修正为真实模式（修复性变更，影响用量看板「模式」列既有展示）；
3. 无需建表：`system_config.ops_policy` 与 `tenants.policy_config.ops_policy` 惰性 JSON；原 `billing_policy` 中间产物（若已写库）读取时兼容并入；
4. 安全：策略仅 admin/tenant_admin；重置全程审计；免费模式 fail-closed（未配置一律收费）；
5. 租户可覆盖组白名单：`billing.mode_rules / package / invite / limits`，其余字段租户提交时强制裁剪。

## 七、运营场景示例

| 场景 | 配置动作 |
|---|---|
| 快速模式推广期免费 | 新增窗口（起止=推广期），overrides `mode_rules.fast.charge=false`；到期自动失效 |
| 快速模式限字数防滥用 | `mode_rules.fast.limit_chars=500`（窗口内外均生效） |
| 老用户回血：重置本月套餐 | 面板开 `package.monthly_reset_enabled=1`，引导租户「重置本月用量」（月度 1 次） |
| 大促双倍邀请奖励 | 窗口 overrides `invite.reward_tokens=600000` |
| 备案期间关闭注册 | `registration.enabled=false` |
| 压测放量 | `limits.max_qps=0`（=不限）、`max_concurrent=0` |

## 八、数据样例

**平台默认（system_config.ops_policy，推广期快速免费）**：
```json
{
  "version": 1,
  "tz": "Asia/Shanghai",
  "billing": {
    "enforced": true,
    "markup_multiplier": 1.5,
    "mode_rules": {
      "fast": { "enabled": true, "charge": false, "markup": 0, "limit_chars": 500 },
      "pro":  { "enabled": true, "charge": true,  "markup": 0, "limit_chars": 0 }
    }
  },
  "promo_windows": [
    {
      "id": "fast_free",
      "name": "快速模式免费推广",
      "start": "2026-09-05", "end": "2026-10-05",
      "priority": 10,
      "overrides": { "billing": { "mode_rules": { "fast": { "charge": false } } } }
    }
  ],
  "package": { "trial_tokens": 300000, "trial_days": 14, "monthly_reset_enabled": true, "monthly_reset_limit": 1 },
  "invite":  { "enabled": true, "reward_tokens": 300000, "reward_days": 14, "max_daily_rewards": 50 },
  "registration": { "enabled": true, "ip_min_interval_sec": 60, "ip_daily_limit": 3, "email_verify_enabled": false },
  "limits":  { "max_qps": 100, "max_concurrent": 50, "default_max_daily_chars": 20000, "default_max_daily_tokens": 20000 },
  "payment": { "mode": "mock", "auto_charge": false },
  "content": { "file_max_mb": 40 }
}
```

**租户覆盖（tenants.policy_config.ops_policy，企业客户定制）**：
```json
{
  "billing": { "mode_rules": { "fast": { "charge": true, "limit_chars": 2000 } } },
  "limits":  { "default_max_daily_chars": 50000, "default_max_daily_tokens": 50000 }
}
```

## 九、验收与测试

### 9.1 单元测试（`internal/ops/policy_test.go`）
1. 默认策略值正确（与散键现值一致）；
2. 解析容错：空串/非法 JSON → 默认；`charge=false` 显式解析；
3. 两层合并：租户覆盖字段生效、未覆盖字段回落平台；`enabled=false` 可显式关闭；
4. 时间窗：`YYYY-MM-DD`/RFC3339 解析、命中判定（含 tz）、优先级排序、窗口内覆盖叠加；
5. 租户不可写平台组（save 时裁剪校验）。

### 9.2 接口 UAT（并入 `scripts/uat/api_uat.sh`）
1. `fast.charge=false` → 快速翻译后 `quota_grants.left` 不变、`usage_ledger` 增行（免费留痕）；
2. `fast.charge=true` + `markup=2` → 快速翻译扣费按 2 倍折算；
3. `fast.limit_chars=10` → 超长快速翻译被拒（MODE_LIMIT_CHARS）、专业模式不受影响；
4. 配置含当前时刻的时间窗 → 窗口内免费、窗口外收费；
5. 开重置 → 扣减套餐后重置 → 余额恢复满额；二次重置被 `OPS_RESET_LIMIT` 拦截；
6. 邀请奖励改策略值 → 受邀注册后按新值入账；
7. 注册关闭 → 注册返回拒绝；限额/支付因子改策略 → 对应行为变化；
8. 台账 `biz_mode` 为真实模式（fast/pro）。

### 9.3 前端 E2E
运营策略面板渲染/保存/窗口编辑与命中徽标/重置按钮链路（并入 `e2e/pixel_uat.spec.ts` 或新增）。

## 十、分期

- **一期（本文）**：策略模型 + 解析顺序 + 时间窗 + 模式定价 + limit_chars + 套餐重置 + 邀请因子 + 面板 + 测试；
- **二期**：注册/限额/支付/内容因子替换散键读点；时间窗定向（audience）；费率表（rate_card）细化；重置审计报表。

## 附：后端文件改造清单（一期）

| 文件 | 改动 |
|---|---|
| `internal/ops/policy.go`（新增） | OperationsPolicy/EffectivePolicy、Parse/Merge/ActiveWindows |
| `internal/tenant/tenant.go` | `WithMode/ModeFromCtx` ctx 助手 |
| `internal/api/billing_api.go` | `ChargeUsageRealtime` 模式感知（charge/markup）；策略 GET/SAVE handler；套餐重置 handler；`effectivePolicy(tid)` 组装 |
| `internal/api/stream.go` | `handleChat`/`handleTranslateFile` 注入模式 ctx + `limit_chars` 闸门 |
| `internal/api/register.go` | 邀请奖励、体验额度读最终策略；`default_max_daily_*` 初始化 |
| `internal/api/server.go` | 新路由注册 |
| `frontend-react/src/components/admin/panels_*.tsx` | `BillingPolicyP` 面板（卡片化） |
| `frontend-react/src/api/ops.ts`（新增） | policy/window/reset 接口 |
| `frontend-react/src/i18n/panels/ops.ts`（新增） | `ops.*` 中英文案 |
| `scripts/uat/api_uat.sh` | §9.2 断言 |
