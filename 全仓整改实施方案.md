# 全仓整改实施方案

> 版本 v1.1 ｜ 2026-08-26 ｜ 状态：**批次 A/B/C/D 已全部实施**（含插队 U1-U4），go build + go test ./... 全绿，vue-tsc 通过
> 依据：三轮源码亲核裁决（40 项结论 = 29 属实 / 8 修正 / 2 撤回 / 1 死代码），非初版转述报告。
> 已拍板决策：① fileproc Python 子进程「保留+加固」；② 流程引擎「接通 Compensable」；
> ③ RefundOrder 口径 = **仅退剩余部分，按消耗比例折算**；④ 匿名翻译端点**直接上锁不留开关**。
>
> ## 实施记录（2026-08-26）
> - **插队 U1-U4 ✅**：文档复制按钮删除；邀请二维码 fetch+blob 鉴权化（api/referral.ts 重写+Referral.vue）；
>   新端点 `GET/POST /api/admin/referral/config`（超管）+ 前端运营参数卡片 + store.ReferralEnabled 双闸
>   （注册绑定处 / ReferralPaidReward）；`/register?ref=` 直达注册面板（Login.vue）
> - **批次 A ✅**：A1 四翻译端点 401（stream.go）/A2 闸门前置+defer 清理/A3 authenticateAPIKeyNoTouch
>   （计数收敛 handler 唯一入口）/A4 MarkInviteCodeUsed 返回 claimed 并强制判定/A5 单邀请人日发放上限
>   referral_max_daily_rewards（默认50，触顶 critical 拒发）/A6 mock 支付空配置封口/A7 删除 auth.Secret 死变量
> - **批次 B ✅**：B1 ExpirePackage+watchdog 改 json_set 单键原子（新增 SetNotifiedExpFlag）；
>   B2 MarkOrderPaid 全链路单 IMMEDIATE 事务（预读定价/套餐在事务外，payments 与权益同事务，
>   ReferralPaidReward 移提交后）；B3 RefundOrder 按拍板口径重写（剩余率 r=台账left 或 usage_ledger 近似、
>   应退金额×r、收回 min(剩余,balance)+缺口告警、orders.refund_money 落库迁移）；B4 RecordUsage 扣减与
>   ledger 同事务（deductWithGrantsTx 抽取）；B5 randSuffix→crypto/rand（拒绝采样去偏置）+
>   BillingIndexMigrate（orders.order_no 唯一索引失败降级普通+告警、api_keys.key_hash 普通索引）；
>   B6 租管配额 clamp（quota_max_qps/concurrent 可调）+ 超管无 X-Tenant-ID 时显式拒绝
> - **批次 C ✅**：C1 MessageBubble fetch+blob（图片预取 watch immediate）；C2 tickets.ts 补发 mode；
>   C3 sync 端点缺省 fast + extension 显式 mode:"fast"（并加 AbortSignal.timeout 30s）；C4 审批链三改——
>   approve 异步入队（Background）、applyApprovedTextOverride 终稿按语言合并进载荷、applyModeOverride
>   approved 态仅 QA+feedback 短路（杜绝翻案）、runKBMatch 复用载荷只补缺失；C5 run 状态门槛
>   （仅 draft/queued/rejected）；C6 建单/建文件过 gateUsage + 三处入队 Background
> - **批次 D ✅**：D1 子进程统一执行器 subprocess.go（CommandContext 分级超时 FILEPROC_TIMEOUT_SEC
>   默认600s/子工具30s、Setpgid 进程组 SIGKILL 整杀、WaitDelay 排空、stdout/stderr 各4MB限量、
>   可取消闸门排队 acquireProcGateCtx、pdfdocx_*.docx 24h 清扫、JSON 不再靠 LastIndexByte 启发式；
>   四导出函数加 ctx 参数，engine/file.go 与测试同步更新）；D2 OnPhase 字段删除→WithProgressCallback
>   ctx 传递（ctxprogress.go）；D3 LLM Client 去全局45s Timeout，Transport 层 ResponseHeaderTimeout60s/
>   TLS15s/Idle90s；D4 workerLoop safeProcessJob recover（panic→MarkFailed）+ 引擎9处管线 goroutine
>   recoverPipeline 兜底；D5 readZipEntry UncompressedSize64 预检+LimitReader 64MB；D6 MarkFailed
>   单条 CASE WHEN 条件 UPDATE
> - 待办移交批次五/六：P1 十项与 P2 工程债未动，见下文原方案

---

## 批次一 · P0-A 安全止血

| # | 事项 | 问题（证据） | 改法 | 验收 |
|---|---|---|---|---|
| A1 | 匿名翻译四端点上锁 | `/api/chat(/stream)`、`/api/translate(/stream)` 无 authUser，匿名以租户1消耗配额（stream.go:85,176,208,276） | 四 handler 入口 `authUser==nil → 401` | 未登录 curl 得 401；登录后正常 |
| A2 | 上传孤儿文件 | os.Create(176) 在 gateUsage(208) 前，gate 拒绝分支不清理 | gateUsage 提前到落盘前；所有提前 return 分支 defer os.Remove | 余额不足上传后 UploadDir 无残留 |
| A3 | API Key 日配额双扣 | withTenant 中间件 authenticateAPIKey 触发 TouchAPIKey(admin_openapi.go:645)，handler 再调一次 → 每次 +2 | 中间件层只解析不计数（拆 NoTouch 变体），计数收敛 handler 层一处 | DailyCallLimit=N 恰放行 N 次 |
| A4 | 邀请码 TOCTOU | register.go:180 先查 Used → :194 才 MarkUsed，两步无原子性 | MarkInviteCodeUsed 改条件 UPDATE `WHERE used=0` + RowsAffected 判定 | 并发同码注册仅一人成功（补测试） |
| A5 | 邀请奖励铸造面 | BindReferral 即发 30万 token（register.go:294-308），防护仅 regGuard+事后告警 | 发放前校验被邀人邮箱已验证（email_verify 关闭时维持现状） | 未验证邮箱不触发 GrantTrialStack |
| A6 | mock 支付默认放行 | pay.go:186 `v != "" && v != "mock"` → 空配置放行 simulate | 判定改 `pay_mode != "mock"` 即拒（notify 同口径） | 未配置 pay_mode 时 simulate/notify-mock 均 403 |
| A7 | JWT 密钥双源 | iam/auth.go:28 与 auth/auth.go:17 两处硬编码默认值 | 删除 auth 包死变量 Secret（确认无引用后），单一事实源 iam.jwtSecret | grep 硬编码默认密钥零命中 |

## 批次二 · P0-B 资损修复

| # | 事项 | 问题（证据） | 改法 | 验收 |
|---|---|---|---|---|
| B1 | ExpirePackage/watchdog 丢更新 | packages.go:390-401 读-改-写整体覆盖 permissions；watchdog.go:237,252,258 同款 | 摘除/提醒改 json_set 单语句只动 package_code/expires/notified 三字段 | 并发「到期扫描×购买」句数不丢（补并发测试） |
| B2 | MarkOrderPaid 半失败态 | billing.go:573-639 多步写无事务；GetPackage 失败=收钱零发放不可重试；:621 兜底 Charge 错误被弃 | 全函数 BEGIN IMMEDIATE；payments 移权益发放后；兜底失败必须 CreateAlert | 注入 GetPackage 失败→回滚 pending 可重试 |
| B3 | RefundOrder 口径【已拍板】 | 注释"已消耗不追讨" vs 实现余额不足拒绝退款（billing.go:724 vs :777-786）；全函数零测试 | **仅退剩余、按比例折算**：剩余率 r = 剩余token/发放总量。订阅单剩余 = SUM(quota_grants.left WHERE source='order' AND ref_id=订单)；非订阅单剩余 = 总量 − 该租户自 paid_at 起 usage_ledger.quantity 合计（跨单混池近似口径）。应退金额 = ROUND(amount_money×r,2)（r=0 不退款仅作废权益）；收回 = min(剩余token, 当前balance) 守卫式扣回，不足差额 CreateAlert 人工核对。订单置 refunded + 审计记录 r/退款额/收回量 | 补失败注入测试；花掉部分额度后退款成功且金额按比例 |
| B4 | RecordUsage 两段事务 | billing.go:186-195 扣减与台账 INSERT 分离 | 合并同一 IMMEDIATE 事务 | 注入 insert 失败→扣减回滚 |
| B5 | 订单号弱随机+无约束 | randSuffix LCG UnixNano 种子（billing.go:892-903）；orders/order_no、api_keys/key_hash 零索引零唯一 | randSuffix 换 crypto/rand；加唯一索引（先跑重复核对脚本） | 回调幂等测试过；EXPLAIN 走索引 |
| B6 | 租管自提配额 | handleTenantQuotaSave 入参零校验；tid 用 currentTenant 超管不带头误写租户1（billing_api.go:317-321） | 入参 clamp（QPS≤100/并发≤50/负归零）；tid 语义修正 | qps=99999 被收敛；超管默认上下文不动租户1 |

## 批次三 · P0-C 用户可感功能

| # | 事项 | 问题（证据） | 改法 | 验收 |
|---|---|---|---|---|
| C1 | 聊天产物下载必401 | MessageBubble.vue:109-141 裸 img/a 无法带 JWT ↔ stream.go:394 强制401 | 改 fetch+blob URL（复用 ticketDownload 模式） | 图片内联显示、下载可用 |
| C2 | 文件工单 mode 丢失 | tickets.ts:126-133 收下 mode 从不 append FormData ↔ tickets.go:212 FormValue("mode") | append('mode', meta.mode||'') | 选快速建单 t.mode=fast |
| C3 | 划译静默走 pro | normalizeTaskMode("")="pro"（:71-76）↔ sync 端点注释自称缺省 fast（:606）；插件不发 mode | sync 缺省 fast + extension 显式传双保险 | 划词秒级返回 |
| C4 | 审批终稿丢失/翻案 | tickets.go:622-624 写纯文本 FinalResult → Execute 重跑 runKBMatch(workflow.go:144) 新建 payload 覆写；gate 失败 flow.go 置 rejected | approve 改异步入队；runKBMatch 先 loadPayload；approved 态 gate 失败不改判 rejected（skipped+warning） | 审批员改稿进 TM；二次失败状态仍 approved/completed |
| C5 | run 重跑无门槛 | tickets.go:248-270 无状态检查置 queued → completed 可重复计费重跑 | 仅 draft/queued/rejected 允许 run | completed 工单调 run 被拒 |
| C6 | 建单绕闸+入队ctx | create/create-file 自动入队无 gateUsage（tickets.go:118-122）；r.Context() 口径不一 | 建单过 gateUsage；EnqueueTicketRun 用 Background | 超额建单被拒 |

## 批次四 · P0-D 稳定性

| # | 事项 | 问题（证据） | 改法 | 验收 |
|---|---|---|---|---|
| D1 | 子进程无超时【已拍板加固】 | pdfwrite.go:151/175/200 exec.Command+CombinedOutput 无超时；pdf2docx cv.convert 无超时；procgate 容量1 → 一挂死全站停摆 | 新 helper：CommandContext + Setpgid 进程组杀死 + WaitDelay + stdout/stderr 分别 LimitReader(4MB)；超时 extract/apply/legacy 600s、pdfwrite 300s（FILEPROC_TIMEOUT_SEC 可调）；procgate 支持 ctx 取消；extract 前清扫 >24h pdfdocx_*.docx；pdftotext/pdfinfo 加 30s | fake-sleep python 超时杀树+闸释放；pdfwrite_test 回归绿 |
| D2 | OnPhase 数据竞争 | text.go:164-171 引擎单例全局字段无锁读改写 | OnPhase 改 ctx 携带（WithProgressCallback） | go test -race 绿；进度不串台 |
| D3 | 45s 全局超时使 ctx120s 失效 | client.go:106 http.Client Timeout=45s vs :351 ctx 120s | 去全局 Timeout，Transport 层保 ResponseHeaderTimeout | 慢供应商 60s 响应不再伪超时 |
| D4 | goroutine 裸奔 | 全仓仅 bot.go:48 一处 recover；workerLoop(service/ticket.go:86) 无保护 | workerLoop/processJob 包 defer recover→MarkFailed；engine goroutine 同理 | 注入 panic→job=dead、进程存活 |
| D5 | zip 解压无上限 | docx.go:123-135 io.ReadAll 不查 UncompressedSize64 | 预检 + LimitReader(64MB)，超限降级 xlsx 对照表 | 高压缩比样本报错不 OOM |
| D6 | MarkFailed 竞态 | direct.go:90-102 SELECT 后独立 UPDATE 无事务 | 单条 CASE WHEN 条件 UPDATE | 并发巡检×标记状态机合法（补测试） |

## 批次五 · P1（P0 收口后排期）

1. Compensable 接通【已拍板】：FlowStep+Compensable（默认 kb_match/evals×2=true 其余 false）；GetFlow 基线改读 GetFlowSteps()；admin_flow/Workflow.vue/i18n 透传；重试 sleep 改 select ctx+jitter；state 记 retrying
2. 时间口径统一 UTC（存量兼容读）
3. 索引补齐 9 组（tickets/jobs/kb_entries.source_text/users.org_id/notifications.user_id 等）
4. 五个列表端点分页
5. tenant.Delete/GDPR 清单对齐 30 表
6. webhooks.secret AES-GCM 加密
7. BumpTmHit 按 stage 去重（消 6 倍膨胀，workflow.go savePayload×6）
8. stage_models TTL 缓存；cultureRules 锁外查询
9. QA 数字归一化比对
10. 熔断按 (provider,model) 维度 + 结构化错误类型

## 批次六 · P2 工程债

死代码清理（openapi.Middleware/kb.AddTerm/Index.Update/TicketsPanel.vue 等 12 处）· spa.go 复用 resolveSafePath · core.ts 头合并 + 全局 401 收敛 · i18n 补缺失 key（fb.workbench/users.colUser/org.typePlatform/common.refresh/en.roleAdmin）· MessageBubble 中文数据串判断改常量 · LangMultiSelect other 开关接通 · 部署指南 §六§九回收 · OpenAPI 文档字段对齐实现 · SDK 示例修正 · restore_drill 排除活库候选 · extension host_permissions 收窄/icons/超时/storage.local · CI/gitleaks/deploy.sh 入库/LICENSE

---

## 附：插队修复（用户报告，2026-08-26）

| # | 类型 | 描述 | 状态 |
|---|---|---|---|
| U1 | Bug | OpenAPI 文档右上角复制按钮遮挡 English 切换，删除 | 待办 |
| U2 | Bug | 邀请好友二维码页崩溃；二维码/产物下载报「需要授权」（疑似裸 img/a 带 JWT 端点同类问题） | 待办 |
| U3 | 特性 | 超管可配置邀请裂变全部参数：功能开关 / 受邀赠送 token 数量 / 邀请者永久 token 数量 | 待办 |
| U4 | Bug | 邀请链接应直达注册弹窗（预填邀请码），而非停留在登录页 | 待办 |
