# 端到端 UAT 缺陷清单与处置记录（2026-09-14）

> 来源：全仓字节级代码审查（4 路并行深读：计费交易 / 翻译引擎 / 前后端契约 / 安全多租户）+ 自动化 UAT 实测。
> 每条均经源码逐行复核（file:line 可查证），非二手结论。
> 处置状态：✅ 已修复（2026-09-14 本次）/ 🏷️ 定性为特性（保留）/ 📋 待办（列入 backlog）。
>
> **修复后回归（已实测）**：PG 主矩阵与 SQLite 矩阵均为——race 检测通过、API 67/67、
> 交易专项 216/216、Playwright **27/27 全过**（含修复的 mobile_uat 两条）；前端 vitest 61/61、tsc/build 通过。

## 一、UAT 基线（修复前实测）

| 维度 | 结果 |
|---|---|
| go test -race ./internal/...（PG 主矩阵闸门） | ✅ 通过 |
| 后端 API 主链路 UAT（PG / SQLite 双方言） | ✅ 67/67 × 2 |
| 后端交易/功能专项 T 套件（PG / SQLite 双方言） | ✅ 216/216 × 2 |
| 前端单测 vitest | ✅ 61/61 |
| Playwright 前端 E2E | ⚠️ 25 过 / 2 败（mobile_uat 用例落后于 Landing 改版，已修） |

## 二、P0（资金/接管级）——全部已修复

| # | 缺陷 | 位置 | 处置 |
|---|---|---|---|
| P0-1 | 欠费结算无差别清零付费订阅（kind='plan'）与永久余额，清零量不落流水无法追偿；且后台 KB 向量重建（无 abort 钩子）也可触发清零停服，用户无感知权益归零 | store/billing.go:192 / billing/sink.go:252 / engine.go:2142 | ✅ SettleExhausted 重写：事务内权威复核（TOCTOU 收口）+ 有界清零（消耗≤owed，试用/临期优先）+ 调整流水（charge_kind='settle'）；纯后台批次不再触发清零，仅告警留痕 |
| P0-2 | 部门管理员可接管租户管理员：子树校验 `target.OrgID>0` 才生效，org_id=0（注册/SCIM/建号默认值）整段被跳过——重置密码/改用户两处可越权；Delete 处口径正确，三处复制粘贴漂移 | api/auth.go:857 / :752（Delete :920 正确） | ✅ 两处改为：目标不存在 404、org_id≤0 一律拒绝，与 Delete 对齐 |
| P0-3 | 品牌子域登录链断裂 + JWT 裸奔：Login.tsx 仍 `/?token=<JWT>` 重定向，App.tsx（E3）对 token 参数「仅抹掉不消费」→ 配了品牌子域的租户登录必落到未登录页，且 JWT 进历史/Referer | api/auth.go:155 / Login.tsx:132 / App.tsx:30 | ✅ 后端跨子域登录改发一次性 sso_code（60s、单次消费、走 /api/auth/sso/exchange），不再返回裸 token；前端改跳 `/?sso_code=` |
| P0-4 | auto_charge=1 时任意租户管理员可自报任意大额 tokens 零支付即时入账（无渠道/金额校验）——生产误开即无限自充 | api/admin_billing.go:189 | ✅ auto_charge 仅对 super_admin 生效，租户管理员订单一律 pending 走支付/人工确认 |
| P0-5 | 敏感词合规闸可被 Unicode 绕过：仅 ToLower+Contains，全角字母/零宽字符插入/字间空格全部穿透「命中不进模型」承诺 | internal/sensitive/sensitive.go:97 | ✅ 两遍归一化检测：强口径（小写+NFKC+零宽剥离）+ 宽松口径（剥离全部空白）；词表同口径。实测：全角/零宽/空格混淆全部命中，正常文本无误报 |

## 三、P1（资金错误/安全/可靠性）

| # | 缺陷 | 位置 | 处置 |
|---|---|---|---|
| P1-1 | token 迁移：部分租户 Charge 失败仍置位全局 migrated 标记，存量句数余额凭空消失且永不重试 | billing/migrate.go:85 | ✅ 有失败不置位，下次启动幂等重试（成功租户句数镜像已清零，天然去重） |
| P1-2 | usage_ledger 三种语义混写（实扣/非强制期留痕/欠费留痕），退款消耗核算把未扣费留痕也算进去 → 少退用户钱 | store/billing.go:1538 / LogUsage / LogUsageBatch | ✅ 新增 charge_kind 列（charge/log/settle，补列迁移+建表同源），退款只认 `IN ('','charge')`；sink 欠费批次留痕改走 'log' |
| P1-3 | 欠费「复核→清零」跨事务 TOCTOU：并发扣减可插入，复核说够、清零时空被误判真欠费 | billing/sink.go:233-252 | ✅ 复核移入 SettleExhausted 同事务，不足返回 ErrSettleNotNeeded，sink 回插重试 |
| P1-4 | BootResume 无条件重置全部 running 任务（假设单实例），多实例下重启他方实例 → 双跑双扣费 | service/ticket.go:865 | ✅ 只回收无租约/租约陈旧（>120s 未心跳）任务；健康实例心跳 60s，可稳定区分死活 |
| P1-5 | 工单无并发互斥：runTicket 只挡 completed，同一工单可被两个 job 并发执行（双份翻译、FinalResult 互相覆盖、计费双倍） | service/ticket.go:230 / store/tickets.go | ✅ 新增 ClaimTicketForRun CAS（draft/queued/rejected → in_progress 原子认领），抢不到静默退出 |
| P1-6 | 供应商 5xx/401 不触发降级也不进熔断（只认 429/网络错误），5xx 风暴时每段直接失败 | engine/engine.go:1393 / llm/client.go:721 | ✅ 新增 isServerError（Code≥500 或 401）纳入降级链与熔断计数 |
| P1-9 | KB 模糊子串「整句采用」无重叠度校验（LIKE %子串% + 长度差≤30 即采用），包含查询子串的另一句话的标准译文被写进成品 | engine/engine.go:649 / kb/db.go:763 | ✅ 查询串须覆盖候选句 ≥50% rune 才整句采用，否则降级为例句参考 |
| P1-10 | pdftotext 裸 exec.Command 无 ctx/超时，绕过 subprocess 资源闸，恶意 PDF 可挂死 goroutine | fileproc/pdf.go:33 | ✅ CommandContext + 120s 超时 |
| P1-11 | Webhook SSRF：默认 client 跟随 302（校验后可重定向进内网/云元数据）+ 校验与拨号间 DNS rebinding TOCTOU | store/webhooks.go:233/278/464 | ✅ 统一 webhookHTTPClient：禁跟随重定向 + Dialer.Control 在真实连接目标上校验 IP |
| P1-12 | 验证码防爆破 check-then-act 竞态：分段锁 + Redis Get/Set 弱一致，并发脉冲可全读 attempts=0，10 分钟窗口近全空间穷举 6 位码 | api/auth.go:414 / email_verify.go:127 | ✅ per-key 互斥锁（vcodeLockOf）串行化「读→判→比→错计/消费」全流程（改密码 + 邮箱码两处） |
| P1-13 | caddy-ask 回环白名单失效：Caddy 同机反代下外部请求 RemoteAddr 也是 127.0.0.1，任何人可枚举租户子域 | api/tenant.go:655 | ✅ 回环放行仅对无 X-Forwarded-For 的直连请求生效，经代理必须命中 CIDR 白名单 |
| P1-14 | SSO 兑换码仅写 Redis（静默丢弃），「单实例+无 Redis」部署 SSO 登录整体失效 | api/sso.go:166 / vcode.go:29 | ✅ vcode 层统一进程内存回退（带 TTL+惰性清扫），Redis 不可用时 SSO/验证码全可用 |
| P1-15 | 充值 points×rate 无上限，int64 溢出可产生负 token/负金额订单脏数据 | api/pay.go:99 | ✅ 上限校验（2^40 积分 + 溢出防护），非法值 400 |
| P1-16 | 前端 KB/收款台 raw fetch 裸 `.json()`：不判 resp.ok、不触发 401 拦截，登录过期表现为静默失败 | api/kb.ts ×8 处 / billing.ts:187 / PlansP.tsx:247 | ✅ kb.ts 统一 fetchJSON 守卫（401→handleUnauthorized、非 2xx 抛错）；adminQRUpload 补守卫；PlansP 收款台补 API_BASE 拼接 |
| P1-17 | 分片上传 uploadId 内存新生成不落盘，刷新即全量重传，「断点续传」形同虚设 | api/kb.ts:123 | ✅ uploadId 按「文件名+大小」持久化 localStorage，合并成功即清除 |
| P1-18 | 同面板裸 token 与积分混排（fmtNum/fmtPoints 并存），违反 S1「token 裸值不外露」承诺 | selfservice.tsx:69-71/159 | ✅ token 余额行统一 fmtPoints；句数近似行保留句数口径 |

## 四、定性为特性（按拍板保留，不修改）

| # | 内容 | 依据 |
|---|---|---|
| F-1 | LLM 失败重试/对冲败者/流式回退的 token 全部向用户计费 | 实扣=实际 token 换算积分，重试成本属供应商侧风控范畴 |
| F-2 | 文件翻译部分失败：okCount>0 即整单 completed，失败文件已扣 token 不退 | 同上：计费口径为「实际消耗 token→积分」，非整单交付口径 |

## 五、待办 backlog（P2/P3，未在本次修复范围）

1. **对账盲区**：reconcile 只勾稽 orders↔payments，账本级漏扣/多扣（sink 缓冲>5 万条丢弃最旧、LogUsage 口径混写）无监控——建议补 `SUM(ledger.cost where charge_kind='charge')` vs 双桶扣减的勾稽规则。
2. **口径漂移**：PointsFromTokens 向上取整 vs PackageTokenAmount 截断 vs TokensToFen 四舍五入；前端 pointsOf 补位规则后端无——统一取整方向。
3. **OpenAPI 建单无估算预检**（余额 1 token 可建 40MB 大单，中途触发欠费链）；并发名额 HTTP 返回即释放（TOCTOU）。
4. **遗留口令哈希**：无盐 SHA-256/固定盐仍被接受（B10 透明升级已做，观察清零后移除）；JWT_SECRET 一钥多用派生静态加密密钥，无 key-version 轮换。
5. **SSO state 不绑 provider、无 nonce/PKCE**；A 提供方 state 可用于 B 回调。
6. **上传仅扩展名校验**（无 magic bytes），风险转嫁给 fileproc 子进程解析器。
7. **SPA 前缀检查缺路径分隔符**（dist* 兄弟目录可命中）；品牌 brand_links url 无 scheme 白名单（javascript: 放行，前端渲染侧风险）。
8. **限流/日配额原子性**：rate_limits 读改写非原子（PG 并发丢更新）；CheckDailyQuota 查询失败 fail-open。
9. **占位符保护未覆盖文件批量路径**（D19 缺口，QA 事后告警不阻断）；`<sN>` 标记注入可能错位配对。
10. **兜底模型供应商错配**（非火山主路时 HunyuanFallbackModel 大概率 404）；culture enforced 拦截=静默删语言。
11. **性能**：sink flush 持全局锁做 DB 查询；多 Sheet 写回 O(origs×rows) 且只替换首处；zip 全量读入内存。
12. **测试资产**：EstimateResp 类型落后于后端契约（缺 points_min/max 等）；工单详情轮询无异常兜底；登出未清租户上下文；admin_ping 群发而非选中 webhnote（admin_webhooks.go:140）。

## 六、验证方式

```bash
bash scripts/uat/run_uat.sh          # PG 主矩阵（本次修复后回归，见文末结果）
DB_DRIVER=sqlite UAT_SKIP_RACE=1 bash scripts/uat/run_uat.sh   # SQLite 快跑
cd frontend-react && npx vitest run && npx tsc --noEmit && npm run build
cd backend-go && go build ./... && go test ./...
```
