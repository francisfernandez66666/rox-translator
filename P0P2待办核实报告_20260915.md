# P0/P1/P2 待办核实报告（2026-09-15）

> 背景：全场景 UAT 调研（329 断言 + go test -race 28 包全绿）后输出的待办清单，
> 本报告对除「真实支付渠道联调」外的全部条目做**代码级逐项核实**（以代码为准，
> 文档仅参考），修正初版结论中的 2 处误判，并给出证据与处置建议。
> UAT 执行记录：A/B 主链路 67 + T 交易专项 235 + Playwright 27 + vitest 61，全绿；
> T37-subtree-still-ok 失败定位为 UAT 脚本自身 bug（body 内联转义漂移），
> 已修复 `scripts/uat/api_uat_txn.sh:771` 并两轮回归 235/235。

---

## 一、核实结论总表

| 编号 | 条目 | 初版结论 | 核实结果 | 证据 |
|---|---|---|---|---|
| P0-2 | orgs 同级同名唯一约束缺失 | 缺陷 | **真实（已复现）** | 见下 |
| P1-1 | 多实例横向扩容未闭环 | 缺陷 | **真实（半闭环）** | 见下 |
| P1-2 | 备份/容灾未定时化 | 缺陷 | **不成立（误判，已具备）** | 见下 |
| P1-3 | 压测无量化基线 | 缺陷 | **部分成立** | 见下 |
| P1-4 | 审计日志可篡改 | 缺陷 | **真实** | 见下 |
| P2-1 | 无群通知集成 | 缺陷 | **不成立（误判，已具备）** | 见下 |
| P2-2 | 无报表导出 | 缺陷 | **部分成立（审计/GDPR 已有，运营报表缺）** | 见下 |
| P2-3 | 桌面端无签名公证 | 缺陷 | **真实（有意留白）** | 见下 |
| P2-4 | 前端多语言打包 e2e 缺失 | 缺陷 | **部分成立** | 见下 |
| P2-5 | SDK 无版本化发布管道 | 缺陷 | **真实** | 见下 |

---

## 二、逐项证据与处置

### P0-2 orgs 同级同名唯一约束缺失 —— 真实（唯一 P0 级代码缺陷）

**证据链**：
- 建表（SQLite）：`backend-go/internal/store/store.go:379` —— `CREATE TABLE IF NOT EXISTS orgs (...)` 后仅建普通索引 `idx_orgs_tenant(tenant_id, parent_id)`（store.go:389），**无唯一约束**；
- PG 建表同构无唯一索引（实测 `\d orgs` 仅有 `orgs_pkey` + `idx_orgs_tenant`）；
- 写入路径：`backend-go/internal/iam/store.go:298` `CreateOrg` 裸 INSERT，不预查重名；
- API 层拦截依赖 `store.IsUniqueViolation(err)`（`backend-go/internal/api/orgs.go:265-268`）→
  **无约束则永不触发**，「创建失败：同级下已存在同名组织」为死代码；
- **实机复现**：同一租户根下成功创建两个「T37研发部」并存（org id=1 与 id=2，PG 方言）。

**影响**：组织树出现同名节点（前端树渲染/预算下钻按 id 仍可用，但用户辨识与按名引用混乱）；无资金影响。

**处置建议（30 分钟）**：
1. 迁移层补索引（双方言）：`CREATE UNIQUE INDEX IF NOT EXISTS idx_orgs_unique ON orgs(tenant_id, parent_id, name)`；
   - 存量脏数据需先去重（参照 `BalanceAccountMigrate` 的「先去重后建唯一索引」模式，`backend-go/internal/store/billing.go:122-128`）；
   - SQLite 改表走 `rebuildTableWithTripleUnique` 同款重建路径（kb/db.go 已有先例）。
2. 迁移幂等挂在 `Store.New` 迁移链（与 BalanceAccountMigrate 并列）。
3. UAT 补断言：连续两次创建同名组织，第二次必须返回「同级下已存在同名组织」。

---

### P1-1 多实例横向扩容未闭环 —— 真实（半闭环，注释已自认）

**证据链**：
- 已闭环部分：QPS/并发限流有 Redis 路径（`backend-go/internal/billing/quota.go:25-38`，
  并发信号量 `concurrency.New("quota:conc:"+tid, n, redis.Get())` 跨实例共享；QPS 走 Redis 秒窗 INCR）；
- 未闭环部分：
  1. **UsageSink 影子余额为进程内存缓存**（`backend-go/internal/billing/sink.go:85-99`）：
     `shadow`/`shadowOk` map + `InvalidateShadow(tid)` 仅清**本进程**缓存。充值路径
     （`backend-go/internal/api/register.go:572` 等处）调用后，**其它实例的影子余额仍是旧值**
     → 多实例下「已充值仍提示组织 token 已耗尽」——这正是他们修过的单实例 bug
     （sink.go:86-89 注释自述）的多实例版，错误翻译（abort）会误中断正常请求；
  2. watchdog 每日备份/订阅到期扫描/工单留存扫描（`backend-go/internal/api/watchdog.go:155-210`）
     为各实例独立执行 → 多实例重复备份、重复推送到期提醒；
  3. 观察性（SLO 采样、内存监控）同为实例本地状态。

**结论**：架构事实上锁定单实例部署；「Redis 横向扩容改造」仅完成了限流一半。
**处置建议**：影子余额改 Redis 读写（或 TTL 短缓存+余额变动经 Redis pub/sub 失效广播）；
watchdog 任务加 Redis 分布式锁（`internal/infra/distlock` 已有现成实现未接线）。

---

### P1-2 备份/容灾未定时化 —— **不成立（初版误判，修正）**

**证据链（系统能力已完备）**：
- 服务内置自动定时备份：`backend-go/internal/api/watchdog.go:155-170` ——
  ticker 按 `backup_hours`（默认每日）执行 `runBackup`，**启动即备一次**；
- 双方言备份实现：`backend-go/internal/store/backup.go` —— SQLite `VACUUM INTO`
  在线一致性快照；PG `pg_dump --format=custom`（watchdog.go:408 实测输出
  `tm_20260915_062513.bak.dump`，保留最近 7 份）；
- 异地容灾钩子：`backup_remote_cmd` system_config（watchdog.go:412-416，
  `{path}` 占位符接 rclone 等外置命令，失败仅告警不阻断）；
- 恢复演练：`deploy/restore_drill.sh` 取最新备份做完整性检查+行数抽样；
  本机侧拉取 `deploy/fetch_backups_local.sh`（crontab 每日 10:00）。

**遗留（流程性，非系统能力）**：恢复演练未纳入 systemd timer / 定期演练日历，
属运维流程欠账；异地钩子是否已在生产配置 `backup_remote_cmd` 需上线时核对。
**处置建议**：把 restore_drill 挂 systemd timer 每周跑 + 演练失败进 S9 告警收口。

---

### P1-3 压测无量化基线 —— 部分成立

**证据链**：
- `deploy/loadtest/k6.js:28` 有 `thresholds: { app_errors: ['rate<0.10'] }`（错误率<10%）
  与 `check status==200`；
- 缺：P99 延迟、吞吐（RPS）、单位成本等**量化验收阈值与历史基线记录**（loadtest
  报告未入库、无与《容量预告与超卖预案》数值对齐的通过/失败判定）。

**处置建议**：k6 补 `http_req_duration: ['p(99)<N']` 与目标 RPS 阶梯；每轮压测结果
归档到 deploy/loadtest/results/，作为发布闸门的可选检查项。

---

### P1-4 审计日志可篡改 —— 真实

**证据链**：
- `audit_logs` 为普通表（store.go 建表，无 append-only/WORM/hash 链）；
- 写入失败**静默忽略**：`backend-go/internal/store/audit.go:43` `_, _ = db.Exec(...)`；
- 保留清理仅防膨胀：`startAuditRetention`（server.go:141，默认 365 天）按保留期 DELETE；
- 拥有 DB 写权限者（DBA/超管经 SQL）可直接 UPDATE/DELETE 审计行，事后不可发现；
- GDPR 导出（`backend-go/internal/store/gdpr.go` ExportTenantData 含审计）导出的是
  **可被先篡改的副本**。

**影响评估**：对试运营阶段（单机自建 PG、超管即老板）为**低风险**；对企业客户
合规诉求（SOC2/等保）将成为硬伤。
**处置建议（分期）**：近期——审计写入失败改记日志+告警（不再静默）；中期——
`hash = sha256(prev_hash + row)` 前后链（同租户序），检测点放超管审计面板；
远期——每日审计归档到对象存储（不可覆盖桶）+ WORM。

---

### P2-1 无群通知集成 —— **不成立（初版误判，修正）**

**证据链**：`backend-go/internal/api/bot.go` 已实现**企业微信群 + 钉钉群**机器人通知
（system_config：`wecom_webhook_url` / `dingtalk_webhook_url`，未配置静默跳过、
异步 fire-and-forget）。接入点已覆盖：
- 模型熔断 / 余额耗尽等 critical 告警；
- S9 Alertmanager 告警收口（s9_alerts.go：firing 落库+推群）；
- 订阅到期摘除（watchdog）、S7 触达序列「运营群留痕」（watchdog.go:322）。

**真实缺口（收窄后）**：Slack/Teams 海外渠道；通知事件不可配置订阅（当前为代码内
固定接入点）。
**处置建议**：出海前补 Slack 兼容 webhook（同 bot.go 模式加一个 provider 即可）。

---

### P2-2 无报表导出 —— 部分成立

**已有**（初版漏判）：
- 审计日志 CSV 导出：`frontend-react/src/components/admin/panels_a.tsx:88`
  （`/api/system/audit?export=csv` 前端触发下载）；
- 租户全量数据导出（GDPR）：`backend-go/internal/store/gdpr.go`
  ExportTenantData（用户脱敏/订单/发票/用量/审计/KB 全量 JSON）。

**真实缺口**：用量/成本/漏斗等**运营分析报表**无 CSV/定时报表（usage 仅有面板展示与
usage_ledger 明细查询，无聚合导出端点）。
**处置建议**：超管用量面板加 `?export=csv`（后端聚合查询已现成，只缺导出格式化）。

---

### P2-3 桌面端无签名公证 —— 真实（有意留白）

**证据**：`build.sh:13-14` 注释明示「ad-hoc 签名仅限本机；对外分发需 Apple Developer ID
签名 + 公证（notarytool），届时替换下方 codesign 参数即可」；build.sh:91 为
`codesign --force --deep --sign -`（ad-hoc）。
**处置建议**：若 .app 对外分发，购买 Developer ID 后按注释切换；若仅内部使用可维持现状。

---

### P2-4 前端文件翻译多语言打包 e2e 缺失 —— 部分成立

**已有**：后端 A16 UAT 断言 docx→双段翻译→xlsx 对照交付（scripts/uat/api_uat.sh:163-190）；
前端 e2e 有文件工单上传建单（`frontend-react/e2e/dark_admin_upload.spec.ts:96`
setInputFiles txt）。后端多目标语言逻辑有 UAT 覆盖。
**真实缺口**：前端「多目标语言选择 → 翻译完成 → 打包下载（zip）」无 Playwright 断言
（前端产物列表渲染与下载按钮仅被 P4 工单页截图用例浅覆盖）。
**处置建议**：pixel_uat.spec.ts 补一条「上传 txt + 双目标语言 + 等完成 + 点击下载」链路。

---

### P2-5 SDK 无版本化发布管道 —— 真实

**证据**：
- `sdk/typescript/package.json`：version 1.0.0、无 publish 脚本、未上 npm registry（私仓引用）；
- `sdk/python/translator_sdk.py`：单文件直发，无 setup.py/pyproject、无 twine；
- `sdk/java`：同未上 Maven。

**影响**：客户集成靠「拷文件」，版本升级无通道、无 CHANGELOG、无语义化版本承诺。
**处置建议**：注册 npm/PyPI 账号 → tsc 构建产物发布 `@langcross/sdk`；Python 打
pyproject 后 twine；Java 可暂缓（国内客户量小）。CI 加 tag 触发发布。

---

## 三、修正后的待办优先级（除支付外）

| 优先级 | 条目 | 工作量预估 |
|---|---|---|
| **P0** | orgs 唯一索引（先去重后建约束，双方言迁移 + UAT 断言） | 0.5h |
| P1 | 多实例：影子余额迁 Redis + watchdog 接 distlock | 1-2d（规模化前必做） |
| P1 | 压测基线量化（P99/RPS 阈值 + 结果归档） | 0.5d |
| P1 | 审计防篡改分期（近期：写入失败告警） | 近期 1h / 远期 2d |
| P2 | restore_drill 挂 systemd timer + 失败进告警 | 0.5h |
| P2 | 用量报表 CSV 导出端点 | 0.5d |
| P2 | 前端多语言打包下载 e2e 一条 | 1h |
| P2 | SDK 发布管道（npm/PyPI） | 0.5d |
| P2 | Slack/Teams 渠道（出海前） | 2h/渠道 |
| P2 | .app 公证（如对外分发） | 0.5d + $99/年 |

---

## 四、附：本次 UAT 执行与修复记录

- **执行环境**：PG 16 + pgvector（生产同构方言），mock LLM（OpenAI 兼容）；
- **结果**：go test -race 28 包全过；A/B 67；T 235；Playwright 27；vitest 61 —— 0 FAIL；
- **修复**：`scripts/uat/api_uat_txn.sh` T37-subtree-still-ok —— 内联 `\"` 转义 body
  在 `ck "$(post ...)"` 双层命令替换下偶发解析漂移致 400「请求格式错误」。改为
  body 先落变量（T37BODY）再传参；后端子树权限逻辑经手工复现确认正确（子树内放行
  + org_id=0 拒绝成对成立）；两轮全量回归 235/235。
- **发现的新缺陷**：P0-2 orgs 唯一约束缺失（复现过程见上，修复方案已给）。

## 五、落地记录（2026-09-15，除支付渠道外全部完成）

终局验证矩阵（PG 主矩阵）：`go test -race ./internal/...` 28 包全绿；
A/B 主链路 **67/67**；T 套件（含新增 T38/T39）**250/250**；
Playwright e2e **28/28**（含新增 M1）；前端 vitest **61/61**；`tsc --noEmit` 通过。

| 条目 | 状态 | 实现要点与证据 |
|---|---|---|
| P0 orgs 唯一索引 | ✅ | `store/orgmigrate.go`（去重+`idx_orgs_sibling_unique`，挂 `store.go` 迁移链）；`orgmigrate_test.go` 锁 API 冲突→`IsUniqueViolation`→400 路径 |
| P1 影子余额多实例 | ✅ | `billing/sink.go` 5s TTL 重播种 + nil 图守卫；`billing/shadowsync.go` Redis 失效广播（RPUSH/BLPOP）；`store/balancehook.go` 全资金变动点 notify（Charge/Refund/Settle/Grant/Claim/Reward 等 11 处） |
| P1 watchdog distlock | ✅ | `api/watchdog.go` `runExclusive(key,ttl,fn)`：watchdog-check / db-backup / subscription-scan（三扫描归组）/ ticket-retention；内存泄漏巡检刻意保持实例本地 |
| P1 压测基线量化 | ✅ | `deploy/loadtest/k6.js` `P99_MS/ERR_RATE_MAX` 环境变量化阈值 + `thresholds_passed` 出参；`run_capacity_matrix.sh` 多台阶矩阵；结果 JSON+CSV 归档 `deploy/loadtest/results/` |
| P1 审计写失败告警 | ✅ | `store/audit.go` `noteAuditWriteFailure`（原子计数 + 同 action 1 分钟限速日志）；`api/metrics.go` 暴露 `translator_audit_write_failures_total` |
| P2 restore_drill 定时化 | ✅ | `deploy/restore_drill.sh` 双方言（PG 恢复到 `translator_drill_<ts>` 临时库校验+清理）+ 失败回投 S9 告警口；`systemd/translator-restore-drill.{service,timer}`（每周日 03:30，Persistent） |
| P2 用量报表 CSV | ✅ | `store/billing.go` `UsageLedgerForExport`（日期区间+10 万行硬顶，新增 `charge_kind` 映射）；`api/admin_billing.go` `?export=csv`（非超管脱敏/系数与面板同口径）；`panels_a.tsx` 用量看板导出按钮；UAT T38×8 |
| P2 多语言打包下载 e2e | ✅ | UAT T39（OpenAPI 单文件×双语→行内 pre-zip，契约按 `service/ticket.go` `zipOutputs` 实况修正）；Playwright `e2e/multilang_zip.spec.ts` M1（UI 勾选双语→zip 字节断言）；`run_uat.sh` 增 `PW_TARGET` 定向开关 |
| P2 SDK 发布管道 | ✅ | `sdk/python/pyproject.toml`+`__version__`；TS `publishConfig/files/prepublishOnly`（并修潜伏缺陷：devDep 缺 `@types/node` 致 tsc 从未干净编译）；`sdk/CHANGELOG.md`；`scripts/release-sdk.sh`（三端版本同步校验→构建→venv 安装冒烟→npm pack 预演→`--publish` 显式开闸；已跑至 dry-run 全绿） |
| P2 Slack/Teams 渠道 | ✅ | `api/bot.go` `slack_webhook_url`（text）/`teams_webhook_url`（MessageCard）；`admin_packages.go` 配置读写键；后台渠道表单两项；`bot_test.go` 四渠道 payload + 关闭零请求双单测 |
| P2 .app 公证 | ✅ | `build.sh` [4/4] 段：`APPLE_DEVELOPER_ID` 触发 Developer ID+hardened runtime 签名，`APPLE_ID/APPLE_APP_PASSWORD/APPLE_TEAM_ID` 齐备则 notarytool 公证+staple 装订；未配置渠道明示告警不静默；缺省仍 ad-hoc |

### 过程性发现（已一并修复）
- **T39 首轮 FAIL 根因**：并非竞态——单文件多语言工单在 `service/ticket.go` 已把
  各语言产物 `zipOutputs` 预打包为唯一 `result_path`（一行 ticket_file、
  `result_ready=1` 即正确契约）；openapi `download` 的“多行打 zip”仅适用多文件工单。
  断言按实况修正，语言覆盖由 zip 条目名（`_en/_ja`）把关。
- **M1 两轮失败根因**：TDesign Select 勾选交互（选项点击在残留隐藏 popup 上不生效、
  弹窗未收口时二次 click 变 toggle）；最终「fill 过滤 + Enter + 可见项点击兜底 +
  tag 复核」序列稳定通过。
- **TS SDK 从未干净编译**：`sdk/typescript` 缺 `@types/node`，`tsc` 对 `require/Buffer`
  直接报错——发布管线构建步骤使其暴露并修复（属既有缺陷，非本次引入）。
