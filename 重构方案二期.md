# 重构方案（二期）：翻译工单队列 · Gate 闸门 · OpenAPI 计费闭环 · 通知中心

> 版本：v1.0（2026-08-21）
> 状态：已评审通过，按批次执行
> 前置：一期六批次已完成（见 `重构方案.md`）

---

## 一、背景与目标

一期落地了四级角色、IAM 子系统、知识库包优先级链与计费门控。二期解决「落地用户级别」的四个缺口：

1. **大规模翻译缺载体**：对话框逐句翻译不适用于新语言冷启动与批量文件场景，需要工单模式（创建→入队→异步翻译→通知→下载）
2. **开放能力计费不闭环**：OpenAPI 翻译只记字符用量，未接入句数扣减与强制计费门控
3. **语言文化包无消费方**：`role=gate` 字段与安全句数据从未参与翻译流程
4. **Evals 占位 Key 空转**：全局 Key 为随机占位符时评估器仍发起必失败的 API 调用

## 二、架构定稿

### 2.1 三层解耦（各司其职）

```
┌──────────────── API 层（internal/api · openapi）────────────────┐
│ 只做：HTTP 协议 · 鉴权 · 参数校验 · 限流闸门 · 响应组装              │
│ 禁止：直接跑编排逻辑、绕过服务层写业务表                             │
└──────────────────┬────────────────────────────────────────────┘
                   │ 依赖 TicketService 接口（创建=入队，查询=账本）
┌──────────────────▼────────────────────────────────────────────┐
│ 编排器层（internal/orchestrator + service）                      │
│ 只做：五步流水线状态机 · 中间结果 payload · 步骤回调                │
└──────────────────┬────────────────────────────────────────────┘
                   │ 持久化经 store.Store（jobs/tickets/notifications 账本）
┌──────────────────▼────────────────────────────────────────────┐
│ 存储层（internal/store · iam）                                   │
│ 只做：SQL 与状态账本，不含业务决策                                 │
└──────────────────────────────────────────────────────────────┘
```

### 2.2 队列：direct 单实现 + 接缝设计（Kafka 为迭代补充）

```ini
WORKFLOW_QUEUE=direct   # 当前唯一实现：进程内异步执行器
```

- **接缝**：业务代码只接触 `Enqueue(job)` 与 worker 循环两个口子；
  未来触发条件成立时新增单个 kafka driver 文件即可，业务逻辑零改动
- **Kafka 触发条件**（写入本文档备查）：编排器拆独立集群 / 日均万级工单需削峰 /
  同一事件多组独立消费者 / 子系统异构语言生态
- **jobs 表状态账本**：id/type/payload/status(queued|running|done|failed|dead)/attempts/max_attempts/
  leased_by/leased_at/timeout_sec/error——两套驱动可共用，切换不丢数据

### 2.3 direct 执行器语义

| 项 | 行为 |
|---|---|
| 入队 | 创建即返回 ticket_no；job 落 jobs 表 status=queued |
| 执行 | goroutine 工作池 Reserve(queued 且租约过期) → 置 running+leased_at |
| 超时回收 | running 且 leased_at 超过 timeout_sec → 自动重置 queued（重试计数+1） |
| 失败 | attempts<max → 回 queued 延迟重试；超限 → dead（死信） |
| 启动回收 | 服务启动时把残留 running 全部重置 queued（崩溃自愈） |
| 并发 | WORKER_CONCURRENCY 配置（默认 2） |

## 三、Gate 语言文化闸门

### 3.1 概念模型（定稿）

```
安全词 = 知识库 Agent 的参考知识（非独立规则引擎）
├─ Agent 消费（主路径）：approved 条目按目标语言注入初翻/审校上下文
│   · style     → 写作规范指令
│   · forbidden → 用词禁令
│   · replace   → 强制用词映射
└─ 闸门（仅最后硬过滤，零 LLM 调用）
    · replace 对 → 自动替换
    · forbidden 命中 → 标记 gate-fail（locale_gate_enforced 开关，默认关）
```

### 3.2 数据模型（方案 B 结构化字段）

`kb_safety_phrases` 新增列：

| 列 | 类型 | 说明 |
|---|---|---|
| kind | TEXT DEFAULT 'style' | style(风格规范)/forbidden(禁用词)/replace(替换对) |
| replacement | TEXT DEFAULT '' | 替换词（仅 replace 类型使用） |
| status | TEXT DEFAULT 'approved' | pending/approved/rejected |
| source | TEXT DEFAULT 'manual' | manual(人工)/llm(LLM 投喂) |

存量数据默认 approved+manual，行为不变。

### 3.3 LLM 投喂流水线

```
LLM 挖掘产出 JSON → POST /api/admin/kb-safety/bulk-import（status=pending,source=llm）
                  → 安全句面板按 pending 筛选 → 人工通过/驳回
                  → approved 进入 agent 注入集与闸门过滤集
```

## 四、OpenAPI 句数闭环

- `/openapi/v1/translate` 成功后扣减「源句数 × 目标语言数」（复用 meterSentences 门控链）
- 强制计费开启且余额不足 → 返回专用错误码 `sentence_exhausted`
- 响应体携带剩余余额元信息；`/openapi/docs` 文档页同步更新

## 五、工单模式（异步化 + 隐私 + 下载）

### 5.1 权限

| 角色 | 可见范围 | 操作 |
|---|---|---|
| 普通用户 | 仅自己创建的工单（隐私隔离） | 创建/查看进度/下载 |
| 租户管理员 | 仅自己的工单（同上，涉及用户隐私不做跨人管理） | 同上 |
| 超级管理员 | 全部工单 | 查看/重跑/删除 |

### 5.2 流程

```
创建工单（文本粘贴或上传 docx/xlsx/pptx/pdf）→ 入队返回 ticket_no
→ worker 异步执行五步编排（步骤级进度写回）
→ 完成/失败 → notifications 站内信投递
→ 前台下载结果
```

### 5.3 结果交付格式

| 上传类型 | 交付方式 |
|---|---|
| docx / xlsx / pptx | 原格式回写（复用 ApplyDocx/ApplyXlsx/ApplyPptx） |
| pdf | 本期降级为 xlsx 对照表（PDF 原格式排版回写列入后续迭代） |
| 纯文本 | xlsx 对照表（源文+多语言列） |

## 六、通用通知中心

- `notifications` 表：user_id/title/body/ref_type/ref_id/read_at/created_at
- API：未读数 / 列表 / 单条已读 / 全部已读
- 前台铃铛组件：未读红点随 `/api/me/context` 轮询刷新
- 投递源：工单完成/失败（本期）；告警等事件后续统一接入

## 七、批次与验收标准

| 批次 | 内容 | 验收 |
|---|---|---|
| 零 | /metrics Caddy 反代修复；Evals 占位 Key 三步修复 | 点击指标直接打开文本；占位 Key 时评估自动跳过 |
| 一 | Gate 闸门（结构化字段+投喂审核+agent 注入+L2 默认关） | 含 forbidden 的译文触发标记；replace 自动替换；开关关闭时仅提示不拦截 |
| 二 | OpenAPI 句数闭环 | 强制计费开启时开放接口扣减余额；不足返回 sentence_exhausted |
| 三 | 三层解耦重构 + Queue 接缝 + direct 执行器 | handler 不再直调编排器；杀进程重启后中断工单自动续跑 |
| 四 | 工单异步化 + 隐私 + 下载 | 大文件工单立即返回 ticket_no；普通用户只见自己的；docx 工单下载原格式译文 |
| 五 | 通知中心 | 工单完成产生站内信；已读状态正确流转 |
| 六 | 前端 UI | 前台工单入口全流程可用；铃铛未读红点正确 |

## 八、待办登记（另行决策）

| 项 | 说明 |
|---|---|
| PDF 原格式排版回写 | 需引入 PDF 操作库单独评估 |
| Kafka driver 实现 | 见 2.2 触发条件；接口已预留单文件接入位 |
| 语言文化包挖掘 Agent | LLM 从历史语料自动产出 style/forbidden/replace 候选（投喂流水线已就绪） |

## 九、涉及文件清单

- 后端新增：internal/queue/queue.go · internal/queue/direct.go · internal/service/ticket.go ·
  cmd/server 内 worker 装配 · internal/api/admin_safety.go（安全句投喂/审核）·
  internal/api/notifications.go · internal/api/kb_index 相关沿用
- 后端修改：internal/engine/engine.go（gate 注入+硬过滤）· internal/orchestrator/workflow.go ·
  internal/api/tickets.go（权限重整+入队）· internal/api/admin_openapi.go（句数扣减）·
  internal/store/store.go（迁移：jobs/notifications/kb_safety_phrases 列）·
  internal/api/server.go（路由）· cmd/server/main.go（worker 装配+启动回收）
- 前端新增：src/components/Tickets.vue（工单入口）· src/components/admin/Audit.vue 已有 ·
  铃铛组件 src/components/Bell.vue
- 前端修改：ChatWindow.vue（身份栏旁加工单入口+铃铛）· admin/Kb.vue（安全句结构化录入）·
  AdminDashboard.vue（超管工单页注册）· src/api/tickets.ts · src/i18n/
