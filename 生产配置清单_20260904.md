# 生产配置清单（2026-09-04 全量代码审计修复后必配项）

> 用途：对照部署指南 §八，逐项核对/开启生产环境尚未启用的功能。区分「服务器一次性配置」与「system_config/环境变量配置」。
> ★ 2026-09-14 商业化开闸批次更新：积分制配置、Turnstile、敏感词闸、S9 监控三件套均已生产实配（见各节）；剩余待办仅 `billing_enforced`、异地备份、探活观察项。
> 结论前缀：🔴=必须（不上线有资损/合规/安全风险）｜🟠=强烈建议 ｜🟢=可选

---

## 一、服务器侧（root 一次性执行，43.108.86.140）

### 🟢 1. 真实邮件（SMTP）——✅ 已配置且验证可用

生产 systemd 已含 `MAIL_ENABLED=1` + `SMTP_HOST=smtp.mxhichina.com/SMTP_PORT=465/SMTP_USER=noreply@lexicorn.cn/SMTP_PASS/…/SMTP_FROM`，日志可见真实发信成功记录（`[smtp] 发送成功 to=… from=noreply@lexicorn.cn`，含注册验证码/体验额度/告警）。无需操作；如需改发件人调整 `SMTP_FROM` 后 `systemctl restart translator`。

```bash
# 如需核对/调整（drop-in：translator.service.d/mail.conf）
# grep -h '^SMTP_\|^MAIL_' /etc/systemd/system/translator.service.d/mail.conf /etc/systemd/system/translator.service
```

校验：注册新账号 → 收到验证码/欢迎邮件；后台发一条告警测试。

### 🔴→✅ 2. 异地备份（**拉取式本地副本**，09-14 落地）

方案调整：服务器不做任何改动（不装 rclone、不存任何本地凭据），由**运维 Mac 单向拉取**——
- 脚本：仓库 `deploy/fetch_backups_local.sh`，实体副本 `~/Backups/tools/`；rsync 最新 3 份 `tm_*.bak.dump` → `~/Backups/langcross/`，sha256 本地/远端比对校验，保留 7 份，幂等。
- 定时：本机 crontab 每日 10:00，日志 `~/Backups/langcross/fetch.log`。
- ★ 2026-09-15 恢复演练定时化（服务器上安装）：
  ```bash
  cp deploy/systemd/translator-restore-drill.{service,timer} /etc/systemd/system/
  systemctl daemon-reload && systemctl enable --now translator-restore-drill.timer
  ```
  每周日 03:30 自动执行 `deploy/restore_drill.sh`（PG：pg_restore 恢复最新备份到临时库 `translator_drill_<ts>` 校验后清理；SQLite：快照验行数），失败自动进告警中心；环境变量 `DRILL_BACKUP_DIR`、`DRILL_PG_ADMIN_DSN` 与 service 中运行时一致。
- 首拉已验证：3×51MB 副本 sha256 与生产一致（RPO 上限≈24h+拉取周期）。
- 已知边界：本机也丢失=同盘风险（可后补云盘同步该目录）；若需全自动云端异地，再启用下方 rclone 推送（可选）：

```bash
# （可选升级）服务器推 OSS/S3：rclone config 后 system_config 写入
#   backup_remote_cmd = rclone copy "{path}" oss:langcross-backup
```

校验：手动 `bash ~/Backups/tools/fetch_backups_local.sh` 输出 ✅；每月跑一次 `bash deploy/chain_drill.sh` 实测还原（指南 §十一-B）。

### 🟠 3. 探活地址（SELFCHECK_URL）

生产监听固定 127.0.0.1:8787，默认即正确；若日后改端口需同步：
`Environment=SELFCHECK_URL=http://127.0.0.1:<port>/status`。

### 🟢 4. 支付渠道（pay_mode）

`pay_mode=mock` 仍为默认。接真实微信/支付宝前需申请商户号（archive 遗留项），接通后：
- 后台 system_config `pay_mode=wechat|alipay`
- 配置微信/支付宝密钥（AES-GCM / RSA2 已在代码支持）

**USDT 收款（2026-09-15 新增，海外线 langcross.lexicorn.cn 专用）**：与 `pay_mode` 独立开关，境内主体严禁开通。
- 超管「计费与套餐→运营」USDT 分区配置，或直接写 system_config：`usdt_enabled=1`、`usdt_rate_fen_per_usdt`（人民币分/USDT，如 ¥7.2→720）、`usdt_chains=trc20[,erc20,bep20]`、`usdt_addr_trc20/erc20/bep20`（各链收款地址，须合法格式）、`usdt_confirmations_trc20/erc20/bep20`（确认阈值，TRON 默认 19）、`usdt_tail_enabled=1`（尾数对单）、`usdt_auto_settle`（M2 自动入账，**默认 0=仅人工核销，真链冒烟通过前不得开**）
- 链上端点环境变量：`USDT_TRON_BASE`/`TRONGRID_API_KEY`/`USDT_ETH_RPC`/`USDT_BSC_RPC`/`USDT_SCAN_INTERVAL_SEC`（详见《改造方案_USDT收款接入·双轨人工到自动_20260915.md》）
- 资金唯一口：入账经 `MarkOrderPaid`（幂等抢占）；`payments.tx_hash` 唯一索引——一笔链上交易只能核销一单（后台确认与自动对账共用该防线）

---

## 二、system_config 开关核对（后台「系统与运维」Hub（2026-09-15 Tab 精简前名为「系统设置」）或 psql）

| 键 | 现状 | 应设 | 说明 |
|----|------|------|------|
| `watchdog_selfcheck_restart` | 空 | **留空或 "1"**（本次已改默认开启） | 仅显式 `"0"` 才关闭。锁饥饿自愈根因 |
| `alert_email` / `alert_email_cc` | 空 | 填运维收件箱 | 需配合 SMTP（见§一.1）否则仍只写 alerts 表 |
| `usdt_enabled` | 空(=0) | 海外线 **1**，境内 **0** | USDT 收款总开关；开启需先配汇率+≥1 链地址 |
| `usdt_auto_settle` | 空(=0) | **保持 0**，真链冒烟通过后再开 | M2 链上自动入账；关闭时仅超管人工核销 |
| `usdt_rate_fen_per_usdt` / `usdt_addr_*` / `usdt_chains` | 空 | 海外线按 §一.4 配 | 汇率/各链收款地址/开放链 |
| `backup_remote_cmd` | 空 | rclone 命令 | 见§一.2 |
| `captcha_provider` | ✅ `turnstile`（09-14 生产点亮） | 保持 | site/secret key 存 system_config（secret 只写不回显）；注册链路顺序：格式→一次性邮箱黑名单→人机。⚠ secret 曾在会话明文出现，正式运营前建议 Cloudflare 轮换 |
| `registration_enabled` | 1 | 按需 | 0=关闭自助注册 |
| `email_verify_enabled` | ✅ 已开（SMTP 验证可用） | 保持 | 自助注册邮箱验证码强校验；一次性邮箱域注册/发码被 `disposable.go` 黑名单拦截 |
| `email_notify_enabled` | ✅ 1（09-14 确认） | 保持 | 到期/余额耗尽/S7 挽回礼包·老客回访邮件触达 |
| `wecom_webhook_url`/`dingtalk_webhook_url` | 空 | 建议填 | 熔断/余额耗尽群机器人推送 |
| ★ `slack_webhook_url`/`teams_webhook_url` | 空 | 出海客户按需 | Slack/Teams Incoming Webhook（与企微/钉钉同一事件源，后台「注册与触达」区配置） |
| `billing_enforced` | 0 | 1（★ 第四批 D-1：演示站试跑硬扣费→主站） | 强制计费（token 迁移已完成，等效开启） |
| `billing_markup_multiplier` | 1.5 | 1.5 | 成本均摊系数，勿动 |
| `backup_interval_hours` | 24 | 24 | 0=关闭 |
| `points_tokens_rate` | ✅ 300（09-14） | 保持 | 积分↔内部记账 token 汇率，仅服务端折算用；★ 2026-09-19 起前端换算器下线，任何接口不再下发汇率与 token 裸值 |
| `price_fen_per_million_tokens` | ✅ 29900（09-14） | 保持 | 对外定价锚（¥299/10000 分反推）；旧键 `price_fen_per_token` 已废弃 |
| `sensitive_gate_enabled` | ✅ 默认开（09-14） | 保持 | S8 敏感词兜底闸开关；词包 `/opt/translator/data/sensitive_words.txt`（mtime 热加载，见部署指南 §八-B7） |
| `disposable_email_domains` | 空 | 按需 | 一次性邮箱黑名单**增量**域名（内置 70+ 之外，逗号分隔，子域匹配） |
| `openapi_show_tokens` | （已废弃） | — | ★ 2026-09-19 起不再生效：OpenAPI 出参已全面收敛为纯积分口径，不再保留 balance_tokens 兼容字段 |
| `ops_policy`（运营策略） | 空 | 按需（2026-09-05 上线） | 平台级计费因子策略 JSON，超管后台「运营策略」面板可视化配置；租户覆盖走 `tenants.policy_config.ops_policy`，不设则回落平台默认（详见部署指南 §八-E） |

---

## 三、环境变量核对（secrets.env，0600）

| 变量 | 现状 | 应设 |
|------|------|------|
| `JWT_SECRET` | 已随机 | 保持（勿泄露） |
| `ADMIN_INIT_PASSWORD` / `ADMIN_TOKEN` | 已配 | 保持 |
| `MAIL_ENABLED`+`SMTP_*` | ✅ 已实配（mail.conf + 主 unit，noreply@lexicorn.cn） | 保持；改发件人改 `SMTP_FROM` 后重启 |
| `TRUST_PROXY_XFF` | 已设 1 | 保持（Caddy 反代必需） |
| `METRICS_TOKEN` | ✅ 已设 | 保持（★ 09-14 起被 Prometheus 实际消费：`/etc/prometheus/metrics_token` credentials_file，无 token /metrics 恒 401） |
| `ADMIN_TOKEN` | ✅ 已设 | 保持（★ 09-14 起双用途：支付回调/人工确认 + Alertmanager 收口 `?token=`，见部署指南 §八-B8） |
| `REQUIRE_PROD_SECRETS` | 0 | 建议 1（强制启动密钥齐全） |
| `SELFCHECK_URL` | 未设 | 默认即可；改端口时同步 |
| `SILICONFLOW_API_KEY`/`ONLINE_API_KEY` | 后台可配 | 后台「全局模型」优先，重启保留 |

---

## 四、本次代码审计已修复项（2026-09-05 已随 df664fb 上线，无需人工操作）

1. **扩展安全三件套**（`extension/`）：host_permissions 收敛为 optional + 按源授权；API Key 改 `storage.local`（不再上云同步）+ 回显掩码；content.js 协议白名单 + textContent 消毒。
2. **OpenAPI 规范修复**（`openapi.v1.json`）：此前 `/openapi/v1.json` 返回损坏 JSON（缺 `}` 致 root 提前闭合），已修复并补统一 Error schema + 错误码枚举。
3. **错误码统一**（`internal/errors`）：新增 OpenAPI snake_case 错误码常量，OpenAPI 层全部替换字面量；前端 `core.ts` 新增 `ApiError`/`bizErrorCode` 透传稳定错误码。
4. **watchdog 自愈默认开启**：`watchdog_selfcheck_restart` 默认生效（仅显式 "0" 关闭）。
5. **队列租约续期**（`queue`）：新增 `Heartbeat` 接口，worker 长任务每 60s 刷新租约；`RecoverStale` 两步回收（先清持有者再回队），防跨实例双跑。

## 四-B、S9 监控三件套（★ 2026-09-14 已上线，全回环）

| 组件 | 端口（仅 127.0.0.1） | 配置/内存帽 |
|------|---------------------|-------------|
| prometheus（apt 2.45） | 9090 | `/etc/prometheus/prometheus.yml` + `rules/saas.yml`（6 条）；MemoryMax=280M，TSDB 14d |
| alertmanager（v0.27.0 二进制 /opt/alertmanager-dist） | 9093 | `/etc/alertmanager/alertmanager.yml`（含 ADMIN_TOKEN，root:alertmanager 640）；96M |
| prometheus-node-exporter | 9100 | `/etc/default/prometheus-node-exporter`；64M |

告警流向：规则 firing → AM（group 10m/repeat 6h）→ `POST /api/alerts/alertmanager` → 平台告警中心 `kind=prom:*` + 运营群。详见部署指南 §八-B8（含冒烟验证法与重建清单）。

## 五、上线后验证命令

```bash
./deploy/deploy_check.sh https://langcross.lexicorn.cn        # 9 项一键验收
curl -s https://langcross.lexicorn.cn/openapi/v1.json | jq .  # 应为合法 JSON（此前损坏）
curl -s http://127.0.0.1:8787/metrics -H "Authorization: Bearer $METRICS_TOKEN" | grep ^translator_  # 探活正常
curl -s 127.0.0.1:9090/api/v1/targets | jq -r ".data.activeTargets[] | .labels.job + " " + .health"  # ★ S9：三个 job 全 up
systemctl is-active prometheus alertmanager prometheus-node-exporter                          # ★ S9
grep -c panic /opt/translator/log/translator.log              # 无 panic
ls -la /opt/translator/data/backups/                          # 备份在跑
ls <rclone远端>/langcross-backup/                              # 异地备份在跑
```
