# gofmt_gate_test.go 的临时豁免清单（★ #55 gofmt 零门禁，2026-09-22）
#
# 规则：
#   - 一行一个路径，相对 backend-go/；`#` 开头为注释；
#   - `path/to/dir/**` 表示整目录豁免（仅用于并行开发期该目录的未提交中间态）；
#   - **禁止**写 `internal/**`、`.` 之类整仓豁免——门禁的存在意义就是不让格式债静默积累；
#   - 已合格但仍留在本清单的条目会让门禁红灯（测试会提示删除），必须随手清理。
#
# 历史：2026-09-22 建立时全仓 0 不合格（HEAD 上曾有的 7 个文件已 gofmt 修复），故此处为空。
# 若同事并行大改某包导致中间态不合格，请只临时登记该包并在下一提交撤销。
#
# ★ 2026-09-22 缺口批 #55 曾临时豁免 internal/engine/** 与 internal/assist/engine/**（并行改造中间态）。
#   两个目录现已随同事提交恢复合格（`gofmt -l internal cmd` 为空），按门禁规则同步删除过期豁免。
#   下面留空即为「全仓零格式债」的目标态。
