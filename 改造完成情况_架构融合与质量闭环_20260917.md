# 改造完成情况：架构融合与质量闭环（2026-09-17）

> 依据《改造方案_架构融合与质量闭环_20260917.md》五项改造 + P0 清理，本文档逐项记录落地内容、
> 关键决策、与方案的偏差及原因、以及最终闸门实测结果。
> 文件位置即证据：每项均给出具体文件路径。

---

## 闸门实测汇总

| 闸门 | 基线（方案文档记录） | 本次实测 | 结论 |
|---|---|---|---|
| `go build ./... && go vet ./...` | 绿 | 绿（含 assist 新包） | ✅ |
| `go test -race -count=1 ./...` | 22 包全绿 | **全绿**（新增 assist 4 包，共 26 包） | ✅ |
| `scripts/uat/run_uat.sh`（PG 方言） | 67/0 · 308/0 · Playwright 34/1 **红** | **API A49/B67/合计 67/0 · T 308/0 · Playwright 33/33 全绿** | ✅ 唯一红点已消除 |
| `scripts/uat/assist_uat.sh` | 27 断言 | **32 断言全绿**（+5 融合契约断言） | ✅ |
| `frontend-react` `tsc --noEmit` + `vitest` | 79 用例 | **87 用例全绿**（+8） | ✅ |
| `frontend-react` `vite build` | — | 绿 | ✅ |

### 闸门排查记录（2 处环境陷阱，非代码回归）

首次跑主矩阵出现 `T 303/5 FAIL`，逐条取证后确认**均为本地环境产物**，并顺带修掉一个真实的脚本缺陷：

1. **T16（并发扣费压力）误报失败 → 已修脚本缺陷。** 取证方式：把 30 份响应体落盘后统计——
   实际为 3 成功 + 20 QPS 限流 + 7 并发限流，**语义完全正确**（不外溢、不双扣、余额不透支）。
   失败根因是 `api_uat_txn.sh` 的拒绝计数用了 BRE 写法 `grep -c 'a\|b\|c'`——`\|` 是 GNU 扩展，
   在非 GNU grep 下被当字面量，**静默返回 0 命中**，于是 `S+E==30` 恒假。
   **已改为可移植的 `grep -cE 'a|b|c'`**；同文件以外另一处同类写法（`assist_uat.sh:85` 的负向断言，
   在同样环境下会「永远通过」）一并修正。全仓仅此 2 处，已写入 `AGENTS.md` 第 7 条约定。
2. **T28（KB 分片上传）4 断言失败 → 环境残留污染。** 报 `rename ...: operation not permitted`，
   同时本机沙箱记录了 `~/Library/Application Support/能言/_uploads/**` 的 `file-write-unlink` 拒绝
   （多为上一批次遗留的旧分片目录）。清理残留后同一断言组 **7/7 全过**。

→ 结论：**T 套件最终 308/0**。两处均不涉及本次改造的代码路径。

---

## 改造 0（P0）：清理 e2e 生产站硬编码探针

**问题**：`e2e/_tmp_admin.spec.ts` 与 `e2e/_tmp_iframe.spec.ts` 写死生产站 `https://langcross.lexicorn.cn`，
且依赖 CI 不存在的环境变量 → 发布闸门长期带「必红」，钝化对真回归的敏感度。

**落地**：

| 处理 | 文件 |
|---|---|
| 移出闸门 + 加 `ASSIST_TOK` skip 守卫 | `frontend-react/e2e-manual/assist_admin_probe.spec.ts`（原 `_tmp_admin.spec.ts`） |
| 移出闸门 + 加 `ASSIST_IFRAME_PROBE` skip 守卫 | `frontend-react/e2e-manual/iframe_csp_probe.spec.ts`（原 `_tmp_iframe.spec.ts`） |

`playwright.config.ts` 的 `testDir: './e2e'` 天然不含 `e2e-manual/`，两文件均带文件头「为何手工」注释与运行方式。
另在 `AGENTS.md` 写死红线：**e2e 目录禁止写死外部域名/CI 不存在的环境变量**，生产探针职责归 `deploy/` 冒烟脚本。

**结果**：Playwright 从 `34/1 红` 变 `33/33 全绿`（移出 2 条手工用例，闸门零豁免）。

---

## 改造 1A：ai-assist 与主后端技术栈融合

**融合前**：独立 Go module（`module ai-assist` + 独立 go.sum）、独立构建、标准库 `log`、
管理台要求用户手工粘贴 Token、`ci.yml` 零处提及 assist。

**落地清单**：

| 项 | 内容 |
|---|---|
| 并入主 module | `ai-assist/{config,engine,llm,store,api,web,seed}` → `backend-go/internal/assist/*`；`ai-assist/cmd/server` → `backend-go/cmd/assist-server`；删除独立 `ai-assist/go.mod`、`go.sum`（依赖仅 modernc.org/sqlite，与主库同版本零冲突） |
| 单构建产物 | `internal/assist/web/embed.go`、`internal/assist/seed/embed.go` 用 `go:embed` 内嵌管理页与初始 seed；**不再需要投放 `web/`、`seed/` 目录**；`ASSIST_WEB`/`ASSIST_SEED` 仍可指向外置目录做覆盖（显式配置优先） |
| 统一观测 | `llm/llm.go`（3 处）、`engine/engine.go`（4 处）、`api/server.go`（4 处）的 `log.Printf` → `internal/observability` slog（JSON + trace_id）；llm 客户端 `markFail` 等内部函数同步改造 |
| ctx 贯穿 | `Engine.Respond / llmReply / LLMMode / LLMTest / markFail` 全部接 `context.Context`（网络调用可取消），调用点与测试同步更新 |
| 管理台 Token 贯通 | ① `internal/api/admin_assist.go` 新增 `/api/admin/assist/token`（仅超管，审计留痕，支持轮换）；② `cmd/assist-server/main.go` 只读方式（`mode=ro`）打开主服务 SQLite 读 `system_config.assist_admin_token` 并解密；③ `internal/assist/store` 新增 `ReadMainDBConfig`；④ 生效链：env `ASSIST_ADMIN_TOKEN`（优先/保底）→ 主库密文配置 → 内置默认值；⑤ `AssistP.tsx` 免手填（自动下发并注入同源 localStorage） |
| 加密能力下沉 | `internal/secret`（自 `internal/store/crypto.go` 下沉，见改造 2）,assist 直接复用，**不反向依赖业务存储层** |
| systemd 收敛 | `deploy/systemd/ai-assist.service` 改 `ExecStart=/opt/ai-assist/bin/translator-assist`（单二进制两形态），web/seed 投放步骤删除 |
| CI 补齐 | `.github/workflows/ci.yml` 新增 `assist` job（跑 `assist_uat.sh`，自起服务无外部依赖）；单测随 `backend` job 的 `./internal/...` 覆盖 |
| UAT 扩展 | `scripts/uat/assist_uat.sh` 27 → **32 断言**：新增 E2（内嵌管理页校验）、F1/F2（主库 Token 桥接可用性与错误 Token 拒绝）、F3/F4（env 优先级高于主库） |
| 单测 | `internal/api/admin_assist_test.go`（5 用例：鉴权/下发/轮换/审计）；`AssistP.dom.test.tsx`（4 用例：免手填注入/降级手填） |

**保持一致的设计**：**数据隔离保留**——assist 仍用独立 SQLite（`/opt/ai-assist/data/assist.db`），不并入业务库。
方案原文已澄清：割裂的是「仓库/构建/观测/鉴权/CI」五件事，不是数据库。

**已知边界（已写入《部署指南》§十三）**：主库 Token 桥接的前提是 `MAIN_DB` 指向主服务 SQLite；
**主服务切 PostgreSQL 后桥接不适用**，必须显式配 `ASSIST_ADMIN_TOKEN`；`/assist-api` 必须同源路径反代（父子页面共享 localStorage 才能自动注入）。

---

## 改造 2：store 归组、冻结与通用工具下沉（第一步 + 第二步）

**未做**：第三步物理拆包（`webhooks`/`referral` → `internal/biz/*`）——方案定为「评估后排期」，本次不做。

**落地**：

| 项 | 内容 |
|---|---|
| 通用能力下沉 | `internal/secret/secret.go`：静态加密（AES-256-GCM、`enc:v1:` 前缀、掩码判定）自 `store/crypto.go` 下沉；`store/crypto.go` 改写为**同名薄委托**，既有 90+ 调用点**零改动** |
| 引用方向锁定 | 基础包（`internal/secret`）**禁止** import `internal/store`（切断「读一个密文配置要拉起整个存储层」的入口） |
| 域索引 | 新增 `backend-go/internal/store/README.md`：按 6 大域（平台核心/认证授权/计费商业/知识库/工单产物/运营增长）列出 40 个非测试文件的职责，作为新文件归属依据 |
| 冻结规则 | 新增仓库级 `AGENTS.md` 并同步进 store README：**`billing.go`（2201 行）与 `kbpackages.go`（1371 行）只减不增**；`store.go` 不承接业务方法；新增列走 `db.EnsureColumns` 幂等补列；基础包不反向依赖 |

---

## 改造 3：LLM 供应商路由补测试

| 新增文件 | 用例数 | 覆盖 |
|---|---|---|
| `internal/llm/client_test.go` | 14 | `CallChatFallback` 429 → 等待 → `HunyuanFallbackModel` 降级重试；429 耗尽 + `onRateLimited` 回调次数；坏 JSON / 500 / 超时三分支；SSE 首块解析 |
| `internal/engine/stagemodel_crypto_test.go` | 2 | `stage_models` 密文（`enc:v1:`）密钥解密命中；空密钥继承全局 |
| `internal/engine/pickroute_test.go` | 4 | 静态分支全 0 权重取首个、单路由、`DynamicRouting` 开关两路径 |

全部走 `httptest` mock，不依赖外网。

---

## 改造 4：evals 不合格处置（打标 + 运营提醒）

**修复的原始缺陷**：`orchestrator/workflow.go` 的 `SaveRecord(..., "passed")` 状态**硬编码**，
评估分数落库后即无人消费的「死数据」。

**落地**：

| 层 | 内容 |
|---|---|
| 配置 | `system_config.evals_fail_threshold`（默认 60；`<=0` = 整体关闭处置仅记录）；`evals_alert_enabled`（默认开；置 0 = **只打标不打扰运营**，灰度静默观察用） |
| 打标 | `workflow.go` 新增 `applyEvalDisposition()`：`total < 阈值` → `SaveRecord` 状态 `failed`（修复硬编码）；payload 追加 `QualityFlaggedLangs` 与 `EvalNotified`；工单表 `quality_flagged=1` |
| 落库 | `store/tickets.go` 新增 `quality_flagged` / `qa_errors` / `qa_warnings` 三列 + `TicketQualityFlaggedMigrate()`（`db.EnsureColumns` 双方言幂等）+ `SetTicketQualityFlagged()`（单向置 1，不因其他语言达标而撤销复核提示） |
| 提醒 | 告警中心 `alerts`（`kind=eval_quality`，复用既有同 kind 幂等）+ `notify.Bots` 四渠道群机器人（企微/钉钉/Slack/Teams）；**同工单同语言同阶段限频 1 次**（`EvalNotified`），初翻/校对分别计数 |
| 明确不做 | 自动打回重译——gate 硬闸负责确定性规则重译；Judge 主观分重译易震荡烧钱，仅人工决策（方案原文口径，已写入代码注释） |
| 单测 | `orchestrator/evals_disposition_test.go`（6 用例：阈值边界 59.9/60/60.1/95、处置关闭、**提醒单独关闭**、限频幂等跨阶段、多语言独立打标、payload JSON 往返）；`store/ticket_quality_test.go`（6 用例：幂等迁移、打标回读、单向性） |

**与方案的偏差（1 处，已补实现）**：方案列了 `evals_alert_enabled` 但首版实现把它与阈值开关合并；
本次已拆分为独立开关（判定放在幂等标记之前——未真正提醒就不该记 `EvalNotified`，否则开关调回后该条永不再提醒），并补对应用例。

---

## 改造 5：用户侧 QA 报告透出

**问题**：QA 报告此前只写进**下载的 xlsx 对照表**，前端全仓 grep `qa_report` **零命中**——
付费用户在界面内感知不到质检投入。

**落地**：

| 层 | 内容 |
|---|---|
| API | `internal/api/tickets.go` 新增 `ticketQualityView` + `parseTicketQuality()`，工单详情响应增 `quality` 字段（`qa_report` / `eval_scores` / `review_eval_scores` / `quality_flagged_langs`）；列表接口经 `tickets` 表新列零成本透出 `quality_flagged` / `qa_errors` / `qa_warnings`（避免为渲染徽标解析整份 `final_result` payload） |
| 前端列表 | `TicketsPage.tsx` 新增「质检」列：`qa_errors>0` → 红点「N 项错误」；仅 warnings → 黄点「N 项提示」；`quality_flagged=1` → 「质检存疑」徽标 |
| 前端详情 | 详情抽屉新增「质检报告」区块：Pass/Errors/Warnings 汇总行 + Issues 明细表（语言/规则/级别/说明），后接各语言初翻与校对五维评估分 |
| 文案口径 | error 级强调「已自动重译后仍存在，建议人工复核」，避免误读为未处理 |
| i18n | `dicts.zh.ts` / `dicts.en.ts` 双语同步补齐 |
| 测试 | `TicketsPage.qa.dom.test.tsx`（4 用例：徽标三分支 + 质检区块渲染） |

**与方案的偏差（1 处）**：方案设想 `qa_report`「在 FinalResult 解析时一并返回」；
实现改为**独立解析函数 + 独立 `quality` 响应字段**，不污染既有工单详情结构，
且旧数据（非 JSON / 无 `qa_report`）解析失败时返回零值不报错。

---

## 遗留与后续

- **改造 2 第三步**（`webhooks`/`referral` 试点拆 `internal/biz/*`）与 **改造 1B**（`/health` 接 Alertmanager 黑盒、
  assist SQLite 定时备份）按方案留待评估后排期。
- `internal/openapi`、`internal/observability`、`internal/infra/ratelimit` 仍无测试文件（UAT 兜底覆盖）。
- assist 管理页 `admin.html` 仍为单文件内联 JS（方案定为 B 阶段再考虑 React 化，当前 iframe 方案可用）。
- 融合后二进制名由 `assist-server` 变更为 `translator-assist`，**首升需同步改 systemd unit 并删旧文件**（已写入《部署指南》）。
