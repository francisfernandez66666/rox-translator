# 能言 SaaS 平台 · 部署指南

> 更新：2026-09-20 ｜ 架构：Go 单二进制 + React + LangCross 自研组件库前端 + PostgreSQL + pgvector
> ★ 2026-09-20（bd34c36 批次，用户指示部署主站）：落地页四项反馈（质量数字口径 qs3「质量可验证」/qs4「降本 80%–90%」+ WordSwap 加载动效放大居中 + 落地页 LangSelect 与首访浏览器语言自动检测）+ AI 销售助手复合意图让位修复。主站前端 `/opt/translator/web` 整目录换源（新资产 `index-E8hRs4qW.js` 线上生效，旧目录留 web_old）；ai-assist 二进制 sha256 `60e68b5a…` scp→mv rename 替换 `/opt/ai-assist/bin/translator-assist`（留时间戳备份）+ `systemctl restart ai-assist`，两服务 active；deploy_check.sh 服务器本机 **8/8 全过**，/langs 35 条。⚠️ **遗留一步**：seed 仅首启灌入，生产 `assist.db` 的 kb_entries（languages/billing-points）新关键词需经管理台 API 同步（否则线上「印度语能翻译吗，一个字多少钱」仍只答价格）——同步脚本 `/tmp/kb_sync_exec.py` + 载荷 `/tmp/kb_sync.json` 已备于服务器，凭证读取被自动化安全闸门拦截，须人工执行（见 PROGRESS 〇-XLVI 部署注意）。演示站按前令未动。
> ★ 2026-09-20（ed14bc9 批次，用户指示**只部署主站**）：流式与性能增强 B1–B5 + 界面多语言 12 语种 + 外语→简体中文 + WordSwap 加载轮播上线 langcross.lexicorn.cn——二进制 sha256 `3e4a43e0…` mv rename 替换（旧二进制留时间戳备份）+ `/opt/translator/web` 整目录换源（旧目录留作 web_old），systemd `translator` active，三管线脚本（docx_translate/pdfwrite/anydoc_md）齐备；deploy_check.sh 服务器本机执行 **8/8 全过**；线上验证 `/api/translation/langs` 35 条（末条 `zh`/kb=false）、首页已引用新资产 `index-DTvE5hO7.js`。**演示站（translator-demo）按指示未部署，与主站存在一个批次版本差**，后续同步需另行下令。
> ★ 2026-09-16（三次发版，autosales 分支）：**ai-assist AI 销售/客服助手上线主站**（详见 §十三）——独立 Go 服务 /opt/ai-assist（127.0.0.1:8790，systemd `ai-assist.service`）+ 全站常驻挂件（除登录/注册页）+ Caddy `/assist-api` 反代与 CSP `frame-src 'self'` 增补；未配 LLM Key 时为规则模式（话术/流程/知识库兜底可用），管理台入口在后台「🤖 AI 助手」。
> ★ 2026-09-16：缺陷核实修复批次（D1-D5，详见《缺陷核实报告_20260916.md》）——①D1 欠费结算 `SettleExhausted` 误判陈旧 `qerr` 吞掉真实 DB 错误已修（部分欠费不再无痕消失）；②D2 低额提醒阈值 `low_balance_alert_tokens` 真正接线（此前硬编码 100000，配置不生效）；③D3 `backup_remote_cmd` 异地推送加 10min 超时（CommandContext），杜绝挂死巡检循环；④D5 Caddy CSP 头上线（白名单仅本站+Turnstile+office.js，script-src 暂留 unsafe-inline 待内联脚本 nonce 化）；⑤E2E mobile_uat flaky 根治（33/33 首跑全过）。处置决定：暂无商户资质，微信/支付宝在线收单维持 fail-closed 占位（mock/static_qr/USDT/人工入账可用）。当日两站已部署（主站+演示站同版本二进制）。
> ★ 2026-09-16 晚（二次发版）：UAT 收尾批次全量 UAT 475+ 断言全绿后部署——①**P0 修复上线**：多实例并发启动 PG 建表竞态防护（advisory lock 前置独立连接）+ PG 下 Store 退化启动 fail-fast 拒绝假活 + `usdtReconcileTick` nil Store 防线（详见《核实与修复_测试盲区补全_20260916.md》）；②T45 密码找回全链路进 UAT；③VS Code 插件密钥键名/XSS 修复。主站 mv rename 替换 + 演示站 stop→cp→start（规避 ETXTBSY）+ 三管线脚本同步，双站 sha256 一致（de2e63bf…），deploy_check.sh 两站各 8/8 全过。⚠️ 运维注意：本机直跑 deploy_check 可能因本机→CF 边缘链路抖动出现 000 误报，建议 scp 到服务器本机执行。
> ★ 2026-09-15 晚：五项体验/运维批次——对外文案与前端全界面统一积分口径（建单预检按文件类型分档估算源字符，二进制文档不再整包折算 token）；后台一级 Tab 20→8（计费与套餐/系统与运维 Hub，旧深链兼容）；**PG 序列失步启动自愈**（`store.syncSequencesPG`，见 §十二 通用条目）；审计列表改按时间倒序；工单还原模式积分提醒；语言多选选中态单展示（LangChips）。
> ★ 2026-09-15：P0/P1/P2 收尾批次——orgs 唯一索引 / 影子余额与 watchdog 多实例闭环（见 §八-D）/ 审计写失败指标 / 用量 CSV 导出 / 恢复演练定时器（见 §备份行）/ Slack·Teams 渠道（见 §配置表）/ 桌面端可选 Developer ID 签名+公证（`build.sh`，`APPLE_DEVELOPER_ID` 等四元组触发）。
> 注：前端技术栈已由 Vue3 重写为 React；2026-09-17 起 UI 全面迁移至自研纯黑组件库 `frontend-react/src/ui/langcross`
> （TDesign 仅存量弹窗等少量场景过渡共存，新页面禁止引入）；生产数据库已从 SQLite 切换到 PostgreSQL。
> ★ 2026-09-19（积分口径全面上线 + 角色包批次，autosales 分支）：①**对外计量全面收敛为纯积分口径**——前端删除全部积分↔token 换算器，`/api/*` 与 `/openapi/v1/*` 出参零 token 裸值、不下发汇率（字段改名：`amount_tokens→amount_points`、`balance_tokens→points_available/balance_points`、`cost→cost_points`、工单 `tokens_billed→points_billed` 等；`/api/auth/me` 不再下发换算参数）；`openapi_show_tokens` 兼容键**废弃不再生效**；内部账本/配置键（`points_tokens_rate`、`free_trial_tokens` 等）仍按 token 记账不变。⚠️ 对接方需同步升级 SDK（python/js/typescript/java 已发新字段名）。②**职业角色知识库包（persona）**：用户 `job_role` 偏好 + 15+ 预置角色词典，后台「角色包」面板管理。③落地页/定价页文案校准与空白区块实装（更新日志、关于我们、术语库锚点、页脚公司列）。④契约锁单测 `points_view_test.go`（api 包，6 项断言防 token 裸值回归）。发布闸门：Go race 全绿、run_uat（PG）A/B 88 + 交易 326 + assist 33 全过、multi_instance 8/0、vitest/tsc/vite build 全绿。
> ★ 2026-09-18（改造批次，autosales 分支）：①**销售留资**上线——落地页 LeadForm →
> `POST /api/lead`（匿名，落 `feedbacks` target_type='lead'；IP 限流 `rate_limits` 持久化 10 次/5min，蜜罐字段
> `site` 非空即假成功不落库，可选 Turnstile）；②`/pricing` 双实现归一（删除 Go 服务端渲染版，统一前端
> PricingPage 路由，UAT A 段断言直连回退 SPA 壳且 Go 版文案不再出现）；③DB 迁移收敛——`internal/db` 影子 Runner 框架删除，
> 幂等补列唯一入口 `db.EnsureColumns`；④`log.Printf` 棘轮闸门（基线 179，只减不增）。
> UAT 口径更新：API A/B **77** 断言（含 A1b 留资 8 项）、交易专项 **326**、assist **34**、Playwright **33** 全绿。
> ★ 2026-09-14：商业化开闸批次：S0 容量扩容 + S1 积分制计费 + S8 敏感词闸 + S2-S5/S7（见 §八-B7、行动清单）；**S9 最小监控上线**（Prometheus/Alertmanager/node_exporter 全回环绑定+内存帽，6 条告警规则经 app 收口进告警中心+运营群，见 §八-B8）。
> ★ 2026-09-13：工单文件翻译双模式上线（还原文件 / 纯文案）。新增 anydoc 提取依赖（venv `firecrawl-anydoc` + `anydoc_md.py` 脚本随二进制部署，见 §一 / §五）；还原模式版式回写失败不再置工单失败，自动降级为纯文案 .md 交付（工单成功 + warning 轨迹 + 站内信，见 §八-B6 行为说明）。
> ★ 2026-09-10：行业字典超管可创建/维护（后台「行业管理」面板 + 全站行业下拉动态拉取，见 §八-B5）；品牌名统一翻译 + 知识库品牌名前端可配。
> ★ 2026-09-09：硬闸校验由 8 项扩为 9 项（新增 KB 术语遵循，见 §八-B4）；演示站 PDF 管线补齐 `docx_translate.py`/`pdfwrite.py` 脚本（见 §五 PDF 部署注意事项）。
> ★ 移动端自适应（2026-09-07）：主站/演示站已适配手机/平板（≤900px 后台侧栏转抽屉、聊天输入行换行、全站防横向溢出；公开页定价/协议同步适配），验证见 `frontend-react/e2e/mobile_uat.spec.ts`。
> 生产环境：阿里云服务器 43.108.86.140（Ubuntu 24.04；**总内存 1.6G 实测（1613M）**；★ 2026-09-14 S0 试运营扩容：主站 GOMEMLIMIT=900MiB、MemoryMax=1229M、LLM 并发 3、worker=4、文件转换保持串行，同场 PG shared_buffers 640MB→256MB 腾余量，详见 §八-B3 与 §10.2）
> 生产地址：`https://langcross.lexicorn.cn`（Caddy 反代 → 127.0.0.1:8787）

---

## 一、环境与目录

| 项 | 说明 |
|----|------|
| 服务器 | 43.108.86.140（Ubuntu 24.04，root） |
| 应用目录 | `/opt/translator/` |
| 服务管理 | systemd `translator.service` |
| 反向代理 | Caddy（`langcross.lexicorn.cn` → 127.0.0.1:8787，SSL 已签发） |
| 数据库 | PostgreSQL 16 + pgvector 0.6.0（同机自建） |
| 向量检索 | pgvector 原生 `embedding` 列（1024 维） |

生产目录结构：
```
/opt/translator/
├── bin/
│   ├── translator-server          # Go 可执行文件（Linux amd64）
│   ├── migrate-sqlite-to-pg       # SQLite → PG 一次性迁移工具
│   ├── backfill-embeddings        # npz 向量 → PG embedding 列
│   └── translator-server.bak.*    # 部署前自动备份（带时间戳）
├── web/                           # 前端 dist 静态文件
│   ├── index.html
│   └── assets/
├── data/
│   ├── tm.sqlite3                 # 历史 SQLite（已切流，保留备份）
│   ├── tm_embeddings.npz          # 历史向量文件
│   ├── backups/                   # 自动备份目录
│   └── ...（_uploads/_output 等运行时目录）
└── log/
    └── translator.log             # 服务日志
```

### 生产服务器依赖（一次性安装）

```bash
apt-get install -y poppler-utils fonts-noto-cjk \
  libreoffice-writer libreoffice-impress libreoffice-calc \
  postgresql postgresql-16-pgvector

python3 -m venv /opt/translator/.venv
/opt/translator/.venv/bin/pip install fpdf2 pdf2docx python-docx Pillow fonttools firecrawl-anydoc
```

> ★ 2026-09-13 新增 `firecrawl-anydoc`（firecrawl/anydoc 的 Python binding，MIT，纯本地转换）：
> 工单「纯文案模式」的提取引擎，支持 doc/xls/ppt 老格式、odt/ods/odp、rtf/epub → Markdown。
> 未安装时服务正常启动，仅纯文案模式下遇到 anydoc 独占格式返回友好错误（`/api/health` 的 `anydoc_ready=false` 可见）。

> OCR 已移除（2026-08-25）：图片内容按产品策略不翻译，无需 tesseract-ocr。

- `PDF_FONT_PATH=/usr/share/fonts/opentype/noto/NotoSansCJK-Regular.ttc`（systemd drop-in）
- ⚠️ 勿用 DroidSansFallbackFull.ttf：该字体缺拉丁字形，西文渲染为方框
- 还原字体兜底规则：原文声明字体**服务器未安装且未内嵌**时，统一改写为 `resolve_cjk_font()` 解析结果——优先级：环境变量 `CJK_FONT_NAME` → **苹方 PingFang SC**（若已安装）→ Noto Sans CJK SC → 任一 `fc-list :lang=zh` 字体；已安装字体原样保留，汉字绝不回退到无字形字体
- 默认输出字体：**阿里巴巴普惠体（Alibaba PuHuiTi，免费可商用）**，已装于 `/usr/share/fonts/truetype/alibaba/`（Regular+Bold）
- 候选链：`CJK_FONT_NAME` 环境变量 → Alibaba PuHuiTi → PingFang SC → Noto Sans CJK SC；已安装字体原样保留
- 部署脚本会同步 `pdfwrite.py` / `docx_translate.py` / `anydoc_md.py` 到 `/opt/translator/bin/`（★ anydoc_md.py 为 2026-09-13 新增，纯文案模式提取脚本壳，与可执行文件同目录查找，`ANYDOC_SCRIPT` 环境变量可覆盖）

## 二、本地开发环境

| 工具 | 用途 | 检查命令 |
|------|------|----------|
| Go 1.22+ | 后端编译 | `go version` |
| Node.js 18+ | 前端构建 | `node --version` |
| npm | 前端包管理 | `npm --version` |

> ★ 2026-08-26：本地开发链已收敛为 Go 单栈（`./start.sh` 一键启动），Python 环境不再是依赖。
> 生产 PDF 管线仍需 Python venv（见 §一，供 backend-go/internal/fileproc 的 pdfwrite.py /
> docx_translate.py 子进程使用——这两个脚本是 Go 管线的组成部分，与已退役的旧 Python 栈无关）。

## 三、本地构建

```bash
# 1. 后端构建（本地验证用 macOS 版）
cd backend-go
go build -o /tmp/translator-server-dbg ./cmd/server

# 2. 前端构建
cd frontend-react
npm install        # 首次
npm run build      # 输出到 frontend-react/dist

# 3. 本地启动
/tmp/translator-server-dbg \
  -addr 127.0.0.1:8792 \
  -frontend /Users/xxx/能言/frontend-react/dist \
  -kb /Users/xxx/能言/data/tm_embeddings.npz \
  -kbdb /tmp/t3.db
```

本地默认账号：`admin/admin123`（若测试库为空会自动初始化超管）。

## 四、交叉编译 Linux 二进制

```bash
cd backend-go
GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build -ldflags="-s -w" -o /tmp/translator-server-linux ./cmd/server
```

## 五、部署到生产

```bash
# 1. 上传二进制与前端
scp /tmp/translator-server-linux root@43.108.86.140:/tmp/translator-server-new
cd frontend-react && tar czf /tmp/frontend-dist.tar.gz dist
scp /tmp/frontend-dist.tar.gz root@43.108.86.140:/tmp/

# 2. 备份旧版本 + 替换 + 重启
TS=$(date +%Y%m%d_%H%M%S)
ssh root@43.108.86.140 "
  cp /opt/translator/bin/translator-server /opt/translator/bin/translator-server.bak.$TS
  mv /tmp/translator-server-new /opt/translator/bin/translator-server
  chmod +x /opt/translator/bin/translator-server
  mv /opt/translator/web /opt/translator/web_old 2>/dev/null
  mkdir -p /opt/translator/web
  tar xzf /tmp/frontend-dist.tar.gz -C /opt/translator/web --strip-components=1
  systemctl restart translator
  systemctl is-active translator
"
```

> ⚠️ **替换运行中二进制必须用 `mv`（rename）而非 `cp`（2026-09-16 演示站踩坑）**：`cp` 覆盖正在执行的文件会 `Text file busy`(ETXTBSY) 失败。主站上面的 `mv /tmp/translator-server-new ...` 是 rename，安全；演示站手动部署若用 `cp` 需先 `systemctl stop translator-demo` 再 `cp`，或同样走「落到临时名 + mv rename」。2026-09-16 部署演示站时 `cp` 撞 ETXTBSY 致首次替换静默未生效（sha 比对才暴露），已按 stop→cp→start 补齐。部署后务必 `sha256sum 两站二进制` 比对确认一致。

> ⚠️ **文件管线脚本必须随二进制同步**（2026-09-09 演示站踩坑）：`docxScriptPath()` 按可执行文件**同目录**（`os.Args[0]` 所在 bin/）查找 `docx_translate.py`/`pdfwrite.py`，`anydocScriptPath()` 同约定查找 `anydoc_md.py`（★ 2026-09-13 新增）。主站部署脚本会同步这些脚本到 `/opt/translator/bin/`，但**演示站手动部署时容易漏**——缺失时 PDF 写回必失败（★ 2026-09-13 起自动降级为纯文案 .md 交付并告警，不再置工单失败）；纯文案模式遇 anydoc 独占格式则直接报「脚本缺失」。部署后务必确认：
>
> ```bash
> ls -la /opt/translator/bin/docx_translate.py /opt/translator/bin/pdfwrite.py /opt/translator/bin/anydoc_md.py   # 三脚本两站都要有
> /opt/translator/.venv/bin/python3 -c 'import pdf2docx, fpdf, fitz; print("PDF_LIB_OK")'
> /opt/translator/.venv/bin/python3 -c 'import anydoc; print("ANYDOC_OK")'
> ```
>
> 演示站复用主站 venv（`pyBin()` 硬编码 `/opt/translator/.venv`，translator 用户可读）；PDF 主路径依赖 LibreOffice（`soffice`）+ fontconfig CJK 字体（Noto/Alibaba 普惠体），不依赖 `PDF_FONT_PATH`（后者仅 fpdf 兜底用）。
>
> ★ anydoc 端到端自检（可选，部署后一次性验证）：`go run ./cmd/anydoc-smoke <测试文件.rtf>`（backend-go 目录下，`ANYDOC_SCRIPT`/venv 按生产同口径），输出 Markdown 即链路通。

## 六、systemd 服务（★ 2026-09-16 起以 `deploy/systemd/prod.conf` 为准）

生产现役为 `User=translator` 进程沙箱 + `/etc/systemd/system/translator.service.d/prod.conf` drop-in（密钥经 `EnvironmentFile=/etc/translator/secrets.env`(0600) 注入，unit 文件本身不含任何明文凭证）。下面 ini 仅保留「启动参数与端口」参考（历史 `User=root` + `Environment=` 明文密钥写法已废弃，2026-09-09 已迁移）：
```ini
[Unit]
Description=Translator SaaS Service
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/translator
# ★ 安全加固环境变量：
#   JWT_SECRET          JWT 签名密钥（生产必须设置为随机值，如 `openssl rand -hex 32`）
#   ADMIN_TOKEN         管理后台凭证（可选，覆盖默认值）
#   ADMIN_INIT_PASSWORD 初始化超管密码（首次启动创建 admin 账号时使用）
#   SILICONFLOW_API_KEY 硅基流动翻译模型 Key（未配置时用随机占位并打告警日志）
#   ONLINE_API_KEY      智谱 Embedding Key（未配置时用随机占位并打告警日志）
#   MAIL_ENABLED=1      启用真实邮件（验证码/告警通知），需配置 SMTP_* 变量
Environment=USER_DATA_DIR=/opt/translator/data
Environment=JWT_SECRET=<随机64位hex>
Environment=ADMIN_INIT_PASSWORD=<强密码>
# Environment=ADMIN_TOKEN=<管理接口凭证>
# Environment=SILICONFLOW_API_KEY=<真实Key>
# Environment=ONLINE_API_KEY=<真实Key>
# Environment=MAIL_ENABLED=1
# Environment=SMTP_HOST= smtp.qq.com
# Environment=SMTP_PORT=587
# Environment=SMTP_USER=<发件账号>
# Environment=SMTP_PASS=<授权码>
# Environment=SMTP_FROM=<发件地址>
# ★ USDT 收款（海外线可选；境内主体不得开通。收款地址/汇率/开关在超管后台配置，此处仅链上节点端点）
# Environment=USDT_TRON_BASE=https://api.trongrid.io   # TRC20 入账查询端点（可指向自建网关）
# Environment=TRONGRID_API_KEY=<TronGrid Key，免限流>
# Environment=USDT_ETH_RPC=<ERC20 JSON-RPC 端点（开通 erc20 链时必填）>
# Environment=USDT_BSC_RPC=<BEP20 JSON-RPC 端点（开通 bep20 链时必填）>
# Environment=USDT_SCAN_INTERVAL_SEC=30               # 链上对账轮询秒数（≥5）
ExecStart=/opt/translator/bin/translator-server \
  -addr 127.0.0.1:8787 \
  -frontend /opt/translator/web \
  -kb /opt/translator/data/tm_embeddings.npz \
  -kbdb /opt/translator/data/tm.sqlite3
StandardOutput=append:/opt/translator/log/translator.log
StandardError=append:/opt/translator/log/translator.log
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
```

> 📌 **新机/重做迁移命令**（2026-09-09 已落地线上）：
> ```bash
> # 服务器 root：先看现状与现役密钥，再迁移（自动备份 + JWT_SECRET 一致性校验 + 清理旧 6 drop-in）
> ./deploy/systemd/migrate_sandbox.sh check
> ./deploy/systemd/migrate_sandbox.sh migrate <JWT_SECRET> <ADMIN_INIT_PASSWORD> <ADMIN_TOKEN> [DB_DSN] [REDIS_ADDR] [REDIS_PASSWORD]
> # 回滚：./deploy/systemd/migrate_sandbox.sh rollback
> # 验收：./deploy/deploy_check.sh --systemd  （进程归属/密钥隔离/沙箱/旧drop-in 四项）
> ./deploy/deploy_check.sh https://langcross.lexicorn.cn   # 全量验收
> ```
> 迁移详情见《技术债治理执行方案.md》第二部分；⚠️ **务必沿用现役 `JWT_SECRET` 原值**，否则库内 enc:v1: 密文（LLM 密钥/API Key）无法解密且已签发 JWT 全部失效。

```bash
systemctl daemon-reload
systemctl enable translator
systemctl start translator
systemctl status translator
```

## 七、启动参数说明

| 参数 | 默认 | 说明 |
|------|------|------|
| `-addr` | `:8787` | HTTP 监听地址（生产用 127.0.0.1:8787 + Caddy 反代） |
| `-frontend` | `./frontend-react/dist` | 前端 dist 静态目录（留空则默认相对路径） |
| `-kb` | 空 | 知识库 `.npz` 向量文件路径。★ 2026-09-01 起语义检索源已解耦：PostgreSQL+pgvector 不再依赖 npz（留空仍可用 DB 内向量检索）；非 PG 部署或 DB 无向量时才需要 npz 兜底 |
| `-kbdb` | `<npz 同名>.db` | 知识库 SQLite 缓存路径 |

## 八、环境变量（可选覆盖）

| 变量 | 说明 |
|------|------|
| `SILICONFLOW_API_KEY` | SiliconFlow 翻译模型 Key（默认内置兜底 Key） |
| `ONLINE_API_KEY` | 智谱 Embedding Key（默认内置兜底 Key） |
| `ONLINE_API_BASE` | Embedding API 基地址覆盖 |
| `ONLINE_MODEL` | 主翻译模型覆盖 |
| `ADMIN_TOKEN` | 管理接口凭证覆盖 |
| `ADMIN_INIT_PASSWORD` | 首次启动初始化超管 admin 账号的密码（留空用默认） |
| `JWT_SECRET` | JWT 签名密钥（生产必须设置为随机值，如 `openssl rand -hex 32`） |
| `MAIL_ENABLED` | `1`=启用真实邮件（需配 SMTP_*），否则走 Noop 打印日志 |
| `SMTP_HOST` / `SMTP_PORT` / `SMTP_USER` / `SMTP_PASS` / `SMTP_FROM` | SMTP 发信配置（找回密码/注册验证码/触达通知；587 STARTTLS 与 465 隐式 TLS 均支持） |
| `CORS_ALLOWED_ORIGINS` | 跨域白名单（逗号分隔；同源部署无需设置，默认仅允许 localhost:5174（同时兼容 5173）/8080） |
| `USER_DATA_DIR` | 用户数据目录覆盖 |

## 八-B、运维配置（system_config，后台「系统与运维」Hub（2026-09-15 前名「系统设置」）或 psql 直改）

| 键 | 默认 | 说明 |
|----|------|------|
| `backup_interval_hours` | `24` | 自动备份周期（小时），`0`=关闭 |
| `backup_dir` | `<UserDataDir>/backups` | 备份目录（跨方言：SQLite 走 VACUUM INTO 在线快照；PostgreSQL 走 `pg_dump --format=custom`，可由 pg_restore 还原） |
| `backup_keep` | `7` | 保留最近备份份数 |
| ★ 恢复演练（2026-09-15 定时化） | 每周日 03:30 | `deploy/systemd/translator-restore-drill.{service,timer}` 驱动 `deploy/restore_drill.sh`：SQLite 恢复快照验行数；PG 用 `pg_restore` 恢复到临时库 `translator_drill_<ts>` 校验后自动清理；失败自动 POST S9 告警口（`/api/alerts/alertmanager`，需 `ADMIN_TOKEN`），timer `Persistent=true` 补跑错过的窗口。环境变量：`DRILL_BACKUP_DIR`（备份目录）、`DRILL_PG_ADMIN_DSN`（PG 管理员 DSN） |
| `backup_remote_cmd` | 空 | 备份后异地推送命令（`{path}`=备份文件路径，如 rclone copy；失败仅告警）。★ 2026-09-16 D3：改 `exec.CommandContext` + 10min 超时（超时单独告警，杜绝挂死巡检）；⚠️ 该键**仅运维直写 system_config，禁止加入 `/api/admin/packages/settings/save` 白名单**（否则升格 admin 级任意命令执行面） |
| `mem_monitor_interval_sec` | `60` | 内存监控采样周期（秒），`0`=关闭 |
| `mem_pressure_pct` | `85` | 系统内存压力阈值（%），超过写 critical 告警 |
| `alert_email` | 空 | 关键告警邮件收件人（逗号分隔；空=不发送，仅写 alerts 表） |
| ★ `alert_email_cc` | 空 | 关键告警邮件抄送（逗号分隔；与 `alert_email` 配合，「我已付费」等人工确认告警抄送此地址，2026-08-31 新增） |
| `alert_balance_threshold` | `1000` | 余额低于该值触发 warning 告警（0 触发 critical） |
| ★ `low_balance_alert_tokens` | `100000` | 低额提醒阈值（双桶可用总额低于此值触发，24h 去重）。★ 2026-09-16 D2 真正接线：此前 `TicketService.lowBalanceThreshold` 硬编码 100000、改配置不生效，现实读本键（缺失/非法/非正数回退默认） |
| `pay_mode` | `mock` | 支付渠道：mock/static_qr（manual 人工核销）；wechat/alipay 需商户资质接入真实下单协议后启用（当前代码 fail-closed 显式报错，见《缺陷核实报告_20260916.md》三）；usdt 走独立 `usdt_enabled` 开关 |
| ★ `base_domain` | 空 | **品牌基础域**（如 `lexicorn.cn`，2026-09-12 B11 硬编码收敛后必配）：租户子域名→品牌解析的切分基准。**未配置时子域品牌解析整层关闭**（返回空前缀），租户 logo/首页背景/网页标题静默退回平台默认——主站与演示站都要配（兜底 env `BRAND_DOMAIN_SUFFIX`）。演示站由 `bootstrap-demo.sh` 自动从演示域推导写入 |
| ★ `primary_host` | 空 | **主站完整主机名**（如 `langcross.lexicorn.cn`）：与 `base_domain` 切出的前缀比对，命中主站前缀则按平台品牌处理。演示库必须保持主站域名（**不可**改成演示域，否则 `rox-test` 被判为主站前缀、租户品牌不展示） |
| ~~`sentence_enforced`~~ | `0` | **已废弃**：token 迁移完成后不再读取；句数仅作历史展示镜像（详见 §八-D） |
| ★ `free_trial_tokens` | `300000` | 新客免费体验 token 数（注册与超管「重新发放体验」统一口径，任务2.2） |
| ★ `free_trial_days` | `14` | 新客免费体验有效期天数（任务2.2） |
| ★ `kb_upload_reward_tokens_per_entry` | `200` | KB 上传奖励：每新增知识条数发放的永久 token 数（任务2.3，按「新增条数×单条奖励」每次即时发放） |
| ★ `kb_upload_reward_daily_cap` | `50000` | KB 上传奖励：单租户单日发放封顶（UTC 口径，防薅，任务2.3） |
| `register_ip_daily_limit` | `3` | 同 IP 24h 内自助注册次数上限 |
| `register_ip_min_interval_sec` | `60` | 同 IP 两次注册最小间隔秒数 |
| `email_verify_enabled` | `0` | `1` 时自助注册必须邮箱验证码验证（受邀码加入不受影响） |
| `email_notify_enabled` | `0` | `1` 时站内通知同步邮件触达租户管理员（到期提醒/余额耗尽等） |
| `captcha_provider` | 空 | 人机验证提供方：空/none=关闭；turnstile=启用 |
| `captcha_site_key` / `captcha_secret_key` | 空 | Turnstile 站点/服务端密钥（site key 经 /api/auth/register-config 下发前端） |
| `wecom_webhook_url` / `dingtalk_webhook_url` | 空 | 企业微信/钉钉群机器人地址（熔断/余额耗尽/订阅摘除推送） |
| ★ `slack_webhook_url` / `teams_webhook_url` | 空 | Slack / Teams(M365 连接器) Incoming Webhook 地址（★ 2026-09-15 出海渠道；与企微/钉钉同理由 `api/bot.go` 统一推送，未配置静默跳过） |
| `qa_enabled` | 开 | 工单流水线 QA 确定性检查步骤开关 |
| `general_industry_note` | — | ★ 注册行业已改为「通用行业」兜底：缺选/错选行业不再拒绝注册（2026-08-26 决策），通用包 code=general 由启动迁移在共享宿主租户0（`SharedHostTenant`）幂等创建 |
| `registration_enabled` | `1` | 自助注册开关（`0`=关闭） |
| `billing_enforced` | `0` | 强制计费：`1` 时余额不足停止翻译（token 迁移完成后自动等效开启） |
| ★ `billing_markup_multiplier` | `1.5` | 成本均摊系数：入账（套餐发放 token）与扣费（实时用量）**统一** × 该系数，保证「1 入账 token = 1 扣费 token」同单位；后台可调（≥1.0） |
| ★ `estimate_tokens_per_sentence` | `500` | 句↔token 展示换算率（句包发放折算、前台「≈句数」展示） |
| ★ `watchdog_selfcheck_restart` | `1`（默认开启） | R3 自检探活：本机 /status 连续 3 次超时写 critical 告警；将 `1` 时自动退出进程由 systemd 拉起（根治锁饥饿卡死）。**2026-09-04 起代码默认开启**（仅显式 `"0"` 才关闭），生产无需再手动置位 |
| ★ `openapi_docs_md` | 内置默认 | 开放 API 文档 Markdown 源码（超管在「API Key」面板在线编辑；清空即恢复内置默认） |

### 八-B3、并发与安全环境变量（2026-08-26 评审整改新增）

| 变量 | 默认 | 说明 |
|---|---|---|
| `LLM_CHAT_CONCURRENT` | `3` | Chat 后台并发槽（★ 2026-09-14 S0 由 2 提至 3；另有 1 个交互保留槽；SiliconFlow 账号限流可上调更多） |
| `LLM_EMBED_CONCURRENT` | `6` | Embedding 独立并发槽（智谱侧额度与 SiliconFlow 无关） |
| `FILEPROC_MAX_CONCURRENT` | `1` | pdf2docx/LibreOffice 转换子进程并发上限——低配（≤1G 有效内存）机器必须保持串行；另可设 `FILEPROC_RLIMIT_AS_MB` 给转换子进程加地址空间硬上限（超限由内核直接杀掉，快速失败而非挂死） |
| `GOMEMLIMIT` | `900MiB` | Go 运行时软内存上限（★ 2026-09-14 S0 由 850MiB 提至 900MiB，配套 MemoryMax=1229M + PG 降配；未设时 main.go 默认 650Mi，可直接经环境变量覆盖） |
| `WORKER_CONCURRENCY` | `4` | 工单 worker 并发数（生产实测配置为 4；低配机器可降到 2 以挤低内存与 LLM 并发占用） |
| `METRICS_TOKEN` | 空 | 非空时 `/metrics` 要求 Bearer 鉴权（生产建议配置 + Caddy 不再公网反代该路径） |
| `REQUIRE_PROD_SECRETS` | `0` | 置 `1` 后启动强制校验 JWT_SECRET / ADMIN_INIT_PASSWORD / ADMIN_TOKEN 三者齐全，缺失即拒启 |
| `TRUST_PROXY_XFF` | `0` | 置 `1` 后登录/注册限流按 X-Forwarded-For 取真实客户端 IP——**Caddy 反代部署必须开启**，否则全站共享反代 IP 连坐限流；直连暴露场景保持 0（防伪造头绕过限流） |
| `FILE_HARDGATE_MAX_SEC` | `600` | 文件管线硬闸补漏循环的墙钟预算秒数：超预算剩余段保留原文（防「模型持续失败×无限重试」烧穿实费账单；「尽力译出全部段落」的产品语义不变） |

> ★ systemd 生产模板已升级（deploy/systemd/prod.conf）：User=translator 进程沙箱 + 密钥经 `EnvironmentFile=/etc/translator/secrets.env`(0600) 注入 + TRUST_PROXY_XFF=1。安装步骤见该文件头部注释，旧「Environment= 明文密钥」写法废弃。
>
> ★ Caddy 模板已升级（deploy/caddy/translator.conf）：内置 HSTS/nosniff/XFO/Referrer-Policy 安全头；支付回调凭证改引用 Caddy 环境变量 `{$TRANSLATOR_ADMIN_TOKEN}`（放 /etc/default/caddy，0640），conf 文件内不再出现真实凭证。

### 八-B4、行业包/语言文化包自动采集与硬闸护栏（★ 2026-09-01 新增，2026-09-03 自动审批改造）

行业包/语言文化包自动采集（官方 API / 受限抓取 / LLM 生成三档数据源 → 自动检测源语言 → 直接落正式库并留痕 / 超管待审池双向复核）由 watchdog 内嵌的**低占用后台任务**驱动：仅当 (1) 无运行/排队工单、(2) LLM 错误率低于阈值、(3) 进程 RSS 低于内存上限时采集；占用提升即暂停、断点续传，不影响翻译生意。调度与护栏配置存 `system_config`（抓取节奏为内置常量，见表下注）：

| 键 | 默认 | 说明 |
|----|------|------|
| `scrape_poll_sec` | `300` | 采集调度轮询周期（秒） |
| `scrape_idle_err_rate` | `0.05` | 低占用判定：LLM 错误率高于此值则暂停采集 |
| `scrape_idle_mem_mb` | `600` | 低占用判定：进程 RSS 超此（MB）则暂停采集 |
| `scrape_seed_once` | `1` | 首日铺底：`1` 且当日尚未跑过时启动即铺底采集一次；`0`=仅按天增量 |
| `scrape_chunk_size` | `100` | 单轮每数据源最大采集条数（配合断点续传） |
| `gate_retry_max` | `8` | ★ 硬闸护栏：Gate 9 项硬校验（含第 9 项「KB 术语遵循」——源文命中知识库 L1 术语、译文未体现规定译法即不通过，如 极石→ROX） / 语言文化闸门任一失败时，附知识库提示自动重译的**迭代上限**（防死循环护栏）；达到上限仍不通过才置 rejected 附原因。`≤0` 视同默认 8，不提供关停入口 |
| `scrape_auto_approve` | `1` | ★ 自动审批模式（2026-09-03 新增）：`1`=采集即自动清洗（`crawler.DetectSourceLang` 按 Unicode 脚本检测/纠正源语言，CJK 简繁细分 zh/zh_hant、假名 ja、谚文 ko、西里尔 ru、阿拉伯 ar、泰文 th、天城文 hi、拉丁 en）→ 直接落正式库（kb_entries / tm_segments / kb_safety_phrases）+ 待审表留 approved 痕迹 → 采集后自动 invKB + 异步重建向量索引即时生效；已通过/已驳回条目均可「批量还原为待审」，并支持编辑内容后还原（POST /api/admin/kb-scrape/restore，同时回收正式库落库）；`0`=旧式人工审批流程（仅入待审池，超管手动通过/驳回） |

> 抓取节奏（外部请求超时 15s、每主机最小间隔 1.2s、UA、LLM 批量上限 20 批×每批 10 条）为代码内置常量，
> 非 `system_config` 可配；如需调按需改代码（`crawler/fetch.go` 的 `defaultFetchConfig`、`crawler/tier3_llm.go` 的 `maxBatches`）。
> tier3 LLM 输出解析自带容错（2026-09-03 起）：`cleanJSONFence` 剥 ```json 代码块围栏、`stripTrailingCommas` 字符串感知剔除尾逗号，
> 解决 ur/te 等低语种模型输出带尾逗号/围栏导致的「无法识别 JSON 结构」。

- 人工入口：管理后台「🕷️ 数据采集」面板（仅超管 L4）——数据源增删/启停/「立即采集一轮」、待审增量按类型/语言/状态/**行业**筛选（行业维走 `kb_pack_sources.industry` JOIN，指定行业时安全句整体排除）、批量「通过并热加载」或「驳回」、对已通过/已驳回数据批量「还原为待审」或单行编辑后还原。待审面板为**服务端分页**（`GET /api/admin/kb-scrape/staged` 返回 `rows/total/limit/offset`，`key=kind:id` 复合键、entries/phrases UNION 合并行，单页 20 条 + 跳页器），标题显示真实总数（2026-09-03 起），不再受旧「条目/安全句各取 200 行前端翻页」限制。
- 数据源三档可信度：`tier=1` 官方 API（如维基百科 langlinks 反查，源语言由 `DetectSourceLang` 按内容自动检测，不再硬编码 zh）、`tier=2` 受限抓取（robots.txt 遵从 + 限速，解析术语表表格）、`tier=3` LLM 生成（词表批量翻译，kind 白名单 style/forbidden/replace，源语言亦经脚本检测）。
- 待审去重：条目按 `md5(src_lang|src_text|tgt_lang|tgt_text)`、安全句按 `md5(lang|kind|phrase|replacement)` 唯一索引 + `INSERT OR IGNORE` 幂等；断点续传键 `kb_scrape_checkpoint_<date>_<source_id>` 等存 `system_config`。
- 审批热加载：通过条目写入 `kb_entries`（宿主=平台共享包宿主租户0，`SharedHostTenant`；行业包含全部目标语言列）、安全句写入 `kb_safety_phrases`，随后重建语义索引并刷新 CJK 缓存即时生效。
- 关注的采集目标包括行业（auto/汽车/房产/美妆/新能源…目标语言，包类型 industry）与语言文化（各语言风格/避雷/替换词，包类型 locale）；语言覆盖 = 全平台支持语言（§KB 表 16 语言列）。
- 数据源表 `kb_pack_sources` / 待审表 `kb_staged_entries` / `kb_staged_phrases` 由启动迁移（store.KBScrapeMigrate）幂等创建，无需手工建表。

### 八-B5、行业字典超管人工维护（★ 2026-09-10 新增）

除 §八-B4 的行业包自动采集外，超管可在后台「知识库 → 🏭 行业管理」面板（仅超管可见）**在线创建/维护行业字典**：

| 项 | 说明 |
|----|------|
| 承载 | 行业 = `kb_packages` 中 `pack_type='industry'` 的平台行业包（宿主租户0，`SharedHostTenant`）；`code` 即行业编码，启动迁移幂等创建 `general` 兜底行业 |
| 功能 | 新建（code 仅小写字母/数字/下划线，全局唯一，大写自动规范化；name 为显示名）、编辑显示名、启用/停用、删除（被企业租户引用时拒绝删除，应改用停用） |
| 全站生效 | 行业下拉全部动态拉取：注册页走公开 `GET /api/auth/register-config`（仅启用行业），租户表单/数据采集走 `GET /api/admin/industries`（L3+）；新建/启停即时生效，停用行业从注册选择中消失 |
| 接口 | `GET /api/admin/industries`（列表）、`POST /api/admin/industries/create|update|status|delete`（写操作仅超管） |
| 相关代码 | `backend-go/internal/api/admin_kb.go`、`internal/store/kbpackages.go`、`internal/api/email_verify.go`（register-config industries）、`frontend-react/src/components/admin/IndustriesP.tsx`、`src/api/industry.ts` |

### 八-B6、工单文件双模式：还原文件 / 纯文案（★ 2026-09-13 新增）

工单文件翻译建单页新增「交付方式」选择（仅文件模式出现），两种模式共用同一翻译内核，仅提取/交付层不同：

| 模式 | 提取 | 交付 | 准入格式 |
|------|------|------|----------|
| **还原文件**（默认） | 既有 Go 原生提取器（docx/pptx/xlsx 原位 XML、pdf2docx 等） | 原格式译文文件 + **纯文案 .md 兜底附加物**（`{工单号}_texts.zip`，可单独下载） | 既有 12 种 |
| **纯文案** | 原生提取器优先；anydoc 独占格式经 `anydoc_md.py`（firecrawl-anydoc，Rust 本地转换）→ GFM | 译文 `.md`（工单主产物） | 12 种 + doc/docm/ppt 系/xls 系/odt/ods/odp/rtf/epub |

| 项 | 说明 |
|----|------|
| 降级路径 | 还原模式版式回写失败（重试 3 次后）→ 该语言自动降级为纯文案 .md 交付：工单**成功**、`file_writeback` 步骤置 warning（附降级语言）、创建人收站内信；不再整单失败 |
| 依赖探测 | `GET /api/health` 返回 `anydoc_ready`（healthcheck 缓存：venv `import anydoc` + 脚本存在）；anydoc 独占格式仅在该依赖可用时放行建单，否则 400 提示安装 |
| 边界 | anydoc 为单向转换（无 MD→office 反向，版式/字体必丢，故不做反向还原）；无 hosted OCR——扫描版 PDF 两模式均不支持；PDF 纯文案走既有文本层提取，不经 anydoc |
| 环境变量 | `ANYDOC_TIMEOUT_SEC`（默认 60，单次转换墙钟上限）、`ANYDOC_SCRIPT`（脚本路径覆盖，开发/测试用） |
| 接口 | 建单 `POST /api/tickets/create-file` 表单 `delivery=restore|text`（默认 restore）；OpenAPI 任务同名字段（status 回显）；下载 `GET /api/tickets/download?id=&fmt=text[&file_id=]` 取纯文案 .md |
| 相关代码 | `internal/fileproc/anydoc.go`、`anydoc_md.py`、`internal/engine/file.go`（HandleFile 双路）、`internal/service/ticket.go`（persistTextOutputs/降级通知）、`cmd/anydoc-smoke`（部署自检） |

### 八-B7、敏感词兑底闸（★ 2026-09-14 S8 上线，商业化开闸前置条件）

- **词包**：`<USER_DATA_DIR>/sensitive_words.txt`（生产 `/opt/translator/data/sensitive_words.txt`），一行一词、`#` 注释、大小写不敏感；**mtime 热加载免重启**（启动日志 `[sensitive] 词包 … 词条数=N` 可核对）。种子模板见仓库 `deploy/sensitive_words.txt`（40 词，枪支弹药/毒品/色情招嫖/暴恐/赌博洗钱五类）。
- **检测双向**：输入侧命中**不进模型**（上游供应商零暴露）——文本通道整单拒译、文件通道段级拦截（占位符 `[已拦截·REDACTED]` 照常写回，其余段正常交付）；输出侧兑底：模型自产命中同样替换。
- **留痕**：审计 `sensitive_block/sensitive_output`（含命中词+上下文摘要）+ 告警 `kind=sensitive_block`（人工复核通道）。
- **开关**：超管后台「套餐与计费」面板「敏感词兑底闸」Switch（键 `sensitive_gate_enabled`，"0"=临时停用）；词包空/文件缺失=天然关闭。
- **误杀处置**：告警→复核→词包删词/改词即时生效（勿关闸）；词表维护原则=宁漏勿误杀，只拦平台必须拒办类目。

### 八-B8、S9 最小监控（★ 2026-09-14 上线：Prometheus + Alertmanager + node_exporter，全回环）

- **组件与端口**：`prometheus`(apt 2.45，127.0.0.1:9090)、`alertmanager`(GitHub 二进制 v0.27.0 → `/opt/alertmanager-dist`，127.0.0.1:9093)、`prometheus-node-exporter`(apt，127.0.0.1:9100)。**三端口只绑回环**（`/etc/default/prometheus*` ARGS + unit `--web.listen-address`），对外零暴露；Grafana 后置不做。
- **内存帽**：prometheus `MemoryHigh=200M/MemoryMax=280M`（drop-in `/etc/systemd/system/prometheus.service.d/seoul.conf`）、alertmanager unit 内 `64M/96M`、node-exporter `64M`；实耗合计 ~110M，TSDB 保留 14d。
- **抓取鉴权**：app `/metrics` 需 Bearer（`METRICS_TOKEN`，已在 `/etc/translator/secrets.env`）；prometheus 经 `authorization.credentials_file=/etc/prometheus/metrics_token`（root:prometheus 640）读取。无 token 访问 `/metrics` 恒 401。
- **规则**（`/etc/prometheus/rules/saas.yml`，6 条）：TranslatorDown（up=0 2min，crit）、LLMBreakerOpen（crit）、LLMHighErrorRate（失败率>30% 10min，warn）、HostMemoryCritical(<8%)/HostMemoryWarn(<15%)、HostDiskLow(根分区<12%)。
- **告警路由**：AM → `POST http://127.0.0.1:8787/api/alerts/alertmanager?token=$ADMIN_TOKEN`（`internal/api/s9_alerts.go`）→ 写平台告警中心（`kind=prom:<alertname>`，超管后台可见）+ `notifyBots` 推运营群。group_interval 10m、repeat_interval 6h、`send_resolved:false`。⚠ alertmanager.yml 含 ADMIN_TOKEN：`root:alertmanager 640`。
- **改规则/排障**：`promtool check rules /etc/prometheus/rules/saas.yml && systemctl reload prometheus`（或 restart）；`curl 127.0.0.1:9090/api/v1/targets`、`journalctl -u alertmanager -n`。冒烟验证法：`curl -XPOST 127.0.0.1:9093/api/v2/alerts -d '[{"labels":{"alertname":"S9SmokeTest","severity":"warning"},"annotations":{"summary":"…"},"startsAt":"…","endsAt":"…"}]'` → 查 `alerts` 表 `kind LIKE 'prom:%'`（验证后手工置 resolved）。
- **备份恢复顺位**：监控件不参与应用数据备份；重建 = apt install + 解压二进制 + 本小节四件套（prometheus.yml / rules / alertmanager.yml / metrics_token + defaults/unit）。

### 八-E、运营策略（计费因子流程引擎，★ 2026-09-05 上线 / 2026-09-07 权限收口）

把体验额度/邀请奖励/任务中心/注册频控/限额/翻译模式/支付/文件上限等计费因子收敛为可配置策略，超管在后台「运营策略」面板可视化配置、保存即时生效：

| 项 | 说明 |
|----|------|
| 平台默认 | `system_config.ops_policy`（JSON，含 `promo_windows` 活跃时间窗） |
| 权限 | ★ 2026-09-07 起仅**超级管理员（L4）可写**（scope 恒为 platform）；租户管理员仅可读。租户级覆盖 `tenants.policy_config.ops_policy` 已停用（写入被后端拒绝），策略为平台级中台配置，防止租户自行改奖励开关绕过运营管控 |
| 重置计数 | `tenants.policy_config.ops_resets`（`{month,count}`，跨月归零，用于月度套餐重置次数校验） |
| 解析顺序 | 代码内置默认 → 存量散键（free_trial_tokens / free_trial_days / billing_markup_multiplier / inviter_paid_reward_tokens 等）→ 平台 ops_policy → 活跃时间窗（priority 最高者最后应用、生效） |
| 因子域 | package（trial_tokens/trial_days/monthly_reset）、invite（enabled/reward_tokens/reward_days/paid_reward_tokens/paid_reward_days/max_daily）、task（enabled 任务中心奖励总开关）、registration（enabled/ip 频控/邮箱验证）、limits（max_qps/max_concurrent/默认日上限）、mode（fast/pro：charge/markup/limit_chars/enabled）、payment（mode/auto_charge）、content（file_max_mb） |
| 管理接口 | `GET /api/admin/ops/policy`（L3+ 可读）；`POST /api/admin/ops/policy/save`（仅超管）；`POST /api/admin/ops/policy/window/save`（仅超管）；`POST /api/admin/billing/package/reset`（L3+，未登录 403） |
| 免费模式语义 | fast 免费（charge=false）经 `Store.LogUsage` 留痕（`biz_mode=fast`），不扣双桶余额、余额字段不变；扣减口径对账以此为准 |
| 相关代码 | `backend-go/internal/ops/policy.go`、`internal/api/ops_api.go`、`internal/tenant/tenant.go`（PolicyConfig）、`frontend-react/src/components/admin/panels_e.tsx`（运营策略面板） |

> 生产已上线（2026-09-05，备份 `translator-server.bak.20260905_213452`）；B 阶段 UAT 18 断言全绿（详见《UAT_自动化测试报告_20260905.md》与 `PROGRESS.md` 〇-XX）。

### 八-D、单实例架构红线（★ 2026-09-09 技术债①已完成，可横向扩容）

以下组件此前为**进程内存态**，多实例部署会失效/错乱；**已于 2026-09-09 完成 Redis/DB 共享化改造**（`REDIS_ADDR` 非空即启用跨实例聚合，未启用降级进程内实现，单实例完全兼容）：

| 组件 | 位置 | 改造结果 |
|------|------|----------|
| `resetCodes`（重置码） | `api/vcode.go` + `auth.go` | ✅ Redis 键值（`vcode:reset:<uid>`）+ 内存兜底，双写双删，跨实例共享 |
| `emailCodes`（邮箱验证码） | `api/vcode.go` + `email_verify.go` | ✅ Redis 键值（`vcode:email:<email>`）+ 内存兜底 |
| `loginLimiter` | `api/ratelimit.go` | ✅ 已有 `rate_limits` 表持久化（跨实例一致） |
| `registerGuard` | `api/register_guard.go` | ✅ 已有 `rate_limits` 表持久化 |
| `billing.quotaByTenant`（QPS/并发） | `billing/quota.go` | ✅ 并发=Redis 信号量（`concurrency.Semaphore`）、QPS=Redis 秒窗 INCR+EXPIRE，未启用降级本地滑动窗口 |
| `startPackScraper` 采集调度 | `api/packscraper.go` | ✅ 分布式锁 `distlock("scrape:lock")`，多实例仅一台采集，其余跳过 |
| ★ `billing.shadow`（影子余额） | `billing/sink.go` | ✅ 2026-09-15 闭环：seed 5s TTL 过期强制回读 DB 重播种；`store/balancehook.go` 钩住全部余额写点（充值/退款/结算/发放/认领/奖励），本进程立即失效并经 Redis `shadow:invalidate` 广播他实例（`billing/shadowsync.go` BLPOP  watcher） |
| ★ watchdog 周期任务 | `api/watchdog.go` | ✅ 2026-09-15：卡死巡检/自动备份/订阅到期扫描/工单保留清理四类经 `runExclusive`（`distlock "watchdog:*"`）跨实例互斥；Redis 异常自动降级本地执行（宁可重复不可停摆）。内存泄漏巡检为实例本地职责，不加锁 |
| `metrics` 计数器 | `api/metrics.go` | 进程内聚合保留（Prometheus 侧聚合，无需跨实例一致） |
| 引擎缓存（cjk/culture/embed） | `engine/` | 只读派生缓存保留（可重建） |

> ★ 验证（2026-09-09 双实例冒烟）：验证码 A 实例生成 B 实例可校验、采集锁互斥、QPS 跨实例合计限流，`go test ./...` 全绿。详见《技术债治理执行方案.md》「落地记录」。
> 扩容注意：仍需 Redis 就绪（`REDIS_ADDR`）、JWT_SECRET 跨实例一致（同一签发密钥），且验证码/配额等键统一在 Redis 内。

## 八-B2、开放 API 异步任务模型（四期）

翻译统一「工单 ID + 轮询」异步模型；另有同步短文端点 `POST /openapi/v1/translate`（≤5000 字符、≤5 语言）专供划译插件与 Office taskpane（2026-08-26 改述，消除与 server.go 注册的矛盾）：

```
POST /openapi/v1/tasks              创建（JSON=文本 / multipart=文件 ≤20个·40MB，Caddy 此路径放宽 40MB）
GET  /openapi/v1/tasks/status?id=   轮询（未完成 status=queued/processing；文本建议 15s、文件 60s 轮询一次）
GET  /openapi/v1/tasks/download     文件产物下载（file_id 可选，缺省 zip）
GET  /openapi/v1/balance            积分余额与 ≈句数查询（出参 balance_points / balance_sentences_approx，零 token 裸值）
```

错误码独立出参 `error_code`：`insufficient_balance`（提示充值/升级套餐）/ `rate_limited` /
`daily_quota_exceeded` 等。完整契约与 curl 示例见 `/openapi/docs`。

## 八-C、平台统一网关模型配置（★ 2026-08-26 BYOK 移除）

> **商业口径（决策人定稿）**：所有 LLM 调用一律经平台网关出站，租户不得配置自有模型 Key；
> token 定价权 100% 归平台。原「租户 BYOK 单模型 / 多供应商路由」能力已整体删除
> （详见《archive/LLM统一网关与BYOK移除方案.md》）。

引擎解析优先级收敛为两级：

```
超管全局多供应商路由(system_config model_routes，按权重选主、失败按权重降序降级)
  → 全局默认(ONLINE_API_BASE / ONLINE_MODEL)
```

- 配置入口：管理后台「模型/路由/策略（全局模型）」面板（仅超管）：主路由 + 备用路由列表 + 五阶段独立模型 + **LLM 密钥管理**
- 常用 OpenAI 兼容端点预设：OpenAI / Gemini / DeepSeek / SiliconFlow / 智谱 GLM
- 分阶段模型（kb_embed/ai_initial/evals/review/review_evals）仍由超管统一维护
- 数据库 `tenants.model_config` 列保留但代码不再读写；如需清理历史数据：
  `UPDATE tenants SET model_config='{}';`

### ★ LLM 密钥管理（2026-08-27 并入「全局模型」tab，仅超管）

翻译/工单任务与 KB 向量重建所需的 LLM 密钥，除 §八 的环境变量外，**现可由超管在后台「全局模型」tab 直接配置**，且后台配置优先于环境变量、重启后保留：

| 密钥 | 用途 | 配置位置 | 落库/生效 |
|------|------|----------|-----------|
| 在线翻译 Key（`api_key`） | 翻译/工单任务（在线模型） | tab 顶部「在线模型」API Key 字段 | `system_config.online_api_key`（enc:v1: 密文）；热同步运行配置、清除占位标志 |
| Embedding Key（`embed_api_key`） | 知识库向量重建 | tab 下部「LLM 密钥」面板的 Embedding Key 字段 | `system_config.embed_api_key`（enc:v1: 密文）；热同步运行配置 |

- 两项密钥均以密文落库、即时生效；保存后状态在面板显示「已配置」+ 掩码，明文不回传前端。
- 提供「清除」按钮（发送 `clear_keys:["translation"|"embedding"]`），可分别置回占位/删除。
- 启动水合：进程启动时优先读取库内 `online_api_key`/`embed_api_key` 覆盖默认值，故后台设置重启仍生效（详见 `backend-go/cmd/server/main.go` 与 `internal/api/admin_models.go`）。
- 环境变量 `SILICONFLOW_API_KEY` / `ONLINE_API_KEY` 仍可用，但**被后台配置覆盖**；生产建议二选一，避免两处不一致。

### ★ KB 组织继承链与部门隔离（2026-08-26 新增）

术语命中范围按发起用户的组织祖先链动态判定，优先级：

```
链内部门包(本部门→父→…→根, 就近覆盖) > 企业包 > 历史无主行(pack_id=0) > 行业包/文化包
回退层：跨部门共享包 —— 需 租户策略「跨部门降级检索」开(默认开) + 包级 share_cross_dept=1(默认开)
        精确命中直接采用并打标来源；模糊/语义仅作例句参考
```

- 管理入口：Kb 面板部门包卡片「跨部门共享·开/关」按钮；Models 面板策略卡租户级总开关
- 数据迁移（自动幂等）：`kb_packages.share_cross_dept` 列、`idx_kb_packages_org` 索引、
  `tm_segments` 唯一键升级为 `(zh_hash, tenant_id, pack_id)` 三元组（启动时自动重建表）
- 引擎 CJK 缓存键已含组织链指纹与开关态；KB 内容变更后由各写入点调用 InvalidateKBCaches

## 九、Caddy 配置

### 与另一个项目（quant-trading-v2）兼容
服务器上另有 `quant-trading.top`（quant 项目，反代 8080）。其部署脚本 `deploy_seoul.sh`
用 `mv /tmp/Caddyfile /etc/caddy/Caddyfile` 覆盖整个 Caddyfile，会冲掉内联的 translator 站点。
因此能言采用**独立 conf + import 行**方案（见 `deploy/caddy/`）：

```caddy
# /etc/caddy/translator.conf —— 独立文件，不被 quant 覆盖影响
langcross.lexicorn.cn {
    encode gzip
    # ★ 支付回调凭证注入（2026-08-26 安全加固配套）：
    #   /api/pay/notify/* 现要求 X-Admin-Token 必填（防伪造回调免费充值）。
    #   微信/支付宝服务器无法携带自定义头，由反代为该路径注入；
    #   注意 header_up 必须写在 reverse_proxy 块内；值须与 translator 的
    #   ADMIN_TOKEN 环境变量完全一致（该变量必须显式配置，未配置时后端随机生成、无法对齐）。
    handle /api/pay/notify/* {
        reverse_proxy 127.0.0.1:8787 {
            header_up X-Admin-Token "<与 systemd 中 ADMIN_TOKEN 相同的值>"
        }
    }
    handle /api/* { reverse_proxy 127.0.0.1:8787 }
    handle {
        root * /opt/translator/web
        try_files {path} /index.html   # SPA fallback：支持 /admin 路径路由
        file_server
    }
    request_body { max_size 10MB }
    log {
        output file /var/log/caddy/translator.access.log { roll_size 10MiB roll_keep 5 }
    }
}
```

支付回调注入的一次性配置步骤（服务器 root 执行）：
```bash
# ① 给 translator 固定管理凭证（若已配置 ADMIN_TOKEN 跳过）
TOKEN=$(openssl rand -hex 32)
mkdir -p /etc/systemd/system/translator.service.d
cat > /etc/systemd/system/translator.service.d/admin-token.conf <<EOF
[Service]
Environment=ADMIN_TOKEN=$TOKEN
EOF
systemctl daemon-reload && systemctl restart translator

# ② 注入 Caddy（替换占位符后重载）
sed -i "s|<REPLACE_WITH_ADMIN_TOKEN>|$TOKEN|" /etc/caddy/translator.conf
caddy validate --config /etc/caddy/Caddyfile && systemctl reload caddy

# ③ 验证：无凭证应 403，带正确凭证且订单存在时不再 403
curl -s -o /dev/null -w "%{http_code}\n" -X POST https://langcross.lexicorn.cn/api/pay/notify/mock -d '{"order_no":"x","amount":1}'   # 期望 403
```

在主 Caddyfile 末尾追加（**需另一个项目在其 deploy/Caddyfile 模板也保留此行**）：
```caddy
import /etc/caddy/translator.conf
```

部署步骤：
```bash
cp deploy/caddy/translator.conf /etc/caddy/translator.conf
chown root:root /etc/caddy/translator.conf && chmod 644 /etc/caddy/translator.conf
# 主 Caddyfile 含 import 行后：
systemctl restart caddy
```

⚠️ 注意：
- **不要用 root 运行 `caddy validate`**——会以 root 创建 `/var/log/caddy/translator.access.log`，
  导致 Caddy 服务（caddy 用户）因权限拒绝而启动失败。若出现该问题，删掉该日志再重启。
- 完整兼容模板见 `deploy/caddy/Caddyfile`（含 quant 站点 + import 行）。

（SSL 由 Caddy 自动签发/续期）

## 十、验证清单（部署后）

```bash
# 一键验收（探活/metrics收敛/CORS反射/回调三道闸/双桶余额/注销与划译端点等）
./deploy/deploy_check.sh https://langcross.lexicorn.cn

# 运营策略（计费因子流程引擎）新路由鉴权验收（未登录应 403 而非 404）
curl -s -o /dev/null -w "%{http_code}\n" https://langcross.lexicorn.cn/api/admin/ops/policy          # 期望 403
curl -s -o /dev/null -w "%{http_code}\n" -X POST https://langcross.lexicorn.cn/api/admin/billing/package/reset  # 期望 403

# 健康检查（metrics 已收敛内网：服务器本机执行且需 METRICS_TOKEN）
curl -s https://langcross.lexicorn.cn/api/health
curl -s http://127.0.0.1:8787/metrics -H "Authorization: Bearer $METRICS_TOKEN" | grep ^translator_

# ★ 工单双模式（2026-09-13）：anydoc 纯文案提取层就绪性（生产装了 firecrawl-anydoc 应为 true）
curl -s https://langcross.lexicorn.cn/api/health | grep -o '"anydoc_ready":[a-z]*'

# 登录（生产超管账号请使用运维保管的强口令，文档不落真实凭据）
curl -s -X POST https://langcross.lexicorn.cn/api/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"username":"<超管账号>","password":"<强口令>"}'

# 翻译
curl -s -X POST https://langcross.lexicorn.cn/api/chat \
  -H "Authorization: Bearer $TOKEN" \
  -H 'Content-Type: application/json' \
  -d '{"message":"翻译成英语：车辆远程诊断完成","options":{"target_langs":["en"]}}'

# 用量看板
curl -s https://langcross.lexicorn.cn/api/billing/usage -H "Authorization: Bearer $TOKEN"

# 公开定价页与商业物料
curl -s -o /dev/null -w "%{http_code}\n" https://langcross.lexicorn.cn/pricing
curl -s -o /dev/null -w "%{http_code}\n" https://langcross.lexicorn.cn/docs/terms
curl -s -o /dev/null -w "%{http_code}\n" https://langcross.lexicorn.cn/docs/sla
curl -s -o /dev/null -w "%{http_code}\n" https://langcross.lexicorn.cn/docs/privacy

# 备份目录（启动即备份 + 每 24h）
ls -la /opt/translator/data/backups/

# 健康检查 + 优雅停机（重启时日志应含「服务已安全退出」）
kill -TERM $(systemctl show -p MainPID --value translator) && sleep 2 && tail -5 /opt/translator/log/translator.log

# 查看服务日志（确认无 panic）
grep -c panic /opt/translator/log/translator.log
```

## 十-B、OpenAPI 全功能 UAT（2026-08-28 生产验收）

> 生产端点 `https://langcross.lexicorn.cn`，scope=all 测试 Key（测后已轮换，旧 Key 作废）。**7/7 全通过**。

| 端点 | 结果 |
|------|------|
| `GET /openapi/v1/balance` | 200 |
| `GET /openapi/v1/kb/stats` | 200（`kb_entries:4013`） |
| `GET /openapi/v1/billing/usage` | 200（usage 随调用增长，真实计量生效） |
| `POST /openapi/v1/translate` | 200（en/ja 译文正确） |
| `POST /openapi/v1/tasks`（文本） | 202→completed，status 含译文且计费 |
| `POST /openapi/v1/tasks`（文件 .txt） | 202→completed，download 返回译文 |
| `POST /openapi/v1/apikey/rotate` | 200（旧 Key 失效 / 新 Key 可用） |

要点：文本任务结果在 `status.translations`，其 `download` 返回 `no_result` 为预期；文件任务 `download`：单文件工单直接回传该文件产物（含「单文件×多语言」——各语言产物在工单服务层预打包为 `<名>_translated.zip`，见 `service/ticket.go zipOutputs`）；多文件工单缺省打包全部产物 zip，`?file_id=` 取单文件；同步 `translate` 响应 `points_used` 现回填真实用量（积分口径，2026-09-19 起 token 裸值不再外发）（引擎注入用量收集器后由 `UsageTokens` 汇总，与 balance/usage 一致——2026-08-28 前曾恒为 0，已由提交 91dc8c5 修复，详见 `PROGRESS.md` 〇-E）。

## 十、PostgreSQL 切流（2026-08-30 已落地）

> 状态：生产已从 SQLite 切到 **PostgreSQL 16 + pgvector 0.6.0**（同机自建，非独立 RDS）。
> 验证：`curl /status` 返回 `dialect":"postgres"`；PG 内行数与原 SQLite 完全一致（users 10 / tickets 38 / tm_segments 4014 / audit_logs 513 …）。

### 10.1 启用方式（systemd）
`/etc/systemd/system/translator.service` 增加两行（原密钥保持不变）：
```ini
Environment=DB_DRIVER=postgres
Environment="DB_DSN=postgres://langcross:<密码>@127.0.0.1:5432/langcross?sslmode=disable"
```
DSN 明文存于服务器 `/opt/translator/pg_dsn.txt`（0600，仅本机）；数据库角色 `langcross`，库 `langcross`。

### 10.2 数据库底座
- 安装：`apt-get install -y postgresql postgresql-16-pgvector`
- 扩展：`CREATE EXTENSION vector;`（建库时已自动启用）
- 1.6G 机器调优（★ 2026-09-14 S0 修订）：`/etc/postgresql/16/main/conf.d/02-seoul-2g.conf`
  ```
  shared_buffers = 256MB        effective_cache_size = 768MB
  max_connections = 40          work_mem = 16MB
  maintenance_work_mem = 128MB  random_page_cost = 1.1
  checkpoint_completion_target = 0.9   wal_buffers = 16MB
  default_statistics_target = 100
  ```
  > 历史：08-30 切 PG 时按"2G 机器"配 shared_buffers=512MB（生产实际漂移到 640MB）。
  > S0 决策实测宿主机仅 1613M，主站提顶到 1229M 的前提就是 PG 让出 ~380M 常驻内存；
  > 当前库体量（业务+KB 向量 <1GB）256MB 足够。**shared_buffers 调整需重启 postgresql 生效。**

### 10.3 切流步骤回顾（供重做/新机参考）
1. 停服：`systemctl stop translator`
2. 建 schema：`DB_DRIVER=postgres DB_DSN=... translator-server --init-db`
3. 迁数据：`migrate-sqlite-to-pg -sqlite /opt/translator/data/tm.sqlite3 -dsn "..."`
   （跳过 jobs/ticket_state 瞬态表；`ON CONFLICT DO NOTHING` 幂等。admin 密码哈希需从源库同步回 PG，否则登录失败）
4. 填向量：`backfill-embeddings -npz /opt/translator/data/tm_embeddings.npz`（复用 npz 已算向量写回 `tm_segments.embedding`，零成本、秒级；已写回 3347 条）
5. 起服：`systemctl start translator`

### 10.4 相关工具（已部署于 /opt/translator/bin/）
| 工具 | 作用 |
|------|------|
| `migrate-sqlite-to-pg` | SQLite → PG 一次性数据迁移 |
| `backfill-embeddings` | npz 向量 → PG `embedding` 列（启用 pgvector 原生检索，低成本） |
| `rebuild-kb-index` | 调用 Embedding API **重新计算**向量写回 PG（需 `ONLINE_API_KEY`/`EMBED_API_KEY`；覆盖 npz 缺失向量的段，可选） |

### 10.5 回滚
- 改回 SQLite：删掉 10.1 的 `DB_DRIVER`/`DB_DSN` 两行（或 `cp /etc/systemd/system/translator.service.pre-pg.bak /etc/systemd/system/translator.service`），`systemctl daemon-reload && systemctl restart translator`。
- 源 SQLite 未被改动，备份在 `/opt/translator/data/backups/tm.sqlite3.pre-pg-<时间戳>.bak`；切流前也建议 `cp tm.sqlite3 tm.sqlite3.bak`。

## 十一、备份与回滚

- 每次部署前自动备份 `translator-server.bak.<时间戳>`
- 业务数据（PostgreSQL）：`pg_dump "$DB_DSN" > backups/langcross-<时间戳>.sql`（切流前 SQLite 源库保留于 `data/backups/tm.sqlite3.pre-pg-*.bak`）
- 向量数据：`/opt/translator/data/tm_embeddings.npz`
- 回滚到 SQLite：`cp /etc/systemd/system/translator.service.pre-pg.bak /etc/systemd/system/translator.service && systemctl daemon-reload && systemctl restart translator`（见 §十.5）

### 十一-B、灾备演练与 RPO/RTO 实测（★ 2026-09-14）

**DB 备份还原（主链路，实测通过）**
- 应用每 24h + 每次启动做 `pg_dump -Fc` 到 `/opt/translator/data/backups/tm_*.bak.dump`（单份 ≈51MB）。
- **RPO**：≤24h（日备周期）；本次演练最新备份距当前 **33.5 分钟**。
- **RTO**：`pg_restore` 51MB 到 scratch 库 **8.0 秒**；瓶颈在「发现故障+重启应用」而非还原本身。
- **演练法**（scratch 库隔离，行数对账后自动 DROP）：
  ```bash
  LATEST=$(ls -t /opt/translator/data/backups/tm_*.bak.dump | head -1); S=drill_$(date +%s)
  sudo -u postgres createdb "$S"
  sudo -u postgres pg_restore -d "$S" --no-owner "$LATEST"
  for t in users tenants orders usage_ledger quota_grants tickets packages system_config alerts; do
    a=$(sudo -u postgres psql -d langcross -Atc "SELECT count(*) FROM $t")
    b=$(sudo -u postgres psql -d "$S" -Atc "SELECT count(*) FROM $t")
    [ "$a" = "$b" ] && echo "ok $t=$a" || echo "MISMATCH $t a=$a b=$b"; done
  sudo -u postgres dropdb "$S"
  ```
  本次 9 表对账全 ok。
- **运维注意点（已处置）**：备份按 `backup_keep` **份数**清理，同日多次重启会挤掉日备（曾最旧仅 2.4h）→ 已置 `system_config.backup_keep=14`（51M×14≈714M，磁盘余量充足）。
- **未闭环（🔴 开闸前必补）**：`backup_remote_cmd` 仍空=无异地副本，同机故障备份随库一起丢（《生产配置清单》§一.2 rclone 方案）。异地副本落地前，RPO 口径仅对同机误操作/误删有效。

**S9 监控件重建（RTO ≈5 分钟，无状态）**：`apt install prometheus prometheus-node-exporter` + 解压 alertmanager 二进制 + §八-B8 四件套配置即恢复；应用回 8787 后自动重抓。

**应用二进制回滚**：`translator-server.bak.<ts>` mv 覆盖回 + `systemctl restart translator`（<10 秒）。

**异地副本（拉取式，★ 09-14 闭环）**：服务器零改动；运维 Mac `crontab 每日 10:00` 执行 `~/Backups/tools/fetch_backups_local.sh`（rsync 最新 3 份→`~/Backups/langcross/`，sha256 远端/本地比对，留 7 份）。注意：**笔记本硬盘也是单点**，重要周期建议另开云盘同步该目录；服务器侧 `backup_remote_cmd`（rclone 推 OSS）为可选升级。

**全链路商业化演练（开闸验收，可重放）**：`bash deploy/chain_drill.sh`（服务器 root）——取最新计划备份还原 scratch 库、起一次性实例（127.0.0.1:8799，隔离 USER_DATA_DIR，scratch 内清 captcha/邮件/webhook、pay_mode=mock），curl 走真实 HTTP：注册(UTM 归因/礼包 1000 分/默认 Key)→翻译扣费→耗尽硬停→充值 manual 单→「我已付费」→超管确认到账→退款回收，共 19 断言；trap 兜底 kill 实例+dropdb+清临时，**不触碰生产实例与库**。2026-09-14 首跑 19/19（记录见 PROGRESS 〇-XXXV）。


## 十三、AI 助手（assist，★ autosales；2026-09-17 改造 1A 融合后更新）

> ★ 改造 1A（2026-09-17）：ai-assist 已**并入主仓单 module**（`backend-go/internal/assist/*`，
> 入口 `backend-go/cmd/assist-server`），独立 SQLite 数据隔离不变。关键变化：
> ① 单构建产物——管理台页面与初始 seed 经 `go:embed` 随二进制编译，**不再需要投放 `web/` 与 `seed/`**；
> ② 统一观测——日志改走主仓 slog JSON（带 trace_id），不再散落 `log.Printf`；
> ③ 鉴权贯通——管理 Token 免手填（详见下「管理凭证」行）；
> ④ CI 补齐——`assist` 单测随 backend job、`assist_uat.sh` 独立 assist job（此前 ci.yml 零处提及 assist）。

从 ai-scrm-v2 剥离的轻量接待服务，独立进程 + SQLite，与主站单体解耦：
挂件挂掉只影响右下角助手（前端显示离线提示），不阻塞翻译主功能。

| 项 | 值 |
|----|-----|
| 服务器目录 | `/opt/ai-assist/`（仅 bin/data；web/seed 已内嵌，目录可不存在） |
| systemd | `ai-assist.service`（User=translator，密钥 `/etc/ai-assist/secrets.env` 0600） |
| 监听 | 127.0.0.1:8790，Caddy `handle /assist-api/*` 反代（见 §九 conf） |
| CSP | `frame-src 'self' …`（2026-09-16 增补：管理台同源 iframe 所需） |
| 管理台 | 站内「管理后台 → 🤖 AI 助手」（L3+），或直连 `/assist-api/assist/admin` |
| 管理凭证 | **免手填**：主后台「AI 助手」面板经 `/api/admin/assist/token`（仅超管）下发并注入同源 localStorage。生效 Token 解析链：env `ASSIST_ADMIN_TOKEN`（优先/保底）→ 主库 `system_config.assist_admin_token`（enc:v1: 密文，可在该面板轮换）→ 内置默认值 |
| 源码/构建 | 主仓 `backend-go/`；`cd backend-go && CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o dist/translator-assist ./cmd/assist-server` |

上线行为：官网落地页/前台工作台/管理后台右下角常驻挂件（登录/注册页自动隐藏）。
未配 LLM Key 时为**规则模式**：话术直配/引导流程/知识库兜底均可用；配 Key 后开全 AI。

启用全 AI（拿到硅基流动或任意 OpenAI 兼容 Key 后，★ 2026-09-16 R0 起两法任选）：
```bash
# 法一（推荐）：主站管理后台在线配置，免登服务器免重启
#   管理后台 → 🤖 AI 助手 → 配置 → 「🤖 LLM 接入」卡：
#   填 llm_base_url / llm_api_key / llm_model（+可选备用模型）→ 🔌测试连通 → 保存
#   保存后下一条对话即生效（引擎热加载）；api_key 仅以掩码回显；顶部徽标显示当前模式
#   （✅已接入(后台配置) / ✅已接入(环境变量) / ⚠️规则模式）

# 法二（env 兜底，优先级高于管理台配置；CI/多实例注入场景用）：
ssh root@43.108.86.140
vi /etc/ai-assist/secrets.env   # 取消注释 ASSIST_LLM_* 四行并填 Key/模型
systemctl restart ai-assist && journalctl -u ai-assist -n 5 --no-pager
```

> ★ R0 同义词表（口语问法对齐知识库关键词）：seed 仅空表灌入，**存量部署需在管理台
> 「配置 → 同义词归一」粘贴一次**（默认三组：充值=充钱|交钱|付钱|怎么充 / 翻译=翻一下|翻个|转成 / 价格=多少钱|费用|贵不）。

> ★ 管理 Token 免手填：生产（PG）按下方 P2-8 检查单配置 `ASSIST_ADMIN_TOKEN`；
> `MAIN_DB` SQLite 桥接仅适用于主服务仍为 SQLite 的老部署，切 PG 后不再配置。
> `/assist-api` 必须是同源路径反代（父页面与 iframe 共用 localStorage 才能自动注入）。

> ★ P2-8（2026-09-18）PG 生产部署 ASSIST_ADMIN_TOKEN 检查单（缺一即「免手填」失效）：
> 1. `/etc/ai-assist/secrets.env` 已配 `ASSIST_ADMIN_TOKEN=<强随机值>`（0600，与主库 `system_config.assist_admin_token` 同值）；
> 2. 主服务切 PG 后不要再配 `MAIN_DB`（SQLite 桥接已不可用，留着只会误导排障）；
> 3. Caddy 保留 `/assist-api/*` 同源反代（跨域时 iframe localStorage 注入必失败）；
> 4. 自检：管理后台「🤖 AI 助手」面板徽标应显示 Token 已注入（401 时按面板自助指引核对 1）；
>    轮换走 `/api/admin/assist/token`（仅超管）后，须同步更新 secrets.env 并 `systemctl restart ai-assist`。

验证：
```bash
curl -s https://langcross.lexicorn.cn/assist-api/health                                  # {"ok":true}
curl -s https://langcross.lexicorn.cn/assist-api/api/assist/greeting                     # 欢迎词+chips
curl -s -X POST https://langcross.lexicorn.cn/assist-api/api/assist/chat \
  -H 'Content-Type: application/json' -d '{"session":"t1","message":"怎么充钱"}'           # 命中充值引导（R0 同义词）
curl -s -o /dev/null -w '%{http_code}\n' https://langcross.lexicorn.cn/assist-api/assist/admin   # 200
# 本机（服务器上）管理 API 抽查 llm_mode 与待补料清单：
curl -s 127.0.0.1:8790/api/assist/admin/sessions -H "X-Assist-Admin: <token>" | grep -o '"llm_mode":"[a-z]*"'
```
自动化回归：`bash scripts/uat/assist_uat.sh`（**38 断言**，本机自起临时实例，不碰生产；
含内嵌 seed/管理页、主库 Token 桥接与 env 优先级契约、复合意图让位 CI1/CI2）。CI：`.github/workflows/ci.yml`
的 `assist` job 自动跑该脚本，单测随 `backend` job 的 `./internal/...` 覆盖。

运维注意：
- **更新发布**：新二进制 scp 到 `/tmp/translator-assist-new` 后 `mv` rename 到 `/opt/ai-assist/bin/translator-assist`（防 ETXTBSY，同主站口径）再 `systemctl restart ai-assist`。
  ⚠️ 融合后二进制名由 `assist-server` 变更为 `translator-assist`，首升需同步改 unit 的 `ExecStart` 并删掉旧的 `assist-server` 文件。
- **静态资源**：管理页与 seed 已内嵌，升级后 `/opt/ai-assist/web`、`/opt/ai-assist/seed` 可删除；
  仍可用 `ASSIST_WEB` / `ASSIST_SEED` 指向外置目录做临时覆盖（显式配置时外置优先）。
- **知识库/话术/流程**：管理台在线改，落 `/opt/ai-assist/data/assist.db`；seed 仅首启空表灌入，不会覆盖线上编辑。回滚数据：直接删 `assist.db*` 重启（会重新灌 seed，历史会话丢失）
- **内存**：MemoryMax=150M；日志 `journalctl -u ai-assist` 或 `/opt/ai-assist/data/assist.log`（★ 融合后为标准 slog JSON，单行一条，含 trace_id）
- **CSP 分流（★ 2026-09-16 管理台拒嵌踩坑）**：整站严格 CSP 的 `frame-ancestors 'none'` 会连带套在 `/assist-api/*` 响应上，管理台 iframe 被拒。已在 Caddy 用 `@strict_csp not path /assist-api/*` 互斥分流（严格版+Cloudflare beacon 域 / assist 宽松版 frame-ancestors 'self'，X-Frame-Options 同口径 DENY/SAMEORIGIN）。⚠️ Caddy `header{}` 块内不支持 matcher 语法（validate 静默通过但完全不生效），matcher 必须定义在块外；改 CSP 后四路响应头务必实测（主站/strict、/api/strict、/assist-api 页面/API 宽松）
- 仓库模板：`deploy/systemd/ai-assist.service`、`deploy/caddy/translator.conf`（含 assist 段与 CSP 分流）；
  服务源码说明见 `backend-go/internal/assist/README.md`

## 十四、常见问题

| 问题 | 排查 |
|------|------|
| 服务重启后访问 502 | `systemctl status translator` 看退出原因；确认 `-addr 127.0.0.1:8787` 未被占用 |
| 前端页面白屏/样式错乱 | 确认 `/opt/translator/web` 下 `assets/` 已更新（清浏览器缓存） |
| 翻译错误率高 | `curl /metrics` 看 `translator_llm_error_rate`；检查 `model_routes` 配置与供应商 Key |
| 告警面板提示 | 看门狗 5 分钟周期；余额阈值/熔断/错误率告警自动进入 alerts 表 |
| 本地测试库残留假路由 | `sqlite3 <kbdb> "DELETE FROM system_config WHERE key='model_routes'"` 后重启 |
| 演示站首页返回「前端页面尚未构建」 | `bootstrap-demo.sh` 若缺对 `$DEMO_DIR/web` 的 `chmod -R o+rX`，web 目录保持 root:caddy 750/640，translator 进程（后端 handleSPA）读不到 index.html。修复：`chmod -R o+rX /opt/translator-demo/web`，并将脚本升级到含该步骤的版本 |
| ★ 审计列表「停更」（2026-09-15 案例：界面停在切流前一天） | SQLite→PG 切流按显式 id 导入但序列未同步 → 新行拿低位 id，旧 `ORDER BY id DESC` 把新审计沉底（假性停更），且序列逼近存量后主键冲突**静默丢审计**。修复三层：①启动自愈 `store.syncSequencesPG`（setval max(id)+1000，幂等，覆盖 audit_logs/ticket_files/eval_records 等 9+ 表）；②审计列表排序改 `created_at DESC, id DESC`；③存量热修 DO 块两库逐表 setval（同下条手工口径，批量脚本见 PROGRESS 〇-XXXVIII）。判别法：`SELECT (SELECT last_value FROM xxx_id_seq),(SELECT MAX(id) FROM xxx);` 前者小于后者即失步 |
| 用户注册/演示种号报 `users_pkey` 主键冲突 | `users_id_seq` 序列失步（2026-09-07 生产曾 =1 而 MAX(id)=10007）。克隆到演示站后继承失步序列，插入即撞主键。修复：`SELECT setval('users_id_seq', GREATEST((SELECT COALESCE(MAX(id),1) FROM users),1), true)`（生产+演示两库都要执行；同型序列失步此前还发生在 notifications/api_keys/kb_entries，见 PROGRESS 〇-IV） |
| 服务崩溃 `fatal error: concurrent map writes` | 旧版 metrics 计数映射并发读写竞态（`internal/api/metrics.go`）。已由提交 `48afb94` 用 `sync.RWMutex` 修复；如遇旧二进制崩溃请升级到该提交之后的版本 |
| 翻译 PDF 却下载到 Excel（演示站，旧症状） | 站点 `bin/` 缺 `docx_translate.py`/`pdfwrite.py` 脚本 → PDF 写回失败。★ 2026-09-13 起写回失败自动降级为纯文案 .md 交付（工单成功 + warning + 站内信），「下载到 Excel / 工单失败」均已被降级替代。修复：从主站拷贝脚本（含 `anydoc_md.py`）到 `bin/` 并 `chown translator:translator`，确认 venv 库可导入（见 §五 部署注意事项） |
| 纯文案模式建单被拒：「老格式仅在纯文案模式支持」/「anydoc 依赖未安装」 | 前者是还原模式白名单正确拦截（改用纯文案模式或先转新版格式）；后者为 venv 缺 `firecrawl-anydoc` 或 `bin/anydoc_md.py` 缺失（`/api/health` 的 `anydoc_ready=false` 可确认），按 §一/§五 补装 |
| 升级 2026-09-12 之后版本，租户子域（如 `rox.`/`rox-test.`）logo/登录页背景/标题丢失 | B11 运营身份硬编码收敛移除 `lexicorn.cn` 内置兜底：`system_config.base_domain`（及主站的 `primary_host`）未显式配置时，子域→租户品牌解析关闭，`/api/tenant/branding` 返回平台默认空品牌（`tenant_id:0`）。修复：psql 补 `base_domain=<基础域>` + `primary_host=<主站主机名>`（见 §八-B 两行；演示站重跑 bootstrap-demo.sh 幂等自愈）。★ 2026-09-14 演示站发版实测踩坑记录 |
| 工单报「XX 翻译失败：N/N 段未能译出」 | 漏译率硬闸正确拦截（★ 2026-09-10）：某语言未译出 >50% 即置工单失败。根因通常是余额配额耗尽或模型调用瞬时故障，重新发起即可；若反复出现请核查该租户余额/实时计费日志（`[usagesink] flush tenant=%d 余额不足` / `[translate] ... 翻译失败`） |
| 对话翻译某语言缺失但无报错 | ★ 2026-09-10 起不再静默：余额不足实时中止时回复尾部追加「⚠️ 余额不足：本次翻译可能不完整，请充值后重新发送」；工单建单前亦预检余额。若未见提示且语言缺失，核查 `journalctl -u translator-demo --since` 的 `[translate]` 失败日志 |
| 硬闸校验出现「术语遵循」失败 | 正常护栏：源文含知识库 L1 术语（如「极石」→ROX）而译文未体现规定译法，硬闸自动附 KB 提示重译（默认 ≤8 次）。若重译仍失败需检查 KB 术语目标译文是否合理（如阿拉伯语 ROX 未命中模型输出） |