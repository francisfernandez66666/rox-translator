# 翻译助手 SaaS 平台 · 部署指南

> 更新：2026-08-20 ｜ 架构：Go 单二进制 + Vue3 前端 + SQLite
> 生产环境：阿里云服务器 43.108.86.140（Ubuntu 24.04，1.6G 内存）
> 生产地址：`https://translator.quant-trading.top`（Caddy 反代 → 127.0.0.1:8787）

---

## 一、环境与目录

| 项 | 说明 |
|----|------|
| 服务器 | 43.108.86.140（Ubuntu 24.04，root） |
| 应用目录 | `/opt/translator/` |
| 服务管理 | systemd `translator.service` |
| 反向代理 | Caddy（`translator.quant-trading.top` → 127.0.0.1:8787，SSL 已签发） |
| 数据库 | SQLite 单文件（知识库 `tm.sqlite3` + 业务库同文件） |
| 向量文件 | `tm_embeddings.npz`（1024 维） |

生产目录结构：
```
/opt/translator/
├── bin/
│   ├── translator-server          # Go 可执行文件（Linux amd64）
│   └── translator-server.bak.*    # 部署前自动备份（带时间戳）
├── web/                           # 前端 dist 静态文件
│   ├── index.html
│   └── assets/
├── data/
│   ├── tm.sqlite3                 # 知识库 SQLite
│   ├── tm_embeddings.npz          # 知识库向量
│   └── ...（_uploads/_output 等运行时目录）
└── log/
    └── translator.log             # 服务日志
```

## 二、本地开发环境

| 工具 | 用途 | 检查命令 |
|------|------|----------|
| Go 1.22+ | 后端编译 | `go version` |
| Node.js 18+ | 前端构建 | `node --version` |
| npm | 前端包管理 | `npm --version` |

## 三、本地构建

```bash
# 1. 后端构建（本地验证用 macOS 版）
cd backend-go
go build -o /tmp/translator-server-dbg ./cmd/server

# 2. 前端构建
cd frontend
npm install        # 首次
npm run build      # 输出到 frontend/dist

# 3. 本地启动
/tmp/translator-server-dbg \
  -addr 127.0.0.1:8792 \
  -frontend /Users/xxx/翻译助手/frontend/dist \
  -kb /Users/xxx/翻译助手/backend/data/tm_embeddings.npz \
  -kbdb /tmp/t3.db
```

本地默认账号：`admin/admin123`（若测试库为空会自动初始化超管）。

## 四、交叉编译 Linux 二进制

```bash
cd backend-go
GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build -ldflags="-s -w" -o /tmp/translator-server-linux ./cmd/server
```

## 五、部署到生产

```bash
# 1. 上传二进制与前端
scp /tmp/translator-server-linux root@43.108.86.140:/tmp/translator-server-new
cd frontend && tar czf /tmp/frontend-dist.tar.gz dist
scp /tmp/frontend-dist.tar.gz root@43.108.86.140:/tmp/

# 2. 备份旧版本 + 替换 + 重启
TS=$(date +%Y%m%d_%H%M%S)
ssh root@43.108.86.140 "
  cp /opt/translator/bin/translator-server /opt/translator/bin/translator-server.bak.$TS
  mv /tmp/translator-server-new /opt/translator/bin/translator-server
  chmod +x /opt/translator/bin/translator-server
  mv /opt/translator/web /opt/translator/web_old 2>/dev/null
  mkdir -p /opt/translator/web
  tar xzf /tmp/frontend-dist.tar.gz -C /opt/translator/web --strip-components=1
  systemctl restart translator
  systemctl is-active translator
"
```

## 六、systemd 服务

`/etc/systemd/system/translator.service`：
```ini
[Unit]
Description=Translator SaaS Service
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/translator
ExecStart=/opt/translator/bin/translator-server \
  -addr 127.0.0.1:8787 \
  -frontend /opt/translator/web \
  -kb /opt/translator/data/tm_embeddings.npz \
  -kbdb /opt/translator/data/tm.sqlite3
StandardOutput=append:/opt/translator/log/translator.log
StandardError=append:/opt/translator/log/translator.log
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
```

```bash
systemctl daemon-reload
systemctl enable translator
systemctl start translator
systemctl status translator
```

## 七、启动参数说明

| 参数 | 默认 | 说明 |
|------|------|------|
| `-addr` | `:8787` | HTTP 监听地址（生产用 127.0.0.1:8787 + Caddy 反代） |
| `-frontend` | `./frontend/dist` | 前端 dist 静态目录（留空则默认相对路径） |
| `-kb` | 空 | 知识库 `.npz` 向量文件路径；留空不加载向量检索 |
| `-kbdb` | `<npz 同名>.db` | 知识库 SQLite 缓存路径 |

## 八、环境变量（可选覆盖）

| 变量 | 说明 |
|------|------|
| `SILICONFLOW_API_KEY` | SiliconFlow 翻译模型 Key（默认内置兜底 Key） |
| `ONLINE_API_KEY` | 智谱 Embedding Key（默认内置兜底 Key） |
| `ONLINE_API_BASE` | Embedding API 基地址覆盖 |
| `ONLINE_MODEL` | 主翻译模型覆盖 |
| `ADMIN_TOKEN` | 管理接口凭证覆盖 |
| `USER_DATA_DIR` | 用户数据目录覆盖 |

## 九、Caddy 配置

```caddy
translator.quant-trading.top {
    reverse_proxy 127.0.0.1:8787
    encode gzip
}
```
（SSL 由 Caddy 自动签发/续期）

## 十、验证清单（部署后）

```bash
# 健康检查
curl -s https://translator.quant-trading.top/api/health
curl -s https://translator.quant-trading.top/metrics | grep ^translator_

# 登录（生产超管 taadmin）
curl -s -X POST https://translator.quant-trading.top/api/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"username":"taadmin","password":"taadmin123"}'

# 翻译
curl -s -X POST https://translator.quant-trading.top/api/chat \
  -H "Authorization: Bearer $TOKEN" \
  -H 'Content-Type: application/json' \
  -d '{"message":"翻译成英语：车辆远程诊断完成","options":{"target_langs":["en"]}}'

# 用量看板
curl -s https://translator.quant-trading.top/api/billing/usage -H "Authorization: Bearer $TOKEN"

# 查看服务日志（确认无 panic）
grep -c panic /opt/translator/log/translator.log
```

## 十一、备份与回滚

- 每次部署前自动备份 `translator-server.bak.<时间戳>`
- 业务数据：`/opt/translator/data/tm.sqlite3`（SQLite 单文件，可直接 cp 备份）
- 向量数据：`/opt/translator/data/tm_embeddings.npz`
- 回滚：`cp /opt/translator/bin/translator-server.bak.<时间戳> /opt/translator/bin/translator-server && systemctl restart translator`

## 十二、常见问题

| 问题 | 排查 |
|------|------|
| 服务重启后访问 502 | `systemctl status translator` 看退出原因；确认 `-addr 127.0.0.1:8787` 未被占用 |
| 前端页面白屏/样式错乱 | 确认 `/opt/translator/web` 下 `assets/` 已更新（清浏览器缓存） |
| 翻译错误率高 | `curl /metrics` 看 `translator_llm_error_rate`；检查 `model_routes` 配置与供应商 Key |
| 告警面板提示 | 看门狗 5 分钟周期；余额阈值/熔断/错误率告警自动进入 alerts 表 |
| 本地测试库残留假路由 | `sqlite3 <kbdb> "DELETE FROM system_config WHERE key='model_routes'"` 后重启 |