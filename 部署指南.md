# ============================================================================
# ROX Agent 部署指南 — 从0开始，一步一步来
# ============================================================================
# 适用对象：0基础小白，macOS + M1芯片
# 最终效果：本地运行极石智能助手，支持翻译/PRD/出图三个技能
# ============================================================================

---

## 📋 你需要的环境

| 工具 | 用途 | 检查命令 | 没有的话 |
|------|------|----------|----------|
| Python 3.10+ | 后端运行 | `python3 --version` | 去 python.org 下载 |
| Node.js 22+ | 前端运行 | `node --version` | 去 nodejs.org 下载 |
| npm | 前端包管理 | `npm --version` | 随Node.js自带 |

---

## 第1步：创建项目目录

打开终端（Command+空格 搜"终端"），逐行粘贴执行：

```bash
# 在桌面创建项目文件夹
mkdir -p ~/Desktop/ROX-agent/backend/skills/{translation,prd,image}
mkdir -p ~/Desktop/ROX-agent/backend/data
mkdir -p ~/Desktop/ROX-agent/frontend/src/{api,components,stores,types}

# 验证目录创建成功
echo "=== 目录结构 ==="
find ~/Desktop/ROX-agent -type d | sort
```

**期望输出：** 看到类似这样的目录树
```
/Users/你的用户名/Desktop/ROX-agent
/Users/你的用户名/Desktop/ROX-agent/backend
/Users/你的用户名/Desktop/ROX-agent/backend/data
/Users/你的用户名/Desktop/ROX-agent/backend/skills
/Users/你的用户名/Desktop/ROX-agent/backend/skills/image
/Users/你的用户名/Desktop/ROX-agent/backend/skills/prd
/Users/你的用户名/Desktop/ROX-agent/backend/skills/translation
/Users/你的用户名/Desktop/ROX-agent/frontend
/Users/你的用户名/Desktop/ROX-agent/frontend/src
/Users/你的用户名/Desktop/ROX-agent/frontend/src/api
...（省略）
```

---

## 第2步：把代码文件放进去

把上面输出的26个文件，按目录结构放到对应位置。

**最简单的方式：** 用 Finder 打开桌面的 ROX-agent 文件夹，把文件拖进去。

**验证所有文件都在：**

```bash
echo "=== 后端文件 ==="
ls -la ~/Desktop/ROX-agent/backend/
echo ""
echo "=== 技能文件 ==="
ls -la ~/Desktop/ROX-agent/backend/skills/translation/
ls -la ~/Desktop/ROX-agent/backend/skills/prd/
ls -la ~/Desktop/ROX-agent/backend/skills/image/
echo ""
echo "=== 前端文件 ==="
ls -la ~/Desktop/ROX-agent/frontend/
ls -la ~/Desktop/ROX-agent/frontend/src/
ls -la ~/Desktop/ROX-agent/frontend/src/components/
```

**期望输出：** 每个目录下都有对应的文件，没有报错

---

## 第3步：复制知识库数据

翻译功能需要知识库文件（你旧项目里已有的）：

```bash
# 从旧项目复制知识库到新项目（替换路径为你的旧项目实际路径）
cp ~/Desktop/ROX-agent旧版/data/tm.sqlite3 ~/Desktop/ROX-agent/backend/data/
cp ~/Desktop/ROX-agent旧版/data/tm_embeddings.npz ~/Desktop/ROX-agent/backend/data/

# 验证
echo "=== 知识库数据 ==="
ls -lh ~/Desktop/ROX-agent/backend/data/
```

**期望输出：**
```
tm.sqlite3       （约3MB）
tm_embeddings.npz（约13MB）
```

⚠️ 如果没有这两个文件，翻译功能仍然可以工作（走在线模型兜底），只是无法知识库精确匹配。

---

## 第4步：配置API密钥

用任意文本编辑器打开 `~/Desktop/ROX-agent/backend/.env`，把API Key填上：

```bash
# 用VS Code打开（推荐）
open -a "Visual Studio Code" ~/Desktop/ROX-agent/backend/.env

# 或者用系统文本编辑器
open -e ~/Desktop/ROX-agent/backend/.env
```

**需要修改的行：**
```
ONLINE_API_KEY=你的API_KEY写这里    ← 改成你的智谱API Key
REVIEW_API_KEY=你的API_KEY写这里    ← 改成你的智谱API Key
```

💡 智谱API Key获取：去 https://open.bigmodel.cn 注册，在"API密钥"页面创建，免费。

**验证配置：**
```bash
cd ~/Desktop/ROX-agent/backend
python3 -c "
from dotenv import load_dotenv
import os
load_dotenv()
key = os.getenv('ONLINE_API_KEY', '')
if key and key != '你的API_KEY写这里':
    print('✅ API Key已配置')
else:
    print('⚠️ API Key未配置，在线翻译不可用')
"
```

---

## 第5步：安装Python依赖

```bash
cd ~/Desktop/ROX-agent/backend

# 安装依赖（约30秒）
pip3 install -r requirements.txt

# 验证
python3 -c "
import fastapi; print('✅ fastapi', fastapi.__version__)
import uvicorn; print('✅ uvicorn', uvicorn.__version__)
import openai; print('✅ openai', openai.__version__)
import numpy; print('✅ numpy', numpy.__version__)
import requests; print('✅ requests', requests.__version__)
"
```

**期望输出：** 每行都显示 ✅ + 版本号

⚠️ 如果报错 `No module named 'xxx'`，手动装：
```bash
pip3 install xxx
```

---

## 第6步：安装前端依赖

```bash
cd ~/Desktop/ROX-agent/frontend

# 安装依赖（首次约1~2分钟）
npm install

# 验证
echo "=== node_modules ==="
ls node_modules/.package-lock.json && echo "✅ 前端依赖安装成功" || echo "❌ 安装失败"
```

---

## 第7步：验证后端能启动

```bash
cd ~/Desktop/ROX-agent/backend

# 启动后端（会占用终端，测试完按 Ctrl+C 停掉）
python3 main.py
```

**期望输出：**
```
==================================================
🚀 ROX Agent 启动中...
==================================================
🔍 扫描技能目录: .../skills
  ✅ 技能已注册: image — 极石出图：...
  ✅ 技能已注册: prd — 极石PRD生成：...
  ✅ 技能已注册: translation — 多语言翻译：...
🎯 技能加载完成，共 3 个: ['image', 'prd', 'translation']
==================================================
✅ 启动完成！已加载 3 个技能
==================================================
INFO:     Uvicorn running on http://127.0.0.1:8000
```

**另开一个终端窗口测试API：**
```bash
# 测试健康检查
curl http://127.0.0.1:8000/api/health

# 期望输出：{"status":"ok","version":"2.0.0","skills":["image","prd","translation"]}

# 测试翻译
curl -X POST http://127.0.0.1:8000/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "翻译：检查胎压"}'

# 期望输出：{"skill":"translation","reply":"...翻译结果...","data":{...}}

# 测试PRD
curl -X POST http://127.0.0.1:8000/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "写一个车机语音助手的PRD"}'

# 期望输出：{"skill":"prd","reply":"📄 PRD已生成：...","data":{...}}

# 测试出图（占位）
curl -X POST http://127.0.0.1:8000/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "画一张海报"}'

# 期望输出：{"skill":"image","reply":"🎨 收到出图需求...功能开发中...","data":{"status":"coming_soon"}}
```

**测试完停掉后端：** 在后端终端按 `Ctrl+C`

---

## 第8步：验证前端能启动

```bash
cd ~/Desktop/ROX-agent/frontend

# 启动前端开发服务器
npm run dev
```

**期望输出：**
```
VITE v8.x.x  ready in xxx ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

浏览器打开 http://localhost:5173 ，应该能看到：
- 标题栏 "🚗 ROX 智能助手" + 技能标签 [🌐 翻译] [📄 PRD] [🎨 出图]
- 欢迎页 + 3个快捷按钮
- 底部输入框

⚠️ 此时点按钮会报错（后端没启动），没关系，下一步一起启动。

**测试完停掉前端：** 终端按 `Ctrl+C`

---

## 第9步：一键启动（正式使用）

```bash
cd ~/Desktop/ROX-agent

# 一键启动前后端
python3 launcher.py
```

**期望输出：**
```
==================================================
🚀 ROX Agent 一键启动器 (技能可插拔架构 v2.0)
📁 项目路径: /Users/xxx/Desktop/ROX-agent
==================================================
✅ 日志目录就绪
🔍 检查Python依赖...
✅ Python依赖OK
🔍 检查前端依赖...
✅ 前端依赖已存在
🚀 启动后端 (Port: 8000)...
✅ 后端启动完成，已加载技能: ['image', 'prd', 'translation']
🚀 启动前端 (Port: 5173)...
✅ 前端启动完成
💻 打开浏览器...

==================================================
🎉 启动完成！
👉 前端: http://localhost:5173
👉 API文档: http://localhost:8000/docs
⚠️ 按 Ctrl+C 停止所有服务
==================================================
```

浏览器自动打开 http://localhost:5173 ，试试输入：
- `翻译：检查胎压` → 翻译技能
- `写一个车机语音助手的PRD` → PRD技能
- `画一张海报` → 出图技能（当前为占位）

**停止服务：** 终端按 `Ctrl+C`

---

## 🔧 常见问题

### Q1: 后端启动报错 `ModuleNotFoundError: No module named 'xxx'`
```bash
# 手动安装缺失的包
pip3 install xxx

# 或者重新装全部依赖
cd ~/Desktop/ROX-agent/backend
pip3 install -r requirements.txt
```

### Q2: 前端启动报错 `command not found: vite`
```bash
cd ~/Desktop/ROX-agent/frontend
rm -rf node_modules        # 删掉旧的
npm install                # 重新安装
```

### Q3: 前端页面空白 / 不显示
- 确认打开的是 http://localhost:5173 （不是8000）
- 确认后端也在运行（另开终端 `curl http://127.0.0.1:8000/api/health`）

### Q4: 翻译报错 `ONLINE_API_KEY not configured`
- 检查 `.env` 文件是否在 `backend/` 目录下
- 检查 API Key 是否已填写（不是默认的"你的API_KEY写这里"）

### Q5: 翻译报错 `Connection refused` (Ollama)
- Ollama没启动，不影响在线翻译
- 如需离线翻译：`ollama serve` 启动Ollama

### Q6: 端口被占用
```bash
# 查看8000端口占用
lsof -i :8000
# 杀掉占用进程
kill -9 PID号

# 查看5173端口占用
lsof -i :5173
kill -9 PID号
```

---

## 🧩 新增技能（3步走）

想加一个新技能（比如"手册查询"），只需要：

### 步骤1：建文件夹
```bash
mkdir -p ~/Desktop/ROX-agent/backend/skills/manual
```

### 步骤2：写 skill.py
在 `skills/manual/skill.py` 里写一个继承 BaseSkill 的类：

```python
import os, sys
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from base_skill import BaseSkill

class ManualSkill(BaseSkill):
    @property
    def name(self) -> str:
        return "manual"                    # 技能唯一标识

    @property
    def description(self) -> str:
        return "手册查询：输入问题，查询极石汽车用户手册"  # 技能描述

    @property
    def keywords(self) -> list[str]:
        return ["手册", "说明书", "怎么用", "如何操作"]     # 触发关键词

    def handle(self, params: dict) -> dict:
        message = params.get("message", "")
        # 你的处理逻辑写在这里...
        return {
            "skill": self.name,
            "reply": f"📖 手册查询：{message}",
            "data": {},
        }
```

### 步骤3：写 __init__.py
在 `skills/manual/__init__.py` 里写：
```python
from .skill import ManualSkill
__all__ = ["ManualSkill"]
```

**重启后端** → 新技能自动被发现并注册，前端自动显示新标签！

---

## 📁 最终目录结构

```
~/Desktop/ROX-agent/
│
├── launcher.py                    # 一键启动器
│
├── backend/                       # 后端（Python FastAPI）
│   ├── main.py                    # FastAPI主入口，统一API路由
│   ├── base_skill.py              # 技能基类，所有技能继承它
│   ├── skill_registry.py          # 技能注册器，自动扫描+智能路由
│   ├── .env                       # API密钥配置（⚠️不要提交Git）
│   ├── requirements.txt           # Python依赖列表
│   ├── data/                      # 知识库数据
│   │   ├── tm.sqlite3             # 翻译记忆库（SQLite）
│   │   └── tm_embeddings.npz      # 向量索引（NumPy）
│   └── skills/                    # 技能目录（可插拔）
│       ├── translation/           # 翻译技能
│       │   ├── __init__.py
│       │   ├── skill.py           # 翻译技能逻辑（对接lib.py）
│       │   └── lib.py             # 翻译核心引擎（你已写好的）
│       ├── prd/                   # PRD生成技能
│       │   ├── __init__.py
│       │   └── skill.py           # PRD生成逻辑
│       └── image/                 # 出图技能
│           ├── __init__.py
│           └── skill.py           # 出图逻辑（当前占位）
│
└── frontend/                      # 前端（Vue3 + TypeScript）
    ├── index.html                 # HTML入口
    ├── package.json               # 前端依赖配置
    ├── vite.config.ts             # Vite构建配置（含代理）
    ├── tsconfig.json              # TypeScript配置
    └── src/
        ├── main.ts                # Vue应用入口
        ├── env.d.ts               # Vite类型声明
        ├── App.vue                # 根组件（标题栏+聊天区）
        ├── api/
        │   └── index.ts           # 后端API客户端
        ├── stores/
        │   └── chat.ts            # Pinia状态管理
        ├── types/
        │   └── index.ts           # TypeScript类型定义
        └── components/
            ├── SkillBadge.vue     # 技能徽章组件
            ├── MessageBubble.vue  # 消息气泡组件
            └── ChatWindow.vue     # 聊天窗口组件
```

---

## 🔄 数据流（用户发一条消息的完整路径）

```
用户输入 "翻译：检查胎压"
    │
    ▼
前端 ChatWindow.vue
    │ store.sendMessage("翻译：检查胎压")
    ▼
前端 api/index.ts → POST /api/chat
    │
    ▼
后端 main.py → chat()
    │ registry.route("翻译：检查胎压")
    ▼
后端 skill_registry.py → 扫描3个技能的 can_handle()
    │ translation: 0.9 ← 最高！
    │ prd: 0.0
    │ image: 0.0
    ▼
后端 skills/translation/skill.py → handle()
    │ lib.translate_one("检查胎压", ...)
    ▼
后端 lib.py → 知识库匹配 → 在线模型翻译 → 语法复查
    │
    ▼
返回 { skill: "translation", reply: "翻译结果...", data: {...} }
    │
    ▼
前端 MessageBubble.vue → 渲染消息气泡 + 技能徽章 + 匹配报告
```
