# 上生产操作手册（2026-09-04 全量审计修复版）

> 对应审计修复批次：扩展安全三件套 / 错误码统一+OpenAPI 规范修复 / watchdog 默认自愈 / 队列租约加固。
> 生产：43.108.86.140（Ubuntu 24.04），Caddy → 127.0.0.1:8787，PostgreSQL 16 + pgvector。
> 前置：按《生产配置清单_20260904.md》配置 SMTP / 异地备份 / 开关。

---

## 一、本地构建

```bash
# 后端（Linux amd64 交叉编译；含本次所有 Go 改动）
cd backend-go
GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build -ldflags="-s -w" -o /tmp/translator-server-linux ./cmd/server

# 前端（含 core.ts 错误码透传）
cd ../frontend-react
npm run build    # 输出 frontend-react/dist
tar czf /tmp/frontend-dist.tar.gz dist
```

## 二、上传与切换（scp + systemd）

```bash
scp /tmp/translator-server-linux root@43.108.86.140:/tmp/translator-server-new
scp /tmp/frontend-dist.tar.gz root@43.108.86.140:/tmp/

ssh root@43.108.86.140 "
  TS=$(date +%Y%m%d_%H%M%S)
  # 1. 备份旧版本
  cp /opt/translator/bin/translator-server /opt/translator/bin/translator-server.bak.\$TS
  # 2. 替换二进制
  mv /tmp/translator-server-new /opt/translator/bin/translator-server && chmod +x /opt/translator/bin/translator-server
  # 3. 替换前端
  mv /opt/translator/web /opt/translator/web_old.\$TS 2>/dev/null
  mkdir -p /opt/translator/web
  tar xzf /tmp/frontend-dist.tar.gz -C /opt/translator/web --strip-components=1
  # 4. 重启并探活
  systemctl restart translator
  systemctl is-active translator
"
```

## 三、验证（部署后 10 分钟内完成）

```bash
./deploy/deploy_check.sh https://langcross.lexicorn.cn          # 9 项一键验收

# ★ 本次修复重点——OpenAPI 规范此前损坏，应输出合法 JSON
curl -s https://langcross.lexicorn.cn/openapi/v1.json | jq .paths | head -5

# 健康 + 无 panic
curl -s https://langcross.lexicorn.cn/api/health
grep -c panic /opt/translator/log/translator.log

# ★ PG 序列健康（2026-09-15 审计停更根因：切流导入未同步序列）——期望 前者>=后者；
#   新二进制启动时 syncSequencesPG 自动 setval(max+1000) 自愈，此项仅作复核（两库都查）
psql "$DB" -Atc "SELECT (SELECT last_value FROM audit_logs_id_seq),(SELECT MAX(id) FROM audit_logs);"

# 错误码字段（统一错误码体系）——故意用错 Key 看 error_code 出参
curl -s -X POST https://langcross.lexicorn.cn/openapi/v1/translate \
  -H "Authorization: Bearer tk_bad" -H 'Content-Type: application/json' \
  -d '{"text":"hello","target_langs":["en"]}' | jq .   # 期望 {error_code:"invalid_api_key"}

# 队列/看门狗日志（启动回收 + 心跳无报错）
tail -50 /opt/translator/log/translator.log | grep -E 'worker|watchdog|panic'
```

## 四、扩展更新（浏览器侧手动，非服务器）

扩展文件已改（`extension/`）：manifest / popup.js / content.js。
- Chrome 扩展管理页 → 开发者模式 → 重新加载「能言 · 划词翻译」
- 行为变化：设置里 API Key 不再回显明文（留空保留原值）；保存时弹「授权该服务地址」请求
  → 首次需点允许，跨域翻译才可用。
- 存量用户旧配置（storage.sync）不会自动迁到 local——需在弹窗重新保存一次（Key 可留空保留）。

## 五、回滚

```bash
ssh root@43.108.86.140 "
  cp /opt/translator/bin/translator-server.bak.<TS> /opt/translator/bin/translator-server
  mv /opt/translator/web_old.<TS> /opt/translator/web
  systemctl restart translator
"
```

## 六、本次批次改动文件（供复核）

| 端 | 文件 | 内容 |
|----|------|------|
| 后端 | `internal/errors/codes.go` | OpenAPI snake_case 错误码常量 |
| 后端 | `internal/api/{api_openapi_tasks,admin_openapi}.go` | 错误码字面量→常量（JSON 输出值不变） |
| 后端 | `internal/api/openapi.v1.json` | **修复损坏 JSON** + 统一 Error schema + 错误码枚举 |
| 后端 | `internal/api/watchdog.go` | selfcheck 默认开启 + SELFCHECK_URL 可配 |
| 后端 | `internal/queue/{queue,direct}.go` | Heartbeat 租约续期 + RecoverStale 两步回收 |
| 后端 | `internal/service/ticket.go` | worker 60s 租约心跳 |
| 后端 | `internal/api/verify_embed_test.go` | 新增：嵌入式 OpenAPI 规范合法性回归测试 |
| 后端 | `internal/queue/direct_test.go` | 新增：Heartbeat / RecoverStale 竞态测试 |
| 前端 | `src/api/core.ts` | ApiError + bizErrorCode（错误码透传） |
| 扩展 | `manifest.json` | host_permissions→optional（按源授权） |
| 扩展 | `popup.js` | storage.sync→local、Key 掩码、按源申请权限 |
| 扩展 | `content.js` | 协议白名单 + textContent 消毒 |
| 文档 | `生产配置清单_20260904.md` | SMTP/异地备份/开关核对 |
| ★ 恢复演练（2026-09-15） | `systemctl list-timers translator-restore-drill.timer` | 每周日 03:30 自动恢复演练（失败进告警中心）；手动补跑 `systemctl start translator-restore-drill.service` |

## 七、配置侧待办（见配置清单，独立于发版）

1. ~~配置 `MAIL_ENABLED=1` + SMTP_*~~：**已完成**——上线前已复核生产 mail.conf/主 unit 含完整 SMTP（noreply@lexicorn.cn）且日志真实发信成功
2. 配置 `backup_remote_cmd`（rclone 异地）——⏳ 仍待办
3. `watchdog_selfcheck_restart` 留空即可（本次已改默认开启）——✅ 无需操作
4. 视需要开 `captcha_provider=turnstile` / `email_verify_enabled=1`——⏳ 仍待办

## 八、2026-09-05 运营策略（计费因子流程引擎）批次补充

> 本手册面向 09-04 审计修复批次；09-05 新批次（提交 `df664fb`，已部署生产，备份 `translator-server.bak.20260905_213452`）补充如下，构建/切换/回滚步骤与本手册一致：

- 新能力：运营策略计费因子流程引擎（平台 `system_config.ops_policy` + 租户覆盖 + 活跃时间窗），超管后台「运营策略」面板可视化配置；同时修复 P1（企业成员邀请加入）/P2（平台包订阅回落）/P3（计费落库 Flush）。
- 新路由：`GET /api/admin/ops/policy`、`POST /api/admin/ops/policy/save`、`POST /api/admin/ops/policy/window/save`、`POST /api/admin/billing/package/reset`（未登录应 403）。
- 配置：无需新增环境变量；计费因子建议从后台「运营策略」面板配置（含免费翻译模式 `mode.fast.charge=false` 时走 `LogUsage` 留痕，不扣双桶余额）。
- 验收：`deploy_check.sh` 通过 + 上述新路由 403 + `/status` dialect=postgres + 重启后 `grep -c panic` 为 0；B 阶段 UAT 18 断言全绿（详见《UAT_自动化测试报告_20260905.md》）。
