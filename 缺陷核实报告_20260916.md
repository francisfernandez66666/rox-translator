# 缺陷与缺失核实报告（2026-09-16）

> 背景：全量代码评测（《项目评测报告_20260916》会话结论）产出一批疑似缺陷/缺失。
> 本报告逐条回到真实代码（file:line 级证据）核实其**真实性**，并如实修正不成立或已修复的判定。
> 核实同时以自动 UAT 实测为参照：PG 与 SQLite 双方言矩阵全绿（API 67/67、T 套件 295/295、
> Playwright 32+1 flaky 通过、Vitest 63/63）。

## 一、核实结论总览

| # | 原判定 | 核实结果 | 修正后定级 |
|---|---|---|---|
| 1 | SettleExhausted 误用 `qerr` 吞错误 | ✅ **成立**（潜在资损留痕缺口） | P2 |
| 2 | 低额告警阈值配置未接线 | ✅ **成立**（注释与实现不符） | P3 |
| 3 | backup_remote_cmd 任意 shell = 高危 RCE | ⚠️ **大幅修正**：无 HTTP 写入路径，需先有 DB 直写权；残留问题仅为 exec 无超时 | P3（运维） |
| 4 | 微信/支付宝未真实接入 | ✅ **成立**（但属「已知 fail-closed 整改」而非隐藏缺陷；回调验签侧已真实现） | 功能缺失·商业阻断项 |
| 5 | USDT 真链冒烟未完成、自动对账默认关 | ✅ **成立**（代码注释明确此为有意闸口） | 功能缺失·上线前置项 |
| 6 | JWT_SECRET 随机兜底无 fail-fast | ❌ **不成立**：已有 `REQUIRE_PROD_SECRETS=1` 启动强校验 + systemd 生产配置默认开启 | 已修复（D2/S1 整改） |
| 7 | 前端 token 存 sessionStorage、无 CSP | ✅ **部分成立**：sessionStorage 属实；后端应用层无安全响应头，但 Caddy 层已有 HSTS + X-Frame-Options=DENY，仅缺 CSP | P3 |
| 8 | Playwright 存在 flaky 用例 | ✅ **成立**：`mobile_uat.spec.ts` 超时边缘，靠 retries=1 兜过 | P3（测试稳定性） |
| 9 | README 等文档数据滞后 | ❌ **不成立（README 主基线）**：README:103 的 67/295/33 与今日实测一致 | 撤回 |
| 10 | 无自动续费/多币种/税务开票 | ✅ **成立**，但续费为「手动续订+触达」的设计选择 | 功能缺失（产品决策项） |

## 二、成立缺陷详单（证据与修复建议）

### D1（P2）`SettleExhausted` 永久余额兜底分支检查陈旧变量，真实 DB 错误被静默吞掉
- 证据：`backend-go/internal/store/billing.go:242` 查询台账得到 `qerr`，:245 已判空返回；
  :280-281 永久余额行用新变量 `err` 接 `QueryRow(...).Scan(...)`，但 :292 的失败分支写成
  `else if !errors.Is(qerr, sql.ErrNoRows) && qerr != nil`——此处 `qerr` 恒为 nil（:245 已拦截），
  条件永远为假。
- 影响：当该 SELECT 因瞬时故障（连接中断、PG 语句超时、锁等待失败等非 ErrNoRows 错误）失败时，
  函数不会返回错误，而是把「永久余额未清零」当作正常路径继续 Commit：
  `consumed < owed` 的部分欠费**无痕消失**——调用方 `backend-go/internal/billing/sink.go:328-338`
  只在 `serr != nil` 时打日志，此时拿到的却是 `(consumed, nil)`，连日志都没有。
  违背本函数「杜绝无痕归零/收入泄漏」的设计初衷（:297 注释）。
- 触发概率：低（单行表、错误窗口窄），但属资金路径，静默性差。
- 修复：改为 `} else if !errors.Is(err, sql.ErrNoRows) { return 0, err }`。
- 回归现状：`pgfix_regression_test.go:34` 的 TestSettleExhausted 只覆盖正常/无需结算两态，
  未覆盖此错误分支；建议加注入式错误用例（或至少 code review 级锁死）。

### D2（P3）低额告警阈值配置项存在但未接线
- 证据：`backend-go/internal/service/ticket.go:443-446` `lowBalanceThreshold()` 注释声称读
  `system_config low_balance_alert_tokens`，实现为 `return 100000` 硬编码；全库检索
  （除种子数据外）无任何 `GetConfig("low_balance_alert_tokens")` 调用。
  种子值出处 `backend-go/internal/store/billing.go:2097`。
- 消费方 `TenantLowBalanceAlerts(threshold)`（billing.go:2153）本身参数化正确，
  双桶口径查询无问题——**唯一缺陷是阈值永远改不动**，超管在配置表里改了也不生效。
- 修复：`lowBalanceThreshold` 增加 store 读取（含解析兜底 100000），或删配置项以正名。

### D3（P3，原 P1 降级）`backup_remote_cmd` 异地备份推送 shell 无超时
- 证据：`backend-go/internal/api/watchdog.go:452-461`，`exec.Command("/bin/sh", "-c", full)`
  裸执行，无 `CommandContext` 超时；同文件其余子进程用法（对比 fileproc 的 P1-10 整改）均已带 ctx。
- 风险边界修正（重要）：核查全部 system_config 写入路径——
  `/api/admin/packages/settings/save`（admin_packages.go:290-329，super_admin + **类型化白名单结构体**）、
  `/api/admin/ops/policy/save`（ops_api.go:228，仅写 `ops_policy`）均**不包含** `backup_remote_cmd`，
  全仓库无 HTTP 路由可写该键。即：能配这条命令的前提是已持有数据库直写权限（等同宿主机权限），
  原判定「超管凭证泄露=RCE」**不成立**，予以撤回。
- 残留问题：① 运维直写 DB 启用后，若命令挂死会阻塞该 watchdog 巡检 goroutine 的备份循环（无超时）；
  ② 未来若有人把该键加进 settings 白名单，立即升为 admin-RCE——建议在代码注释中显式禁止入白名单。
- 修复：`exec.CommandContext` + 5~15min 超时；失败已有告警（:457）保留。

### D4（P3）E2E `mobile_uat` 用例贴 30s 超时线，构成 flaky
- 证据：`frontend-react/e2e/mobile_uat.spec.ts:96-98` 逐路由 `waitUntil:'networkidle'` + 固定
  `waitForTimeout(1200)`；本轮 PG 全量跑中该用例首跑超时、retries=1（playwright.config.ts:10）
  复跑通过（run_uat_full.log:505-531，"1 flaky"）。
- 性质：测试稳定性问题，非产品缺陷；路由数增长后必然复发。
- 修复：该用例 `test.setTimeout(60000)` 或压缩固定等待改为 `expect.poll(无溢出)`。

### D5（P3）安全响应头：应用层无 CSP
- 证据：`grep X-Frame|CSP|Strict-Transport backend-go/internal/api` 零命中；
  `deploy/caddy/translator.conf:19-22` 由反代统一注入 HSTS + `X-Frame-Options DENY`（无 CSP）；
  开发态 Go 直连（默认回环，main.go:62）无任何安全头。
- 前端 token 存 sessionStorage（`frontend-react/src/api/core.ts`，已核实）：XSS 发生时
  会话内即可窃取，CSP 是当前缺失的主要纵深防御。
- 修复：translator.conf 增 `Content-Security-Policy`（default-src 'self' 起步，
  白名单放行模型域名/收款码图片）；或在 Go 中间件统一注入。

## 三、商业功能缺失（核实为真、多数已自我标注）

| 缺失项 | 证据 | 说明 |
|---|---|---|
| 微信 Native 下单未实现 | payment.go:157-162：CreateOrder 直接 `return error`（注释「2026-09-16 fail-closed 整改」） | **回调验签已真实实现**（:166-208 AES-256-GCM 解密 + 拒绝明文伪造报文）；缺的只是下单 API 与商户证书链。配置齐全时上层拒单，不会给用户废码——资损方向安全 |
| 支付宝当面付下单未实现 | payment.go:220-225：同上 fail-closed | 回调 RSA2 验签 + app_id/seller_id 串单复核已实现（:229-262，A5 整改） |
| USDT 真链冒烟未完成 | payment/usdt.go:13、api/pay_usdt_watch.go:9、store/usdt.go:43 | 自动对账双闸 `usdt_enabled=1 && usdt_auto_settle=1`，默认 0=仅人工核销；T42 用 mock_chain 全绿。**当前形态无自动入账资损风险**，但对外承诺「自动到账」前必须真链冒烟 |
| 发票税务通道（冲红/开具） | store/billing.go:1798、:1899，api/admin_billing.go:400 | 代码明确标注「属资质遗留项」，台账状态闭环可审计，正式税务对接需资质后补 |
| 订阅自动续费 | watchdog.go:313、store/packages.go:598 | 现状=手动续订 + T-3/exp7/1/3 四档触达去重，**设计选择**而非遗漏；如需自助扣款依赖支付渠道打通（与上行联动） |
| 多币种定价 | store/billing.go:29、:115（currency 固定 'tokens'，订单金额单位「分」） | 出海 USDT 收款实为唯一非 CNY 路径，且按固定汇率配置（usdt_rate_fen_per_usdt） |

## 四、撤回/不成立的原判定（诚实记录）

1. **JWT_SECRET fail-fast 缺失——不成立**：`cmd/server/main.go:70-83`（评审整改 D2）
   `REQUIRE_PROD_SECRETS=1` 时 JWT_SECRET/ADMIN_INIT_PASSWORD/ADMIN_TOKEN 任一缺失 `log.Fatalf`；
   生产配置 `deploy/systemd/prod.conf:52`、`deploy/cloud/seoul-cutover.md:96`、
   `scripts/bootstrap-demo.sh:300` 均已置 1。随机兜底（iam/auth.go:36-49）仅限本地开发且大声告警。
   附带发现：`store/crypto.go`（模型密钥 AES）同源于 JWT_SECRET 派生，轮换需同步重存密文——代码已有
   解密失败告警（admin_models.go:40），属运维注意事项。
2. **「文档普遍滞后」——对 README 主基线不成立**：README.md:103 宣称的发布闸门数字
   （API 67 / T 295 / Playwright 33 / 含 T42-T43）与本次双方言实测完全吻合。
3. **backup_remote_cmd RCE 定性——过重**（见 D3 风险边界修正）。

## 五、核实方法与附录

- 逐条 file:line 实读（非 grep 转述）；关键函数通读：SettleExhausted 全流程（billing.go:199-310）、
  支付 Provider 全集（payment.go:92-262）、USDT 三闸（usdt.go/store/usdt.go/pay_usdt_watch.go）、
  低额告警链路（ticket.go:444-466 → billing.go:2153）、system_config 全部写入路由
  （admin_packages.go:290/ops_api.go:228/白名单性质确认）。
- 实测基线（2026-09-16）：
  - PG：`bash scripts/uat/run_uat.sh` → API 67/67、T 295/295（84s）、Playwright exit=0；
  - SQLite：`DB_DRIVER=sqlite bash scripts/uat/run_uat.sh` → 67/67、295/295、E2E exit=0；
  - 前端：Vitest 63/63；go test 22 包含 -race 全绿。
- 日志：`/var/folders/df/8gcc2g6s1pv099v661pdbt480000gn/T/opencode/uat/`
  （run_uat_full.log / run_uat_sqlite.log / vitest.log）。

## 六、建议处理顺序

1. D1 一行修复（`qerr`→`err`）+ 错误分支回归用例——资金静默错误优先。
2. D2 接线或删配置项（10 分钟）。
3. D3 CommandContext 超时 + 白名单警示注释。
4. D4 e2e 超时参数（消除 flaky 源头）。
5. D5 CSP 头（translator.conf 一行起步）。
6. 商业：微信/支付宝真实下单（证书/密钥到位后，Provider 接口已预留）→ USDT 真链冒烟 →
   打开 usdt_auto_settle。

## 七、修复落实（2026-09-16，同日）

处置决定（负责人确认）：**暂无商户资质，微信/支付宝在线收单维持现状
（fail-closed 占位 + mock/static_qr/usdt/人工入账），不接真实 SDK**；其余全部修复。

| 项 | 状态 | 改动 |
|---|---|---|
| D1 qerr 吞错 | ✅ 已修 | `store/billing.go:292` 改判 `err`，非 ErrNoRows 真实错误上抛（含修复注释）；编译+store 包单测通过 |
| D2 低额阈值 | ✅ 已修 | `service/ticket.go` `lowBalanceThreshold` 改为 TicketService 方法，实读 `low_balance_alert_tokens`（缺失/非法回退 100000） |
| D3 备份推送 | ✅ 已修 | `api/watchdog.go` 改 `exec.CommandContext` + 10min 超时（超时单独告警）；注释显式禁止该键进入 settings HTTP 白名单 |
| D4 e2e flaky | ✅ 已修 | `mobile_uat.spec.ts` 全文件根治：溢出巡检 `test.setTimeout(90s)` + 盲等改 `expect.poll` 自动收敛；登录助手 `waitForTimeout(2000)` 改 `waitForLoadState('networkidle')`；关键 selector 15s→30s；删除冗余固定等待。复跑 **33/33 首跑全过、0 flaky** |
| D5 CSP | ✅ 已加 | `deploy/caddy/translator.conf` header 块增 Content-Security-Policy（白名单含 Turnstile 与 office.js 外链；script-src 暂留 'unsafe-inline'——后端渲染页/spa.go 品牌注入/office taskpane 均为内联脚本，待 nonce 化后收紧，已注记） |
| 微信/支付宝 | ⏸ 按决定搁置 | Provider fail-closed 形态保留；回调验签实现保留，资质到位后仅需补 CreateOrder 真实调用 |
| USDT 真链冒烟 | ⏸ 前置未备 | 双闸默认关不动（自动入账关闭状态下无资损风险） |

回归口径（最终确认）：`go build/vet` 干净；新增单测 store/service 3 用例绿；
`scripts/uat/run_uat.sh`（PG 主矩阵）多轮复跑——API 67/67、T 套件 295→**301/301**
（新增 T44 源码级回归锁×6：settle err/qerr 禁回退、lowbalance 接线、备份超时+白名单禁入、CSP 头）、
Playwright 首轮暴露 a11y 同类贴线超时 → 一并根治后 **33/33 首过、0 flaky**。
提交 `5f721b5` 已推送 GitHub（按指示仅代码/测试/配置，不含文档与流程图）。
两站已于当日 13:13-13:15 完成同版本部署（外网 health 200、CSP/HSTS 生效、
demo_admin 登录成功、零 error 日志；踩坑「cp 覆盖运行中二进制撞 Text file busy」
已记入《部署指南》§五）。
