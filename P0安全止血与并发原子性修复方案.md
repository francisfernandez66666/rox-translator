# 技术方案 · P0 安全止血与并发原子性修复

> 版本 v1.1 ｜ 2026-08-26 ｜ 性质：**上线收费前必须完成的止血改造** ｜ 状态：✅ 已全部落地（见文末实施记录）
> 背景：全量代码审计（2026-08-25~26）发现 3 个公网可利用安全漏洞与一组计费并发缺陷。
> 原则：每项修复独立可验证；全部改动附全量中文注释；改完跑 `go build/vet/test` 全绿。

---

## 一、修复项总览（8 项 P0 + 6 项顺手 P1）

| # | 级别 | 问题 | 现场位置 | 后果 |
|---|------|------|---------|------|
| P0-1 | 🔴 | `/api/download/` 无鉴权、无目录白名单，任意路径读取 | api/stream.go:363-405 | 公网拖走 SQLite 全库 |
| P0-2 | 🔴 | 支付回调 `X-Admin-Token` 可选 + 三渠道验签形同虚设 + 不校验金额 | api/pay.go:244、payment.go:111/154/195 | 伪造回调免费充值 |
| P0-3 | 🔴 | 密码重置码无尝试次数上限、forgot/reset 无 IP 限流 | api/auth.go:37-46,250,311 | 6 位码 10 分钟内可穷举，账号接管 |
| P0-4 | 🟠 | `DeductWithGrants` DEFERRED 事务 + 快照外拆分 + UPDATE 无守卫 → 台账双花/负库存 | store/quota_grants.go:58-112 | 白皮书「绝不扣负」承诺失效 |
| P0-5 | 🟠 | `MarkOrderPaid` SELECT→UPDATE 非原子、UPDATE 不带状态条件 → 权益双发；payments 流水金额恒 0 | store/billing.go:520-538 | 并发重复发放充值 |
| P0-6 | 🟠 | `CloseStalePendingOrders` 不区分渠道，误杀 static_qr 已确认订单 | store/billing.go:783-797 | 用户已付款订单被取消（资损投诉） |
| P0-7 | 🟠 | `RefundOrder` / `MarkOrderManualConfirm` 同类非原子问题 | store/billing.go:578-588,643-654 | 双退款扣负余额 |
| P0-8 | 🟠 | `EnsureBalance` check-then-insert 竞态 + balance_accounts 无 UNIQUE 约束 → 重复账户行 | store/billing.go:90-102 | 展示与实扣不一致 |

顺手修复的确定性缺陷（P1 批次，同属本方案验收）：

| # | 问题 | 位置 |
|---|------|------|
| P1-a | 低额告警去重查询列名写错：`type='low_balance'` 应为 `kind` | store/billing.go:830 |
| P1-b | `ListPlatformOrgs` SELECT 7 列 Scan 8 目标 → 平台组织树恒空 | iam/store.go:291,300 |
| P1-c | jobs 类型不匹配：入队 `ticket_run`，续跑/卡死释放写 `type='ticket'` | service/ticket.go:55,515、store/tickets.go:290 |
| P1-d | 上传文件名未 `filepath.Base` 清洗 → 路径穿越写 | api/stream.go:168,307 |
| P1-e | KB 条目批量导入缺包归属三重校验（横向越权污染行业包） | api/kb.go:42-92 |
| P1-f | Webhook URL 零校验 → SSRF 打内网/云元数据 | store/webhooks.go:41-74,182-218 |
| P1-g | docx/pptx 译文写回未做 XML 转义（含 `& < >` 即损坏文档）；提取侧实体不匹配 | fileproc/docx.go、pptx.go |

> 密钥泄漏（backend/main.py 硬编码 key 入 git 历史）在《旧Python后端下线与构建链收敛方案》中处理；
> 插件断链（/openapi/v1/translate 不存在）在《LLM统一网关与BYOK移除方案》中以新增同步端点处理。

---

## 二、逐项设计

### P0-1 下载接口鉴权与目录白名单（stream.go）

现状：`handleDownload` 接受 `?path=` 任意路径直接 `os.Open`。

改造：
1. 入口先 `s.authUser(r)` 校验 JWT（401 拒绝）。
2. 路径收敛：仅允许两种来源——
   - 上传目录 `s.Cfg.UploadDir`（临时上传件预览）；
   - 运行产物目录 `<UploadDir>/_output`（工单产物）。
   实现 `resolveSafePath(baseDirs []string, p string) (string, bool)`：`filepath.Clean` 后逐一校验 `strings.HasPrefix(clean, base+separator)`，命中任一才放行；否则 404（不泄露存在性）。
3. 兼容旧行为：URL 路径形式 `/api/download/<file>` 与 `?path=` 都走同一白名单函数。

### P0-2 支付回调强制凭证 + 渠道/金额一致性（pay.go / payment.go）

现状：`X-Admin-Token` 只在请求带头时才比对；MockProvider.VerifyNotify 仅验 `amount>0`；不核对渠道与订单金额。

改造（分层防御，商户号到位后真实验签落地前先做到「不可匿名伪造」）：
1. **凭证必填**：`handlePayNotify` 开头改为——头缺失或与 `s.Cfg.AdminToken` 不符一律 403。回调方（微信/支付宝服务器）无法预置该头的问题，由部署侧反代为 `/api/pay/notify/*` 注入该头解决（部署指南补充 Caddy 片段），或后续接入渠道官方验签后移除该要求（代码留注释说明）。
2. **渠道一致性**：URL path 中的 `channel` 必须与订单落库 channel 匹配（mock 单只接受 mock 回调；manual 单拒绝一切回调，只能走人工确认）。
3. **金额一致性**：查单后按 `amount_tokens × 10` 计算应收分，与 `nt.Amount` 比对（`nt.Amount<=0` 或不一致即拒绝）。
4. **mock 生产封禁**：`pay_mode != "mock"` 时 mock 渠道回调直接 403（与 handlePaySimulate 同口径）。
5. payment.go 三适配器 VerifyNotify 内补注释明确「当前为骨架验签，真实接入点见 ★ 标记」，并把 `Verified = amount > 0` 收紧为 `Verified = amount > 0 && orderNo != ""`（orderNo 已有非空检查，保持不变即可，重点在 API 层三道闸）。

### P0-3 重置码防爆破 + 接口限流（auth.go）

现状：`resetCode{Code, ExpiresAt}` 无 Attempts；forgot/reset 无 IP 维度限流。

改造：
1. 结构体增加 `Attempts int`；`handleResetPassword` 验证失败时 `Attempts++`，`Attempts >= 5` 即作废该码（对齐 email_verify 的 maxTries=5 口径，常量 `resetCodeMaxTries = 5`）。注意加锁内完成读改写，消除现有两次 Lock 分离的窗口。
2. forgot 与 reset 两接口复用注册护栏：键 `"pwd-forgot:"+ip`、`"pwd-reset:"+ip`，日上限 10 次、最小间隔 30s（allow→业务成功→record 模式，与 email-code 一致）。
3. 发送失败分支统一返回成功文案（彻底防账号存在性探测）：日志保留细节，响应不再区分。

### P0-4 DeductWithGrants 原子化（quota_grants.go + kb/db.go DSN）

现状：`s.db.Begin()` 为 DEFERRED；SELECT 拆分发生在取写锁之前；UPDATE 无 `left>=?` 守卫。

改造（双保险）：
1. **DSN 加 `_txlock=immediate`**（kb/db.go 构建 DSN 处追加参数）：全库所有事务升级为 BEGIN IMMEDIATE，写事务开启即持写锁，消除全部 check-then-write 竞态面（同时受益 P0-5/7/8）。SQLite 单机写场景标准做法，性能影响可忽略。
2. **守卫兜底**：台账核销 UPDATE 改 `SET left=left-? WHERE id=? AND left>=?`，`RowsAffected==0` 视为并发冲突回滚返回 `ErrInsufficientBalance`（即使未来有人改回 DEFERRED 也不会扣负）。
3. 过期行语义不变（过滤式跳过）；永久桶条件更新已有守卫不动。

### P0-5 MarkOrderPaid 原子确认 + 流水记实金额（billing.go）

改造：
1. 确认置位改单条条件更新并先行执行：
   ```sql
   UPDATE orders SET status='paid', paid_at=?, manual_confirm=0
   WHERE id=? AND tenant_id=? AND status='pending'
   ```
   `RowsAffected()==0` 直接返回「订单不存在或已处理」（天然幂等：重复确认零发放）。
2. 条件更新成功后再读取订单字段走权益分流（pkgID 分流逻辑保持不变）。
3. payments 流水 `amount_money` 写真实应收分（tokens×10），修正对账原料。
4. `MarkOrderPaidByOrderNo` 保持「查单→委托 MarkOrderPaid」，幂等由第 1 步条件更新保证（原 status=='paid' 早退可删）。

### P0-6 关单豁免人工确认单（billing.go）

`CloseStalePendingOrders` 的 WHERE 增加：
```sql
AND NOT (channel='manual' AND manual_confirm=1)
```
已点「我已付费」的静态码单不受 15 分钟限制，等待超管核实；未确认的 manual 单仍正常关单。offline 场景（若有）同理按 channel 豁免策略扩展。

### P0-7 RefundOrder / MarkOrderManualConfirm 条件更新（billing.go）

1. RefundOrder：置 refunded 改 `UPDATE ... WHERE id=? AND tenant_id=? AND status='paid'` + RowsAffected 判定；随后 `Deduct` 改用带守卫的余额扣减（复用 balance>=? 条件更新，不足则返回错误并回滚订单状态——实现上先扣款后置状态更稳：顺序调整为条件更新扣余额 → 成功后置 refunded；两步包在一个 IMMEDIATE 事务里）。
2. MarkOrderManualConfirm：UPDATE 加 `AND manual_confirm=0 AND status='pending'` + RowsAffected 判定，替代现查询+更新的两步式。

### P0-8 EnsureBalance 幂等化 + UNIQUE 索引（billing.go + store 迁移链）

1. 新增 `BalanceAccountMigrate()`（挂到 Store.New 迁移链，QuotaGrantMigrate 之后）：
   - 先去重：`DELETE FROM balance_accounts WHERE id NOT IN (SELECT MIN(id) FROM balance_accounts GROUP BY tenant_id)`；
   - 再建唯一索引 `CREATE UNIQUE INDEX IF NOT EXISTS idx_balance_tid ON balance_accounts(tenant_id)`。
2. EnsureBalance 改 `INSERT INTO ... ON CONFLICT(tenant_id) DO NOTHING`（或 INSERT OR IGNORE），一次语句完成，消除 check-then-insert 竞态。

### P1 批次设计要点

- **P1-a**：`alerts` 表去重查询列 `type` → `kind`；顺带删除恒假的 `_ = enforced` 占位（灰度期也提醒的语义以注释固化）。
- **P1-b**：ListPlatformOrgs SELECT 补第 8 列 `COALESCE(o.token_limit,0)`，与 orgCols 其余调用方对齐。
- **P1-c**：store/tickets.go:290 与 service/ticket.go:515 的 `type='ticket'` 统一改为 `'ticket_run'`（与 EnqueueType 常量对齐；service/ticket.go 定义常量 `queueTicketRunType = "ticket_run"` 三处引用）。
- **P1-d**：uniqueName 入口 `filepath.Base(name)` 清洗目录成分；两处 savePath 组装同步收口。
- **P1-e**：handleKBEntriesImport 在解码后补齐与 handleImportKB 相同的三重校验：`GetKBPackage(pkgID, tid)` 存在性 → `canManagePackType(u, pkg.PackType)` → dept_admin 时 `IsOrgInSubtree` 部门子树归属。
- **P1-f**：webhooks.go 新增 `validateWebhookURL(raw string) error`：
  - scheme 必须 http/https；
  - `net.LookupHost` 解析后逐 IP 拒绝：环回、私网（RFC1918）、链路本地（含 169.254 元数据）、未指定、组播；
  - UpsertWebhook 保存时强校验；postWebhooks 每次投递前再校验一次（DNS 可能漂移）；
  - 解析失败（域名不存在）保存时即报错。
- **P1-g**：fileproc 新增 `xmltext.go`：
  - `escapeXML(s string)`：转义 `& < > " '`（写入 docx/pptx 前必须过此函数）；
  - `unescapeXMLText(s string)`：反转义五个预定义实体（提取匹配键时使用，与 encoding/xml 提取侧行为对齐）；
  - docx.go：`paragraphRunText` 返回文本经 unescape（原文键与 ExtractTexts 的 xml.Unmarshal 输出一致）；`translateDocxParagraph` 写入译文前 escape；
  - pptx.go：`rawPptxText` unescape；`replacePptxParagraph` 写入译文前 escape；
  - 效果双重：① 译文含特殊字符不再产出损坏 XML；② 含实体段落（如 "R&D"）原文键两侧一致，翻译质量与命中率同时修复。

---

## 三、不做的事（明确边界）

- 微信/支付宝官方级验签（AES-GCM/RSA2）：商户号到位后在既有 ★ 接入点落地，本方案先用「凭证必填+渠道一致+金额一致」三道闸保证不可匿名伪造。
- JWT 吊销/jti、密码弱哈希强制迁移、GDPR erase 补表：列入后续《企业级合规加固方案》（本方案不含）。
- SSE 断连终止计费、前端 core.ts header 合并重构等体验类：列入 P2 批次文档。

## 四、验收清单

1. `curl -s -o /dev/null -w "%{http_code}" "https://<host>/api/download/?path=/etc/passwd"` → 401/404，且携带合法 token 也取不到白名单外文件。
2. 无 X-Admin-Token POST `/api/pay/notify/mock` → 403；带头但金额不符/渠道不符 → 400；manual 单回调 → 400。
3. 连续错 5 次重置码后，正确码也被拒；同 IP 第 11 次 forgot → 429。
4. 并发 20 goroutine 同时 DeductWithGrants(总量 N)：最终 `SUM(left)+永久扣减 == N`，无负值（新增 `quota_grants_concurrency_test.go` 用例）。
5. 双请求并发 MarkOrderPaid 同一订单：仅一笔 payments 流水、权益只发一次。
6. manual 单点「我已付费」16 分钟后状态仍 pending；普通 mock 单 16 分钟后被 cancelled。
7. `go vet ./... && go test ./...` 全绿（含新增并发用例）。

---

## 五、实施记录（2026-08-26）

| 项 | 状态 | 落点 |
|---|---|---|
| P0-1 下载鉴权+白名单 | ✅ | stream.go：authUser + resolveSafePath（UploadDir/_output 白名单，Clean+分隔符前缀匹配） |
| P0-2 回调三道闸 | ✅ | pay.go：AdminToken 必填、mock 按 pay_mode 封禁、渠道一致、金额= tokens×10 核对；payment.go 骨架验签注释加固 |
| P0-3 重置防爆破 | ✅ | auth.go：resetCode.Attempts≤5（单锁内读改写）、forgot/reset 复用 regGuard 独立计数键 |
| P0-4 扣减原子化 | ✅ | kb/db.go DSN `_txlock=immediate`；quota_grants.go 守卫式核销 + RowsAffected 判定；新增 quota_grants_concurrency_test.go（20 并发 15/5 断言 + 溢出回滚断言，10 轮全绿） |
| P0-5 订单原子确认 | ✅ | billing.go MarkOrderPaid 条件更新先行 + payments 记真实分值 |
| P0-6 关单豁免 | ✅ | CloseStalePendingOrders 排除 `channel='manual' AND manual_confirm=1` |
| P0-7 退款/人工确认原子化 | ✅ | RefundOrder 改 IMMEDIATE 事务内「先守卫扣款→条件置 refunded」；MarkOrderManualConfirm 条件更新 |
| P0-8 余额账户幂等 | ✅ | BalanceAccountMigrate（去重+唯一索引，挂 Store.New 链）；EnsureBalance 改 INSERT OR IGNORE |
| P1-a~g | ✅ | kind 列名 / token_limit 补列 / ticket_run 统一（tickets.go:290+service:515）/ uniqueName Base 清洗 / KB 导入三重校验 / validateWebhookURL 保存+投递双闸 / xmltext.go（EscapeXML+UnescapeXMLText 接入 docx/pptx） |

**口径微调说明**：P0-3 第 3 条「发送失败统一成功文案」未采纳——MAIL_ENABLED 关闭时走 noop 模式本就返回成功，
失败仅发生在真实 SMTP 配置故障场景，此时如实报错更利于用户排障；枚举风险经评估为边际（需先命中
「账号存在+已绑邮+SMTP 故障」三条件叠加）。如后续需要彻底防探测，改回统一文案即可（单处改动）。

**遗留提醒**：
- Caddy 需为 `/api/pay/notify/*` 注入 `X-Admin-Token` 头（部署指南 §九 已更新配置片段），否则真实渠道回调会被 403；
- 微信/支付宝官方级验签仍待商户号到位后在 payment.go ★ 接入点落地。
