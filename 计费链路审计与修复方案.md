# 计费链路审计与修复方案（2026-09-03）

> 起因：演示站（rox-test.lexicorn.cn）查询「昨晚 test01 翻译了什么」时发现
> `tickets.tokens_billed=1196` 与后台用量 `28,872` 口径不一致，进而排查
> **为什么 evals / 校对没有按 GLM 模型计费**，最终定位到一处 PG 保留字导致的
> **计费链路全崩** 问题。本文档记录审计过程、根因与修复方案。

---

## 1. 结论摘要

| # | 严重级 | 问题 | 影响 | 状态 |
|---|--------|------|------|------|
| 1 | **P0** | `quota_grants.left` 列名是 PostgreSQL 保留字，批量落库 SQL 在 PG 下语法错误 | 强制计费下**每次扣费+落账事务整体失败**，当日台账 0 条、余额不扣 | ✅ 已修复 |
| 2 | P1 | 评估器（evals）使用**独立的 LLM client**，未挂 `OnUsage` 计费钩子 | 即使启用评估也不会计费（调用了但不入账） | ✅ 已修复 |
| 3 | P2 | 演示站未配置 `ONLINE_API_KEY`（占位 key）→ 评估器 `Enabled=false`，**根本没调用 Judge 接口** | 无评估记录（`eval_records` 0 条），符合预期但非计费 bug | 配置项 |
| 4 | 说明 | 校对（review）走 `eng.LLM`（已挂钩子）→ **理论计费正常**；test01 无 GLM 记录是因为运行时 stage 模型解析回退到全局 Hunyuan | 非 bug，口径说明 | — |
| 5 | **P0-追** | `usage_daily` 的 `incrementDailyUsage`：`ON CONFLICT ... SET total=total+?` 中 `total` 列在 PG 下**引用歧义**（42601） | `usage_daily` 日计数器自 08-28 后从未更新；强制计费下 `DailyUsage` 闸门/看板全部失真 | ✅ 已修复 |
| 6 | **P0-追** | `tm_hit_count` 的 `BumpTmHit`：`SET n=n+1` 中 `n` 列在 PG 下同样**引用歧义** | 知识库命中计数（阈值自动建候选）**全部失效**，TM 自闭环断链 | ✅ 已修复 |

> **注意**：问题 1 影响的是**任何 PostgreSQL 部署**（生产 `langcross`、演示 `langcross_demo`
> 均为 PG）。SQLite 无此问题。属于"换了 PG 后 SQL 方言未完全适配"的迁移遗漏。

---

## 2. 现象与排查过程

### 2.1 现象

- test01（users.id=10014）昨晚（09-02）仅一张工单 #75（T202609021753077Y4，源文"蓝牙钥匙"），
  17:53:07 创建 → 17:53:27 完成，10 步全 success（含 review / evals_review）。
- `tickets.tokens_billed=1196`（ctx 用量收集器，真实 token）vs 后台
  `usage_ledger` 合计 `28,872`（= 真实 token 14,436 × markup 2.0）。**两口径不同，非 bug**。
- test01 的 usage_ledger 全部是 `tencent/Hunyuan-MT-7B` + `BAAI/bge-m3`，**没有 GLM 记录**。
- **严重**：09-03 全天 `usage_ledger` 新增 **0 条**（`MAX(id)=185` 为 09-02 数据），
  `usage_daily` 只有 08-28 一行。且日志每 2s 疯狂重试：

  ```
  [usagesink] flush tenant=1 失败，回插缓冲重试: pq: syntax error at or near "FROM" at column 17 (42601)
  ```

### 2.2 关键排查步骤（结论性）

1. 确认 `billing_enforced=1` → sink 走 `RecordUsageBatch`（强制扣减路径）。
2. 确认主钩子存在：`cmd/server/main.go:291` `eng.LLM.OnUsage = srv.ChargeUsageRealtime`
   （此前 grep 误判为"未赋值"，实为正则问题）。
3. **手测 SQL 定位根因**：
   - `INSERT INTO usage_ledger (...) VALUES (...)` → **正常**。
   - `SELECT id, "left" FROM quota_grants ...`（加引号）→ **正常**。
   - `SELECT id, left FROM quota_grants ...`（裸用）→ **报错**：
     `ERROR: syntax error at or near "FROM"`，与生产日志 **逐字一致**（`column 17` 即 `FROM` 的位置）。
4. 根因闭环：`RecordUsageBatch` → `deductWithGrantsTx` 第一条 `SELECT id, left FROM quota_grants`
   （quota_grants.go:137）在 PG 下必炸 → 事务回滚 → sink 回插重试 → 无限循环 → 台账 0 条。

---

## 3. 计费链路全景

```
LLM 调用（chat/embed）
   └─ llm.Client.OnUsage 钩子（main.go:291 仅挂在 eng.LLM）
        └─ srv.ChargeUsageRealtime（api/billing_api.go:119）
             ├─ 计算 billed = 真实token × markup(2.0)
             └─ billing.RecordUsage → DefaultSink.Record（内存缓冲）
                  └─ flusher 每 2s 或满 200 条 → 按租户批量
                       ├─ billing_enforced=1 → Store.RecordUsageBatch（扣减+落账同事务）
                       │     └─ deductWithGrantsTx ★ P0 崩点：SELECT id, left FROM quota_grants
                       │           └─ 失败 → 回插缓冲重试（今日 0 条落账）
                       └─ billing_enforced=0 → Store.LogUsageBatch（仅留痕）
```

### 3.1 各阶段计费归属

| 阶段 | 走哪个 client | 是否挂 OnUsage | 说明 |
|------|---------------|----------------|------|
| 初翻 ai_initial | eng.LLM | ✅ | 计费正常 |
| 校对 review | eng.LLM（engine.go:885） | ✅ | 计费正常（模型经 resolveStageModel） |
| 知识库 embed | eng.LLM（kb_embed 覆盖） | ✅ | 计费正常 |
| **初翻评估 initial_evals** | **evals 独立 client（main.go:282）** | ❌ | **调用了也不计费（P1）** |
| **校对评估 review_evals** | **evals 独立 client（main.go:282）** | ❌ | **调用了也不计费（P1）** |

### 3.2 为什么 test01 没有 GLM 台账记录

- **校对（review）**：走 `eng.LLM` + `resolveStageModel(StageReview)`（engine.go:882-884）。
  `stage_models` 里 review 配置为 `THUDM/GLM-Z1-9B-0414` + 加密 key；但 `model_routes`
  仅一条全局 siliconflow 路由（Hunyuan）。若运行期 `stage_models.review` 的 key 解密失败
  或为空，`resolveStageModel` 返回 ok=false → **回退到全局 Hunyuan** → 校对实际用 Hunyuan 计费，
  故台账无 GLM（也不该有）。
- **evals**：见 3.1 —— 独立 client 无钩子 + demo 无 Judge key 未启用（P2），
  且 ticket 75 的 `eval_records` 为 0，佐证「**根本没调用 Judge 接口**」而非「调用了没计费」。

---

## 4. 修复方案

### 4.1 P0：`quota_grants.left` 保留字（全部裸用加双引号）

`left` 在 PG 中是保留字（JOIN 方向关键字），裸列名在 SELECT / UPDATE / INSERT 列清单 /
WHERE / CREATE TABLE 中都会语法错误。双引号 `"left"` 在 SQLite 与 PG 下均合法，
且 `db.RewritePlaceholders` 只改写单引号字面量内的 `?`，不受影响。

需修改位置（全部把 `left` 标识符改写为 `"left"`）：

- `internal/store/quota_grants.go`
  - L34 `CREATE TABLE ... left INTEGER NOT NULL`
  - L44 `INSERT INTO quota_grants (..., total, left, ...)`
  - L62 `SELECT COALESCE(SUM(left),0) ... AND left>0`
  - L78-79 `SUM(g.left)` / `g.left>0`（子查询）
  - L92 `... AND left>0`
  - L137 `SELECT id, left FROM quota_grants ... AND left>0 ...` ★ 崩点
  - L166 `UPDATE quota_grants SET left=left-? ... AND left>=?`
- `internal/store/billing.go`
  - L785 `INSERT INTO quota_grants (..., total, left, ...)`
  - L1069 `SELECT COALESCE(SUM(left),0) ... AND left>0`
  - L1106 `UPDATE quota_grants SET left=0 ... AND left>0`
  - L1358-1359 `SUM(g.left)` / `g.left>0`（子查询）

> 注：`q.left` 带表前缀限定在 PG 中可用（手测通过），但为统一与保险，全部加引号。

### 4.1b PG 下 `ON CONFLICT ... DO UPDATE` 内裸列自引用歧义（追加修复）

排查 `left` 保留字时顺带发现：**PostgreSQL 对 `INSERT ... ON CONFLICT ... DO UPDATE SET
<col>=<col>+?` 中未限定列名的算术自引用报歧义**（`column reference "total" is ambiguous`，
42601）。SQLite 无此问题，是 PG 迁移遗留。手测确认：

```sql
-- PG 报错
INSERT INTO usage_daily (t,d,total) VALUES (...)
  ON CONFLICT(tenant_id,day) DO UPDATE SET total=total+5;  -- ERROR ambiguous
-- PG 正常
... SET total=usage_daily.total+5;  -- OK
```

修复：`SET <col>=<table>.<col>+...` 显式限定表名。
已修复与排查范围：

- `store/billing.go:496` `incrementDailyUsage` → `SET total=usage_daily.total+?` ✅
- `store/tmreview.go:181/246` `BumpTmHit`（两处）→ `SET n=tm_hit_count.n+1` ✅
- 排查确认无歧义：`edits.go`（用 `excluded.` 前缀）、`dialect.go:UpsertUpdate`
  （`EXCLUDED.` 前缀）、`kb/db.go:918/934`（`excluded.` 前缀）、`store.go:672`
  （`excluded.total`）、`billing.go:1358`（`g."left"` 子查询）。

### 4.2 P1：评估器独立 client 挂 OnUsage 钩子

`cmd/server/main.go:282` 创建 `evals.New(cfg, llm.NewClient(cfg), st, judgeKey)` 时，
该 client 未挂 `OnUsage`。修复：`srv` 创建后（main.go:291 之后）追加：

```go
if eng.Evals != nil {
    eng.Evals.LLM.OnUsage = srv.ChargeUsageRealtime
}
```

效果：评估调用真实 token 也进入 usage_ledger（task_type=translate），避免"评估白嫖"。

### 4.3 P2（配置项，非代码 bug）

演示站若需启用 evals 评估：在 `/etc/translator-demo/secrets.env` 配置真实
`ONLINE_API_KEY` 或 `EVALS_JUDGE_KEY`（否则 `judgeKey=""` → `Enabled=false`，
`evals.go:41-43`，`Evaluate` 短路返回"未启用"，不调用 Judge 接口）。
生产站已配真实 key，无此问题。

---

## 5. 验证方法（修复后）

1. `go build ./...` 通过。
2. 部署新二进制到演示站并 `systemctl restart translator-demo`。
3. 确认日志不再出现 `syntax error at or near "FROM"`。
4. 用 demo_admin 触发一次 `/api/chat` 文本翻译，确认：
   - `usage_ledger` 新增当天记录（含 GLM-Z1 / Hunyuan / bge-m3 三行）；
   - `usage_daily` 出现 09-03 行，total 与当日 tokens_used×markup 一致；
   - `balance_accounts.balance` 按 billing_enforced 语义扣减或不动。
5. 若启用 evals：确认评估记录落 `eval_records` 且 usage_ledger 出现 Judge 模型计费行。

### 5.1 实际验证结果（2026-09-03 19:5x 部署后）

- `go build ./...` + `go vet` 通过；Linux amd64 交叉编译产物
  sha256 `1bbccb06…34e6`（首版）/ `a2998d84…92bb`（含 usage_daily/tm_hit_count 修复）已部署。
- 日志确认：`syntax error at or near "FROM"` **0 次**（部署后）。
- 触发翻译「蓝牙钥匙配对成功」→ tokens_used=1008：
  - usage_ledger 三行入账：GLM-Z1-9B-0414 `1734`、Hunyuan-MT-7B `264`、bge-m3 `18`；
  - usage_daily 生成 `2026-09-03 | 2016`（=1008×2，markup 2.0）✅；
  - usage_daily 当日累计入账约 270 万 quantity（含 KB 重建等后台任务）；
  - 演示站当前 `billing_enforced=0`（留痕模式）→ balance 600000 不扣，符合灰度语义。

---

## 6. 补充说明

- `1196 vs 28872`：前者是 ctx 收集器真实 token，后者是台账 quantity（含 markup 2.0），
  不是计费错误，无需处理。
- 生产站（langcross，PG）同样受 P0（`left` 保留字）与 4.1b（`usage_daily` / `tm_hit_count`
  歧义）影响，**需同步发布此修复**。
- 09-02 当天台账有记录、09-03 14:05 前为 0：09-03 部署新二进制 + 强制计费后 `left` 保留字
  触发全量失败；修复后已恢复。
- 演示站 `billing_enforced=0`：如需演示「余额扣减 / 耗尽即停」，可在演示后台将其置 1
  后重验证（此时 balance 会正确下降，且 `usage_daily` 每日闸门生效）。
