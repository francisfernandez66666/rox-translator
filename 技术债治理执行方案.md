# 技术债治理执行方案 · 单实例横向扩容改造 + systemd 沙箱化

> 版本：v1.1 ｜ 更新：2026-09-09（技术债① 已落地）
> 范围：两条技术债——
> **① 进程内存态组件改造为可横向扩展（P0/P1 已完成，见「落地记录」）**
> **② 生产 systemd 迁移到沙箱化部署（deploy/systemd/prod.conf，待服务器操作）**

---

# 第一部分 · 技术债① 内存态组件横向扩容改造

## 一、现状盘点（改造对象）

依据 `ARCHITECTURE_REVIEW` / `PROGRESS.md` §八-D「单实例架构红线」，需改造的**进程内存态组件**共 8 处：

| # | 组件 | 位置 | 数据形态 | 现状 |
|---|------|------|----------|------|
| 1 | `resetCodes`（密码重置验证码） | `api/auth.go:38` | `map[int64]resetCode` 进程内存 | 验证码存内存，重启/多实例失效 |
| 2 | `emailCodes`（邮箱验证码） | `api/email_verify.go:44` | `map[string]*emailCode` 进程内存 | 同上 |
| 3 | `loginLimiter`（登录暴力破解限流） | `api/ratelimit.go:39` | `map[string]*loginAttempt` 内存 + **Store 已持久化** | **已有 `rate_limits` 表后端**，内存仅兜底 |
| 4 | `registerGuard`（注册防薅护栏） | `api/register_guard.go:31` | `map[string]*regAttempt` 内存 + **Store 已持久化** | **已有 `rate_limits` 表后端**，内存仅兜底 |
| 5 | `quotaByTenant`（QPS/并发配额窗口） | `billing/quota.go:33` | `map[int64]*Quota` 进程内存 | **纯内存**（QPS 窗口/并发计数），上限配置已持久化 |
| 6 | `startPackScraper`（采集调度 goroutine） | `api/packscraper.go:22` / `watchdog.go:172` | 后台 goroutine | 单实例驻留，多实例重复采集/重复通知 |
| 7 | `metrics` 计数器 | `api/metrics.go:28` | `map[string]*int64` 原子计数 | 进程内聚合（Prometheus 抓取，非强一致必需） |
| 8 | `cjkCacheByTenant` / `cultureCache` / `embedcache` | `engine/` | 进程缓存 | 只读派生缓存，可重建，非一致性必需 |

## 二、改造策略：Redis 统一后端 + 内存兜底（分层接口）

**核心原则**：Redis 已就绪（`internal/infra/redis`，支持 SETNX/INCR/DECR/EXPIRE/DEL）。所有内存态组件抽象为**统一接口**，Redis 启用时走 Redis 聚合，未启用时降级内存（保持单实例兼容，生产双写/读 Redis）。

```
┌─────────────────────────────────────────────────┐
│  memorystore 抽象层（新增 internal/infra/store）  │
│  interface:  Get/Set/SetNX/Incr/Decr/Expire/Del  │
│     ├── redisStore（Redis 启用 → 跨实例聚合）      │
│     └── memStore（Redis 未启用 → 进程内兜底）       │
└─────────────────────────────────────────────────┘
```

## 三、逐组件改造方案

### 3.1 resetCodes / emailCodes（验证码 → Redis 键值）

**改造**：验证码从 `map` 改为 Redis key（`memstore.Set(key, val, TTL)`），Redis 未启用时回退内存 map。

| 项 | 方案 |
|----|------|
| resetCodes key | `vcode:reset:<userID>`（值=JSON `{code, attempts}`，TTL 10min） |
| emailCodes key | `vcode:email:<lowerEmail>`（值=JSON `{code, attempts, sentAt}`，TTL 10min） |
| 原子性 | 校验+消费用 `SetNX` 式或 `Get`+`Set` 读改写；attempts 递增用 `Incr` |
| 多实例 | 任意实例生成的码其他实例可见（同租户多实例负载均衡成立） |
| 兜底 | `redis.Get()` 为 nil 时用内存 map（现有实现） |

### 3.2 loginLimiter / registerGuard（已有持久化，补 Redis 计数层）

**现状**：已有 `rate_limits` 表后端（`st.RateRecord/RateLoad/RateSetLock/RateReset`），多实例共享。**这已满足跨实例一致性**，无需 Redis。
**改造**：仅需确认 `rate_limits` 表在 PG/SQLite 双方言下读写原子（UPDATE 原子），即可标为「已完成」。补齐 Redis 计数以减轻 DB 写压力（可选优化）。

### 3.3 quotaByTenant（QPS/并发 → Redis 滑动窗口 + 信号量）

**现状**：纯内存，多实例各自计数（QPS/并发上限会被 N 实例放大 N 倍）。
**改造**：
- **并发计数**：复用 `internal/infra/concurrency/semaphore.go`（已有 Redis list 令牌桶实现）。
- **QPS 滑动窗口**：用 Redis `INCR` + `EXPIRE` 实现每秒窗口（key=`quota:qps:<tid>:<epochSec>`），近似滑动窗口。
- **上限配置**：已在 `system_config(tenant_quota_<tid>)` 持久化，启动回放。

### 3.4 startPackScraper（采集调度 → 分布式锁）

**改造**：`runPackScrapeOnce` 执行前用 `memstore.SetNX("scrape:lock", instanceID, TTL)` 获取**分布式锁**，多实例仅一台采集；其余跳过。
- 锁 TTL > 单轮采集最长时间（如 30min），防止采集中途锁过期双跑。
- 需引会话续期（heartbeat）或足够大的 TTL 兜底。
- 采集进度本身已在 `system_config(kb_scrape_checkpoint_...)` 断点续传（多实例共享），故加锁即安全。

### 3.5 metrics / 缓存（保留进程内，无需改造）

- `metrics`：Prometheus 聚合指标，多实例可各自抓取后由 Prometheus 侧聚合，**无需跨实例一致**。
- 引擎缓存（cjk/culture/embed）：只读派生缓存，可重建，**非一致性必需**，保留进程内。

## 四、工作量与优先级

| 优先级 | 组件 | 工作量 | 依赖 |
|--------|------|--------|------|
| P0 | startPackScraper 分布式锁 | 小 | 必须（否则扩容后重复采集） |
| P0 | quotaByTenant QPS/并发 | 中 | 必须（否则扩容后限流失效） |
| P1 | resetCodes / emailCodes 验证码 | 小 | 建议（否则验证码体验降级） |
| P1 | loginLimiter / registerGuard 确认 | 小 | 已基本完成，补 Redis 可选 |
| P2 | metrics / 缓存 | 无 | 不改造 |

## 五、验收

1. `go test ./...` 全绿（含新增 `memstore` 单测）。
2. 本地单实例回归：登录限流、注册护栏、验证码、QPS 限流、采集调度行为不变。
3. 双实例冒烟（本地起 2 个进程连同一 Redis）：
   - 验证码：A 实例生成，B 实例可校验。
   - QPS：并发请求跨实例合计限流生效。
   - 采集：仅 1 台采集（`scrape:lock` 生效），无重复通知。

---

## 落地记录（2026-09-09 · 技术债① 已完成）

### 已落地改动

| # | 组件 | 落地位置 | 状态 |
|---|------|----------|------|
| 1 | resetCodes / emailCodes | `api/vcode.go`（新增 `vcodeSet/vcodeGet/vcodeDel`）+ `api/auth.go`、`api/email_verify.go` 读写切到共享层 | ✅ 已完成 |
| 3 | loginLimiter / registerGuard | 已有 `rate_limits` 表持久化（跨实例一致），确认即算完成 | ✅ 确认 |
| 5 | quotaByTenant | `billing/quota.go`：并发改 `concurrency.Semaphore`（Redis 槽位/SETNX 或本地 channel，`TryAcquire` 返回释放闭包配对）；QPS 改 Redis 秒窗（INCR+EXPIRE），未启用降级本地滑动窗口；上限配置仍内存驻留+system_config 持久化 | ✅ 已完成 |
| 6 | startPackScraper | `api/packscraper.go`：复用 `distlock.New("scrape:lock")`（Redis SETNX+看门狗续期，未启用降级进程锁），抢锁失败跳过本轮 | ✅ 已完成 |
| 7 | metrics | 进程内聚合保留（Prometheus 侧聚合，无需跨实例一致） | ✅ 不改 |
| 8 | 引擎缓存 | 只读派生缓存保留（可重建） | ✅ 不改 |

### 新增 Redis 原语

- `infra/redis`：新增 `Set(key, val, ttl)`（无条件写入，验证码覆盖用）。
- `api/vcode.go`：验证码双后端读写器（Redis 优先读、本地 map 兜底、双写双删）。

### 测试与验证

- 新增 `billing/quota_test.go`（并发/信号量上限、QPS 秒窗、跨租户隔离、-race 并发安全）。
- 双实例冒烟（本地两进程连同一 Redis :16379）实证：
  - **验证码跨实例共享**：实例1 生成码 `606281` → 实例2 校验注册成功 → Redis 键消费后删除。
  - **采集锁互斥**：`SETNX` 键存在时抢锁失败（distlock 据此让另一实例跳过）。
  - **QPS 跨实例聚合**：Redis 秒窗 INCR×7，上限 5 时第 6 个被 BLOCK。
- `go build ./...`、`go vet ./...`、`go test ./... -count=1` 全绿。

---

# 第二部分 · 技术债② 生产 systemd 沙箱化迁移

> **执行入口（已就绪，2026-09-09）**：
> - 一键迁移脚本 `deploy/systemd/migrate_sandbox.sh`（`check`/`migrate`/`rollback` 三模式，含自动备份 + JWT_SECRET 一致性校验 + drop-in 清理）
> - drop-in 成品 `deploy/systemd/prod.conf`（已按生产实测覆盖 GOMEMLIMIT=850Mi / MemoryMax=1150M / WORKER_CONCURRENCY=4 / PDF_FONT_PATH，并收窄 ReadWritePaths 白名单）
> - 本地验收 `deploy/deploy_check.sh --systemd`（进程归属/密钥隔离/沙箱/旧drop-in 四项）
> - 下方 Step 1-6 为方案基线；执行时可直接用脚本，无需逐条手敲。

## 一、现状（历史 root + 明文密钥部署）

**生产实况（2026-09-04 复核）**：线上 `translator.service` 仍为历史 `User=root` + `Environment=` 明文密钥部署，并叠加 6 个 drop-in：
`concurrency` / `hardening` / `mail` / `mem` / `pdffont` / `secrets`。

与 `deploy/systemd/prod.conf` 沙箱要求的差距：
| 项 | 现状（root 明文） | 目标（沙箱） |
|----|--------------------|--------------|
| 运行账号 | `root` | `translator`（`nologin`） |
| 密钥 | `Environment=` 明文 | `EnvironmentFile=/etc/translator/secrets.env`(0600) |
| 进程沙箱 | 无 | NoNewPrivileges / ProtectSystem / PrivateTmp / RestrictSUIDSGID / ProtectKernelTunables / LockPersonality |
| 内存红线 | 六个 drop-in 叠加 | 收敛进 prod.conf |

## 二、改造核心难点

| 难点 | 说明 | 对策 |
|------|------|------|
| **停机窗口** | 切换 systemd 需重启服务 | 低峰窗口执行 + `deploy_check.sh` 快速验收 |
| **六个 drop-in 合并** | 主 unit + 6 drop-in 各自定义密钥/参数 | 统一收敛进 prod.conf 或 1 个 drop-in |
| **目录权限** | translator 用户需读 `/opt/translator`、写 `_uploads/_output/backups/log` | 一次性 `chown -R translator:translator` |
| **Caddy 回调对齐** | `X-Admin-Token` 必须与 translator 一致 | secrets.env 的 `ADMIN_TOKEN` 与 Caddy 环境变量同值 |
| **PDF 转换子进程** | pdf2docx/LibreOffice 需访问字体/venv | 授 translator 读字体路径 + venv（或 venv 归 translator 所有） |
| **Redis/DB 连接** | DSN 明文（`pg_dsn.txt` 0600） | 一并迁入 secrets.env（`DB_DSN`） |

## 三、迁移步骤（服务器 root 执行）

### Step 1 · 备份现状（可回滚）

```bash
TS=$(date +%Y%m%d_%H%M%S)
cp /etc/systemd/system/translator.service /opt/translator/data/translator.service.pre-sandbox.$TS
cp -r /etc/systemd/system/translator.service.d /opt/translator/data/translator.service.d.pre-sandbox.$TS
systemctl stop translator
```

### Step 2 · 建专用运行账号并授权目录

```bash
useradd -r -s /usr/sbin/nologin translator || true
# 目录归属一次性授权（保留备份数据）
chown -R translator:translator /opt/translator
# PDF 转换子进程依赖：字体 + venv（若 venv 在 /opt/translator/.venv 已随 chown 覆盖）
chmod o+rX /usr/share/fonts/opentype/noto /usr/share/fonts/truetype/alibaba 2>/dev/null || true
```

### Step 3 · 收敛密钥到 0600 独立文件

```bash
mkdir -p /etc/translator && chmod 750 /etc/translator
# 从现有 unit/drop-in 提取真实密钥，写入 secrets.env（一次迁移，勿泄露日志）
cat > /etc/translator/secrets.env <<EOF
JWT_SECRET=<现役 JWT_SECRET 值>
ADMIN_INIT_PASSWORD=<现役值或重新生成>
ADMIN_TOKEN=<现役 ADMIN_TOKEN 值（须与 Caddy 一致）>
METRICS_TOKEN=$(openssl rand -hex 16)
DB_DRIVER=postgres
DB_DSN=<从 /opt/translator/pg_dsn.txt 读取>
REDIS_ADDR=<现役值，可选>
REDIS_PASSWORD=<现役值，可选>
SMTP_HOST=<现役值> SMTP_PORT=<现役值>
SMTP_USER=<现役值> SMTP_PASS=<现役值>
SMTP_FROM=<现役值>
INFO_SMTP_HOST=<现役值> INFO_SMTP_PORT=465
INFO_SMTP_USER=<现役值> INFO_SMTP_PASS=<现役值>
INFO_SMTP_FROM=<现役值> INFO_SMTP_ENABLED=1
MAIL_ENABLED=1
EOF
chmod 600 /etc/translator/secrets.env
```

> ⚠️ **注意**：若迁移后 `JWT_SECRET` 变化，库内所有 `enc:v1:` 密文（LLM 密钥/API Key）将无法解密，且已签发 JWT 全部失效。**务必沿用现役 `JWT_SECRET` 原值**，或接受全量重登 + 重配密钥的代价。

### Step 4 · 重写 systemd 主 unit + 收敛 drop-in

**主 unit** `/etc/systemd/system/translator.service`（`User`/`Group` 可留空，由 drop-in 覆盖，保持与历史结构兼容）：

```ini
[Unit]
Description=Translator SaaS Service
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/translator
ExecStart=/opt/translator/bin/translator-server \
  -addr 127.0.0.1:8787 \
  -frontend /opt/translator/web \
  -kbdb /opt/translator/data/tm.sqlite3
StandardOutput=append:/opt/translator/log/translator.log
StandardError=append:/opt/translator/log/translator.log
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
```

**drop-in** `/etc/systemd/system/translator.service.d/prod.conf`（直接复用 `deploy/systemd/prod.conf`，合并 6 个 drop-in 的参数）：

```ini
[Service]
User=translator
Group=translator
EnvironmentFile=/etc/translator/secrets.env
Environment=REQUIRE_PROD_SECRETS=1
Environment=TRUST_PROXY_XFF=1
# 内存红线（生产实测 850MiB；prod.conf 默认 650Mi，此处按生产覆盖）
Environment=GOMEMLIMIT=850Mi
MemoryMax=1150M
# 并发
Environment=LLM_CHAT_CONCURRENT=2
Environment=LLM_EMBED_CONCURRENT=6
Environment=FILEPROC_MAX_CONCURRENT=1
Environment=WORKER_CONCURRENCY=4
# PDF 字体
Environment=PDF_FONT_PATH=/usr/share/fonts/opentype/noto/NotoSansCJK-Regular.ttc
# 进程沙箱
NoNewPrivileges=true
ProtectSystem=full
PrivateTmp=true
ProtectKernelTunables=true
ProtectControlGroups=true
RestrictSUIDSGID=true
LockPersonality=true
# 可写目录白名单（数据/上传/输出/日志/备份）
ReadWritePaths=/opt/translator/data /opt/translator/log /opt/translator/_uploads /opt/translator/_output /opt/translator/data/backups
```

> ⚠️ **删除旧 6 个 drop-in**（concurrency/hardening/mail/mem/pdffont/secrets），避免与新 prod.conf 重复定义密钥/参数冲突：
> ```bash
> rm -f /etc/systemd/system/translator.service.d/{concurrency,hardening,mail,mem,pdffont,secrets}.conf
> ```

### Step 5 · 同步 Caddy 回调凭证

确认 `/etc/caddy/translator.conf` 中 `/api/pay/notify/*` 的 `header_up X-Admin-Token` 值 == secrets.env 的 `ADMIN_TOKEN`。若 Caddy 引用环境变量 `{$TRANSLATOR_ADMIN_TOKEN}`（在 `/etc/default/caddy`），则同步该值。

### Step 6 · 重载并验证

```bash
systemctl daemon-reload
# 预检：以 translator 用户跑一次权限自检（可选）
systemctl start translator
systemctl status translator

# 验收
./deploy/deploy_check.sh https://langcross.lexicorn.cn
journalctl -u translator -n 30   # 确认无「生产密钥强校验」拒启 / 无 panic
curl -s https://langcross.lexicorn.cn/api/health
# 邮件/回调/Redis/DB 冒烟
curl -s -o /dev/null -w "%{http_code}\n" -X POST https://langcross.lexicorn.cn/api/pay/notify/mock -d '{"order_no":"x","amount":1}'  # 期望 403
```

## 四、验收清单

| # | 检查 | 通过标准 |
|---|------|----------|
| 1 | 进程归属 | `ps -o user,cmd -p <PID>` 显示 `translator` |
| 2 | 密钥隔离 | `systemctl cat translator` 无明文密钥；`/etc/translator/secrets.env` 权限 600 |
| 3 | 沙箱生效 | `systemctl show translator` 含 `NoNewPrivileges=yes`、`ProtectSystem=full` |
| 4 | 数据库/向量 | `/api/health` 全 true、`/status` dialect=postgres、pgvector 检索正常 |
| 5 | 翻译 | 文本翻译 + 文件翻译（含 PDF 转换，验证字体/venv 权限） |
| 6 | 邮件 | 注册/找回密码邮件真实送达 |
| 7 | 支付回调 | 无凭证 403、带凭证不 403 |
| 8 | Redis | `redis.Enabled()`、`/status` Redis 已启用 |
| 9 | 计费 | 运营策略/余额/用量看板正常 |
| 10 | 日志 | 重启后 0 panic，无权限类报错 |

## 五、回滚

```bash
systemctl stop translator
cp /opt/translator/data/translator.service.pre-sandbox.$TS /etc/systemd/system/translator.service
rm -rf /etc/systemd/system/translator.service.d
cp -r /opt/translator/data/translator.service.d.pre-sandbox.$TS /etc/systemd/system/translator.service.d
systemctl daemon-reload && systemctl start translator
```

## 六、关键风险提示

1. **JWT_SECRET 不可变**：迁移必须沿用现役值，否则全量重登 + 库内密文重配。
2. **目录权限遗漏**：`_uploads/_output/backups/log` 若漏 chown，翻译产物/备份会写失败。
3. **字体/venv 权限**：PDF 转换子进程读不到字体/venv 会渲染方框或转换失败，需 `chmod o+rX`。
4. **drop-in 残留冲突**：旧 6 个 drop-in 未删会导致密钥/参数重复定义，systemd 以最后加载者为准，需清理。
5. **MemoryMax**：从 950M（prod.conf）按生产实测调回 1150M，避免低峰重启被 OOM 误杀。

---