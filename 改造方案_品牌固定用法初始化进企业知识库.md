# 技术方案 · 品牌固定用法初始化进企业（租户）知识库 · 防止翻译漂移

> 版本 v1.0 ｜ 2026-09-04 ｜ 决策人：项目所有者 ｜ 状态：🕐 设计定稿，待实施

需求拍板：
① 品牌名/品牌英文名属**固定用法**，必须在租户落地时**初始化进企业知识库（企业包 L1 术语）**，防止后续 AI 翻译把品牌名音译为拼音（极石→jixi/jishi）等漂移。
② 注册/建租户时**引导填写**品牌中文名、品牌英文名（以及其它语言名）。
③ 语言覆盖策略：**没有其它语言名称时，品牌英文名覆盖所有非 zh/zh_hant 目标语**；zh_hant 沿用品牌中文名。

---

## 一、背景与目标

### 1.1 问题现象（实测）
俄文翻译「极石汽车驾驶要领」时，品牌「极石」未被正确命中知识库译法 ROX，而是被模型音译为 **Jixi**（`Центр обслуживания автомобилей Jixi`）。

根因链：
1. 租户企业包 `kb_entries`/`tm_segments` 中**没有** `极石→ROX` 这类品牌术语；
2. `kb_match` 阶段精确/语义检索均未命中该品牌词（没有该条数据）；
3. 模型只能自由发挥 → 按拼音音译为 jixi（俄文环境又绕过 `postprocess.brandReplace` 只能事后补救的 jishi/jieshi/jixi 正则，且该硬编码只覆盖极石一家）。

### 1.2 目标
- **租户落地即种入品牌固定用法**：新建（注册/超管建租户/受邀场景缺省）租户时，把品牌中文名→各目标语名称作为 **L1 术语**初始化进企业包。
- **引导录入**：注册表单与超管建租户表单补充「品牌中文名 / 品牌英文名（或其它语言名）」录入项。
- **语言兜底**：仅有品牌中文名+英文名时，英文名覆盖全部非 zh/zh_hant 目标语；未提供英文名则不种入（避免污染）。
- **可维护**：后台品牌定制（`brand_name`）仍由租户管理员维护；`brand_names`/`brand_name_en` 持久化到租户表，供展示与后续补种。

### 1.3 术语
- **品牌中文名**：`brand_name`（注册引导项，同后台品牌展示名），也是术语**源文本**（source_lang=zh）。
- **品牌英文名**：`brand_name_en`，作为各非中文目标语的**兜底译文**。
- **品牌多语言名**：`brand_names`（JSON `{"zh":"极石","en":"ROX","ru":"..."}`），显式指定某语言的品牌名时优先于英文名兜底。

---

## 二、方案总览

```
注册表单 / 超管建租户 / 品牌定制
   ↓ 提交品牌 中文名 + 英文名（或其它语言名）
后端 handleRegister / handleTenantCreate / handleTenantBrandingSet
   ↓ seedTenantBrandTerms(tid, brandName, brandNameEn, brandNamesJSON)
   ↓ ① 持久化 tenants.brand_names / brand_name_en　② 校验 JSON
   ↓ ③ Store.SeedBrandTerms(tid, names)
   ↓    企业包(tenant, L1) 幂等写 kb_entries + tm_segments（priority=1）
   ↓
后续翻译 kb_match：FindEntriesBySourceScoped → 命中「极石→ROX」
   → ai_initial 注入术语例句 → 模型沿用 ROX
```

三层防线：
1. **知识库命中（治本）**：企业包 L1 术语种入 → 精确命中 + 例句注入 prompt。
2. **提示词语境**：`workflow.go` 把 `res.Examples` 持久化并注入 ai_initial（既有整改，见 `workflow.go:186`）。
3. **后处理兜底**：`postprocess.go:267 brandReplace` 保留作最后保险（jishi/jieshi/jixi→ROX）。

---

## 三、数据模型变更

### 3.1 tenants 表新增列（幂等补列，PG/SQLite 双兼容）

| 列 | 类型 | 缺省 | 说明 |
|---|---|---|---|
| `brand_names` | TEXT | `'{}'` | 品牌多语言名 JSON（`{"zh":"极石","en":"ROX"}`） |
| `brand_name_en` | TEXT | `''` | 品牌英文名（兼容旧字段；统一入口为 brand_names JSON） |

复用 `db.EnsureColumns`（`tenant.go ensureTable` 的 `cols` map）幂等补列，无需独立迁移。

### 3.2 Tenant 结构体
```
BrandName   string (既有, 品牌展示名/中文名)
BrandNames  string // 多语言名 JSON
BrandNameEn string // 品牌英文名
```

### 3.3 术语条目归属
- 包：租户企业包（`kb_packages.code='tenant'`，优先于行业/文化包）。
- 层：`LayerTerm`（L1 术语）。
- 源：`source_lang='zh'`，`source_text=品牌中文名`。
- 译文：按语言兜底（见第四节）。
- 写库：复用 `Store.SaveEntry`（幂等 upsert `kb_entries` + 同步 `tm_segments` priority=1）。

---

## 四、种入策略（核心规则）

```
input: names = {"zh":"极石","en":"ROX"}（en 可缺省）
for lang in kb.AllLangs:                     # 34 列语言
    if lang == "zh": continue
    tgt = names[lang]                        # 1) 显式语言名优先
    if tgt == "":
        if lang == "zh_hant": tgt = names["zh"]   # 2) 繁体沿用中文名
        else:                tgt = names["en"]    # 3) 其余语言英文名兜底
    if tgt == "": continue                   # 4) 无可用译文则不种入该语言
    SaveEntry(tid, tenantPkgID, LayerTerm, "zh", names["zh"], lang, tgt, module="brand")
```

边界：
- 未填任何品牌名 → 不种入（`len(names)==0` 直接 return）。
- 有中文名无英文名 → 仅 zh_hant 种入（zh→zh_hant 品牌名不变）。
- 非法 brand_names JSON → 忽略该字段，回退到 brandName/brandNameEn。
- 幂等：重复调用按 (tenant, pkg, src, lang) upsert，不会产生重复行。

---

## 五、代码改动清单

### 5.1 后端
| 文件 | 改动 |
|---|---|
| `internal/tenant/tenant.go` | Tenant 加 `BrandNames`/`BrandNameEn` 字段；`ensureTable` cols 补 `brand_names`、`brand_name_en`；`tenantColumns`/`scanTenant` 同步；新增 `SetBrandNames`、`SetBrandNameEn` |
| `internal/store/kbpackages.go` | 新增 `SeedBrandTerms(tid, names) error`（第四节规则，写在「≡ 条目 =」段落首） |
| `internal/api/register.go` | 注册 request 加 `brand_name`/`brand_name_en`/`brand_names`；`handleRegister` 建租户后调 `seedTenantBrandTerms`；新增 `seedTenantBrandTerms` 辅助函数 |
| `internal/api/tenant.go` | `handleTenantCreate` 同样支持品牌名录入并种入 |
| `internal/api/tenant.go` | `handleTenantBrandingSet` POST 保存时同步更新 brand_names 并补种（后台维护一致性） |

### 5.2 前端
| 文件 | 改动 |
|---|---|
| `src/components/Login.tsx` | 注册表单「企业用户/管理员（新建企业）」区块新增「品牌中文名」「品牌英文名（或其它语言名）」录入；提交时带上 brand_name/brand_name_en/brand_names |
| `src/api/auth.ts` | `authRegister` 入参类型补 `brand_name`/`brand_name_en`/`brand_names` |
| `src/i18n/dicts.zh.ts` `dicts.en.ts` | 注册表单品牌名录入文案（中/英） |

### 5.3 说明
- `postprocess.brandReplace` **保留**：对未种入品牌名（未填英文名等）场景仍做兜底清洗；本方案未来可将其泛化为「按租户 brand_names 动态替换」，不在本次范围。

---

## 六、影响面与风险

| 项 | 评估 |
|---|---|
| DB 兼容 | `EnsureColumns` 幂等补列，PG/SQLite 均自动补，无停机 |
| 存量租户 | 不回填（避免误判企业品牌名）；后台品牌定制保存时对该租户**触发补种**，运营可在后台录入品牌名后即生效 |
| 术语污染 | 仅租户自己的企业包，优先级低于部门包；不会跨租户泄漏（SaveEntry 按 tenant 隔离） |
| 性能 | 每次最多 34 条 upsert，注册/建租户低频路径，无影响 |
| 品牌名翻译 | 品牌名通常不译（ROX 各语言保持 ROX）；对确有本地化品牌名的语种（如法文/俄文变体），由 `brand_names[lang]` 显式覆盖 |

---

## 七、验收标准

1. 新建企业租户：注册填品牌中文名「极石」+ 英文名「ROX」→ `kb_entries` 出现 `极石 / zh→en=ROX`、`zh→ru=ROX` …共 33 条（除 zh），`tm_segments` priority=1。
2. 仅填中文名「极石」→ 只种入 zh_hant 一条（极石→极石）。
3. 不填品牌名 → 企业包无 brand 模块术语。
4. 俄文翻译含「极石汽车服务中心」文档 → 译文出现 `ROX`（而非 Jixi/Jishi）。
5. 后台品牌定制改品牌名后再次保存 → 该租户术语被更新（幂等 upsert）。
6. `go build ./...` 、`go test ./internal/store/... ./internal/tenant/... ./internal/api/...` 全绿。