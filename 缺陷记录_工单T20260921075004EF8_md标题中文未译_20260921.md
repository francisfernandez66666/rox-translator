# 缺陷记录 — `.md` 工单翻译质量问题（BUG-20260921-MD01）

- **缺陷编号**：BUG-20260921-MD01
- **工单号**：`T20260921075004EF8`
- **文件**：`产品方案书_v5.1.md`（Markdown）→ 目标语言 **英语**
- **工单状态**：已完成（**未判失败**，这是定位关键线索之一）
- **创建时间**：2026-09-21 07:50:04
- **报告人**：用户（现象：译文文件里有大量中文；观察到的规律是「**`#` 后面的中文不翻译**」）
- **定位人**：AI 助手（只读排查，未改任何产品代码）
- **最后更新**：2026-09-21 23:50 —— **用户裁定「代码围栏内不翻译」是缺陷**（我先前误判为设计），
  并要求完整交付口径：**清洗脏数据 → 保留原文原格式 → 译文为目标语言 → 文件名也要翻译**。
  本次已按该口径重列缺陷（见 §1）。
- **2026-09-22 00:20 补**：用户提供**原文档** `/Users/zhangzifei/Downloads/产品方案书_v5.1.md`（712 行 / 22,708 字）。
  已对该原件做**真实往返实测**（`ExtractTexts` → `ApplyAlignedText`，假翻译器出纯 ASCII），
  据此：① 拿到围栏清单并验证「围栏口径」是否够用（**§5.7.6**）；
  ② **新发现两个缺陷 RC-5 / RC-6**（§7 / §8）——其中 RC-5 的严重度**高于**原有一族
  （不是"少翻一段"，而是**把原文里的技术标识符整段删掉**）。
  **仍未改动任何产品代码**。

---

## 1. 交付口径（用户裁定）与现状差距

**要求**：翻译产物 = **清洗掉脏数据** 后，**保留原文原格式**、**内容为对应目标语言**、**文件名也翻译掉**。

| # | 要求 | 现状 | 判定 |
|---|---|---|---|
| **R1** | **代码围栏（```）内的内容也要翻译**（保留围栏标记，只译内容；**真代码不译、注释译**） | 提取侧**整段不入库**（`text_formats.go:104-108`），回写侧**永不替换**（`:148-152`）；且 `d5_writeback_test.go:44` 把"围栏内不译"**断言为正确** | ❌ **缺陷（已裁定）** → **§5（RC-3）** |
| **R2** | 保留原文原格式 | 围栏标记 / 表格 / 缩进 / 空行保留 ✅；但**行内标记被系统性剥除**：`**` 实测 **381→0**、反引号内 **16 个技术标识符被整段删除** | ❌ **缺陷（2026-09-22 实测）** → **§7（RC-5）** |
| **R3** | 交付「对应的翻译结果语言」 | 正文内容为目标语 ✅；但**目录锚点 `#中文…` 不同步**（标题已译、锚点没译 ⇒ 目录点不动） | ⚠️ 低优先 → **§8（RC-6）** |
| **R4** | **文件名也要翻译掉** | **零实现**：`file.go:708` 起 `baseName` 恒为原名，全部 4 处产物命名（`:717 :728 :782 :1051`）都用原名拼 | ❌ **缺口** → **§6（RC-4）** |
| **R5** | 清洗掉脏数据 | `engine.PostProcessTranslation` 清洗链覆盖译文脏数据 ✅；但 **KB「源文=译文」脏行**不清洗、不重试、不告警 | ❌ **部分** → **§4（RC-2）** |
| **R6** | **译文里不应残留中文** | 实测：源文含中文行 **418** 行 → 产物**仍含中文 87 行**（**围栏内 72** + **目录锚点 15**）= **20.8%** | ❌ → **§5 / §8** |

---

## 2. 已排除的负面结论：`# 标题`（有空格）的解析链路是正确的

排查假设：既然规律是「`#` 后面的中文不翻译」，先怀疑 Markdown 提取器把标题整行当结构丢弃、或回写时键对不上。

**实测方法**：直接调用该链路的真实函数做往返（`extractMarkdown` → `ExtractTexts` → `ApplyAlignedText`），用一个「假翻译器」把每个提取到的键替换成 `EN<键>`，再逐行找**仍是中文**的行（=漏译行）。

**结果：`# 标题`（井号后有空格）形态全部正常**：

| 源行 | 提取到的键（=送模型的内容） | 回写结果 |
|---|---|---|
| `# 产品方案书 v5.1` | `产品方案书 v5.1` | `# EN<产品方案书 v5.1>` ✅ |
| `## 一、产品定位` | `一、产品定位` | `## EN<一、产品定位>` ✅ |
| `### 1.1 核心能力` | `1.1 核心能力` | `### EN<1.1 核心能力>` ✅ |
| `- 支持十二语种互译` | `支持十二语种互译` | `- EN<支持十二语种互译>` ✅ |
| `1. 第一步：上传文件` | `第一步：上传文件` | `1. EN<第一步：上传文件>` ✅ |
| `> 引用块内的中文说明` | `引用块内的中文说明` | `> EN<引用块内的中文说明>` ✅ |

→ 结论：`mdStructPrefixRe` 对「有空格」写法工作正常（`#` 剥离、标题翻译、回写粘回）。
**这一条不是 bug**，可以排除——免得把修复力气花错地方。

（代码位置：`internal/fileproc/text_formats.go:96 extractMarkdown` / `:134 ApplyAlignedText` / `:188 mdStructPrefixRe`。）

---

## 3. RC-1【代码缺陷】前缀正则不支持「无空格」写法

### 3.1 代码事实

`internal/fileproc/text_formats.go:188`：

```go
var mdStructPrefixRe = regexp.MustCompile(`^([ \t]*(?:#{1,6}|>|[-*+]|\d+[.)])[ \t]+)`)
//                                                            ^^^^^^^ ← 标记后必须有空格/制表符
```

`text_formats.go:113`（提取侧）：匹配不上 ⇒ **整行（含 `#`）成为送模型的键**。

### 3.2 实测矩阵（20 种写法）

**6 / 20 把结构标记留在了键里，全部是「无空格」形态**：

| 写法 | 送模型的键 | 判定 |
|---|---|---|
| `# 标题` / `#\t标题` / `####  标题` | `标题` | ✅ 标记已剥离 |
| **`#标题`** | **`#标题`** | ❌ 含标记 |
| **`##标题`** | **`##标题`** | ❌ 含标记 |
| **`###1.1 三井号无空格`** | **`###1.1 三井号无空格`** | ❌ 含标记 |
| `- 列表项` / `* 列表项` / `+ 列表项` | `列表项` | ✅ |
| **`-列表项`** | **`-列表项`** | ❌ 含标记 |
| `1. 编号项` / `1) 编号项` | `编号项` | ✅ |
| **`1.编号项`** | **`1.编号项`** | ❌ 含标记 |
| `> 引用块` | `引用块` | ✅ |
| **`>引用块`** | **`>引用块`** | ❌ 含标记 |

### 3.3 为什么这会让标题留中文

键以 `#` 开头 → 模型极易当成**注释/元语法**而**原样回显** → 被回显检测丢弃
（`file.go:512`、`:554` 均为 `!= 源文`）→ 硬闸补漏（`:576-669`）仍回显
→ **连续 2 轮零进展熔断**（`:609-614`）→ 写回时该行「未命中」→ **按设计保留原文**
（`text_formats.go:169-171`）→ 标题仍是中文。

### 3.4 附带缺陷：翻译成功反而会**丢掉 `#`**

`text_formats.go:158` 回写取 `pfx = mdStructPrefixRe.FindString(raw)`，「无空格」形态取不到 ⇒ `pfx=""`
⇒ 不粘回 ⇒ 整行替换成译文 ⇒ **`#` 标记消失**（Markdown 层级被破坏）。

**即「无空格」标题当前没有正确结局**：回显 → 留中文；译成功 → 丢标记。

### 3.5 对本工单的实际命中度：**0**（2026-09-22 用原件复核）

对原文档 `产品方案书_v5.1.md` 全文扫描：

| 形态 | 行数 |
|---|---|
| 「标记 + 空格」写法（`mdStructPrefixRe` 匹配 ⇒ 解析正确） | **143** |
| 「结构标记无空格 + 含中文」（RC-1 真命中） | **0** |

> 复核方法提示：初筛曾把 15 行 `**关键设计原则**：…` 误判为「无空格列表项」。**不是**——
> `mdStructPrefixRe` 要求标记后跟 `[ \t]+` 或行尾，`**` 后紧跟 `*` 不满足；且提取期
> `emphasisRe` 会先把 `**` 剥掉（详见 §7）。**凡"命中数"结论必须用第二种方法复算**。

→ **结论：RC-1 是真实缺陷，但不是本工单现象的原因**（这份文档一次都没踩到）。
用户看到的「`#` 后面的中文不翻译」真正对应的是 **RC-2（KB 坏命中）** 与 **RC-3（围栏整块不译）**，
另加本次新实测出的 **RC-5 / RC-6**。

---

## 4. RC-2【代码缺陷】KB 直配命中缺「同文」守卫 + 命中后永久跳过补漏

### 4.1 三条写入口，只有 KB 那条没有守卫

| 位置 | 路径 | 守卫 |
|---|---|---|
| `file.go:483` | **KB 直配命中** | `addTrans(lc, texts[i], kbVal[i])` ← **无任何守卫** |
| `file.go:513` | KB 语言·模型补漏 | `batch[i] != texts[idx]` ✅ |
| `file.go:555` | 其他语言·模型直翻 | `batch[i] != t` ✅ |
| `file.go:636` / `:653` | 硬闸重试 | `v != m` ✅ |

`file.go:465-472` 原文：

```go
if v, ok := r.Translations[lc]; ok && v != "" {   // ← 只判非空，不判「是否等于源文」
```

`addTrans`（`file.go:399-406`）是裸 map 写，无校验。

### 4.2 后果一：`源文 = 译文` 的 KB 行把中文当"译文"写进成品

来源可能是：**双语对照/TMX 导入**（真实双语文件里大量条目本就 `source == target`）、
**术语自映射条目**、**历史污染沉淀**。

### 4.3 后果二（更致命）：命中后硬闸补漏**永久跳过**该段

`file.go:596-601`：`if _, ok := langTranslations[lc][t]; !ok { … }` —— 键已存在即跳过。
KB 命中已把键写进 `langTranslations` ⇒ **不重试、不计入 `untranslated`（`:660-664`）、不告警、永久锁死**。

### 4.4 与用户现象的吻合度

标题是**跨文档版本最容易被 KB 精确命中**的段（`产品方案书 v5.1` 与 `v5.0` 标题几乎不变），
一旦沉淀一条坏行，**每次上传新版本都稳定复现且只有标题受影响**。

---

## 5. RC-3【代码缺陷 · 用户裁定】代码围栏内的内容完全不翻译

> **用户裁定**：「谁跟你说代码围栏内不译是设计的，这是缺陷。」
> 期望：**围栏标记保留，围栏内的内容照常翻译并回填**。

### 5.1 代码事实

**提取侧**（`internal/fileproc/text_formats.go:101-110`）：

```go
inCode := false
for _, raw := range strings.Split(string(b), "\n") {
    t := strings.TrimSpace(strings.TrimRight(raw, "\r"))
    if strings.HasPrefix(t, "```") || strings.HasPrefix(t, "~~~") {
        inCode = !inCode          // 围栏开合翻转
        continue                  // ← 围栏行本身不入库
    }
    if inCode || t == "" || sepRe.MatchString(t) {
        continue                  // ← 围栏内的每一行都不入库 ⇒ 永不翻译
    }
```

**回写侧**（`text_formats.go:144-154`）：同样维护 `inCode`，`inCode` 时 `continue` ⇒ **永不替换**。
注释也明确写着（`:129`）：「围栏（```/~~~）内与分隔线行**永不替换**」。

→ 净效果：**围栏整块原样保留源语言**（中文文档里就是中文）。

### 5.2 加重情节：既有测试把这个错误行为**固化为正确**

`internal/fileproc/d5_writeback_test.go:43-45`：

```go
if !strings.Contains(res, "```go\n你好\n```") {
    t.Fatalf("代码围栏内容被误翻译: %q", res)     // ← 把「不翻译」断言成正确
}
```

**后果**：修好 RC-3 会让 CI 变红。所以修复必须**同步改这条断言**，否则会被当成回归而回滚——
这是这个缺陷能长期存在的直接原因之一。

### 5.3 为什么对「产品方案书」这类文档影响很大

中文产品文档里的 ``` 块**大量不是代码**，而是：

```
┌──────────────┬──────────────┐
│  用户下单     │  系统计费     │      ← ASCII 流程图 / 架构图
└──────────────┴──────────────┘
```

```
示例对话：
用户：这个多少钱？
客服：……                              ← 话术示例 / 报错样例 / 配置样例
```

这类内容**必须翻译**。现在它们整块留中文 → 直接构成用户看到的「**大量中文**」。

### 5.4 修复时必须保留的能力（别修坏）

围栏**标记本身**、围栏**语言标注**（```go）、**缩进/空行/对齐**都必须原样保留——
即「只替换内容、不动骨架」。

### 5.5 ★ 围栏翻译策略（用户裁定，2026-09-21 23:5x）

> **裁定原文**：「代码内容，根据是真代码还是真注释区别，代码的话不翻译，注释的话翻译成对应语言。」

即 **不是"整块全译"，也不是"整块不译"，而是按语义分类**：

| 围栏内的行 | 处理 |
|---|---|
| **注释**（行注释 / 块注释 / 行尾注释） | ✅ **翻译**成目标语言 |
| **真代码**（语句、表达式、键名、结构骨架） | ❌ **不翻译**（逐字节保留） |
| 围栏标记行（```、```lang）与缩进/空行 | ❌ 永不改动 |

**注释语法按语言标注分派**（实现所需的最小表）：

| 语言标注 | 行注释 | 块注释 |
|---|---|---|
| `go` `js` `ts` `jsx` `tsx` `java` `c` `cpp` `cs` `rust` `swift` `kt` `scala` `php` `dart` `groovy` | `//` | `/* … */` |
| `py` `sh` `bash` `zsh` `yaml` `yml` `toml` `ruby` `perl` `r` `makefile` `dockerfile` `conf` `ini` `properties` | `#` | py: `"""…"""`；ruby: `=begin/=end` |
| `sql` `lua` `haskell` | `--` | sql: `/* … */` |
| `html` `xml` `vue` `svelte` | — | `<!-- … -->` |
| `lisp` `clojure` `elisp` | `;` | |
| `tex` `latex` | `%` | |
| `bat` `cmd` | `REM` / `::` | |
| `vb` | `'` | |
| **无标注 / `text` / `plain` / `plaintext` / `txt` / 未知语言** | **无"代码"概念 ⇒ 整块视为文本，全部翻译** | |
| `json` | **JSON 无注释语法 ⇒ 整块按代码保留**（键名与值都不动） | |

**必须一并处理的边界情况（否则实现必然踩坑）**：

1. **注释里的"指令"不能翻**（翻了会坏工具链）——需指令白名单前缀，命中则整行保留：
   `//go:build`、`//go:generate`、`//nolint`、`//export`、`# noqa`、`# type: ignore`、
   `# -*- coding: utf-8 -*-`、`/* eslint-disable */`、`#!/bin/sh`（shebang）、`// TODO(author)` 的署名部分。
2. **字符串里的伪注释**：`println("// 不是注释")` —— 只按**行首/前置空白**判定注释，
   不做全文扫描，避免把字符串内容当注释翻掉。
3. **行尾注释**：`x := 1 // 中文说明` —— **只翻 `//` 之后的部分**，`x := 1 ` 前缀逐字节保留。
4. **跨行块注释**：`/* 中文说明\n   更多中文\n */` —— 翻说明段，**`/*` 与 `*/` 所在行原样保留**。
5. **无语言标注的围栏**（用户文档里最常见）→ 按"文本"整块翻译。
6. **ASCII 图的列宽对齐**：框线行属"非注释"→ 保留；但**框内的中文说明**若要翻，
   译文长度会破坏 `┌──┬──┐` 的对齐 → **列为新的待确认项（见 §10-①）**。
7. **围栏归属需在提取侧标记**：当前提取/回写各自独立维护 `inCode`，修复时要让**回写能知道
   每一行的围栏语言与"是否注释"**，否则回写无法按同一政策筛选。

### 5.6 修复对既有测试的影响（重要）

`d5_writeback_test.go` 的用例是 ```go 围栏内放 `你好`（**不是注释**）。按新政策：
`go` 围栏里的裸中文属"真代码"→ **不翻译** → 该断言**恰好仍会通过**，
但它**没有表达正确意图**。因此修复时要：
1. 改写该断言为显式表达政策（go 围栏内的非注释不得被翻）；
2. **新增**两条断言：```go 围栏内的 `// 中文注释` **必须被翻译**；`x := 1` **必须逐字节不变**；
3. 新增一条：**无语言标注**的围栏内中文**必须被翻译**（这是产品文档最常见形态）。

### 5.7 ★ 对「代码不译 / 注释译」这条口径的工程评估（2026-09-21）

**结论：方向正确（与业界通行做法一致），但只靠这一条不足以修好本工单，建议把围栏再分三类。**

#### 5.7.1 它对的地方
1. **与业界一致**：微软 / Google / MDN 的开发者文档本地化，代码样例都是「注释翻、代码不翻」——
   这条口径是行业标准做法，不是拍脑袋。
2. **保住技术正确性**：标识符、关键字、字符串字面量一旦被翻，代码直接失效。
3. **可判定、可回归**：能写出「非注释行逐字节不变」的自动断言，比"整块全译"好验证得多。

#### 5.7.2 三个缺口（按严重度）

**缺口 1（最要紧）：这条口径预设「围栏里是代码」，但中文产品文档里多半不是。**

| 围栏里实际常见的内容 | 按本口径的结果 |
|---|---|
| 真源码样例（```go / ```json） | ✅ 注释被翻，符合预期 |
| **ASCII 示意图（`┌──┬──┐`）** | ❌ 没有"注释"概念 → 框内中文**仍然不翻** |
| **示例对话 / 报错样例 / 话术样例** | ❌ 同上 |
| **配置样例（yaml/toml 的中文值）** | ❌ 值不是注释 → 整块保留 |

→ **风险：你最想解决的那批中文，可能一条都不会被翻。**
→ **必须先确认**：`产品方案书_v5.1.md` 的围栏里到底装的是什么？（贴一段出来即可定）

**缺口 2：无语言标注的围栏没有归属。**
中文文档里 ``` 绝大多数**不写语言标注**，而本口径没有规定这种怎么处理。两种选法各有代价：

| 选法 | 好处 | 代价 |
|---|---|---|
| 无标注 → **整块翻译** | 符合产品文档现实，能真正消掉"大量中文" | 有人贴**未加标注的真代码**时会被翻坏 |
| 无标注 → **整块保留** | 绝对安全 | **你在文档里看到的示意图中文永远不会被翻，等于没修** |

**缺口 3：数据类围栏的"值"要不要翻？**
`json` 无注释语法；`yaml`/`toml` 只有 `#`。按本口径 → 整块保留。
但配置示例文档的正确做法通常是**翻字符串值、不动键名与结构**——本口径覆盖不到。

#### 5.7.3 三个必须写进实现的技术风险（不处理会翻坏东西）

1. **`//` 出现在字符串或 URL 里时不是注释。**
   `u := "https://x.dev/a"` —— 若按"行内含标记即注释"扫描，会把 `//` 之后整段当注释翻掉 →
   **字符串被破坏**。必须做**简易词法**：跟踪引号开合状态，并排除紧跟在 `:` 后的 `//`（`://`）。
2. **同一标记在不同语言里含义不同。**
   Python 的 `//` 是**整除运算符**（`a // b`）不是注释；`#` 在 py/sh/yaml 是注释、在 C 系是预处理指令。
   → 注释标记**必须按语言标注分派**，不能全局一刀切（§5.5 的表是必需项，不是可选项）。
3. **注释里的"指令"翻了会坏工具链。**
   `//go:build`、`//go:generate`、`//nolint`、`# noqa`、`# type: ignore`、`/* eslint-disable */`、
   `#!/bin/sh`、`# -*- coding: utf-8 -*-`、`// TODO(name)` 里的署名/单号 → 一律**整行保留**；
   注释里的 URL、issue 号、`file.go:12` 这类引用同样不该翻。

#### 5.7.4 建议：围栏分**三类**，而不是两类

| 围栏类型 | 判定方式 | 处理 |
|---|---|---|
| **真源码** | 有明确代码语言标注（`go`/`js`/`ts`/`java`/`py`/`c`/`rust`/`sql`/…） | **只翻注释** ← 本口径 |
| **文本 / 示意图** | 无标注、`text`/`plain`/`plaintext`，或内容明显非源码 | **整块翻译**（含框线行；ASCII 图列宽会错位 → 需重排或接受错位） |
| **数据** | `json`/`yaml`/`xml`/`csv`/`toml` | **只翻字符串值**，键名与结构不动 |
| （兜底） | 无法判定 | 按"文本"整块翻译，并在产物/轨迹里**给出提示**：贴真代码请加语言标注 |

#### 5.7.5 怎么验（便宜且能防回归）

对每种语言准备一段同时含 **代码行 + 注释行 + 字符串里的 `//`** 的样例，断言：
① 代码行与字符串**逐字节不变**；② 注释行已变；③ 围栏标记与缩进逐字节不变；
④ `//go:build` / `# noqa` 未被翻；⑤ 行尾注释的代码前缀逐字节不变。
ASCII 图：断言框线行不变（若走"文本"分支则放宽为"可错位但内容已译"）。

#### 5.7.6 ★ 实测：你这份 `产品方案书_v5.1.md` 的围栏清单（2026-09-22）

**这是判定"该口径够不够用"的唯一硬证据。** 原件 712 行 / 22,708 字，**10 个围栏块**：

| # | 起始行 | 语言标注 | 内容行 | 含中文行 | 围栏里实际装的东西 |
|---|---|---|---|---|---|
| 1 | 77 | **无** | 33 | 12 | **ASCII 架构图**（`┌──┬──┐`：网页端 / Go 后端 / 数据层） |
| 2 | 142 | **无** | 18 | 13 | **ASCII 分层图**（KB 四层优先级） |
| 3 | 179 | **无** | 11 | 2 | **伪码**（`func Translate(ticket) { … }`，中文在 `//` 注释里） |
| 4 | 207 | **无** | 16 | 4 | **伪码**（`func ProcessTicket(ticket):`，中文在 `#` 注释里） |
| 5 | 281 | **无** | 18 | 12 | **ASCII 知识生长图**（①②③ 三条回路） |
| 6 | 341 | **无** | 23 | 12 | **伪码**（`func EvaluateAgent(...)`：注释 + 中文值都有） |
| 7 | 368 | **无** | 10 | 9 | **裁判 Prompt 模板**（整段中文业务文本，**根本不是代码**） |
| 8 | 546 | **无** | 4 | 4 | **ASCII 甘特图**（`████` 条 + Week 标签） |
| 9 | 575 | **无** | 4 | 4 | ★ **安全词正则白名单** |
| 10 | 584 | **无** | 4 | 0 | 单位/缩写白名单（纯 ASCII，无中文） |
| | | **0 块有标注** | **141** | **72** | 占全文含中文行（418）的 **17.2%** |

**四个决定性事实：**

**① 语言标注命中率 = 0 / 10。** 没有一块写了 ` ```go ` / ` ```json `。
⇒ 「按语言标注分派注释语法」这条设计（§5.5 表）**在这份真实文档上一次都不会被触发**——
标注表本身没错，但它单独解决不了这份文档。

**② 三块（#1 #2 #5）是 ASCII 示意图，共 37 行中文。**
照「代码不译 / 注释译」字面执行 → **框线行属"非注释" → 框内 37 行中文一行都不会被翻**。
这就是用户看到的那批中文的主体（37 / 72）。

**③ ★ 第 9 块（安全词正则）绝对不能被翻译。** 它的内容是：

> `高压|触电|电击|禁止拆卸|禁止触摸|禁止打开|高温烫伤|安全气囊|儿童锁|Prop 65|ECE R100|GB/T 44298|`
> `Recall|召回|Warning|Caution|Danger|高压电池舱|高压危险|漏电|短路|电弧`

这些中文是**用来匹配中文源文的正则字面量**，不是"待翻译的正文"。
若按「无标注 → 整块翻译」这条兜底规则 → 送模型 → 译成英文 →
**正则再也匹配不到中文源文 ⇒ 安全句硬闸静默失效**。
（该文档自己的风险表第 2 条写的就是「安全句意译 = 法律合规风险、车企禁上牌」。）

**④ 第 7 块是 Prompt 模板**（`你是一名资深汽车领域翻译评估专家…`）：
功能上是"喂给模型的输入"，但在**文档里它就是要被翻译的正文**。
说明「按'看起来像不像代码'判断」也不可靠——**标注缺失时只能靠内容语义**。

**⇒ 因此结论只能是「分三类以上」，并且必须能识别"白名单 / 正则 / 配置"这类*不可翻数据*：**

| 类 | 本文件实例 | 处理 |
|---|---|---|
| **真源码 / 伪码** | #3 #4 #6 | **只翻注释与中文说明串**，代码骨架逐字节不变 ← **你的口径** |
| **文本 / 示意图 / Prompt** | #1 #2 #5 #7 #8 | **整块翻译**（`████` 条与框线行不动；ASCII 图列宽会错位 → 重排或接受） |
| **数据 / 白名单 / 正则** | #9 #10 | ★ **整块保留**（其中中文是**匹配模式**，翻了会破坏功能） |
| 兜底（判不了） | — | 按"文本"整块翻 + 在产物侧给出提示；并**为"不可翻块"提供显式标记**（如 ` ```regex ` / ` ```nolocalize `），让用户能精确表达"这块别翻" |

**⇒ 直接回答用户的问题**：口径**方向对**（§5.7.1），但**只有"代码不译/注释译"这一条，修不好这份文档**：
它会同时**漏掉 37 行 ASCII 图中文**（该翻的没翻）和**翻坏第 9 块安全词正则**（不该翻的翻了）。

---

## 6. RC-4【缺口 · 用户裁定】产物文件名不翻译

> **要求**：文件名也翻译掉。

### 6.1 代码事实：零实现

`internal/engine/file.go:708`：

```go
baseName := strings.TrimSuffix(filepath.Base(filePath), filepath.Ext(filePath))
```

此后 **4 处产物命名全部直接拼原名**，没有任何一步调用翻译：

| 位置 | 产物名 |
|---|---|
| `file.go:717` | `{原名}_{lc}_text.md` |
| `file.go:728` | `{原名}_{lc}.md` |
| `file.go:782` | `{原名}_{lc}{ext}` |
| `file.go:1051` | `{原名}_translated.xlsx` |

`git grep -i "文件名|filename|translatedName|nameTranslat"` 在 `internal/engine/` 与
`internal/fileproc/` 的**非测试代码中零命中** → 确认**从未实现**。

### 6.2 期望形态（按用户口径）

`产品方案书_v5.1.md`（→en）应交付形如 **`Product Proposal_v5.1_en.md`**（英文文件名 + 目标语言标注）。

### 6.3 修复要点

- 文件名要**送到模型翻一次**（短串、单独一次调用即可，可复用 `BatchTranslate` 单元素批）。
- 需处理：**扩展名必须保留**（`.md` 不能被翻）、**非法/保留字符清理**（`/\:*?"<>|`、前后空格、`.` 结尾）、
  **长度上限**（多数文件系统 255 字节），以及**下载响应头 `Content-Disposition`** 的同步
  （否则前端下载名会与磁盘名不一致）。
- 失败降级：文件名翻译失败时**保留原名**，不能因此判工单失败。

---

## 7. RC-5【代码缺陷 · 2026-09-22 实测新发现】行内标记被剥除 ⇒ 译文丢标记、丢内容

> **性质**：与围栏政策**无关**，是独立缺陷；**严重度高于 RC-1 / RC-3** ——
> 它不是"少翻一段"，而是**把原文里的技术标识符整段删掉**，且**不可恢复**（提取期就没进翻译表）。
> 命中 `file.go:732` 的 `.md` 主产物与 `:835` 的还原产物 —— **不是边角情况**。

### 7.1 代码事实

`internal/fileproc/text_formats.go:25`：

```go
emphasisRe = regexp.MustCompile(`\*\*([^*]+)\*\*|\*([^*]+)\*|__([^_]+)__|_([^_]+)_|` + "`" + `([^` + "`" + `]+)` + "`" + `|~~([^~]+)~~`)
//                             $1 ↑            $2 ↑         $3 ↑        $4 ↑    $5 ↑               $6 ↑
```

**替换串只引用了 `$1`**（提取侧 `:116`、回写侧 `:165` 两处都是同一写法）：

```go
t = emphasisRe.ReplaceAllString(t, "$1")
```

⇒ `**加粗**` → 只丢标记、留内容（`$1` 命中）；
⇒ `*斜体*` / `__粗__` / `_斜_` / 反引号代码 / `~~删除~~` → **整段（含内容）被删掉**，
   因为 **`$2`~`$6` 在替换串里根本没被引用**（Go 正则中未引用的捕获组直接丢弃）。

**且无任何回填逻辑**：`git grep emphasisRe` 只命中 `text_formats.go` 的提取与键规整两处；
`mdStructPrefixRe` 是**唯一**会被粘回的标记（`pfx`，`:158` / `:179`）。
⇒ 除 `#` / `>` / `-` / `1.` 之外的行内标记**永久消失**。

### 7.2 实测（真实文档往返：`ExtractTexts` → `ApplyAlignedText`，假翻译器输出纯 ASCII）

| 标记 | 原文 | 产物 | 差值 |
|---|---|---|---|
| `**` | **381** | **0** | **−381（100% 丢失）** |
| 单个反引号 | 94 | 60 | −34 |
| 围栏标记（三个反引号） | 20 | 20 | 0（围栏不动 ✅） |
| `│` / `┌` / `█` | 151 / 11 / 36 | 151 / 11 / 36 | 0（围栏不动 ✅） |

**反引号内的 16 个技术标识符从产物中彻底消失**（提取时就没进翻译表 → 送不到模型 → 写回无从恢复）：

```text
state / approved_by / approved_at / source_lang / dst_lang
full_len            shrink_draft         shrink.enabled=false
{ ok: bool, patch: [...], suspicious: [...] }
strings.Contains    ^[^<].*              runeCount ≤ max_chars
no_shrink_safe      regexp / strings     evalResult.score
翻译系统成本速算表_最简版.xlsx            ← ★ 连一个文件名都被删掉
```

**正文样例**（均在围栏外，非围栏内容）：

```text
原| > **文档状态**：定稿 · 更新版（2026-08-17，含 Eval 交叉裁判 + 最新峰谷定价 + Gemini 3.7）
产| > <译文>                        ← 10 行引用块的加粗标记全部消失

原| - 工单开 shrink → 校对对象 = `shrink_draft`（在人工长度预算内改刀）
产| - <译文>（在人工长度预算内改刀）  ← `shrink_draft` 被整段删除
```

### 7.3 为什么这比"漏译"更值得先修

| 维度 | RC-3（围栏漏译） | RC-5（标记剥除） |
|---|---|---|
| 后果 | 该翻的没翻，**一眼看得出**是中文 | 标记消失 + **内容被删**，**看不出**（除非逐字比对） |
| 可恢复性 | 原文行还在产物里（整块是源语言） | 被删的标识符**在产物里根本不存在** |
| 违反的口径 | R1（围栏内容要翻） | **R2（保留原文原格式）** |
| 影响面 | 10 块 / 72 行 | **381 处加粗 + 34 处反引号**，遍布全文 |

### 7.4 修复要点

1. **不要"剥掉标记"，要"记录骨架"**：把行内标记当作与 `pfx` 同级的**骨架节点**记录，
   回写时**只替换文本节点**、标记原位粘回。这与 RC-3 的"只替换注释段"是**同一套机制**——
   **建议两件事一次做掉**（否则要动两遍同一条写回链）。
2. 替换串至少改为**按组回填**（`$1`/`$2`… 各配一个包装），先止住"整段删除"这条血。
3. **反引号内的内容必须送模型**（`shrink_draft`、`{xxx}`、文件名都是文档内容，不是标记）；
   若担心技术标识符被误翻，应像 RC-3 一样**标记为"不翻"但仍原样保留**，而不是删掉。
4. 补断言（每条都要能复现）：
   - `**加粗**` / `*斜体*` / 反引号代码 / `~~删除~~` 四类，写回后**标记与内容逐字节不变**；
   - 含标记行的**译文替换后标记仍在原位**；
   - 真实文档回归口径：`strings.Count(out, "**") == strings.Count(src, "**")`。

---

## 8. RC-6【缺口 · 低优先】目录锚点不同步 ⇒ 译文的目录点不动

实测 15 行目录项（`产品方案书_v5.1.md` 第 13–27 行），全部形如：

```text
原| - [一、项目背景与目标](#一项目背景与目标)
产| - [<译文>](#一项目背景与目标)      ← 链接文字译了，锚点没译
```

`ApplyAlignedText:175-177` 对「整行唯一链接」**有意保住 URL**（`singleLinkRe`）——
这对真实 URL 是**正确**的（URL 不该翻）。但**目录锚点 `#中文…` 是随标题文本生成的**：
标题译成英文后，GitHub / VSCode 重新生成的锚点会变成 `#i-project-background`，
而译文里仍是 `#一项目背景与目标` ⇒ **目录点击跳不过去**。

- **性质**：功能缺口（**不是漏译**）。§1 的 R6 统计里那 15 行"残留中文"就是它。
- **建议**：标题与锚点**成对处理**（标题翻译时同步重算 slug）。
- **优先级**：低（不影响内容正确性），但应登记，避免以后被当成"漏译"重复排查。

---

## 9. 为什么工单状态是「已完成」而不是「失败」

`internal/engine/file.go:100-108`：

```go
func leakedLang(lang string, remain, total int) string {
	if remain > 0 && remain*2 > total {   // 只有 >50% 才判失败
		return lang
	}
	return ""
}
```

标题 + 围栏块在文档里通常**远不到 50%**（正文段落才是大头）→ **「大量中文」与「已完成」可以同时成立**，
用户拿不到任何失败或告警提示。这与用户描述完全自洽。

---

## 10. 一步确证（用现成日志，不必重跑翻译）

```bash
# ① KB 命中数：命中高 ⇒ 偏向 RC-2；命中极低 ⇒ 偏向 RC-1
grep -E "\[kb-match\].*命中=" server.log | grep -E "lang=en"
# ② 硬闸收尾：打印的是「键不存在」的段数（不含 KB 坏命中）
grep -E "\[tm-hardgate\].*结束：仍有" server.log
# ③ 是否熔断：出现「连续 2 轮零进展」= 模型稳定回显 ⇒ RC-1
grep -E "\[tm-hardgate\].*零进展|round=" server.log
```

| 观测 | 指向 |
|---|---|
| ① 命中数 ≈ 未翻的标题数，② 报告的未译段**很少** | **RC-2** |
| ① 命中很低，② 未译段数 ≈ 标题数，③ 有「连续 2 轮零进展」 | **RC-1** |
| 未翻的内容**在代码围栏块里** | **RC-3**（无需日志即可确认） |
| 产物文件名是中文原名 | **RC-4**（无需日志即可确认） |
| **产物里所有 `**加粗**` 都变没了、反引号里的 `shrink_draft` 之类消失** | **RC-5**（无需日志，与翻译质量无关） |
| 只有**目录那 15 行**的 `#锚点` 是中文，正文都已译 | **RC-6**（无需日志） |

**旁证（最直接）**：把交付的译文 `.md` 打开，按这五类数一遍——
① 无空格的 `#标题` → RC-1；② 有空格的 `# 标题` 却没翻 → RC-2；③ 围栏块内整块中文 → RC-3；
④ 加粗/反引号标记整体消失 → RC-5；⑤ 只剩目录锚点是中文 → RC-6。

> ★ **注意**：对**本工单**而言 ① 的答案是 **0**（§3.5 已用原件复核），别在 RC-1 上花时间。

**查 KB 脏行**：

```sql
SELECT id, tenant_id, pack_id, zh, updated_at FROM tm_segments
 WHERE en IS NOT NULL AND en <> '' AND en = zh;
SELECT id, tenant_id, package_id, source_text, target_lang, target_text FROM kb_entries
 WHERE source_text = target_text;
```

---

## 11. 修复建议（未实施；「改行为 + 补断言」必须成对）

### RC-1（前缀正则）
1. `mdStructPrefixRe` 的 `[ \t]+` 放宽为 `[ \t]*`（注意约束 `#{1,6}` 不要吞正文）。
2. 回写侧用同一正则取 `pfx`，保证「原本无空格 → 只粘回标记」。
3. 补断言：无空格形态下 ① 键**不含** `#`；② 写回后 `#` **仍在**。

### RC-2（KB 守卫）
1. `file.go:467` 补 `&& v != t`（与模型路径同口径）。
2. 更稳：在 `addTrans` 内统一收口 `if orig == translated { return }`，**并计入 `untranslated`**
   （否则它在统计里"消失"，永远发现不了）。
3. 存量脏数据：跑 §8 两条 SQL → **导出留档**后人工复核清理（勿直接删）。
4. 补断言：桩造「KB 返回源文」→ 键不入 `langTranslations`、段进 `untranslated`。

### RC-3（围栏内：按 §5.5 用户口径 + §5.7.6 实测，修正为**三类**）

> §5.5 是用户裁定口径（代码不译 / 注释译）；§5.7.6 用**真实文档**证明：
> 只按语言标注分派会**漏掉 37 行 ASCII 图中文**，且会把第 9 块**安全词正则一起翻坏**。
> ⇒ 实现按**三类**走，标注缺失时按**内容语义**判定。

1. 提取侧：`inCode` 时**不再无条件 `continue`**，改为三类分派：
   - **真源码 / 伪码**（有代码语言标注，或内容高度疑似代码）→ **仅注释行 / 行尾注释的注释部分**入表；
   - **文本 / 示意图 / Prompt**（无标注、`text`、内容非源码）→ **每行入翻译表**（含 ASCII 图框内文字）；
   - **数据 / 白名单 / 正则**（`json`/`yaml`/…，或内容形如正则/键值白名单）→ **整块不入表**（保留原文），
     并额外提供**显式标记**（` ```regex ` / ` ```nolocalize `）让用户能主动声明"这块别翻"。
   - 同时**记录每行的"围栏语言 + 类别 + 是否注释 + 注释起始列"**，供回写精确定位。
2. 回写侧：按同一政策**只替换被标记的可译部分**；围栏标记行、语言标注、缩进、代码本体**逐字节不变**。
   行尾注释必须**只替换注释段**，保留 `x := 1 ` 前缀。
3. **必改**`d5_writeback_test.go`（`:43-45`）：现断言只表达"go 围栏内裸中文不得被翻"（按新政策恰好仍成立，
   但意图不明）→ 改为显式表达政策，并见 §5.6 的三条新增断言。
4. 新增断言：**无语言标注**围栏内中文 → 必须被翻译；```go 内 `// 中文注释` → 必须被翻译；
   `x := 1` 与 `/* … */` 标记 → 必须逐字节不变。
5. 指令白名单（§5.5-①）需单独一组断言，防 `//go:build`、`# noqa`、shebang 被误翻。

### RC-4（文件名翻译）
1. 新增「文件名翻译」步骤：把 `baseName` 作为**单段**送模型（走 `directOther` 同口径的回显守卫），
   拿到结果做 §6.3 的清理（保留扩展名、去非法字符、截断长度）。
2. 产物命名改用译文名：`{译文名}_{lc}{ext}`；失败则回落原名（不判死）。
3. 同步下载响应的 `Content-Disposition`（否则前端下载名不一致）。
4. 补断言：中文名 → 英文名（含扩展名保留、非法字符清理、失败回落原名三条）。

### RC-5（行内标记：记骨架、替换节点，别删标记）

1. `emphasisRe.ReplaceAllString(t, "$1")` ⇒ 改为**按组回填**（每个组配一个包装串），
   先止住 `*斜体*` / 反引号 / `~~删除~~` 的"整段删除"。
2. 更彻底的解：把行内标记当作与 `pfx` 同级的**骨架节点**，回写时**只替换文本节点**。
   ★ **与 RC-3 共用同一套"提取期记骨架 + 回写期按列替换"机制** —— 建议一次做掉，别动两遍写回链。
3. 反引号内容（`shrink_draft`、`{xxx}`、文件名）**必须送模型**；若判定为不可翻，应"保留不翻"而非删除。
4. 补断言：四类标记「写回后逐字节不变」+「译文替换后标记仍在原位」+ 真实文档 `**` 计数守恒。

### RC-6（目录锚点同步）

1. `.md` 写回时，若命中行是「标题行」或「整行唯一链接且 URL 以 `#` 开头」，
   则按同一批译文的标题**重算锚点 slug**，使目录与标题一致。
2. 补断言：标题译成英文后，目录条目锚点同步为英文 slug（不再是 `#中文`）。

### 通用加固
- 把「回显/同文 = 未译出」抽成**唯一判定函数**，KB / 模型 / 硬闸三处共用，杜绝缘故漏写守卫。
- 漏译率硬闸在「未译段 > 0」时就应在工单话术与审批台**列出未译段清单**（现在只给数量），
  否则用户只能靠肉眼发现残缺交付。
- ★ **格式保真要有独立闸门**：现在所有闸门都只看「中文有没有变少」，**没有任何闸门看"标记有没有丢"**
  —— 这正是 RC-5（381 处加粗消失）能潜伏至今的原因。
  建议在产物落地前加一道**结构指纹比对**：对源/产物分别统计 `**`、反引号、围栏、表格行数、
  代码块行数的计数，**差值不为 0 就打警告并写入工单轨迹**。这条闸门比任何单点断言都便宜且覆盖面大。

---

## 12. 待确认（阻塞「改法」而不阻塞「记录」）

**① 围栏内注释的策略 —— 已裁定（2026-09-21 23:5x）**
「**代码不翻译、注释翻译成对应语言**」，详见 **§5.5**（含注释语法分派表与 7 条边界情况）。
**2026-09-22 用原件实测后修正**（**§5.7.6**）：只按标注分派**不够** ——
实际文档 **0/10 块有标注**，且有 3 块 ASCII 图、1 块安全词正则 ⇒ **改为三类分派**。

**② ASCII 图（`┌──┬──┐`）里中文说明的处置 —— 已给出建议，等确认**
这份文档有 **3 块 ASCII 图 / 37 行中文**。翻 → 列宽对齐被破坏；不翻 → 整图仍是中文。
**建议**：按**整块翻译**执行，框线行与 `████` 条原样保留，接受列宽错位（后续再做"按图重排"能力）。

**③ 安全词正则 / 白名单围栏（第 9 块）怎么保护 —— 需确认**
建议：**整块不翻** + 提供显式标记 ` ```regex ` / ` ```nolocalize `。
**待确认**：能否接受"用户必须显式标注"这个前提？若不能，就得做**启发式识别**
（含 `|`、`\d`、`*`、`+` 等正则元字符，或形如 `A|B|C` 的高密度竖线行 → 判为不可翻）。
启发式有误伤风险，**建议先走显式标注**。

**④ 文件名翻译的粒度 —— 待确认**
- 只翻主名（`产品方案书_v5.1` → `Product Proposal_v5.1`）？
- 还是连版本号/括号里的中文也一并处理（`产品方案书（终版）` → `Product Proposal (Final)`）？

**⑤ RC-5 与 RC-3 是否一次做掉 —— 待确认**
两者都需要"**在提取期记录行内骨架、回写期按位置替换节点**"这同一套机制。
分开做等于把同一条写回链改两遍。**建议合并为一次改动**。

**⑥ 是否现在动手改代码 —— 已裁定：继续只记录，先不改**
本文件保持"只读定位 + 完整修复方案"的形态；**六项**修复方案与断言写法均已就绪，随时可执行。
若要动手，建议顺序（按"收益/风险"排，非按编号）：

| 序 | 项 | 理由 |
|---|---|---|
| 1 | **RC-5** | **381 处标记 + 16 个标识符**在丢失，用户看不出，且是"内容删除"级 |
| 2 | **RC-2** | 2 行改动、收益大、零政策争议（很可能是本工单现象的主因） |
| 3 | **RC-3 + RC-5 的骨架机制合并** | 同一条写回链，合并做；含改 `d5` 测试 |
| 4 | **RC-4** | 独立步骤，零风险 |
| 5 | **RC-6** | 低优先 |
| — | RC-1 | 真实缺陷，但**本工单 0 命中**（§3.5），可最后做 |

---

## 13. 复现与取证命令（本次实际用过，可复核）

### 13.1 往返诊断（不用起服务、不烧 token）

```bash
cd backend-go
# 在 internal/fileproc/ 下建临时 _test.go（本次用过，已删除；按此重建）：
#   ① keys, _ := ExtractTexts(源.md)          → 打印全部「送模型的键」，肉眼即可看出围栏/标记被剥
#   ② tr[k] = "EN0001" 形式（★必须纯 ASCII）  → ApplyAlignedText(".md", src, out, tr)
#   ③ 逐行比对 src/out：含中文的行 = 漏译；统计 ** / 反引号 / 围栏 计数 = 格式保真度
go test -count=1 -run 'TestZZRealDoc' -v ./internal/fileproc/
```

> ★ **假翻译器必须输出纯 ASCII**（如 `EN0001`）。若写成 `"[EN]" + 键`，
> 产物里仍带原中文，"漏译率"会**永远算出 100%** —— 本次第一版就踩了这个坑。

### 13.2 关键代码位置

```bash
cd backend-go
sed -n '93,123p;134,192p' internal/fileproc/text_formats.go  # 提取/回写/前缀正则/围栏跳过
sed -n '21,27p'           internal/fileproc/text_formats.go  # ★ RC-5 的 emphasisRe 与 "$1" 替换
sed -n '31,50p'           internal/fileproc/d5_writeback_test.go  # 把围栏不译断言为正确的测试
sed -n '399,406p;465,472p;483p;512,513p;554,555p;596,601p;609,614p;660,668p;100,108p' internal/engine/file.go
sed -n '705,735p;776,790p' internal/engine/file.go            # 产物命名（文件名未翻译）
```

### 13.3 真实文档围栏清单（§5.7.6 的来源）

```bash
/Users/zhangzifei/.workbuddy/binaries/python/versions/3.13.12/bin/python3 - <<'PY'
import re
src = open("/Users/zhangzifei/Downloads/产品方案书_v5.1.md", encoding="utf-8").read().split("\n")
inf, n = False, 0
for i, l in enumerate(src):
    m = re.match(r'^\s*(`{3,}|~{3,})\s*(\S*)\s*$', l)
    if m and not inf:
        inf, n = True, n + 1
        print("块%d 第%d行 标注=%s" % (n, i + 1, m.group(2) or "（无）"))
    elif m and inf:
        inf = False
PY
```

### 13.4 本次实测结论（可直接引用）

| 结论 | 数据 |
|---|---|
| `# 标题`（有空格）形态的提取→回写 | **完全正确**（`mdStructPrefixRe` 工作正常） |
| `#标题`（无空格）等 6 种形态 | 会把结构标记留在送模型的键里 → 但**本工单命中 0 行**（§3.5） |
| 代码围栏内 | **整块不翻译**（提取侧 + 回写侧双重跳过）；原件 **10 块 / 72 行**中文受影响 |
| 行内标记 | `**` **381 → 0**；反引号内 **16 个技术标识符被整段删除**（RC-5） |
| 目录锚点 | 15 行 `#中文锚点` 未同步（RC-6） |
| 围栏标记 / 表格 / 缩进 / 空行 | 计数守恒 ✅（`│` 151、`┌` 11、`█` 36、围栏标记 20，均无变化） |

---

## 14. 与既有交付物的关系

- 已登记到 `全量UAT实测与架构商业评价_20260921.md` §3.5（并在 §0 与 P0 追加项中引用）。
- **不属于**已记录的 A5（AI 顾问菜单权限口径），也不是 T28 环境假红。
- 性质上与 §4.1-5「**编排装配层零测试**」同源：`internal/engine/file.go`（1,100+ 行主编排）
  与 `internal/fileproc/text_formats.go` 的这条链路**几乎没有回归测试**，
  所以「三条写入路径只有两条有守卫」「围栏内不译被当成正确」「行内标记被整段删除」
  这类问题**没有任何闸门能发现**。

### 14.1 本族缺陷的严重度排序（建议据此排修复顺序）

| 序 | 缺陷 | 性质 | 用户可感知度 |
|---|---|---|---|
| 1 | **RC-5** 行内标记被剥除 | **内容删除**（381 处标记 + 16 个标识符） | **低**（看不出，最危险） |
| 2 | **RC-2** KB 同文命中 | 脏数据当译文写死且永久跳过补漏 | 高（正是用户报的现象） |
| 3 | **RC-3** 围栏整块不译 | 该翻的没翻（72 行 / 17.2%） | 高（"大量中文"主体之一） |
| 4 | **RC-4** 文件名不翻译 | 口径缺口（零实现） | 中（用户已裁定要求） |
| 5 | **RC-6** 目录锚点不同步 | 功能缺口（15 行） | 低（但目录点不动） |
| 6 | **RC-1** 无空格前缀正则 | 真缺陷，双向错（留中文 / 丢 `#`） | **本工单 0 命中** |

### 14.2 一条被本次实测推翻的旧结论

§1 原先把 **R2（保留原文原格式）** 判为「格式 ✅ / 内容受 R1 拖累」。
**2026-09-22 真实文档往返实测证明这是错的**：格式**并没有保住** ——
`**` 381 处全丢、反引号内容 16 段被删。已修正为 ❌ 并升级为独立缺陷 RC-5。
→ 教训：**「格式保真」不能靠读代码判断，必须做源/产物的结构指纹比对**（见 §11 通用加固）。
