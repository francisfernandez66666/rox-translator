# 技术方案 · 全仓端到端评审：P0 缺陷修复与交付收口

> 版本 v1.0 ｜ 2026-08-26 ｜ 性质：**开启强制计费（billing_enforced=1）与对外发布前的第三批整改**
> 来源：全仓端到端评审（后端 Go 3.36 万行 + 前端 Vue/TS 1.22 万行 + 插件/SDK/部署物料 × 15 份存量方案交叉核对），关键指控均经源码二次核实。
> 原则：每项独立可验证、独立可回滚；改完跑 `go build/vet/test ./...` 全绿 + `vue-tsc --noEmit && vite build` 全绿。
> 与既有方案关系：《P0安全止血》《评审整改·余额贯通》《并发优化》三批覆盖之外的**新增缺陷面**。

---

## 〇、结论摘要

| 维度 | 判定 |
|---|---|
| 架构 | 分层清晰、迁移纪律与并发整改成熟；但 store 上帝对象（329 导出符号）、资金状态三处散布、单实例红线无路线图、12 个包零测试 |
| 新发现 P0 安全 | 3 项（跨租户越权写 / SQL 标识符注入 / KB 元数据路径穿越）——全部位于既有方案的验收盲区 |
| 新发现资损/双跑 | 4 项（巡检释放竞态 / 句数镜像丢更新 / OpenAPI 预检口径回潮 / 硬闸×实费账单雪球） |
| 静默功能失效 | ListUsersByRole 列数错位 → 超管通知链路整体瘫痪（该 bug 能存活至今 = 该路径零测试的直接证据） |
| 发布阻断 | Python SDK 对现网契约必失败；插件 broad host_permissions 上架必打回 |

---

## 一、修复项总览

### A 组 · P0 安全（公网可利用，必修）

| # | 问题 | 现场位置 | 后果 |
|---|------|---------|------|
| A1 | **跨租户安全句审核越权**：`SetSafetyPhraseStatus(id)` SQL 仅 `WHERE id=?` 无租户条件；handler 为 requireDeptAdmin | api/admin_kb.go:537-560 + store/kbpackages.go:435-447 | 任一租户的部门管理员遍历自增 ID 改判他租安全句（approved 即生效于该租户文化闸门） |
| A2 | **SQL 标识符注入**：`SaveEntry` 将 `target_lang` 直拼进 tm_segments 列名，值来自请求体 JSON 无白名单 | store/kbpackages.go:315-318、531-533；入口 api/kb.go:104、admin_kb.go:286、bitext.go 兜底 | dept_admin+ 可注入非法标识符破坏语句结构（对比 kb/db.go SaveBack 用 AllLangs 固定表为正确范式） |
| A3 | **KB 导入元信息路径穿越**：导入阶段 `temp_id` 未校验格式 → `metaPath=kb_<tempID>.json` 可指任意路径；其 JSON 内 `FilePath` 又控制后续 `ParseKBFile` 打开任意文件且 defer 中 `os.Remove(savePath)` | api/kb.go:469-489 | 读 oracle + 任意文件删除原语（服务以 root 运行） |
| A4 | **API Key 非密码学随机**：明文 Key = `"rk_"+randSuffix(24)`，randSuffix 为 UnixNano 种子 LCG | store/apikeys.go:44（randSuffix 定义 billing.go:876） | 安全凭证用伪随机；订单号/发票号用它无害，Key 必须换 crypto/rand |
| A5 | **数据竞态 panic 面**：`wordBoundaryCache` 裸 map 并发读写（chat/SSE 并发触发 ParseTargetLangs） | engine/lang.go:115-126 | 生产即 `concurrent map writes` 崩溃（metrics.go 同类前科已修过一次） |

### B 组 · P1 资损 / 双跑 / 口径回潮

| # | 问题 | 现场位置 | 后果 |
|---|------|---------|------|
| B1 | **A1(旧) 口径回潮**：OpenAPI 文本/文件任务余额预检仍用旧单桶 `GetBalance().Balance<=0` | api/api_openapi_tasks.go:181-186、254-259 | 仅有体验台账的新租户过得了 gateUsage 却死于这道重复预检——正是《评审整改》A1 要消灭的口径分裂 |
| B2 | **巡检释放竞态（双跑双扣费）**：RequeueStalledTickets 第二段兜底 UPDATE 对「工单已 queued/cancelled」的 running 租约**无条件清空、不看租约年龄**；而入队顺序是「置 queued→建 job→worker 认领→才翻 in_progress」，认领窗口内 sweep 触发即释放活跃租约 → 第二 worker 再领同一 job 双副本执行 | store/tickets.go:309-313（对照 queue/direct.go:57-63 租约回收机制） | 双份 LLM 消耗+计费；取消路径同理放大 |
| B3 | **句数镜像读改写丢更新**：SetSentenceBalance/Add/Deduct 及 GrantPackageSentences/ApplyPaidPackageIdentity/ApplyIncrementMirror 全部「SELECT permissions→内存改→整体覆盖」，无事务无条件递增 | store/packages.go:174-302 | 并发发放/扣减互相踩；句数镜像与 token 双通道下超额翻译 |
| B4 | **legacy Deduct 仍有 check-then-act**：先 SELECT balance 再无条件 UPDATE；注释「单机 SQLite 可接受」不成立。P0-4 只修了 DeductWithGrants | store/billing.go:151-167 | 并发 Deduct 可双双通过检查扣成负余额 |
| B5 | **硬闸重试无上限 × 实费计费 = 账单雪球**：复核补漏 `for attempt:=1;;attempt++` 仅受 25min job 超时约束；模型持续回显段落反复全额烧 token，chargeTokens 照单全收 | engine/file.go:279-303 | 客户资损 + 供应商成本爆炸（特性语义保留，但必须加预算护栏） |
| B6 | **文件管线绕过统一网关**：BatchTranslate 直接用 `cfg.OnlineAPIBase/OnlineModel/HunyuanFallbackModel`，不吃 model_routes 权重路由/降级链也不吃 stage_models | engine/engine.go:1299-1339 | BYOK 移除宣称的「平台统一调度与定价权」对文件工单（主力负载）不成立 |
| B7 | **usageRecord 无锁并发写**：translateLangsConcurrent 3 路 goroutine 各自 singleLang 成功路径调 NoteUsageModel 写同一 rec（string 字段并发写） | engine/engine.go:131-138 + 640-686 | -race 必报；计量字段可能撕裂 |

### C 组 · P1 功能静默失效

| # | 问题 | 现场位置 | 后果 |
|---|------|---------|------|
| C1 | **ListUsersByRole SELECT 13 列 Scan 14 目标**（缺 deactivate_at），每行 Scan 必败恒返回空 | store/users.go:136-155 | 超管反馈回复/告警触达的站内信+邮件通知链路整体瘫痪 |
| C2 | **QPS/并发上限不持久化**：quota save 只写内存 Quota，重启回默认 10/3 | api/billing_api.go:299-343 | 配额静默漂移 |
| C3 | **审计 diff 失真**：handleBillingConfigSave 的 before 值在 SetConfig **之后**读取，before==after 恒成立 | api/billing_api.go:268-278 | 强制计费开关审计不可信 |
| C4 | **clientIP 不解析 XFF**：Caddy 反代后全体用户共享 127.0.0.1，一人爆破登录/注册全站连坐冷却 | api/ratelimit.go:97-107 | 反代生产环境可用性缺陷 |
| C5 | **GDPR 擦除清单缺口**：EraseTenantData 漏 referral_rewards（含邮箱快照）/tm_review/tm_hit_count/feedbacks/notifications/eval_records/ticket_files/ticket_state/output_artifacts/quota_grants/balance_accounts/jobs | store/gdpr.go:82-98 | 「彻底清除」名不副实；与 privacy 页对外承诺冲突 |

### D 组 · 发布阻断（SDK / 前端 / 部署物料）

| # | 问题 | 现场位置 |
|---|------|---------|
| D1 | **Python SDK 整体不可用**：create/wait 强制检查 `r["success"]`，而后端任务接口成功响应无该字段 → 必抛异常；failed 出参读 `r.error` 但后端给 `error_code/message` | sdk/python/translator_sdk.py:108,154,174；对照 JS 版判 `=== false` 所以能跑（从未做过集成测试的铁证） |
| D2 | **前端审批台崩溃**：admin/Tickets.vue v-for 别名 `t` 遮蔽 i18n 函数 t，区块渲染即 TypeError | frontend/src/components/admin/Tickets.vue:151-162 |
| D3 | **roleLevel 双实现互相矛盾**：Login.vue 三级（dept_admin=1）vs admin/store.ts 四级（=2）→ 部门管理员登录页被拒、刷新恢复却能进 | frontend/src/components/Login.vue:312-316 vs components/admin/store.ts:20-25 |
| D4 | 前端杂项：core.ts removeEventListener 传新箭头函数永远移除不掉；PlansPanel max_daily_tokens undefined→NaN→null、style 标签连开两次；Audit.vue CSV 导出裸 `<a>` 无法带鉴权头必 401 | core.ts:83、PlansPanel.vue:360,433、Audit.vue:89-95 |
| D5 | systemd prod.conf 无任何沙箱指令（root 裸跑）；注释要求填的三把密钥占位符不在模板里；密钥 Environment= world-readable | deploy/systemd/prod.conf |
| D6 | Caddy 无 HSTS/XFO/nosniff/Referrer-Policy；X-Admin-Token 占位硬编码在 git 内 conf 且 chmod 644 指引等于公开回调凭证 | deploy/caddy/*.conf |
| D7 | .gitignore `/*.md` 把全部 15 份交付文档挡在版本库外；node_modules/.DS_Store 已有 1273 个文件被跟踪（ignore 对已跟踪文件无效） | .gitignore:25 |

### 特性确认（非缺陷）

| 项 | 结论 |
|---|---|
| 硬闸「直到全部译出或超时」的产品语义 | ✅ 维持（《评审整改》§〇 已定稿）。B5 只加**成本预算护栏**（墙钟上限 env 可配），不改语义本体 |
| randSuffix 用于订单号/发票号/工单号 | ✅ 保留（可枚举性无安全影响），仅 API Key 明文生成换 crypto/rand |
| 单实例红线九处进程态 | 本批不成文改造，仅把 clientIP XFF 开关做成 env 门控（TRUST_PROXY_XFF），其余维持成文红线 |

---

## 二、逐项设计

### A1 安全句租户隔离
`SetSafetyPhraseStatus(id, status)` → `SetSafetyPhraseStatus(id, tid, status)`，SQL 加 `AND tenant_id=?`；
handler 传 `s.kbTenant(r, u)`（与同面板其他操作一致）。影响行数 0 时报「记录不存在」（不泄露他租存在性）。

### A2 target_lang 白名单
store.SaveEntry 入口校验：`tgtLang` 必须 ∈ `kb.AllLangs`（store 已 import kb，无环），否则返回错误；
kb_entries 写入参数化不受影响，tm_segments 列名拼接自此只接受白名单值。三个入口（kb.go/admin_kb.go/bitext.go）无需改动（错误自然上抛为 200+success=false）。

### A3 temp_id 格式校验 + FilePath 白名单双重闸
导入阶段：
① `tempIDRe := ^[0-9a-f]{24}$`（识别侧 randHex(12)=24 hex），不符直接 400；
② meta.FilePath 经 `resolveSafePath([]string{s.Cfg.UploadDir}, p)` 校验必须落在上传目录内（复用 stream.go 既有函数），否则 400。
识别阶段写入侧不变。

### A4 API Key 密码学随机
apikeys.go 新增 `randToken(n int)`（crypto/rand → hex），`plain := "rk_" + randToken(20)`（40 hex 字符 ≈160bit）；
key_prefix 展示逻辑不变（plain[:10]）。Rotate 路径复用 CreateAPIKey 则自动受益（实施时核对 rotate 是否独立生成点）。

### A5 wordBoundaryCache 加锁
`var wbMu sync.Mutex` 包住查+写（临界区纳秒级，无性能顾虑）。

### B1 OpenAPI 预检双桶化
两处 `GetBalance(tid).Balance<=0` 改为 `TenantRemainTotal(tid)` 合计 ≤0 判断（文案不变）。

### B2 巡检第二段删除
删除 RequeueStalledTickets 的兜底释放 UPDATE，改为注释说明职责边界：
- 活跃 worker 保护由第一段 NOT EXISTS(running) 承担；
- 过期租约由 DirectQueue.Reserve 的 `leased_at<=?` 回收（唯一合法回收点）；
- 进程重启的全量释放已由 BootResume 显式执行。
取消场景的重复执行防护由 runTicket 收尾守卫承担（现状已有）。

### B3 句数镜像原子化
- Add/DeductSentences 改单条 SQL 原子语句：`UPDATE tenants SET permissions=json_set(COALESCE(permissions,'{}'), '$.sentence_balance', COALESCE(json_extract(permissions,'$.sentence_balance'),0) ? ?), updated_at=? WHERE id=?`（Deduct 另加 `AND COALESCE(json_extract(...),0)>=?` 守卫，RowsAffected==0 → ErrSentenceExhausted）。执行后回读返回新余额。
- GrantPackageSentences / ApplyPaidPackageIdentity / ApplyIncrementMirror / SetSentenceBalance：整函数包 `s.db.Begin()`（DSN _txlock=immediate ⇒ BEGIN IMMEDIATE，单写者库内即互斥），事务内完成读-改-写。
- modernc.org/sqlite 含 JSON1 扩展；若运行环境验证不支持则回退方案（tx 包裹读改写）仍成立——实施时以测试为准。

### B4 legacy Deduct 守卫化
改为单条条件更新 `... SET balance=balance-? WHERE tenant_id=? AND balance>=?`，RowsAffected==0 → ErrInsufficientBalance；标记 Deprecated 注释指向 DeductWithGrants。

### B5 硬闸成本预算护栏
file.go 补漏循环增加：
- 墙钟预算：`hardGateBudget = env FILE_HARDGATE_MAX_SEC（默认 600）`；超时 break 并 `[tm-hardgate] budget exhausted` 日志；
- 零进展熔断：连续 2 轮 missing 数量不减 → break（防模型稳定回显时的无限烧钱）。
产品语义不变（预算内仍尽力译出全部段落；ctx 取消保护保留）。

### B6 BatchTranslate 接入统一网关
BatchTranslate 启动时解析一次：`base,key,model = resolveModel(ctx)`；`resolveStageModel(ctx, StageAIInitial)` 命中则覆盖；
runChunk 与其 429 兜底改用解析结果（fallback 模型仍 HunyuanFallbackModel 但 base/key 用解析值）；
成功路径补 `e.NoteUsageModel(ctx, usageProvider(base), model)`（依赖 B7 加锁）。
末段逐条兜底走 e.singleLang 不动（天然全路由）。

### B7 usageRecord 加锁
usageRecord 增加 `mu sync.Mutex`；NoteUsageModel/UsageModel 内加锁读写。

### C1 ListUsersByRole 补列
SELECT 追加 `COALESCE(deactivate_at,'')`（对齐 iam userCols 口径）。

### C2 QPS/并发持久化
system_config 新键 `tenant_quota_<tid>`（JSON {qps,concurrent}）；
save 接口在写内存后落库；NewServer 在 Bill 初始化后 `LoadTenantQuotas()` 全量回放。

### C3 审计 before 先读后写
handleBillingConfigSave 把 GetConfig("billing_enforced") 提前到 SetConfig 之前。

### C4 clientIP XFF 门控
`TRUST_PROXY_XFF=1` 时取 `X-Forwarded-For` 第一跳；默认关闭保持现行为（防直连伪造）；
deploy/systemd/prod.conf 与部署指南注明反代部署需开启。

### C5 GDPR 擦除清单补全
EraseTenantData 追加 12 表 DELETE（referral_rewards/tm_review/tm_hit_count/feedbacks/notifications/eval_records/ticket_files/ticket_state/output_artifacts/quota_grants/balance_accounts/jobs），
并追加磁盘产物清理（遍历 tickets.result_path/file_path + ticket_files 两列 os.Remove best-effort）。

### D1 SDK 契约对齐
Python：create/get 不再要求 success 字段（HTTP 状态码为准）；错误消息读 `message`/`error_code`；
JS：轮询失败消息同样改 `message ?? error`；README 同步契约说明。补最小集成冒烟说明（curl 对照）。

### D2~D4 前端四连
Tickets.vue 循环变量改名 `tk`；roleLevel 统一引用 admin/store.ts 版本（Login.vue 删除本地实现）；
core.ts 保存 abort handler 引用再移除；PlansPanel loadQuota 补 max_daily_tokens 默认 0、合并重复 style 标签；
Audit.vue CSV 导出改 fetch+blob 带 Authorization。

### D5 systemd 加固
prod.conf 重写：User=translator（文档注明建号步骤）、NoNewPrivileges/ProtectSystem=full/ProtectHome/PrivateTmp/MemoryMax；
密钥改 `EnvironmentFile=/etc/translator/secrets.env (0600)`；模板含 REQUIRE_PROD_SECRETS 所需三变量占位符与 TRUST_PROXY_XFF。

### D6 Caddy 安全基线
两个站点块加 header 指令（HSTS/X-Content-Type-Options/X-Frame-Options DENY/Referrer-Policy/Permissions-Policy）；
支付回调凭证改 Caddy 环境变量引用 `{$TRANSLATOR_ADMIN_TOKEN}`，conf 文件本身不含真实值；chmod 指引同步修正。

### D7 .gitignore 修正
删除 `/*.md`；追加 `*.log`、`.env`；注明 node_modules/.DS_Store 需一次性 `git rm -r --cached` 清理（本方案只改规则不动索引，清理动作留给运维窗口）。

---

## 三、验收清单

1. `go build/vet/test ./...` 全绿（A2/A5/B3/B7 附新用例或既有用例回归）。
2. A1：他租 dept_admin 对任意 id 调 safety-phrases/status → 「记录不存在」；本租正常流转。
3. A2：target_lang=`en'); DROP TABLE--` 类载荷 → 400/失败，tm_segments 无损。
4. A3：temp_id=`../../x` 或 FilePath 越界 → 400。
5. B1：构造「台账 300000/永久 0」租户，billing_enforced=1 下 OpenAPI 创建文本/文件任务均 202（不再 insufficient_balance）。
6. B2：模拟「job running + ticket queued」中间态触发 sweep → 租约不被释放；日志无双副本审计。
7. B4/B3：并发 20 goroutine Deduct/DeductSentences 总量守恒、无负值（沿用并发测试模式各加一例）。
8. B5：mock 恒回显模型注入下，补漏循环在预算/零进展护栏内退出，不再吃满 25min。
9. B6：配置 model_routes 指向 mock 端点 → BatchTranslate 请求打到路由而非全局默认（httptest 断言）。
10. D1：Python SDK translate_and_wait 对本地 mock 后端全链路通过。
11. D2/D3：审批台面板渲染无 TypeError；dept_admin 登录页可进 /admin。
12. `vue-tsc --noEmit && vite build` 全绿。

## 四、实施顺序

| 序 | Commit | 内容 |
|---|--------|------|
| 1 | `p0-authz-and-injection` | A1+A2+A3+A4+A5 |
| 2 | `money-and-sweep-correctness` | B1+B2+B3+B4+B7+C3 |
| 3 | `engine-gateway-and-hardgate` | B5+B6 |
| 4 | `silent-failures-batch` | C1+C2+C4+C5 |
| 5 | `sdk-contract-fix` | D1 |
| 6 | `frontend-p0-batch` | D2+D3+D4 |
| 7 | `deploy-hardening` | D5+D6+D7 |

## 五、实施记录（2026-08-26）

| 项 | 状态 | 落点 |
|---|---|---|
| A1 安全句租户隔离 | ✅ | store.SetSafetyPhraseStatus 加 tid 参数与 `AND tenant_id=?` 条件（kbpackages.go）；handler 传 kbTenant(r,u)（admin_kb.go:551） |
| A2 target_lang 白名单 | ✅ | 新增 isValidLangColumn(kb.AllLangs 白名单)；SaveEntry 入口拒绝非法语言码；SetKBPackageEnabled 重写回路对历史脏数据跳过（纵深防御） |
| A3 temp_id 双重闸 | ✅ | api/kb.go：tempIDRe(`^[0-9a-f]{24}$`) 格式校验 + meta.FilePath 经 resolveSafePath(UploadDir) 白名单 |
| A4 API Key 密码学随机 | ✅ | store/apikeys.go 新增 randToken(crypto/rand,160bit)，CreateAPIKey 换源；两条 rotate 路径复用 CreateAPIKey 自动受益 |
| A5 wordBoundaryCache 加锁 | ✅ | engine/lang.go：wordBoundaryMu 保护读改写 |
| B1 OpenAPI 预检双桶化 | ✅ | api_openapi_tasks.go 两处 GetBalance 预检改 TenantRemainTotal 合计判断 |
| B2 巡检释放竞态删除 | ✅ | RequeueStalledTickets 删除第二段无条件释放 UPDATE，注释固化四条职责边界（NOT EXISTS/Reserve 租约回收/BootResume/收尾守卫） |
| B3 句数镜像原子化 | ✅ | Add/DeductSentences 改 json_set 单语句原子自增自减+守卫；GrantPackageSentences/ApplyPaidPackageIdentity/SetSentenceBalance 包 BEGIN IMMEDIATE 事务（token 入账同事务含 EnsureBalance 等价语句）；ApplyIncrementMirror 单语句化 |
| B4 legacy Deduct 守卫 | ✅ | 改单条 `balance>=?` 条件更新 + RowsAffected 判定，标记 Deprecated |
| B5 硬闸成本护栏 | ✅ | file.go 补漏循环加墙钟预算（FILE_HARDGATE_MAX_SEC 默认 600s）+ 连续 2 轮零进展熔断；产品语义不变 |
| B6 BatchTranslate 统一网关 | ✅ | 批量主路改 resolveModel→stage_models(ai_initial) 解析，429 兜底沿用解析凭据，成功路径 NoteUsageModel 记账——文件管线不再绕过 model_routes |
| B7 usageRecord 加锁 | ✅ | engine.go：usageRecord.mu 保护 NoteUsageModel/UsageModel 并发读写 |
| C1 ListUsersByRole 补列 | ✅ | SELECT 追加 COALESCE(deactivate_at,'') 对齐 userCols 14 列契约 |
| C2 配额持久化 | ✅ | system_config 新键 tenant_quota_<tid>（SaveTenantQuotaCfg/LoadTenantQuotaConfigs）；save 接口落库、NewServer 启动回放 |
| C3 审计 before 先读 | ✅ | handleBillingConfigSave 的 before 值读取移至 SetConfig 之前 |
| C4 clientIP XFF 门控 | ✅ | TRUST_PROXY_XFF=1 时取 X-Forwarded-For 第一跳（默认关闭防伪造），prod.conf 已同步开启 |
| C5 GDPR 擦除补全 | ✅ | EraseTenantData 补 12 表（referral_rewards/tm_review/tm_hit_count/feedbacks/notifications/eval_records/ticket_files/ticket_state/output_artifacts/quota_grants/balance_accounts/jobs）；新增 EraseTenantDataFull（先收集工单文件→删表→删文件），API 层已切换 |
| D1 SDK 契约修复 | ✅ | Python：create/get 以 task_id/status 存在为准、失败消息读 message/error_code、默认轮询间隔按 type 15s/60s、文档删除 poll_interval_sec/balance_tokens 虚构字段；JS：createFileTask 同口径、waitTask 失败消息与间隔对齐。py ast + node import 双验证通过 |
| D2 审批台 t 遮蔽 | ✅ | Tickets.vue 循环变量改名 tk，i18n 函数恢复可见 |
| D3 roleLevel 归一 | ✅ | Login.vue 删除本地三级实现，统一引用 admin/store 四级版（dept_admin=2 可正常登录后台） |
| D4 前端杂项 | ✅ | core.ts abort handler 引用保存后移除；PlansPanel loadQuota 补 max_daily_tokens ?? 0、删除重复 `<style scoped>` 开标签；Audit.vue CSV 导出改 fetch+blob 带 Authorization |
| D5 systemd 加固 | ✅ | prod.conf 重写：User=translator 沙箱九件套（NoNewPrivileges/ProtectSystem=full/PrivateTmp/MemoryMax 等）、密钥改 EnvironmentFile=/etc/translator/secrets.env(0600)、模板占位符与 REQUIRE_PROD_SECRETS 三变量对齐、TRUST_PROXY_XFF=1 |
| D6 Caddy 安全基线 | ✅ | translator.conf 增加 HSTS/nosniff/XFO DENY/Referrer-Policy/Permissions-Policy/-Server 头块；支付回调凭证改 {$TRANSLATOR_ADMIN_TOKEN} 环境变量引用（/etc/default/caddy 0640），conf 与 git 内零真实凭证 |
| D7 .gitignore 修正 | ✅ | 删除 `/*.md`（15 份交付文档回归版本库）；追加 secrets.env/*.log；注明 node_modules/.DS_Store 需一次性 git rm --cached 清理 |
| 回归测试 | ✅ | 新增 review_regression_test.go 四用例：并发 Deduct 守卫（33/7 守恒）、句数镜像并发（25/25 守恒）、A2 注入载荷拦截、C1 通知链路复活 |
| 终验 | ✅ | go build/vet/test ./... 全绿；go test -race（engine/store/api）全绿；vue-tsc --noEmit 零错误；vite build 成功 |

### 遗留提醒（本批不含，运维窗口执行）

1. `git rm -r --cached frontend/node_modules` + `.DS_Store` 清理并强推（1273 个已跟踪垃圾文件）；
2. 生产按新 prod.conf 步骤建 translator 运行账号与 /etc/translator/secrets.env，并在 /etc/default/caddy 配置 TRANSLATOR_ADMIN_TOKEN 后 reload；
3. 服务器开启 `TRUST_PROXY_XFF=1`（反代部署前提）；
4. SDK 发 PyPI/npm 前补一次对 mock 后端的端到端冒烟（本次为静态契约对齐）。

## 六、UAT 实测记录（2026-08-26，黑盒全流程）

> 环境：全新临时库 + 占位 LLM Key（验证优雅失败分支），156 断言通过；
> 过程中所有 FAIL 逐条复核归类为：脚本引号/断言写法噪声、以及下述 4 个**真缺陷（当场修复并回归）**。

### UAT 发现并修复的缺陷

| # | 缺陷 | 根因 | 修复 |
|---|---|---|---|
| UAT-1 | 强制计费余额拒绝返回 `error_code=rejected`，违反 OpenAPI insufficient_balance 契约 | gateErrorCode 未覆盖「token 已耗尽…充值」文案 | 补「耗尽/充值」映射（api_openapi_tasks.go） |
| UAT-2 | refund_revoke 告警从未落库 | CreateAlert 在 IMMEDIATE 事务持锁期间经独立连接写入，busy_timeout 后被吞 | 移至 tx.Commit() 之后（store/billing.go） |
| UAT-3 | 二次退款/pending 单退款返回裸 `sql: no rows in result set` | ErrNoRows 未映射友好文案 | 「订单不存在或状态不允许退款」 |
| UAT-4 | 取消与 worker 认领竞态：cancelled 被 in_progress 回写覆盖，且执行不中止继续烧 token | 无状态联动取消机制 | 认领前查态防覆盖 + runTicket 内 3s 取消监视器联动 ctx + 错误分支保持 cancelled 态 |

### UAT 期间落地的新需求

- **邀请好友前台记录**：ListReferrals 补 invitee_email 快照与 paid 标记（同被邀人存在 paid_perm 行即 true）；Referral.vue 记录表新增「被邀邮箱 / 邀请状态✅ / 是否已付费⏳✅」列；i18n 中英补齐。验收：records 含 uid/邮箱/+30万体验/+14天/+50万永久/paid=true 全字段。

### 场景覆盖清单（全部通过）

认证注册（含 oneid 邮箱唯一/弱密码/行业兜底三分支/IP 防刷）、双桶出参四接口一致、强制计费三态（台账放行/归零拒/审核发放后放行）、mock 充值链（B1 金额回填/B2 流水实收/幂等/发票非0元）、回调三道闸+渠道一致性矩阵(manual×4渠道)+mock 生产封禁、static_qr 我已付费→超管核实开通、15min 关单+manual 豁免（布种收割实测）、套餐 free/paid/increment 三分流落账（台账 t+29天/句数镜像 100+5+10+50=165 对账吻合）、退款两分支（plan 作废+身份清空 / 永久桶精确扣回25000）+幂等、邀请裂变（叠加发放/首笔 paid 一次性永久奖/再购不触发/换绑撞库闸）、站内工单（创建/执行/通知/失败不计费/取消）、KB 注入拦截 live、跨租户安全句越权 live、temp_id 双闸 live、download 白名单 live、重置码 5 错锁定 live、配额持久化 live。

### 遗留观察（不构成本批缺陷，报备待议）

- **O1 文本管线空译文完成态**：LLM 全部失败时文本工单仍标记 completed（译文空、QA 报 error、不计费）。文件管线已有硬闸，文本管线建议二期对齐「零产出→rejected」。本次未改：涉及产品语义变更需拍板。

---

*本文与《P0安全止血与并发原子性修复方案.md》《评审整改·余额贯通与商业化收口方案.md》并列构成收费前三批整改；任何规则变更先修订本文再动码。*
