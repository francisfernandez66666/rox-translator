# P0/P1/P2 问题核实与修改方案（2026-09-18）

> 本文是《端到端全量审计报告（2026-09-18）》所列全部 P0/P1/P2 问题的**逐条真实代码核实记录**。
> 核实方式：只读代码（grep/sed 到行）+ 实跑闸门，未改任何业务代码。
> 结论口径：**证实** = 代码证据链完整、问题真实存在；**部分证实** = 现象存在但原报告表述有偏差；**证伪** = 与代码不符。
> 所有行号以 2026-09-18 工作区（branch `autosales`，含未提交改动）为准。

---

## 〇、结论速览

| 级别 | 条目 | 判定 |
|---|---|---|
| P0-1 | AI 助手会话历史无鉴权 + sid 可预测 | ✅ 证实（比原报告更严重：sid 非加密随机） |
| P0-2 | kb-stats 匿名访问落到租户 1 | ✅ 证实 |
| P0-3 | 安全响应头：Go 侧为零；Caddy CSP 含 unsafe-inline | ✅ 证实（Caddy 已有 nosniff/DENY/frame-ancestors，弱点在 unsafe-inline 与裸端口） |
| P0-4 | 退化译文仅 log 观测，无指标/告警落点 | ✅ 证实 |
| P0-5 | 质量闸门可被模式静默旁路 | ✅ 证实（属设计内行为，缺「谁在用/用了多少」的观测与租户侧披露） |
| P0-6 | CI 静默跳过 Python 自检 | ✅ 证实 |
| P0-7 | pdf_overlay 零命中仍判成功 | ✅ 证实（代码注释自认，Go 丢弃 stdout） |
| P1-1 | 生产实际 0 个可用支付渠道 | ✅ 证实（4 适配器已写、默认 mock，商户参数未配） |
| P1-2 | 发票为人工流程 | ✅ 证实（有 CRUD 接口，无税局/开票服务商对接） |
| P1-3 | 营销面无线索捕获 | ✅ 证实（全仓无 lead/留资端点） |
| P1-4 | distlock：Redis 故障时巡检静默停摆 | ✅ 证实（原报告「降级」表述不准：报错侧是全员跳过，非降级本进程） |
| P1-5 | engine.go 2 处裸 SQL 绕过方言改写，PG 下静默失效 | ✅ 证实（**建议升为 P0 处理**，详见正文） |
| P2-1 | api→store 层次耦合（855 调用点） | ✅ 证实 |
| P2-2 | db/migrate.go Runner 生产零调用 | ✅ 证实（仅测试引用） |
| P2-3 | 日志双轨：log.Printf 181 处 vs observability 8 处 | ✅ 证实（原报告 247/9 为全仓口径，本文给复测口径） |
| P2-4 | 前端 UI 迁移残留（7/53 组件仍 TDesign）+ 本次 e2e 5 红灯 | ✅ 证实 |
| P2-5 | a11y：axe 实测 /billing 有 critical（select-name） | ✅ 证实 |
| P2-6 | /pricing 双实现（Go 服务端页 + SPA 路由） | ✅ 证实 |
| P2-7 | 告警规则仅 5 条 | ✅ 证实（alerts.yml 实测 5 条，「5+1」中另 1 条未找见） |
| P2-8 | assist 子服务存储层仅 SQLite | ✅ 证实（代码注释自知 PG 下 ReadMainDBConfig 不可用，靠 env 兜底） |

另：原报告 1 条**部分证伪**——「assist_uat B1 弱断言」：该用例实际还断言了 `'"source":"'` 并含兜底守卫（api_uat 系列同类），并非只断言 HTTP 200。不再列入待修。

---

## 一、P0 逐条核实与修改方案

### P0-1 AI 助手会话历史无鉴权 + sid 可预测 —— ✅ 证实（维持 P0）

**证据**
- `backend-go/internal/assist/api/server.go:229-250`：`handleHistory` 只取 `?session=` 查询参数即 `s.db.History(sid, limit)` 返回全部消息，无任何鉴权/签名/归属校验；`server.go:43` 路由注册于 C 端公开组。
- `server.go:117-119`：`newSessionID() = "s" + UnixMilli + randHex(4)`；`server.go:526-535` 的 `randHex` 是 **LCG（线性同余）**，种子 = `time.Now().UnixNano() ^ pid<<20`——非 `crypto/rand`。持有任一 sid 即可推出同种序列；结合毫秒时间戳可枚举窗口内会话。原报告「可枚举」成立且偏保守（实为**可预测**）。

**修改方案**
1. `handleHistory` 增加归属校验：响应 `chat` 时下发 `HttpOnly` 的 `assist_sid` Cookie（或 HMAC 签名 sid），history 只接受与之一致的请求；开放 SDK 场景改为 `session + 一次性 token`。
2. `newSessionID` 换 `crypto/rand` 16 字节 hex（保持 `s` 前缀兼容）。
3. 断言：`assist/api/server_test.go` 补一条「A 会话 Cookie 不得读 B 历史」用例 + `randHex` 输出统计唯一性单测。

### P0-2 kb-stats 匿名可访问，落到租户 1 数据 —— ✅ 证实（维持 P0）

**证据**
- `backend-go/internal/api/server.go:232` 路由在 `routesTranslate`（无中间件鉴权——`Handler()` 链仅 version/traceID/metrics/tenant/CORS/bodyLimit/accessLog，`server.go:502-503`）。
- `server.go:591-625` `withTenant`：未登录且无 API Key → **强制 tenant 1（rox）**（`:620-622` 注释自认）。
- `server.go:792-808` `handleKBStats`：直接 `s.currentTenant(r)` → `Stats(tid)` 输出 total_tm_entries / per-lang / segments / tenant_id，且 `server.go:810-815` `currentTenant` 无值时默认 1。
- 对照同组其余端点（estimate/import-kb 等）均在 handler 内自行 `requireLogin`/额度校验，唯 kb-stats 裸奔。

**修改方案**
1. `handleKBStats` 开头补 `u, err := s.requireLogin(r)`（或仅超管可见），错误 401。
2. 中间件「匿名默认租户 1」保留（公开接口需要），但在 `withTenant` 第 4 分支加观测计数 `translator_anon_requests_total{path}`，便于发现下一个同类洞。
3. 断言：`scripts/uat/api_uat.sh` 补一条 `curl 无 Cookie /api/translation/kb-stats → 401` 用例。

### P0-3 安全响应头 —— ✅ 证实（表述修正）

**证据**
- Go 侧：`Handler()` 全链（`server.go:500-503`）**零**安全头设置；任何绕过 Caddy 直连 8891/8892（多实例、内网、事故排障）即裸奔。
- Caddy 侧：`deploy/caddy/translator.conf:21`（nosniff）、`:38`（严格 CSP）、`:39`（assist CSP）、`:41-42`（X-Frame-Options DENY/SAMEORIGIN）。头不缺，但 `script-src` 含 **`'unsafe-inline'`**（:38-39 均有）——任何注入点可直接执行脚本，CSP 对 XSS 基本失效。

**修改方案**
1. Go `Handler()` 链加 `withSecurityHeaders`（nosniff / frame-ancestors 'none' / Referrer-Policy），保证多实例与内网口同标准。
2. CSP 去 unsafe-inline：先清点内联脚本来源（`/office/taskpane.html`、assist 内嵌页、模板 toast），改 nonce 或外置文件；无法外置的页面单独 `@matcher` 放宽，不给全站。
3. 断言：`uat/run_uat.sh` 冒烟段 grep 响应头含 CSP 且无 unsafe-inline（改造完成前可先断言 nosniff+frame-options）。

### P0-4 退化译文观测无落点 —— ✅ 证实（维持 P0，严重度中）

**证据**
- `backend-go/internal/engine/postprocess.go:426-436` `IsSuspectOutput` 注释明示「**仅用于观测与诊断**」；`:462-466` 唯一消费点为 `defer` 里一条 `log.Printf`，**无 metrics、无告警、无落库**；且 `grep IsSuspectOutput internal/` 除 postprocess 外零引用——没有第二个看门人。
- 同文件 `:459-461` 注释自认「本包未接 observability」。

**修改方案**
1. `postprocess.go` 检出点加 `observability` 计数 `translator_suspect_output_total{lang}`（无 ctx 问题：用包级 counter 注册，仿 `withMetrics` 的现有 registry）。
2. `deploy/observability/alertmanager/alerts.yml` 增规则：`increase(translator_suspect_output_total[1h]) > N` → warning。
3. 断言：UAT 里 mock LLM 返回 `"•"×200` 退化样本 → `/metrics` 计数 +1。

### P0-5 质量闸门可被模式静默旁路 —— ✅ 证实（属设计内，缺观测与披露）

**证据**
- `backend-go/internal/orchestrator/flow.go:210-243` `applyModeOverride`：
  - `ticket.Mode == "fast"` → 关 `kb_match/evals_initial/evals_review/gate/culture_gate/feedback`（**:229-233**），即快速模式**无硬闸门、无文化闸**；
  - `ticket.CreatedBy == 0`（API 任务）→ 关 `approval/feedback`（**:236-239**），即开放 API 默认**跳过人工审批**全自动交付；
  - 已批准工单重跑 → 只留 qa/feedback（:211-222，C4 设计意图正确）。
- 旁路全程无落库标记：`ticket_segments`/工单表不记录「本单未经 gate」，运营与租户均不可见。

**修改方案**
1. `Execute` 内当 fast/apiTask 旁路生效时，在工单 `quality_flags`（或幂等补列 `gate_bypassed`）记录旁路种类，审批台与 API 响应可见。
2. 开放 API 文档与定价页明示「API 模式默认无人工审批」；租户级开关 `api_requires_approval`（默认按套餐等级）。
3. 断言：api_uat 补断言——API 工单响应含 `gate_bypassed:"approval"`。

### P0-6 CI 静默跳过 Python 自检 —— ✅ 证实（维持 P0）

**证据**
- `.github/workflows/ci.yml`：后端 job 仅 `go vet` + `go test -race ./internal/... ./cmd/...`；全文件无 `pip`/`pymupdf`/`python-docx` 安装步骤（唯一 python 步骤 :144 是 SDK unittest）。
- `backend-go/internal/fileproc/docx_table_layout_test.go:8-28`：`runPySelftest` 找不到依赖时脚本退出码 2 → `t.Skip`（`pdf_overlay_test.go` 同型）。CI 环境（ubuntu-latest 系统 python3 无 pymupdf/pdf2docx）→ **pdf_overlay / docx_translate / pdf 表布局三套自检恒 skip，绿灯不代表产物正确**。今天是本地 venv 实跑才发现 4 处缩进损坏——正是 CI 抓不到的那类。

**修改方案**
1. ci.yml backend job 增加一步：`pip install pymupdf python-docx pdf2docx lxml && python3 backend-go/internal/fileproc/pdf_overlay.py selftest && python3 .../docx_translate.py selftest`（不进 venv，跑完即走，约 +40s）。
2. 断言：自检失败必须红灯——去掉本地跑「退出码 2=skip」的对称性：CI 步骤里退出码非 0 即 fail。
3. （可选）`go test` 里对 fileproc 增设 `PY_SELFTEST_REQUIRED=1` 时 skip→fail。

### P0-7 pdf_overlay 零命中仍判成功 —— ✅ 证实（维持 P0）

**证据**
- `backend-go/internal/fileproc/pdf_overlay.py:579-580` 头注释自认：「零命中不报错……不能把成功退出当成译文已替换（命中数看 stdout 的 replaced=）」。
- `pdf_overlay.go:62-78` `ApplyTranslatedPdfOverlay` 返回值 `_, err :=` **丢弃 stdout**，注释再次自认「本函数目前丢弃 stdout」。上游（engine 翻译主链）只见 err==nil → 提取键与写回键口径漂移（如 pdftotext 回退）时，交付「未翻译的原样 PDF」并正常扣费。

**修改方案**
1. `ApplyTranslatedPdfOverlay` 解析 stdout JSON 的 `replaced/segments` 计数并返回结构体；`replaced==0 && segments>0` → 返回 error 触发既有「回退旧提取链」分支；`replaced < segments*0.6` → 记 alert（复用 `CreateAlert`）。
2. `ticket_segments` 增记命中数，审批台展示「N/M 段已替换」。
3. 断言：`pdf_overlay_test.go` 补 Go 侧用例——喂全不命中的 translations 必须得到 error 而非 nil。

---

## 二、P1 逐条核实与修改方案

### P1-1 生产实际 0 可用支付渠道 —— ✅ 证实

**证据**：`backend-go/internal/api/pay.go:14`「渠道：mock（默认）/wechat/alipay/static_qr」+ `:116`「默认 mock」；`internal/store/usdt.go:30-45` `usdt_enabled` 默认 0。四套适配器代码在，但商户号/API Key/钱包地址未配置，USDT 走「改造方案_20260915」双轨人工确认。收银台可下单但**无一条真实自动到账路径**。
**方案**：上线前置检查项——`deploy_check.sh` 增「pay_mode≠mock 且渠道参数齐」硬校验；管理台 /api/pay/channels 返回真实可用清单，前端为空时明示「联系销售」而非静默 mock。
**断言**：uat 交易套件补 T 用例：pay_mode=mock 时下单响应必须携带 `"mock":true` 标记，防误上生产。

### P1-2 发票人工流程 —— ✅ 证实
**证据**：`server.go:413-416` 仅 invoices create/void/list CRUD（`admin_billing.go`/`my_billing.go`），无税务服务商/电子开票对接，开具即后台录一条记录。
**方案**：短期在开票接口挂审计+邮件通知（已有 audit 可复用）；中期接诺税/百望类 API，留 `invoice_provider` 配置位。属商业运营项而非缺陷，降档跟随收款自动化批次。

### P1-3 营销面无线索捕获 —— ✅ 证实
**证据**：全仓 grep `api/lead|留资|线索` 仅命中 `Landing.tsx:36,44` 的演示文案，无端点无表单提交路径；/pricing 公开页也无联系方式后端。
**方案**：Landing 尾 CTA 改「留资表单」（公司/邮箱/意向语言），新 `POST /api/lead`（限流+验证码，复用 feedback 通道落库），ReferralP 同款通知。

### P1-4 distlock Redis 故障行为 —— ✅ 证实（原报告「降级为本地锁」表述需修正）
**证据**：`internal/infra/distlock/lock.go:60-62` SetNX 出错返回 `(false,nil,err)`；调用方 `internal/service/ticket.go:470-472` `if err != nil || !got { continue }` → **Redis 故障期间所有实例同轮跳过卡死巡检/订单关闭/低额提醒**（静默停摆而非并发重复执行）；`watchdog.go:423`、`packscraper.go:39` 同型。`redis.Get()==nil`（未配置）才走 localLock（`lock.go:28-33`），单实例语义正确。
**方案**：TryLock 返回 err 时按「保守降级本进程执行 + 打 `translator_distlock_errors_total`」——宁可偶发重复告警也不停摆（三处调用本身幂等）。断言：infra_integration_test 补 Redis 关闭态用例，断言 sweep 仍执行。

### P1-5 engine.go 裸 SQL 绕过方言（建议升 P0 处理）—— ✅ 证实
**证据**：`internal/engine/engine.go:2054` `e.St.DB().Query("SELECT id, pack_type FROM kb_packages")`（无占位符，PG 可跑但**绕过 db 层**）；`:2199` `Query(... sp.lang=? ... pkg.tenant_id IN (?,0))` 用 `?` 占位符——驱动为 **lib/pq**（go.mod:8，占位符 `$1`），`DB()` 是裸 `*sql.DB`（store.go:76），不经 `db.Query` 方言改写 → **PG 生产下该查询必错**，且外层 `if err == nil` 吞错 → 文化闸词库在 PG 上**静默全失效**。全仓同类裸调用共 6 处（engine 2、status 2、billing/migrate 1、service/ticket 1）。UAT PG 矩阵今天全绿恰好证明矩阵未覆盖 culture_gate 断言。
**方案**：1) 两处改 `db.Query(e.St.DB(), db.CurrentDialect(), ...)`；2) 吞错改为记 alert；3) 断言：run_uat PG 方言补一条文化闸命中样例（现有 locale 包 + 埋词），红灯即失效可见；4) 全仓 `DB().Query|DB().Exec` 裸调用清零或用 `go vet` 自定义 lint 拦截。

---

## 三、P2 逐条核实与修改方案

### P2-1 api→store 层次耦合 —— ✅ 证实（量级修正）
**证据**：实测 `internal/api/*.go` 中 `s.Store.` 调用 **855 处**（原报告 860 量级正确）；store 包 77 文件/17,071 行，被 api 37 个文件 import（AGENTS.md:12 与 store/README.md:4 的「74 文件/16.8k 行/90+ 引用」已过时，待文档批次修正）。
**方案**：维持既定「窄域 service 下沉」节奏，不加新债：新 handler 一律经 `internal/service`；季度盘点 855→曲线。文档先行（本条即文档项）。

### P2-2 db/migrate.go Runner 生产零调用 —— ✅ 证实
**证据**：`grep NewRunner` 仅命中 `internal/db/*_test.go`；生产迁移全走 `store.go` 幂等补列/EnsureColumns。Runner 是「影子框架」，双轨迁移机制易误导贡献者。
**方案**：二选一并写入 AGENTS.md：A) 删 Runner，测试改测 EnsureColumns；B) 将 `store.migrate()` 的 DDL 序列改由 Runner 驱动（收益：有版本表）。推荐 A（改动小）。

### P2-3 日志双轨 —— ✅ 证实（口径修正）
**证据**：`internal/` 全域 `log.Printf` **181 处** vs `observability.` **8 处**（原报告 247/9 为含 cmd/子服务全仓口径，方向一致）。AGENTS.md:22 明令统一 slog。
**方案**：按包批量迁移（engine/fileproc/orchestrator 三类占大头），真需要 ctx 的接口一次改签名；PR 闸门 `grep -c log.Printf` 只减不增。

### P2-4 前端迁移残留 + 今日 e2e 5 红灯 —— ✅ 证实
**证据**：7/53 组件仍渲染 TDesign（PlansP/BillingHub 内层等）；本次 `run_uat.sh`（PG）后端 **A/B 67 + T 326 全绿**，前端 Playwright **28 passed / 5 failed**：
1. `admin_tabs_lang T1`：一级菜单实测 8 项 ≠ 断言 9（新菜单 `assist` 项 `minLevel:3`，测试账号权限或渲染分支变化）；
2. `a11y_errors G6`：/billing axe critical `select-name`（TDesign Select 无 aria-label，即遗留面板）；
3. `mobile_uat ×3`：`.lc-shell-burger` 解析到但 computed hidden；/billing 横向溢出。
**方案**：burger 显示断点与 App 渲染条件对齐（本项与 mobile.css 重写相关，优先）；billing 页 Select 补 aria-label（并入 P2-5）；菜单断言改为按当前用户 minLevel 动态期望。修复后 5 项全部纳入 `run_uat.sh` 发布闸门回归。

### P2-5 a11y —— ✅ 证实
**证据**：axe 实跑 /billing critical（select-name）；已修的 langcross Link/aria-label 链路（DataTable.tsx、TicketsPage.tsx、FooterP.tsx）之外，TDesign 残留页是主要违规源。
**方案**：与 P2-4 绑定——迁移收尾的页面逐个跑 axe critical=0；a11y spec 从「关键页」扩到全后台路由清单。

### P2-6 /pricing 双实现 —— ✅ 证实
**证据**：`server.go:180` Go 服务端渲染 `/pricing`（`public.go:22-23`），同时 SPA `App.tsx:273` 亦注册 `/pricing <PricingPage/>`。直连 URL 走 Go 版，站内跳转走 React 版——两份页面文案/价格已可能漂移。
**方案**：Go 侧 `/pricing` 改 301 至 SPA 或输出同模板壳；保留 handlePlans JSON API。断言：uat 断言两端 HTML 一致性或删除后端版后 `/pricing` 200。

### P2-7 告警规则仅 5 条 —— ✅ 证实
**证据**：`deploy/observability/alertmanager/alerts.yml` 实测 5 条（熔断、LLM 错误率、余额、实例存活、goroutine 泄漏）；「5+1」的第 6 条未在仓库找见。未覆盖：翻译失败率、队列积压、pdf 零命中（P0-7 指标）、distlock 错误（P1-4）。
**方案**：随 P0-4/P0-7/P1-4 落指标同步补 4 条规则，目标 ≥9 条。

### P2-8 assist 子服务存储 SQLite-only —— ✅ 证实（已知约束，注释自认）
**证据**：`internal/assist/store/store.go:14` 仅 `modernc.org/sqlite`，`:30` 直开 SQLite 文件，`:331-332` 注释明示「主服务切 PG 时 ReadMainDBConfig 不可用，需 env ASSIST_ADMIN_TOKEN」。生产（PG）部署下 Token 桥接已实际失效，靠 env 兜底可跑但「管理台免手填」卖点掉线。
**方案**：短期部署侧固化 `ASSIST_ADMIN_TOKEN`（写入《部署指南》核对清单）；中期 assist 存储接 `internal/db` 双方言（工作量~1 天，含 ListSessions/History 查询改写）。

---

## 四、实施顺序建议（未动代码，待授权）

| 批次 | 内容 | 理由 |
|---|---|---|
| 批次 1（安全，半天） | P0-1 sid+鉴权、P0-2 kb-stats 登录、P1-5 裸 SQL（升急） | 三处均为「线上即可被利用/已静默失效」；P1-5 在 PG 生产是现存功能性 bug |
| 批次 2（质量闸门，1 天） | P0-7 零命中报错、P0-6 CI 补 Python 自检、P0-4 suspect 指标 | 三者合起来让「产物正确性」在 CI 与生产同时可见 |
| 批次 3（透明度，1 天） | P0-5 旁路落库+披露、P1-4 distlock 保守降级、P2-7 补告警 | 观测/披露类，依赖批次 2 指标命名 |
| 批次 4（商业闭环） | P1-1 渠道前置校验、P1-3 留资、P1-2 随收款自动化 | 商业化试运营计划内项 |
| 批次 5（还旧债） | P2-4/5 e2e 五红清零+TDesign 收尾、P2-6 pricing 归一、P2-2/3 结构治理 | 发布闸门恢复全绿是本批验收线 |

> 每条落地时同步补本文「断言」列所列自动化用例（AGENTS.md 三-3：修一个缺陷必须留一条能复现的断言）。
