# 流式与性能增强方案（Prompt Caching × 流式打满 × 段落虚拟化）

> 成文：2026-09-19 ｜ 性质：调研 + 设计方案 ｜ **状态：B1–B5 已于 2026-09-20 全部实施并过全量闸门**（race/PG UAT A88·T326/E2E 33×/多实例 8/0），代码随 ed14bc9 推送 autosales；部署另行按发布链执行。文中「未实施」为成文时口径，保留作设计依据。
> 缘起：用户提出三个增强方向的研究诉求，并指出核心痛点——「整套流程走下来时间比较久，前端流式响应比较慢、不细节，容易给人等待焦虑」。
> 本文所有现状结论均来自对当前代码的逐行核查（文件:行号可复核），不是推测。

---

## 〇、一页结论（先看这个）

| 方向 | 一句话结论 | 建议 |
|---|---|---|
| **流式打满（双态渲染）** | 后端 token 级 delta 一直在推，但前端 `MessageBubble.tsx:201-204` 的三态互斥导致**逐字文本被进度条完全遮蔽**——现在的"流式"用户根本看不见。这是感知等待的第一元凶，修复成本最低、收益最大 | **第一优先，纯前端 1~2 天** |
| **文件翻译逐段上屏** | 「批级/语言级可行、段级终稿不可行」：初翻按 15 段/批调用，审校是整语言一次调用，且上屏后还有品牌归一/敏感词/闸门三道覆写——必须设计成 draft/final 双事件 | 第二优先，前后端 3~5 天 |
| **Prompt Caching** | **反转结论：当前 prompt 里不存在长且稳定的前缀，直接开缓存命中率≈0**；且主力模型是 siliconflow 上的 Hunyuan-MT-7B（官方端点对缓存支持存疑）。先做埋点与"前缀重组"，再谈接入 | 先 Phase 0 埋点，接入缓行 |
| **react-virtuoso 编辑器虚拟化** | `EditorPage` 504/578 段是实测常态且无任何分页/memo/上限；但**先做计算收敛（每击键重排全表 + 每段重编译 200 词正则），再虚拟化**，否则收益减半 | 第二~三优先，2~4 天 |

**实施顺序建议：A1 流式双态 → A2 逐段上屏（SSE 通道先行）+ 编辑器计算收敛 → B 缓存埋点与前缀重组 → C 虚拟化 →（可选）任务队列通道的段落中间态。**

---

## 一、现状事实清单（调研证据，均已核到行号）

### 1.1 后端流式链路（backend-go）

- SSE 骨架已完备：`api/stream.go` 有 progress/delta/done/error 四类事件、20s 心跳（`sseHeartbeat` :69）、写序列化锁（D20）、空闲超时护栏在前端侧（`translate.ts:23-32`）。
- **token 级 delta 只覆盖一条路径**：`WithStreamSink` 只在 `handleChatStream` 注入（stream.go:164）；文件流 `/api/translate/stream` 无 sink、`BatchTranslate` 走非流式 `CallChat`（engine.go:1816）。
- **三种情况连文本路径都没有 delta**：① Hunyuan 模型（官方端点不支持流式）② 熔断打开 ③ 流式失败透明回退非流式（engine.go:1517-1520；hedge.go:229）。**UI 双态设计必须兼容"无 delta、只有 progress→done"的降级形态。**
- delta 是模型原始输出：含 `<t>…</t>` 输出契约标签与标签外注释，剥标签的 `PostProcessTranslation` 只在整段返回后才跑（postprocess.go:478-486）。**前端 draft 层必须自带增量标签剥离，否则会露出 `<t>`。**
- 审校 pass 走非流式（engine.go:994），**不会二次推 delta**——draft 上屏后到 done 之间是"审校静默期"，需要"审校中"状态位表达。
- 多目标语言（≤3 路并发）delta 按 `lang` 字段区分（stream.go:162-167），前端目前只在单目标时消费（useChat.tsx:167,172）。
- 流式与 hedge 互斥（hedge.go:235）：为逐段 delta 开流式会牺牲对冲竞速，尾延迟可能变差——文件路径不宜直接复用 token 流，段级事件才是正解。

### 1.2 前端渲染链路（frontend-react）

React 18.3.1 + zustand 5.0.15 + vite 5.4；无任何虚拟列表依赖。

- `useChat.tsx:174` 每 token 一次 `set`，**渲染层零节流**；`useChat.tsx:330` 整 store 无 selector 订阅 → 任一字段变化全工作台重渲染；`MessageBubble` 无 memo（全仓 `React.memo` 0 处）。
- `MessageBubble.tsx:204` 每 token 全串重跑 `renderMarkdown`（20 次正则替换）→ 单条消息代价 O(n²)。
- `ChatWindow.tsx:111-113` 每 token 读一次 `scrollHeight` 强制同步布局。
- ★ 阻塞性缺陷：`MessageBubble.tsx:198-204` `showProgress` 与 `showMarkdown` 互斥，流式期间 `progress` 恒在 → **delta 写进 content 也不渲染**。"逐字流式"名存实亡。
- 可复用资产：`StatusPill`（初译中/审校中/终稿三态徽章）、`Skeleton`、`@keyframes lc-mo-caret`（已存在零引用，补一行 class 即得流式光标）、`lc-mo-peak`（终稿替换提亮）、`MessageBubble.tsx:131-168` 检查点编排骨架（类名断链待接）、`useReveal` 的 IO+jsdom 降级范式。
- 无障碍先例：motion.css 的 reduced-motion 整块兜底 + `AiRegisterFlow.typeText` 的「reduced 直出终态」语义——新双态动效必须照此加分支。

### 1.3 工单编辑器（EditorPage.tsx）

- 全仓唯一逐段渲染处：`EditorPage.tsx:217-273` 纯 `segments.map`，每段 textarea+select+批注 input（约 13 静态节点 + 术语高亮动态节点）。
- 段落量级：后端注释实测同一工单 504 源段/578 译段（api/editor.go:236-238）；前后端均无分页、无 LIMIT、无截断（store/segments.go:82-92）。
- 现存放大器（比 DOM 数量更致命）：① 一次击键写整个 rows map → 全表重渲染（:137-138）；② `highlightTerms` 每段每次渲染重编译 200 词正则（:29-37）；③ `dirtyEdits` 每次全量遍历（:141-150）；④ 保存后整表重拉（:170）。
- 「还原排版文件」无前端预览（纯 blob 下载，CSP `object-src 'none'` 阻断内嵌预览）→ 虚拟化不必考虑预览层。

### 1.4 Prompt 组装现状（决定缓存能不能做）

- 主装配 `singleLangRaw`（engine.go:1296-1363）：消息结构为 `system(术语ref + 固定提示句 + 文化块) + user(指令 + 待译文本)`。
- **★ 反转事实：术语/角色/行业包不是 prompt 文本块，而是检索可见域（PackScope）**——进 prompt 的只有"本次源文命中的 ≤5 条术语行"（engine.go:1315），内容逐请求变，却被放在 **system 最前面**，恰好污染了缓存前缀头部。
- 批量路径（文件翻译主负载，engine.go:1794-1802）：**全仓唯一稳定的前缀**（同语言所有批次共享"指令+编号契约"头部），但太短（约一两百 token，低于主流厂商 1024-token 缓存门槛），且无术语/文化注入。
- 五个"前缀杀手"（接入缓存前必须拆）：① ref 占住 system 头部（engine.go:1353）；② 文化规则 SQL 无 ORDER BY，60s TTL 重载后行序抖动（engine.go:2216-2222）；③ 术语行按条数编号 1./2./3.，少一条全变（engine.go:1114-1121）；④ 有/无 KB 命中两套 system 形态互不为前缀（engine.go:1347-1363）；⑤ DynamicRouting/降级链/hedge 换供应商 → 缓存按厂商天然分片（engine.go:1179、1428-1452，hedge.go:280-287）。
- 供应商现实：主链路 = siliconflow `tencent/Hunyuan-MT-7B`（不走流式、不走 hedge）+ GLM-4-9B fallback（config.go:245-253）；计量归组识别 siliconflow/智谱/openai/火山/通义（engine.go:1526-1540）。**Hunyuan-MT 这类部署在第三方的小模型，端点是否有前缀缓存基本要实测说话。**
- 现成模板：请求体是 `map[string]interface{}`（client.go:443），`x_term_constraints`（H2 约束解码）已示范"按能力位附加厂商私有字段"（client.go:458-460、SupportsConstraints 门控）——加 `supports_cache` 能力位与之同构。
- 度量缺口：`Usage` 只读 prompt/completion_tokens（client.go:343-346），**没读 `prompt_tokens_details.cached_tokens`**；无 TTFT、无 LLM 时延直方图；路由 P50/P95 样本已有（hedge.go:37-73）但只走 `/api/admin/ops/routes` 不进 /metrics。

---

## 二、方案 A：打满流式响应（双态渲染 + 逐段上屏）

### A1（第一优先）聊天双态渲染：draft 上屏 → 审校后替换

**目标**：把用户感知等待从"全程黑盒进度条"压缩到"首 token 延迟"，等待期有内容可读、有状态可信。

改动全部在前端（后端零改动），分四步：

1. **解除遮蔽（阻塞缺陷修复）**
   `MessageBubble.tsx:198-204` 三态互斥改为共存：`content && progress` 时渲染 draft 正文 + 进度降级为顶部细丝/角标（`StatusPill` 复用：初译中/审校中/终稿）。补一条 `*.dom.test.tsx` 锁死"progress 与 content 共存"防回归（AGENTS.md 改动原则：缺陷必须带断言）。
2. **draft 增量清洗**
   delta 累积器按语言分桶（去掉 `onlyLang` 限制，多目标语言按 lang 各自渲染行），对累积文本做增量 `<t>…</t>` 剥离（与 postprocess.go:200-207 同一正则口径，注释标明同源），尾部半截标签不渲染；标签外注释（`（：1. …）` 模式）按已闭合段落截断展示。
3. **渲染合帧与订阅收敛（性能四件套）**
   ① delta 写 `useRef` 缓冲，`requestAnimationFrame`（或 ~50ms setTimeout）合帧后一次 `patchMsg`；② `useChat.tsx:330` 拆 selector（messages 与 flags 分开订阅）；③ `MessageBubble` 加 memo + 流式期间跳过 `renderMarkdown`（draft 用纯文本 + `white-space:pre-wrap`，done 后一次性走 Markdown）；④ 滚动 effect 改 rAF 内读 `scrollHeight`。
   ⚠ 流式双态字段（`draft`/`stage`）不落盘：`serializeForPersist`（lib/chatStorage.ts:39）现在只剥 progress，需同步剥 draft，防污染历史。
4. **等待感设计（降低焦虑的细节）**
   - `@keyframes lc-mo-caret` 补一个 `.lc-mo-caret` class → 打字光标；
   - 审校静默期（delta 停、done 未到）切 `lc-mo-bar` 不定量细丝 + 「审校中」StatusPill——这段是真实存在的等待，现在的 UI 会误以为卡死；
   - 终稿替换瞬间接回 `MessageBubble.tsx:131-168` 已有编排骨架（补 `.cp-light` 节点与 CSS 类名，`lc-mo-peak` 提亮一次）；
   - 无 delta 降级形态（Hunyuan/熔断/回落）：保持现有量尺，但把"预计剩余"从假百分比改为"已完成 N 段/术语命中 M 个"的**事实计数**；
   - 全部动效走 motion.css reduced-motion 兜底（直出终态，语义不变）。

**验收**：单目标语言逐字可见；多目标语言各行独立落地；断言测试覆盖 draft 共存、标签剥离、合帧（>2 token 帧合并为 1 次 set）；reduced-motion 直出终态。

### A2（第二优先）文件翻译逐段上屏：segment_done 双事件

**可行性边界（调研硬结论）**：段级终稿不可行（审校整语言一次调用 + 上屏后还有品牌归一/敏感词/闸门三道覆写 + 漏译率硬闸可回收整单），**批级/draft 级可行**。落地形态：

```
segment_done { lang, index, source, draft, stage:"initial" }   ← 初翻每批（15 段）完成即推
segment_final { lang, index, target, stage:"reviewed|gated" }  ← 该语言审校+闸门覆写完成后推
segments_sealed { lang }                                        ← 该语言不再变化
```

**后端改动点（engine/file.go + api/stream.go）**：
1. `handleTranslateFileStream` 增加事件通道：`HandleFile` 加一个可选 `emit func(kind string, payload any)` 参数（与 prog 同闭包风格，写 SSE 走既有 sseMu 锁）。
2. 三个 emit 挂载点：KB 直译（file.go:447 处 `addTrans` 后立即推，替代 :460-471 的攒齐屏障）、批回调（engine.go:1865-1867 升级为携带本 chunk 的 index+译文，调用点 file.go:482/520/603）、语言收尾（wg.Wait() file.go:538 / 闸门覆写全部完成后推该语言 segment_final）。
3. 段身份稳定键：引擎内是"源文串→译文"映射、无 index（extract.go 全局去重）——**事件必须带 `source_hash` + 首次出现的 seg 序号**，前端对号入座以此为准；不能用 percent（progress 的 done/total 三种混合语义、非单调，file.go 调研 §2.2）。
4. 载荷 schema 不发明：对齐 `EditorSegment{index,source,target,edited_text,status,note}`（api/editor.go:31-38）。
5. 计费一致性：SSE 文件 handler 现在不调 `billing.Flush`（对比非流式 stream.go:516）——segments_sealed/error 分支各补一次 Flush；事件字段命名走积分口径（对外 `cost_points`，禁 token 裸值，AGENTS.md 约定 5 + 既定积分决策）。
6. 敏感词占位段（gates.go:155）带 `placeholder:true` 标记，前端不得当译文展示/编辑。

**前端改动点**：
1. `consumeSSEStream` 增加 `segment_done/segment_final/segments_sealed` 分支（translate.ts 纯函数扩展，同文件补 node 环境单测——喂假 reader 序列即可）。
2. 聊天文件气泡：流式期间渲染"已到段落列表 + 骨架占位"（Skeleton 锁高），done 后切下载卡；
3. 工单详情/编辑器：`segment_done` 实时填充行（此时行状态 draft 黄），`segment_final` 替换并清状态；编辑器保存按钮在 sealed 前置灰。
4. **队列路径（/openapi/v1/tasks、工单 worker）本期不动**：任务模式要同等体验需段落中间态落库 + 轮询接口暴露增量，工作量明显大于 SSE 侧，放二期；API 文档注明"SSE 通道有实时段落、任务通道完成后可得"即可。

**风险披露**：已上屏 draft 在整单失败（漏译率硬闸 file.go:646-651、写回降级 :814-825）时被 error 事件收尾——前端必须把 draft 区标为"未完成，非交付物"，不做假成功。

### A3（伴随项）真实延迟度量先行

任何"变快"的声称都要有数：
- `Usage` 增读 `prompt_tokens_details.cached_tokens`（client.go:343-349）；
- `StreamChat` 首次 `onDelta` 处打 TTFT 点（client.go:668-674 现无计时），写入路由统计（hedge.go:45-46 扩展 `ttftMs/cachedTok`）→ 自动进 `/api/admin/ops/routes`；
- `/metrics` 新增 `translator_llm_seconds_bucket`（或复用 4096 环采样导出 p50/p95）、`translator_llm_cached_tokens_total`、`translator_llm_prompt_tokens_total`（metrics.go:238-248 同段）；
- 部署侧验证 SSE 缓冲：确认 Cloudflare 边缘与源站 Caddy 对 `text/event-stream` 无攒块（X-Accel-Buffering 头已设，需实测 delta 帧到浏览器是否均匀——"流式不细节"可能一半是代理攒缓冲造成）。**验证方法：生产页 DevTools Network → EventStream 看帧间隔，或 curl -N 计时。**

---

## 三、方案 B：模型侧 Prompt Caching

### 3.1 厂商机制速览（口径截至 2026-09，接入前以官网实测为准）

| 厂商 | 机制 | 关键约束 | 对本系统的含义 |
|---|---|---|---|
| DeepSeek | 全自动磁盘 KV 缓存，无需任何参数 | 前缀完全一致才命中；命中输入价大幅下降 | 若路由含 DeepSeek 端点，只需把前缀做稳即免费受益 |
| OpenAI | 自动前缀缓存（≥1024 token 前缀），命中输入价约 5 折 | 无需改请求体；前缀短于门槛=无意义 | 需要"够长的稳定头部"才有资格谈收益 |
| Gemini | 隐式缓存（自动）+ 显式 Context Caching（API 创建 cachedContent，带 TTL 存储费） | 隐式命中不保证 | 同上 |
| 通义/豆包/智谱 | 上下文缓存（隐式自动为主，豆包/通义另有显式） | 各家最小可缓存长度与计费不同 | 计量归组里已识别 volces/aliyun/bigmodel，可分路灰度 |
| Anthropic 式显式断点（cache_control 分块） | 请求体 content 需数组分块 | 本项目 `messages` 类型是 `[]map[string]string`（client.go:449），改造波及 20+ 调用点 | **不做**；等值收益用隐式路线即可拿到 |

**现实约束**：当前主负载跑在 siliconflow 的 Hunyuan-MT-7B——第三方托管的开源小模型部署，是否有前缀缓存/命中折扣完全未知（其非流式、小上下文，本来就偏"快而廉"路线）。**所以 B 的正确顺序不是"接缓存"，而是"先让 prompt 具备可缓存的前缀 + 装上度量"，接入按路由能力位灰度。**

### 3.2 分阶段路线

**Phase 0 · 度量（并入 A3，半天）**：`cached_tokens` 解析 + 按路由记缓存命中率/TTFT。没有这一步，后面全部不可验证。

**Phase 1 · 前缀重组（缓存前置工程，1~2 天，本身也改善质量一致性）**
1. `singleLangRaw` 消息结构重排为三段前缀族：`system = 固定翻译规范头（指令骨架+输出契约+角色/行业层的"固定表述"）+ 文化块(排序稳定) → user = 本次检索术语 ref（移到待译文本紧邻前）+ 待译文本`。即把 §1.4 杀手①④拆掉：per-request 检索结果从 system 头部下移到 user 尾部。
   - 副作用红利：无 KB 命中 / 有命中两种形态共享同一 system 前缀（杀手④消除）。
2. 文化规则查询补 `ORDER BY id`（engine.go:2216）；术语行去编号或按 `source_text` 字典序定序（killer③）。
3. 批量路径（file 主负载）把"指令+编号契约"扩成"指令+编号契约+**该语言该租户的固定术语层摘要**（取租户可见域内 top-N 高频术语，按 (tenant,lang) 缓存渲染文本，非 per-request 检索）"——同时解决"批量路径没有术语注入"的质量口径不一致（调研发现：单语路径有 system 术语、批量路径完全没有）与"前缀太短"两个问题。
4. `uiLang` 双语种各自成前缀族（现状即如此），路由侧无额外动作。

**Phase 2 · 能力位接入（1 天）**：`ProviderConfig` 加 `SupportsCache bool json:"supports_cache"`（与 SupportsConstraints 同构，config.go:30-32）；按路由注入厂商对应开关字段（若某厂商需要显式参数如 Gemini 的 cachedContent 名——本期只做"自动命中型"隐式缓存，即 DeepSeek/OpenAI/通义/豆包路线，不做需额外 API 的显式对象管理）。
⚠ 同批必修：admin_models.go:157-159 单模型保存路径逐字段手构 ProviderConfig 会静默吞新字段（连 SupportsConstraints 都被丢过），改结构体透传。

**Phase 3 · 灰度与解耦实验**：缓存收益评估只在 `DYNAMIC_ROUTING=0 + HEDGE_ENABLED=0` 的静态路由窗格做（否则选路抖动让命中率数据不可解释）；以「cached/prompt 比、TTFT p50/p95、账单折算」三数定去留。
**注意**：缓存按 `base|model` 分片——降级链换模型即换缓存池，fallback 频率高的租户命中天然低，属预期内，不算回归。

### 3.3 ROI 粗算框架（上线后用真实数替换）

收益 ≈ Σ(命中请求 × prompt_tokens × 折扣 × 命中率) + TTFT 下降的感知改善。文件批量路径是"同前缀 × N 批"的复读机形态，若 Phase 1 把前缀做到 ≥1024 token 且路由含支持自动缓存的端点，理论命中率上限 ≈ (批数-1)/批数。**成本侧要盯**：显式缓存型厂商（Gemini）存储计费，批量场景可能倒挂——所以只做隐式。

---

## 四、方案 C：工单编辑器性能（计算收敛 + react-virtuoso）

### 4.1 施工顺序（先算后虚，勿倒置）

**C0 · 计算收敛（先做，1~2 天，独立可发布）**
1. `highlightTerms` 正则提到组件外 useMemo（按 terms 数组身份缓存一个编译好的 RegExp + 合并词分支），消灭"每段每渲染重编译 200 词交替式"。
2. `rows` 更新改 per-segment：编辑只 patch 对应行（把 textarea 改非受控 + `defaultValue` + onBlur 提交，或引入 `rowsVersion` 局部订阅），消灭"一次击键重排全表"。
3. 段行组件 `memo` 化（props 收窄到本段数据 + 稳定回调）；`dirtyEdits` 改增量脏集合（编辑时 add、保存后 clear）。
4. 高亮词数封顶：正则参与词与 chip 同口径截断（现在前端 chip 只渲染 30、高亮却 200 词全参与，EditorPage.tsx:211 vs :29-46）。

**C1 · 虚拟化（C0 落地后再上，1~2 天）**
- 选型：**react-virtuoso**（~14kB gzip，动态高度测量成熟，`Virtuoso` 组件式 API 对 map 改造最小；React 18 兼容已确认版本约束面无障碍）。备选 `@tanstack/react-virtual` 更轻但动态高度+滚动锚定需自研，textarea 场景工作量更大——不取。
- 改造：`segments.map` → `Virtuoso data={segments} itemContent={...}`；行高不固定（textarea auto-height + 术语多行），用 virtuoso 的 dynamic height + `initialTopMostItemIndex`；**跳转定位**（按段号定位/从工单详情带 `#seg-N` 进入）是主要工作量；
- 搜索/过滤（若有词位跳转）保持全量索引，只虚拟渲染窗口；
- 测试：vitest jsdom 无 `ResizeObserver`，需按 `useReveal.dom.test.tsx` 桩类手法补 `vi.stubGlobal('ResizeObserver', …)`——写进方案避免将来测试现场抓瞎。
- 后端配套（小改）：`handleTicketSegments` 无 LIMIT 是隐患但**本期不动接口分页**（编辑器全量段落数据本身用于 dirty 比对与术语高亮，分页改造牵连大），加一个响应体大小告警即可；真正的兜底应放在建单分段上限或按语言分页，单列后续评估。

### 4.2 与 A2 的合流

A2 的逐段上屏与 C0/C1 是同一个 `EditorPage`：`segment_done` 事件写入 rows 时走 per-segment patch 路径（C0 成果），新段入场用 `lc-mo-up` 错峰动画（复用 motion.css，虚拟化窗口内才可见，天然不卡）。**先 C0 后 A2 的工单侧接入，或同批做。**

---

## 五、批次拆分与工作量（建议执行序）

| 批次 | 内容 | 面 | 估算 | 发布闸门要点 |
|---|---|---|---|---|
| **B1** | A1 流式双态（含阻塞缺陷 + 性能四件套 + 光标/审校态）+ A3 度量后端半边（cached_tokens/TTFT 打点） | 前端为主 + llm/hedge/metrics 小改 | 2 天 | tsc/vitest/build；新增 2~3 个 dom 测试；/metrics 新指标断言进 deploy_check |
| **B2** | C0 编辑器计算收敛 | 前端 EditorPage | 1~2 天 | 需补一个"编辑不重排全表"的行为断言 |
| **B3** | A2 SSE 逐段上屏（后端双事件 + 聊天气泡 + 编辑器实时填充） | 前后端 | 3~5 天 | run_uat PG（计费触界）+ 积分口径出参断言 + multi_instance_e2e |
| **B4** | B Phase 1 前缀重组 + 文化块排序 + 批量术语层 | 引擎 prompt | 1~2 天 | UAT 质量回归（术语注入位移可能改变命中行为，需对比样例集） |
| **B5** | C1 react-virtuoso + B Phase 2/3 缓存能力位与灰度 | 前端 / 配置 | 各 1~2 天 | 缓存实验走静态路由窗格 |

依赖：B3 依赖 B1（同文件消费路径）与 B2（同组件）；B4 是 B5 缓存位的硬前置。

## 六、红线与约束（AGENTS.md + 既定决策）

1. 所有新对外字段/事件走积分词汇表（`cost_points` 等），零 token 裸值、零汇率（2026-09-19 积分口径决策）；`cached_tokens` 只进内部路由统计与 /metrics，不进任何用户面响应。
2. 新增代码全量中文注释（文件头职责 + 关键逻辑）；i18n 中英同步（流式状态文案进 `dicts.*` 或对应 panel）。
3. SQL 双方言（文化块 ORDER BY、段落查询兼容 SQLite/PG）。
4. 测试断言 `grep -E`；e2e 不写死生产域名；SSE 边缘缓冲验证放 deploy/ 冒烟脚本职责。
5. 提交/推送/部署按既定 gating：每批单独请示，docs 本地不推。

## 参考来源

- DeepSeek 上下文磁盘缓存官方文档：https://api-docs.deepseek.com/zh-cn/guides/kv_cache/
- DeepSeek 缓存降价公告：https://api-docs.deepseek.com/zh-cn/news/news0802/
- Gemini Context Caching 官方文档：https://ai.google.dev/gemini-api/docs/caching
- Google ML Kit 前缀缓存实践：https://developers.google.com/ml-kit/genai/prompt/android/prefix-caching
- 第三方 DeepSeek KV Cache 成本分析（参考观点，非权威）：https://www.iceyao.com.cn/2026/08/20/deepseek-disk-cache-deep-dive/
