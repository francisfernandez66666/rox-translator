# 组织与用户权限关系（生产环境快照）

> 快照：2026-08-22 ｜ 清理：2026-08-26（移除已解决项）｜ 数据整理备份：`backups/pre_iam_20260822_212846.db`
> 注：此为时点快照，实时组织树以管理后台「组织」面板为准。

---

## 一、租户（tenants 表）— 整理后

| id | code | 名称 | 状态 | 说明 |
|----|------|------|------|------|
| 1 | rox | ROX极石汽车 | active | 正式租户 |
| 5 | test | 测试租户 | active | 新建（试用额度：500 句 + 日限 2 万字符） |

## 二、账号清单 — 整理后

### 平台级（tenant_id=0）

| id | 用户名 | 角色 | 状态 |
|----|--------|------|------|
| 1 | admin | admin（旧名，等同超管） | active |

> 平台级停用的 liudi(id=9) **已删除**（两个 liudi 合并为租户1 的 id=4 一个）。

### 租户 1「ROX极石汽车」

| id | 用户名 | 角色 | 组织归属 |
|----|--------|------|----------|
| 4 | liudi | tenant_admin | **ROX极石汽车根组织 (org 2)** ✅已挂 |
| 7 | yutong.zhang | user | **ROX极石汽车根组织 (org 2)** ✅已挂 |
| 10 | liuxu | dept_admin | 信息系统部 (org 7) |

### 租户 5「测试租户」

| id | 用户名 | 角色 |
|----|--------|------|
| 2 | user01 | user |
| 3 | taadmin | tenant_admin |

## 三、组织树

```
平台（tenant 0）
└─ 能言 (root, org 9)

ROX极石汽车（tenant 1）
├─ ROX极石汽车 (root, org 2)   ← liudi、yutong.zhang
├─ 信息系统部 (org, org 7)      ← liuxu（部门管理员）
│   └─ 数字化营销 (dept, org 8)

测试租户（tenant 5, 新建）
```

## 四、唯一性约束（新增强制）

- ✅ 生产库已建唯一索引 `idx_users_tid_username ON users(tenant_id, username)`——同租户下不允许重名
- ✅ 代码层 `CreateUser` 命中约束返回友好提示「同租户下用户名已存在」；启动时容错建索引（存量重名只记日志不阻断）
- 跨租户仍允许同名（如未来测试租户再建一个 liudi 不受影响）

## 五、遗留异常

（无——原三项均已处理：admin 改名已完成、测试账号已整理、展示问题随四期面板重构关闭）

## 六、服务状态

→ 实时状态看 `/status` 与告警面板；内存护栏与自检探活配置见《部署指南.md》§八-B。
